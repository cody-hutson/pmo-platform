<!-- repo-integrity: allow-issue-ref -->
<!-- reference-durability: allow-link -->
<!-- reference-durability: allow-url -->
# RELEASE_LOG_ARCHIVE-version-less

Archive segment of [`RELEASE_LOG.md`](RELEASE_LOG.md) — the **version-less** release family.

This file is the same record as its parent ledger, relocated. It inherits its
parent's records class under `core/governance/RECORDS_POLICY.md`: a segment is a
disposition *destination*, never itself a disposition *source*. Nothing here is
a lesser record for having aged out of the working set, and nothing here is
eligible for a disposition its parent is not.

It lives in the same directory as its parent deliberately, so a recursive grep
over that directory still finds this content exactly as it did before the move.
Each block below retains its heading in the parent ledger, with a pointer here,
so every anchor into the parent still resolves.

Blocks are appended by `release/tools/sweep-release-corpus.py`, and this file is
never itself swept. Existing blocks are not rewritten, with one named exception:
the Stage-13 close-out injects an `**Outcome:**` field in place when a retro
`--outcome` names a release whose body already lives here. That injection is
idempotent, adds a single field to a single block, and removes nothing.

---
#### Deployment Log governance-ci-gates
**Files deployed:** Single release PR #3799 merge (6-member release — #1632, #1485, #1484, #2656, #1486, #3795 — topology D-C SINGLE; 19 tracked files). The release promotes four load-bearing `deploy.sh --check` verifiers — which ran only at deploy time and so could not block a merge — into **warn-mode pre-merge CI gates**, reconciles one criterion-ID collision, and records the deploy-time enforce-flip decisions. **#1632 (reconcile G3-05 collision):** re-IDs the divergent `G3-05` references across three surfaces (`core/skills/eval-writer/references/playbook-stage-gate.md`, `core/schemas/stage-io-contracts.md`, and the third divergent surface `core/disciplines/operating-model.md`) to the canonical "AC are measurable", and rebuilds `packages/eval-writer.skill`; a corpus-wide `grep -rn "G3-05"` now all-agrees. **#1485 (+#3795) (required-subset CI runner):** factors `_c38_compute_verdict` to top level and adds `cmd_check_required_subset` + `--check-required-subset` (an enumerated allowlist seeded with Check 38, excluding dedicated-mirror checks) with a thin-caller `.github/workflows/deploy-check-ci.yml` + `.github/deploy-check-ci.enforce` (ships `warn`); **#3795** wires the previously-dead close-completeness gate to a thin-caller `.github/workflows/close-completeness.yml` honoring the existing sentinel. **#1484 (Check 32 CI mirror):** factors `_c32_compute_verdict`, adds `cmd_check_release_corpus` + `--check-release-corpus` and a `# gate-efficacy: posture=required` header on Check 32, with a path-filtered `.github/workflows/release-corpus-completeness.yml` + `.enforce` (warn). **#2656 (Check 7 CI mirror):** factors `_c7_compute_verdict`, adds `cmd_check_package_freshness` + `--check-package-freshness`, a `.github/workflows/skill-package-freshness.yml` + `.enforce` (warn), and a new gate criterion **G6-06** (`gate-criteria-spec.md`, schema v2.3→v2.4) plus pipeline-doc notes (`stage-06-engineering.md` / `stage-12-execute.md` / `release-process.md`). **#1486 (enforce-flip decisions):** records the per-check flip-decision ledger in `core/standards/gate-efficacy-standard.md` — the flip mechanism (`resolve_check_mode`) is already in-tree and proven, and **all flips are deferred** because the drain signal is operator-instance / git-ignored (unreadable by a PR agent) and the cohort is still warning at scale. Single-engine discipline (CIAC-2): each `_cNN_compute_verdict` body is shared by the inline `deploy.sh --check` block AND its CI probe — the inline checks keep byte-identical deploy-time verdicts. Engineering Commit 0 landed the release plan `release/releases/plans/governance-ci-gates_RELEASE_PLAN.md`. Corpus close-out (RELEASE_LOG combined-12+13 `VERIFIED` row + INDEX + DIGEST + RELEASE_NOTES + plan `domain_practice` fold-in) via this Stage 13 chore PR.
**Mechanism:** git merge-commit (PR #3799 → main at `18292527e38f0994e397424c31ed34f6d1f74052`), **version-less / theme-named**, git-native. **Version-less per D-Version condition (B)** (milestone title is slug-only) following the `cross-reference-integrity-ci` / `release-version-stamping` / `public-flip-*` precedents — no `vX.Y` assigned, no signed-annotated tag cut, and no GitHub Release; `.version` unchanged. Phase B4 deploy: one skill package rebuilt in-PR — `packages/eval-writer.skill` for the #1632 reference edit (deliberately dogfooding the new #2656 Check 7 package-freshness gate green); no `deploy.sh --deploy` skill-copy invocation (the 19 changed paths are gate tooling + new workflows + `.enforce` sentinels + governance/reference corpus + the rebuilt eval-writer package). The `automated-closeout.sh` single-`--version` path is version-keyed and its `validate_version` (tightened by #1801) rejects the pure-alpha slug, so this Stage 13 close-out was assembled via the documented **Phase-B chore-PR fallback** (the binding is on the output set, not the mechanism).
**Timestamp:** 2026-07-24 (PR #3799 merge 2026-07-24T17:56:38Z UTC; MERGE_SHA `18292527e38f0994e397424c31ed34f6d1f74052`)
**Cycle-Time:** N/A — no gate-outcome/plan-review-go event for governance-ci-gates in the pipeline event log; no deployment-status/deploy-skill or deploy-harness event carried a governance-ci-gates version tag (version-less, git-native release; mechanism: compute-cycle-time.sh)
**Result:** SUCCESS — release PR #3799 merged to main via merge commit (D-C SINGLE; `git revert -m 1` rollback convention); no tag (version-less). CI **40/40 pass**, MERGEABLE/CLEAN; install/onboarding regression gate green. Stage-7 DevTest **PASS** (per-gate fixture detection — each new `--check-*` probe detects its seeded failure fixture, a real gate not a no-op; the `deploy.sh` factoring is byte-identical to before the change). Stage-8 QA **ACCEPT — 24/25 AC PASS + 4 CIAC MET**. Stage-9 Plan Review **GO** (operator-authorized 2026-07-24), 13/13 dimensions PASS; goal-conformance (A7) **DIVERGED-WITH-RATIONALE** — Half A ("checks run as pre-merge CI gates") delivered (4 gates live, warn-mode), Half B ("gates are *required*" + "drained shakedowns flipped to enforce") deferred to the operator-side branch-protection flip per the operator-approved Option A (Stage-4 #3734); a planned deferral, not a miss. Dogfood on the PR's own tree: all four new probes exit 0 (`--check-required-subset` OK · `--check-close-completeness` SKIP/dormant · `--check-package-freshness` OK · `--check-release-corpus` INCOMPLETE → warn-mode, not blocking).
**Outcome:** (finalized at Stage 13 VERIFIED) — 6 members #1632 / #1485 / #1484 / #2656 / #1486 / #3795, all auto-closed at the release-PR merge via its Issue References block (`closes #…`). Post-deploy main exhibits the AFTER state: the release ledger's completeness (Check 32), a skill's package-freshness (Check 7), the hook-registry index / load-bearing `deploy.sh --check` subset (Check 38), and the close-out output-set each have a machine gate reachable at the pull-request boundary — all **warn-mode-initial** (they report, they do not block); the G3-05 criterion-ID collision is drained to a single canonical meaning. **`#1486` flipped 0 checks** (defer-all): the drain evidence is operator-instance / git-ignored data a PR agent cannot read, so no shakedown-drain is verifiable from a fresh checkout — the flip mechanism ships proven, every flip deferred to a tracked one-token follow-up. **`#3795` (close-completeness dead-gate)** folded into #1485 — the dormant `--check-close-completeness` probe now has its thin-caller workflow. `cross-cutting` release class, **MODERATE** reversibility of record (Stage-9 A5) — the whole-release revert is itself a single cheap `git revert -m 1` of the release PR (all-additive workflows + warn-mode sentinels + append-only register rows + behavior-preserving `deploy.sh` factoring, deploy no-op ⇒ nothing to re-sync), the one MODERATE surface being the future operator-side enforcement flip; 30-day Stage 13 outcome window. **Required-flip is a post-close operator step:** flipping any gate to blocking is a one-token `.enforce` sentinel edit + an operator-side branch-protection `required_status_checks` addition, gated on a clean dogfood — the only MODERATE surface, reversible by un-requiring. Out-of-scope discovery filed: **#3804** (the stale G3-04 / G3-06 sibling-ID mis-citations on the same constructs — a separate pre-renumber-rot class with ambiguous canonical mappings, NOT re-mapped here). No tag (version-less); no GitHub Release.

#### Deployment Log governance-ci-checks
**Files deployed:** Governance self-validation bundle — one release PR #3662 (`release/governance-ci-checks → main`, merge-commit `f0d95a58`), 7 issues, 47 files (+3766/−125), **0 `.skill` packages rebuilt — this release touches zero skill sources**. Built in four serialized waves on a single branch (P0 fully-serial, operator-ratified). **Wave A — the `deploy.sh` check cluster.** **#1039** adds **Check 55** (work-hierarchy drift) over the net-new `core/deploy/tools/check-work-hierarchy.py`: an H1 doc-invariant leg (banned parent tier, SSOT-derived vocabulary, citation-guarded) and an H2 backlog leg (epic-under-epic via one paginated GraphQL query). The H1 predicate was **narrowed at build time** from "any token outside the licensed kind vocabulary" to the ADR-049 closed banned-tier set `{Initiative, Roadmap}` after the broad form measured 8 findings / 8 false positives (the KM altitude ladder, the SAFe row of the Layer-2 methodology map, ADR-018's projection, and ADR-049's own lowercase label-mapping title) — flagging the SSOT map for containing methodology tier names is incoherent. **#2219** adds **Check 56** (milestone↔epic membership) over `core/deploy/tools/check-milestone-epic-membership.py`: M1 membership (enforce-capable, override-able) + M2 description↔membership reconciliation (warn-only regardless of resolved mode, per the Stage-5 asymmetric-severity design); placement resolved by **D-A** to `deploy.sh --check` rather than `repo-integrity.yml` (milestone↔epic membership is repo-state, not a PR-diff property). **#2682** widens **Check 16** in place — the single fetch drops `--label improvement` to cover all open intake, invariant I2 gains a `type:epic` + `sub-task` exemption, and the check is **decoupled** onto its own `.mode` file so a shared enforce-flip elsewhere cannot enforce the untested broadened net; `core/specs/label-taxonomy.md` Rule 2 gains a per-type status-label applicability table and Rule 6 is restated as a type exemption, and `release/references/specs/ticket-information-architecture.md` was reconciled in the same commit (widening Check 16 rendered its Invariant-Enforcement table factually false — the identical self-contradicting-doc defect Stage 5 flagged for `label-taxonomy.md`, in a second file the spec's grep missed). **#2106** adds **Check 57** (extraction-contract validity) over `core/deploy/tools/check-extraction-contract.py`, asserting `log "Check N:"` emitters ↔ `# Check N` definition blocks entirely inside `deploy.sh` with **no doc-side marker** — the original enumeration-parity premise died with the commit that resolved its prerequisite by *removing* the enumeration in favour of a derive-from-source pointer, so re-adding a marker would have re-created the duplicate surface that removal eliminated (**D-C**). **Wave B — #2685** extends dead-reference coverage to `core/ADRs/`, `core/deploy/`, repo-root `*.md` and `.github/` (excluding `release/releases/`, already covered recursively), and — beyond either Stage-5 option — factors the previously byte-identical inline `--target-paths` string out of `deploy.sh` Check 14 and `link-check.yml` into **one shared file** read by both via a new `--target-paths-file` flag, making divergence structurally impossible rather than convention-enforced; `release/tools/check-release-links.py` `is_skippable` is broadened to treat all three live release-artifact filename shapes consistently and gains its first `--self-test`, wired as a hard precision probe in CI. **Wave C — #1490 + #3009.** #1490 ships `release/tools/check-adr-durability.py` (+565, 30-case self-test) and an `adr-durability` job on `repo-integrity.yml`: R1 status-enum (leading-token rule), R2 stale-anchor (no hardcoded SHA, no live population count in durable prose), R3 operator-handle. R2 precision was **measured, not asserted** — a naive predicate scored 41 corpus findings with a ~85% false-positive tail; three self-tested structural guards (plural-only nouns plus the exemplar shape, no identifier prefix, closed reference-word prefix set) cut it to 6 genuine. #3009 ships **G-CL9** (the authority — a Gate 13 criterion + self-repair row in `core/schemas/gate-criteria-spec.md`, schema minor-bumped additively, plus the Stage 13 Close narrative in `release/governance/release-process.md` carrying the surgical status-field-and-provenance-only constraint) and **Check 58** (the backstop, structurally never enforce-capable — a dedicated advisory-only emitter with no mode switch, no enforce branch, no issue-count increment, no `resolve_check_mode` call, and exit 0 on findings) over `core/deploy/tools/check-adr-flip.py` (**D-B: both**). **Wave D — DevTest rework** (see Mechanism). **Data change:** commit `8fc664d` flipped **23 ADR `status:` fields Proposed → Accepted** — 23 files, 23 insertions, 23 deletions, every changed line a status line.
**Mechanism:** git merge-commit (PR #3662 → main at `f0d95a58baeb67f7bbabf565259e6c128b79b153`). **Version-less by recorded determination** — D-Version condition **(B)**, milestone title is slug-only: **no tag claimed at Stage 12**, no version slot contended for, the Engineering-Commit-0 version re-verify and the Stage-12 atomic version claim both inapplicable, and **no GitHub Release published** (Surface 1 requires a tag). Follows the `public-flip-install-blockers` / `intake-elicitation-skill` / `domain-aware-stage5-design` / `memory-to-corpus-codification` / `cross-reference-integrity-ci` / `declarative-gating-model` version-less precedents. Phase B4 deploy = **no-op** (zero skill sources touched; no `.skill` rebuild). **Wave D fixed two real defects in work this release introduced, before merge.** Stage-7 **F1 (HIGH)**: Wave B single-sourced the scan *scope* across both callers but left the *allowlist* split — `link-check.yml` passed the tracked list carrying the CHANGELOG skip class while `deploy.sh` Check 14 passed only the operator-instance file, so two callers scanning byte-identical files returned different verdicts and the deploy-time baseline went **0 → 84** while CI stayed at 0; fixed by making `--allowlist` repeatable and UNION in argument order, with Check 14 passing the tracked base first and the instance file on top (measured after: deploy-time **84 → 0**, CI **0 → 0**; four coupled docs reconciled). Stage-7 **F2 (MEDIUM)**: Check 55's documented H2 exemption was **structurally unreachable in both accepted spellings** (`#242 …` swallowed by a blanket comment filter; `242 …` loaded under one key and looked up under another) and its self-test **passed vacuously** — it would still have passed with `load_exemptions` deleted; both axes were fixed (a tight `^#\d+\s+\S` comment carve-out; issue-ref key normalization on load and lookup) and the suite **de-vacuumed by mutation testing** — gutting the loader took the pre-fix suite from 15/15 PASS to a post-fix 18/21 with the three exemption cases correctly killed. Stage-7 **F3**: the fix was applied but **its causal claim did not survive verification** — the predicted `named-not-member` precision jump (12 → 2) did not occur; resolving all 11 refs individually found only **1** explained by closure, **7** belonging to no milestone and **3** to a different milestone, all genuine, so the all-states basis buys ~1 finding, not ~10. Cost was engineered down rather than accepted: fetching membership *through* `milestones(states:OPEN)` instead of scanning all 3232 repo issues is a **12.6× speedup** (16.5s → 1.3s), verified equivalent over the open-milestone population rather than assumed. Self-tests at the final head: work-hierarchy **21/21**, milestone-epic **19/19**, extraction-contract **9/9**, adr-flip **12/12**, adr-durability **30/30**, doc-links **11 fixtures**, release-links PASS. CI **37/37 pass** on the final head. Stage-7 DevTest **PASS-WITH-FINDINGS** (1 HIGH / 3 MEDIUM, all release-introduced findings reworked in Wave D) · Stage-8 QA **ACCEPT-WITH-EXCEPTIONS** (19 of 25 AC MET / 4 PARTIAL / 2 NOT-MET at head `25634bc`; every PARTIAL traces to an approved design decision — warn-mode-first per CIAC-1, the retired-reserved carve-out, D-B's never-enforce-capable posture) · Stage-9 **GO** (operator-authorized, with three recorded decisions: **D-1** approve the 23 ADR flips, **D-2** the live epic-under-epic edge, **D-3** retag #1490 into this milestone). **Scope changed mid-release, in both directions:** #1490 was PULLED to `adr-corpus-conformance` (milestone #286) at Stage-4 plan review, built on this branch at Wave C under an explicit hub instruction (flagged by the spoke, not silently absorbed), then **retagged back into #266 by operator decision at the Stage-9 gate** — the milestone assignment corrected to match where the work landed; #286 now holds only #1488. **Stage-12 gap repaired at Stage 13:** no Stage-12 chore PR landed for this release, so no `DEPLOYED` RELEASE_LOG row existed; this row is authored **directly at `VERIFIED`** via the governed Phase-B chore-PR fallback per `stage-13-close.md § Phase A8` (the `automated-closeout.sh` path is doubly unavailable here — its CLI `validate_version` gate hard-rejects a version-less slug before phase 1, and its preflight would additionally fail on the absent DEPLOYED row and absent tag; the binding is on the output set, not the mechanism).
**Timestamp:** 2026-07-22 21:07 UTC (release merge at `f0d95a58`; no tag — version-less)
**Cycle-Time:** N/A — (mechanism: compute-cycle-time.sh; version-less release carries no `vX.Y` key, so no `gate-outcome/plan-review-go` or `deployment-status/deploy-*` event resolves to it; excluded from the N=3 baseline)
**Velocity:** planned 24 pts / delivered 24 pts (1.00) — class cross-cutting; 7 delivery slices; size mix 1×L + 2×M + 4×S; 47 files changed; allocation 2/6/16 pts (feature/debt/protocol-slack); 0 deferred / 0 cut; 6 non-blocking follow-ups filed (#3706, #3707, #3708, #3709, #3710, #3711). Note the planned figure is *current membership sized*: the Stage-4 plan of record was 20 pts across 6 members, and #1490 (M, 4 pts) was retagged in at Stage 9 (mechanism: compute-release-velocity.sh).
**Result:** SUCCESS — release PR merged to main with CI 37/37 green; all six gate mechanisms plus the ADR durability lint landed and self-tested; the 23-record ADR ratification flip landed under operator authorization at the Stage 9 gate.
**Outcome:** PARTIAL
**Outcome rationale:** Every mechanism in the Release Outcome Statement shipped and is verified, but the statement's Success Indicator — *"a net-new instance fails (or warns, during shakedown) at PR/deploy"* — is satisfied only in its **warn** half. All six gates ship warn-mode-first per CIAC-1, **no `.mode` file is committed**, and every enforce-flip is deferred; #1490's flip is additionally **hard-gated on #1488** (milestone #286, unstarted), so the release enforces nothing today by design. Three substantive residuals stay open and are recorded rather than closed silently: (1) **ADR-035** is held `Proposed` — its ratifying review closed so a flip is due, but §Decision part 4 is superseded-in-part by ADR-038, making the *target value* an operator call rather than a mechanical flip; (2) **ADR-057** is held `Proposed` — the sole record carrying no conditional-ratification wording, classified AMBIGUOUS, outside #3009's class by construction; (3) the **#242 → #1153 epic-under-epic edge is live and confirmed**, leaving #1039's own clean-state criterion (AC-3) unmet — the gate is working, the state is not clean, and the real question is a design one (does the hierarchy model permit sub-epics? if yes the H2 rule is too strict; if no, #242 needs re-parenting). Wave D made the H2 exemption mechanism actually work, so the edge is now genuinely exemptible as a third path. Rated PARTIAL rather than SUCCESS because the capability is built but not yet active, and because two of the audit's own records and one of its own findings remain open by deliberate hold.

#### Deployment Log release-version-stamping
**Files deployed:** No runtime-skill or harness file is deployed by this release's own mechanism — the changed set is **release-tooling + governance/reference-corpus + one regression test + one CI-workflow wiring + the `.version` source file**, with **no `SKILL.md` and no `packages/` touched** (deploy no-op; the only mutated runtime-adjacent surfaces are `core/deploy/deploy.sh` and `release/tools/automated-closeout.sh`, which are scripts the repo ships, not deployed skill mirrors). Single release PR #1693 (`fix/version-stamp-ownership → main`, 8 files) closes #1643 — the `.version` source-of-truth was frozen at `v2.08` inside the `v2.09`/`v2.10` tags, so the `notify-version-skew.sh` SessionStart hook reported a perpetual "update available" that no `git pull`/`update.sh` could clear (the stale value is committed at the source; `update.sh` only re-propagates it per ADR-017). The 3-layer fix: **(L1)** corrects the stale value (now tracks the shipped release line on `main`); **(L2)** makes the `.version` stamp an owned Stage 13 close-out step — a new `phase_bump_version` in `release/tools/automated-closeout.sh` (with dry-run/PR-body wiring and `.version` added to the chore-commit staging list) plus declaring `.version` in the `release/references/pipeline/stage-13-close.md` output set + Phase B mechanism; **(L3)** adds `core/deploy/deploy.sh` **Check 39** asserting `.version` matches the latest reachable release tag (FAILs ≥2 minors behind, tolerates the legitimate 1-version Stage 12→13 window as WARN), with regression coverage in `core/deploy/tests/test_version_stamping.sh` wired into the suite via `.github/workflows/install-tests.yml`, and a `docs/UPDATE.md` clarification that `.version` is release-cut-owned. **Version-less identity:** milestone `release-version-stamping` (Release Class A — Routine) ships version-less per operator decision (Stage 4, 2026-06-20) following the `public-flip-install-blockers` / `intake-elicitation-skill` / `domain-aware-stage5-design` / `memory-to-corpus-codification` / `cross-reference-integrity-ci` precedents — no `vX.Y` assigned to this release, no signed-annotated tag cut, and no GitHub Release. Recorded explicitly: the tag-cut and Surface 1 (GitHub Releases) emit steps are deliberately omitted and their absence is not a close defect (version-less path; approved deviation, not omission). Corpus close-out (RELEASE_LOG `DEPLOYED → VERIFIED` + INDEX + DIGEST + RELEASE_NOTES) via this Stage 13 chore PR — hand-authored because `automated-closeout.sh` is version-keyed and cannot run for a version-less release (the documented Phase-B chore-PR mechanism).
**Mechanism:** git merge (PR #1693 → main), version-less, git-native [no skill deploy]. No skill-package rebuild and no `./core/deploy/deploy.sh --deploy` — the changed set carries no `{core,operations,release}/skills/` or `packages/` paths, so there is nothing to deploy or re-sync (deploy no-op; the merge activates Check 39 and the new regression on main). No tag (version-less).
**Timestamp:** 2026-06-20 UTC (PR #1693 merged 2026-06-21T01:22:51Z UTC at merge commit `1d1c0ec2ed3475be4403f5d606fd5a05f7501e11`; no tag — version-less).
**Cycle-Time:** N/A — version-less release (no deployment-status/deploy-skill or deploy-harness event in the pipeline event log; git-native release); mechanism: compute-cycle-time.sh.
**Result:** SUCCESS — release PR #1693 merged to main via merge commit `1d1c0ec2ed3475be4403f5d606fd5a05f7501e11` (D-C SINGLE; `git revert -m 1` rollback convention); no tag (version-less). Stage 7 Dev Testing PASS and Stage 8 QA ACCEPT — #1643's acceptance criteria verified, including the Check 39 WARN-vs-FAIL boundary calibration (no false-fail against the legitimate 1-version Stage 12→13 window) and the governance-edit-pairs-with-code-edit + new-phase-idempotency checks. **Issue #1643 stays OPEN at merge** (VERIFY-ONLY: PR #1693 used `References #1643` in its Issue References block — `closingIssuesReferences` empty — so the issue is closed manually at Stage 13 with per-AC verification evidence).
**Outcome:** Finalized at Stage 13 VERIFIED. Post-deploy main exhibits the AFTER state: `.version` tracks the shipped release line, `automated-closeout.sh` owns the `.version` bump at close, `stage-13-close.md` declares `.version` in the Stage 13 output set, and `deploy.sh` Check 39 guards against `.version` drifting behind the latest reachable release tag. Release Class **A (Routine)**, CHEAP reversibility / HIGH confidence (single `git revert -m 1` of the release PR; the L2/L3 additions are additive close-out tooling + an additive warn-mode-tolerant check, no data migration, deploy no-op means nothing to re-sync post-revert). **Surface 1 (GitHub Release) N/A** — version-less release (no version integer to tag or publish).

#### Deployment Log parallel-launch-quota-budget-gate
**Files deployed:** No runtime-skill or harness file is deployed by this release's own mechanism — the changed set is **text-only K1 corpus + release-pipeline spec/protocol docs + one release tool**, with **no `SKILL.md`, no `packages/`, and no hook/harness path touched** (deploy no-op, mirrors v1.13). The release (single PR #911, 2-issue `novel` release, D-C SINGLE; sequenced #24 → #23) adds quota-awareness to the parallel-spoke launch path. **#24 (foundation):** authors the canonical `### Per-Account Usage Window Constraint` subsection in `release/references/how-to/hub-spoke-bridge.md § Spoke Launch Mechanisms` plus a Stage-5-row note in `release/references/specs/release-personas.md`. **#23 (active gate):** builds the dual-checkpoint enforcement — a plan-time Checkpoint A in `release/references/pipeline/stage-04-planning.md` and a runtime Checkpoint B in `hub-spoke-bridge.md § Procedure 2` / § Spoke Launch Mechanisms — that references #24's subsection, with the new `release/references/standards/quota-budget-protocol.md` protocol/schema doc, the `release/references/standards/pipeline-event-log-schema.md` telemetry surface (reconciled to the real `payload`-convention surface per the Stage-5 R-8 finding — no fictional `spoke-completion` event), and the `release/tools/append-pipeline-event.sh` emitter. `release/ADRs/ADR-026-spoke-launch-quota-reservation-telemetry-event.md` (+ README index row) records the decision. Engineering Commit 0 landed the release plan `release/releases/plans/_unversioned/parallel-launch-quota-budget-gate_RELEASE_PLAN.md`. The reconciled cutover/exemption clause uses the **no-version-token introducing-release / SHA form** (the markerless idiom — "applies to releases entering the pipeline on or after this constraint's introducing-release merge SHA; the introducing release itself is exempt"), so the reference-durability Class-V version-apparatus detector does not match it and no `allow-version-ref` marker was needed. Corpus close-out (RELEASE_NOTES + LOG `DEPLOYED → VERIFIED` + INDEX + DIGEST + CHANGELOG) lands at the Stage 13 chore PR.
**Mechanism:** git merge (PR #911 → main, `--merge`) + signed-annotated tag push (Tag-SHA-Direct pattern per stage-12-execute.md § Phase B; the version-less `parallel-launch-quota-budget-gate` tag points at the merge SHA `b2b5f69873075de6f9516e3f1d053a7829416114`; ED25519 signature valid — `git tag -v parallel-launch-quota-budget-gate` reports a Good signature; zero signing bypasses). No skill-package rebuild and no `./deploy.sh --deploy` — the changed set carries no `skills/`, `packages/`, or harness paths, so there is nothing to deploy or re-sync (deploy no-op). **Version-less identity:** authored in-pipeline as `v1.15`; `v1.15`/`v1.16`/`v1.17` (and subsequently `v1.18`/`v1.19`/`v1.20`) were all claimed by concurrent releases, so this release ships **version-less** under the slug identity `parallel-launch-quota-budget-gate` (collision-proof; the #769 version-collision class) — the tag name IS the slug; every load-bearing `v1.15` anchor was reconciled to the slug / introducing-release-SHA form per the release plan Deviation Log item (a).
**Timestamp:** 2026-06-14 15:39 UTC (PR #911 merge; MERGE_SHA `b2b5f69873075de6f9516e3f1d053a7829416114`)
**Cycle-Time:** N/A — no gate-outcome/plan-review-go event for this release in the pipeline event log; no deployment-status/deploy-skill or deploy-harness event recorded (deploy no-op — text-only release; mechanism: compute-cycle-time.sh)
**Result:** SUCCESS — release PR #911 merged to main via merge commit `b2b5f69873075de6f9516e3f1d053a7829416114` (D-C SINGLE; `git revert -m 1` rollback convention); signed-annotated version-less `parallel-launch-quota-budget-gate` tag points at the merge SHA; ED25519 signature valid (`git tag -v parallel-launch-quota-budget-gate` → Good signature); zero signing bypasses. Pre-merge: Phase A.6 mergeability MERGEABLE/CLEAN; Phase A.5 main-divergence 2 commits (the concurrent v1.20 Stage 13 close-out — CHANGELOG / RELEASE_DIGEST / RELEASE_INDEX / RELEASE_LOG / v1.20 notes) with ZERO overlap on this release's 9 changed files; Phase B0 zero dependents. The #769 version-collision re-check is moot for this release — it ships version-less (no version integer to claim), so the tag name `parallel-launch-quota-budget-gate` was verified FREE (local + `git ls-remote --tags origin`) before tagging instead. **Issues #23 + #24 stay OPEN at merge** (VERIFY-ONLY: PR #911 used `References #23, #24` in its Issue References block — `closingIssuesReferences` empty — so the two issues are closed manually at Stage 13 with per-AC verification evidence).
**Outcome:** Finalized at Stage 13 VERIFIED (the Stage 13 chore PR transitions this row `DEPLOYED → VERIFIED`). Post-deploy main exhibits the AFTER state: the parallel-spoke launch path is quota-aware — `hub-spoke-bridge.md` carries the canonical Per-Account Usage Window Constraint subsection, the Stage-4 plan-time Checkpoint A and the Procedure-2 / Spoke-Launch runtime Checkpoint B enforce the dual-checkpoint quota gate referencing it, `quota-budget-protocol.md` is the protocol/schema home, and the event-log schema + `append-pipeline-event.sh` carry the reservation/telemetry surface. `novel` release class, CHEAP reversibility (single `git revert -m 1` of the release PR — additive text-only protocol change: the gate is additive over the prior "launch all N" behavior; no data migration, no consumer state to unwind; deploy no-op means nothing to re-sync post-revert), 30-day Stage 13 outcome window. **Surface 1 (GitHub Release) deferred to Stage 13** (requires `release/releases/notes/_unversioned/parallel-launch-quota-budget-gate_RELEASE_NOTES.md`, authored at Stage 13 per convention; this is a purely-additive non-breaking release, so no Stage-9 note-review gate was triggered — per the v1.20 precedent).

#### Deployment Log cross-reference-integrity-ci
**Files deployed:** Single release PR #745 merge (3-issue release, D-C SINGLE; 14 tracked files). The release hardens the platform's PR-time reference-integrity enforcement across three file-disjoint CI surfaces. **#314 (reference-durability positional `#N` parity — DONE):** the position-blind CI existence check is replaced by a true line-position classifier extracted to NEW `core/hooks/lib/positional-issueref.awk` and invoked by both the hook (`core/hooks/block-fragile-refs.sh`) and the `.github/workflows/reference-durability.yml` CI, closing the one divergent shape (a self-describing `#N` in body prose with the reference block BELOW it) where the hook flagged `OUTSIDE-BLOCK` while CI passed — a flip-to-enforce false-negative; the divergence fixture is wired into `core/hooks/run-fragile-ref-fixtures.sh` + `core/hooks/testdata/cutover-fixtures.txt` (25 passed / 0 failed). Per the PR's recorded deviations, the diff-hunk mapper was inlined in the CI step (not shipped as a second lib — no current consumer; scope discipline) and `core/deploy/deploy.sh` was NOT touched. **#169 (link-check PR gate — DONE):** NEW `.github/workflows/link-check.yml` promotes `core/deploy/tools/check-doc-links.py` to a PR-time gate, with `<OPERATOR_INSTANCE_*>` token-class exclusion in the engine + a repo-resident allowlist NEW `core/deploy/allowlists/skip-doc-link-check-ci.txt` (CI has no operator-instance allowlist), and the 13 pre-existing broken refs drained to 0 (baseline 221 → 0 over the #169 scope); `core/rules/doc-link-maintenance.md` + `release/references/standards/release-notes-standard.md` document the PR-time protection. **#130 (forward-looking IMP-XXX / skill-count drift CI — DONE, re-scoped C+D):** NEW `core/deploy/tools/check-skill-count-imp.py` (detector) + NEW `.github/workflows/skill-count-imp-check.yml` (added-lines-delta workflow), new files only (zero edits to existing files) — the absent `imp-references.tsv` made the literal TSV-consumer ACs dead-on-arrival, so the issue was re-scoped to forward CI (no historical artifact needed) with the routing-conflict-detection portion split to its own issue. Engineering Commit 0 landed the release plan `release/releases/plans/_unversioned/cross-reference-integrity-ci_RELEASE_PLAN.md`. Corpus close-out (RELEASE_NOTES + LOG `DEPLOYED → VERIFIED` + INDEX + DIGEST + CHANGELOG) via the Stage 13 chore PR.
**Mechanism:** git merge (PR #745 → main), version-less, git-native [no skill deploy]. **Version-less release per operator authorization (Stage 12 gate, 2026-06-13)** following the `public-flip-install-blockers`, `intake-elicitation-skill`, `domain-aware-stage5-design`, and `memory-to-corpus-codification` precedents — no `vX.Y` assigned, no signed-annotated tag cut, and no GitHub Release; `.version` unchanged. No skill, package, or harness deploy — the PR's 14 changed paths contain zero `{core,operations,release}/skills/`, `packages/`, or `harness/` entries; `./core/deploy/deploy.sh --deploy` ran and reported `No skill, package, or harness changes detected. Nothing to deploy.` (exit 0; git-native release — the merge activates the three CI gates on main). Recorded explicitly: the tag-cut and Surface 1 (GitHub Releases) emit steps are deliberately omitted and their absence is not a close defect (version-less path; approved deviations, not omissions).
**Timestamp:** 2026-06-13 14:54 UTC (PR #745 merge; MERGE_SHA `5ebec77d5997d7bbbc77cc21efd93ce2cdd5c168`)
**Cycle-Time:** N/A — no gate-outcome/plan-review-go event for cross-reference-integrity-ci in the pipeline event log; no deployment-status/deploy-skill or deploy-harness event (git-native release; mechanism: compute-cycle-time.sh, exit 0)
**Result:** SUCCESS — release PR #745 merged to main via merge commit (D-C SINGLE; `git revert -m 1` rollback convention); no tag (version-less). Stage 12 Phase A.5 divergence pre-check: `origin/main` at branch-base `51794f2` unchanged since the Stage 12 gate — zero divergence, zero matrix-path overlap; Phase A.6 mergeability MERGEABLE/CLEAN (21/21 checks SUCCESS, the 19 PR gates + CodeQL/Analyze); Phase B0 zero dependent PRs. Phase H deploy: near-no-op (zero changed skills/packages/harness); `deploy.sh --check --warn` reported only pre-existing warn-mode drift (none release-introduced; the reference-durability detector fixture 23 passed / 0 failed, Check 23 LOG↔INDEX aligned, Check 32 release-corpus completeness OK). Phase J.5 rebuild hygiene: zero rebuilt packages (no skills changed). Primary fast-forwarded to the merge SHA `5ebec77`.
**Outcome:** (finalized at Stage 13 VERIFIED) — 3 issues #314 / #169 / #130, all carried via the release PR's `References #314/#169/#130` block (NOT close-keywords) and therefore held OPEN for Stage 13 manual closure with per-AC verification evidence; all three marked closed (completed) at Stage 13 on the Stage 8 ACCEPT/PASS verdicts (sub-tasks #697 / #698 / #699), the merge SHA `5ebec77`, and the live warn-mode-initial CI gate per issue. Post-deploy main exhibits the AFTER state: the platform's PR-time reference-integrity enforcement is hardened across three file-disjoint CI surfaces — the reference-durability hook and CI now agree byte-for-byte on positional bare-`#N` classification (no flip-to-enforce false-negative), a `link-check.yml` PR gate enforces intra-repo link integrity with `<OPERATOR_INSTANCE_*>` token exclusion and a repo-resident allowlist (broken-ref baseline drained to 0), and a `skill-count-imp-check.yml` PR gate catches forward IMP-XXX references in skill specs and skill-count drift. `novel` release class, CHEAP reversibility (single `git revert -m 1` of the release PR; all deliverables additive CI/tooling; warn-mode-initial means no PR is hard-blocked during shakedown), 30-day Stage 13 outcome window. **Check 14 flip-to-enforce assessment (first release after the link-check merge, per `doc-link-maintenance.md`):** keep all three new gates at warn-mode-initial through the standard 2-3-release shakedown window — they ship green at 0-baseline and have no warn-log history yet; flip-to-enforce deferred with rationale (explicit per the timeline's no-silent-deferral rule). Outbound roadmap edge: issue #130 Blocks #235 (OPEN, milestone #197, Later horizon) — out of this release's execution; the split routing-conflict-detection portion lives as its own follow-up issue #704 (OPEN, off-milestone). Pre-flip-to-enforce hardening (the #130 bounded-recall + #314 cosmetics) filed off-milestone at this close.

#### Deployment Log memory-to-corpus-codification
**Files deployed:** None — zero deliverable repo diff (verification-only release per the approved plan's D-Disposition A). Release PR #604 merge carried the committed release plan only (`release/releases/plans/_unversioned/memory-to-corpus-codification_RELEASE_PLAN.md`, Engineering Commit 0); the release's substance is verification plus an operational deploy on the operator instance. Verification: all nine ticket-targeted patterns confirmed already present in the tracked corpus — the five workspace guardrails in `core/CLAUDE.md.template` (whole-plan approval, authorization-scope enumeration, skill-boundary transparency, surgical edits, harness-tooling governance) and the four git-workflow rules in `core/rules/git-workflow.md` (fast-forward-by-default primary sync, never-restore-uncommitted-deletions, parser-clean PR-body discipline, reviewer-field-expected-empty) — with the per-AC verification record and the line-by-line content-richness diff committed in the release plan. **Operational deploy (the release's only substantive mutation; executed at Stage 12 on the operator instance, outside git):** 5-item manifest — the three superseded memory files (the primary auto-sync, primary working-tree, and PR-body close-keyword feedback memories) trashed AFTER the full-body archive comment landed on the Stage 12 sub-task #602; their three index lines removed from the memory store's index; the release's codification-ledger row evicted per the store's own encode-and-evict contract. Post-state verification 3/3 PASS (file-absence scan → no files; index scan → no matches; ledger-row count → 0; literal commands and outputs in the Stage 12 evidence comment on #602). Corpus close-out (this LOG row — folded from Stage 12 Phase B5 into the Stage 13 chore PR per the approved D-Topology A two-PR shape — plus INDEX + DIGEST + RELEASE_NOTES + CHANGELOG) via this Stage 13 chore PR.
**Mechanism:** git merge (PR #604 → main) + Stage 12 operational deploy on the operator instance (archive-first, then Trash — recoverable). **Version-less release per operator decision (Stage 4 D-Identifier A, 2026-06-09)** following the `public-flip-install-blockers`, `intake-elicitation-skill`, and `domain-aware-stage5-design` precedents — no `vX.Y` assigned, no signed-annotated tag cut, and no GitHub Release; `.version` unchanged. No skill or package deploy (`deploy.sh --deploy` not invoked — zero deliverable diff). Recorded explicitly: the tag-cut, Surface 1 (GitHub Releases) emit, and skill-deploy steps are deliberately omitted and their absence is not a close defect (version-less + zero-diff path; approved deviations, not omissions).
**Timestamp:** 2026-06-10 18:08 UTC (PR #604 merge; MERGE_SHA `5b8eb2df14eec9edd28b3bbcf9d58cb70281e511`)
**Cycle-Time:** N/A — no gate-outcome/plan-review-go event and no deployment-status event for memory-to-corpus-codification in the pipeline event log (verification-only release; mechanism: compute-cycle-time.sh)
**Result:** SUCCESS — release PR #604 merged to main (1 file: the committed plan); CI green; PR #604 MERGEABLE/CLEAN at first poll, zero dependent PRs. Stage 5/7/8 SKIP per the approved stage-applicability matrix (zero behavior-bearing changes); Stage 9 Plan Review GO (operator-rendered 2026-06-10 on the provenance-complete briefing under the Standing-GO Authorization Model; 9/9 plan verification one-liners PASS; readiness scan 12 PASS / 1 PARTIAL accepted as structural residual; G-PR7 ALIGNED; G-PR8 CLEAN). Primary checkout fast-forwarded to the merge SHA (`5b8eb2d`).
**Outcome:** (finalized at Stage 13 VERIFIED) — 2 issues #356 / #357, both marked closed at Stage 13 with reconciled per-AC closure evidence posted to each issue. Post-deploy state exhibits the AFTER: all nine patterns live in the tracked corpus as the single source of truth, and the operator memory store carries none of the nine superseded feedback memories, their index lines, or the codification-ledger row. Provenance (why a no-fix diff was the correct release shape): all nine encodings pre-date this release — two landed in the original repository's lineage (2026-04-18 and 2026-05-12 commits) and seven in the clean-room public-seed build (this repository's first commit, 2026-06-05); the tickets migrated to this repository open and were bundled unverified, caught by the Stage 4 G-PL1 currency gate (the first re-check). `routine` release class, CHEAP reversibility (repo side: git revert of the plan/corpus commits; operator-instance side: restore verbatim from the #602 archive comment), 30-day Stage 13 outcome window. Out-of-scope discovery R-1 (the deploy-managed rules mirror absent at the operator instance) filed as #633 at close entry; #429 remains the owner of the migration-playbook scope through its own Triage.

#### Deployment Log domain-aware-stage5-design
**Files deployed:** 11 files via release PR #503 merge (single-branch, D-C SINGLE; shared-release-PR append model — #1 + #345 + #346 commits accrued to one branch and one PR). The release makes Stage 5/7 design **domain-aware** instead of pmo-markdown-bound, layering three independent mechanisms onto the existing §5.7 `domain_practice` provenance machinery on top of a shared keystone substrate. **Keystone substrate (#1, build-FIRST):** `release/references/pipeline/stage-04-planning.md` §5.7 — a `domain:` class field added to the `domain_practice` label across Mode A, Mode B, AND the pipeline-internal exemption, plus A3-time deliverable-domain classification from the File-Change-Matrix and the "already-encoded" exemption reconciled to reference the new domain guides (sourcing-exempt ≠ classification-exempt). All three issues consume this one field. **#1 (design-exploration protocol + per-domain best-practice guide class):** NEW `release/references/standards/design-exploration.md` (a Stage-5 Phase-A4 micro-protocol inserting divergent generation → convergent narrowing BEFORE the trade-off matrix, 8 sections incl. AC5 worked example + Tier-A process-flow §7 + reflexive cutover §8); NEW `core/standards/domain-best-practices/software.md` + `governance.md` (Applicability-Profile-bearing K1 guides — GoF / ADR-Nygard / Fowler for software with the mandatory ET5 paired contraindication, PMBOK7 / PRINCE2 / Nonaka SECI / Diátaxis for governance with the ET3 paired applicability note); additive wiring of design-exploration + the domain-guide index into `release/references/pipeline/stage-05-solutioning.md` (Phase A4 + §5.7); the Stage-5 chip pattern in `release/references/how-to/hub-spoke-bridge.md` (D-D second landing); and framework-catalog registration in `core/specs/framework-catalog.md` (3 EXTERNAL source rows + 2 INTERNAL guide rows). **#345 (domain-aware impact-analysis selector):** NEW Phase A3.1 domain-aware impact-analysis method selector in `release/references/pipeline/stage-05-solutioning.md` (5-row selector admitting non-markdown fan-out methods + a documented opt-out, markdown `blast-radius.sh` preserved as the DEFAULT — additive, 0 deletions to the A3 mandate) + the code-domain worked example in `release/references/protocols/blast-radius-protocol.md` §12 (grep-reproducible import-graph fan-out → first-order tier) + `release/references/templates/design-review-checklist.md` Section 1 (in-place augmentation admitting non-markdown methods while preserving the markdown DEFAULT). **#346 (domain-best-practice review criterion):** `release/references/templates/design-review-checklist.md` check 4.6 (domain-best-practice conformance against the target domain's authoritative practice, citing the external guides + `applicability-framework.md` §2/§3/§4) + `release/references/pipeline/stage-07-dev-testing.md` Phase C conditional domain-practice conformance dimension + the `DOMAIN-PRACTICE-NOT-ASSESSED` honest flag + the dimension-count cascade fix ("5 always-on + 1 conditional"). Engineering Commit 0 landed the release plan `release/releases/plans/_unversioned/domain-aware-stage5-design_RELEASE_PLAN.md`. Corpus close-out (RELEASE_NOTES + LOG row + INDEX + DIGEST + CHANGELOG) via this Stage 13 chore PR.
**Mechanism:** git merge (PR #503 → main). **Version-less release per operator decision (Stage 4, 2026-06-07)** following the `intake-elicitation-skill` and `public-flip-install-blockers` precedents — no `vX.Y` assigned, no signed-annotated tag cut, and no GitHub Release; `.version` is unchanged. The slug-named milestone never had a version assigned. Recorded explicitly: version-less — no tag, no GitHub Release. The Surface 1 (GitHub Releases) emit and any tag-cut step are deliberately omitted and their absence is not a close defect (the version-less path).
**Timestamp:** 2026-06-07 19:49 UTC (PR #503 merge; MERGE_SHA `552b33efd5b98011d456eccb527da4e7925a8f14`)
**Result:** SUCCESS — release PR #503 merged to main (14 commits · 11 files · +648/−17). Stage 7 Dev Testing PASS (3/3 issues, 2 fixed iterations — the F-1 release-plan exemplar mismatch and a §12 fabricated worked example both caught and verified). Stage 8 QA PASS — #1 6/6 AC (sub-task #490), #345 3/3 AC (sub-task #491), #346 3/3 AC (sub-task #492); zero FAIL, zero conditional-pass. Stage 9 Plan Review GO (operator-rendered; `novel` → Deep depth) with hub Empirical Verification 8/8 (substrate `domain:` across Mode A/B/exemption · both guides carry the Applicability Profile + ET5 paired contraindication · all 3 consumers read `domain:` · cascade swept with no stale count · PII noreply-clean · PR body parser-clean References-only · catalog primitive `check-version-anchors.py` EXIT 0). No tag (version-less); no GitHub Release.
**Outcome:** ATTAINED — all three issues' acceptance criteria MET against merged main (per-issue evidence in the Stage 13 close-out verification; #1 6/6, #345 3/3, #346 3/3). Post-deploy main exhibits the AFTER state: Stage 5/7 design is now domain-aware — a deliverable's domain is classified once at Planning A3 (the keystone `domain:` field), a governed design-exploration step runs divergent generation → convergent narrowing before the trade-off matrix, the impact-analysis instrument is selected per domain (markdown `blast-radius.sh` default; non-markdown fan-out admitted for code/component/solution domains; documented opt-out), and the design review now assesses conformance to the target domain's authoritative practice with an honest not-assessed flag, all backed by the first two per-domain best-practice guides. `novel` release class, CHEAP reversibility, 30-day Stage 13 outcome window. Deferred follow-ups: #504 (pre-existing `deploy.sh` Check-18 stale catalog path `release/specs/` → `core/specs/` — NOT introduced by this release; catalog verified via the primitive at deploy) + #505 (domain-fan-out impact-analysis instrument — the executable counterpart to #345's spec-level method admission, deferred to a follow-up issue).

#### Deployment Log intake-elicitation-skill
**Files deployed:** 18 files via release PR #424 merge (single-branch, D-C SINGLE) — one new operations skill plus its registration, package, ADR, funnel pointer, and the documentation-roster updates that name it. The new `intake-desk` skill directory: `operations/skills/intake-desk/SKILL.md` plus `references/elicitation-loop.md`, `references/type-map.md`, `references/technique-library.md`, `references/output-contract.md`, and `evals/evals.json`. The architectural-boundary ADR `core/ADRs/ADR-016-intake-front-door-architectural-boundary.md` (intake front door as a distinct component — verb-disjoint boundary vs ppm-agent / project-initiator / architecture, plus the downstream owned-assumption handoff contract) and its `core/ADRs/README.md` index row. The `core/deploy/deploy.sh` registration (one line — `intake-desk` added to the `OPERATIONS_SKILLS` per-module array, NOT the deprecated `SKILL_LIST`; this corrected the issue body's stale registration target) and the built package `packages/intake-desk.skill`. The intake-style-guide funnel pointer `release/references/how-to/intake-style-guide.md` (one link-free durable sentence naming `intake-desk` as the intake funnel). The doc-roster updates that name the new skill: `README.md`, `operations/README.md`, `docs/FIRST_STEPS.md`, `docs/GETTING_STARTED.md`, `docs/module-apis.md`, and `core/diagrams/architecture-platform-structure.md`. Corpus close-out (RELEASE_NOTES + LOG row + INDEX + DIGEST + CHANGELOG) via this Stage 13 chore PR.
**Mechanism:** git merge (PR #424 → main). **Version-less release per operator decision (Stage 4, 2026-06-06)** following the `public-flip-install-blockers` precedent — no `vX.Y` assigned, no signed-annotated tag cut, and no GitHub Release; `.version` stays `v3.19`. Recorded explicitly: version-less — no tag, no GitHub Release. The Surface 1 (GitHub Releases) emit and any tag-cut step are deliberately omitted and their absence is not a close defect (the version-less path).
**Timestamp:** 2026-06-06 19:38 UTC (PR #424 merge)
**Result:** SUCCESS — release PR #424 merged to main with green CI (17/17 checks). Stage 13 verification re-confirmed the deployed skill against merged main: `deploy.sh --check` Check 5 (skill-roster-drift) OK — the `OPERATIONS_SKILLS` array matches the on-disk `intake-desk` dir (23 skills across the per-module arrays); Check 6 (canonical-structure) OK for `intake-desk` (required `version` frontmatter present and well-formed); Check 7 (package-freshness) OK for `intake-desk`. The four pre-existing Check 6 FAILs (`project-initiator` D-Refs threshold; `eval-writer` / `pmo-qa-auditor` / `prompt-builder` missing `version`) are unrelated to this release and predate it. No tag (version-less); no GitHub Release.
**Outcome:** ATTAINED — all five of #412's acceptance criteria MET against merged main (5/5 PASS; per-criterion evidence in the Stage 13 close-out verification). The platform gains a conversational intake front door: `intake-desk` meets an idea at any altitude (run-the-business vs change-the-business, then a specific level proposed as an assumption and confirmed), proposes the correct work-item type and its place in the intake hierarchy and re-routes on reclassification (AC1), elicits the type/level-appropriate field set derived at use time from each `.github/ISSUE_TEMPLATE/<type>.yml` — a bug's reproduction/environment, a story's acceptance criteria/value, an initiative's outcomes/domain (AC2), applies the 5-test rule live as the clarity gate before logging (AC3), emits a logged item to the work tracker after a binary AskUserQuestion confirm and never a tracked scratch file — the originating defect this skill exists to close (AC4), and binds the MVP to the current three-type set (`improvement` / `bug` / `observation`; no `adr`) via a parameterization seam that the work-item type system later repoints with no SKILL.md rewrite (AC5). `novel` release class, CHEAP reversibility, 30-day Stage 13 outcome window. Deferred/out-of-scope follow-ups enriched at Stage 5 rev-2: target-tracker parameterization (#384 / #409 / #383), downstream assumption investigate-and-close (#427), root-cause guidance (#428), migration playbooks (#429).

#### Deployment Log public-flip-install-blockers
**Files deployed:** 25 files via release PR #627 merge (single-branch, D-C SINGLE) — install / deploy / update tooling and tests plus two ADRs and the skill-deployment rule: `install.sh`; `update.sh`; `core/deploy/deploy.sh`; `core/deploy/compose.py`; `core/deploy/lib-composition.sh`; `core/deploy/composition-surface-manifest.sh`; `core/hooks/notify-version-skew.sh`; `core/rules/skill-deployment.md`; `core/settings.json.template`; `core/standards/composition-surface-spec.md`; `docs/scripts/validate-install.sh`; `docs/scripts/setup-workspace.sh`; `docs/INSTALL.md`; `docs/UPDATE.md`; `.version` (reconciled to the v3.x tag scheme per #614); `.github/workflows/install-tests.yml`; `core/ADRs/ADR-013-detect-install-path-session-resolution.md`; `core/ADRs/ADR-014-managed-section-two-hash-tamper-detection.md`; `core/ADRs/README.md`; and the deploy-test suite (`core/deploy/tests/test_compose.py`, `test_deploy_sandbox.sh`, `test_detect_install_path_spaces.sh`, `test_install_end_to_end.sh`, `test_lib_composition.sh`, `test_notify_version_skew.sh`). Corpus close-out (RELEASE_NOTES + LOG row + INDEX + DIGEST + CHANGELOG) via this Stage 13 chore PR.
**Mechanism:** git merge (PR #627 → main). Version-less release per operator decision (Stage 4, 2026-06-03) — no `vX.Y` assigned and no signed-annotated tag cut; `.version` stays `v3.19`. The `#614` `.version` reconciliation is to the independent v3.x tag scheme, not a version for this release.
**Timestamp:** 2026-06-04 05:21 UTC (PR #627 merge)
**Result:** SUCCESS — release PR #627 merged to main with green CI; DT/QA ran 8/8 test suites (72 assertions) with the full acceptance matrix PASS and security strengthened by the #612 two-hash managed-section tamper detection. No tag (version-less); no GitHub Release.
**Outcome:** SUCCESS — all 14 engineered issues' acceptance criteria MET (PASS per issue; see the Stage 13 close-out verification), #265 carried as a verified no-op (the `deploy.sh --report` continue-past-FAIL behavior was already present from the earlier v18→v1.0x re-version; no code shipped), and #632 folded in as a post-GO fast-follow (`notify-version-skew.sh` repaired + wired, regression test in CI, standalone main-targeted PR superseded). The release closes the fresh-install / onboarding blockers gating the private→public flip: the headline zero-skills fresh-install defect (#606) and the no-Cowork hard-fail (#607) are fixed, alongside a config-first install-path resolver (ADR-013, #233/#243), honored sandbox overrides (#611), two-hash managed-section tamper detection (ADR-014, #612), a wired no-change update exit (#613), validate-install repairs (#608/#609), deploy `--check/--report` fixes (#610/#265), count + `.version` reconciliation (#242/#614), deploy-test harness hygiene (#615), and manual-mode `.skill` bootstrap (#144).
