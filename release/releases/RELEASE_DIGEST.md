# RELEASE_DIGEST

Corpus-level digest grouped by version family. Per-version 3-5 line summary. Appended at Stage 13 chore PR per [`stage-13-close.md § Phase B`](../references/pipeline/stage-13-close.md).

## Knowledge Corpus

### v1.24 (2026-06-14) — The Stage-2 Triage A6.5 management-task detection bash is corrected so its automated backlog-hygiene checks stop silently returning zero: the orphaned-dependency check (Pattern 1b, #309) and the P1-blocked-by-lower check (Pattern 2a, #309) now read each issue's body section and return correct results, plus a stale cross-spec citation in `release-process.md` is fixed (CF-3) and the de-scoped `yq`-toolchain finding (CF-1) is evidence-closed against ADR-012's retirement of G3-13

Single-issue `routine`-class release (CHEAP reversibility, single-branch D-C SINGLE) resolving the three v1.02-triage carry-forward findings on issue #309. Before this release, the Stage-2 Triage A6.5 management-task detection table in `release/references/pipeline/stage-02-triage.md` extracted issue-body sections with the range form `awk '/^### Dependencies/,/^### /'` (and the parallel `/^### Priority/,/^### /`) — a **non-functional range**, because the start regex `^### Dependencies` is itself matched by the end regex `^### `, so the range opens and closes on the heading line, the body is dropped, and the downstream `grep` returns nothing. The orphaned-dependency and P1-blocked-by-lower advisory checks therefore returned a silent zero even when matching issues were present. The changed set is **two K1/reference docs (`stage-02-triage.md`, `release/governance/release-process.md`) + one CI-gate allowlist (`core/hooks/reference-durability-allowlist.txt`)** — no `SKILL.md`, no `packages/`, no `core/rules/` mirror or hook (deploy no-op, mirrors the v1.22 text/tool-only profile). **CF-2 (awk silent-zero):** all **3 affected instances** — Pattern 1b orphaned-dependency extraction, Pattern 2a P1-blocked-by-lower dependency extraction, Pattern 2a blocker-priority extraction — replaced with the robust `awk 'BEGIN{flag=0} /^### <heading>/{flag=1;next} /^### /{flag=0} flag'` form, which skips the heading via `next` and toggles a flag to emit the body (markdown table-cell pipe-escaping preserved; Pattern 4a — a `grep -cE`, no awk — correctly untouched, which also corrected the issue body's stale "1b/2a/4a" instance inventory: the real third instance is Pattern 2a Priority, not a 4a awk). **CF-3 (citation drift):** the Stage-2 Triage-cadence paragraph in `release-process.md` cited the sister management-task spec's "Stale issues" detection at `Phase A4 (ii) detection-pattern (1a)` — stale (the pattern lives at `Phase A6.5 Pattern (1a)`; Phase A4 there is the unrelated feasibility quick-check) — corrected to `Phase A6.5 Pattern (1a)` (the underlying P1-SLA bilateral contract is intact; only the anchor was wrong). **CF-1 (yq toolchain) DE-SCOPED:** the `yq` dependency targeted the **G3-13 Roadmap-Cascade Validation** detection bash in `core/schemas/gate-criteria-spec.md`, which ADR-012 (roadmap-instance-descope, Accepted 2026-06-02) already retired (roadmap instances are operator-local, not reusable platform structure) — no live detection bash carrying the dependency remains, so CF-1 was evidence-closed at verification rather than engineered. **Three CI-gate remediations rode along** on the touched lines: operator-handle tokenization, an `allow-link` marker (per-file reference-durability), and a `stage-02-triage.md` entry in `core/hooks/reference-durability-allowlist.txt` so the issue-ref detector does not false-positive on two pre-existing `#`-shaped patterns the fix re-surfaced (the `#5-process` anchor fragment of a `[Phase B B3](#5-process)` link + the `#[0-9]+` grep regex inside the detection bash — not net-new fragile refs; mirrors the sibling stage-03 / stage-04 / stage-12 allowlist entries). **#309 stayed OPEN at merge** (PR #1068 used `References #309` — `closingIssuesReferences` empty — so #309 is closed manually at Stage 13 with per-AC verification evidence). PR #1068 merged to main via merge commit `10977af642da65c1bbb2b35332ff3cba70cf5bb4` with the CI suite green; signed-annotated `v1.24` tag points at the merge SHA (ED25519 signature valid — `git tag -v v1.24` → Good signature; zero signing bypasses); no skill-package rebuild and no `./core/deploy/deploy.sh --deploy` (deploy no-op). **Re-versioned planned v1.21 → v1.24:** authored/branched/PR-titled as `v1.21` (`release/v1.21-pipeline-detection-bash-debt`); `v1.21`/`v1.22`/`v1.23` were each claimed by concurrent releases (governance-as-code-quality-gates, deploy-toolchain-defect-cleanup, pmo-skill-reference-substrate) between planning and execute, so the release landed at next-free **v1.24** (the #769 version-collision class); the branch name + build-commit messages retain the as-authored `v1.21` label (a cosmetic build-record artifact), every durable artifact ships v1.24. This release's Stage 4 plan was authored as a GitHub sub-task #1057 comment, not an in-repo `*_RELEASE_PLAN.md` file. `routine` class, CHEAP reversibility (single `git revert -m 1` of the release PR — corrective doc/spec-text edits only; no data migration, no consumer state to unwind; deploy no-op means nothing to re-sync post-revert). 30-day Stage 13 outcome window per the `routine` class.

### v1.23 (2026-06-14) — The change-management skill becomes a pluggable methodology toolkit (ADKAR consolidated as the single source of truth + Kotter/Lewin/Bridges/McKinsey-7S codified + a `methodology-selection` selector wired into SKILL.md Step 2.5), plus an intake-governance standard (`intake-desk`) and a cross-pipeline sub-task methodology reference

8-issue `novel`-class release (scope grown mid-build 3 → 8 by operator directive — the methodology suite was the expansion). Before this release, `change-management` codified only ADKAR — and ADKAR's 1-5 scale + barrier rule were defined twice, divergently, in `references/impact-assessment.md` and `references/readiness-checklist.md` — while Kotter/Lewin/Bridges/7-S were name-dropped per delivery-approach but had no usable reference and no selection mechanism. **#267 (ADKAR SSOT):** `adkar-framework.md` authored as the single source of truth for the scale + barrier-point rule; both siblings stripped their duplicate definition and now reference it, keeping only their application-specific content (one authoritative definition remains corpus-wide). **#1011/#1012/#1013/#1014:** the four methodologies codified as structurally-consistent references mirroring the ADKAR exemplar — Kotter 8-Step (org-process), Lewin 3-Stage (foundational meta-frame + force-field), Bridges Transition (psychological layer), McKinsey 7-S (static alignment-diagnostic via the 21-pair consistency assessment) — each registered in `core/specs/framework-catalog.md` (canonical_doc populated; `evolving`/12mo for ADKAR/Kotter/Bridges, `stable`/36mo for the fixed Lewin/7-S classics). **#1015 (keystone):** `methodology-selection.md` encodes the selection judgment (five axes — unit-of-change primary + delivery-approach + org-scope + time-horizon + dominant-risk tie-breaker — a methodology table, a combination table that promotes the `impact-assessment.md` seed, and a combination-logic engine) and wires it into `SKILL.md` as a new Step 2.5 gated to Modes A/C/D; the seed table's "Primary Framework" column is reconciled to point at the selector so one authoritative selection view remains (the selector gets no catalog row — a mechanism, not a framework; out-of-suite seed entries named-not-mapped). The cluster routed through one batched `change-management` version bump (v1.10 → v1.23). **#268 (intake-governance):** `operations/skills/intake-desk/references/intake-governance.md` — business-case tiering (`[budget]` = Tier 2), WSJF (`CoD ÷ Job Size`), tier→SLA (Tier 2 = 3 business days), a six-type MECE demand taxonomy, anti-pattern detection reusing (not forking) the `gate-checklists.md` `>95%` threshold, and a mandatory worked acceptance trace; `intake-desk` SKILL.md → v1.23 + ref-row; framework-catalog Cost-of-Delay `canonical_doc` pointer. **#224 (sub-task methodology):** `release/references/standards/sub-task-methodology.md` cross-pipeline best-practices standard summarizing the Stage 6 §A2 protocol + a `release-process.md` Stage-6 cross-ref. The two changed skills' `.skill` packages rebuilt at Stage 12 (Check 7). `novel` class, CHEAP reversibility (additive K1 docs + skill-version bumps + catalog rows; no prior consumers; `git revert -m 1`). **Re-versioned v1.21 → v1.22 → v1.23** — governance-as-code-quality-gates shipped v1.21 and deploy-toolchain-defect-cleanup shipped v1.22 to `main` mid-build, so this release re-merged current `main` each time (no content conflict; only the version-named plan file collided) and landed at next-free **v1.23**; the branch name + build-commit messages retain the legacy `v1.21` label (cosmetic build-record artifact), every durable artifact ships v1.23. A consolidated independent Stage 7+8 review returned GO (all 8 ACs PASS, every worked example re-derived by hand, three minor findings swept). #224 auto-closed at the release-PR merge; the other seven parents closed manually at Stage 13 with per-AC verification (the PR body's comma-list `Closes` closed only the first reference). PR #1043 merged to main via merge commit `23cafa4be9408be7d4320d458c780f1df0e0a08c` with the CI suite green; signed-annotated `v1.23` tag at the merge SHA (ED25519 signature valid; zero signing bypasses). 30-day Stage 13 outcome window per the `novel` class.

### v1.22 (2026-06-14) — The platform's deploy toolchain runs deterministically: eleven deploy-toolchain defects + one folded-in same-class defect repaired on `core/deploy/deploy.sh` (the most-edited file in the repo) and the release tools, fixing two `grep -c … || echo 0` arithmetic double-emits (Check 19 #76 + Check 28/29 #1058), a silently-swallowed read-only `references/` install failure (#88), a `cowork_install_path` `#`-clip (#332), a `blast-radius.sh` non-ASCII locale crash (#92), missing Check-18 `--report` parity (#104), a Check-17 env-var rename (#111), four LOW cleanup findings (#331), and adding a mode-invocation-drift Check 35 + `### Mode Provenance` block (#26)

11-issue + 1-folded-in release (`routine` class, CHEAP reversibility, single-branch D-C SINGLE) repairing the platform's deploy toolchain. Before this release, `core/deploy/deploy.sh --check` could exit on a `syntax error in expression` from a `grep -c … || echo 0` double-emit (a `0\n0` two-line capture that breaks the subsequent arithmetic), the `references/` mirror cleanup swallowed a read-only-orphan failure with `2>/dev/null || true` and then hard-failed opaquely on the later `cp -R` (the Cowork-install stale read-only orphan), the `cowork_install_path` reader clipped a quoted path at a literal `#`, `release/tools/blast-radius.sh` crashed with `sort: Illegal byte sequence` + a broken pipe on any non-ASCII-bearing file, and several governance checks lacked `--report` parity or carried a stale env-var name. **`core/deploy/deploy.sh` is the dominant serialization surface — 7 of the 11 issues + the folded-in fix touch it on mutually-disjoint check regions (Check 19 / Check 17 / Check 8 / `cmd_report` / new Check 35 append / Check 28-29), serialized by merge order under D-C SINGLE.** The changed set is **`core/deploy/deploy.sh` + four release-tool/install scripts (`blast-radius.sh`, `update.sh`, `validate-install.sh`, `notify-version-skew.sh`) + two K1/reference docs (`hub-spoke-bridge.md`, `label-taxonomy.md` + the Stage-6 pipeline spec)** — no `SKILL.md`, no `packages/`, no `core/rules/` mirror or hook (deploy no-op, mirrors the v1.13 text/tool-only profile). **#76 (Check-19 arithmetic double-emit):** the Check-19 pipeline-event-log capture replaced with the robust `… || true); var=${var:-0}` form. **#1058 (Check-28/29 double-emit, folded in at re-baseline):** the identical defect class at the Check-28 (`_has_section`/`_has_field`) + Check-29 (`_has_h2`/`_has_xref`) capture sites — out of scope for the original Check-19 work because it lived in two *different* checks — re-applied as a **signed** commit after the original unsigned foreign commit from a concurrent session was dropped on re-baseline. **#88 (references/ read-only self-heal):** the silent `rm -rf … 2>/dev/null || true` swallow replaced with a `remove_mirror_subtree` helper that self-heals a read-only Cowork-install orphan and surfaces an actionable RO-perms error + remediation rather than hard-failing opaquely on `cp -R`. **#332 (`cowork_install_path` `#`-clip):** the `[^"#]+` capture pattern at the scattered reader sites + the Check-8 reader fixed so a quoted path containing a literal `#` passes through whole. **#111 (Check-17 env-var rename):** `C15_THRESHOLD_OVERRIDE_FILE` → `C17_*` (the Check-17 namespace, post-renumber). **#104 (Check-18 `cmd_report` parity):** a Check-18 PASS/FAIL row added to `cmd_report` for Stage-13 evidence parity. **#26 (Check 35 mode-invocation-drift + Mode Provenance):** a brand-new **Check 35** appended after the shipped Check 34 (multi-mode SKILL.md mode-enum recognizability; reuses the existing `flag_warn_or_issue` / `DEPLOY_CHECK_MODE` warn-mode machinery — no new helper, flag, or mode file) plus its `cmd_report` parity block, with a `### Mode Provenance` block added to the Procedure 3 Spoke Template in `release/references/how-to/hub-spoke-bridge.md`. **#92 (blast-radius.sh locale):** `LC_ALL=C` pinned on the dependency-graph `sort`. **#331 (four LOW findings):** F1 `update.sh` EX_NOCHANGE Phase-5-redeploy consideration; F2 `validate-install.sh` A9 `PMO_PLATFORM_DEPLOY_ROOT` override; F3 `notify-version-skew.sh` dangling spec xref; F4 `deploy.sh` DEPLOY_ROOT comment. Engineering Commit 0 landed the release plan `release/releases/plans/v1.22_RELEASE_PLAN.md`. **Check-number collision resolved at re-baseline:** the mode-drift check was authored as `Check 34`, but the concurrently-shipped governance-as-code-quality-gates release (v1.21, PR #1040) had already authored its own template↔schema `Check 34`, so this release's check was renumbered to **Check 35** and appended after the shipped Check 34, with the in-function locals renamed `c34_*`→`c35_*` / `c34r_*`→`c35r_*` to avoid a same-function variable collision; `--check` on the merged main confirms both Check 34 (`OK: all 1 schema-bearing template(s) conform`) and Check 35 (`OK: all 9 multi-mode skill(s) expose a machine-recognizable mode-enum`) run and the double-emit residue is gone (zero `grep -c … || echo 0` patterns remain). **Nine issues #88 / #76 / #332 / #111 / #104 / #26 / #331 / #92 / #1058 auto-closed at the release-PR merge.** Two stale-against-the-tree issues were **evidence-closed at Stage 13 with no code change** — #343 (the skill-deployment session-UUID refresh: both literal UUIDs are absent tree-wide, `core/rules/skill-deployment.md` is fully tokenized to `[SESSION_UUID]`, and ADR-013 already re-pointed deploy.sh Check 8 off the doc-UUID-grep mechanism, so the issue's premise no longer exists) and #122 (the file-router Check 10 trailer: file-router's last non-merge commit `be1db81` already carries a Skill-Editor-Audit-Trail trailer, so Check 10 already passes for file-router — the v11.04 finding was superseded by that later pmo-skill-editor Mode-A edit). **#106 (status-label) stays OPEN** — its Check-16 framing is stale (Check 16 was re-scoped to scan `--label improvement` issues only and no longer scans sub-tasks), and the residual gap (the Stage-6 creation-path `status:` stamp + the Rule-6 clarification + the open-sub-task backfill) is the operator/hub's operational follow-up. PR #1081 merged to main via merge commit `edb99eb6313f6d991edc7ee0c231c918654c94fb` with the CI suite green; signed-annotated `v1.22` tag points at the merge SHA (ED25519 signature valid — `git tag -v v1.22` → Good signature; zero signing bypasses); no skill-package rebuild and no `./core/deploy/deploy.sh --deploy` (deploy no-op). **Re-baselined v1.20 → v1.21 → v1.22:** authored in-pipeline as v1.20, re-versioned to v1.21 when sior-escalation-discipline (v1.19) + health-and-raid-determinism (v1.20) consumed the tracked numbers, then re-versioned to v1.22 when governance-as-code-quality-gates took v1.21 and its Check 34; the next-free rule applied to updated facts three times. Unlike the v1.18→v1.20 / v1.20→v1.21 legacy-branch-name precedent, this re-baseline cut a fresh `release/v1.22-deploy-toolchain-defect-cleanup` branch off current `main` so the version identity, branch name, plan file, tag, and RELEASE_LOG row all agree on v1.22. 30-day Stage 13 outcome window per the `routine` class.

### v1.21 (2026-06-14) — The platform's automated quality-gate surface gains two detection capabilities: a template↔schema contract-fidelity check (`deploy.sh` Check 34, #318) that asserts each schema-bearing canonical template carries every section its governing schema mandates, and an L5 cascade-completeness gate (`pmo-qa-auditor` G8, #79) that greps a Stage-5 spec's changed-file set for un-swept OLD-value cascade occurrences as defense-in-depth over the existing L1–L4 prevention surfaces

2-issue release (`novel` class, CHEAP reversibility, single-branch D-C SINGLE; sequenced #318 → #79) adding two automated quality gates. Before this release, a canonical template could silently drift from the schema that governs its instances — Check 13 covered canonical↔mirror copy-fidelity only, with no contract-fidelity axis — and a rename/restructure that swept most but not all OLD-value occurrences had no audit-time detector across the whole changed-file set (the L1–L4 prevention surfaces operate at authoring/review time within the change matrix; L5 was deferred out of the #818 v11.07b scope because `pmo-qa-auditor` SKILL.md edits must route through the `pmo-skill-editor` Gate 2 hook). The changed set is **`core/deploy/deploy.sh` + one skill (`pmo-qa-auditor`) + K1 corpus/spec docs + one reconciled template** — no `core/rules/` mirror, no hook, no harness path (no rules-mirror deploy-sync required; the only deployable artifact is the rebuilt `.skill` package). **#318 (Check 34 — template↔schema conformance):** adds a `TEMPLATE_SCHEMA_MAP` manifest + a Check 34 block to `core/deploy/deploy.sh` asserting each schema-bearing canonical template carries every section its governing schema mandates — the third template-fidelity axis over Check 13's copy-fidelity. The manifest mechanism mirrors the proven `TEMPLATE_SYNC_MAP` pattern (the in-template-frontmatter alternative is governance-barred by `template-storage.md` §3.4); scope is opt-in by manifest presence, so a template the manifest does not name is a structural skip rather than a false failure (satisfying the no-false-positive AC). `operations/templates/open-meetings-tracker-template.md` was the first divergence the check caught — it shipped `ACTIVE` / `COMPLETED` sections against `tracker-schemas.md` Tracker 3's `Upcoming Meetings` / `Recently Completed` / `Recurring Cadences` structure (13-field `MTG-###` entry) — and was reconciled to the schema in the same release; the new axis is documented in `core/standards/template-storage.md` §3.5b. Check 34 reuses the existing `flag_warn_or_issue` / `DEPLOY_CHECK_MODE` / `deploy-check.mode` warn-mode machinery — no new helper, flag, or mode file — and ships warn-mode-first per the established shakedown precedent. The reconciled Open Meetings template changes what newly-scaffolded projects start with (verified safe against `project-initiator` Step 4). **#79 (G8 — L5 cascade-completeness gate):** adds a G8 check to `core/skills/pmo-qa-auditor/SKILL.md` that, when the output under audit is a Stage 5 spec carrying a `### Cascade-Sweep` block, re-runs each declared sweep against the changed-file set on the release branch and verifies the swept-declaration table enumerates every OLD-value occurrence found — the L5 audit-time, across-the-changed-file-set detector for the cascade-omission failure mode, the defense-in-depth complement to L1–L4's authoring/review-time, within-the-matrix prevention (the Stage 5 § 5.6 spec already cedes the whole-changed-file-set sweep to "`pmo-qa-auditor` automation"). The supporting detail lives in the net-new `core/skills/pmo-qa-auditor/references/cascade-completeness-detection.md`, and the cascade-omission anti-pattern in `core/specs/failure-mode-standard.md` cross-references L5 as the automated-detection surface. The SKILL.md edit routed through `pmo-skill-editor` Mode A (Gate 2 hook discipline preserved), and because the SKILL.md + a new `references/` doc changed, the `pmo-qa-auditor.skill` package was rebuilt at Stage 13 to carry the new check + reference (Check 7 package-freshness); the runtime skill-mirror sync (`./core/deploy/deploy.sh --deploy pmo-qa-auditor`) is flagged for the operator/hub to run from the primary post-merge (not run from the transient Stage 12 worktree). Both issues #318 + #79 auto-closed at the release-PR merge. PR #1040 merged to main via merge commit `62595481ef14bb4f37dd841809f5ca8b1568ce45` with the CI suite green, signed-annotated `v1.21` tag at the merge SHA (ED25519 signature valid — `git tag -v v1.21` → Good signature; zero signing bypasses). `novel` class fires on novel trigger (b) — three D-class plan decisions (Release Class + Branch Topology + Version) — and trigger (c) — ADR-class Stage 5 design choices on the cascade gate's deploy-time-vs-audit-time placement and Check 34's schema-scoping mechanism. Version confirmed **v1.21** at the Stage 12/13 close — the version was free (no concurrent collision, unlike the v1.18→v1.20 chain), so no re-version was needed. 30-day Stage 13 outcome window per the `novel` class.

### parallel-launch-quota-budget-gate (2026-06-14, version-less) — The release pipeline becomes quota-aware: a dual-checkpoint quota-budget gate estimates and re-validates parallel spoke/subagent waves against the operator's per-account usage-window envelope (Checkpoint A plan-time #23, Checkpoint B runtime #23) over the canonical Per-Account Usage Window Constraint subsection (#24), so a parallel batch never half-completes from cumulative usage-window exhaustion

2-issue release (`novel` class, CHEAP reversibility, single-branch D-C SINGLE; sequenced #24 → #23) making the parallel-spoke launch path quota-aware. Before this release, parallel batches launched blind to the operator's ~5-hour cumulative usage window and silently failed partway through once it was drawn down — the v11.27 first-failure (2026-05-24): nine Stage 5 spokes launched near the tail of the window, all returning a session limit on pure cumulative startup draw with no spoke completing work. The changed set is **text-only K1 corpus + release-pipeline spec/protocol docs + one release tool** — no `SKILL.md`, no `packages/`, no hook/harness path (deploy no-op, mirrors v1.13). **#24 (foundation):** authors the net-new `### Per-Account Usage Window Constraint` subsection in `release/references/how-to/hub-spoke-bridge.md § Spoke Launch Mechanisms` (renamed from the earlier "Quota Constraint" framing per the operator's 2026-05-24 correction that a usage window ≠ a rate limit) — the cumulative-draw failure mode, a cumulative-vs-rate-limit contrast table, the load-bearing mitigation set (pre-flight quota check / quota-budgeting per release / window-aware launch timing / serialize-on-failure / reduce-per-spoke), in-prompt `sleep` stagger demoted to a labeled secondary rate-limit-only defense (explicitly never the usage-window mitigation), and the v11.27 tail-of-window cumulative-exceed empirical anchor — plus a Stage-5-row note appended inside the existing Procedure 2 Step 5 Parallelism Rules table cell (4-col shape preserved) and a Stage-5 persona anti-pattern ("does not assume parallel-safe means usage-window-free") on the correct Stage-5 card in `release/references/specs/release-personas.md`. The binding variable is documented as the remaining-window envelope, not a fixed concurrent count; a batch-size heuristic is a floor for *running* the check, not a stagger trigger. **#23 (active gate):** builds the dual-checkpoint enforcement. Checkpoint A is a terminal Phase A6 Quota-Budget Pre-Check in `release/references/pipeline/stage-04-planning.md` (parallel-eligible count A2-sourced from the Stage Applicability Matrix) emitting a `### Quota Budget` plan section with a PASS / WARN / FAIL routing tree that references the protocol (register-or-remove, not re-stated). Checkpoint B is the load-bearing runtime check — a new Procedure 2 Step 5.5 plus a Checkpoint-B invocation hook on the § Spoke Launch Mechanisms default multi-launch paragraph in `hub-spoke-bridge.md` — rendering a PROCEED / SERIALIZE / DEFER / REDUCE-scope verdict on the usage-window axis before each parallel launch, with a deviation-logged operator-initiated one-batch DEFER override exit (a stale-envelope DEFER is escapable; no deadlock, no reflexive-override training) and a Step 5 orthogonality note recording that "parallel-safe" is coordination semantics, not usage-window semantics (the two gates compose). The new `release/references/standards/quota-budget-protocol.md` is the protocol/schema + operator-interaction-surface home (session-start capture + per-batch optional override); `release/references/standards/pipeline-event-log-schema.md` § 3 adds a `spoke-launch` event type with a `quota-reservation` subtype carrying `tokens_used:` on the payload (reconciled to the real `payload`-convention surface per the Stage-5 R-8 finding — no fictional `spoke-completion` event invented), with `release/tools/append-pipeline-event.sh` carrying the data-driven enum plus a static fallback mirror in lockstep (`--self-test` PASS, 11 event types). `release/ADRs/ADR-026-spoke-launch-quota-reservation-telemetry-event.md` (+ README index row, renumbered from a Stage-5 ADR-024 draft to avoid an in-flight collision) records the telemetry-event decision. The reconciled cutover/exemption clause uses the no-version-token introducing-release / merge-SHA markerless idiom, so the reference-durability Class-V version-apparatus detector does not match it and no `allow-version-ref` marker was needed for the cutover prose (both touched files also sit on the durability path-allowlist). Stage 7 DT escapes F-1 (`quota-budget-protocol.md` durability markers) and F-6 (shared-PR operator-memory gate) were both resolved and re-verified green; Stage 8 QA returned binary ACCEPT — **#23 all 11 reconciled ACs PASS** (sub-task #787), **#24 all 6 reconciled ACs MET** (sub-task #783) — with the DEFER override, telemetry-row validation, and envelope-vs-rate-limit fencing edge cases all clear and regression clean (keep-both merge, phase chain intact, #24 foundation preserved, ADR renumber clean). **Issues #23 + #24 stayed OPEN at merge** (PR #911 used `References #23, #24` — `closingIssuesReferences` empty — so they are closed manually at Stage 13 with per-AC verification evidence). PR #911 merged to main via merge commit `b2b5f69873075de6f9516e3f1d053a7829416114` with live CI 22/22 green and MERGEABLE/CLEAN; signed-annotated version-less `parallel-launch-quota-budget-gate` tag points at the merge SHA (ED25519 signature valid — `git tag -v parallel-launch-quota-budget-gate` → Good signature; zero signing bypasses); no skill-package rebuild and no `./deploy.sh --deploy` (deploy no-op). **Version-less identity:** authored in-pipeline as `v1.15`; `v1.15`/`v1.16`/`v1.17` (and subsequently `v1.18`–`v1.20`) were all claimed by concurrent releases during this session's pipeline run, so this release ships **version-less** under the slug identity `parallel-launch-quota-budget-gate` (collision-proof; the #769 version-collision class) — the tag name IS the slug; every load-bearing `v1.15` anchor was reconciled to the slug / introducing-release-SHA form per the release plan Deviation Log item (a). Follows the `public-flip-install-blockers` / `intake-elicitation-skill` / `domain-aware-stage5-design` / `memory-to-corpus-codification` / `cross-reference-integrity-ci` version-less precedents — with the distinction that this version-less release IS tagged and Release-bearing (prior version-less releases shipped with no tag / no GitHub Release). 30-day Stage 13 outcome window per the `novel` class.

### v1.20 (2026-06-14) — Project health and RAID escalation become deterministic: a single canonical cross-level metric → RAG-threshold → decision-rule index (#271), a watermelon-detection reference that consumes it (#270), an escalation-thresholds doc-of-record (#269), and a stale-RAID auto-escalation protocol on the `OPERATIONS.md` mirror pair (#261), so the same situation produces the same color and the same escalation regardless of which skill reports it

4-issue release (`novel` class, CHEAP reversibility, single-branch D-C SINGLE; two independent soft chains metric-registry → watermelon-detection and escalation-thresholds → stale-RAID protocol) making project-health surfacing and RAID escalation deterministic. At survey baseline, health colors and escalation timing depended on which skill produced them and how each one set its own lines; there was no single home for metric → threshold → decision mappings or for RAID age-escalation. All four targets were confirmed absent in the live tree, so every change is a pure ADD or append. **#271 (metric-registry):** authors the net-new `operations/skills/weekly-status-rollup/references/metric-registry.md` as a cross-level (portfolio / program / project / team) INDEX of metric → RAG-threshold → decision-rule mappings — it REFERENCES sibling-owned bands (`comms-writer/references/channel-formats.md` for schedule/budget/risk/scope; `delivery-engine/references/capacity-model.md` for capacity-utilization; `delivery-engine/references/backlog-health.md` for portfolio cut-points; `escalation-thresholds.md` for RAID-age) and OWNS only the metrics that exist nowhere else (Overdue RAID Count, the cross-level roll-ups), fixing a lower-inclusive / upper-exclusive boundary convention so boundary values classify deterministically and mapping 🟡 YELLOW ≡ Amber so the referenced sources read without translation drift. Its consuming skill is `weekly-status-rollup` (Section 1 Portfolio Health Dashboard + Health Indicators). **#270 (watermelon-detection):** authors the net-new `core/skills/pmo-qa-auditor/references/watermelon-detection.md` — eight named green-masking-risk signals (W1–W8) for a status reported GREEN while a contradicting band reading (overdue RAID, an SPI/CPI breach, a milestone slip) is present, each with detection logic, data sources, false-positive filters, and an escalation action, plus the rule that composes fired signals into a watermelon verdict; it consumes the registry thresholds by registry role-name (does NOT restate the values, per duplicate-source-discipline) and is read by pmo-qa-auditor in Mode A (Single Output Review) + Mode C (Push-to-Resolve Audit). **#269 (escalation-thresholds):** authors the net-new `operations/skills/ppm-agent/references/escalation-thresholds.md` as the PPM Agent doc-of-record for the two things the PPM Agent owns — the score → escalation-tier routing (the score-band → tier partition) and the age-based auto-escalation thresholds (warn-vs-escalate) — while REFERENCING the delivery-engine 5×5 probability × impact scoring scale (`raid-templates.md §5`) rather than re-deriving it, and reproducing the product grid only as a routing aid (the two `12`-cells both land in the Program band). **#261 (stale-RAID auto-escalation protocol):** appends a new `### Stale-RAID Auto-Escalation Protocol` H3 under the existing `## Cross-Skill Protocols` H2 in the `OPERATIONS.md` canonical+mirror pair (`core/governance/OPERATIONS.md` canonical + `operations/OPERATIONS.md` byte-identical mirror, edited in the same commit) — age = `today − open-date` validated against the RAID Log `[Project]_RAID_Log.csv`, two warning/escalate bands by RAID type (Issue `I-*`: > 14 / > 30 days; Risk `R-*`: > 30 / > 60 days; Assumptions and Dependencies are not age-escalated), and a Nominal → Warning (`[RECOMMENDED]` owner nudge) → Escalate (flag naming owner + breached threshold + routed tier) action ladder; PPM Agent owns the weekly RAID review and `[RISK]` / `[DECISION]` routing while Delivery Engine emits the escalation and hands the tag back to PPM Agent for routing (no self-routing, max-depth-2). The protocol cites `escalation-thresholds.md` as its doc-of-record and never auto-closes / auto-reassigns / mutates the RAID Log without the normal Document Tier 1 approval gate. Beyond the planned append, the `OPERATIONS.md` mirror pair (enrolled in deploy Check 9) was reconciled in the same commit so mirror-drift holds on main (the only permitted inter-file delta is the relative-link-path difference; the stale-RAID append adds no cross-doc links). Consumption is reference-by-relative-link throughout — each consumer cites the canonical-source doc rather than re-deriving thresholds — which keeps a single owner per threshold and avoids the only cross-issue file contention; the three new reference docs are consumed via their owning skills' `references/` directory, so the three `.skill` packages (`packages/weekly-status-rollup.skill`, `packages/pmo-qa-auditor.skill`, `packages/ppm-agent.skill`) were rebuilt at Phase J.5 to carry the new docs (Check 7 package-freshness). All four issues #271 / #270 / #269 / #261 auto-closed at the release-PR merge. PR #933 merged to main on the 3rd Stage 12 attempt (prior attempts BLOCKED on two CI gates [FIXED, commits ff8b5f8 + 2fdaf97] and a v1.19 version collision [FIXED — re-versioned to v1.20, commit 73fd104]) with the CI suite green (21 checks), signed-annotated `v1.20` tag at merge SHA `626b9926270216c639b7c7c13727c0648733028f` (ED25519 signature valid; zero signing bypasses); skill-mirror deploy-sync (`./deploy.sh --deploy`) was flagged for the operator/hub to run from the primary post-merge (not run from the transient Stage 12 worktree). Re-versioned **v1.18 → v1.19 → v1.20** in-pipeline after the concurrent `cross-release-impact-model` release (#87, PR #928) claimed `v1.18` and the concurrent `sior-escalation-discipline` release (PR #941) claimed `v1.19`; the branch `release/v1.18` retains its name as a legacy/cosmetic identifier per the v1.17 precedent. 30-day Stage 13 outcome window per the `novel` class.

### v1.19 (2026-06-14) — Critical/High escalations across the skill suite now emit a single shared SIOR (Situation / Impact / Options / Recommendation) block — authored once in a canonical protocol doc and consumed by relative link — with a mandatory Recommendation + confidence and a named decision owner: comms-writer as the reference implementation (#179), ppm-agent [COMMS] routing (#178), pmo-technical-analyst CRITICAL/HIGH findings (#177), and delivery-engine dependency/RAID surfaces (#934)

4-issue release (`novel` class, CHEAP reversibility, single-branch D-C SINGLE; sequenced #179 → {#178 ‖ #177 ‖ #934}) consolidating the platform's SIOR escalation discipline. At survey baseline, SIOR lived inline across eight files with divergent structure, no shared severity-threshold policy, and — in comms-writer's case — no Recommendation component at all (the 2026-04-18 skill review's P1 gap); each consumer implied its own threshold and format. **#179 (keystone):** authors the net-new `core/standards/sior-escalation-protocol.md` as the single source — the four mandatory ordered components (Situation / Impact / Options / Recommendation), the canonical severity-threshold policy (CRITICAL = always; HIGH = SIOR + stakeholder-authority check; MEDIUM = conditional on blocking a downstream deliverable; LOW = no), five worked escalation-class examples, and the decision-owner-mapping pattern (read free-text `## Key People`, warn-and-route-to-PgM when authority data is absent). The doc is presentation-neutral (content/structure, not rendering) so a future dual-format model can layer on it. comms-writer's named **Escalation** type is re-specified to emit named SIOR sections with an explicit mandatory Recommendation + confidence — the prior Ask-conflation stripped and the Ask demoted to a closing call-to-action (the load-bearing rule: a Recommendation states the agent's judgment, an Ask requests a decision; output presenting only an Ask is non-conformant) — and SIOR is carried into the name-keyed Escalation entry in `core/schemas/per-skill-output-contracts.md`. **#178:** ppm-agent Section 5 `[COMMS]` routing emits a voice-neutral SIOR block, its two inline copies (`references/push-to-resolve.md` + `references/proactive-follow-up-tracking.md`) reconciled to the canonical doc. **#177:** pmo-technical-analyst emits a severity-conditional SIOR block on CRITICAL/HIGH findings with an authority check + decision-owner mapping. **#934 (folded in at Stage 5 discovery):** delivery-engine reconciles its two inline copies (`references/dependency-rules.md §4.2` + `references/raid-templates.md §7`) to the canonical doc. Each of #178/#177/#934 also adds one new failure mode. Consumption is **LINK-REFERENCE** (ADR-025) — consumers reference the canonical by relative markdown link; the canonical is NOT injected into any skill's packaged `references/`, so it never enters the Check 13b same-basename collision surface, requires no `deploy.sh` change and no `.skill` rebuild for injection, and eliminates the only cross-issue file contention (all four issues touching `deploy.sh`) the Stage 4 plan flagged. The four skill packages rebuilt only because each consumer skill's own content changed (Check 7 package-freshness). **`ADR-025-sior-escalation-canonicalization.md`** (CREATED) records the architecture decision — canonical home, link-reference consumption, the severity-threshold policy, and the graceful-degradation decision-owner mapping; it was renumbered from the Stage 5 spec's ADR-024 draft because the in-flight PR #911 claims `release/ADRs/ADR-024-…`. All four issues #179 / #178 / #177 / #934 auto-closed at the release-PR merge. PR #941 merged to main with CI 22/22 green, signed-annotated `v1.19` tag at merge SHA `787029df87e2ea6e961e9cdc5a030ffa248fcfd2` (ED25519 signature valid; zero signing bypasses); Stage 12 rebuilt the four `.skill` packages (`comms-writer`, `ppm-agent`, `pmo-technical-analyst`, `delivery-engine`) so Check 7 package↔source parity holds, and the explicit-name deploy installed all four skills + four packages to the runtime targets (0 failures). Re-versioned **v1.18 → v1.19** in-pipeline after the concurrent `cross-release-impact-model` release (#87, PR #928) claimed the `v1.18` tag first. 30-day Stage 13 outcome window per the `novel` class.

### v1.18 (2026-06-14) — The release pipeline gains a durable, repo-agnostic model of the concurrent + planned release set and its mutual impact, consulted at Stages 3/4/5/9/12: a structural-blast-radius (F1–F6) contention axis at Stage 3/4 (#87), cross-release coherence at Stage 5, a GO baseline-currency criterion (G-PR9) at Stage 9, and a semantic GO-invalidation check at Stage 12 Phase A.5

1-issue release (`cross-cutting` class, CHEAP reversibility, single-branch D-C SINGLE) adding the root-cause fix for the four 2026-05-16 four-milestone parallel-run failures: a path-invalidation collision with zero ticket-dependency edge, a silently-voided Stage-9 GO, a stale-pinned cross-PR audit, and an un-flagged parallel-unsafe bundle. The pipeline previously modeled each release in isolation — there was no durable, queryable model of the concurrent + planned release set, so cross-release collisions surfaced only late or by operator memory. **#87 (cross-release impact model):** introduces a third contention axis — **structural-blast-radius** — alongside the existing ticket-dependency-edge and same-path-overlap classifiers. `release/references/pipeline/stage-03-bundle.md` defines the `§ A9.6.1` axis: a 4-token git mover-classifier (`RENAME` / `RELOCATE` / `DELETE-RECREATE` / `DIR-RESTRUCTURE`), each tied to a reproducible `git diff --name-status --find-renames` signal; the mover's break surface `SURFACE(R)` computed via the F1–F6 reference-form sweep (the **F4 mover-internal-outbound form is EXCLUDED** — it is the mover's own rewrite obligation, not an edit to target files, and including it would over-serialize against high-traffic governance files); the intersection predicate `EDITSET(sibling) ∩ SURFACE(R) ≠ ∅`; and a `Tier-S` verdict + `structural-blast-radius` edge extending the live standing Parallelization Map. `release/references/pipeline/stage-04-planning.md` adds the authoritative A4 structural sub-audit (the first pipeline point a release branch exists), the sibling-merge stale-pin self-invalidation trigger (a pinned A4 audit re-runs when a sibling parallel release merges after the pin and before Stage 9), and Phase A0 Tier-S re-derivation. `release/references/pipeline/stage-05-solutioning.md` adds a cross-*release* coherence row to the Collective Review Decision Briefing. `core/schemas/gate-criteria-spec.md` adds **`G-PR9`**: the Stage-9 GO records its baseline SHA and a sibling-merge revalidation predicate (verdicts CURRENT / STALE-REVALIDATE / STALE-VOID), composing with the live Phase-A6.5 mid-pipeline-divergence check as a second axis (registry now `G-PR1..G-PR7` + `G-PR9`, no renumber, added cleanly above the live-but-unregistered G-PR8 gap owned by #887). `release/governance/release-process.md` adds the Stage 12 Phase A.5 semantic GO-invalidation check, which catches a referenced path renamed since the GO baseline even when a plain `git merge` reports no textual conflict for a prose reference; its byte-identical `.claude/rules/release-process.md` mirror syncs via `./deploy.sh --deploy`. `release/skills/release-planner/SKILL.md` is updated to consult the model (runtime-skill mirror; `packages/release-planner.skill` rebuilt in-PR). **`ADR-024-cross-release-impact-model.md`** (CREATED) records the architecture decision. All three structural surfaces (the A4 trigger, G-PR9, Phase A.5) share one merge-style-agnostic `--name-status --find-renames` predicate so they answer the same question the same way (the repo lands siblings via both merge commits and squash / fast-forward non-merge commits); every new criterion carries the introducing-release-exempt cutover clause (reflexive-pipeline-loop discipline — the introducing release cannot fire its own new gate). The model's persistence reuses systems already in place — the live Parallelization Map in the milestone description + the release plan's recorded baseline SHA — with no new datastore and no repo-specific hardcoded paths, milestone names, or counts. Stage 7 Dev Testing returned 7/7 ACs PASS (the one Warning-severity escape, a missing `domain_practice` provenance label, was resolved on the release branch with the pipeline-internal exemption form); Stage 8 QA returned ACCEPT with the AC-7 over/under-serialization pair validated both directions against the implemented F4-excluded `SURFACE(R)` (v11.08↔v11.11 caught and serialized; v16.02↔v11.11 correctly not over-serialized) and six boundary cases each resolving to the correct verdict with no regressions. **#87 stayed OPEN through merge** — PR #928 used `References #87` in its Issue References block (`closingIssuesReferences` empty), so #87 was closed manually at Stage 13 with the per-AC verification evidence comment. PR #928 merged to main with all CI green, signed-annotated `v1.18` tag at merge SHA `7d9307bbfcf317e29bfe34a772f5fac08a8934ea` (zero signing bypasses); Stage 12 rebuilt the `release-planner.skill` package so Check 7 package↔source parity holds on main, and the deploy-sync of the release-process rules mirror + the release-planner skill mirror was flagged for the operator/hub to run from the primary post-merge (not run from the transient Stage 12 worktree). 30-day Stage 13 outcome window per the `cross-cutting` class.

### v1.17 (2026-06-14) — The two PMO authoring skills narrow to their PMO-unique surface and offload commodity work to Anthropic at design time: comms-writer narrowed to 6 PMO-unique types (#174) + owning exec-brief/stakeholder-email via own-with-harvest (#173), artifact-generator catalog narrowed to 27 + a Wrapper Mode and Part-D routing tree (#176, #175), and a duplicate ADR-022 collision resolved to ADR-023 (#791)

5-issue release (`novel` class, CHEAP reversibility, single-branch D-C SINGLE, sequenced #174 → #173 → #176 → #175 → #791) refactoring the two PMO authoring skills — `comms-writer` and `artifact-generator` — to their genuinely PMO-unique surface and giving each an Anthropic-voice offload path that runs at design time rather than as a runtime dependency. **#174 (narrow comms-writer to 6 PMO-unique primary types):** `comms-writer` SKILL.md is narrowed to the 6 communication types that are genuinely PMO-unique, with the PMO-critical rules (audience calibration, escalation discipline, the status-theater guardrails) preserved through the narrowing rather than dropped — the narrowing removes scope the skill never needed to own, not the rules that make its output principal-grade. **#173 (comms-writer owns exec-brief + stakeholder-email — own-with-harvest):** `comms-writer` takes ownership of executive-brief and stakeholder-email generation under the **own-with-harvest** pattern: the Anthropic voice is harvested into the skill at design time, so generation produces the voice without any runtime Anthropic dependency. **#176 (narrow artifact-generator catalog — Part A+B):** the `artifact-generator` catalog is narrowed to its PMO-unique artifact set — 27 artifacts after the narrowing plus the own-with-harvest additions — removing the commodity artifacts an Anthropic scaffolding skill produces better. **#175 (artifact-generator Anthropic offload Wrapper Mode + Part-D routing tree — Part C+D):** a NEW **Wrapper Mode** in `artifact-generator` offloads commodity artifact scaffolding to an Anthropic skill, and a **Part-D routing decision tree** in `CLAUDE.md` routes an artifact request to the PMO catalog versus the Anthropic offload path so the boundary between PMO-unique and commodity work is explicit at request time. **#791 (resolve duplicate ADR-022 collision):** the duplicate `ADR-022` basename collision in `core/ADRs/` is resolved by renumbering the skill-sourcing-coupling-posture ADR to `ADR-023-skill-sourcing-coupling-posture.md` (the platform-config-vs-operator-toml-split ADR retains `ADR-022`), with the cross-references reconciled and the skill-sourcing registry trigger-(d) wired. This release was authored in-pipeline as `v1.15`; the concurrent `platform-self-measurement-and-quality-method` release claimed `v1.15` first and `delivery-capacity-and-lifecycle-gating` landed `v1.16`, so the canonical version is **v1.17** — the signed `v1.17` tag points at this release's merge SHA `79d8827d0fb57f671ad49c0a80acf9e376bc5a55`. All 5 issues #173 / #174 / #175 / #176 / #791 auto-closed at the release-PR merge; PR #864 merged to main with the CI suite green, signed-annotated `v1.17` tag at merge SHA `79d8827d0fb57f671ad49c0a80acf9e376bc5a55` (zero signing bypasses); Stage 12 rebuilt the `comms-writer.skill` + `artifact-generator.skill` packages so Check 7 package↔source parity holds on main, and the explicit-name `./core/deploy/deploy.sh --deploy comms-writer artifact-generator` deployed both skills + both packages to the runtime targets (0 failures). 30-day Stage 13 outcome window per the `novel` class.

### v1.16 (2026-06-13) — The delivery-engine sizes capacity, holds estimate ranges, and gates the lifecycle: a capacity model (#272), an estimation standard owning the focus-factor table + Cone-of-Uncertainty + range enforcement (#273), a 15-stage universal delivery lifecycle (#274), and an eleven-gate project lifecycle (#275)

4-issue release (`novel` class, CHEAP reversibility, single-branch D-C SINGLE, sequenced #272 → #273 → #274 → #275) turning the delivery-engine's sprint-planning + release-readiness surfaces from rules-of-thumb into governed method across four reference foundations under `operations/skills/delivery-engine/references/`, with the SKILL.md mode wiring (Modes C/D/E/F/G) that consumes them. **#272 (capacity-model.md):** a NEW reference modeling *effective* delivery-team capacity — the capacity formula (§1), context-switching penalties across concurrent projects (§3), Brooks's-Law late-addition thresholds (§4), the 60/20/20 effort-allocation split (§5), team-stability + vendor-ramp thresholds (§6–§7), staffing-concentration / bus-factor risk (§8), and the demand-versus-supply gap RAG bands (§9); wired into Mode D (formula + penalties) + Mode E (demand-supply gap). Two inputs are referenced not restated — the focus factor (owned by `estimation-standards.md` §3) and the platform bus-factor rule (owned by `core/disciplines/km-protocols.md` §4). **#273 (estimation-standards.md):** a NEW reference, the canonical home for estimation discipline and the **sole owner of the focus-factor table** (§3, promoted unchanged from its prior single occurrence in `sprint-defaults.md` §1.2 — the single-source move) — plus the Cone-of-Uncertainty band widths bounding precision by lifecycle phase (§1; the normative cone rule rejects a stated range tighter than the item's phase band), planning-horizon commit-vs-forecast rules, the three-zone buffer model, the velocity-and-estimates-as-ranges enforcement rule (§5), and the contingency-vs-management-reserve distinction; wired into Mode D (bands + horizon + table + buffer model) + Mode E (velocity-as-range enforcement). **#274 (lifecycle-stages.md):** a NEW reference enumerating the **15-stage universal delivery lifecycle** — the temporal axis, five-phase super-grouped (Capture → Prepare → Build → Validate → Deliver) — with per-stage entry/exit criteria + consumed/produced artifacts and a 5-of-8 methodology mapping; wired into Mode C (execution-stage entry criteria) + Mode F (validation-stage exit criteria, incl. the QA/Acceptance exit predicate that blocks a move to Plan Review while a P1 defect is open); carries an explicit boundary statement that this is the project delivery lifecycle, NOT the platform's own 13-stage release pipeline. **#275 (gate-definitions.md):** a NEW reference, the canonical sequence of **eleven project-lifecycle gates** `LG-0` (Idea Screen) through `LG-10` (Closure), each with entry/exit criteria, key decisions, authority holder, required artifacts, and escalation path; wired into Mode F (per-gate exit criteria + transition rule) + Mode G (gate-decision attribution to the correct gate + authority holder) + Mode C (situates the sprint DoR gate as LG-4); cross-references the sibling gate-TYPE taxonomy (`gate-checklists.md` — Phase / Quality / Flow / Approval / Hypothesis) rather than restating it, and the `LG-` prefix is deliberately namespaced to avoid collision with the release-pipeline's `G-PR*/G-EX*/G-CL*` stage gates. Together the three governance docs form a clean three-axis model: `lifecycle-stages.md` (temporal stage sequence), `gate-definitions.md` (gate-instance sequence), and `gate-checklists.md` (gate-type taxonomy), each owning one axis and cross-referencing the others. A concurrent v1.15 merged into main mid-pipeline; the resulting `delivery-engine` SKILL.md conflict was resolved at Stage 12 with BOTH edit-sets preserved (the v1.15 RCA additions + this release's Mode C/D/E/F/G wiring — keep-both, not last-writer-wins). Two CI gates (durable-corpus fragile-reference `#N`) flagged at Stage 9 Plan Review were de-fragiled before merge. PR #859 merged to main with the CI suite green, signed-annotated `v1.16` tag at merge SHA `a915daa94caa68423b42cbf7e7db69a4dfbc37e1` (zero signing bypasses); Stage 12 rebuilt the `delivery-engine.skill` package so Check 7 package↔source parity holds on main, with the runtime skill-copy deferred per the content-release precedent (canonical deployment = merge + signed tag; the reference docs activate git-native on the merge). 30-day Stage 13 outcome window per the `novel` class.

### v1.15 (2026-06-13) — The platform measures and improves its own agent quality: a base-vs-build platform-health audit mode on a cadence (#358), a data-driven 6–10 failure-mode target range for complex skills (#359), an invokable RCA method + a migration playbook with intake/pipeline handoff (#754), and a verified triage of the 34-day-stale harness audit (#125)

4-issue release (`novel` class, CHEAP reversibility, single-branch D-C SINGLE, sequenced #359 → #358 → #754 → #125) making the platform measure and raise the quality of its own agents across four surfaces — a mixed bundle of three authoring efforts plus one analysis-class deliverable. **#358 (Platform Health Audit mode):** a NEW `platform-health` mode integrated into `core/skills/pmo-qa-auditor/SKILL.md` that audits base-vs-build compatibility drift on a registered cadence, consuming the re-homed predecessor inputs `release/references/protocols/platform-health-audit-framework.md` + `core/specs/anthropic-base-vs-build-registry.md` (both verified present at Stage 5), with the deferred v11.01f body ACs resolved against the live §4 placeholder; this was the highest-blast-radius issue in the bundle (skill-mode surface + new protocol section + cadence registration). **#359 (data-driven failure-mode target range):** `core/specs/failure-mode-standard.md` § Minimum Count amended with a 6–10 target-range addendum for complex skills (replacing a bare ≥3 floor with an evidence-based target); the cutover risk the issue body named was MOOT — the v1.10 `failure-mode-coverage-completion` release had already shipped and all 5 per-category remediation issues #6/#138/#139/#140/#141 were already CLOSED, so the scenario of remediation reviewed against the old floor while the new range lands could not occur. **#754 (invokable discipline methods):** two NEW discipline docs — `core/disciplines/root-cause-analysis.md` (the invokable RCA method) + `core/references/how-to/migration-playbook.md` (the migration playbook) — wired with an intake/pipeline handoff via `core/rules/operations-bridge.md` + the `core/disciplines/README.md` and `core/references/how-to/README.md` indexes; the issue offered a "focused skill" option but the docs-as-disciplines path was taken (so Stage 12 carried no skill deploy for #754). **#125 (verified triage — analysis-class):** a verified triage of the 34-day-stale 2026-05-10 harness audit (NOT a restatement) — F2 RESOLVED (link-check CI workflows now exist), F4 RESOLVED-by-pruning (calibration-data + iteration-log deleted tree-wide), F1 PARTIALLY-RESOLVED (eval dirs 2→4 but `evals/results/` still absent), F3 UNVERIFIABLE-HERE (runtime hook `.mode` lives install-root, not in repo), F5 UNVERIFIED→install-root; F2 + F4 explicitly NOT re-filed, only the F1 residual + the F5 triage call survive as follow-up proposals for routing; Dev Testing + QA were SKIPPED (no runtime surface — verification is evidence-quality of the triage at Plan Review). PR #858 (14 tracked files) merged to main with the full CI suite green, signed-annotated `v1.15` tag at merge SHA `dfb3836a2b8ac9807113ae1beb7a91629a3e2874`; Stage 12 rebuilt 3 `.skill` packages (`pmo-qa-auditor`, `delivery-engine`, `intake-desk`) so Check 7 package↔source parity holds on main. 30-day Stage 13 outcome window per the `novel` class.

### v1.14 (2026-06-13) — The adapter-config foundation: one discoverable platform-config surface with a 5-rung hierarchy resolver (#22), and a clone→working-install onboarding-umbrella spine with four host extension points (#703) — the two surfaces every host adapter composes into

2-issue release (`novel` class, CHEAP reversibility, single-branch D-C SINGLE, serialized #22 → #703) landing the **adapter-config foundation** — the canonical platform-config surface and onboarding spine that every host-adapter ticket (#10/#11/#12/#13) composes into. **#22 (unified platform-config surface + 5-rung resolver):** reframed off the issue body's stale "new YAML/MD file" premise once the hub verified the surface is NOT greenfield (operator-identity template + composition-surface spec + deploy rung-reader + ADR-017 §162 all pre-existed), the surface split into two Layer-1 templates by concern — NEW `core/config/operator.toml.template` (operator ENVIRONMENT/IDENTITY: identity, paths, `[adapters]` host-selectors, `[methodology].default_delivery_approach`; `chmod 600` + depersonalization-token-bearing) and NEW `core/config/platform-config.toml.template` (platform BEHAVIOR: `[bundling]`, `[release_class]`, `[relationship_mapping]`, `[calibration]`; freely tunable, no PII) — with a NEW field schema `core/schemas/platform-config-schema.md`, a NEW human-readable catalog `docs/platform-config-reference.md`, and the **Platform-Config Resolution Protocol** authored into `core/governance/OPERATIONS.md` (the 5-rung most-specific-wins cascade global→portfolio→program→project→individual, a 3-level default-fallback that never hard-fails a release, single-resolution-at-the-hub with spokes reading injected values, and a two-track update governance split on the Layer-1/Layer-2 boundary). Three consumers wired as worked examples (`release-planner` Mode A + `release-executor` Mode A resolve named fields at session start; `core/deploy/deploy.sh` extends its rung-reader to the behavior surface). Scope = mechanism + resolver + 3 wired consumer examples, NOT a migration of every scattered config item; the split rationale is recorded in NEW `core/ADRs/ADR-022-platform-config-vs-operator-toml-split.md`. **#703 (onboarding-umbrella spine):** NEW `docs/ONBOARDING_JOURNEY.md` — the whole-journey map (the ordered clone→working-install arc with four uniform Extension-Point blocks naming where each host choice — repo, ticketing, KB, AI tool — plugs in), linked from the `README.md` Getting Started region; Stage 5 set the boundary explicitly (journey-spine + extension-point contract, NOT a re-statement of the GETTING_STARTED / FIRST_STEPS walkthroughs) and Stage 8 verified non-duplication. The journey spine is the surface host adapters #10/#11/#12/#13 compose into in later releases. The release PR #759 stamped both `release/skills/release-planner/SKILL.md` + `release/skills/release-executor/SKILL.md` at `v1.13` in error; the Stage 12 close-out re-stamped both to `v1.14`, rebuilt the two `.skill` packages so they embed v1.14, and anchored the resolution-protocol cutover SHA to the literal PR #759 merge SHA `71047a5` (this release itself exempt per reflexive-pipeline-loop discipline). Both issues #22 + #703 auto-closed at the release-PR merge; single release PR (#759; 20 tracked files) merged to main with the full CI suite green, signed-annotated `v1.14` tag at merge SHA `71047a5` (ED25519; zero signing bypasses). Stage 12 deployed `release-planner` + `release-executor` + 2 rebuilt packages to both runtime targets (0 failures). 30-day Stage 13 outcome window per the `novel` class.

### v1.12 (2026-06-13) — Corpus durability enforced by two file-disjoint CI gates: shared references single-sourced with a collision-detecting rebuild check (#316), and raw issue/PR/milestone URLs prohibited outside the named 5-surface release ledger (#311)

2-issue release (`novel` class, MODERATE reversibility, single-branch D-C SINGLE, serialized #316 → #311) hardening corpus durability across two file-disjoint enforcement surfaces, both shipping **warn-mode-initial** per the `bypass-mode-readiness.md` shakedown precedent. **#316:** the two hand-duplicated shared references are single-sourced to NEW canonical homes `core/standards/output-format.md` (was 6 byte-identical copies, md5 `c43de7d8…`) and `core/standards/operational-artifacts.md` (was 2 copies); the 6+2 per-skill mirror files were deleted and `core/deploy/deploy.sh` extended to inject both canonicals via `TEMPLATE_SYNC_MAP`, carrying a **Check 13b template-injection collision sub-assertion** that fails on any reintroduced same-basename copy (identical OR divergent — the operator-added divergent prong) via a narrow explicit-basename resolver extension. The deferred `depends_on:` frontmatter mechanism was verified to CONFLICT with the Anthropic skill-creator schema (`template-storage.md` §3.4/D5), so the release made 0 SKILL.md frontmatter edits — only a one-line `comms-writer` cross-path body fix; the single-source contract is documented in `template-storage.md` + `skill-deployment.md` + `universal-vs-localized-context.md`. **#311:** a NEW Class U raw `github.com/.../{issues,pull,milestone}` URL detector `BLOCK-FRAGILE-REF-004` was added to `core/hooks/block-fragile-refs.sh` and the byte-identical `.github/workflows/reference-durability.yml` CI (with an `is_ledger_exempt()` tracking-surface exemption covering top-level `CHANGELOG.md`); the named **5-surface ref-permitted allowlist** was formalized in `universal-vs-release-pipeline-split-rule.md` (adding CHANGELOG to the prior 4), the new URL class registered in `reference-durability-standard.md`, and fixtures wired into `run-fragile-ref-fixtures.sh` + `cutover-fixtures.txt`; Delta-3 (version-cutover-clause prohibition) was folded **verify-only** with R3 evidence citing the already-live `CUTOVER_RE` lines, and `git-workflow.md` documents the new raw-URL prohibition. DT PASS ×2 / QA ACCEPT ×2; `deploy.sh --check` Check 13/23/32 OK and the reference-durability detector fixture 33 passed / 0 failed; Phase B4 deployed the 6 reference-consuming skills + 6 rebuilt `.skill` packages to both runtime targets (0 failures). Both issues marked closed at Stage 13 on per-AC evidence + merge SHA `5714ae5` + live gate per issue (release PR #748 used `References` → manual close). **Check 14 flip-to-enforce assessment: DEFER** — Check 14 keeps warn-mode through the standard shakedown window (no warn-log backlog), and the two gates introduced this release are brand-new with zero warn-log history, below the bypass-mode-readiness ≥3-day review threshold; the deferral is explicit per the doc-link-maintenance mandate. Milestone description PATCHed at close with the `## Release Class` (novel + posture) H2 + `### Release Outcome Statement` H3 per the Stage 9 GO Condition 1. Adversarial review #733 absorbed at Stage 5 (3 corrections, 2 design-mandates built); #746 filed out-of-scope. 30-day Stage 13 outcome window per the `novel` class.

### memory-to-corpus-codification (2026-06-10, version-less) — Verification-only close-out of the memory-to-corpus codification scope (nine encodings verified pre-shipped with provenance; operator-instance eviction of the three superseded memories; tickets marked closed on evidence)

2-issue release closing out the Knowledge Corpus initiative's R01 codification scope by verification rather than authoring; `routine` release class, CHEAP reversibility, single-branch (D-C SINGLE, standard two-PR shape), **version-less** (operator decision — no `vX.Y`, no git tag, no GitHub Release; `.version` unchanged), following the `domain-aware-stage5-design` / `intake-elicitation-skill` / `public-flip-install-blockers` precedents. Stage 4's G-PL1 currency gate found both tickets' corpus work already complete on main: all five #356 workspace guardrails (whole-plan approval, authorization-scope enumeration, skill-boundary transparency, surgical edits, harness-tooling governance) live in `core/CLAUDE.md.template`, and all four #357 git-workflow rules (fast-forward-by-default primary sync, never-restore-uncommitted-deletions, parser-clean PR-body discipline, reviewer-field-expected-empty) live in `core/rules/git-workflow.md`, with a line-by-line content-richness diff finding no load-bearing nuance missing from the corpus. Provenance (assembled at the Stage 9 gate after the operator twice challenged the no-fix diff, then rendered GO on the provenance-complete briefing): two encodings land from the original repository's lineage (2026-04-18; 2026-05-12) and seven from the clean-room public-seed build (this repository's first commit, 2026-06-05); the tickets migrated to this repository open and were bundled unverified — the currency gate was the first re-check. The deliverable repo diff is empty (release PR #604 = the committed plan only, Engineering Commit 0); the release's only substantive mutation was the Stage 12 operational deploy on the operator instance — archive-first eviction of the three superseded feedback memories tied to #357 (plus their three index lines and the codification-ledger row), post-state 3/3 PASS, with the verbatim pre-deletion archive preserved on the Stage 12 sub-task #602. Stage 5/7/8 SKIP per the approved stage-applicability matrix; both tickets marked closed at Stage 13 with reconciled per-AC evidence (stale paths in the migrated AC text reconciled in-plan: the deployed-workspace guardrail file → the tracked template; the legacy runtime-rules path → the tracked rules module). Milestone description amended at the gate (D-Scope A — the third value item belongs to #429 through its own Triage; sizing corrected to ~8 pts across 2 issues). The 2026-05-23 intake-pre-rendered-artifacts pattern was confirmed as a second instance and promoted (N=2 — already-satisfied issues take the verification-only delivery route). Out-of-scope discovery R-1 (the deploy-managed rules mirror absent at the operator instance) filed as #633 at close entry. 30-day Stage 13 outcome window per the `routine` class.

## Operations capability — Intake

### intake-elicitation-skill (2026-06-06, version-less) — Conversational intake front door (`intake-desk` — meets an idea at any altitude, types it, elicits the right fields, applies the 5-test live, and logs a typed item — never a scratch file)

Single-issue release delivering a new operations-module skill, `intake-desk`, the conversational front door for intake; `novel` release class, CHEAP reversibility, single-branch (D-C SINGLE), **version-less** (operator decision — no `vX.Y`, no git tag, no GitHub Release; `.version` stays `v3.19`), following the `public-flip-install-blockers` precedent. Before this release intake was cold form-filling against static templates with no funnel — a gap that had already produced an improvised scratch file committed in the wrong place. `intake-desk` turns a half-formed idea into a well-formed, correctly-typed, correctly-placed work item logged to the work tracker. It meets the idea at its altitude by assumption-then-confirmation (the run-the-business vs change-the-business cut, then a specific level — initiative / story / task / bug — proposed as a labeled assumption the user confirms or corrects; an unresolvable altitude is carried as a triage-owned `[ASSUMPTION – CONFIRM]` rather than stalling), proposes the work-item type and its place in the intake hierarchy, and holds the type provisional so it re-routes on reclassification (a "bug" that is really a missing capability becomes an `improvement`). It elicits the type- and level-appropriate field set — a bug's reproduction and environment, a story's acceptance criteria and value, an initiative's outcomes and domain — with the required fields derived at use time from each `.github/ISSUE_TEMPLATE/<type>.yml` (the living source) rather than duplicated, selected via a BABOK-grounded domain-adaptive technique library, and applies the 5-test rule live as the clarity gate (the stop condition is the 5-test passing for the type at the altitude, not a question count). It captures one work item per request — a container's child work is a body decomposition callout for later slicing, never auto-created — and emits to the configured work tracker (GitHub Issues MVP via `gh issue create`) only after a binary AskUserQuestion confirm, carrying required structured fields (a bug's Severity, an improvement's Category) via labels or a labeled first body line and falling back to the observation tier when a field cannot be represented; unresolved unknowns are emitted as stage-owned `[ASSUMPTION – CONFIRM]` handoff items. The skill has no write path to a tracked file — the originating defect it exists to close. Two modes: Elicit (guided interview) and Triage-readiness check (run the 5-test against a draft). The MVP binds to the current three work-item types (`improvement` / `bug` / `observation`; no `adr` — authoring an ADR is an architecture act, not intake) via a parameterization seam the work-item type system later repoints with no SKILL.md rewrite; the elicitation-technique content is sourced to IIBA BABOK Guide v3 (the platform's first encoding of the requirements-elicitation domain, upgrading the Stage 4 SHIP-WITH-FLAG label to Mode A). ADR-016 records the intake-front-door architectural boundary (verb-disjoint vs ppm-agent / project-initiator / architecture) and the downstream owned-assumption handoff contract. Single release PR (#424) merged to main with green CI (17/17); Stage 7 DT found 5/5 ACs PASS and Stage 13 re-confirmed 5/5 against merged main, with `deploy.sh --check` Checks 5/6/7 OK for `intake-desk`. No tag, no GitHub Release (version-less). 30-day Stage 13 outcome window per the `novel` class. Out-of-scope follow-ups enriched at Stage 5 rev-2: target-tracker parameterization (#384 / #409 / #383), downstream assumption investigate-and-close (#427), root-cause guidance (#428), migration playbooks (#429).

## v3.* — Automation & Enforcement

### v1.13 (2026-06-13) — Runtime test gating + the platform's first standing regression corpus: runtime code tests run in CI and gate releases (#319), code-test outcomes are machine-readable in the pipeline (#430), and a standing sandboxed install/onboarding/update regression suite gates releases (#706). Re-versioned v1.12 → v1.13 (concurrent corpus-durability-enforcement claimed v1.12); deploy no-op

3-issue release (`cross-cutting` class, CHEAP reversibility, single-branch D-C SINGLE, serialized #319 → #430 → #706) making runtime code tests a first-class gated surface at Stages 6–7 and standing up the platform's first standing regression corpus. **Re-versioned v1.12 → v1.13:** the release was planned/branched/committed/PR-titled as `v1.12` (branch `release/v1.12-runtime-test-gating`); the concurrent `corpus-durability-enforcement` release (#316/#311, PR #748) claimed `v1.12` first (its tag + RELEASE_LOG row landed ahead), so the canonical version is **v1.13** — the signed `v1.13` tag points at this release's merge SHA `fba3df4`. PR #747's title and the merged commit messages retain the immutable "v1.12" (a force-push to rewrite them is blocked + inappropriate) and stand as the as-authored historical record; the v1.13 RELEASE_LOG row + the v1.13 tag are the version of record. The release class was re-classified novel → cross-cutting at the Stage 4 C8 scope expansion (trigger (c) — three in-bundle compositional edges #319→#430 / #319→#706 / #430→#706); posture Tight engagement density · Deep Stage 9 review · Stage 5 activation ALL · 30-day Stage 13 outcome window. Before this release runtime code tests were not gated and their results were not machine-readable, the committed deploy/hook tests rotted outside CI with a stale hardcoded assertion, and there was no standing regression corpus so install/onboarding regressions could land undetected through a release. The release closes all three. **#319 (test-suite rot — hook/deploy tests under CI):** the eight committed hook tests (`core/hooks/tests/*.test.sh` + their `test-runner.sh` harness) ran in no CI workflow and failed in the source tree because they depended on the deployed runtime layout (co-located token-resolved allowlists + a `.mode` file); the release makes the hook suite portable + CI-gated via a NEW `core/hooks/tests/setup-ci-layout.sh` shim and four refreshed hook tests (`block-destructive` / `block-egress` / `block-shell-injection` / `block-skill-direct-edit`) wired into `.github/workflows/install-tests.yml`, so a PR that breaks a wired hook test fails CI regardless of checkout location, and it makes the composition manifest-count assertion in `core/deploy/tests/test_lib_composition.sh` manifest-derived rather than a hardcoded literal that re-rots when the manifest grows. The Stage 5 AC-currency reconciliation (G-PL1 reconcile-don't-annotate) had already confirmed two of the issue's three original defects FIXED on main and the third a phantom, so the build targeted the still-live core. **#430 (runtime code-test gating + machine-readable `test-run` tracking):** runtime code-test suites become a first-class gated surface at Stages 6–7 — a NEW `release/references/standards/runtime-suite-selection-map.md` documents the changed-code-path → gating-suite mapping, `release/references/pipeline/stage-06-engineering.md` + `stage-07-dev-testing.md` gain the steps that select and run the mapped suite, `release/references/standards/pipeline-event-log-schema.md` adds a `test-run` event type with the DT↔QA handoff carrying the test-results block, and the event-log writer/reader pair `release/tools/append-pipeline-event.sh` + `release/tools/query-pipeline-event.sh` was repaired so `test-run` events emit and read back (this repair resolved the dedup survivor #590 — the extinct `pmo-platform/engineering/` path that made Surface B emission impossible — which closed on the fix); `release/references/pipeline/stage-09-plan-review.md` was updated for the new gated surface. **#706 (standing install/onboarding/update regression suite):** a NEW sandboxed CI-gated suite `core/deploy/tests/run-install-regression.sh` + `core/deploy/tests/test_upgrade_config_durability.sh` (wired into `install-tests.yml`) productizes the pre-public-flip QA sweep (Suites A–H under a `HOME`→`/tmp` sandbox: a fresh sandboxed install, post-install token-substitution + file-link/reference integrity, two-phase deploy success, and user-config durability across a simulated version upgrade), deterministic scoring (no judge layer), reusing the existing CI test harness as the regression floor — the most build-ready slice of the #325 regression-suite decomposition (the seed corpus already existed). The #325 behavioral / judge-graded scenario engine (tickets C2–C5) and bring-your-own scenarios (C7) stay out of scope and sequenced to a later #325 slice. **Deploy no-op:** PR #747's 17 changed paths are confined to `core/hooks/tests/` + `core/deploy/tests/` + `.github/workflows/` + `release/references/**` + `release/tools/**` (plus the Engineering-Commit-0 plan) with zero `{core,operations,release}/skills/` / `packages/` / harness entries, so `./deploy.sh --deploy` had nothing to deploy (git-native release; the merge activates the new CI gates + pipeline behavior on main). Dev Testing PASS + QA ACCEPT across the three issues; single release PR (#747; 17 tracked files) merged to main with all 22 CI checks green — including the gates contributed by the two concurrent releases (cross-reference integrity, intra-repo link verification, skill-count/IMP-XXX delta, durable-corpus fragile-reference delta) plus the new hook-test / install-regression / shell harnesses this release wires in — after the Stage 12 / Stage 9 Phase A6.5 pre-merge sync (`c09fbd1`, "sync main (concurrent cross-reference-integrity-ci release) pre-Stage-12") merged the concurrent release into the branch and re-validated this release against the concurrent releases' new gates at 22/22. All three issues #319 / #430 / #706 closed COMPLETED at the release-PR merge (2026-06-13 17:36 UTC), plus #590 closed by the #430 event-log path repair. 30-day Stage 13 outcome window per the `cross-cutting` class. Fast-follows filed at close: #758 (extinct-path resolution in the release-analytics tools — compute-cycle-time / synthesize-release-learnings — + `OPERATOR_INSTANCE_EVALS_RESULTS_PATH` codification) / #760 (`check-doc-links.py` hardcodes repo-root containment — cannot validate a sandboxed/deployed tree). Process improvement filed: #769 (Stage 12 / A6.5 pre-merge must verify the version number is still unclaimed, not just file/gate divergence — the gap that produced this release's v1.12→v1.13 collision).

### cross-reference-integrity-ci (2026-06-13, version-less) — Cross-reference & reference-durability CI: the pre-commit hook's reference-integrity enforcement is now mirrored into three file-disjoint PR-time CI gates (positional `#N` parity + link-check + forward IMP-XXX / skill-count drift), warn-mode-initial

3-issue release hardening the platform's PR-time reference-integrity enforcement across three file-disjoint CI surfaces; `novel` release class, CHEAP reversibility (single `git revert -m 1` of release PR #745; all deliverables additive CI/tooling), single-branch (D-C SINGLE, the plan committed as Engineering Commit 0, serialized per-issue commits in the #314 → #169 → #130 order), **version-less** (operator decision at the Stage 12 gate — no `vX.Y`, no git tag, no GitHub Release; `.version` unchanged), following the `public-flip-install-blockers` / `intake-elicitation-skill` / `domain-aware-stage5-design` / `memory-to-corpus-codification` precedents. Before this release, outbound-reference integrity was a one-shot detection run by hand, the reference-durability CI check was position-blind versus the pre-commit hook (under-enforcing at flip-to-enforce), and the v11.01e imp-references dataset was unused for drift detection. The release closes all three. **#314 (reference-durability positional `#N` parity):** the position-blind CI existence check is replaced by a true line-position classifier extracted to NEW `core/hooks/lib/positional-issueref.awk` and invoked by BOTH the hook (`core/hooks/block-fragile-refs.sh`) and `.github/workflows/reference-durability.yml`, closing the one divergent shape (a self-describing `#N` in body prose with the reference block BELOW it) where the hook flagged `OUTSIDE-BLOCK` while CI passed; the divergence fixture (a FLAG + CLEAN pair) is wired into `core/hooks/run-fragile-ref-fixtures.sh` + `core/hooks/testdata/cutover-fixtures.txt` (25 passed / 0 failed). Per the recorded deviations, the diff-hunk mapper was inlined in the CI step (no second lib — no current consumer; scope discipline) and `core/deploy/deploy.sh` was not touched. **#169 (link-check PR gate):** NEW `.github/workflows/link-check.yml` promotes the `core/deploy/tools/check-doc-links.py` primitive to a PR-time gate with `<OPERATOR_INSTANCE_*>` token-class exclusion in the engine and a repo-resident allowlist (NEW `core/deploy/allowlists/skip-doc-link-check-ci.txt`, since CI has no operator-instance allowlist); the 13 pre-existing genuine broken refs were source-fixed to 0 (baseline 221 raw broken-ref rows → 0 real residual over the #169 scope — 200 angle-bracket placeholders + 8 meta-doc literals skipped natively by the engine, 13 genuine residual fixed); the gate runs on PR open + push + a push-to-`main` post-merge guard, is the SAME primitive over the SAME `--target-paths` as deploy-time Check 14 (so the two verdicts cannot drift), and `core/rules/doc-link-maintenance.md` + `release/references/standards/release-notes-standard.md` document it as the standing PR-time protection. **#130 (forward IMP-XXX / skill-count drift CI, re-scoped C+D per D-130-Rescope):** NEW `core/deploy/tools/check-skill-count-imp.py` (detector) + NEW `.github/workflows/skill-count-imp-check.yml` (added-lines-delta workflow), new files only (zero edits to existing files); the absent `imp-references.tsv` — confirmed absent from the working tree AND the entire git history — made the literal TSV-consumer ACs dead-on-arrival, so the issue was re-scoped to forward-looking CI (no historical artifact needed) and the routing-conflict-detection portion split to its own issue #704 (OPEN, correctly off milestone #112); added-lines-delta scoping is confirmed end-to-end, so pre-existing corpus rot and CLOSED-issue history structurally cannot fire. Stage 8 QA returned binary ACCEPT/PASS on all three (sub-tasks #697 / #698 / #699): #314 all 3 ACs PASS (the modified-file-diff boundary maps to true file lines and flags `OUTSIDE-BLOCK`; hook ⇄ CI byte-identical), #169 7/7 ACs MET (broadened exclusions mask 0 real broken refs across a 178-file population audit; AC #6's stale "~50 per X1" surgically reconciled to the true 221→0 figure), #130 both salvageable ACs MET with the F1/F3/F4 remediation holding at AC level (drift fires 11/11, 0 over-fire across 17 legit framings, live IMP-099 fires with precise path-scoping). Single release PR (#745; 14 tracked files) merged to main with 21/21 CI green (19 PR gates + CodeQL); near-no-op deploy (zero changed skills/packages/harness — `deploy.sh --deploy` reported nothing to deploy); primary fast-forwarded to the merge SHA `5ebec77`. All three issues #314 / #169 / #130 marked closed at Stage 13 on the Stage 8 verdicts + merge SHA + the live warn-mode-initial gate per issue. **Check 14 flip-to-enforce assessment (first release after the link-check merge, per `doc-link-maintenance.md`):** keep all three new gates at warn-mode-initial through the standard 2-3-release shakedown window — they ship green at 0-baseline with no warn-log history yet; flip-to-enforce deferred with rationale (explicit per the timeline's no-silent-deferral rule). 30-day Stage 13 outcome window per the `novel` class. Outbound roadmap edge: issue #130 Blocks #235 (OPEN, milestone #197, Later horizon). Pre-flip-to-enforce hardening (the #130 bounded-recall + #314 cosmetics) filed off-milestone at this close.

### v3.20 (2026-06-07) — Release-corpus verification surface (real checks, not grep proxies): corpus tools rewired to the live layout + fail-loud deploy-checks; ≥90% parser; live-taxonomy labels

5 issues replacing grep-proxy release-corpus checks with real verification and closing the systemic "green-on-broken" deploy-check failure mode; `novel` release class, CHEAP reversibility, single-branch (D-C SINGLE), shared-release-PR append model. The two release-corpus tools were hardcoded to pre-restructure `pmo-platform/...` paths and passed vacuously against the live `core/`/`release/` layout — `lint_release_corpus.py` (Check 20) now lints the real `release/releases/notes/` directory with the stale v11.18 cutover-floor exemption removed (11 live notes were going unchecked), and `generate_release_index.py` (Check 23 `--verify`) now exits non-zero on a path-resolution failure instead of exiting 0 on a dead path (#83); the `#85` Scope-column `#N`-token miscount is moot by construction since the live INDEX carries no Scope column (6-column schema), and the generator was retargeted to it. The fail-loud-on-path-error contract was generalized across the rest of the deploy-check family (`deploy.sh` exits non-zero on path-resolution failure — no more green-on-broken), with a confirmed pre-restructure old-path default repaired in `check-version-anchors.py` and `check-doc-links.py` rewired in the same pass (#459). `release/tools/bundle-issues-parser.py` (the `release-planner` Mode A engine) was hardened against the dominant substrate failure modes — field-name heading-alias heterogeneity and legitimately-absent optional sections previously scored as parse failures — lifting combined-clean from a sub-target baseline to the ≥90% Mode A acceptance target on the live conformant-bundleable substrate, locked by new fixtures (`conformant_bundleable_sample.json` + `test_bundle_issues_parser.py`) (#291). `release-executor` Mode G's Pattern-Review file-new step was retargeted off a non-existent `category:` label to live-taxonomy labels only (Triage-applied type label; Category in-body), fixing a latent `gh issue create` failure (#425); `release/skills/release-executor/SKILL.md` + `release/tools/automated-closeout.sh` updated and both skill packages rebuilt. The plan carried D-class re-scope decisions for #83/#85/#291 after Stage-4 verification found the original ACs overtaken by the modular-monolith restructure (the `novel` trigger), warranting a Deep Stage 9 review for the deploy-check blast radius. Single release PR (#496) merged to main; signed-annotated tag `v3.20` at the merge SHA; the PR also carried a prior-release v1.03 RELEASE_LOG/RELEASE_INDEX backfill (historical provenance unrecoverable post-re-versioning, recorded as a backfill). 30-day Stage 13 outcome window per the `novel` class. The INDEX regenerate is the first production dogfood of the FM-1 recent-first sort fix — 11 rows, v3.20 at top, v1.03 retained.

### public-flip-install-blockers (2026-06-04, version-less) — Fresh-install & onboarding blockers gating the private→public flip (zero-skills fix + no-Cowork deploy + config-first install path + tamper-aware update)

15 issues (14 engineered + 1 verified no-op) closing the fresh-install / onboarding blockers that gate the private→public flip; `novel` release class, CHEAP reversibility, single-branch (D-C SINGLE), **version-less** (operator decision — no `vX.Y`, no git tag; `.version` stays `v3.19`). The headline launch blocker was that a fresh `./install.sh` deployed **zero skills** — Phase 2 used release change-detection (an empty diff on a clean clone) instead of a full-roster deploy; bootstrap now deploys the complete roster (#606), and manual mode installs `.skill` packages (#144). A config-first `detect_install_path()` resolution ladder (ADR-013) reads `operator.toml`, emits structured terminal output, and removes the hardcoded fallback session-path + literal session UUID that could reference an orphaned session (#233, #243); a `COWORK_AVAILABLE` flag-guard makes a session-less machine deploy the user-local skill roster instead of hard-failing, so a no-Cowork install reaches exit 0 with the full roster rather than exit 1 / 0 skills (#607). The documented sandbox overrides (`--workspace-root` / `--config-root`) are now honored through Phase 2 skill deployment, not just Phase 1 (#611). `update.sh` gains two-hash managed-section tamper detection with the documented tamper backup (ADR-014, #612) and a wired `EX_NOCHANGE` exit 64 (#613). `validate-install.sh` stops false-positiving on legitimate `CLAUDE.md` vocabulary (A5, #608) and checks the `$HOME` install skills path rather than the workspace path (A9, #609). `deploy.sh --check` no longer false-DRIFTs on sync-map-injected references and `--warn` exits 0 (#610); the `--report` continue-past-skill-FAIL behavior was already present from the earlier re-version and was carried as a verified no-op (#265). The `deploy.sh` SKILL_LIST count and the skill-deployment "custom" count are reconciled (#242); `.version` is reconciled from `v1.04` to the latest-tag v3.x scheme with the public-flip tagging convention documented, independent of this version-less release (#614); the composition-surface count drift (14↔17) is reconciled and CI install-tests now run the full suite rather than 2 of 5 (#615). Folded in as a post-GO fast-follow: `notify-version-skew.sh` was non-functional (a `REPO_ROOT` path bug) and unwired — it is repaired (2-candidate `.version` resolver, snapshot shipped by `setup-workspace`/`update`, `SessionStart` wiring via `core/settings.json.template`, regression test in install-tests CI), making #614's reconciliation functional and superseding the standalone main-targeted PR (#632). Single release PR (#627) merged to main with green CI; DT/QA ran 8/8 test suites (72 assertions), full acceptance matrix PASS, security strengthened by the #612 two-hash tamper detection. No tag, no GitHub Release (version-less). 30-day Stage 13 outcome window per the `novel` class.

### v3.19 (2026-06-03) — Stage 13 close-out reliability backstops (completeness gate + outcome-bound mandatory close + tooling smoke gate + slug/Keychain hardening + working orphan `--apply`)

5 issues hardening Stage 13 Close so a release can no longer close incomplete and the close-out tooling can no longer rot unexercised; `automation`/`process-protocol` cluster, CHEAP reversibility, additive warn-mode-initial tooling + protocol text across `deploy.sh`, GitHub Actions CI, the Stage 13 spec, the hub-spoke bridge, and two `release/tools/` scripts. A `deploy.sh --check` Check 32 release-corpus-completeness gate iterates every RELEASE_LOG row and asserts its matching INDEX row + DIGEST entry + NOTES file exist — the mechanical backstop converting the documented-but-skippable "Appended at Stage 13" convention into a guard, after the canonical incident closed a release with only the LOG row (#576). The close-out completeness mandate is reframed outcome-bound: a release MUST close with its complete enumerated output set verified on main (the Procedure 7 Step 4 Verification table is the canonical set), with the automated close-out the mandated mechanism and the documented Phase B chore-PR the named fallback when preflight legitimately blocks; hand-assembling the corpus row-by-row is prohibited, and an operator merge-ahead is an explicit supported close path that does NOT waive the close outputs (#581, reworked to outcome-bound per adversarial review at #598). A `release-tooling-smoke.yml` CI gate runs `automated-closeout.sh --self-test` + `--check-paths` (both offline) on any PR touching `release/tools/**` or the RELEASE_LOG schema, plus a precision probe proving the path gate FAILS on a deliberately-broken corpus path — so the exact rot that hid stale paths / a stale column index for several releases fails the PR instead of the next close (#583). `automated-closeout.sh` gains a schema-aware `extract_milestone_slug` (returns the field-2 slug on the 8-column LOG, not the field-1 version; self-test covers 8-col + 5-col) and a locked-Keychain credential fallback (`GIT_TERMINAL_PROMPT=0` + a `gh auth git-credential` helper) so a headless/locked-Keychain session degrades gracefully instead of hanging (#584). `cleanup-orphan-state.sh --apply` is wired to actually execute git-porcelain removal (the `REMOVE` action ran as a no-op before), expands `--release-close <slug>` detection to include the sibling `chore/<slug>-*` Stage 12/13 corpus branches, prunes stale remote-tracking refs via `git remote prune`, and adds a post-apply verify that reclassifies any survivor to `SKIPPED — survived apply` rather than silently claiming success (#556). Merge-ahead topology — no single release PR; the five backstops landed via four direct-to-main PRs (#588, #591, #592, #598) with green CI, closed via the documented Phase B chore-PR fallback. This release is the first reflexive consumer of its own repaired close-out automation, validated (`--self-test` + `--check-paths` PASS on main) on its own merge-ahead close. Tag `v3.19` + GitHub Release backfilled at the operator authorization gate.

### v3.18 (2026-06-03) — Corpus-integrity enforcement (reference-durability standard + three-primitive spine + three PR-time integrity gates)

2 issues shipping forward-prevention corpus-integrity enforcement; `cross-cutting` class, MODERATE reversibility, additive warn-mode-initial tooling across the harness-hook layer, `deploy.sh`, and GitHub Actions CI. A reference-durability standard codifies a durability ladder plus a self-containment test, enforced forward-only by three primitives — a PreToolUse hook (`block-fragile-refs.sh`), a `deploy.sh --check` check (Check 31), and a CI backstop (`reference-durability.yml`) — with the Class-L/V regex byte-identical across all surfaces and a checked-in 23-case fixture gating Class-V precision so it cannot silently regress (#538). Three PR-time repository-integrity gates (`repo-integrity.yml`) add depersonalization (operator/collaborator PII in the C6 governance domains, added-line-scoped, exempting intentional-identity files and release-tracking files), issue-reference validity (resolution + designated-block placement, PR-number and transferred-issue aware), and dead-file references (#410). Every gate scans the net-new added-line delta — pre-existing rot is out of scope (forward-prevention only). Single branch topology; one release PR (#562); merged ahead of the formal Stage 7–9 / 12 gates by operator decision with all 11 CI gates green. Three scope boundaries surfaced and were fixed by real use during build and close-out (whole-file → added-line, C6-domain scoping, `release/releases/` tracking exemption at #574); one positional-consistency fix deferred (#572). Untagged.

## v1.* — Pipeline Discipline

### v1.11 (2026-06-12) — Cleanup-orphan-state reliability: the workspace sweep gains a SELF guard, an all-process liveness gate (ADR-021), and a fixed-point apply — safe to run from anywhere, complete in one pass

4-issue release (#333 SELF guard · #326 liveness gate · #53 fixed-point apply · #57 VERIFY-ONLY) hardening `release/tools/cleanup-orphan-state.sh`, the destructive-action workspace sweep invoked at every Stage 13 close; `novel` release class, CHEAP reversibility (single `git revert -m 1`), single-branch (D-C SINGLE, the plan committed as Engineering Commit 0, serialized per-issue commits in protective-gates-first order #333 → #326 → #53), signed-annotated `v1.11` tag at the merge SHA `38f97d31…`. Before this release the sweep could classify its own runtime worktree as removable (the running session's cwd met every REMOVE condition — demonstrated live at Stage 4), gated removals only on branch-name/merge/dirty heuristics with zero process-liveness logic, and left branches freed by same-run worktree removals for a second invocation (apply ordering structurally guaranteed the residual). The release closes all three: #333 adds the **SELF action class** (self-cwd computation + primary-clause robustification + worktree dedup guard; the script's own runtime worktree reads `SELF — script's own runtime worktree (protected)` in both emitters and is apply-inert including under `--force`); #326 adds the **all-process `lsof` cwd liveness oracle** (absolute-path binary, self-canary, fail-closed with a blocked-removals counter when the oracle is unavailable) with the live clause at precedence slot 3, apply-time re-verification seconds before each removal (scope-lock amendment A-Recheck), and a `liveness_oracle` JSON field — oracle selection recorded in NEW `release/ADRs/ADR-021-liveness-oracle-selection.md` (Accepted at the 2026-06-11 scope-lock; documented cwd-vs-fd boundary residual with a 30-day re-triage criterion toward the all-fd predicate); #53 adds **`resolve_freed_branches`** — ONE bounded re-evaluation pass between apply and verify (freed set derived from REMOVED worktree rows; unrowed `--historical` branches fresh-classified; SELF/live/dirty-skipped worktrees keep their branches attached) so a single `--apply` reaches fixed point. #57 (the v1.02 apply-path dead-code defect) was dispositioned VERIFY-ONLY at Stage 4 — the hardening already shipped in the seed tree — and was marked closed at Stage 13 with the three-link evidence comment (Stage 4 disposition at `eecbb5e` + Stage 7 fresh audit at `25960aa` + Stage 8 acceptance at `f2b3152`, 4/4 ACs MET) per D-VerifyOnly-57. The self-test grows three end-to-end fixtures (SELF guard / synthetic live holder / fixed-point apply) on the preserved apply/verify/prune guards and passed at every commit on the branch (~10 s budget accepted per R-8); QA executed three hub-authorized fixture-bounded REAL applies (SELF `--apply --force` survival; live-held before/after with both oracle scans observed; the #53 defect-scenario fixed-point), and Stage 12 ran the post-merge live-workspace dry-run regression (own worktree SELF, two live sessions SKIPped, exit 0) plus `--self-test` PASS on merged main. The two invoker docs updated to the four-phase apply vocabulary (`core/rules/git-workflow.md` § Step 10 + `release/references/pipeline/stage-13-close.md` Phase C4, which also simplifies the Stage 13 choreography: detach-before-apply now matters only to release the release branch for same-run draining). Single release PR (#668; 6 tracked files) merged with 18/18 CI green after a clean divergence merge of 16 v1.10-close-out commits (zero matrix overlap); #333/#326/#53 auto-closed at merge. Dogfood guard honored: this release's own Stage 13 cleanup runs the NEW script with halt-revert-rerun on misbehavior. 30-day Stage 13 outcome window per the `novel` class. Completing the milestone unblocks the deploy-decomposition wave (#341, #45, #44, #43, #42) and #257. Follow-ups routed: #654 (worktree-agent-* sweep coverage) / #655 (gh-call batch-prefetch) / #656 (protect-list install seeding) / #659 (allowlist invocation forms, DL-8) / #669 (--help usage window) / #670 (duplicate branch rows under --all) / #679 (append-pipeline-event.sh pre-restructure path).

### v1.10 (2026-06-12) — Failure-mode coverage completion across all five categories: 45 fresh-authored domain-specific entries take all 23 skills to 100% coverage in TRIG / INPUT / PROC / OUT / HAND, with version-field + packaging hygiene riding along

5-issue release (#6 OUT · #140 INPUT · #139 PROC · #141 TRIG · #138 HAND) completing domain-specific failure-mode coverage across the entire skill catalog; `novel` release class, CHEAP repo-level reversibility (runtime MODERATE — re-deploy at reverted HEAD), single-branch (D-C SINGLE, the plan committed as Engineering Commit 0, one Engineering chip at a time in the approved OUT → INPUT → PROC → TRIG → HAND small-to-large sequence), signed-annotated `v1.10` tag at the merge SHA. Before this release every one of the 23 skills was missing at least one category — coverage stood at 52.2 (TRIG) / 78.3 (INPUT) / 78.3 (PROC) / 82.6 (OUT) / 65.2 (HAND) percent, with the boundary categories (TRIG + HAND) the catalog's biggest authoring blind spot. The release lands **45 fresh-authored entries** (35 PRIMARY + 9 TRIG SECONDARY + the operator-included comms-writer send-boundary HAND SECONDARY per adversarial CD-2) taking all five categories to 100% on the live-23 denominator and the catalog entry census from 102 to 147; both high-complexity expansion targets hold (pmo-process-designer and pmo-technical-analyst at 6 entries / 5 categories each). This was a fresh-authoring release — the source audit's per-entry drafts were unreachable post re-versioning (12 one-liner seeds survived, covering 8 of the cells) — so every entry was Stage-5-designed against the skill's live operational surface, adversarially reviewed by five structurally-independent reviews (zero Blockers; Minor/Cosmetic deltas applied as Stage 6 text edits; one Major — the weekly-status-rollup Check 6 size-threshold breach at the final commit — disposed at Collective Review as `references/` adjacencies on BOTH crossing files), and Stage-8 QA-graded per issue with G7-06 LLM-graded domain-specificity means ≥ 4.0 on every modified file. The canary's two new entries (OUT + HAND) stay inside the ADR-04 fixture boundary per D-CanaryScope. Hygiene rode along per plan R6: every deployed modified skill bumps `version: v1.10` (canary `v1.10-canary`), the three core-module skills (eval-writer / pmo-qa-auditor / prompt-builder) ADD the previously-missing `version:` field, and NEW `references/` docs land for project-initiator (scaffold-output verification) + weekly-status-rollup (roll-up input-coverage checklist). Single release PR (#658; 26 tracked files, +1972/−24) merged with full CI green after a clean divergence merge of 9 v1.09-close-out commits (zero matrix overlap); Stage 12 Phase H deployed 22 skills + 22 rebuilt `.skill` packages to both runtime targets with 0 failures (packages committed via chore PR #671; RELEASE_LOG row via #672), working around two pre-existing deploy-toolchain defects in-mechanism (BSD-sed alternation no-op in change auto-detection; stale 21-name package-builder roster). All five parents auto-closed at merge; #288 unblocked downstream. 30-day Stage 13 outcome window per the `novel` class. Follow-ups filed at close: #673 (Check 6 red-on-main detection gap — also records the Stage 12 toolchain discoveries as bundling candidates) / #674 (release-executor Dry-Run-Record PROC entry git-native mis-route) / #675 (blast-radius.sh locale pin) / #676 (canary § Skill 12 contract reconciliation) / #677 (catalog-wide stale-path sweep, 15 files).

### v1.09 (2026-06-11) — The agent-to-script promotion framework (when repeated agent work earns a script, and what a script owes once it exists): the AS0–AS4 ladder (ADR-020) + six governed decision-table sections, grounded in an executed 71-script census + ranked 8-candidate inventory

Single-issue release (#187) landing the platform's governed answer to script-promotion decisions; `novel` release class, CHEAP reversibility, single-branch (D-C SINGLE, the plan committed as Engineering Commit 0), signed-annotated `v1.09` tag at the merge SHA. Before this release the script estate followed at least five divergent governance patterns (deploy tooling without rule IDs; hooks with rule IDs + allowlists + warn→enforce shakedown; skill-bundled eval scripts; checkpoint primitives; ungoverned operator-instance scripts), with no inventory of what was scripted versus agent-executed and no rule for when a repeated pattern earns scripting. The release closes that gap with three dependency-chained deliverables. The public deliverable is the NEW tracked standard `core/standards/agent-script-promotion-framework.md`: the form-anchored five-rung **AS0–AS4 promotion ladder** (agent procedure → documented command → tracked tool → checkpoint-wired → autonomous guard; establishing `core/ADRs/ADR-020-agent-script-promotion-ladder.md`, **Accepted** at the 2026-06-10 scope-lock) with a deterministic caller-type AS2/AS3 boundary test (AS3 iff the invoker is another governed executable — the Stage 13 close-out orchestrator is the named canonical boundary case), the mixed-determinism rule (judgment-class steps split-promote their evidence-gathering substrate only; scripting judgment end-to-end is a named failure mode), six governed decision-table sections (§1 five promotion triggers + five do-NOT-promote counter-signals including redundant-promotion; §2 placement-by-invocation-surface + the REQUIRED promotion decision line + the PR-bound discovery anchor; §3 per-rung testing standards; §4 per-rung drift signals + the audit re-run trigger; §5 point-of-use citation discovery — no new SKILL.md frontmatter field per D-2, upstream verdict `aligned`; §6 versioning/deprecation with demotion as a governed change), four failure modes, and worked examples SP-1 + SP-4 inlining the executed inventory's substance. The grounding deliverables landed operator-instance per D-AuditPlacement: the 71-script census (9 coded columns; path-set-identical to the tracked-tree glob at the audit SHA; runtime-mirror identity confirmed — zero divergence after declared exclusions) with the 22-skill mechanical-vs-judgment segmentation derived from the live deploy arrays, and the ranked 8-candidate opportunity inventory (4 READY / 2 NEEDS-STABILIZATION / 2 WATCH under the D-5 readiness-gated lexicographic ranking with the FMF-5 comparator; #287 mapped → SP-1, #286 mapped → SP-2 as the skill-mode-vs-script boundary case, #234 documented-excluded on zero live roster occurrences per its pre-decided rule). Registration: one INTERNAL `core/specs/framework-catalog.md` row (Check 18 exit 0 at Engineering AND re-run PASS post-merge); the four skill-creator-sourced `upstream-reference-catalog.md` entries re-verified + bumped per approved-plan recommendation 4; both OPERATIONS.md mirrors untouched per the ratified D-4 (risk R6 retired). All 8 adversarial dispositions from the Stage 5 scope-lock folded and QA-verified landed (5 corrections + 3 adopted counter-designs: CDF-1 caller-type test, CDF-2 readiness-class-exempt cap, CDF-3 PR-bound discovery anchor). Stage 7 DT skipped per the applicability matrix (no executable tooling; the conditional re-activation clause correctly did not trigger); Stage 8 QA independently re-verified all 10 reconciled ACs PASS (counts re-derived live; census set-identical 71=71; segmentation name-for-name vs the live arrays); Deep Stage 9 review (`novel` class) under the Standing-GO model. Single release PR (#636) merged to main (6 tracked files, +827/−8) with 17/17 CI green; the audit + inventory are outside the diff by design. 30-day Stage 13 outcome window per the `novel` class. Post-release intake queues per the scope-lock disposition (none filed in-release): the 4 READY candidates 1:1 per G-Hybrid; the catalog `canonical_doc` reconciliation + km-governance count narrative as one item; the ADR-019 README index gap; the shelfware/guard-visibility findings.

### v1.08 (2026-06-08) — The skill-suite architecture spine (decomposition axis + factory guard): a binding decomposition rule (ADR-019 — Specialists compose, not absorb) + the audited + codified skill↔pipeline alignment standard (DT-1/2/3) + a fail-loud Anthropic-scaffolder-drift guard in `pmo-skill-refiner`

3 issues landing the skill-suite architecture spine; `novel` release class, MODERATE reversibility at the release level (governance + tooling, additive), single-branch (D-C SINGLE, shared-release-PR append model), signed-annotated `v1.08` tag at the merge SHA. Before this release the suite's decomposition axis was an open question (role-named skills colliding with the existing function-named skills, e.g. Release Manager ↔ release-planner+release-executor, Principal Engineer ↔ pmo-technical-analyst), skill modes fragmented against the 13-stage pipeline with no governing standard, and the `pmo-skill-refiner` factory could silently degrade when its wrapped Anthropic `skill-creator` scaffolder changed format. The release closes those three gaps. #406 establishes the **binding decomposition rule** in the new establishing `ADR-019-specialists-compose-not-absorb.md` (records the operator-adopted 2026-06-06 decision that role-named **Specialists COMPOSE shared function-skills, not absorb them**, and a role MAY span several Specialists split per the skill-boundary test — distinct trigger surface AND distinct write-scope AND distinct primary role; `status: Proposed`), citing the existing **cascade rule C1 (depth bound: max 2)** at `agent-handoff-framework.md` plus the Skill Chaining Protocol (C1–C7 + the 4-skill allowlist) as the composition substrate a thin-Specialist-composes design already relies on (routing depth stays ≤2 by construction; the ADR coins no new bound), with a forward-reference to #2 as the reconciliation venue rather than a hard ordering dependency. #2 lands the **skill↔pipeline alignment standard** (`skill-pipeline-alignment.md`, NEW) — the codified output of the alignment audit, with decision-tests DT-1/DT-2/DT-3 — cross-linked into `execution-framework.md` and the `core/governance/OPERATIONS.md` + `operations/OPERATIONS.md` mirror pair; its analysis/recommendations registers are operator-local read-once artifacts, not tracked deliverables. #193 ships the **fail-loud factory guard** — a structure-assertion pre-injection check added to `pmo-skill-refiner` by extending `quick_validate.py` (NEW `test_scaffolder_drift_guard.py` covering it) that captures the expected skill-creator output skeleton and FAILS LOUDLY on mismatch, composed with the existing `upstream-reference-catalog.md` drift-check (re-verified) rather than a parallel compat-shim subsystem; #193's two other proposed mechanisms (a compat-shim layer and version-pinning) were flagged out as over-engineering for current state. The fourth milestone work item, #8, was a clean **Tier-0 Premise Rejection** (C3 premise-invalidation): ADR-012 had already de-scoped *tracked* roadmap instances — instances are operator-local and the initiative-roadmap *framework* already ships in-repo — and removed `core/governance/roadmaps/`, so #8 carries no tracked deliverable; #193's native `Blocked by #160` was confirmed a roadmap-consolidation mislink (#160 is the unrelated Project-Data-Architecture initiative) and not a genuine blocker. Single release PR (#570) merged to main (9 files · +1067/−10) with all 24 CI checks green; Stage 7 Dev Testing PASS + Stage 8 QA PASS across the three surviving issues; Stage 9 GO (Deep review). 30-day Stage 13 outcome window per the `novel` class. Follow-ups spawned (reference, not closed by this release): SPA-001 census fix → #63; proof-of-mechanism remediation → #571. The role-skill build that consumes ADR-019 (the Specialist suite itself) is downstream and out of this release.

### v1.07 (2026-06-07) — The work-item type layer (methodology-agnostic, best-practice TEMPLATE: a thin generic `Work Item` entity beneath Milestone/Workstream + a domain-neutral work-organization mapping framework + a declarative type-pack meta-schema; decoupled from the release-pipeline ticket model)

3 issues landing the foundation of the work-item type layer as a methodology-agnostic, best-practice TEMPLATE; `novel` release class, EXPENSIVE reversibility at the release level (CHEAP pre-consumption), single-branch (D-C SINGLE, shared-release-PR append model), signed-annotated `v1.07` tag at the merge SHA. Before this release there was no work-item layer beneath Milestone/Workstream and no governed map from methodologies to a work hierarchy, so delivery work could not be typed, placed, or rolled up across methodologies. The release closes that gap on the hard #506→#532+#507 sequence. #506 (C1) establishes a **thin generic `Work Item` entity** — the hierarchy node — in `project-entity-model.md §4` (Entity Core 7 + a `work_item_type` discriminator + a `parent_ref` BELONGS_TO edge + relationships-by-reference) with the work-item→Milestone rollup edge in §5, ratified by the establishing `ADR-018-work-item-type-layer.md` (NEW; records the WITL D1-hybrid / D2-methodology-projected / D4-now-scope decisions from the #409 umbrella, renumbered from ADR-017 after a Phase-A6.5 collision with #537's ADR-017) and authorized by a Tier-2 SCOPE CHANGE rendered at the Deep Stage 9 GO — the one new entity admitted to the FROZEN roster, re-frozen with a dated amendment line mirroring the 2026-05-16 RAID-Item precedent — with the G2 cascade adding the generic-entity field schema + bounded X-rule to `entity-field-schemas.md §3/§4`. #532 lands the **work-organization mapping framework** (`work-organization-mapping-framework.md`, NEW) — the domain-neutral projection target: concept → hierarchy-by-methodology → best-practice work-item schemas / user plug-and-play. #507 (C2) lands the **declarative type-pack meta-schema** (`work-item-type-schema.md`, NEW) — methodology-projected kinds, built-vocab relationships, EAD-materialized per-type schemas — projecting onto #532's framework and resolving `work_item_type` against #506's entity, with `framework-catalog.md` registration, the `intake-desk` `type-map.md` seam generalized, and the reference-durability allowlist enabler. The architecture is K1 governance + K4 user config and is deliberately decoupled from the release-pipeline ticket model so a user brings their own work types (or takes a best-practice default) and an agent understands their work structure by nature. The skill-propagation refit (delivery-engine / ppm-agent / tracker-manager / weekly-status-rollup reading the registry at invocation) was held OUT to keep this a `novel` 2-spine release rather than a `cross-cutting` multi-skill one. Single release PR (#526) merged to main (12 files · +1070/−40); Stage 7 Dev Testing PASS + Stage 8 QA PASS across the three issues; Stage 9 GO (Deep review) with DT/QA findings folded in at Phase A6.5 (README 18-entity reconciliation, meta-schema version + F7 Test-kind-vs-artifact note, framework version pin). 30-day Stage 13 outcome window per the `novel` class. Deferred to later milestones under the #409 umbrella: C3 methodology breakdown · C5 propagation · C6 rollup/translation-fidelity · C7 storage/projection · C8 glossary · C9 migration.

### domain-aware-stage5-design (2026-06-07, version-less) — Domain-aware Stage 5/7 design (design no longer pmo-markdown-bound: keystone `domain:` field + governed design-exploration step + per-domain impact-analysis selector + domain-best-practice review criterion + the first two domain guides)

3 issues making Stage 5/7 design domain-aware instead of pmo-markdown-bound; `novel` release class, CHEAP reversibility, single-branch (D-C SINGLE, shared-release-PR append model), **version-less** (operator decision — no `vX.Y`, no git tag, no GitHub Release; `.version` unchanged), following the `intake-elicitation-skill` and `public-flip-install-blockers` precedents. Three independent mechanisms layer onto the existing §5.7 `domain_practice` provenance machinery on top of a shared keystone substrate built first. The keystone (#1) adds a `domain:` class field to the `domain_practice` label in `stage-04-planning.md` §5.7 across Mode A, Mode B, AND the pipeline-internal exemption, plus A3-time deliverable-domain classification from the File-Change-Matrix and the "already-encoded" exemption reconciled to reference the new domain guides (sourcing-exempt ≠ classification-exempt) — all three issues consume this one field. #1 ships the design-exploration protocol (`design-exploration.md`, NEW) — a Stage-5 Phase-A4 micro-protocol inserting divergent generation → convergent narrowing BEFORE the trade-off matrix, closing the gap that the design-review checklist demanded "≥3 alternatives" with no governed generation step — plus the first two per-domain best-practice guides (`core/standards/domain-best-practices/software.md` + `governance.md`, NEW), Applicability-Profile-bearing K1 guides governed by the four shipped corpus protocols (GoF / ADR-Nygard / Fowler for software with the mandatory ET5 paired contraindication; PMBOK7 / PRINCE2 / Nonaka SECI / Diátaxis for governance with the ET3 paired applicability note), wired additively into `stage-05-solutioning.md` (Phase A4 + §5.7) and the `hub-spoke-bridge.md` Stage-5 chip pattern, with framework-catalog registration (3 EXTERNAL + 2 INTERNAL rows). #345 adds a Phase A3.1 domain-aware impact-analysis method selector in `stage-05-solutioning.md` (admits non-markdown fan-out for code/component/solution domains and a documented opt-out, preserving markdown `blast-radius.sh` as the DEFAULT — additive, 0 deletions to the A3 mandate) with a grep-reproducible code-domain worked example in `blast-radius-protocol.md` §12 and a Section-1 accommodation in `design-review-checklist.md`; the executable code-fan-out instrument is deferred to a follow-up issue (spec-level method admission this release). #346 adds the domain-best-practice review criterion — `design-review-checklist.md` check 4.6 (assess against the target domain's authoritative practice, citing the external guides + `applicability-framework.md` §2/§3/§4, not internal conventions) and a `stage-07-dev-testing.md` Phase C conditional domain-practice conformance dimension with a `DOMAIN-PRACTICE-NOT-ASSESSED` honest flag — implemented stronger than the Stage-5 design by dropping the governance exemption short-circuit so a governance deliverable PASSes by full-walk assessment, not by bypass — plus the dimension-count cascade fix ("5 always-on + 1 conditional"). Single release PR (#503) merged to main (14 commits · 11 files · +648/−17); Stage 7 Dev Testing PASS (3/3, 2 fixed iterations); Stage 8 QA PASS (#1 6/6, #345 3/3, #346 3/3; zero FAIL); Stage 9 GO (Deep review) with hub Empirical Verification 8/8. No tag, no GitHub Release (version-less). 30-day Stage 13 outcome window per the `novel` class. Deferred follow-ups: #504 (pre-existing `deploy.sh` Check-18 stale catalog path) + #505 (domain-fan-out impact-analysis instrument).

### v1.06 (2026-06-06) — Pipeline-discipline capability slice (first fix-now/defer/accept finding-disposition framework for Dev Testing + QA, single-sourced via Option-C extraction; Stage-5 template BLOCK-DESTRUCTIVE-022 guard)

3 issues in the v1.* Pipeline Discipline arc; operator-rendered `novel` class at the Stage 4 Decision Gate (revised from the milestone's initial `routine` on a first-of-kind decision-weighting protocol), Deep Stage 9 review, GO-WITH-CONDITIONS (dimension-9 adversarial review accepted as inline-hub — the dedicated agent pends the unshipped #375 agent-layer carryover), CHEAP reversibility, additive doc/protocol edits across one new shared standard + two pipeline shards + a Stage-5 template. The dominant Stage 4 finding was staleness: all four originally-bundled issues predated the modular-monolith restructure, so #31 (mid-spoke cross-ticket scope detection) was deferred and re-homed to v1.22-hub-spoke-routing — its agent-definition AC is blocked on the unshipped #375 carryover — and the remaining three were reconciled (stale paths + dead issue numbers) to the current shards before Engineering. #220 created the platform's first fix-now / defer / accept finding-disposition framework: a corrected 5-factor matrix (three draft defects fixed — a double-counted factor demoted to a gate-modifier, an added deterministic tie-break, a closed structural hole), a two-tier decision logic (a hard severity-precedence gate before a weighted disposition), and a scoring skeleton; per the operator's Collective Review scope-lock the stage-agnostic core was extracted (Option C) to a new shared standard `release/references/standards/finding-disposition-framework.md` that both review stages reference, with `stage-07-dev-testing.md` keeping only its open-Blocker Step 0 + worked example + Phase D/E pointers + automation row. #216 adapted it for Stage 8 QA as a thin delta by reference (no duplicated mechanics), strengthening only Step 0 — a not-met acceptance criterion needs a recorded operator override to defer or accept (a 5-field record carried through the existing CONDITIONAL ACCEPT verdict) — plus PARTIAL handling and three-lane integration. #120 hardened the Stage-5 design-spec template with a required-when-triggered BLOCK-DESTRUCTIVE-022 callout (4 invocation forms) for new in-tree CLIs (prevention-only; the motivating allowlist gap was already remediated). D-C SINGLE branch topology; one release PR (#449); combined Stage 7 Dev Testing (PASS, 3/3) + Stage 8 QA (ACCEPT, 13/13 ACs); signed-annotated `v1.06` tag at the merge SHA; CI 16/16. 30-day Stage 13 outcome window per the `novel` class.

### v1.05 (2026-06-05) — Pipeline-discipline capability slice (design-artifact pilot + centralized diagram home; reusable surface-enumeration checklist; release-plan frontmatter complete)

3 issues in the v1.* Pipeline Discipline arc; operator-rendered `novel` class at the Stage 5 Collective Review scope-lock (revised from `routine` on a new `core/diagrams/` structural pattern + first-of-kind design-artifact backfill, and a D-class location-canonicalization ADR), CHEAP reversibility, text/diagram additions across `core/diagrams/`, `core/ADRs/`, `core/standards/design-artifact-standard.md`, the Stage 5 solutioning + design-review-checklist references, and the 5 release-plan files. The dominant Stage 4 finding was substrate staleness — two of three issues were scoped against a pre-re-versioning substrate that no longer exists and were operator-reconciled before Engineering. #64 (design-artifact discipline Part 2) was scope-locked to Option A — a pilot plus a per-flow-type scope definition — producing the `architecture`-flow pilot `core/diagrams/architecture-platform-structure.md` (a centralized current-state tree referenced from ≥3 parents with reciprocal links from the architecture-overview + operating-model disciplines), canonicalizing the centralized-diagram location to `core/diagrams/` via the committed ADR-015 (numbered 015 because ADR-013/014 already exist on-branch and the core-ADR convention is monotonic), and applying 8 corrected paths to the design-artifact standard; the bulk corpus backfill fissioned to #398. #91 (Stage 5 surface-enumeration undercount) was scope-locked to Option B — a reusable 6-form ref-form checklist at `release/references/protocols/doc-corpus-reorg-ref-forms.md` plus a gated check (Phase A3.2 in stage-05-solutioning + design-review-checklist check 1.5), with the ref-form set re-derived against the current modular-monolith corpus off the dead pre-re-versioning evidence anchor. #116 (frontmatter-schema backfill umbrella) had its original "76 historical files" premise voided by the re-version — the live corpus is 5 plans + 7 notes (all 7 notes already frontmatter-complete), so the real work was the 5-plan 6-field frontmatter backfill (the `lint_release_corpus.py` validator had already shipped); the reconciled scope's `deploy.sh` Check 15 enforce-flip was recorded as void-target (Check 15 release-corpus RETIRED in v2), not forced, and the 5 plans + this plan were added to the reference-durability allowlist to make the schema-mandated `issues:` frontmatter mergeable. D-C SINGLE branch topology; one release PR (#401); signed-annotated `v1.05` tag at the merge SHA. 30-day Stage 13 outcome window per the `novel` class. Out-of-scope follow-ups auto-logged: #398 (bulk design-artifact backfill), #399 (corpus-wide path-drift sweep), #400 (blast-radius mirror-detection / typo fix); the release-note validator / note-schema work is tracked at #83.

### v1.04 (2026-06-02) — Stage-4 planning discipline (currency + crisping gates, plan versioning, methodology-design variant)

3 issues codifying Stage-4 planning discipline within the v1.* Pipeline Discipline arc; `routine` class (Deep review on the methodology-design ADR), MODERATE reversibility, text-only protocol/governance diffs across Stage 4 Planning, Stage 5 Solutioning, the release-planner skill, and the release-process governance summary. Stage 4 entry now runs two mandatory named gates — Phase A0.5 AC/substrate currency reconciliation and Phase A0.6 pre-plan crisping — replacing prompt-dependent checks with structural enforcement (#42). A release-plan versioning protocol makes the plan a committed file with git history as the version trail and an explicit minor-vs-major re-planning rule (#337). The original methodology-design item (#192) fissioned into a design core (#517) and an Option-A-contingent renumber sweep (#518); ADR-011 selected Option B — a Stage 5 Research-Methodology Design persona variant with gate criteria (§ 7) and a Stage 5→6 handoff schema (§ 12) — over a new 14th stage, so #518 voided and the pipeline stays 13 stages. Single branch topology; one release PR (#540); signed-annotated `v1.04` tag. At PR review the operator flagged issue-refs/links/version-anchored cutover clauses leaked into the governance + ADR + plan additions; these were stripped and the rules reframed version-agnostic, with references retained only in this tracking corpus per operator policy (enforcement tracked at #558).

### v1.03 (2026-06-02) — Bundle protocols (domain sourcing + Parallelization Map + cascading-rebundle)

3 issues codifying bundle-and-planning protocol within the Pipeline Discipline arc; `routine` class, CHEAP reversibility, text-only protocol/governance diffs across Stage 3 Bundle, Stage 4 Planning, Stage 5 Solutioning, and Stage 7 Dev Testing plus the release-process governance summary. A domain-best-practice sourcing-or-flag step at Stage 4 Planning requires either citing authoritative external guidance or emitting a machine-readable provenance marker (unsourced-domain token + date + one-line reason); software/governance/pipeline-internal releases are exempt via an explicit exemption marker; the marker carries through Stage 5 Solutioning (upgrade-where-possible) and is verified present-and-dated at Stage 7 Dev Testing. A standing milestone Parallelization Map convention puts a dated parallel-safe / hard-blocked / soft-coupled record in every milestone description, reconfirmed at the Stage 3 bundle-mutability refresh checkpoint and the Stage 4 Planning entry currency check, with a hard-versus-soft edge classifier (reproducible grep sorting cross-milestone references into hard / soft / file-contention) as canonical edge semantics. A cascading-rebundle protocol adds a bundle-mutability trigger so re-milestoning a milestone's namesake deliverable prompts donor-milestone re-evaluation (amend / re-bundle / defer per churn-budget threshold), firing independently of and composing with the existing mutability triggers. All three protocols are forward-only and the release is reflexively exempt from satisfying its own conventions. Single branch topology; one release PR; signed-annotated tag. In-release authoring-standard change: release-corpus entries from this release forward are link-free and version-reference-free, described by capability rather than by issue/PR citation; pre-existing index/digest rows left as-authored pending an operator decision on the catalog-wide convention.

### v1.02 (2026-06-02) — Triage/Bundle protocol codification

4 issues codifying Stage 2/3 Triage and Bundle protocol within the v1.* Pipeline Discipline arc. Operator-rendered `cross-cutting` at Stage 4 (stricter than spoke first preference `novel`; multi-trigger ceiling — ≥3 named core governance surfaces touched). Roadmap-to-bundle cascade gate G3-13 + Phase A9.5 validation sub-step under A9 Bundle Composition Doctrine (#38 — schema bump v1.7→v1.8); observation pattern-review cadence + `decision-discipline.md § 4.1.1` Operational Cadence + OPERATIONS.md canonicalization (Option A1 mirror enrollment) + Pattern Review skill SPLIT into release-planner Mode D DRAFT and release-executor Mode G EXECUTE (#183); Stage 2 triage cadence integrated with release rhythm + `[CALIBRATE-AFTER-3]` P1-SLA annotation + bilateral cross-spec contract (#347); management-task identification as Phase A6.5 triage-output category (#343, D-R6 Option A canonical-edit). D-C SINGLE branch topology; one release PR (#493); signed-annotated `v1.02` tag. Adversarial Design Review ACTIVE (Deep depth) — 4 paired reviews surfaced Blockers resolved via Stage 5 iteration / Stage 6 [ADJUST]. ADR #490 (substrate-vs-canonical precedent) + 3 carry-forward findings tracked at #492.

### v1.01 (2026-06-01) — Pipeline-discipline capability slice

5 issues addressing intake protocol gaps + Stage 12 tag policy + Stage 4 cross-PR audit divergence + Stage 13 close discipline. Re-classified from `routine` to `cross-cutting` at Stage 5 Collective Review (R-A scope expansion). Symmetric content-shape intake routing (#66), signed-annotated tag corpus alignment across 7 file locations (#101 — first reflexive consumer of own prescription), mid-pipeline divergence checkpoint with G-PR8 verdict at Stage 9 Phase A6.5 (#105), intake AC temporal-discipline / Anti-pattern D (#237), AC-Drift Handling Protocol with 3-verdict closed enum (#274). D-C SINGLE branch topology; one release PR (#460); signed-annotated `v1.01` tag (ED25519). Reflexive cutover discipline preserved across 3 protocol-shipping subjects.
