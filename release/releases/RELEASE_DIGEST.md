<!-- repo-integrity: allow-memory-ref -->
# RELEASE_DIGEST

Corpus-level digest grouped by version family. Per-version 3-5 line summary. Appended at Stage 13 chore PR per [`stage-13-close.md § Phase B`](../references/pipeline/stage-13-close.md).

**Date anchor — close-out event (UTC).** The `(<date>)` in each `### vX.Y (<date>)` heading dates the **Stage-13 close-out run**, sampled once per run in UTC — not the merge. `RELEASE_LOG.md` and `RELEASE_INDEX.md` carry the **merge** anchor, so this file may read a day later for a release whose close-out crossed a UTC midnight; that is the two anchors working, not a contradiction. Taxonomy and format rules: [`date-variable-convention.md § Emission-Time Anchors`](../../core/standards/date-variable-convention.md). Entries predating this declaration are grandfathered — anchors are declared forward, never backfilled.

## Knowledge Corpus

### v4.04 (2026-08-01) — Blocked tracker writes now work, and checks that could not fail now can

### v4.03 (2026-08-01) — Release records are now complete, and the check that proves it actually runs

### v4.02 (2026-07-31) — Release close-out results can now be trusted as read

### v4.01 (2026-07-30) — The session learning retro can now actually run

### v4.0 (2026-07-29) — See where agent spend goes, and measure estimates against reality

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v4.md)_

### v3.100 (2026-07-29) — The release pipeline can now record the decisions it makes

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.99 (2026-07-29) — Release runs now say how reversible each decision is, and where they stand

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.98 (2026-07-28) — Readiness checks now catch work another epic owns, and render a map

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.97 (2026-07-27) — Portability is now a first-class engineering value

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.96 (2026-07-26) — Measure and attribute agent token spend from local session data

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.95 (2026-07-26) — Runtime posture is now operator-configurable: the Stage-2 backlog-decision surface, security-hook activation, and spoke model/effort all resolve from canonical config that survives updates, and v2.00's referenced config keys now exist with defaults

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.94 (2026-07-26) — Intake and gate hardening: a Stage-2 acceptance-fit gate (reject-by-default for agent-authored / net-new-sweeping items), an issue-body author-association trust boundary, a module skill-consultation forcing function, and an extend-before-create gate at Stage 5

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.93 (2026-07-25) — Release version numbers now bind when a release ships, not when it is planned

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.92 (2026-07-25) — Architecture baseline: a consolidated actor-model + governance-as-contract statement, a cross-chain index, and a standing as-built conformance audit that checks delivered work against the platform's own architecture

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.91 (2026-07-25) — Cross-platform install foundation: Windows entry-points, a doctor command, and safer multi-destination tracking

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.90 (2026-07-25) — Accurate release-impact analysis: path-true counts + directory-move detection

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.89 (2026-07-25) — Reusable Change Impact Matrix and Training Plan templates, plus a RAID-log template header reconcile

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.88 (2026-07-25) — Six deploy & tooling defects cleaned up — including an XSS sink hardened

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.87 (2026-07-24) — Design diagrams and process flows in the docs are now labeled and findable

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### governance-ci-gates (2026-07-24, version-less) — Load-bearing governance checks now run at the pull request, before a change can land instead of after

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-_unversioned.md)_

### v3.86 (2026-07-24) — Trackers now match each project's methodology, and stale analysis notes get flagged

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.85 (2026-07-24) — Release close-out now guards its published notes, packages, and ledgers

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.84 (2026-07-24) — Two releases can now finish at the same time without tripping over each other

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.83 (2026-07-23) — Self-measurement for release close-out; a session sensor that ships switched off

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### governance-ci-checks (2026-07-22, version-less) — Governance rules now check themselves at build and merge time

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-_unversioned.md)_

### v3.82 (2026-07-22) — Exec briefs name the real consequence; status updates cite their sources

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.81 (2026-07-22) — Theme-named releases no longer vanish from the release record

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.80 (2026-07-22) — Release close-outs are now reliable for theme-named releases

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.79 (2026-07-17) — Both link checkers now agree, and the deploy tooling stops repeating itself

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.78 (2026-07-17) — PORTFOLIO.md composed deterministically from per-project rollups: freshness-gated health scoring, cross-component risk aggregation, and a portfolio/program/project tier taxonomy

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.77 (2026-07-17) — On-demand health-check rollups, semantic routing-conflict detection, and a clearer SKILL.md sizing rule

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.76 (2026-07-17) — The knowledge base now orients a fresh reader: frontmatter enforced corpus-wide, self-audit paths refreshed, vision + install model codified

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.75 (2026-07-17) — A first-class Finding entity and an ownership-collision build check

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.74 (2026-07-12) — Durable CI gates lock the two build-security advisory classes; Security becomes the 6th first-class value

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.73.1 (2026-07-12) — Security-hook fixes now reach workspaces you already installed

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.73 (2026-07-12) — Security patch: hardened hooks + safer local eval-review tool

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.72 (2026-07-12) — The release orchestrator is safer and finishes cleaner

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.71 (2026-07-12) — New projects use one simpler folder set and a single inbox

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.70 (2026-07-12) — Release pipeline re-verifies freshness and constrains agent write-access

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.69.1 (2026-07-11) — Security patch: the hook perimeter no longer fails open, and the fix actually deploys

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.69 (2026-07-11) — Platform maintenance: deploy-check validation cleared to green

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.68 (2026-07-10) — Release quality checks now run automatically and escalate only on failure

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.67 (2026-07-10) — Intake now adapts to your project's methodology

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.66 (2026-07-09) — Engineering documents now start from a canonical template, not a blank page

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.65.1 (2026-07-04) — The pipeline no longer trusts comments from people outside your repo

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.65 (2026-07-04) — The pipeline now tests what a release does, not just reads what it says

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.64 (2026-07-03) — QA can now grade a release against its own acceptance criteria

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.63 (2026-07-03) — The release pipeline now runs on GitHub's native PR-review surfaces

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.62 (2026-07-03) — Writing down an architecture decision is now a guided, one-command step

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.61 (2026-07-03) — Foundation for a queryable warehouse of your project documents

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.60 (2026-07-03) — Methodology packs: pick a delivery method, or bring your own

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.59 (2026-07-03) — One structural vocabulary for docs, backlog, and release records

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.58 (2026-07-02) — System-Specialist template — one method, a specialist per system

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.57 (2026-07-02) — Impact analysis reads code, and git-native releases stop dead-ending

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.53 (2026-07-02) — ADRs gain a canonical frontmatter schema, and the durability lint stops mis-flagging colors

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.52 (2026-07-02) — The eval framework is complete — tested refiner scripts, runnable eval sets, and chain-aware QA

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.51 (2026-07-02) — Skill guidance now cites its canonical escalation and best-practice sources. A `routine`-class tail-closeout pointing two skill docs at the platform's single source of truth. **#931** replaces the generic ">30 days = auto-escalate" remediation in `delivery-engine/references/raid-templates.md` §9 (Stale-RAID anti-pattern row) with the canonical type-split thresholds (issues >14d warn / >30d escalate; risks >30d warn / >60d escalate) and a cite to the doc-of-record `ppm-agent/references/escalation-thresholds.md` §3, resolving a divergence surfaced during v1.18. **#2208** adds a `## Reference docs` section to `pmo-tier-2-support/SKILL.md` citing `core/disciplines/root-cause-analysis.md` (its RCA method) and `core/standards/domain-best-practices/support.md` (the support best-practice guide shipped in v3.35); the guide's `consumers:` already named the skill, so no edit there, and the `.skill` package was rebuilt (Check-7 content-hash verified). Two slices on one branch / one merged PR (#3013, `--merge` at `1139a06`), build order #931 → #2208 under D-C SINGLE / P0 fully-serial. Closes the last tail of skill-function-hardening (#1105) and skill-bp-anchoring (#2164). Provisional **v3.49** was claimed by a concurrent release and re-versioned forward to **v3.51** at the atomic tag claim (forward-only per bundle-composition-doctrine §5.3). CHEAP / `git revert -m 1` of PR #3013 + package rebuild; additive documentation only, no schema/behavior/data change.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.50 (2026-07-02) — The release pipeline can evaluate every stage transition from its own gate specs alone. Completes the platform's stage-gate criteria surface (milestone 68-stage-gate-criteria-completeness, 7 issues) so a release run needs zero out-of-spec operator knowledge to judge whether a stage is done. **#28** registers discrete gate IDs for the two stages that had none — **G4 Plan Readiness** (G4-01..05) and **G6 Engineering Completeness** (G6-01..05) — in `core/schemas/gate-criteria-spec.md` (schema **v1.21 → v1.22**), each with a matching Self-Repair block + Inheritance row, warn-mode, bound into `stage-04-planning.md` §7 / `stage-06-engineering.md` §7. **#96** makes G1-04 bidirectional (under- AND over-specification scorable from the criterion text; schema **v1.22 → v1.23**). **#118** anchors the existing prose judgment dimensions of Stages 9/12/13 to their structural criteria (G-PR\*/G-EX\*/G-CL\*) via a new Per-Boundary Judgment-Dimension Anchors subsection in `gate-evaluation-spec.md` (schema **v1.1 → v1.2**; prose Path-B, no J-\* IDs). **#29** authors the 5 remaining Stage-I/O boundary contracts (2→3, 6→7, 7→8, 8→9, 9→12) in `stage-io-contracts.md`, taking the surface to all 10 boundaries authored. **#501** adds a composite Stage-5 structural-premise review gate (§3.2 obligation rider on T3, §7.2 SR-G1..SR-G4 gate-blocking criteria, Stage 5→6 determination row + Validation Rule 4, blocking check 4.8) so the Stage 5→6 hand-off *asserts* structural conformance rather than assuming it. **#889** adds the `core/disciplines/ticket-architecture-reconciliation.md` discipline (when-fires + 4-step procedure + ticket-age staleness signal + 3 failure modes) filling #501's two reserved seam hooks. **#119** adds a Validation-method column to every gate-criteria table + a 6th Schema field row and lifts inline CLI out of ~24 criterion cells verbatim (no ID renumbered — 78 unique IDs pre/post; schema **v1.23 → v2.0**, BREAKING major by the spec's own column-addition rule, consumer-inert). `cross-cutting`-class (effective 33 pts, operator size-target override 2026-07-01); seven issues on one branch / one merged PR (#2928, `--merge` at `d5e0883`), build order #28 → #96 → #118 → #29 → #501 → #889 → #119 under D-C SINGLE / P0 fully-serial. The branch was rebased onto `origin/main` after v3.48 merged mid-build; the `gate-criteria-spec.md` version chain re-sequenced v1.20 → v1.21 (G3-18, v3.48) → v1.22 → v1.23 → v2.0 with G3-18 given its Validation-method cell — no v3.48 content lost. Provisional **v3.49** was claimed by a concurrent release and re-versioned forward to **v3.50** at the atomic tag claim (forward-only per bundle-composition-doctrine §5.3). MODERATE / `git revert -m 1`; governance + doc authoring only, no new skill, no stored-data change, runtime blast radius none.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.49 (2026-07-02) — Governed file movement: the file router now governs every direction a file can move — inbound, generated-staging, promotion, and cross-project — not just inbound arrivals, and intake gains two matching refinements (milestone 08-INTAKE-governed-file-movement, #211, 3 issues). **#240** extends `file-router` from inbound-only to the full four-direction set; it resolves the promotion target and CITES artifact-generator's existing PROMOTE/REVISE/REJECT gate rather than restating it, owning a genuinely new approval gate only for cross-project-out, expressed as two gate-types — a confidence-threshold gate for the inbound family (inbound + cross-project, both Layer-2 scored) and a flat-approval gate for the Domain-C family (generated-staging + promotion, target pre-stamped). **#565** adds an `intake-desk` Phase-2 container-altitude existing-owner scan (hard-gate, containers only) that checks the tracker for an existing owner before proposing a new container, and reconciles failure mode #7 in place (stays 9 FMs, no redundant tenth). **#289** adds `intake-desk` **Mode C — Ambient Auto-Log** (non-interactive detect→create governed by the `automation_level` ceiling, with an honest no-hook-backstop safety framing and the per-item→standing weakening stated not softened; the Tier-0 set — governance / financial / security / RAID-close — never auto-created regardless of level; 9→12 FMs) and rewires the `core/CLAUDE.md.template` auto-logging rule to invoke it. `routine`-class; three EXTEND-mode slices to existing operations-module skills on one branch / one merged PR (#2861, `--merge` at `ee649ad`), build order #240 → #565 → #289 under D-C SINGLE / P0 fully-serial (the #565→#289 order serialized the two edits contending on `intake-desk/SKILL.md`). Provisional v3.46 re-versioned forward to v3.49 at the atomic tag claim (forward-only per bundle-composition-doctrine §5.3, anchor = published v3.48). The `intake-desk` + `file-router` `.skill` packages were rebuilt at close-out. MODERATE / `git revert -m 1 ee649ad` + redeploy prior skill packages; two operational skills + one governance rule, no new skill, no stored-data change.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.48 (2026-07-02) — The design gate adapts to your delivery method, notes become non-authoritative, and health-status colours get one home. Closes the last three items of two knowledge-corpus cleanup epics. **#1087** registers gate criterion **G3-18** (Archetype-appropriate design invocation conditioned on `delivery_approach`) in `core/schemas/gate-criteria-spec.md` (schema v1.20 → v1.21, non-breaking additive — one criterion row + one self-repair row, no ID renumber) following the live G3-05 `deliverable_type` conditioning precedent: at the Bundle→Planning (Stage 3→4) boundary the design-invocation expectation conforms to the declared method — CASE 1 (Scrum/XP/Lean → design-first before slicing; Waterfall/PRINCE2 → phase-gate; Kanban → continuous, no sprint-slice gate), CASE 1-ARRAY (Hybrid → phased constituent governs), CASE 3/absent/`Custom` → base behaviour UNCHANGED via an explicit no-op default (never a silent default-fire); it adds a methodology lens to *when* design is expected and never removes the base Stage 5 Phase 0 Activation Gate, with a paired Stage-8 regression gate `G3-18-absent-axis-noop`; recorded in **ADR-066**. **#1093** codifies a Universal Preferences bullet in `core/CLAUDE.md.template` — operator notes are directional inputs, not authoritative specs; differences vs the corpus render as non-authoritative observations, and an agent MUST NOT perform a gross deletion / mass-restructure based on simple notes alone without first reconciling against the SSOT and confirming with the operator (canonical-corpus findings unaffected). **#930** records **ADR-065** naming `comms-writer/references/channel-formats.md` the single-source owner of the health-RAG bands + watermelon concept, and single-sources the concept via see-also cross-references in `channel-formats.md` and `ppm-agent/references/evidence-quality.md` (thresholds unchanged; the two edited skills' `.skill` packages rebuilt at close-out). `novel`-class; milestone 105-knowledge-corpus-tail-closeout, three independent cards on one branch / one merged PR (#2918, `--merge` at `dd9c284`), build order #930 → #1087 → #1093 under D-C SINGLE / P0 fully-serial. The branch was rebased onto `origin/main` pre-merge to inherit ADR-064 and clear the ADR-number contiguity gate; v3.48 free at the atomic claim (anchor = published v3.47). Closes the last child of KA-Standards #1171 and KA-Discipline #1174. Follow-up #2917 (SPI-band canonical reference) is out of scope + non-blocking. CHEAP / `git revert -m 1`; two ADRs + one advisory gate criterion + two cross-references + one governance rule, no blocking gate, runtime blast radius none.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.47 (2026-07-02) — The PMO's operational data becomes agent-native and self-maintaining

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.45 (2026-07-01) — Pipeline docs now say what each stage is for, and one Stage-9 gate is on the books

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.44 (2026-07-01) — Backlog triage now runs as one command instead of issue-by-issue by hand

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.43 (2026-07-01) — Spoke-execution safety: five properties a pipeline spoke can no longer violate by construction. A Stage-6 pipeline spoke is now, by construction, unable to (1) push unsafely under multi-chip parallelism, (2) drift from its intake substrate undetected, (3) spawn commit work without a signing key, (4) wander to the operator's primary checkout, or (5) run outside security-hook coverage. **#307** authors **ADR-062** (`core/ADRs/ADR-062-substrate-vs-canonical-precedent.md`) codifying the substrate-vs-canonical precedent — a canonical-spec edit wins over a substrate-body (issue-body) mutation, issue bodies remain historical-record — and wires the cross-repo-citation-translation discipline into `release/references/pipeline/stage-05-solutioning.md` A1 scope-assessment, indexing the ADR in `core/ADRs/README.md`. **#47** adds the Stage-5 **M1.1 substrate-drift reconciliation chip step** to `release/references/how-to/hub-spoke-bridge.md` (one canonical home): at spoke-entry the chip compares each checkable issue-body claim against live state and emits a structured, machine-readable drift report — four fields per row (`drift_class` closed-enum {state-lifecycle | population-count | milestone-description | currency-reconciled-upstream}, `body_claim`, `current_state`, `gap_age_days`) — deferring to the Stage-4 A0.5/A0.6/A0.7 currency reconciliation for any axis already reconciled at planning and routing a per-drift disposition (`current-state-driven AC interpretation` | `escalate-body-amendment`) without ever auto-amending the body; the paired `intake-substrate` PROC entry lands in `core/standards/failure-mode-standard.md` (the OPTIONAL `release/tools/check-substrate-drift.sh` primitive was scoped OUT — the chip-spec + failure-mode entry are the load-bearing deliverables). **#1639** corrects the BLOCK-DESTRUCTIVE-019 worktree-exemption base in `core/hooks/block-destructive.sh` from workspace-root to repo-root (`${PRIMARY_ROOT}/pmo-platform/.claude/worktrees/`, derived per ADR-017 checkout-independence), retargets 13 `core/config/allowlists/script-execution-allowlist.txt` worktree-glob entries + the header example from `[CLAUDE_WORKSPACE_ROOT]` to `[PMO_PLATFORM_ROOT]`, and repoints the `core/hooks/tests/block-destructive.test.sh` fixtures to the repo-rooted base with both-direction gate-teeth (repo-rooted worktree ALLOW; retired workspace-rooted BLOCK; primary cwd BLOCK) — a LIVE always-enforce defect (the hook has no `.mode` gate) fixed before #1531's hook-loading wiring lands so the hook does not mis-block worktree Layer-1 edits. **#1642** adds the spoke-template no-`cd`/never-primary clause in `hub-spoke-bridge.md` (AC-1) plus the `core/rules/git-workflow.md § Primary Checkout Discipline` spoke-facing line (AC-3); AC-2 subagent-hook coverage is deliberately owned by #1531. **#1351** adds the CLAUDE.md Tier-1 governance self-check and a Tier-2 structural local-work entry point rooted outside `pmo-platform/` (`core/CLAUDE.md.template`) — T1+T2a this release, runtime teeth T2b/T2c fast-follow once #1531/#1642 land. **#59** adds the `ssh-add -l` pre-flight to the `hub-spoke-bridge.md` commit-producing-spoke spawn procedures (Stage 6 Engineering, Stage 12 Execute chore-PR, Stage 13 Release-Close chore-PR — the spawns that produce a commit) plus a user-side handoff on empty-agent, so a commit-producing spoke is not spawned into a state where `commit.gpgsign=true` would kill it at the commit step. `novel`-class; milestone 67-spoke-execution-safety, six cards on one branch / one squash-merged PR (#2799), build order #307 → #47 → #1639 → #1642 → #1351 → #59 under D-C SINGLE topology / P0 fully-serial posture (the 3-way `hub-spoke-bridge.md` edit forecloses parallel Engineering chips on the shared tip). #20 (subsumed by v3.28's ADR-052 parallelism-posture taxonomy) and #1472 (folded into #1531's deploy path + #1639's hook base) were closed during readiness. CHEAP-to-MODERATE — `git revert` of the single release PR restores prior state byte-for-byte for the additive doc/spec/ADR cards; the one MODERATE surface is #1639's executable hook edit (the revert is still clean, but if a wrong base had shipped and #1531 had already flipped enforce, worktree edits would be blocked in the interim — hence the #1639-before-#1531-enforce ordering mitigation). #1531 stays open (separate milestone `96-update-install-config-safety`).

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.42 (2026-07-01) — The branch-cleanup tool now actually cleans up squash-merged branches

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.41 (2026-07-01) — The skill catalog gets a suite-wide structural-hygiene pass and two net-new measurement auditors ship. **Hygiene (Wave A):** **#1135** ratifies the canonical mode-heading separator standard in `core/standards/canonical-skill-structure.md §4` — the em-dash form `<H2-or-H3> Mode <ID> — <Name>` (` — `, U+2014 flanked by spaces), survey-grounded as the established majority idiom over the colon-form minority. **#1136** applies that separator across the colon-form multi-mode SKILL.md set (colon-form → em-dash). **#1132** consolidates the vestigial Mode-Detection sections in `project-initiator` and `eval-writer`. **#1133** brings the SKILL.md catalog to full `## Guardrails (Platform)` conformance — 12 inserts of the missing section + 26 renames of bare `## Guardrails`, preserving pmo-qa-auditor's `## Guardrails (Extended)` variant — and fixes a folded `failure-mode-standard.md` drift. **#571** cross-references delivery-engine's DoR/DoD gate vocabulary to G1/G3 + DT-2; **#676** reconciles the stale canary entry in `core/schemas/per-skill-output-contracts.md §Skill 12`. **Measurement (Wave B):** **#16** adds the net-new `context-budget-auditor` skill (`core/skills/context-budget-auditor/` — SKILL.md + `measure-context-budget.sh`) quantifying token consumption across loaded platform components. **#17** adds the net-new `skill-compliance-auditor` skill (`core/skills/skill-compliance-auditor/` — SKILL.md + `scenario-and-calibration-schema.md` + `measure-skill-compliance.sh`) quantifying trigger-rate accuracy across the skill catalog, together with **ADR-061** defining the trigger-rate metric class and the compliance-auditor ↔ eval-writer boundary; both skills registered in `registry.md` / `deploy.sh` / `OPERATIONS.md`. **#704** ships SPEC-ONLY (routing-conflict detection beyond Jaccard — cross-skill invocation-path analysis: mechanism + sibling-harness decision + O(n²) cost design specified, zero code mutated; deferred implementation → follow-up #2699). `novel`-class; milestone 07-INFRA-hygiene-measurement, nine slices on one branch / one merge across two waves. Stage 4 live-verification found the catalog at 48 skills (not the 23 the source issues assumed); #1133/#1136 were re-priced size:M accordingly. Re-versioned forward v3.39 → v3.41 pre-merge (A.5.6c lineage — the branch descends from v3.40, v3.37 + v3.38 having shipped after a prior concurrent-release collision on provisional v3.37), and `claim-version.sh` then bound v3.41 at the merge SHA with no CAS collision. Additive — CHEAP / `git revert -m 1`; two new `core/` measurement skills + one ADR + catalog-wide SKILL.md structural edits, no `.skill` package committed in-PR (source SKILL.md only; the two new skills' packages build + deploy at close-out), runtime blast radius none.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.40 (2026-07-01) — Four stale documentation references are drained from the internal reference corpus — places where the written guidance had fallen out of step with how the platform works. **#108 (doc-link protocol log-surface)** reconciles `core/standards/doc-link-maintenance-protocol.md` §8 to the shared warn-log the protocol actually writes to (as enforced by `deploy.sh` Check 14), so the protocol documents the surface it uses. **#131 (Projects/ → projects/ casing)** lowercases the genuine capital-P `Projects/` prose references in `core/governance/OPERATIONS.md` and `operations/OPERATIONS.md` to the canonical `projects/` form; the card was re-scoped on 2026-06-30 to the real OPERATIONS prose references (the broad placeholder-token variants routed to #2700), and a changelog line naming the historical `Projects/` casing is preserved intact as an accurate historical record. **#2684 (drain last IMPROVEMENTS.md / IMP-### site)** drains the retired "log to IMPROVEMENTS.md / IMP-###" Self-Update-Protocol instruction in `core/schemas/routing-rules.md`, routing it to the current GitHub-Issues intake and removing the last live pointer to the dead mechanism (split from #4; the separate Pattern-C detection work was removed from this milestone as net-new build and remains under epic #1172). **#2700 (unify project-path placeholder)** unifies the project-path placeholder to the canonical `projects/[Project]/` form across `core/schemas/frontmatter-schema.md`, `navigation-layer-schema.md`, `routing-rules.md`, `sqlite-index-schema.md`, `core/specs/health-check-specification.md`, and `operations/skills/project-initiator/SKILL.md`, owning all placeholder-token variants per the BROAD boundary resolution surfaced by the #131 re-scope; because `project-initiator` is a migrated skill, its compiled `.skill` package (and content sidecar) were rebuilt in the same release PR so the deployed artifact stays atomic with the SKILL.md edit. `routine`-class; milestone 30-doc-link-drift-drainage, four file-disjoint drainage cards on one branch / one merge. The prior direct-build PR #2703 was closed unmerged on 2026-07-01 (bypassed the governance gates; no changes reached main) and this release delivered the same scope through the gated pipeline. No re-version (v3.40 free at the Stage-12 atomic claim; anchor = published v3.38). Additive — CHEAP / `git revert -m 1`; reference-corpus documentation + one migrated skill's text + its rebuilt package, no schema or runtime-check change, runtime blast radius none.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.38 (2026-07-01) — The release pipeline's own rules get three additive precision fixes across the planning, design, and engineering stages, sharpening release-identity and frozen-spec handling so problems are caught earlier instead of surfacing late at deploy. **#3 (placement forward-check)** adds Phase A0.7 / G-PL3 to the Stage-4 planning reference (with a governance summary in the release-process reference): a pre-planning check that runs the unified `git log <base>..origin/main --name-status --find-renames` predicate to catch directory-crossing renames and in-scope deletions on the mainline since the release branch's base — closing the "release cut before a structural reorg → new-file placement collides at deploy" pattern; it composes with the Stage-4 structural sub-audit and the Stage-12 pre-merge divergence check rather than duplicating them. **#226 (commit-group traceability)** adds a "documented simplification — advisory, not enforced" note after Phase B1 in the Stage-6 engineering reference plus an enforcement-posture sentence in the sub-task-methodology standard; it carries the release's genuine design decision (enforce / audit / advisory on the now-Required `Commit group` field), rendered ADVISORY at Stage 5 with a monitored-residual note (no verifier built). **#75 (frozen-spec precision)** adds a required-when-triggered precision rule to the solutioning-output-template's `## Blast Radius` section (with a DEFERRED escape + re-adjudication-cost note) plus a routing pointer in the Stage-5 solutioning reference, disambiguating the written spec from the frozen work product; a stale `templates/` → `standards/` target-path in the milestone description was corrected into the plan at Stage 4. `novel`-class; milestone 66-release-identity-and-spec-hardening, three file-disjoint cards on one branch / one merge. Re-versioned forward v3.37 → v3.38 at the Stage-12 pre-merge freeness check (A.5.6c) after a concurrent sibling release claimed v3.37 first (its plan file occupied the same path) — the branch relabeled to the next-free v3.38 before the merge, and the atomic claim bound v3.38 with no collision; the plan's R1 contingency fired exactly as anticipated (forward-only, no data loss). Additive — CHEAP / `git revert -m 1`; documentation/protocol edits to the release-pipeline reference corpus only, no skill or `.skill` package touched, runtime blast radius none.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.37 (2026-06-30) — Shared project facts now live in one place, and PROJECT.md links to them instead of repeating them. A new shared `projects/_pmo/` area holds one consistent page per entity — people, systems, vendors, workstreams, decisions, and cross-project dependencies — and `PROJECT.md` becomes a thin composed wiki-link index that links out to those pages rather than restating the same facts in every project. **#362** establishes the `_pmo/{people,systems,vendors,workstreams,decisions,dependencies}/` layout with six entity-page templates matching the frozen schema sections (alias-tracking aligned to ADR-040 `person_id` + people-roster, compose not fork) and a `project-initiator` Mode A `_pmo/` bootstrap step. **#363** redesigns PROJECT.md as a ≤50-line composed index that wiki-links INTO #362's entity pages, adds a `project-initiator` Mode A Step-3 template-ref swap, and defines a 4-step live-migration protocol with a backwards-compatibility consumer table against the live `project-schema.md` consumers. **#159** models plans as typed sub-entities (comms / training / hypercare / cutover / change-mgmt) with relationship typing, resolves the deferred `plan_type` G5 enum membership, and drops `plan_subtype`; the composed index's `## Plans` section points at these typed entities. `novel`-class; milestone 21-shared-entity-storage-layout, three stories on one branch / one merge (build order `#159 ∥ #362 → #363`). **AC-5 live-project migration is DEFERRED** — the merge ships the capability only; the gated proof-of-concept migration of ≥1 live PROJECT.md is a follow-up (executor-tracked git-ignored ops tree, out-of-diff, snapshot-before / verify-links-after per plan R1). No re-version (v3.37 free at the Stage-12 atomic claim; anchor = published v3.36). Additive — CHEAP / `git revert -m 1`; structure + schema + templates, runtime blast radius none, the only non-git-reversible step (live migration) deferred out of the release.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.36 (2026-07-01) — Process-domain specialist skills anchored to one best-practice guide, plus a new support-domain guide and the change-domain decision: the four process specialists each gain a compose-by-reference `## Reference docs` pointer (ADR-019; no content absorption) — pmo-scrum-master, pmo-release-train-engineer, and pmo-business-analyst single-anchor to `core/standards/domain-best-practices/process.md`, and pmo-product-owner dual-anchors to BOTH `process.md` and `governance.md` (value framing + accept/reject span both domains, mirroring pmo-technical-program-manager), with the guides' `consumers:` fields appended (#2177, #2178, #2179, #2180); a net-new `core/standards/domain-best-practices/support.md` K1 guide covers service management (ITIL 4), tiered incident & escalation practice (with the paired applicability note), and service reliability (SRE — SLI/SLO, error budgets, toil), registering three EXTERNAL + one INTERNAL row in `core/specs/framework-catalog.md` (#2210); and the change domain's best-practice content stays self-bundled in `change-management/references/` with the framework catalog as its registry — no shared `change.md` is created, pmo-ocm-lead is unchanged (transitive via ADR-019 composition), recorded in ADR-057 (`Proposed`) with a reversal trigger (promote iff a second change-domain designer skill appears), plus a build-philosophy "BP — change" coverage-matrix row and one `change-management/SKILL.md` Reference-docs line (#2211). The process-domain sequel to v3.30 (software) and v3.33 (governance). `routine`-class; milestone 90-skill-anchoring-process-support-change, all six issues on one branch / one merge. No re-version (v3.36 free at the Stage-12 atomic claim; anchor = published v3.35). Additive — CHEAP / `git revert -m 1`; reference wiring + one new guide + one ADR, runtime blast radius none. The four edited process skills' `.skill` packages were rebuilt at the Stage-12 cut so the content-freshness check (Check 7) passes.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.35 (2026-06-30) — Records management, file/folder naming, and 08-Generated cleanup are now governed: a records-management policy (`core/governance/RECORDS_POLICY.md`) defining ISO 15489-1 retention, a four-class classification (vital/important/reference/transient with DEFAULT=Reference), disposition, and authenticity (#372, ADR-054); a unified artifact-naming standard (`core/standards/artifact-naming-standard.md`) covering POSIX-safe syntax, a controlled type vocabulary, explicit folder-naming rules (`_`-prefix reserved for infra), a validation regex, and a retroactive-rename protocol — wired into the seven emitting skills + OPERATIONS.md + a `pmo-qa-auditor` conformance check (#369, ADR-055, absorbing former #117); a new `generated-cleanup` skill running `08-Generated/` cleanup under an unconditional approval gate, grouping candidates by lifecycle state (`lifecycle_state` / `promotion_state`) and archiving to `_archived/` (#277, ADR-056); and `project-initiator` Mode A folder-name validation rejecting `_`-prefix / special-character / non-human-readable names against the new standard (#232). `novel`-class; milestone 20-records-management-naming-and-cleanup, four cards on one branch / one merge. Re-versioned forward v3.34 → v3.35 at the Stage-12 atomic claim (A.5.6c) after a concurrent telemetry release claimed v3.34 between Stage 9 GO and Stage 12 Execute — the branch name is retained. Additive — CHEAP / `git revert -m 1`; runtime blast radius limited to two operational skills (project-initiator validation + the new cleanup skill).

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.34 (2026-06-30) — The release pipeline now measures its own delivery and close-out health

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.33 (2026-06-30) — Governance-domain specialist skills anchored to one best-practice guide: the six governance-domain specialists (pmo-project-manager, pmo-program-manager, pmo-program-coordinator, pmo-portfolio-manager, pmo-knowledge-manager, release-planner) each gain a compose-by-reference `## Reference docs` pointer to `domain-best-practices/governance.md` (ADR-019; no content absorption) and the guide's `consumers:` field lists them (#2172, #2173, #2174, #2175, #2176, #2181). The governance-domain sequel to v3.30's software-domain anchoring; with governance now wired, two of the three domain guides (software + governance) are skill-anchored and only the process guide remains pending. `routine`-class; milestone 89-skill-anchoring-governance, all six issues on one branch / one merge. Re-versioned forward from a provisional v3.32 at the Stage-12 atomic claim after a concurrent release published v3.32 first — the branch name is retained. Additive reference wiring — runtime blast radius none.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.32 (2026-06-30) — The release hub now follows the autonomy rules it operates under

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.30 (2026-06-30) — Software-domain specialist skills anchored to one best-practice guide: the five software-domain specialists (pmo-architect, pmo-principal-engineer, pmo-software-engineer, pmo-devops-sre, pmo-technical-program-manager) each gain a compose-by-reference `## Reference docs` pointer to `domain-best-practices/software.md` (ADR-019; no content absorption), the guide's `consumers:` lists them, pmo-technical-program-manager is dual-domain (also `governance.md`), and the build-philosophy BP×Skills cells are reconciled (software skill-wired; governance/process skill-wiring-pending) so the coverage map no longer over-claims (#2165, #2167, #2169, #2170, #2171, #2209). `routine`-class; milestone 88-skill-anchoring-software. Re-versioned forward from a provisional v3.29 at the Stage-12 atomic claim after a concurrent release published v3.29 first — the branch name is retained; a benign orphan v3.31 tag was left in place. Additive reference wiring — runtime blast radius none.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.29 (2026-06-30) — Stage-6 Engineering parallelism postures: a founding decision record (ADR-052) fixes a named parallelism-posture taxonomy for how a release's Stage-6 engineering work runs concurrently versus serially, wired into the Stage-4 planning + Stage-6 engineering references, the hub-spoke orchestration bridge, the failure-mode standard, and the release-planner skill (#19). `novel`-class; a single-card slice of milestone 73-concurrent-execution-safety (2 issues remain open). Re-versioned forward from a provisional v3.28 at the Stage-12 atomic claim after the label-taxonomy release published v3.28 first — the `release/v3.28` branch name is retained. Additive governance + one skill — runtime blast radius none.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.28 (2026-06-30) — Label-taxonomy canonicalization + a GitHub label-set parity check: the cluster axis gains an orthogonality protocol (one domain cluster per issue vs the `cluster: cross-cutting` span-marker that composes alongside it), the stale cluster color column is reconciled to live and the undocumented `cluster: security` is registered (12 clusters, was 11); observation promotion strips the `[Observation]:` title prefix under a deterministic title↔category parity invariant cross-referenced from the Stage-3 Template-Conversion Rule; and `deploy.sh` Check 51 + `check-label-parity.py` assert label-taxonomy ↔ GitHub label-set parity (enforce-capable MISSING / warn-only ORPHAN, warn-mode-initial, source-agnostic), surfacing pre-existing drift (missing `status:` labels → #1828, unregistered `type:*` → #1777) on its first run. `cross-cutting`-class; additive governance + a warn-mode check — runtime blast radius none.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.27 (2026-06-30) — Platform docs now carry consistent metadata — and a check keeps it that way

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.26 (2026-06-30) — The platform now catches its own governance drift at PR time

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.24 (2026-06-30) — Release-hub orchestrator skill: a net-new whole-release control plane (`release/skills/release-hub/`) that takes a milestone and drives it through the release pipeline by composing the per-stage skills — owning sequencing and readiness-gating, never the stage work itself. Two modes: Mode R (Milestone Readiness) is the pre-flight GO/NO-GO that confirms a bundled milestone is ready to START, and Mode O (Orchestrate Release) runs the full hub-and-spoke orchestration with operator gating only at the named pipeline checkpoints. `novel`-class; additive net-new skill — composes existing stage skills, adds no new autonomous mutation surface, runtime blast radius none.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.23 (2026-06-30) — Project health-check skill: a net-new intent-driven project-state drift auditor (`operations/skills/health-check/`) that audits one project for drift between its tracked state and its canonical sources and emits a categorized 5-section punch list that is never auto-applied; seven modes (`full` / `timeline` / `attribution` / `comms` / `plan <name>` / `raid` / `sources`) on one shared contract, with founding ADR-051 recording the MCP-primary/local-fallback source set + graceful-degradation contract. `novel`-class; net-new skill + one ADR — additive, runtime blast radius none, read-and-recommend only.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.22 (2026-06-29) — PMBOK artifact coverage + PROJECT.md schema axes: PROJECT.md frontmatter gains three additive axes (the first-class `deliverable_type` deliverable-domain axis with a domain-axis consumption pattern + G3-05 conditioning + founding ADR-050; `org_structure_type` + a refs-only `team_roster`), five PMBOK artifact templates land with Trackers 8/9, and a new `lifecycle-tailoring.md` maps the 3-state agent model onto the PMBOK 5-process-group convention. `cross-cutting`-class; additive schema / template / reference fields — no data migration, no stored-content move, no change to any existing PROJECT.md, no breaking change.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.21 (2026-06-29) — Terminology & controlled-vocabulary canonicalization: the K1 glossary is refreshed for AI-agent comprehension (frontmatter + the Role term anchored to the Autonomy-Tier framework + four first-class actor terms), the Tier Disambiguation Table is extended to cover Hierarchy Tier, and canonical `Initiative` / `Roadmap` terms are added and reconciled with the live `epic:*` label namespace via a new ADR. `novel`-class; documentation / governance only — additive/corrective corpus edits, no behavior / schema / runtime change.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v2.41 (2026-06-29) — Governance-hygiene: the root-cause-analysis method is wired by citation to every failure-owning surface its own trigger table named, the hub's rule-computed version step stops surfacing as an operator click-gate, and two reference-doc accuracy fixes land. `routine`-class; slim cherry-picked milestone (release-corpus migration sliced off to #1549) — documentation / governance / skill-text only, no behavior / schema / runtime change.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.40 (2026-06-29) — Transcript & meeting processing: the implicit transcript-handling flow is formalized as one named pipeline with explicit automation boundaries, and meeting materials get a governed lifecycle state plus a scannable redesign. `routine`-class; formalizes existing flow + adds one tracker field + scannable meeting materials — no data migration, no stored-content move, no new project-facing primitive.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.39 (2026-06-29) — The platform now audits itself on a defined cadence and records where its advice differed from your call

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.38 (2026-06-28) — Finishing a release is now reliable and produces the same complete record every time

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.36 (2026-06-28) — Triage-and-bundling signals: six Stage 1–6 triage/bundling judgment calls the release pipeline used to re-derive each run — how an approved ticket's structure is treated downstream, how priority is read, when related issues are grouped, and how sub-tasks are tracked — are converted into codified pipeline outputs, and one repeatedly hand-derived step (the native-dependency mirror) is promoted to a tracked tool. `novel`-class; additive throughout (spec/governance/schema edits plus one new tool and one enforcing gate) — no data migration, no stored-content move, no behavior change to any project-facing surface.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.35 (2026-06-27) — Agent decision-confidence: a written protocol lets an autonomous agent calibrate its own confidence at a non-obvious decision point and decide proceed / pause-to-learn / escalate — from observable cross-check agreement rather than self-reported confidence, scaled to how reversible the action is, with a bounded learn-loop that must inject new outside information and an anti-theater guard that flags a pause which learns nothing. The first live consumer is the release pipeline's Stage-4 bundle-currency check. `routine`-class; additive throughout (a new spec, a new ADR, and four edits wiring the gate into existing disciplines and one pipeline stage) — no data migration, no stored-content move, no breaking change.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.32 (2026-06-27) — Skill-registry identity and currency: the already-shipped skill registry (`core/skills/registry.md`, ADR-038) is made self-checking against the deployed roster — `deploy.sh --check` now fails the build at the row level when the catalog and the deployed roster disagree (a registry row with no roster member, a roster member with no registry row, or a row that resolves to no live `SKILL.md`), with a second per-row field-currency layer that logs (warn-mode-initial) when an entry's recorded modes/dependencies/triggers drift from its skill — and the consumer surface that cited a phantom `dependency-graph.md` (a file that never shipped) is corrected to cite the live registry. `routine`-class; an additive build check plus citation corrections — no data migration, no stored-content move, no breaking change; four drifted `.skill` packages rebuilt at release-cut.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.31 (2026-06-27) — Knowledge-management discipline: the platform's memory gets one written cross-surface read/write SSOT contract — a per-surface table naming, for every place the platform stores a fact (the corpus, the governance context files, the `~/.claude/memory/` store, the `projects/_config/` state files, the operational trackers, the People surface, and `operator.toml`), which surface is the source of truth and who may write it under what Autonomy Tier — and the QA platform-health audit gains a systematic knowledge-management scan that reports stale and missing documentation. `novel`-class; additive throughout (one new discipline doc, one new standard, one new ADR, and reviewed edits to two skills + governed docs) — no data migration, no stored-content move, no breaking change; the two edited skills have `.skill` packages rebuilt at release-cut.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.30 (2026-06-26) — Governance cross-reference currency: display-text path citations across the corpus that still named the platform's pre-reorganization folders (`pmo-platform/{reference,skills,governance,engineering}/…`) are reconciled to their verified current homes under `core/` / `release/` / `operations/`, so internal links and citations in the docs and skill files resolve instead of dead-ending — the 13 catalog `SKILL.md` files drop from 67 stale citations to zero, a 28-file non-corpus sweep rewrites genuine drift while preserving legitimate historical occurrences, and one single-token QA↔Dev-Testing threshold (`≥3` → `>3`) is harmonized. `cross-cutting`-class; documentation/governance citation-accuracy only — no skill behavior, schema, or runtime-check change; the cosmetically text-edited skills have `.skill` packages rebuilt at release-cut.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.29 (2026-06-26) — Corpus-drift reconciliation: ten places where a reference or governance doc had drifted from its canonical source — a stale rule count, a divergent forward-consumer list, an old field name, an imprecise term, an incomplete check enumeration, and a duplicate file — are corrected so each affected K1 doc reads consistently with `deploy.sh`, `review-discipline-principles.md`, and the canonical schemas. `routine`-class; documentation/governance accuracy only — no skill behavior, schema, or runtime-check change; the 2 text-edited skills (`build-reviewer`, `pmo-skill-editor`) have packages rebuilt at release-cut.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.28 (2026-06-26) — Generated-vs-source provenance: when a skill writes a generated artifact, its frontmatter now records which skill + version produced it (`generated_by`) and what upstream evidence it drew from (`source_inputs`), plus a stable `id`, so generated content is auditable to its source and cleanly distinguished from human-authored source artifacts. The single card also closes a long-dangling cross-doc contract — `template-protocol.md` §8's `generated_by` dual-semantics flag now exists on both the template and the instance sides. `novel`-class; additive throughout (every field `Required: No`, forward-only no back-fill); 5 emitter/governance/QA skills bumped to v2.28 with packages rebuilt at release-cut.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.27 (2026-06-26) — Ticket information architecture: three coupled cards establish how a ticket carries confidence, titles, and information. One canonical four-band ordinal staleness-confidence **depth** scale (`S0-NONE` < `S1-SUPERFICIAL` < `S2-SUBSTANTIVE` < `S3-STRUCTURAL`) replaces five incompatible shapes so an operator reads one vocabulary and "old" no longer auto-reads as "structurally broken" (#436); work-item titles drop the redundant `[...]:` type prefix (type lives on the label) behind a repurposed G1-01 title-informativeness floor + a warn-tier `deploy.sh` Check 22 that never forces a bulk re-title of legacy issues (#545); and the Three-Layer ticket information model (Source of Truth / Stage Reviews / State Anchors) is captured with the backlog view rendering pipeline-Stage granularity (#211). `cross-cutting`-class; additive throughout; 2 consumption-skill edits with packages rebuilt at release-cut.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.25 (2026-06-26) — Hub-spoke orchestration discipline: the orchestration failure-modes the platform learned by trial-and-error are codified into `hub-spoke-bridge.md` — the bridge reference the hub reads at runtime — so a class of cross-ticket / cascade / chip-safety failures is caught at authoring time rather than after red CI or a mis-routed spoke. Cascade-completeness chip block, a necessity/value-add hub-eval dimension, de-fragile durable-corpus authoring, four memory→corpus chip disciplines (verbatim-source / issues-as-spoke-contract / hook-safe git idioms / concurrent-spoke contention recovery), and a mid-Stage-5 cross-ticket detection rule (Spoke A); a #189 empirical hook-inheritance suite — CI regression + operator probe + Check-46 deploy-check (Spoke B). `novel`-class; additive throughout (no `SKILL.md` edited); two disjoint single-writer spokes.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.24 (2026-06-26) — Corpus conventions and standards hygiene: two standards docs relocate into `core/standards/` (with a 90-file inbound reference sweep), four net-new corpus docs land (a minimal-addition authoring discipline, the third domain-best-practices domain, an audit status-badge convention, a Claude Code runtime-state catalog), three reference docs are reconciled to live state, and a substrate-neutrality audit confirms the bare toolkit. `novel`-class; additive doc/governance only — no skill behavior, schema, or deploy-state change.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.26 (2026-06-26) — People-graph activation: the people-graph that shipped structurally in v2.23 is now active on a fresh install — install/update seed the operator-instance roster (create-once, never-clobber), the adoption path is documented, and a consumption eval verifies the four consuming skills resolve read-only from a populated roster. `routine`-class; additive; activation layer only.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.23 (2026-06-26) — Functional people-graph: PMO agents resolve people from one maintained, never-committed graph — a closed-allow-list roster (data surface) composed at read-time with the frozen Person + Resource entities into a capability/coverage view, with the four leadership-owner fields carried as typed `ref→Person` instead of free text. ≥4 skills read names / owners / coverage read-only; the graph + its maintenance + its consumption are registered governance; a de-identified template ships in-repo while the filled roster stays out-of-repo and commit-protected. `novel`-class; additive; functional-coordination only — not an HR/performance system.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.22 (2026-06-25) — Comms-and-facilitation reference substrate: facilitation techniques get a governed K1 home a delivery skill can cite in-context, the canonical meeting-agenda + meeting-recap output-format specs live once (single-sourced out of `comms-writer`), and a recap↔follow-up boundary keeps a recap a *reference* to discrete follow-up records rather than their sole container. `novel`-class; additive; QA PASS on all 3 cards, 0 defects.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### declarative-gating-model (version-less) (2026-06-24) — Declarative cross-methodology gate conditions: the work-item type-pack meta-schema can now declare a gate whose pass/fail condition is a *related* work item's workflow status — or a *set-aggregate* over related items (a Kanban WIP limit) — not just the item's own fields. Research-led `novel`-class release de-risked by two timeboxed spikes before the build.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-_unversioned.md)_

### v2.21 (2026-06-23) — Decision-rendering standardization: makes the hub's operator-facing Decision Briefing auditable along the "alignment with agent operating principles" axis (previously unstructured). Three coupled cards — information-sufficient briefings (#337: pre-load referenced spec content, enumerate the full option space incl. stance-implied options, render-before-`AskUserQuestion` with a `[STRUCTURAL-DEFECT: unrendered-gate]` marker), a K1 design-principle register + per-option `### Design-Principle Conformance` D-Gate subsection mirroring the live `Upstream compatibility` mechanism (#320, keystone), and standing per-gate-class framing directives that ADD-never-SUBTRACT (#321). `cross-cutting`; 3 issues; MODERATE / HIGH.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.20 (2026-06-23) — Field-lifecycle and CMDB automation: entity fields get a governed, agent-driven lifecycle and the skill registry becomes the platform's configuration-management catalog. Nine deliverables — shared + portfolio entity-lifecycle transition protocols (G8, #156/#1155), a G10 write-permission matrix governing who writes which field per stage aligned with the Agent Write Permissions table (#154/#208), skills that emit/consume the transitions (#1156), the `artifact_state`→canonical `lifecycle_state`+Domain reconciliation (`promotion_state` carve-out, dual-read removed, artifact-generator + artifact-lint migrated; #1865/#1866), the registry-as-CMDB evolution + per-project Artifact Register (Option A, ADR-038; #202), and a project-tracking integrity sweep (dormancy 10bd / overdue 3–5bd / evidence-backed gate; #752). `cross-cutting`; 9 deliverables; QA ACCEPT (~48/48 ACs, 0 defects); MODERATE / HIGH.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.19 (2026-06-22) — Co-management governance cleanup: finishes de-broadcasting the operator-specific naming from the public codebase (follow-up to v2.18) and single-sources the dual-framing status rule. Retires the legacy co-management compatibility shim (`project-initiator` + `project-schema §7` + the "accepted via shim" clauses), genericizes the residual K4 operator-config field names to `co_management_*`, and removes the redundant `CLAUDE.md.template` copy of the Dual-Framing Bridge rule (repointing its 7 citations to the one `OPERATIONS.md` home). Minor (shim retirement is a back-compat contract change); 2 issues #1846 + #1853; CHEAP / HIGH.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.18.1 (2026-06-22) — Operator-instance path decoupling: the operator needle/instance path now resolves through a single resolver (`pmo_instance_path()`/`pmo_localized_needles()`, `${CLAUDE_WORKSPACE_ROOT:-$HOME/Claude}` canonical per ADR-032, no new variable) instead of ~6 inconsistent inline literals; ships a tracked `core/config/localized-context-needles.txt.example` + create-once scaffold (gitignored content), persistence byte-identical across `update.sh` (pre-update backup), and a **fail-closed** PII pre-commit guard (warn-mode-initial) closing the silent fail-open. `cross-cutting`; 1 story #1830; advances #529/#1492/#383, absorbs #1826, design-bound ADR-032, resolver boundary coordinated with #1412; sequenced onto #1847 (Check-43-allowlisted). Patch off v2.18; MODERATE / HIGH.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### public-flip-depersonalization-enforcement (version-less, 2026-06-21) — Public-flip depersonalization + path-portability **enforcement**: the residual operator-handle leak is tokenized, the GitHub-Projects token vocabulary is registered first-class, and four warn-mode-initial guards (path-portability `deploy.sh` Check 43, depersonalization-token Check 44, the gh-issue-ops path-leak hook, the draft-file hook + repo-integrity gate) move the public-repo boundary from manual vigilance to standing enforcement; `operator.toml` gains lossless round-trip and the orphan `PMO_INSTANCE_PATH` converges onto `CLAUDE_WORKSPACE_ROOT`; 9 build issues + 1 verify-only, governance + hooks (no skills/packages touched)

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-_unversioned.md)_

### v2.18 (2026-06-21) — Hybrid co-management decouple + Hybrid-Two: the `Hybrid` methodology is now defined cleanly as a **user-configurable two-archetype combination** (`delivery_approach: [A, B]`, e.g. `[Scrum, Kanban]`) and decoupled from the co-management arrangement it was previously tied to — the legacy co-management field is renamed `dual_framing_enabled` (legacy key still accepted via a deprecation shim), the dual-framing status artifact is renamed the "Dual-Framing Bridge", the operator-specific token survives only as operator-local K4 config, and dual-framing behavior is unchanged; 2 issues, K1 schema + a registered artifact + registries + two governance files + 26 skills + the XC-06 gate (deploy syncs skills + rebuilds packages)

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.17 (2026-06-21) — Architecture-altitude discipline: every new capability is interrogated for its abstraction altitude (does it extend an existing platform seam, or solve point-wise?) as a forcing function at Stage 4/5 — closing the gap that let the version-claim design land host-coupled until the operator caught it off-pipeline; a Stage-4 T3 obligation rider + an `altitude` distinctness axis (point-fix / extend-seam / new-abstraction) + a blocking §4.7 design-review gate + a HOST-BINDING-LEAK leakage class with a warn-mode detector + a scoped A6.5 adversarial exception + `[adapters].repo_host` cross-references; 7 work items, governance-doc + one detector, no skills or packages touched (deploy no-op)

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.16 (2026-06-21) — Concurrent-release version-claim determinism: a release's version is now claimed atomically at the merge tag (defer-to-merge + ref-CAS) instead of reserved at Stage 4, so two releases in flight can no longer silently collide on a number and the slower one lose its slot — defined as a host-agnostic capability with a config-selected `repo_host` adapter (GitHub/git the v1 reference), founding ADRs + corpus standards + new release-claim tooling + a CI freeness gate + a machine-readable re-version ledger; 14 issues, governance/standards/pipeline/tooling, no skills or packages touched (deploy no-op)

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.15 (2026-06-20) — Role-Specialist suite GA: the final five Release-to-Sustain role agents (Release Manager, Tier-1/2 Support, OCM Lead, Knowledge Manager) plus the capstone PMO Skill Router — the platform's role-Specialist suite reaches its full 19-Specialist roster, addressable as one routable unit, with a router that reads a single logical skill registry to send a role-shaped request to the Specialist that owns it; 6 new thin Specialists + the registry + ADR-035, additive, no application logic

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.14 (2026-06-21) — Autonomy-phase-out foundation: one canonical pipeline-wide rollout convention (`shadow → warn → enforce → removed`) plus a companion operator-touchpoint inventory + phase-out-plan schema — the language and structure for introducing, watching, enforcing, and eventually retiring a pipeline change are lifted from scattered per-mechanism homes into a single convention, and a schema is added for inventorying every human-in-the-loop checkpoint and planning each one's retirement; 2 `type:task` cards, additive governance/schema, no application logic

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.09 (2026-06-20) — Deploy + release-tooling health: 11 surgical fixes to the platform's own deploy and release scripts — several built-in checks that silently skipped their own job now run, and several maintenance scripts that pointed at pre-reorganization folder paths now resolve the live tree — sequenced D-C SINGLE (one branch, one commit per issue) because five issues edit `core/deploy/deploy.sh`; all additive or corrective, no application logic

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.04 (2026-06-20) — Close-out retrospective register template + a decided release-corpus public-vs-instance home: the per-release close-out ceremony ships as a public combined template (full Kerth retro + full PMBOK 7 lessons-learned) landed onto a ratified ADR-017-aligned decision (ADR-032) classifying every release-corpus surface S1-public-capability vs S3-operator-instance-content — #1412 designed-here (ADR; corpus migration execution DEFERRED), one combined #360+#361 template, `stage-13-close.md` §6 naming the register + its COEXIST relationship to the machine-generated learnings triple — 3 cards, two work-classes, no application logic

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.07 (2026-06-20) — Ambient intake automation, governed by one operator-set dial: the daily-processing cycle becomes a governed, scheduled, self-reporting capability bounded by a single plain-language `automation_level` ceiling (off / recommend / bounded_auto) per `effective = min(automation_level, per-action max)` — an `operator.toml` dial (#322), an ambient inbox + dedup cursor (#1159), scheduled Path-A intake sweeps (#1160) and Path-B external-sync with evidence-gated auto-close (#1161), a consolidated daily-status digest + sweep-health heartbeat (#1162), and a unified payload-triggered PreToolUse autonomy-ceiling enforcement hook shipped warn-mode-initial (#1163, ADR-031) — 6 cards, governance/spec/config + one hook + one SKILL.md edit, no application logic

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.08 (2026-06-20) — Artifact lineage graph + a recommend-only generated-surface lint: the generated-artifact surface (`08-Generated/` + promoted folders) gets a lineage graph and a graph-integrity lint, so duplicate / orphaned / stale / displaced AI-generated artifacts surface for operator approval instead of silently accumulating — horizontal-lineage frontmatter fields wired into both Domain A and Domain C of the artifact schema (#334) and a NEW Autonomy-Tier-1 recommend-only `artifact-lint` skill running five graph-integrity checks (#1165) — 2 issues, additive (a new skill + new optional schema fields), no existing field/file/behaviour removed or renamed

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.13 (2026-06-20) — Finding-disposition discipline: finding-disposition and deferral handling become uniform across the pipeline — behavioral acceptance criteria survive the gates intact, requirements-clarity rejects route upstream, and deferred items gain validity criteria plus a re-evaluation cadence. Milestone `63-finding-disposition-discipline` (#100 / #215 / #221 / #285), PR #1657 (merge `b2c12ad`). CHEAP reversibility — `git revert -m 1` of PR #1657.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.12 (2026-06-20) — Field-first intake enforcement: Stage 1 intake moves to field-level form enforcement and a field-first data architecture — structured Projects/Jira fields become the source of truth over label/body parsing, and the intake template gains explicit domain representation. Milestone `12-field-first-intake-enforcement` (#27 / #54 / #344), PR #1699 (merge `28860f1`). MODERATE reversibility — `git revert -m 1` of PR #1699.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.11 (2026-06-20) — Role-Specialist delivery coverage: twelve principal-level role-Specialist agents — Product Owner, Business Analyst, Program/Project/Portfolio Manager, Scrum Master, Release Train Engineer, Principal/Software Engineer, Architect, QA Lead, DevOps/SRE — each composing the platform's existing function-skills rather than re-implementing them. Milestone `04-ROLE-delivery-coverage` (#450 + #1109–#1119), PR #1555 (merge `78ea10a`). CHEAP reversibility — `git revert -m 1` of PR #1555.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.10 (2026-06-20) — Agent artifact disciplines: two agent editing disciplines are codified — canonical-form application and reconcile-don't-annotate — and wired into the decision discipline. Milestone `24-agent-artifact-disciplines` (#144 / #413), PR #1556 (merge `cb81165`). CHEAP reversibility — `git revert -m 1` of PR #1556.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.06.1 (2026-06-20) — Deploy-detection hotfix: repairs `deploy.sh --deploy` change-detection broken by a sidecar mis-enumeration and a BSD-`sed` alternation no-op, so the deploy path again correctly detects and syncs edited skills on macOS. Hotfix of milestone `36-ci-gate-trustworthiness-and-parallel-pr-safety` (#1475), PR #1474. CHEAP reversibility — `git revert -m 1` of PR #1474.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.06 (2026-06-20) — CI-gate trustworthiness + parallel-PR safety: the platform's automated checks get a stated contract — a green check must mean the thing it claims is actually true, verified against real content rather than a stand-in signal — plus parallel-PR safety hardening. Milestone `36-ci-gate-trustworthiness-and-parallel-pr-safety` (#1101 / #673 / #18 / #90), PR #1461 (merge `a74f814`). CHEAP reversibility — `git revert -m 1` of PR #1461.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.05 (2026-06-19) — Agent-discipline codification: a cluster of working agreements that had guided the platform's agents session-to-session now live in the platform's own rules, and a new architecture decision defines how knowledge moves from an agent's memory into the durable corpus — and how the redundant copy is cleaned up once it lands. Milestone `35-agent-discipline-codification` (#58 / #63 / #132 / #426 / #427 / #502 / #527 / #530 / #531 / #546), PR #1409. CHEAP reversibility — `git revert -m 1` of PR #1409.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.03 (2026-06-18) — Role-skill factory foundation + first two pilots: the role-Specialist factory exists end-to-end and ships its first two products — a feeding-document consumption path wired into `pmo-skill-refiner` (#186), the 14-section `pmo-feeding-document-v02.md` format reference and 5 `operations/skills/_shared/` building-block files all role skills consume (#186), the 2 pilot agents pmo-technical-program-manager (composes pmo-technical-analyst + delivery-engine) and pmo-program-coordinator (composes tracker-manager + daily-status) built via the refiner (#185), and the feeding-document format locked post-Phase-1 (#185) — 2 skill-build cards, no application logic beyond the skills themselves

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.02 (2026-06-18) — Bundling capacity + sizing/parse-rate gates: the pipeline's Bundle stage enforces capacity and sizing as gates rather than guidance — a risk-weighted release-capacity model keyed on Release Class with sizing-guidance consolidation and a pinned rounding mode (#290, ADR-027), a per-milestone story-point-sum gate that routes oversize milestones to disposition (#294, G3-15), a Mode-A ≥90% parse-rate gate against the conformant-bundleable denominator (#293, G3-14), and a RELEASE_LOG velocity instrument capturing planned-vs-delivered / files-changed / allocation actuals (#281) — 4 governance/spec/schema cards, no application logic

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.01 (2026-06-16) — Skill-function hardening (estimation / lifecycle / status): seven existing function-skills deepened (delivery-engine estimation enforcement + 15-stage lifecycle tracking + 10-gate enforcement + tech-debt capacity floor + Fowler-quadrant/CoD classification; ppm-agent intake-governance/WSJF + org-model/stale-RAID; tracker-manager RAID dedup + cascade; weekly-rollup watermelon via-public-api + metric governance; daily-status formula RAG + buffer; process-designer INVEST/GWT/NFR; technical-analyst ADR-immutability/FDD/DORA) — 12 EXTEND-class stories, no new skills

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v2.00 (2026-06-15) — Skill-function hardening: five existing function-skills deepened (change-management Mode G adoption tracking + tiered Mode D hypercare with SLA compliance; release-executor quality-gate ladder + progressive rollout + documented close-out; pmo-qa-auditor 8-detector failure-mode battery + RACI gate; artifact-generator canonical lifecycle states + zombie detection; release-planner typed artifact linking) — 11 build cards, all EXTEND except the release-planner RECONCILE card; no new skills

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_

### v1.24 (2026-06-14) — The Stage-2 Triage A6.5 management-task detection bash is corrected so its automated backlog-hygiene checks stop silently returning zero: the orphaned-dependency check (Pattern 1b, #309) and the P1-blocked-by-lower check (Pattern 2a, #309) now read each issue's body section and return correct results, plus a stale cross-spec citation in `release-process.md` is fixed (CF-3) and the de-scoped `yq`-toolchain finding (CF-1) is evidence-closed against ADR-012's retirement of G3-13

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v1.md)_

### v1.23 (2026-06-14) — The change-management skill becomes a pluggable methodology toolkit (ADKAR consolidated as the single source of truth + Kotter/Lewin/Bridges/McKinsey-7S codified + a `methodology-selection` selector wired into SKILL.md Step 2.5), plus an intake-governance standard (`intake-desk`) and a cross-pipeline sub-task methodology reference

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v1.md)_

### v1.22 (2026-06-14) — The platform's deploy toolchain runs deterministically: eleven deploy-toolchain defects + one folded-in same-class defect repaired on `core/deploy/deploy.sh` (the most-edited file in the repo) and the release tools, fixing two `grep -c … || echo 0` arithmetic double-emits (Check 19 #76 + Check 28/29 #1058), a silently-swallowed read-only `references/` install failure (#88), a `cowork_install_path` `#`-clip (#332), a `blast-radius.sh` non-ASCII locale crash (#92), missing Check-18 `--report` parity (#104), a Check-17 env-var rename (#111), four LOW cleanup findings (#331), and adding a mode-invocation-drift Check 35 + `### Mode Provenance` block (#26)

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v1.md)_

### v1.21 (2026-06-14) — The platform's automated quality-gate surface gains two detection capabilities: a template↔schema contract-fidelity check (`deploy.sh` Check 34, #318) that asserts each schema-bearing canonical template carries every section its governing schema mandates, and an L5 cascade-completeness gate (`pmo-qa-auditor` G8, #79) that greps a Stage-5 spec's changed-file set for un-swept OLD-value cascade occurrences as defense-in-depth over the existing L1–L4 prevention surfaces

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v1.md)_

### parallel-launch-quota-budget-gate (2026-06-14, version-less) — The release pipeline becomes quota-aware: a dual-checkpoint quota-budget gate estimates and re-validates parallel spoke/subagent waves against the operator's per-account usage-window envelope (Checkpoint A plan-time #23, Checkpoint B runtime #23) over the canonical Per-Account Usage Window Constraint subsection (#24), so a parallel batch never half-completes from cumulative usage-window exhaustion

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-_unversioned.md)_

### v1.20 (2026-06-14) — Project health and RAID escalation become deterministic: a single canonical cross-level metric → RAG-threshold → decision-rule index (#271), a watermelon-detection reference that consumes it (#270), an escalation-thresholds doc-of-record (#269), and a stale-RAID auto-escalation protocol on the `OPERATIONS.md` mirror pair (#261), so the same situation produces the same color and the same escalation regardless of which skill reports it

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v1.md)_

### v1.19 (2026-06-14) — Critical/High escalations across the skill suite now emit a single shared SIOR (Situation / Impact / Options / Recommendation) block — authored once in a canonical protocol doc and consumed by relative link — with a mandatory Recommendation + confidence and a named decision owner: comms-writer as the reference implementation (#179), ppm-agent [COMMS] routing (#178), pmo-technical-analyst CRITICAL/HIGH findings (#177), and delivery-engine dependency/RAID surfaces (#934)

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v1.md)_

### v1.18 (2026-06-14) — The release pipeline gains a durable, repo-agnostic model of the concurrent + planned release set and its mutual impact, consulted at Stages 3/4/5/9/12: a structural-blast-radius (F1–F6) contention axis at Stage 3/4 (#87), cross-release coherence at Stage 5, a GO baseline-currency criterion (G-PR9) at Stage 9, and a semantic GO-invalidation check at Stage 12 Phase A.5

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v1.md)_

### v1.17 (2026-06-14) — The two PMO authoring skills narrow to their PMO-unique surface and offload commodity work to Anthropic at design time: comms-writer narrowed to 6 PMO-unique types (#174) + owning exec-brief/stakeholder-email via own-with-harvest (#173), artifact-generator catalog narrowed to 27 + a Wrapper Mode and Part-D routing tree (#176, #175), and a duplicate ADR-022 collision resolved to ADR-023 (#791)

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v1.md)_

### v1.16 (2026-06-13) — The delivery-engine sizes capacity, holds estimate ranges, and gates the lifecycle: a capacity model (#272), an estimation standard owning the focus-factor table + Cone-of-Uncertainty + range enforcement (#273), a 15-stage universal delivery lifecycle (#274), and an eleven-gate project lifecycle (#275)

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v1.md)_

### v1.15 (2026-06-13) — The platform measures and improves its own agent quality: a base-vs-build platform-health audit mode on a cadence (#358), a data-driven 6–10 failure-mode target range for complex skills (#359), an invokable RCA method + a migration playbook with intake/pipeline handoff (#754), and a verified triage of the 34-day-stale harness audit (#125)

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v1.md)_

### v1.14 (2026-06-13) — The adapter-config foundation: one discoverable platform-config surface with a 5-rung hierarchy resolver (#22), and a clone→working-install onboarding-umbrella spine with four host extension points (#703) — the two surfaces every host adapter composes into

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v1.md)_

### v1.12 (2026-06-13) — Corpus durability enforced by two file-disjoint CI gates: shared references single-sourced with a collision-detecting rebuild check (#316), and raw issue/PR/milestone URLs prohibited outside the named 5-surface release ledger (#311)

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v1.md)_

### memory-to-corpus-codification (2026-06-10, version-less) — Verification-only close-out of the memory-to-corpus codification scope (nine encodings verified pre-shipped with provenance; operator-instance eviction of the three superseded memories; tickets marked closed on evidence)

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-_unversioned.md)_

## Operations capability — Intake

### intake-elicitation-skill (2026-06-06, version-less) — Conversational intake front door (`intake-desk` — meets an idea at any altitude, types it, elicits the right fields, applies the 5-test live, and logs a typed item — never a scratch file)

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-_unversioned.md)_

## v3.* — Automation & Enforcement

### v3.56 (2026-07-02) — Stage-3 bundling composer + version-identity: a self-triggering Stage-3 composer

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v1.13 (2026-06-13) — Runtime test gating + the platform's first standing regression corpus: runtime code tests run in CI and gate releases (#319), code-test outcomes are machine-readable in the pipeline (#430), and a standing sandboxed install/onboarding/update regression suite gates releases (#706). Re-versioned v1.12 → v1.13 (concurrent corpus-durability-enforcement claimed v1.12); deploy no-op

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v1.md)_

### cross-reference-integrity-ci (2026-06-13, version-less) — Cross-reference & reference-durability CI: the pre-commit hook's reference-integrity enforcement is now mirrored into three file-disjoint PR-time CI gates (positional `#N` parity + link-check + forward IMP-XXX / skill-count drift), warn-mode-initial

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-_unversioned.md)_

### v3.20 (2026-06-07) — Release-corpus verification surface (real checks, not grep proxies): corpus tools rewired to the live layout + fail-loud deploy-checks; ≥90% parser; live-taxonomy labels

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### public-flip-install-blockers (2026-06-04, version-less) — Fresh-install & onboarding blockers gating the private→public flip (zero-skills fix + no-Cowork deploy + config-first install path + tamper-aware update)

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-_unversioned.md)_

### v3.19 (2026-06-03) — Stage 13 close-out reliability backstops (completeness gate + outcome-bound mandatory close + tooling smoke gate + slug/Keychain hardening + working orphan `--apply`)

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

### v3.18 (2026-06-03) — Corpus-integrity enforcement (reference-durability standard + three-primitive spine + three PR-time integrity gates)

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v3.md)_

## v1.* — Pipeline Discipline

### v1.11 (2026-06-12) — Cleanup-orphan-state reliability: the workspace sweep gains a SELF guard, an all-process liveness gate (ADR-021), and a fixed-point apply — safe to run from anywhere, complete in one pass

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v1.md)_

### v1.10 (2026-06-12) — Failure-mode coverage completion across all five categories: 45 fresh-authored domain-specific entries take all 23 skills to 100% coverage in TRIG / INPUT / PROC / OUT / HAND, with version-field + packaging hygiene riding along

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v1.md)_

### v1.09 (2026-06-11) — The agent-to-script promotion framework (when repeated agent work earns a script, and what a script owes once it exists): the AS0–AS4 ladder (ADR-020) + six governed decision-table sections, grounded in an executed 71-script census + ranked 8-candidate inventory

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v1.md)_

### v1.08 (2026-06-08) — The skill-suite architecture spine (decomposition axis + factory guard): a binding decomposition rule (ADR-019 — Specialists compose, not absorb) + the audited + codified skill↔pipeline alignment standard (DT-1/2/3) + a fail-loud Anthropic-scaffolder-drift guard in `pmo-skill-refiner`

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v1.md)_

### v1.07 (2026-06-07) — The work-item type layer (methodology-agnostic, best-practice TEMPLATE: a thin generic `Work Item` entity beneath Milestone/Workstream + a domain-neutral work-organization mapping framework + a declarative type-pack meta-schema; decoupled from the release-pipeline ticket model)

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v1.md)_

### domain-aware-stage5-design (2026-06-07, version-less) — Domain-aware Stage 5/7 design (design no longer pmo-markdown-bound: keystone `domain:` field + governed design-exploration step + per-domain impact-analysis selector + domain-best-practice review criterion + the first two domain guides)

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-_unversioned.md)_

### v1.06 (2026-06-06) — Pipeline-discipline capability slice (first fix-now/defer/accept finding-disposition framework for Dev Testing + QA, single-sourced via Option-C extraction; Stage-5 template BLOCK-DESTRUCTIVE-022 guard)

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v1.md)_

### v1.05 (2026-06-05) — Pipeline-discipline capability slice (design-artifact pilot + centralized diagram home; reusable surface-enumeration checklist; release-plan frontmatter complete)

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v1.md)_

### v1.04 (2026-06-02) — Stage-4 planning discipline (currency + crisping gates, plan versioning, methodology-design variant)

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v1.md)_

### v1.03 (2026-06-02) — Bundle protocols (domain sourcing + Parallelization Map + cascading-rebundle)

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v1.md)_

### v1.02 (2026-06-02) — Triage/Bundle protocol codification

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v1.md)_

### v1.01 (2026-06-01) — Pipeline-discipline capability slice

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v1.md)_

## v2.* — (arc TBD; rename at major-prefix maturation)


### 63-finding-disposition-discipline (2026-06-21, version-less) — **Finding-disposition and deferral discipline, applied uniformly across the pipeline**

### release-version-stamping (2026-06-20, version-less) — **The platform version file stays accurate after a release** — a `Routine`-class version-less release fixing #1643: the repo-root `.version` source-of-truth had frozen at `v2.08` *inside* the `v2.09`/`v2.10` tags (the bump was an unwritten manual habit that became permanently absent once Stage 13 was automated), so `notify-version-skew.sh` reported a perpetual "update available" no `git pull`/`update.sh` could clear (the stale value is committed at the source; `update.sh` only re-propagates it per ADR-017). 3-layer fix: **(L1)** corrects the stale value so `.version` tracks the shipped release line; **(L2)** makes the stamp an owned Stage 13 step — a new `phase_bump_version` in `release/tools/automated-closeout.sh` (dry-run/PR-body wired, `.version` added to the chore-commit staging list, idempotent) + declaring `.version` in the `release/references/pipeline/stage-13-close.md` output set + a `docs/UPDATE.md` clarification; **(L3)** adds `core/deploy/deploy.sh` **Check 39** asserting `.version` equals the latest reachable release tag (FAIL ≥2 minors behind, WARN-tolerant of the legitimate 1-version Stage 12→13 window), with regression coverage in `core/deploy/tests/test_version_stamping.sh` wired via `.github/workflows/install-tests.yml`. Version-less per operator Stage 4 decision following the `public-flip-install-blockers` / `cross-reference-integrity-ci` precedents — no tag, no GitHub Release. Additive close-out tooling + a warn-tolerant check; CHEAP / `git revert -m 1` of PR #1693.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-_unversioned.md)_

### 12-field-first-intake-enforcement (2026-06-21, version-less) — **Intake checks more of a work item's form, and keeps its priority in sync**

### 38-governance-cross-reference-currency (2026-06-26, version-less) — **Stale internal links across the docs and skill files are corrected**

### v2.37 (2026-06-28) — **Published release notes can no longer silently drift from the note we wrote** — a `routine`-class release-notes close-out conformance slice making the close-out conformant on **every** authoring path (script AND hub-direct) and keeping the public GitHub Release page faithful to the in-repo source note. **#2120** extends the note-content lint to catch the posting defects the structural check missed (un-versioned Release title; repo-relative body links that 404 on the published page) via a whole-body link-purity check (lint check 13), reconciles the un-versioned title-format descriptions across `release-notes-standard.md` + `stage-12-execute.md` to the single canonical `vX.Y — <headline>` form, adds two `gh release view`-based LIVE-Release assertions to the Stage 13 close runbook, and syncs the eval rubric 12→13. **#2082** binds the §3.2 note-content lint to the close EVENT (script + hub-direct paths), so a finding for the closing version blocks milestone close regardless of how the release is finished — the RCA fix for the v2.26 governance failure. **#2085** makes the published Release body an enforced deterministic frontmatter-strip transform of the source note (never hand-assembled) with a standing drift gate (`check-release-body-drift.sh` / `deploy.sh` Check 47) asserting body-equals-note after the fact. **#1290 (absorbed, ms 72; ADR-048)** establishes that rigor is an invariant of scaffolding — a named Rigor-Invariance Principle + canonical-checklist attestation at the Procedure 1 scaffolding seam plus a scaffold-independent `deploy.sh` **Check 48** that asserts the complete Stage-13 output set on main for every `VERIFIED` `RELEASE_LOG` row (warn-mode-initial, dormant behind a cutover sentinel), so an abbreviated scaffold can no longer drop a codified close step undetected. Additive standard / lint / check / governance edits + one founding ADR; v2.37's own new gates are cutover-anchored after this merge (reflexive-loop). CHEAP / `git revert -m 1` of PR #2378.

_Archived: [segment](RELEASE_DIGEST_ARCHIVE-v2.md)_
