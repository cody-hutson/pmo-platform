<!-- repo-integrity: allow-issue-ref -->
<!-- repo-integrity: allow-memory-ref -->
<!-- reference-durability: allow-link -->
<!-- reference-durability: allow-url -->
# RELEASE_DIGEST_ARCHIVE-v3

Archive segment of [`RELEASE_DIGEST.md`](RELEASE_DIGEST.md) — the **v3** release family.

This file is the same record as its parent ledger, relocated. It is a **Vital**
record under `core/governance/RECORDS_POLICY.md`, retained permanently, and it
inherits its parent's class: a segment is a disposition *destination*, never
itself a disposition *source*. Nothing here is a lesser record for having aged
out of the working set.

It lives in the same directory as its parent deliberately, so `grep -r` over
that directory still finds this content exactly as it did before the move. Each
entry below retains its heading in the parent ledger, with a pointer here.

Entries are appended by `release/tools/sweep-release-corpus.py`; the file is
append-only and is never itself swept.

---
### v3.100 (2026-07-29) — The release pipeline can now record the decisions it makes

### v3.99 (2026-07-29) — Release runs now say how reversible each decision is, and where they stand
A `novel`-class release on milestone #294 (`release-hub-response-convention-enforcement`), one release PR (#4171), 2 audit-derived conformance slices (#4020 `size:S` → #4021 `size:M`) built serially on one branch, 5 files (+551/−10). **#4020** — the `## Output Contract` scope clause bound every requirement to *"every **Mode R** emission"*, leaving Mode O (where orchestration actually happens) with no labelling duty at all: 41/75 = 54.7% conformance. The fix widens the opener to bind **both modes**, marks the genuinely Mode-R-scoped requirements inside the list, retargets requirement 5 to every decision-class emission in either mode, and adds an **emission-time predicate** paragraph (one question posed while composing, a closed enumeration keyed to the hub's Procedure vocabulary, an operational residual making it a floor not a ceiling, a ceremony bound, the literal `[TIER · confidence: LEVEL]` format at point of use, and the disqualifier that prose reasoning does not satisfy the rule). **#4021** — briefing-render gate + `Stage · gate · progress` anchor (89/117 = 76.1%). Gate 3 gains an emission-time self-gate citing item-1 **by reference** (so it cannot drift against #4020's new field), names **declared deferral** as the legitimate consolidated-briefing form with a falsifiable deferral-vs-skip discriminator, and the briefing contract's declared Mode-O-only scope is reconciled at its H1/intro. A net-new anchor requirement is **non-waivable on short mid-stream turns**, with a matching 5-field Domain-Specific Failure Mode entry under category **OUT**. **D-0 (operator-adjudicated):** the ticket premise was falsified — 31/31 prompt-firing turns *did* render a briefing and all 28 defects fired no prompt; the real defect is the ungoverned deferral, so deferral became a declared form rather than an unnamed exception. **Concurrent-release renumber:** v3.98 independently claimed slot 7 with its own requirement and made the same `Six`→`Seven` bump; resolved by evidence (v3.98's req 7 self-references its number and carries 2 live citations; ours had zero inbound citations by number) — ours renumbered to **8**, count reconciled to **`Eight`**, a value neither release specified alone. Both rate ACs (≥95% / ≥90%) recorded **declared-verification-deferred** into the 30-day outcome window, method = Round-2 `agent-response-governance-audit`; the deferred record carries the instrument-ceiling discriminator (a perfect agent ceilings at 70/75 = 93.3% and 100/117 = 85.5%). Stage-7 PASS · Stage-8 ACCEPT · Stage-9 GO-WITH-CONDITIONS (both cleared) · CI 41/41. **MODERATE reversibility / HIGH confidence** — `git revert -m 1` of the merge plus a package rebuild, read against v3.98's neighbouring requirement rather than applied blind.

### v3.98 (2026-07-28) — Readiness checks now catch work another epic owns, and render a map

### v3.97 (2026-07-27) — Portability is now a first-class engineering value

### v3.96 (2026-07-26) — Measure and attribute agent token spend from local session data

### v3.95 (2026-07-26) — Runtime posture is now operator-configurable: the Stage-2 backlog-decision surface, security-hook activation, and spoke model/effort all resolve from canonical config that survives updates, and v2.00's referenced config keys now exist with defaults
A `novel`-class release on milestone #183 (`56-runtime-config-and-posture`), **D-C SINGLE** topology, one release PR (#4037), 5 slices (#1212 → #102 → #340 → #310 → #339) built P0 fully-serial on one branch, 39 files (+1,655/−32). **#1212** back-fills the `[failure_mode_detectors]` (D6 concurrency-ceiling / D8 walk-back-rate) + `[progressive_rollout]` config sections that v2.00's features already referenced, mirrored across template + schema + catalog. **#102** de-hardcodes the Stage-2 backlog-decision surface (G2-06/G2-12, symmetric twins) from a single GitHub Projects board to a config-field role (Projects the unchanged default). **#340** gives spoke model/effort one canonical `[spoke_runtime]` home (effort a pointer, no shadow-SSOT) and founds **ADR-095**, reversing the discoverability half of the prior "two surfaces by design" stance; `deploy.sh` Check 27 now reads it. **#310** ships opt-in, update-durable security-hook activation with a durable master-enable shared-lib — under a **SECURITY-EXCLUDED floor** so master-OFF never silently disables the egress / PII / credential / gh-path-leak guards (new Check 60, warn-mode). **#339** verifies the launched session's model/effort against `[spoke_runtime]` at SessionStart (non-blocking). Stage-7 PASS · Stage-8 ACCEPT (30 runtime assertions re-run) · Stage-9 GO · CI 44/44. Re-versioned provisional v3.93 → v3.95 at Stage-12 (siblings #4029/#4030 claimed v3.93/v3.94); the ADR and the new check renumbered next-free in the same conflict-resolve (093 → 095, 59 → 60); no orphan tag, no re-version ledger row. **MODERATE reversibility / HIGH confidence** — a single `git revert -m 1` of the merge; the config additions are inert defaults and the security hooks default OFF.

### v3.94 (2026-07-26) — Intake and gate hardening: a Stage-2 acceptance-fit gate (reject-by-default for agent-authored / net-new-sweeping items), an issue-body author-association trust boundary, a module skill-consultation forcing function, and an extend-before-create gate at Stage 5

### v3.93 (2026-07-25) — Release version numbers now bind when a release ships, not when it is planned

### v3.92 (2026-07-25) — Architecture baseline: a consolidated actor-model + governance-as-contract statement, a cross-chain index, and a standing as-built conformance audit that checks delivered work against the platform's own architecture

### v3.91 (2026-07-25) — Cross-platform install foundation: Windows entry-points, a doctor command, and safer multi-destination tracking

### v3.90 (2026-07-25) — Accurate release-impact analysis: path-true counts + directory-move detection

### v3.89 (2026-07-25) — Reusable Change Impact Matrix and Training Plan templates, plus a RAID-log template header reconcile

### v3.88 (2026-07-25) — Six deploy & tooling defects cleaned up — including an XSS sink hardened

### v3.87 (2026-07-24) — Design diagrams and process flows in the docs are now labeled and findable
A `novel`-class knowledge-corpus release on milestone #291 (`design-artifact-backfill`), one release PR (#3796), 66 files (+809/−22), 14 `.skill` packages rebuilt. The platform's *default* design artifact — a section embedded in a parent doc — previously carried no marker, so the artifact set was unenumerable and its completion condition untestable. **#3725** ships the identification mechanism: `design-artifact-standard.md` §9 promotes `depicts:` / `flow_class:` to required, §2 corrects the Human-process current-state cell, and a new §12 adds per-flow-type detection criteria (table-aware — 3 of 7 flow types render as tables and are blind to any fence/Mermaid heuristic) plus the enumeration query; **ADR-089** records the chosen section-level `<!-- design-artifact: … -->` marker over a drift-prone frontmatter index. **#3614**'s spike produced the real gap as a query result and rendered **D-FissionScope → COLLAPSE**: 5 of 6 planned builds were already covered, so **#3798** swept them into 24 embedded-marker declarations (rebuilding the 14 skill-flow packages), and only **#3441** human-process kept a full build — the new dedicated `docs/release-record-keeping.md`. The set at ship time is **24 embedded + 2 dedicated-frontmatter** declarations across the seven flow types. Re-rendered provisional v3.86 → v3.87 after a concurrent claim; no orphan tag, no re-version ledger row. **MODERATE reversibility / HIGH confidence** — a single `git revert -m 1` of the merge; the only wrinkle is re-stripping the markers, nothing to migrate.

### v3.86 (2026-07-24) — Trackers now match each project's methodology, and stale analysis notes get flagged

### v3.85 (2026-07-24) — Release close-out now guards its published notes, packages, and ledgers

### v3.84 (2026-07-24) — Two releases can now finish at the same time without tripping over each other

### v3.83 (2026-07-23) — Self-measurement for release close-out; a session sensor that ships switched off

### v3.82 (2026-07-22) — Exec briefs name the real consequence; status updates cite their sources

### v3.81 (2026-07-22) — Theme-named releases no longer vanish from the release record

### v3.80 (2026-07-22) — Release close-outs are now reliable for theme-named releases

### v3.79 (2026-07-17) — Both link checkers now agree, and the deploy tooling stops repeating itself

### v3.78 (2026-07-17) — PORTFOLIO.md composed deterministically from per-project rollups: freshness-gated health scoring, cross-component risk aggregation, and a portfolio/program/project tier taxonomy
Project→portfolio rollup made explicit and deterministic — one PR (#3549), 5 issues, Class **cross-cutting** (6 in-bundle compositional edges), 13 files (+1764/-7), dependency-ordered #2578→#157→#1771→#276→#1169. **#2578** codifies the portfolio/program/project tier taxonomy (neutral structure + naming conventions) into `core/CLAUDE.md.template` + `core/governance/OPERATIONS.md`, supplying the program tier that previously had no home between portfolio and project. **#157** (foundation) introduces the `portfolio-writeback-contract` standard — the bundle's novel surface — plus `operations/templates/project-rollup-template.md` (the composed-entity template) and the `project-entity-model.md` emit/consume wiring. **#1771** lands `core/deploy/tools/compose-portfolio.py` (+970), the deterministic composer, covered by proj-alpha/proj-beta rollup fixtures and wired into `release-tooling-smoke.yml`. **#276** adds right-sized three-layer health scoring with a freshness hard-gate + integrity multiplier (so a subsystem failure cannot average up to Green), and **#1169** adds methodology-neutral cross-component risk aggregation — both in `weekly-status-rollup` (+162/-4); `ppm-agent` (+26/-1) consumes the contract. Shipped version-agnostic: provisional v3.75 was claimed mid-Stage-6 by a concurrent release, and the atomic ref-CAS claim recomputed next-free = **v3.78** at anchor v3.77, winning on the first attempt. DT 5/5 PASS · QA 24/24 MET · Stage-9 GO. Close-out required landing #2539 first — the pure-alpha slug resolver fell back to the Version field, aborting phase 6 and reporting a false 0-open (41 of 133 LOG rows were mis-resolving; 0 regressions after the fix). Size-ceiling override G3-15: 26 effective pts > 25 ceiling, operator-elected single unit on synthesis-layer coherence. MODERATE reversibility.

### v3.77 (2026-07-17) — On-demand health-check rollups, semantic routing-conflict detection, and a clearer SKILL.md sizing rule
A skill-suite hardening release — three disjoint slices on one PR (#3540). **#155** extends `health-check` with a `rollup` mode (`--scope portfolio|project`, `--depth full|status`) under a compose-not-absorb scope-lock (ADR-019): `--scope portfolio` invokes `weekly-status-rollup` Section 6 for the PORTFOLIO.md composition and stages the proposal to `08-Generated/_health-check/`, never writing the bridge file; the 7 existing modes + the 5-section contract are byte-identically preserved. **#2699** adds `run_routing_audit.py` — a three-tier Jaccard→embed→judge pipeline with a lazily-imported optional embedding backend + stub fallback that keeps the zero-dependency offline CI contract intact; **ADR-084** records the optional-heavy-dependency precedent, and `run_eval_audit.py` is imported read-only. **#3114** re-baselines the ~500-line SKILL.md authoring guideline in `canonical-skill-structure.md §5` as advisory, distinct from the hard `references/` gate (`C6_LINE_THRESHOLD=400`); additive, no skill splits. Re-anchored provisional v3.75→v3.77 after concurrent releases claimed v3.75/v3.76; commits re-signed (CLEAN merge, no admin override). All three slices Stage-8 ACCEPT, Stage-9 GO. CHEAP reversibility.

### v3.76 (2026-07-17) — The knowledge base now orients a fresh reader: frontmatter enforced corpus-wide, self-audit paths refreshed, vision + install model codified
Knowledge-corpus hygiene across `core/` — one PR (#3545), 5 issues, Class **novel**. #2221 frontmatter backfill (38 docs) + Check 50 → global committed-default enforce (Approach D); #2701 REFRESHES AUDIT_FRAMEWORK (19 stale paths → current homes, methodology retained; Collective Review reversed RETIRE→REFRESH on adversarial evidence); #2917 single-sources SPI bands to channel-formats.md; #2702 classifies `[PROJECT_KEY]` example-data; #380 codifies vision/build-surface + ADR-083 (extends ADR-017). Squash-merge `da4c116`, CI 35/35; re-versioned v3.75→v3.76 (concurrent milestone #244 took v3.75). CHEAP.

### v3.75 (2026-07-17) — A first-class Finding entity and an ownership-collision build check
A housekeeping release for the platform's data model and skill packaging. **#2166** promotes the findings register to a first-class `Finding` entity (#19) per **ADR-044** — a §3.19 field schema (8 validation rules + 4 negative tests), a §6 owning-agent triplet (created by `build-reviewer` / `pmo-qa-auditor` / `pmo-technical-analyst`, maintained by `pmo-qa-lead`), and a §7 storage row — and reclassifies the remaining no-entity-home skill outputs (exec status, weekly roll-up, decision briefing, daily-status Teams, comms drafts) as ownerless *renderings*; the draft `severity` enum was reconciled to the shared `{CRITICAL,HIGH,MEDIUM,LOW}` model at Stage-5 review. **#2168** wires the ownership model into `deploy.sh` as Check 54 (ADR-044 I1/I3/I4) — it reconciles declared skill outputs against the owning-agent matrix and escalates on a second maintainer, renderings exempt: 0 collisions / 0 false-positives across 19 entities on release. **#2074** converges `extract-roster-needles.sh` on the shared `pmo_people_roster()` accessor; **#2066** retrofits a `design-exploration.md` ASCII flow-block to Mermaid. Routine class; resumed from an interrupted prior hub session (Stages 4–8 complete) and driven Stage 9 → 13 this session; provisional v3.75 bound at Stage 12 with the concurrent v3.76 milestone re-anchoring cleanly. MODERATE reversibility.

### v3.74 (2026-07-12) — Durable CI gates lock the two build-security advisory classes; Security becomes the 6th first-class value
The durable regression-prevention layer the v3.73 security patch deferred ("handled separately") — CI gates that lock the two build-security advisory classes (the hook fail-open, GHSA-9cjm; the eval-viewer stored XSS, GHSA-rw36) against silent regression. Three slices on one PR (#3428). **#3407** adds a glob-derived fail-closed meta-test over every `block-*.sh` hook (asserts each jq-dependent hook fails closed `exit 2` at all three dependency-resolver paths) + fixes `block-autonomy-ceiling`'s enforce-mode fail-open (jq gate reordered mode-aware, closing the residual ADR-078 flagged) + **S2**: Security elevated to build-philosophy's **6th first-class value** (ADR-081) with a coverage-matrix row (Hooks populated, Agents/Hub-Spokes GAP) and a `software.md §Security` cluster. **#3409** adds a 3-rule custom `template-context-XSS` semgrep set (keys on the missing context-aware encoder, so the fixed generator passes un-annotated) + a reviewed nosemgrep baseline + the **S1** output-encoding standard + hardening of three residual eval-viewer sinks. **#3408** adds transport + jsdom rendered-DOM XSS regression tests + a net-new `eval-viewer-tests.yml` CI workflow + ADR-082 (jsdom canonicalization; first node_modules dependency). Original slice #3410 (deploy-path sourced-lib completeness + `positional-issueref.awk` co-deploy) DROPPED — subsumed by the concurrent v3.73 GHSA-g9g6 fix. Release Class **novel**; merged via merge-commit (not squash, to preserve the reconcile history) at `6d70303` with an admin override past branch protection, CI green 32/32; re-versioned mid-pipeline v3.73 → v3.74 after the concurrent hotfix claimed the provisional v3.73 slot. `pmo-skill-refiner.skill` rebuilt. CHEAP reversibility (MODERATE for the S2 build-philosophy charter edit).

### v3.73.1 (2026-07-12) — Security-hook fixes now reach workspaces you already installed
Delivery-path hotfix to the v3.73 security patch. `deploy.sh` (the engine behind `update.sh`) deploys skills + packages but not the `.claude/hooks` bundle, so a hook/helper security fix — such as the v3.73 GHSA-g9g6 awk-primitive co-deploy — never reached an already-installed workspace through `update.sh`; only a full `setup-workspace.sh` re-run delivered it (#3430). Adds `setup-workspace.sh --refresh-hooks`: a non-interactive, checksum-aware refresh of only the security-hook bundle that reuses the single-sourced `install_hooks` co-deploy list — an unedited platform hook is refreshed to source, an operator-edited hook (diverged from its recorded baseline) is preserved + warned rather than clobbered — plus an `update.sh` Phase 5c that delegates to it and flips the "changed" flag only on a genuinely refreshed hook, preserving the `EX_NOCHANGE` no-op contract. Release Class **hotfix** (code + one 7-case regression test `test_refresh_hooks.sh` + CI wiring only; no skill/governance/schema change, no `.skill` rebuild). Single PR #3431, squash-merged clean at `421658d`; CI 27/27; GHSA-g9g6 hook suite 23/23 and install end-to-end 12/0 unchanged. CHEAP reversibility.

### v3.73 (2026-07-12) — Security patch: hardened hooks + safer local eval-review tool
Coordinated-disclosure fix for two advisories. **GHSA-g9g6-28c9-vrx5** (LOW, CWE-693): the warn/enforce hooks `block-gh-path-leak.sh` + `block-fragile-refs.sh` skipped their check (`exit 0`) when a co-shipped internal primitive was missing/empty/corrupt regardless of `.mode` — and the reference-durability classifier `positional-issueref.awk` was never co-deployed, so its detector had never run on a deployed workspace. Now mode-gated fail-closed (enforce `exit 2`, warn/off degrade), verifying the primitive API (not mere file existence), a caller-owned `exit 2`, and co-deploying the awk in `setup-workspace.sh` + `setup-ci-layout.sh`; ADR-078 amended. **GHSA-fxcr-7x63-3g94** (MEDIUM, CWE-59/200/352): the eval-viewer generator followed symlinks + hardlinks out of the workspace (CWE-59/22), embedded secrets verbatim into the served + shareable static HTML (CWE-200/532), and accepted unauthenticated writes (CWE-352). Now reads are confined to the resolved workspace root (hardlinks rejected), embedded text is size-capped + best-effort redacted, and `POST /api/feedback` is gated by a loopback `Host` + same-origin `Origin` + a per-run CSRF token. Bundled in one PR (#3426), Release Class **security-patch**; verified via three adversarial refutation passes + a clean confirmation sweep (hook 23 + eval-viewer 47 regression cases, full hook suite 426/0); `pmo-skill-refiner.skill` rebuilt at close-out. Requires re-running `setup-workspace.sh` to pick up the hook + helper changes.

### v3.72 (2026-07-12) — The release orchestrator is safer and finishes cleaner
Hardens `release-hub` Mode R (readiness) + Mode O (orchestration). The readiness pre-flight is now strictly recommend-only (a new read-only guardrail + PROC failure-mode entry) and always routes to the next action (GO → Mode O; NO-GO → cleanup loop), and gains a methodology-neutrality + structural-cascade conformance check (checklist group 7, composing `bundle-composition-doctrine` + ADR-033 with zero inline logic). Mode O now routes Stage 12 through the release-manager tail (merge + atomic version-claim + DEPLOYED row, never a bare merge) and runs a per-wave pre-spawn concurrent-PR collision check. Five slices on one PR (#3405), Release Class routine; #2574/#2575 deferred at the Stage-5 scope-lock (owner-extension gap — require extending `release-planner` + `triage-design-rereview`). Self-executed via release-hub Mode O; DT/QA 15/16 AC MET with one count-cascade regression self-repaired; v3.72 claimed next-free above the concurrent v3.71 release.

### v3.71 (2026-07-12) — New projects use one simpler folder set and a single inbox

### v3.70 (2026-07-12) — Release pipeline re-verifies freshness and constrains agent write-access

### v3.69.1 (2026-07-11) — Security patch: the hook perimeter no longer fails open, and the fix actually deploys
Coordinated-disclosure patch for two advisories, plus the delivery-path fix that made the first one reachable. **GHSA-9cjm-v22x-4x33** (High, CVSS 7.3): the PreToolUse security hooks failed **open** — permitting the action — when `jq` was unresolvable under the pinned PATH, which silently disabled the whole perimeter on documented Homebrew installs. Fixed by a shared absolute-path `jq` resolver (`core/hooks/lib/dep-resolve.sh`) plus **mode-coupled fail-closed** posture applied across the entire hook class (twelve hooks), with a second fail-open closed in the same pass — `block-fs-boundary` had permitted `..` traversal when `python3` was unavailable. Per-hook regression coverage across eleven of the twelve hooks (one new suite file plus ten extended; `block-fragile-refs` has no per-hook suite) plus a shared class-wide CI regression guard; **ADR-078** records the dependency-resolution posture. **GHSA-rw36-5pf9-w2vc** (Medium, CVSS 5.0): stored XSS in the eval-viewer — attacker-influenced eval output reached `innerHTML` render sinks unescaped; fixed with **per-sink** escaping plus defense-in-depth transport escaping and xlsx hyperlink sanitization, `pmo-skill-refiner.skill` rebuilt in the same merge. **The delivery-path fix is the load-bearing third piece:** `lib/dep-resolve.sh` was not co-deployed into `.claude/hooks/lib/`, so the hardened hooks could not resolve their shared helper in the deployed layout — the hardening would have shipped without reaching a single installed workspace. Caught and fixed (#3384) **before** the Release was published. Three merges (two from private advisory forks, which carry no PR number), 40 files (+1,551/−277); tag at `075390e5`. **Backfilled** into the release corpus at milestone #304 (#3727) — the release shipped correctly on 2026-07-11 but its Stage-13 ledger writes never ran.

### v3.69 (2026-07-11) — Platform maintenance: deploy-check validation cleared to green

### v3.68 (2026-07-10) — Release quality checks now run automatically and escalate only on failure

### v3.67 (2026-07-10) — Intake now adapts to your project's methodology

### v3.66 (2026-07-09) — Engineering documents now start from a canonical template, not a blank page

Authors the seven software-domain template files the shipped 5-Layer Template Architecture mapped to canons but left `(none — gap)`: ADR (Nygard 2011), Runbook (Google SRE §Runbook Design), Design doc (Google convention, FDD/TDD/HLD/LLD variant discriminator) in Wave 1 (#2149); RFC (IETF RFC 7322 + Rust RFC), PRD (`product-management:write-spec`), Postmortem (Google SRE §Postmortem Culture), Test plan (PMBOK 7 §Quality + `engineering:testing-strategy`) in Wave 2 (#2150). Each carries the 15-field L4 provenance header at `review_status: DRAFT` (never APPROVED/PROMOTED — proof-of-utility gate unmet), with `canon_compat` recorded as anticipated-path-at-DRAFT plus negative live-plugin evidence. Flips `template-taxonomy.md` §4.1–§4.7 + §6 rows 1–6 and adds a new §6 row 9 (Test plan) with an atomic count/§7-note/§2-gap-count sweep (§6 now 9 rows, 0 residual gaps). D-MirrorRegistration ruled canonical-only (zero `TEMPLATE_SYNC_MAP` entries; registration follows the first runtime consumer). Release Class **novel**; tracking umbrella #71 + two wave slices on a single PR / single merge, clean at `671e0bf` (provisional v3.66 claimed clean). Independent Phase-A6.5 adversarial review Major/0-Blocker (advisory); DT PASS both waves (3 Tier-1 fix-forwards cleared on re-review); QA ACCEPT ×3 at score 1.00. Six taxonomy-consumer `.skill` packages rebuild at close. Follow-ups #3289/#3290/#3291/#3299. Outcome SUCCESS; CHEAP reversibility.

### v3.65.1 (2026-07-04) — The pipeline no longer trusts comments from people outside your repo

Hardens the pipeline against prompt-injection through the comment channel. The pipeline reads issue/PR comment threads as its stage-I/O channel; this hotfix adds an author-association gate at the Inter-Stage Feedback Protocol so any comment from an account outside the trusted set (`OWNER`/`MEMBER`/`COLLABORATOR`) is surfaced to the operator and never read as stage content, instructions, or evidence — the check keys on repository relationship, never on how helpful the comment looks (#3261). Trusted-author feedback still flows normally; `CONTRIBUTOR` (merged-PR-only, no standing write) is deliberately excluded. Adds a non-blocking Stage-13 lock-at-close (Phase C5) for finished sub-task threads + the delivery card, and corrects a falsified "no review comments arrive" dormancy claim across 4 carriers. Release Class **hotfix** (operator override held at Stage 9; the cross-cutting mechanical trigger — 5 stage files — was weighed but the Deep-review substance was already met by an independent Phase-A6.5 adversarial review). ADR-076; single PR / single merge, clean at `814c9db`; A6.5 SURVIVES-WITH-FINDINGS (9 findings adopted), DT 11/11, QA 11/11. Issue-body ingestion gate routed to follow-up #3303. Outcome SUCCESS; MODERATE reversibility.

### v3.65 (2026-07-04) — The pipeline now tests what a release does, not just reads what it says

Moves the pipeline's quality stages from reading a change's source to exercising it. Stage 8 now consumes a domain-selected verification run as acceptance evidence and behavioral acceptance criteria get a worked verification method (`stage-08-qa-testing.md`, #103 — the Stage-7 runtime half shipped earlier); cross-issue ACs are expressed as testable predicates and checked as one integrated artifact before the Stage-9 gate (#170); the QC4-05/AV-1 invariant re-verification behind every gate's verdict is made structurally sound, replacing the grep/string-match mechanism (proven false-FAIL, possible false-PASS) with region-scoped verification (#99); and a verification-automation script runs a plan's Verification Plan programmatically as the shared executor for #170/#103 (#227). Release Class **cross-cutting** (spans Stages 4/6/7/8/9/13 + shared verification infrastructure); single PR / single squash merge, clean at `b1a2152` (provisional v3.65 claimed clean, no re-version); sequence #99 → #170 → #103 → #227. A doc-only release still passes Stages 7/8/9 unchanged (new checks fire as no-ops). Outcome SUCCESS; MODERATE reversibility.

### v3.64 (2026-07-03) — QA can now grade a release against its own acceptance criteria

### v3.63 (2026-07-03) — The release pipeline now runs on GitHub's native PR-review surfaces

Wires GitHub's own pull-request review machinery into the release pipeline and brings the two release-manager skills back in step with the spec they execute. The pipeline now opens release PRs in **draft** and flips them to ready-for-review at the Stage-9 gate (new `stage-09-plan-review.md` Phase A8 + a `stage-12-execute.md` draft-state merge guard, #379-F1); documents GitHub PR review-comments as an alternate QA/feedback arrival surface tiered `[ADJUST]`/`[SCOPE CHANGE]`/`[PLAN REJECTION]` (`stage-08-qa-testing.md` + `release-process.md`, #379-F2); and surfaces native review-decision state (`APPROVED`/`CHANGES_REQUESTED`) as a **NON-GATING advisory** Stage-9 input modeled on the shipped A6.5 version advisory (new Phase A9, #379-F3) — each degrading to an honest no-op under the single-operator reviewer convention. Alongside, `release-executor` **Mode A** now names + consumes the plan's **Delivery Strategy** and the current merge/tag/S-2/chore-PR mechanism (#213), and `pmo-release-manager` **Mode 1** now names the Stage-7/Stage-8 upstream-report inputs and the §5 Phase A structure, closing the stale gap notes and flipping `pmo-devops-sre`'s "future Release-Manager" framing to "sibling (built)" (#214). Release Class **routine**; single PR / single merge, clean at `3d8780f` (re-versioned forward v3.62→v3.63 on the #431 collision). Outcome SUCCESS; CHEAP reversibility.

### v3.62 (2026-07-03) — Writing down an architecture decision is now a guided, one-command step

Takes ADR mechanics out of operator memory and into a citable, low-friction workflow. Ships a canonical ADR authoring guide (`core/standards/adr-authoring-guide.md`: when-to-write rubric + copy-paste template + supersession/immutability policy) with the scattered ADR-governance references repointed to it (#431); a new `adr-helper` skill that scaffolds a correctly-numbered `ADR-NNN.md` at decision moments citing the guide as authority, with its `.skill` package built in-PR (#15); ADR-017 + ADR-019 index rows backfilled into `core/ADRs/README.md` (#541); and five residual decision-discipline ADR-threshold citations repointed with zero live residual (#3200). Release Class **novel**; single PR / single merge, clean at `0c4a3f8`; all four QA verdicts ACCEPT. Outcome SUCCESS; CHEAP reversibility.

### v3.61 (2026-07-03) — Foundation for a queryable warehouse of your project documents

Installs the tooling to turn project documents into a searchable index — a node-classification tool (stamps domain/type/lifecycle frontmatter), a relationship-edge layer (write-time emission across file-router / ppm-agent / tracker-manager plus a RAID `source_ref` field), and the SQLite index builder that materializes the warehouse from that frontmatter. Ships ready and safety-gated behind an explicit `--i-am-at-stage-12` token; **the live-document backfill is deferred** to follow-up #3123, so this is foundational rather than immediately visible. A throwaway probe confirmed a scheduled-task session can spawn a sub-agent (VERDICT PASS — unblocks the per-project orchestration epic). Outcome PARTIAL; MODERATE reversibility.

### v3.60 (2026-07-03) — Methodology packs: pick a delivery method, or bring your own

Methodology (Scrum, Kanban, or your own) becomes a self-contained, swappable pack — a shipped best-practice baseline you select, or your own config you bring and override, without hand-editing skills. Foundation release: the pack format (a widened type-pack meta-schema with a `role`/`extends`/`[[labels]]`/optional-`kinds` composition layer), two starter packs + a shared `_common` base, and a methodology-neutral label grammar. Consuming skills are wired in a follow-up (#2021). ADR-069 (composing-unit) + ADR-070 (composition grammar); MODERATE reversibility.

### v3.59 (2026-07-03) — One structural vocabulary for docs, backlog, and release records

### v3.58 (2026-07-02) — System-Specialist template — one method, a specialist per system

### v3.57 (2026-07-02) — Impact analysis reads code, and git-native releases stop dead-ending

Two release-pipeline improvements on milestone `80-solutioning-and-engineering-skill-modes` (2 issues, PR #3082). **#505** adds `domain-blast-radius.sh`, which traces change impact through the code import graph (`from x import y` edges) for `domain: software` deliverables and emits the same schema-v1 surface the design-review checklist consumes; the markdown-tree `blast-radius.sh` default is unchanged and now shares a common emitter library (architecture recorded in ADR-068). **#674** stops the release-executor Dry-Run-Record halt from dead-ending git-native releases (where the PR diff *is* the dry-run review), fixed at both the Step-1 input-validation check and the failure-mode entry; Cowork-lineage plans still route to release-planner Mode C. Provisional **v3.54** re-versioned forward to **v3.57** at the atomic tag claim (forward-only per bundle-composition-doctrine §5.3). MODERATE / `git revert -m 1` of PR #3082 reverses both cleanly; additive tooling + one routing-condition fix, no data migration.

### v3.53 (2026-07-02) — ADRs gain a canonical frontmatter schema, and the durability lint stops mis-flagging colors

### v3.52 (2026-07-02) — The eval framework is complete — tested refiner scripts, runnable eval sets, and chain-aware QA

### v3.51 (2026-07-02) — Skill guidance now cites its canonical escalation and best-practice sources. A `routine`-class tail-closeout pointing two skill docs at the platform's single source of truth. **#931** replaces the generic ">30 days = auto-escalate" remediation in `delivery-engine/references/raid-templates.md` §9 (Stale-RAID anti-pattern row) with the canonical type-split thresholds (issues >14d warn / >30d escalate; risks >30d warn / >60d escalate) and a cite to the doc-of-record `ppm-agent/references/escalation-thresholds.md` §3, resolving a divergence surfaced during v1.18. **#2208** adds a `## Reference docs` section to `pmo-tier-2-support/SKILL.md` citing `core/disciplines/root-cause-analysis.md` (its RCA method) and `core/standards/domain-best-practices/support.md` (the support best-practice guide shipped in v3.35); the guide's `consumers:` already named the skill, so no edit there, and the `.skill` package was rebuilt (Check-7 content-hash verified). Two slices on one branch / one merged PR (#3013, `--merge` at `1139a06`), build order #931 → #2208 under D-C SINGLE / P0 fully-serial. Closes the last tail of skill-function-hardening (#1105) and skill-bp-anchoring (#2164). Provisional **v3.49** was claimed by a concurrent release and re-versioned forward to **v3.51** at the atomic tag claim (forward-only per bundle-composition-doctrine §5.3). CHEAP / `git revert -m 1` of PR #3013 + package rebuild; additive documentation only, no schema/behavior/data change.

### v3.50 (2026-07-02) — The release pipeline can evaluate every stage transition from its own gate specs alone. Completes the platform's stage-gate criteria surface (milestone 68-stage-gate-criteria-completeness, 7 issues) so a release run needs zero out-of-spec operator knowledge to judge whether a stage is done. **#28** registers discrete gate IDs for the two stages that had none — **G4 Plan Readiness** (G4-01..05) and **G6 Engineering Completeness** (G6-01..05) — in `core/schemas/gate-criteria-spec.md` (schema **v1.21 → v1.22**), each with a matching Self-Repair block + Inheritance row, warn-mode, bound into `stage-04-planning.md` §7 / `stage-06-engineering.md` §7. **#96** makes G1-04 bidirectional (under- AND over-specification scorable from the criterion text; schema **v1.22 → v1.23**). **#118** anchors the existing prose judgment dimensions of Stages 9/12/13 to their structural criteria (G-PR\*/G-EX\*/G-CL\*) via a new Per-Boundary Judgment-Dimension Anchors subsection in `gate-evaluation-spec.md` (schema **v1.1 → v1.2**; prose Path-B, no J-\* IDs). **#29** authors the 5 remaining Stage-I/O boundary contracts (2→3, 6→7, 7→8, 8→9, 9→12) in `stage-io-contracts.md`, taking the surface to all 10 boundaries authored. **#501** adds a composite Stage-5 structural-premise review gate (§3.2 obligation rider on T3, §7.2 SR-G1..SR-G4 gate-blocking criteria, Stage 5→6 determination row + Validation Rule 4, blocking check 4.8) so the Stage 5→6 hand-off *asserts* structural conformance rather than assuming it. **#889** adds the `core/disciplines/ticket-architecture-reconciliation.md` discipline (when-fires + 4-step procedure + ticket-age staleness signal + 3 failure modes) filling #501's two reserved seam hooks. **#119** adds a Validation-method column to every gate-criteria table + a 6th Schema field row and lifts inline CLI out of ~24 criterion cells verbatim (no ID renumbered — 78 unique IDs pre/post; schema **v1.23 → v2.0**, BREAKING major by the spec's own column-addition rule, consumer-inert). `cross-cutting`-class (effective 33 pts, operator size-target override 2026-07-01); seven issues on one branch / one merged PR (#2928, `--merge` at `d5e0883`), build order #28 → #96 → #118 → #29 → #501 → #889 → #119 under D-C SINGLE / P0 fully-serial. The branch was rebased onto `origin/main` after v3.48 merged mid-build; the `gate-criteria-spec.md` version chain re-sequenced v1.20 → v1.21 (G3-18, v3.48) → v1.22 → v1.23 → v2.0 with G3-18 given its Validation-method cell — no v3.48 content lost. Provisional **v3.49** was claimed by a concurrent release and re-versioned forward to **v3.50** at the atomic tag claim (forward-only per bundle-composition-doctrine §5.3). MODERATE / `git revert -m 1`; governance + doc authoring only, no new skill, no stored-data change, runtime blast radius none.

### v3.49 (2026-07-02) — Governed file movement: the file router now governs every direction a file can move — inbound, generated-staging, promotion, and cross-project — not just inbound arrivals, and intake gains two matching refinements (milestone 08-INTAKE-governed-file-movement, #211, 3 issues). **#240** extends `file-router` from inbound-only to the full four-direction set; it resolves the promotion target and CITES artifact-generator's existing PROMOTE/REVISE/REJECT gate rather than restating it, owning a genuinely new approval gate only for cross-project-out, expressed as two gate-types — a confidence-threshold gate for the inbound family (inbound + cross-project, both Layer-2 scored) and a flat-approval gate for the Domain-C family (generated-staging + promotion, target pre-stamped). **#565** adds an `intake-desk` Phase-2 container-altitude existing-owner scan (hard-gate, containers only) that checks the tracker for an existing owner before proposing a new container, and reconciles failure mode #7 in place (stays 9 FMs, no redundant tenth). **#289** adds `intake-desk` **Mode C — Ambient Auto-Log** (non-interactive detect→create governed by the `automation_level` ceiling, with an honest no-hook-backstop safety framing and the per-item→standing weakening stated not softened; the Tier-0 set — governance / financial / security / RAID-close — never auto-created regardless of level; 9→12 FMs) and rewires the `core/CLAUDE.md.template` auto-logging rule to invoke it. `routine`-class; three EXTEND-mode slices to existing operations-module skills on one branch / one merged PR (#2861, `--merge` at `ee649ad`), build order #240 → #565 → #289 under D-C SINGLE / P0 fully-serial (the #565→#289 order serialized the two edits contending on `intake-desk/SKILL.md`). Provisional v3.46 re-versioned forward to v3.49 at the atomic tag claim (forward-only per bundle-composition-doctrine §5.3, anchor = published v3.48). The `intake-desk` + `file-router` `.skill` packages were rebuilt at close-out. MODERATE / `git revert -m 1 ee649ad` + redeploy prior skill packages; two operational skills + one governance rule, no new skill, no stored-data change.

### v3.48 (2026-07-02) — The design gate adapts to your delivery method, notes become non-authoritative, and health-status colours get one home. Closes the last three items of two knowledge-corpus cleanup epics. **#1087** registers gate criterion **G3-18** (Archetype-appropriate design invocation conditioned on `delivery_approach`) in `core/schemas/gate-criteria-spec.md` (schema v1.20 → v1.21, non-breaking additive — one criterion row + one self-repair row, no ID renumber) following the live G3-05 `deliverable_type` conditioning precedent: at the Bundle→Planning (Stage 3→4) boundary the design-invocation expectation conforms to the declared method — CASE 1 (Scrum/XP/Lean → design-first before slicing; Waterfall/PRINCE2 → phase-gate; Kanban → continuous, no sprint-slice gate), CASE 1-ARRAY (Hybrid → phased constituent governs), CASE 3/absent/`Custom` → base behaviour UNCHANGED via an explicit no-op default (never a silent default-fire); it adds a methodology lens to *when* design is expected and never removes the base Stage 5 Phase 0 Activation Gate, with a paired Stage-8 regression gate `G3-18-absent-axis-noop`; recorded in **ADR-066**. **#1093** codifies a Universal Preferences bullet in `core/CLAUDE.md.template` — operator notes are directional inputs, not authoritative specs; differences vs the corpus render as non-authoritative observations, and an agent MUST NOT perform a gross deletion / mass-restructure based on simple notes alone without first reconciling against the SSOT and confirming with the operator (canonical-corpus findings unaffected). **#930** records **ADR-065** naming `comms-writer/references/channel-formats.md` the single-source owner of the health-RAG bands + watermelon concept, and single-sources the concept via see-also cross-references in `channel-formats.md` and `ppm-agent/references/evidence-quality.md` (thresholds unchanged; the two edited skills' `.skill` packages rebuilt at close-out). `novel`-class; milestone 105-knowledge-corpus-tail-closeout, three independent cards on one branch / one merged PR (#2918, `--merge` at `dd9c284`), build order #930 → #1087 → #1093 under D-C SINGLE / P0 fully-serial. The branch was rebased onto `origin/main` pre-merge to inherit ADR-064 and clear the ADR-number contiguity gate; v3.48 free at the atomic claim (anchor = published v3.47). Closes the last child of KA-Standards #1171 and KA-Discipline #1174. Follow-up #2917 (SPI-band canonical reference) is out of scope + non-blocking. CHEAP / `git revert -m 1`; two ADRs + one advisory gate criterion + two cross-references + one governance rule, no blocking gate, runtime blast radius none.

### v3.47 (2026-07-02) — The PMO's operational data becomes agent-native and self-maintaining

### v3.45 (2026-07-01) — Pipeline docs now say what each stage is for, and one Stage-9 gate is on the books

### v3.44 (2026-07-01) — Backlog triage now runs as one command instead of issue-by-issue by hand

### v3.43 (2026-07-01) — Spoke-execution safety: five properties a pipeline spoke can no longer violate by construction. A Stage-6 pipeline spoke is now, by construction, unable to (1) push unsafely under multi-chip parallelism, (2) drift from its intake substrate undetected, (3) spawn commit work without a signing key, (4) wander to the operator's primary checkout, or (5) run outside security-hook coverage. **#307** authors **ADR-062** (`core/ADRs/ADR-062-substrate-vs-canonical-precedent.md`) codifying the substrate-vs-canonical precedent — a canonical-spec edit wins over a substrate-body (issue-body) mutation, issue bodies remain historical-record — and wires the cross-repo-citation-translation discipline into `release/references/pipeline/stage-05-solutioning.md` A1 scope-assessment, indexing the ADR in `core/ADRs/README.md`. **#47** adds the Stage-5 **M1.1 substrate-drift reconciliation chip step** to `release/references/how-to/hub-spoke-bridge.md` (one canonical home): at spoke-entry the chip compares each checkable issue-body claim against live state and emits a structured, machine-readable drift report — four fields per row (`drift_class` closed-enum {state-lifecycle | population-count | milestone-description | currency-reconciled-upstream}, `body_claim`, `current_state`, `gap_age_days`) — deferring to the Stage-4 A0.5/A0.6/A0.7 currency reconciliation for any axis already reconciled at planning and routing a per-drift disposition (`current-state-driven AC interpretation` | `escalate-body-amendment`) without ever auto-amending the body; the paired `intake-substrate` PROC entry lands in `core/standards/failure-mode-standard.md` (the OPTIONAL `release/tools/check-substrate-drift.sh` primitive was scoped OUT — the chip-spec + failure-mode entry are the load-bearing deliverables). **#1639** corrects the BLOCK-DESTRUCTIVE-019 worktree-exemption base in `core/hooks/block-destructive.sh` from workspace-root to repo-root (`${PRIMARY_ROOT}/pmo-platform/.claude/worktrees/`, derived per ADR-017 checkout-independence), retargets 13 `core/config/allowlists/script-execution-allowlist.txt` worktree-glob entries + the header example from `[CLAUDE_WORKSPACE_ROOT]` to `[PMO_PLATFORM_ROOT]`, and repoints the `core/hooks/tests/block-destructive.test.sh` fixtures to the repo-rooted base with both-direction gate-teeth (repo-rooted worktree ALLOW; retired workspace-rooted BLOCK; primary cwd BLOCK) — a LIVE always-enforce defect (the hook has no `.mode` gate) fixed before #1531's hook-loading wiring lands so the hook does not mis-block worktree Layer-1 edits. **#1642** adds the spoke-template no-`cd`/never-primary clause in `hub-spoke-bridge.md` (AC-1) plus the `core/rules/git-workflow.md § Primary Checkout Discipline` spoke-facing line (AC-3); AC-2 subagent-hook coverage is deliberately owned by #1531. **#1351** adds the CLAUDE.md Tier-1 governance self-check and a Tier-2 structural local-work entry point rooted outside `pmo-platform/` (`core/CLAUDE.md.template`) — T1+T2a this release, runtime teeth T2b/T2c fast-follow once #1531/#1642 land. **#59** adds the `ssh-add -l` pre-flight to the `hub-spoke-bridge.md` commit-producing-spoke spawn procedures (Stage 6 Engineering, Stage 12 Execute chore-PR, Stage 13 Release-Close chore-PR — the spawns that produce a commit) plus a user-side handoff on empty-agent, so a commit-producing spoke is not spawned into a state where `commit.gpgsign=true` would kill it at the commit step. `novel`-class; milestone 67-spoke-execution-safety, six cards on one branch / one squash-merged PR (#2799), build order #307 → #47 → #1639 → #1642 → #1351 → #59 under D-C SINGLE topology / P0 fully-serial posture (the 3-way `hub-spoke-bridge.md` edit forecloses parallel Engineering chips on the shared tip). #20 (subsumed by v3.28's ADR-052 parallelism-posture taxonomy) and #1472 (folded into #1531's deploy path + #1639's hook base) were closed during readiness. CHEAP-to-MODERATE — `git revert` of the single release PR restores prior state byte-for-byte for the additive doc/spec/ADR cards; the one MODERATE surface is #1639's executable hook edit (the revert is still clean, but if a wrong base had shipped and #1531 had already flipped enforce, worktree edits would be blocked in the interim — hence the #1639-before-#1531-enforce ordering mitigation). #1531 stays open (separate milestone `96-update-install-config-safety`).

### v3.42 (2026-07-01) — The branch-cleanup tool now actually cleans up squash-merged branches

### v3.41 (2026-07-01) — The skill catalog gets a suite-wide structural-hygiene pass and two net-new measurement auditors ship. **Hygiene (Wave A):** **#1135** ratifies the canonical mode-heading separator standard in `core/standards/canonical-skill-structure.md §4` — the em-dash form `<H2-or-H3> Mode <ID> — <Name>` (` — `, U+2014 flanked by spaces), survey-grounded as the established majority idiom over the colon-form minority. **#1136** applies that separator across the colon-form multi-mode SKILL.md set (colon-form → em-dash). **#1132** consolidates the vestigial Mode-Detection sections in `project-initiator` and `eval-writer`. **#1133** brings the SKILL.md catalog to full `## Guardrails (Platform)` conformance — 12 inserts of the missing section + 26 renames of bare `## Guardrails`, preserving pmo-qa-auditor's `## Guardrails (Extended)` variant — and fixes a folded `failure-mode-standard.md` drift. **#571** cross-references delivery-engine's DoR/DoD gate vocabulary to G1/G3 + DT-2; **#676** reconciles the stale canary entry in `core/schemas/per-skill-output-contracts.md §Skill 12`. **Measurement (Wave B):** **#16** adds the net-new `context-budget-auditor` skill (`core/skills/context-budget-auditor/` — SKILL.md + `measure-context-budget.sh`) quantifying token consumption across loaded platform components. **#17** adds the net-new `skill-compliance-auditor` skill (`core/skills/skill-compliance-auditor/` — SKILL.md + `scenario-and-calibration-schema.md` + `measure-skill-compliance.sh`) quantifying trigger-rate accuracy across the skill catalog, together with **ADR-061** defining the trigger-rate metric class and the compliance-auditor ↔ eval-writer boundary; both skills registered in `registry.md` / `deploy.sh` / `OPERATIONS.md`. **#704** ships SPEC-ONLY (routing-conflict detection beyond Jaccard — cross-skill invocation-path analysis: mechanism + sibling-harness decision + O(n²) cost design specified, zero code mutated; deferred implementation → follow-up #2699). `novel`-class; milestone 07-INFRA-hygiene-measurement, nine slices on one branch / one merge across two waves. Stage 4 live-verification found the catalog at 48 skills (not the 23 the source issues assumed); #1133/#1136 were re-priced size:M accordingly. Re-versioned forward v3.39 → v3.41 pre-merge (A.5.6c lineage — the branch descends from v3.40, v3.37 + v3.38 having shipped after a prior concurrent-release collision on provisional v3.37), and `claim-version.sh` then bound v3.41 at the merge SHA with no CAS collision. Additive — CHEAP / `git revert -m 1`; two new `core/` measurement skills + one ADR + catalog-wide SKILL.md structural edits, no `.skill` package committed in-PR (source SKILL.md only; the two new skills' packages build + deploy at close-out), runtime blast radius none.

### v3.40 (2026-07-01) — Four stale documentation references are drained from the internal reference corpus — places where the written guidance had fallen out of step with how the platform works. **#108 (doc-link protocol log-surface)** reconciles `core/standards/doc-link-maintenance-protocol.md` §8 to the shared warn-log the protocol actually writes to (as enforced by `deploy.sh` Check 14), so the protocol documents the surface it uses. **#131 (Projects/ → projects/ casing)** lowercases the genuine capital-P `Projects/` prose references in `core/governance/OPERATIONS.md` and `operations/OPERATIONS.md` to the canonical `projects/` form; the card was re-scoped on 2026-06-30 to the real OPERATIONS prose references (the broad placeholder-token variants routed to #2700), and a changelog line naming the historical `Projects/` casing is preserved intact as an accurate historical record. **#2684 (drain last IMPROVEMENTS.md / IMP-### site)** drains the retired "log to IMPROVEMENTS.md / IMP-###" Self-Update-Protocol instruction in `core/schemas/routing-rules.md`, routing it to the current GitHub-Issues intake and removing the last live pointer to the dead mechanism (split from #4; the separate Pattern-C detection work was removed from this milestone as net-new build and remains under epic #1172). **#2700 (unify project-path placeholder)** unifies the project-path placeholder to the canonical `projects/[Project]/` form across `core/schemas/frontmatter-schema.md`, `navigation-layer-schema.md`, `routing-rules.md`, `sqlite-index-schema.md`, `core/specs/health-check-specification.md`, and `operations/skills/project-initiator/SKILL.md`, owning all placeholder-token variants per the BROAD boundary resolution surfaced by the #131 re-scope; because `project-initiator` is a migrated skill, its compiled `.skill` package (and content sidecar) were rebuilt in the same release PR so the deployed artifact stays atomic with the SKILL.md edit. `routine`-class; milestone 30-doc-link-drift-drainage, four file-disjoint drainage cards on one branch / one merge. The prior direct-build PR #2703 was closed unmerged on 2026-07-01 (bypassed the governance gates; no changes reached main) and this release delivered the same scope through the gated pipeline. No re-version (v3.40 free at the Stage-12 atomic claim; anchor = published v3.38). Additive — CHEAP / `git revert -m 1`; reference-corpus documentation + one migrated skill's text + its rebuilt package, no schema or runtime-check change, runtime blast radius none.

### v3.38 (2026-07-01) — The release pipeline's own rules get three additive precision fixes across the planning, design, and engineering stages, sharpening release-identity and frozen-spec handling so problems are caught earlier instead of surfacing late at deploy. **#3 (placement forward-check)** adds Phase A0.7 / G-PL3 to the Stage-4 planning reference (with a governance summary in the release-process reference): a pre-planning check that runs the unified `git log <base>..origin/main --name-status --find-renames` predicate to catch directory-crossing renames and in-scope deletions on the mainline since the release branch's base — closing the "release cut before a structural reorg → new-file placement collides at deploy" pattern; it composes with the Stage-4 structural sub-audit and the Stage-12 pre-merge divergence check rather than duplicating them. **#226 (commit-group traceability)** adds a "documented simplification — advisory, not enforced" note after Phase B1 in the Stage-6 engineering reference plus an enforcement-posture sentence in the sub-task-methodology standard; it carries the release's genuine design decision (enforce / audit / advisory on the now-Required `Commit group` field), rendered ADVISORY at Stage 5 with a monitored-residual note (no verifier built). **#75 (frozen-spec precision)** adds a required-when-triggered precision rule to the solutioning-output-template's `## Blast Radius` section (with a DEFERRED escape + re-adjudication-cost note) plus a routing pointer in the Stage-5 solutioning reference, disambiguating the written spec from the frozen work product; a stale `templates/` → `standards/` target-path in the milestone description was corrected into the plan at Stage 4. `novel`-class; milestone 66-release-identity-and-spec-hardening, three file-disjoint cards on one branch / one merge. Re-versioned forward v3.37 → v3.38 at the Stage-12 pre-merge freeness check (A.5.6c) after a concurrent sibling release claimed v3.37 first (its plan file occupied the same path) — the branch relabeled to the next-free v3.38 before the merge, and the atomic claim bound v3.38 with no collision; the plan's R1 contingency fired exactly as anticipated (forward-only, no data loss). Additive — CHEAP / `git revert -m 1`; documentation/protocol edits to the release-pipeline reference corpus only, no skill or `.skill` package touched, runtime blast radius none.

### v3.37 (2026-06-30) — Shared project facts now live in one place, and PROJECT.md links to them instead of repeating them. A new shared `projects/_pmo/` area holds one consistent page per entity — people, systems, vendors, workstreams, decisions, and cross-project dependencies — and `PROJECT.md` becomes a thin composed wiki-link index that links out to those pages rather than restating the same facts in every project. **#362** establishes the `_pmo/{people,systems,vendors,workstreams,decisions,dependencies}/` layout with six entity-page templates matching the frozen schema sections (alias-tracking aligned to ADR-040 `person_id` + people-roster, compose not fork) and a `project-initiator` Mode A `_pmo/` bootstrap step. **#363** redesigns PROJECT.md as a ≤50-line composed index that wiki-links INTO #362's entity pages, adds a `project-initiator` Mode A Step-3 template-ref swap, and defines a 4-step live-migration protocol with a backwards-compatibility consumer table against the live `project-schema.md` consumers. **#159** models plans as typed sub-entities (comms / training / hypercare / cutover / change-mgmt) with relationship typing, resolves the deferred `plan_type` G5 enum membership, and drops `plan_subtype`; the composed index's `## Plans` section points at these typed entities. `novel`-class; milestone 21-shared-entity-storage-layout, three stories on one branch / one merge (build order `#159 ∥ #362 → #363`). **AC-5 live-project migration is DEFERRED** — the merge ships the capability only; the gated proof-of-concept migration of ≥1 live PROJECT.md is a follow-up (executor-tracked git-ignored ops tree, out-of-diff, snapshot-before / verify-links-after per plan R1). No re-version (v3.37 free at the Stage-12 atomic claim; anchor = published v3.36). Additive — CHEAP / `git revert -m 1`; structure + schema + templates, runtime blast radius none, the only non-git-reversible step (live migration) deferred out of the release.

### v3.36 (2026-07-01) — Process-domain specialist skills anchored to one best-practice guide, plus a new support-domain guide and the change-domain decision: the four process specialists each gain a compose-by-reference `## Reference docs` pointer (ADR-019; no content absorption) — pmo-scrum-master, pmo-release-train-engineer, and pmo-business-analyst single-anchor to `core/standards/domain-best-practices/process.md`, and pmo-product-owner dual-anchors to BOTH `process.md` and `governance.md` (value framing + accept/reject span both domains, mirroring pmo-technical-program-manager), with the guides' `consumers:` fields appended (#2177, #2178, #2179, #2180); a net-new `core/standards/domain-best-practices/support.md` K1 guide covers service management (ITIL 4), tiered incident & escalation practice (with the paired applicability note), and service reliability (SRE — SLI/SLO, error budgets, toil), registering three EXTERNAL + one INTERNAL row in `core/specs/framework-catalog.md` (#2210); and the change domain's best-practice content stays self-bundled in `change-management/references/` with the framework catalog as its registry — no shared `change.md` is created, pmo-ocm-lead is unchanged (transitive via ADR-019 composition), recorded in ADR-057 (`Proposed`) with a reversal trigger (promote iff a second change-domain designer skill appears), plus a build-philosophy "BP — change" coverage-matrix row and one `change-management/SKILL.md` Reference-docs line (#2211). The process-domain sequel to v3.30 (software) and v3.33 (governance). `routine`-class; milestone 90-skill-anchoring-process-support-change, all six issues on one branch / one merge. No re-version (v3.36 free at the Stage-12 atomic claim; anchor = published v3.35). Additive — CHEAP / `git revert -m 1`; reference wiring + one new guide + one ADR, runtime blast radius none. The four edited process skills' `.skill` packages were rebuilt at the Stage-12 cut so the content-freshness check (Check 7) passes.

### v3.35 (2026-06-30) — Records management, file/folder naming, and 08-Generated cleanup are now governed: a records-management policy (`core/governance/RECORDS_POLICY.md`) defining ISO 15489-1 retention, a four-class classification (vital/important/reference/transient with DEFAULT=Reference), disposition, and authenticity (#372, ADR-054); a unified artifact-naming standard (`core/standards/artifact-naming-standard.md`) covering POSIX-safe syntax, a controlled type vocabulary, explicit folder-naming rules (`_`-prefix reserved for infra), a validation regex, and a retroactive-rename protocol — wired into the seven emitting skills + OPERATIONS.md + a `pmo-qa-auditor` conformance check (#369, ADR-055, absorbing former #117); a new `generated-cleanup` skill running `08-Generated/` cleanup under an unconditional approval gate, grouping candidates by lifecycle state (`lifecycle_state` / `promotion_state`) and archiving to `_archived/` (#277, ADR-056); and `project-initiator` Mode A folder-name validation rejecting `_`-prefix / special-character / non-human-readable names against the new standard (#232). `novel`-class; milestone 20-records-management-naming-and-cleanup, four cards on one branch / one merge. Re-versioned forward v3.34 → v3.35 at the Stage-12 atomic claim (A.5.6c) after a concurrent telemetry release claimed v3.34 between Stage 9 GO and Stage 12 Execute — the branch name is retained. Additive — CHEAP / `git revert -m 1`; runtime blast radius limited to two operational skills (project-initiator validation + the new cleanup skill).

### v3.34 (2026-06-30) — The release pipeline now measures its own delivery and close-out health

### v3.33 (2026-06-30) — Governance-domain specialist skills anchored to one best-practice guide: the six governance-domain specialists (pmo-project-manager, pmo-program-manager, pmo-program-coordinator, pmo-portfolio-manager, pmo-knowledge-manager, release-planner) each gain a compose-by-reference `## Reference docs` pointer to `domain-best-practices/governance.md` (ADR-019; no content absorption) and the guide's `consumers:` field lists them (#2172, #2173, #2174, #2175, #2176, #2181). The governance-domain sequel to v3.30's software-domain anchoring; with governance now wired, two of the three domain guides (software + governance) are skill-anchored and only the process guide remains pending. `routine`-class; milestone 89-skill-anchoring-governance, all six issues on one branch / one merge. Re-versioned forward from a provisional v3.32 at the Stage-12 atomic claim after a concurrent release published v3.32 first — the branch name is retained. Additive reference wiring — runtime blast radius none.

### v3.32 (2026-06-30) — The release hub now follows the autonomy rules it operates under

### v3.30 (2026-06-30) — Software-domain specialist skills anchored to one best-practice guide: the five software-domain specialists (pmo-architect, pmo-principal-engineer, pmo-software-engineer, pmo-devops-sre, pmo-technical-program-manager) each gain a compose-by-reference `## Reference docs` pointer to `domain-best-practices/software.md` (ADR-019; no content absorption), the guide's `consumers:` lists them, pmo-technical-program-manager is dual-domain (also `governance.md`), and the build-philosophy BP×Skills cells are reconciled (software skill-wired; governance/process skill-wiring-pending) so the coverage map no longer over-claims (#2165, #2167, #2169, #2170, #2171, #2209). `routine`-class; milestone 88-skill-anchoring-software. Re-versioned forward from a provisional v3.29 at the Stage-12 atomic claim after a concurrent release published v3.29 first — the branch name is retained; a benign orphan v3.31 tag was left in place. Additive reference wiring — runtime blast radius none.

### v3.29 (2026-06-30) — Stage-6 Engineering parallelism postures: a founding decision record (ADR-052) fixes a named parallelism-posture taxonomy for how a release's Stage-6 engineering work runs concurrently versus serially, wired into the Stage-4 planning + Stage-6 engineering references, the hub-spoke orchestration bridge, the failure-mode standard, and the release-planner skill (#19). `novel`-class; a single-card slice of milestone 73-concurrent-execution-safety (2 issues remain open). Re-versioned forward from a provisional v3.28 at the Stage-12 atomic claim after the label-taxonomy release published v3.28 first — the `release/v3.28` branch name is retained. Additive governance + one skill — runtime blast radius none.

### v3.28 (2026-06-30) — Label-taxonomy canonicalization + a GitHub label-set parity check: the cluster axis gains an orthogonality protocol (one domain cluster per issue vs the `cluster: cross-cutting` span-marker that composes alongside it), the stale cluster color column is reconciled to live and the undocumented `cluster: security` is registered (12 clusters, was 11); observation promotion strips the `[Observation]:` title prefix under a deterministic title↔category parity invariant cross-referenced from the Stage-3 Template-Conversion Rule; and `deploy.sh` Check 51 + `check-label-parity.py` assert label-taxonomy ↔ GitHub label-set parity (enforce-capable MISSING / warn-only ORPHAN, warn-mode-initial, source-agnostic), surfacing pre-existing drift (missing `status:` labels → #1828, unregistered `type:*` → #1777) on its first run. `cross-cutting`-class; additive governance + a warn-mode check — runtime blast radius none.

### v3.27 (2026-06-30) — Platform docs now carry consistent metadata — and a check keeps it that way

### v3.26 (2026-06-30) — The platform now catches its own governance drift at PR time

### v3.24 (2026-06-30) — Release-hub orchestrator skill: a net-new whole-release control plane (`release/skills/release-hub/`) that takes a milestone and drives it through the release pipeline by composing the per-stage skills — owning sequencing and readiness-gating, never the stage work itself. Two modes: Mode R (Milestone Readiness) is the pre-flight GO/NO-GO that confirms a bundled milestone is ready to START, and Mode O (Orchestrate Release) runs the full hub-and-spoke orchestration with operator gating only at the named pipeline checkpoints. `novel`-class; additive net-new skill — composes existing stage skills, adds no new autonomous mutation surface, runtime blast radius none.

A `novel`-class release on milestone #252 `release-hub-orchestrator-skill` (2 issues — #2115 Mode R / #2212 Mode O; bump-class minor) — milestone-as-one-PR topology, one release PR (#2497). **Mode R — Milestone Readiness (#2115):** a pre-flight that confirms a bundled milestone is ready to START before a run is committed. It composes four readiness lenses — triage/duplicate detection, staleness/architecture reconciliation, dependency analysis, and bundle-coherence — into a single GO / NO-GO verdict with a per-finding disposition list, so a milestone that is not actually ready (stale tickets, unresolved dependencies, an incoherent bundle) is caught before the pipeline run starts rather than mid-flight. **Mode O — Orchestrate Release (#2212):** the full hub-and-spoke orchestration. It takes a milestone and drives it through the pipeline by spawning the per-stage spokes in dependency order, owning the sequencing and the readiness-gating between stages while delegating the actual stage work to the composed stage skills (it is a control plane, not a re-implementation of any stage). Operator gating happens at the named pipeline checkpoints — plan-review GO/NO-GO and execute — rather than per action, consistent with the platform's autonomy-tier model. The skill ships with four references — `milestone-readiness-checklist.md` (the Mode R lens contract), `orchestration-playbook.md` (the Mode O stage-sequencing contract), `spoke-launch.md` (the spoke-spawn contract), and `decision-briefing.md` (the operator decision-briefing format) — plus a `core/deploy/deploy.sh` RELEASE_SKILLS roster entry and the built `packages/release-hub.skill` (+ `.sha256`). Merged via merge commit `25f46256bd2747d1eded751771b2e8f024c7d1c0`; signed-annotated `v3.24` tag at the merge SHA (ED25519 signature valid, zero signing bypasses); GitHub Release published with the frontmatter-stripped note body. **Version note:** carried a provisional **v3.24** through the pipeline; the mainline ran hot during the build (v3.22 and v3.23 were both claimed by concurrent sibling releases, two prior provisional advances), so the version was carried as a bump-class declaration and resolved at the Stage-12 atomic claim. At that claim the governed `claim-version.sh --bump minor` computed **v3.24** — v3.23 was the highest mainline tag and v3.24 was free at the claim instant, so no version was abandoned and no `RELEASE_REVERSIONS.md` row was added; the provisional and the claimed version coincide. #2115 + #2212 auto-closed at merge. The `.version` stamp (v3.23 → v3.24), the CHANGELOG append, the RELEASE_LOG/INDEX/DIGEST entries, the release note, and the `chore/v3.24-stage-12-release-log` + `chore/v3.24-stage-13-corpus-update` branches are the Stage 12 + Stage 13 close-out completions; the corpus close-out was assembled via the governed Phase B chore-PR fallback per `stage-13-close.md` § Phase A8 — the automated close-out tool's `transition_release_log` phase mis-derived the milestone slug for this pure-alpha theme-named milestone (the slug resolver fell back to the Version field, which the transition regex cannot anchor as a non-first column), so the tool blocked at the DEPLOYED→VERIFIED transition and the output set was hand-assembled. The new skill deploys to install targets (`deploy.sh --deploy release-hub` — SKILL.md + 4 references + `.skill` package mirrored). CHEAP reversibility / HIGH confidence — additive net-new orchestrator skill; no data migration, no change to any existing skill / schema / stored artifact; the orchestrator composes existing stage skills and gates the operator at the named checkpoints, so it adds no new autonomous mutation surface; whole-release rollback = `git revert -m 1` of PR #2497. 30-day Stage-13 outcome window per the `novel` class.

### v3.23 (2026-06-30) — Project health-check skill: a net-new intent-driven project-state drift auditor (`operations/skills/health-check/`) that audits one project for drift between its tracked state and its canonical sources and emits a categorized 5-section punch list that is never auto-applied; seven modes (`full` / `timeline` / `attribution` / `comms` / `plan <name>` / `raid` / `sources`) on one shared contract, with founding ADR-051 recording the MCP-primary/local-fallback source set + graceful-degradation contract. `novel`-class; net-new skill + one ADR — additive, runtime blast radius none, read-and-recommend only.

A `novel`-class release on milestone #215 `06-HEALTH-project-health-check` (2 issues — #1125 v1 foundation / #1126 v2 extended modes; bump-class minor; both `size:L`) — milestone-as-one-PR topology, one release PR (#2488) preserving the 9-commit branch history. **v1 foundation (#1125):** builds the skill's full seven-mode contract once and implements the three foundation modes — `full` (total sweep, the default), `timeline` (every date/milestone with day-of-week validation; refuses generalized "week of…" ranges), and `attribution` (every owner field; flags missing/unverifiable owners, never invents one). The contract is identical across all seven modes: a **4-intent block** declaring the mode's purpose and bias, the **five fixed H2 output sections** (`## Confirmed` / `## Auto-Actionable` / `## Decisions` / `## Unknowns` / `## Rollup-Diffs`, emitted in that exact order, a no-finding section printing its header with `_(none)_`), a `TRACKER_UPDATES:` block emitted inside `## Auto-Actionable` in the existing tracker-manager schema (**never auto-applied** — routed to `/tracker-manager` on approval), and the `S0-NONE..S3-STRUCTURAL` depth band paired with a HIGH/MEDIUM/LOW confidence label on every finding line (projecting onto the platform's existing `staleness-confidence-standard.md` scale, ADR-043, not a parallel one). Delivered with three reference docs (`mode-intents.md`, `evidence-matrix.md`, `confidence-framework.md`) + an eval suite (7 judges, 11 cases, 3 fixtures, `evals.json`). **v2 extended modes (#1126):** implements the four value-heavier modes on the same contract — `comms` (Communications-Tracker lifecycle: stale-SENT / obsolete-DRAFT / unsent-READY; never infers a response; closures route to `/comms-writer` **status-only**, never message content), `plan <name>` (drift of one named plan; **requires a plan-name argument** and returns an actionable "which plan?" prompt rather than guessing; cautious bias — a promised-but-unreflected item routes to `## Decisions`, not asserted failed), `raid` (RAID-log guardrail enforcement: passive-voice risks, missing owners, missing mitigations, entries stale past 30 days; **never auto-closes a risk**), and `sources` (the canonical-source inventory + graceful-degradation surface: names the MCP-primary set — Confluence/Jira/Smartsheet/SharePoint — and the local-fallback set with per-source freshness verdicts, flags missing-but-expected sources). **Founding ADR-051:** `core/ADRs/ADR-051-health-check-mcp-primary-source-set.md` (Accepted at the Stage-5 Decision Gate 2026-06-29; authored at Stage 6 alongside the skill) records the MCP-primary / local-fallback canonical source set (audience-facing sources are the source of truth; local artifacts are fallback + cross-validation) and the graceful-degradation contract (an unreachable connector continues the run local-only with an `[MCP UNAVAILABLE: <connector>]` banner; SharePoint has no MCP connector today, so its targets degrade to "links exist; content not verifiable"; a finding that could not be cross-validated is capped at MEDIUM and routed to `## Decisions`/`## Unknowns`, never `## Auto-Actionable`). The release also adds the `/health-check` slash command (`commands/health-check.md`), the registry catalog row (`core/skills/registry.md`), the deploy-roster entry (`core/deploy/deploy.sh`), and ships the built `packages/health-check.skill` (+ `.sha256`). Merged via merge commit `97310f9457c5ce25f2543dd9a7beed54941ef149`; signed-annotated `v3.23` tag at the merge SHA (ED25519 signature valid, zero signing bypasses); GitHub Release published with the frontmatter-stripped note body. **Version note:** carried a provisional **v2.42** label through authoring (the branch + its sub-tasks were named when v2.42 was the next slot and ADR-049 the next ADR number); a mid-pipeline `chore(reconcile)` commit (`2d59d98`) merged `main`, added ADR-051, re-versioned the branch to v3.23, and resolved the plan-file conflict; at the Stage-12 atomic claim the governed mechanism computed **v3.23** — v3.22 was the highest mainline tag and v3.23 was free at the claim instant, so no version was abandoned and no `RELEASE_REVERSIONS.md` row was added; the `v3.23_RELEASE_PLAN.md` filename was authored at the re-versioned number. The PR title stale-finding (F-01: the title still read `v2.42 … ADR-049`) was resolved at Stage 12 Step B by retitling to `feat(v3.23): … + ADR-051` before merge. #1125 + #1126 auto-closed at merge with verification-evidence comments; the Stage-12 sub-task #2482 closed at Stage 12 and the Stage-13 sub-task #2483 at Stage 13; milestone #215 closed at Stage 13 by the hub. **Epic #1106 `[Epic] skill-health-check` stays OPEN** — a leaf milestone does not close the epic. The `.version` stamp (v3.22 → v3.23), the CHANGELOG append, the RELEASE_LOG/INDEX/DIGEST entries, the release note, and the `chore/v3.23-stage-12-release-log` + `chore/v3.23-stage-13-corpus-update` branches are the Stage 12 + Stage 13 close-out completions; the corpus close-out was assembled via the governed Phase B chore-PR fallback per `stage-13-close.md` § Phase A8 (the automated close-out tool's preflight blocked on the worktree carrying the pre-authored RELEASE_NOTES). The new skill deploys to install targets (`deploy.sh --deploy health-check` — SKILL.md + 3 references + `.skill` package mirrored). CHEAP reversibility / HIGH confidence — additive net-new skill + one ADR; no data migration, no change to any existing skill / schema / stored artifact; the skill only reads sources and stages recommendations, so running it cannot mutate project state; whole-release rollback = `git revert -m 1` of PR #2488. 30-day Stage-13 outcome window per the `novel` class.

### v3.22 (2026-06-29) — PMBOK artifact coverage + PROJECT.md schema axes: PROJECT.md frontmatter gains three additive axes (the first-class `deliverable_type` deliverable-domain axis with a domain-axis consumption pattern + G3-05 conditioning + founding ADR-050; `org_structure_type` + a refs-only `team_roster`), five PMBOK artifact templates land with Trackers 8/9, and a new `lifecycle-tailoring.md` maps the 3-state agent model onto the PMBOK 5-process-group convention. `cross-cutting`-class; additive schema / template / reference fields — no data migration, no stored-content move, no change to any existing PROJECT.md, no breaking change.

A `cross-cutting`-class release on milestone #194 `18-pmbok-coverage-and-project-schema` (4 issues — #371 / #206 / #351 / #262; Deep Stage-9 review) — one release PR (#2489) preserving the dep-ordered commits, 19-file surface, additive throughout (+686/−14; the deletions are intentional count-cascade + gloss-fix rewrites). **Deliverable-domain axis + ADR-050 (#351, keystone):** `core/schemas/project-schema.md` gains the first-class `deliverable_type` open-enum axis (recognized classes `{software, governance, web, data, enterprise-platform, hardware, process}` + the `^[a-z]+(-[a-z0-9]+)*$` open-escape shape, mirroring `delivery_approach: Custom` openness) defined by the new **V13** rule off the V12 tail (required forward, optional/conformant on legacy; no rule renumbered), with a disambiguation table fixing the boundary against `work_item_type` and the Stage-4 `domain:` field (which now reads `deliverable_type` as its authoritative source). `release/references/specs/methodology-parameterization-v1.md` adds the **§5A Domain-Axis Consumption Pattern** (the domain-axis sibling of §5 — consumers branch on `deliverable_type` orthogonally to `delivery_approach`); `core/schemas/gate-criteria-spec.md` conditions **G3-05** on `deliverable_type` (schema v1.18 → v1.19, non-breaking — G3-05 keeps its ID/type/check/automation columns, the conditioning adds a domain-conformance lens and never removes the base measurability check); and the new `core/ADRs/ADR-050-deliverable-domain-axis.md` (registered in `core/ADRs/README.md`) records the placement-on-frontmatter + open-enum decision (`Proposed` → `Accepted` at the Stage-9 Deep review). **Org-structure + team-roster expansion (#262):** appended off the keystone's post-V13 tail — `org_structure_type` (**V14**, the PMBOK org-shape enum + literal `Custom`, default `functional` documented-not-silent) and `team_roster` (**V15**, a refs-only list of `{person_ref, role_on_project}`); V15-a is the no-inline-PII invariant (closed key-set — the schema cannot hold a roster name, so a commit cannot leak one), V15-b resolves each `person_ref` against the `person_id` anchor (unresolved → BLOCK-WRITE, external → WARN-HEALTH per ADR-040); `team_roster` composes-not-duplicates the people-graph, replaces the free-text `## Key People` prose, and is reconciled against the Stakeholder Register and the frozen Resource entity as three projections of one `person_id`. `delivery_model` was intentionally NOT added (it resolves to the existing required `delivery_approach`). **PMBOK templates + Trackers 8/9 (#206):** five PMBOK artifact templates under `operations/templates/` (project charter, lessons-learned, change-log, RACI, stakeholder-register — token-placeholder form, no inline PII) with **Tracker 8 (Stakeholder Register)** and **Tracker 9 (RACI)** schemas in `core/schemas/tracker-schemas.md` (column-header roles on the same `person_id` anchor as `team_roster`), registered in `core/governance/OPERATIONS.md` and wired into `core/deploy/deploy.sh` deploy-sync; template taxonomy/storage updated. **3-state-to-PMBOK lifecycle mapping (#371, foundation):** the new `core/disciplines/lifecycle-tailoring.md` maps the platform's compact 3-state project model (`ACTIVE → CLOSING → CLOSED`) onto the PMBOK five-process-group convention (citing ADR-004), with 3 pointers into `core/disciplines/README.md`, `core/CLAUDE.md.template`, and `core/governance/OPERATIONS.md`. Merged via merge commit `3aded2afa122f8e24b63d7ec89dced27b31582cb`; signed-annotated `v3.22` tag at the merge SHA; GitHub Release published with the frontmatter-stripped note body. **Version note:** carried a provisional **v2.42** label through the pipeline; at the Stage 12 atomic claim the governed mechanism computed **v3.22** — v3.21 was the highest mainline tag and v3.22 was free at the claim instant, so no version was abandoned and no `RELEASE_REVERSIONS.md` row was added; the `v3.22_RELEASE_PLAN.md` filename was authored at the claimed version. #206 / #262 / #351 / #371 closed at Stage 13 with verification-evidence comments; the Stage-7/8/9/12/13 sub-tasks #2451 / #2452 / #2453 / #2454 / #2455 closed; milestone #194 closed at Stage 13 by the hub. The `.version` stamp (v3.21 → v3.22), the CHANGELOG append, the RELEASE_LOG/INDEX/DIGEST entries, the release note, and the `chore/v3.22-stage-12-release-log` + `chore/v3.22-stage-13-corpus-update` branches are the Stage 12 + Stage 13 close-out completions. Deployable changes (the five new templates) require an install-sync (`deploy.sh --deploy`) on the operator's primary checkout — handed off (Layer-2 install-sync runs outside the release worktree). CHEAP reversibility / HIGH confidence — additive schema / template / reference fields; no data migration, no change to any existing PROJECT.md, no breaking change; whole-release rollback = `git revert -m 1` of PR #2489. 30-day Stage-13 outcome window per the `cross-cutting` class.

### v3.21 (2026-06-29) — Terminology & controlled-vocabulary canonicalization: the K1 glossary is refreshed for AI-agent comprehension (frontmatter + the Role term anchored to the Autonomy-Tier framework + four first-class actor terms), the Tier Disambiguation Table is extended to cover Hierarchy Tier, and canonical `Initiative` / `Roadmap` terms are added and reconciled with the live `epic:*` label namespace via a new ADR. `novel`-class; documentation / governance only — additive/corrective corpus edits, no behavior / schema / runtime change.

A `novel`-class release on milestone #131 `34-terminology-and-controlled-vocabulary` (3 issues — #68 / #128 / #432; bump-class minor; all `size:M`) — **D-C SINGLE** topology, one release PR (#2487) preserving the three dep-ordered commits on a shared glossary surface. **Glossary refresh (#68, keystone):** `core/specs/terminology-glossary.md` gains discoverable YAML frontmatter, anchors the `Role` term to the Autonomy-Tier framework, and adds four first-class actor terms — `{#term-hub}`, `{#term-spoke}`, `{#term-skill}`, `{#term-sub-agent}` — plus an Appendix-A resolution row for the overloaded word "agent", lifting the Total-terms count 19 → 23; the four actor terms give an agent reading the corpus cold the vocabulary to resolve who-acts-at-which-tier from the glossary alone. **Tier-disambiguation extension (#128):** `core/specs/autonomy-tiers.md` adds a Hierarchy Tier row to the Tier Disambiguation Table, a namespace-overlap collision note, and out-of-scope prose stating each named convention's disposition, and cascades the convention count four → five (with `core/CLAUDE.md.template` L310's 2nd "four" → five, the 1st "four" = Autonomy's own 0–3 preserved); the table now covers every named tier convention rather than four, so the Hierarchy-Tier-vs-Autonomy-Tier collision is resolvable without inference. **Initiative / roadmap canonicalization + ADR-049 (#432):** `core/specs/terminology-glossary.md` adds canonical `{#term-initiative}` (Initiative = a multi-milestone grouping theme — a cross-milestone grouping label, never a container tier or `parent_ref` target) and `{#term-roadmap}` (Roadmap = an architected path across milestones, one-or-more initiatives), rewrites Appendix B (the "Initiative … not modeled" assertion removed), and lifts Total-terms 23 → 25; `core/standards/initiative-roadmap-framework.md` gains an additive canonical-scope note (the one-initiative narrative preserved, surgical); `core/specs/label-taxonomy.md` reconciles its §Initiative Labels grouping line to the canonical term and replaces three dead `initiative:*` rows with the live `epic:*` / `project:*` grouping namespaces; and the new `core/ADRs/ADR-049-canonical-initiative-roadmap-vocabulary.md` (registered in `core/ADRs/README.md`) records the canonical Initiative / Roadmap / milestone vocabulary + the `initiative:` → `epic:`/`project:` label mapping — the canonical-vocabulary D-432-Wording decision was rendered at the Stage 5 Collective Review, with the ADR as the governance record. Merged via merge commit `26c32a4e0e1888ab17c99a2de13166249090d7b2`; signed-annotated `v3.21` tag at the merge SHA (ED25519 signature valid); GitHub Release published with the frontmatter-stripped note body. **Version note:** authored as a provisional **v2.42** (rule-computed next-free after the published v2.41); at the Stage 12 atomic claim the governed `claim-version.sh --bump minor` computed **v3.21** — the `v3.20` published Release sits on the mainline first-parent spine and elevated the adapter's major-max frontier above the v2.x line, so next-free resolved to v3.21 rather than v2.42. The operator accepted the governed v3.21 output; the branch name, commit messages, PR #2487 title, and the `v2.42_RELEASE_PLAN.md` filename retain the as-authored v2.42 (preserved, not rewritten — one `RELEASE_REVERSIONS.md` abandoned row, v2.42, no tag cut). #68 / #128 / #432 closed at Stage 13 with verification-evidence comments; the Stage-12/13 sub-tasks #2470 / #2471 closed; milestone #131 closed at Stage 13 by the hub. The `.version` stamp (v2.41 → v3.21), the CHANGELOG append, the RELEASE_LOG/INDEX/DIGEST entries, the release note, and the `chore/v3.21-stage-12-release-log` + `chore/v3.21-stage-13-corpus-update` branches are the Stage 12 + Stage 13 close-out completions. MODERATE reversibility / MEDIUM confidence — #432 ripples canonical vocabulary into the framework + label-taxonomy + ADR-049 (CHEAP per edit; #68 and #128 are purely additive), but runtime blast radius is none (no code consumes the terms); whole-release rollback = `git revert -m 1` of PR #2487. 30-day Stage-13 outcome window per the `novel` class.

### v3.56 (2026-07-02) — Stage-3 bundling composer + version-identity: a self-triggering Stage-3 composer

`novel`-class release (Standard engagement) making Stage 3 Bundling a self-contained, self-triggering composer. **#30** adds a `deploy.sh` Check 53 approved-queue-depth monitor (warn-mode) + a `check-approved-queue-depth.py` primitive that surfaces a bundle-candidate summary when the approved-but-unbundled backlog crosses ≥5, plus a repair of the stale `--check-lifecycle` registry (dormant Checks 11 & 30 preserved). **#52** adds `compute-milestone-positions.py` — a stdlib Kahn's-BFS milestone-position derivation tool (`--dry-run`/`--apply`, idempotent byte-stable `position:` line, cycle-annotating, self-test) producing the value the live-but-dormant G3-07 override tier reads — plus `deploy.sh` Check 52 (position-drift, warn-mode). **#25** records in `release-planner`'s SKILL.md that Stage-3 bundle-composition (Mode A) and Stage-4 planning (Mode B) are intentionally co-located per ADR-019 (the "split into a `pmo-bundle-composer` skill" idea superseded — the separation is delivered via the persona-card split + `bundle-composition-doctrine`). **#415** codifies the `version-less` release-identity mode in `stage-03-bundle.md` §4/§7 + `bundle-composition-doctrine.md` §5/§3, reconciled against the live `version_canonical` grammar gate (version-less = no version key ⇒ the grammar is never invoked); core scope, enforcement fissioned to #3016. Orchestrated via `release-hub` Mode O (readiness NO-GO→GO fix → plan → 17-sub-task scaffold → Stage-5 Solutioning + independent adversarial review + scope-lock → P0-serial build; Stage-6 #52 recovered hub-direct after a spoke session-limit). Version-agnostic branch; provisional v3.51 published by a concurrent release mid-run → claimed v3.56 at cut (skipping the live in-flight v3.54/v3.55). 28/28 CI green. CHEAP-MODERATE / `git revert -m 1` of PR #3096.

### v3.20 (2026-06-07) — Release-corpus verification surface (real checks, not grep proxies): corpus tools rewired to the live layout + fail-loud deploy-checks; ≥90% parser; live-taxonomy labels

5 issues replacing grep-proxy release-corpus checks with real verification and closing the systemic "green-on-broken" deploy-check failure mode; `novel` release class, CHEAP reversibility, single-branch (D-C SINGLE), shared-release-PR append model. The two release-corpus tools were hardcoded to pre-restructure `pmo-platform/...` paths and passed vacuously against the live `core/`/`release/` layout — `lint_release_corpus.py` (Check 20) now lints the real `release/releases/notes/` directory with the stale v11.18 cutover-floor exemption removed (11 live notes were going unchecked), and `generate_release_index.py` (Check 23 `--verify`) now exits non-zero on a path-resolution failure instead of exiting 0 on a dead path (#83); the `#85` Scope-column `#N`-token miscount is moot by construction since the live INDEX carries no Scope column (6-column schema), and the generator was retargeted to it. The fail-loud-on-path-error contract was generalized across the rest of the deploy-check family (`deploy.sh` exits non-zero on path-resolution failure — no more green-on-broken), with a confirmed pre-restructure old-path default repaired in `check-version-anchors.py` and `check-doc-links.py` rewired in the same pass (#459). `release/tools/bundle-issues-parser.py` (the `release-planner` Mode A engine) was hardened against the dominant substrate failure modes — field-name heading-alias heterogeneity and legitimately-absent optional sections previously scored as parse failures — lifting combined-clean from a sub-target baseline to the ≥90% Mode A acceptance target on the live conformant-bundleable substrate, locked by new fixtures (`conformant_bundleable_sample.json` + `test_bundle_issues_parser.py`) (#291). `release-executor` Mode G's Pattern-Review file-new step was retargeted off a non-existent `category:` label to live-taxonomy labels only (Triage-applied type label; Category in-body), fixing a latent `gh issue create` failure (#425); `release/skills/release-executor/SKILL.md` + `release/tools/automated-closeout.sh` updated and both skill packages rebuilt. The plan carried D-class re-scope decisions for #83/#85/#291 after Stage-4 verification found the original ACs overtaken by the modular-monolith restructure (the `novel` trigger), warranting a Deep Stage 9 review for the deploy-check blast radius. Single release PR (#496) merged to main; signed-annotated tag `v3.20` at the merge SHA; the PR also carried a prior-release v1.03 RELEASE_LOG/RELEASE_INDEX backfill (historical provenance unrecoverable post-re-versioning, recorded as a backfill). 30-day Stage 13 outcome window per the `novel` class. The INDEX regenerate is the first production dogfood of the FM-1 recent-first sort fix — 11 rows, v3.20 at top, v1.03 retained.

### v3.19 (2026-06-03) — Stage 13 close-out reliability backstops (completeness gate + outcome-bound mandatory close + tooling smoke gate + slug/Keychain hardening + working orphan `--apply`)

5 issues hardening Stage 13 Close so a release can no longer close incomplete and the close-out tooling can no longer rot unexercised; `automation`/`process-protocol` cluster, CHEAP reversibility, additive warn-mode-initial tooling + protocol text across `deploy.sh`, GitHub Actions CI, the Stage 13 spec, the hub-spoke bridge, and two `release/tools/` scripts. A `deploy.sh --check` Check 32 release-corpus-completeness gate iterates every RELEASE_LOG row and asserts its matching INDEX row + DIGEST entry + NOTES file exist — the mechanical backstop converting the documented-but-skippable "Appended at Stage 13" convention into a guard, after the canonical incident closed a release with only the LOG row (#576). The close-out completeness mandate is reframed outcome-bound: a release MUST close with its complete enumerated output set verified on main (the Procedure 7 Step 4 Verification table is the canonical set), with the automated close-out the mandated mechanism and the documented Phase B chore-PR the named fallback when preflight legitimately blocks; hand-assembling the corpus row-by-row is prohibited, and an operator merge-ahead is an explicit supported close path that does NOT waive the close outputs (#581, reworked to outcome-bound per adversarial review at #598). A `release-tooling-smoke.yml` CI gate runs `automated-closeout.sh --self-test` + `--check-paths` (both offline) on any PR touching `release/tools/**` or the RELEASE_LOG schema, plus a precision probe proving the path gate FAILS on a deliberately-broken corpus path — so the exact rot that hid stale paths / a stale column index for several releases fails the PR instead of the next close (#583). `automated-closeout.sh` gains a schema-aware `extract_milestone_slug` (returns the field-2 slug on the 8-column LOG, not the field-1 version; self-test covers 8-col + 5-col) and a locked-Keychain credential fallback (`GIT_TERMINAL_PROMPT=0` + a `gh auth git-credential` helper) so a headless/locked-Keychain session degrades gracefully instead of hanging (#584). `cleanup-orphan-state.sh --apply` is wired to actually execute git-porcelain removal (the `REMOVE` action ran as a no-op before), expands `--release-close <slug>` detection to include the sibling `chore/<slug>-*` Stage 12/13 corpus branches, prunes stale remote-tracking refs via `git remote prune`, and adds a post-apply verify that reclassifies any survivor to `SKIPPED — survived apply` rather than silently claiming success (#556). Merge-ahead topology — no single release PR; the five backstops landed via four direct-to-main PRs (#588, #591, #592, #598) with green CI, closed via the documented Phase B chore-PR fallback. This release is the first reflexive consumer of its own repaired close-out automation, validated (`--self-test` + `--check-paths` PASS on main) on its own merge-ahead close. Tag `v3.19` + GitHub Release backfilled at the operator authorization gate.

### v3.18 (2026-06-03) — Corpus-integrity enforcement (reference-durability standard + three-primitive spine + three PR-time integrity gates)

2 issues shipping forward-prevention corpus-integrity enforcement; `cross-cutting` class, MODERATE reversibility, additive warn-mode-initial tooling across the harness-hook layer, `deploy.sh`, and GitHub Actions CI. A reference-durability standard codifies a durability ladder plus a self-containment test, enforced forward-only by three primitives — a PreToolUse hook (`block-fragile-refs.sh`), a `deploy.sh --check` check (Check 31), and a CI backstop (`reference-durability.yml`) — with the Class-L/V regex byte-identical across all surfaces and a checked-in 23-case fixture gating Class-V precision so it cannot silently regress (#538). Three PR-time repository-integrity gates (`repo-integrity.yml`) add depersonalization (operator/collaborator PII in the C6 governance domains, added-line-scoped, exempting intentional-identity files and release-tracking files), issue-reference validity (resolution + designated-block placement, PR-number and transferred-issue aware), and dead-file references (#410). Every gate scans the net-new added-line delta — pre-existing rot is out of scope (forward-prevention only). Single branch topology; one release PR (#562); merged ahead of the formal Stage 7–9 / 12 gates by operator decision with all 11 CI gates green. Three scope boundaries surfaced and were fixed by real use during build and close-out (whole-file → added-line, C6-domain scoping, `release/releases/` tracking exemption at #574); one positional-consistency fix deferred (#572). Untagged.
