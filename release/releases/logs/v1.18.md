# Archived Release Log — v1.18

> Active ledger: [RELEASE_LOG.md](../RELEASE_LOG.md). Archived from RELEASE_LOG.md on 2026-06-19.

## Row

| v1.18 | cross-release-impact-model | #87 | #928 | `7d9307bbfcf317e29bfe34a772f5fac08a8934ea` | `v1.18` | VERIFIED | 2026-06-14 |

#### Deployment Log v1.18
**Files deployed:** No runtime-skill or harness file is deployed by this release's own mechanism at Stage 12 — the changed set is K1 corpus + one canonical-with-mirror rule + one runtime-skill SKILL.md whose mirror syncs via `./deploy.sh --deploy` (FLAGGED for the operator/hub to run from the primary post-merge; not run from the transient Stage 12 worktree). The release (single PR #928, 1-issue `cross-cutting` release, D-C SINGLE) adds a durable cross-release impact model consulted at Stages 3/4/5/9/12. **#87 (cross-release impact model):** introduces the **structural-blast-radius** contention axis. `release/references/pipeline/stage-03-bundle.md` defines the `§ A9.6.1` axis — a 4-token mover-classifier (`RENAME` / `RELOCATE` / `DELETE-RECREATE` / `DIR-RESTRUCTURE`), `SURFACE(R)` computed via the F1–F6 sweep, an intersection predicate, and a `Tier-S` verdict + `structural-blast-radius` edge type extending the live Parallelization Map. `release/references/pipeline/stage-04-planning.md` adds the A4 structural sub-audit (mover-set + `SURFACE(R)`, sibling File-Change-Matrix intersection), the sibling-merge stale-pin self-invalidation trigger, and Phase A0 `Tier-S` re-derivation. `release/references/pipeline/stage-05-solutioning.md` adds a cross-*release* coherence row to the Collective Review Decision Briefing. `core/schemas/gate-criteria-spec.md` adds **`G-PR9`** (Stage-9 GO gated on baseline currency before Execute; registry now `G-PR1..G-PR7` + `G-PR9`, no renumber). `release/governance/release-process.md` adds the Phase A.5 semantic baseline-currency check (canonical body; its byte-identical mirror `.claude/rules/release-process.md` syncs via `./deploy.sh --deploy`). `release/skills/release-planner/SKILL.md` is updated to consult the model (runtime-skill mirror; syncs via `./deploy.sh --deploy`; `packages/release-planner.skill` rebuilt in-PR). **`ADR-024-cross-release-impact-model.md`** (CREATED) records the architecture decision. Engineering Commit 0 landed the release plan `release/releases/plans/cross-release-impact-model_RELEASE_PLAN.md`. Corpus close-out (RELEASE_NOTES + LOG `DEPLOYED → VERIFIED` + INDEX + DIGEST + CHANGELOG) lands at the Stage 13 chore PR.
**Mechanism:** git merge (PR #928 → main, `--merge --delete-branch`) + signed-annotated tag push (Tag-SHA-Direct pattern per stage-12-execute.md § Phase B; the `v1.18` tag points at the merge SHA `7d9307bbfcf317e29bfe34a772f5fac08a8934ea`; zero signing bypasses). Deploy-sync of the `release-process.md` rules mirror + the `release-planner` skill mirror (`./deploy.sh --deploy`) is FLAGGED for the operator/hub to run from the primary post-merge — it is NOT run from this transient Stage 12 worktree per `core/rules/git-workflow.md` § Primary Checkout Discipline.
**Timestamp:** 2026-06-14 05:30 UTC (PR #928 merge; MERGE_SHA `7d9307bbfcf317e29bfe34a772f5fac08a8934ea`)
**Cycle-Time:** N/A — no gate-outcome/plan-review-go event for v1.18 in the pipeline event log; no deployment-status/deploy-skill or deploy-harness event recorded at the operator-instance path (this release's own deploy-sync runs from the primary post-merge; mechanism: compute-cycle-time.sh, exit 0)
**Result:** SUCCESS — release PR #928 merged to main via merge commit `7d9307bbfcf317e29bfe34a772f5fac08a8934ea` (D-C SINGLE; `git revert -m 1` rollback convention); signed-annotated `v1.18` tag points at the merge SHA; zero signing bypasses; the in-PR rebuilt `release-planner.skill` package is staged from scrubbed source so package↔source parity (Check 7) holds on main. **Phase A.5 baseline-currency pre-check:** CLEAN — 0 commits diverged on `origin/main` since the PR merge-base (`255ec77`); the `stage-gate-criteria-completeness` cluster (#354 + #119) was still 0-merged at execute time, so the D-3 serialization re-baseline was NOT triggered and the Stage-9 GO remained current. **#87 stays OPEN** (VERIFY-ONLY: PR #928 used `References #87` in its Issue References block — `closingIssuesReferences` empty — so #87 is closed manually at Stage 13 with verification evidence per the AC re-run). **Version note:** v1.18 was claimed by THIS release (tag + DEPLOYED row); a concurrent draft release PR #933 (`release/v1.18`, milestone `health-and-raid-determinism`, mid-Stage-6) also targeted v1.18 and must re-version to the next-free — FLAGGED to the hub, along with the `release/skills/release-planner/SKILL.md` line-5 `version:` anchor reconciliation.
