# RELEASE_INDEX

Corpus-level index of all pmo-platform releases. Chronological-recent-first row order. Appended at Stage 13 chore PR per [`stage-13-close.md § Phase B`](../references/pipeline/stage-13-close.md).

| Version | Milestone | Date | Theme | Release PR | Release Notes |
|---|---|---|---|---|---|
| v3.49 | 08-INTAKE-governed-file-movement | 2026-07-02 | — | #2861 | [notes/v3.49_RELEASE_NOTES.md](notes/v3.49_RELEASE_NOTES.md) |
| v3.48 | 105-knowledge-corpus-tail-closeout | 2026-07-02 | Knowledge-corpus tail close-out — the design-before-slicing gate conditioned on `delivery_approach` (ADR-066 + criterion G3-18, schema v1.20→v1.21), a notes-non-authoritative + destructive-action guardrail in CLAUDE.md (#1093), and the health-RAG band/watermelon canonical home (ADR-065, #930); closes the last child of KA-Standards #1171 and KA-Discipline #1174. | #2918 | [notes/v3.48_RELEASE_NOTES.md](notes/v3.48_RELEASE_NOTES.md) |
| v3.47 | 23-tracker-comms-session-config | 2026-07-02 | — | #2860 | [notes/v3.47_RELEASE_NOTES.md](notes/v3.47_RELEASE_NOTES.md) |
| v3.45 | 42-pipeline-skill-doc-reconciliation | 2026-07-01 | — | #2851 | [notes/v3.45_RELEASE_NOTES.md](notes/v3.45_RELEASE_NOTES.md) |
| v3.44 | 78-pipeline-triage-automation | 2026-07-01 | — | #2801 | [notes/v3.44_RELEASE_NOTES.md](notes/v3.44_RELEASE_NOTES.md) |
| v3.43 | 67-spoke-execution-safety | 2026-07-01 | Spoke-execution safety — five properties that make a Stage-6 pipeline spoke unable, by construction, to (1) push unsafely under multi-chip parallelism, (2) drift from its intake substrate undetected, (3) spawn commit work without a signing key, (4) wander to the operator's primary checkout, or (5) run outside security-hook coverage. **#307** authors **ADR-062** (canonical-spec edit wins over substrate-body mutation; issue bodies remain historical-record) + wires the cross-repo-citation-translation discipline into `stage-05-solutioning.md` A1 + indexes it in the ADR README. **#47** adds the Stage-5 **M1.1 substrate-drift reconciliation chip** (one canonical home in `hub-spoke-bridge.md`, emitting a structured 4-field drift report — `drift_class` / `body_claim` / `current_state` / `gap_age_days`) + the `intake-substrate` PROC failure-mode class (`failure-mode-standard.md`); the optional `check-substrate-drift.sh` primitive was scoped OUT. **#1639** corrects the `block-destructive.sh` BLOCK-DESTRUCTIVE-019 worktree-exemption base from workspace-root to repo-root (derived per ADR-017) + retargets 13 allowlist worktree-globs + repoints the hook-test fixtures with both-direction gate-teeth — a LIVE always-enforce defect fixed before #1531's hook-loading wiring lands. **#1642** adds the spoke-template no-`cd`/never-primary clause in `hub-spoke-bridge.md` + the `git-workflow.md § Primary Checkout Discipline` spoke line (AC-2 subagent-hook coverage deferred to #1531). **#1351** adds the CLAUDE.md governance self-check + a structural local-work entry point rooted outside `pmo-platform/` (`CLAUDE.md.template`; T1+T2a this release). **#59** adds the `ssh-add -l` pre-flight in the commit-producing-spoke spawn procedures + a user-side handoff on empty-agent. `novel`-class; milestone 67-spoke-execution-safety, six cards on one branch / one squash-merged PR (#2799), D-C SINGLE · P0 fully-serial (the 3-way `hub-spoke-bridge.md` edit forecloses parallel chips). #20 (subsumed by v3.28 ADR-052) and #1472 (folded into #1531 + #1639) closed during readiness. CHEAP-to-MODERATE / `git revert` of the release PR restores prior state; the one MODERATE surface is #1639's executable hook edit (clean revert, but a wrong base with #1531 enforce already flipped would mis-block worktree edits — hence the ordering mitigation). #1531 stays open (separate milestone `96-update-install-config-safety`; owns #1642 AC-2). | #2799 | [notes/v3.43_RELEASE_NOTES.md](notes/v3.43_RELEASE_NOTES.md) |
| v3.42 | 85-cleanup-orphan-tooling-reliability | 2026-07-01 | `cleanup-orphan-state.sh` reliability cluster — the branch-cleanup tool now truthfully reaps squash-merged release/chore branches on this squash-merging repo. **#2790** (keystone) resolves `gh` absolutely via `resolve_gh_bin()` (mirroring `resolve_lsof_bin`) so it is reachable under the script's pinned `PATH=/usr/bin:/bin`; without it #2216's MERGED-branch rescue and #655's PR-state prefetch were inert on the canonical host. **#2216** wires `--release-close` to reap squash-merged branches (ancestry SKIP no longer pre-empts the PR-state rescue; `require_pr=0`). **#684** extends `--release-close` scope to `chore/vX.Y-stage-*` branches (additive to v2.09's four slug-anchored patterns; owns the #2048-item-3 cross-milestone duplicate; carries the release's only Stage-10 Docs touch, `git-workflow.md § Step 10`). **#683** lets `--apply` delete a merged branch from a behind-HEAD checkout without `--force` (origin/main-relative eligibility vs HEAD-relative `-d`). **#655** batch-prefetches PR state (O(1) `gh` calls per run). **#670** de-duplicates local-branch rows under `--all`. **#669** trims to a residual `--help`/self-test fix (primary defect resolved earlier by #1678). `routine`-class; milestone 85-cleanup-orphan-tooling-reliability, seven cards on one branch / one squash-merged PR (#2789) against the single file `release/tools/cleanup-orphan-state.sh` (+ `git-workflow.md` for #684). #1552 (refuted P1 data-loss) cut at the Stage-4 gate; #654/#1481/#1552 verified already-resolved. `v3.42` free at the Stage-12 atomic claim (anchor = published v3.41). MODERATE / `git revert`; the only lasting effect of the fixed tool is branch deletions (themselves recreatable from merge commits). | #2789 | [notes/v3.42_RELEASE_NOTES.md](notes/v3.42_RELEASE_NOTES.md) |
| v3.41 | 07-INFRA-hygiene-measurement | 2026-07-01 | Skill-suite structural hygiene plus two net-new measurement auditors. **Hygiene (Wave A):** **#1135** ratifies the canonical mode-heading separator standard (` — `, em-dash flanked by spaces, U+2014) in `canonical-skill-structure.md §4` (survey-grounded em-dash majority over the colon-form minority); **#1136** applies the em-dash separator across the colon-form multi-mode SKILL.md set; **#1132** consolidates vestigial Mode-Detection sections (project-initiator + eval-writer); **#1133** brings the SKILL.md catalog to full `## Guardrails (Platform)` conformance (12 inserts + 26 renames of bare `## Guardrails`, pmo-qa-auditor's `## Guardrails (Extended)` preserved) and fixes a folded `failure-mode-standard.md` drift; **#571** cross-references delivery-engine's DoR/DoD gate vocabulary to G1/G3 + DT-2; **#676** reconciles the stale canary entry in `per-skill-output-contracts.md §Skill 12`. **Measurement (Wave B):** **#16** adds the `context-budget-auditor` skill (token consumption across loaded components); **#17** adds the `skill-compliance-auditor` skill (trigger-rate accuracy) plus **ADR-061** (trigger-rate metric class + eval-writer boundary). **#704** ships SPEC-ONLY (routing-conflict detection beyond Jaccard; deferred code half → #2699). `novel`-class; milestone 07-INFRA-hygiene-measurement, nine slices on one branch / one merge across two waves. Stage 4 live-verified the catalog at 48 skills (not the 23 the source issues assumed); #1133/#1136 re-priced size:M. Re-versioned forward v3.39 → v3.41 pre-merge (A.5.6c lineage — branch descends from v3.40); `claim-version.sh` bound v3.41 at the merge SHA with no CAS collision. CHEAP / `git revert -m 1`; two new `core/` skills + one ADR + catalog-wide SKILL.md structural edits, no `.skill` package committed in-PR (source-only; packages build at close-out), runtime blast radius none. | #2704 | [notes/v3.41_RELEASE_NOTES.md](notes/v3.41_RELEASE_NOTES.md) |
| v3.40 | 30-doc-link-drift-drainage | 2026-07-01 | Four documentation-drift sites drained from the internal reference corpus — no behavior, schema, or runtime change, all guards and historical records preserved. **#108** reconciles `doc-link-maintenance-protocol.md` §8 to the shared warn-log the protocol actually writes (vs `deploy.sh` Check 14). **#131** lowercases the genuine capital-P `Projects/` prose references in `core/governance/OPERATIONS.md` + `operations/OPERATIONS.md` to canonical `projects/` (re-scoped 2026-06-30 to the real OPERATIONS refs; a changelog line naming the historical casing is preserved intact). **#2684** drains the last live IMPROVEMENTS.md / IMP-### Self-Update-Protocol instruction in `routing-rules.md`, routing it to the current GitHub-Issues intake. **#2700** unifies the project-path placeholder to the canonical `projects/[Project]/` form across `frontmatter-schema.md`, `navigation-layer-schema.md`, `routing-rules.md`, `sqlite-index-schema.md`, `health-check-specification.md`, and `project-initiator/SKILL.md` (owns all placeholder-token variants per the BROAD boundary resolution surfaced by the #131 re-scope). `routine`-class; milestone 30-doc-link-drift-drainage, four file-disjoint drainage cards on one branch / one merge. No re-version (v3.40 free at the Stage-12 atomic claim; anchor = published v3.38). CHEAP / `git revert -m 1`; additive documentation edits, the one migrated skill (`project-initiator`) had its `.skill` package rebuilt in-PR so the artifact stays atomic, runtime blast radius none. | #2712 | [notes/v3.40_RELEASE_NOTES.md](notes/v3.40_RELEASE_NOTES.md) |
| v3.38 | 66-release-identity-and-spec-hardening | 2026-07-01 | The release pipeline's own rules get three additive precision fixes to sharpen release-identity and frozen-spec handling across Stages 4/5/6 — a Stage-4 placement forward-check (Phase A0.7 / G-PL3) that detects directory-crossing renames and in-scope deletions on the mainline since the release branch's base, so a "release cut before a structural reorg → new-file placement collides at deploy" pattern is caught up front (#3); a Stage-6 commit-group traceability note (documented simplification — advisory, not enforced) plus a sub-task-methodology enforcement-posture sentence, carrying the release's genuine design decision rendered ADVISORY at Stage 5 (#226); and a Stage-5 frozen-spec prose-vs-artifact precision rule as a required-when-triggered block in the solutioning-output-template's Blast Radius section with a DEFERRED escape, plus a routing pointer in the Stage-5 solutioning reference (#75, with a stale `templates/` → `standards/` milestone-path correction carried into the plan at Stage 4). `novel`-class; three file-disjoint cards on one branch / one merge. Re-versioned forward v3.37 → v3.38 at the Stage-12 pre-merge freeness check (a concurrent sibling release claimed v3.37 first, its plan file also on the same path); the atomic claim then bound v3.38 with no collision — the plan's R1 contingency firing as anticipated, forward-only. CHEAP / `git revert -m 1`; additive documentation/protocol edits to the release-pipeline reference corpus, no skill or `.skill` package touched, runtime blast radius none. | #2683 | [notes/v3.38_RELEASE_NOTES.md](notes/v3.38_RELEASE_NOTES.md) |
| v3.37 | 21-shared-entity-storage-layout | 2026-06-30 | Shared project facts (people, systems, vendors, workstreams, decisions, cross-project dependencies) now live in one shared `projects/_pmo/` area with a consistent page per entity, and `PROJECT.md` becomes a thin composed wiki-link index that links out to those shared pages instead of restating them — plus plans (comms/training/hypercare/cutover/change-mgmt) become typed sub-entities with relationship typing. **#362** establishes the `projects/_pmo/{people,systems,vendors,workstreams,decisions,dependencies}/` layout with six entity-page templates (aligned to ADR-040 `person_id` + people-roster; compose, not fork) and a `project-initiator` Mode A `_pmo/` bootstrap step; **#363** redesigns PROJECT.md as a ≤50-line composed index linking INTO #362's entity pages, adds the Mode A Step-3 template-ref swap, and defines a 4-step live-migration protocol with a backwards-compat consumer table; **#159** models plans as typed sub-entities, resolves the deferred `plan_type` G5 enum membership, and drops `plan_subtype`. `novel`-class; three stories on one branch / one merge (build order `#159 ∥ #362 → #363`). **AC-5 live-project migration DEFERRED** — the merge ships the capability only; the gated POC migration of ≥1 live PROJECT.md (git-ignored ops tree, out-of-diff, snapshot-before/verify-after) is a follow-up per plan R1. No re-version (v3.37 free at the Stage-12 atomic claim; anchor = published v3.36). CHEAP / `git revert -m 1`; additive structure + schema + templates, runtime blast radius none, the only non-git-reversible step (live migration) deferred. | #2676 | [notes/v3.37_RELEASE_NOTES.md](notes/v3.37_RELEASE_NOTES.md) |
| v3.36 | 90-skill-anchoring-process-support-change | 2026-07-01 | Process-domain specialist skills anchored to one best-practice guide, plus a new support-domain guide and the change-domain decision — the four process specialists (pmo-scrum-master, pmo-release-train-engineer, pmo-business-analyst single-anchor; pmo-product-owner dual-anchor to BOTH `process.md` and `governance.md`) each gain a compose-by-reference `## Reference docs` pointer (ADR-019; no content absorption) and the guides' `consumers:` fields list them (#2177, #2178, #2179, #2180); a net-new `core/standards/domain-best-practices/support.md` K1 guide covers ITIL 4 + tiered incident & escalation practice + SRE, with three EXTERNAL + one INTERNAL row registered in `framework-catalog.md` (#2210); and the change domain's best-practice content stays self-bundled in `change-management/references/` with the framework catalog as its registry — no shared `change.md`, pmo-ocm-lead unchanged (transitive via ADR-019), recorded in ADR-057 (`Proposed`) with a reversal trigger, plus a build-philosophy coverage-matrix row (#2211). The process-domain sequel to v3.30 (software) and v3.33 (governance). `routine`-class; milestone 90-skill-anchoring-process-support-change, all six issues on one branch / one merge. No re-version (v3.36 free at the Stage-12 atomic claim). CHEAP / `git revert -m 1`; additive reference wiring + one new guide + one ADR, runtime blast radius none. | #2648 | [notes/v3.36_RELEASE_NOTES.md](notes/v3.36_RELEASE_NOTES.md) |
| v3.35 | 20-records-management-naming-and-cleanup | 2026-06-30 | Records management, file/folder naming, and 08-Generated cleanup are now governed: a records-management policy (`core/governance/RECORDS_POLICY.md`) defining ISO 15489-1 retention, a four-class classification, disposition, and authenticity (#372, ADR-054); a unified `core/standards/artifact-naming-standard.md` covering POSIX-safe syntax + a controlled type vocabulary + explicit folder-naming rules + a validation regex, wired into the seven emitting skills + OPERATIONS.md + a `pmo-qa-auditor` check (#369, ADR-055, absorbing #117); a new `generated-cleanup` skill running `08-Generated/` cleanup under an unconditional approval gate, grouped by lifecycle `artifact_state`, archiving to `_archived/` (#277, ADR-056); and `project-initiator` Mode A folder-name validation against the new standard (#232). `novel`-class; four cards on one branch / one merge. Re-versioned forward v3.34 → v3.35 at the Stage-12 atomic claim (A.5.6c — a concurrent telemetry release claimed v3.34 between Stage 9 GO and Stage 12 Execute; branch name retained). CHEAP / `git revert -m 1`; additive, runtime blast radius limited to two operational skills. | #2610 | [notes/v3.35_RELEASE_NOTES.md](notes/v3.35_RELEASE_NOTES.md) |
| v3.34 | 65-pipeline-telemetry-suite | 2026-06-30 | Pipeline telemetry suite — the release pipeline gains canonical self-measurement: the 4 DORA key metrics (deployment frequency / lead time for changes / change-failure rate / MTTR) at the Build+Deploy layer via a tracked `dora-telemetry.md` schema + `compute-dora-metrics.sh` (#7), and Close-class observability (retro conformance, lessons-learned population, carry-forward closure, cross-release pattern emergence) at the Close layer via `close-class-telemetry.md` + `compute-close-class-telemetry.sh` (#143), with a cross-release aggregation tool extending `synthesize-release-learnings.sh` to emit `cross_release_pattern_emergence_rate` (#2612). `novel`-class — first-of-kind net-new schema + compute tooling; milestone 65-pipeline-telemetry-suite, all three issues on one branch / one merge. No re-version (v3.34 free at the Stage-12 atomic claim). CHEAP / `git revert -m 1`; additive schema + tooling, no skill or `.skill` package touched, runtime blast radius none. | #2621 | [notes/v3.34_RELEASE_NOTES.md](notes/v3.34_RELEASE_NOTES.md) |
| v3.33 | 89-skill-anchoring-governance | 2026-06-30 | Governance-domain specialist skills anchored to one best-practice guide — the six governance-domain specialists (pmo-project-manager, pmo-program-manager, pmo-program-coordinator, pmo-portfolio-manager, pmo-knowledge-manager, release-planner) each gain a compose-by-reference `## Reference docs` pointer to `domain-best-practices/governance.md` (ADR-019; no content absorption) and the guide's `consumers:` field lists them (#2172, #2173, #2174, #2175, #2176, #2181). The governance-domain sequel to v3.30's software-domain anchoring. `routine`-class; milestone 89-skill-anchoring-governance, all six issues on one branch / one merge. Re-versioned forward v3.32 → v3.33 at the Stage-12 atomic claim (a concurrent release published v3.32 first; branch name retained). CHEAP / `git revert -m 1`; additive reference wiring, runtime blast radius none. | #2614 | [notes/v3.33_RELEASE_NOTES.md](notes/v3.33_RELEASE_NOTES.md) |
| v3.32 | 64-hub-autonomy-conformance | 2026-06-30 | — | #2589 | [notes/v3.32_RELEASE_NOTES.md](notes/v3.32_RELEASE_NOTES.md) |
| v3.30 | 88-skill-anchoring-software | 2026-06-30 | Software-domain specialist skills anchored to one best-practice guide — the five software-domain specialists each gain a compose-by-reference `## Reference docs` pointer to `domain-best-practices/software.md` (ADR-019; no content absorption), the guide's `consumers:` lists them, pmo-technical-program-manager is dual-domain (also `governance.md`), and the build-philosophy BP×Skills cells are reconciled (software skill-wired; governance/process skill-wiring-pending) so the coverage map no longer over-claims (#2165, #2167, #2169, #2170, #2171, #2209). `routine`-class; milestone 88-skill-anchoring-software, all six issues on one branch / one merge. Re-versioned forward v3.29 → v3.30 at the Stage-12 atomic claim (a concurrent release published v3.29 first; branch name retained; a benign orphan v3.31 tag left in place). CHEAP / `git revert -m 1`; additive reference wiring, runtime blast radius none. | #2596 | [notes/v3.30_RELEASE_NOTES.md](notes/v3.30_RELEASE_NOTES.md) |
| v3.29 | 73-concurrent-execution-safety | 2026-06-30 | Stage-6 Engineering parallelism postures — a founding ADR (ADR-052) fixes a named parallelism-posture taxonomy (`parallelism-posture-taxonomy.md`) for how a release's Stage-6 engineering work runs concurrently vs. serially, wired into the Stage-4/Stage-6 references, the hub-spoke orchestration bridge, `failure-mode-standard`, and the release-planner skill (#19). `novel`-class; a single-card slice of milestone 73-concurrent-execution-safety (2 issues remain open). Re-versioned forward v3.28 → v3.29 at the Stage-12 atomic claim (v3.28 was published first by the label-taxonomy milestone; the `release/v3.28` branch name is retained). MODERATE / `git revert -m 1`; release-pipeline governance + one skill, runtime blast radius none. | #2585 | [notes/v3.29_RELEASE_NOTES.md](notes/v3.29_RELEASE_NOTES.md) |
| v3.28 | 41-label-taxonomy-canonicalization-backfill | 2026-06-30 | Label-taxonomy canonicalization + a GitHub label-set parity check — the cluster axis gains an orthogonality protocol (one domain cluster per issue vs the `cluster: cross-cutting` span-marker that composes alongside it), the stale cluster color column is reconciled to live, and the undocumented `cluster: security` is registered (12 clusters, was 11) (#80); observation promotion strips the `[Observation]:` title prefix under a deterministic title↔category parity invariant, cross-referenced from the Stage-3 Template-Conversion Rule (#74); and `deploy.sh` Check 51 + `check-label-parity.py` assert label-taxonomy ↔ GitHub label-set parity (enforce-capable MISSING / warn-only ORPHAN, warn-mode-initial, source-agnostic) — its first run surfaced pre-existing drift routed to #1828 / #1777 (#749). `cross-cutting` class; all three issues on one branch / one merge per the milestone-as-one-PR model. Claimed v3.28 at the Stage-12 atomic claim (anchor v3.27, no collision). MODERATE / `git revert -m 1`; the warn-mode check cannot break CI, runtime blast radius none. | #2586 | [notes/v3.28_RELEASE_NOTES.md](notes/v3.28_RELEASE_NOTES.md) |
| v3.27 | 28-corpus-frontmatter-standardization | 2026-06-30 | — | #2545 | [notes/v3.27_RELEASE_NOTES.md](notes/v3.27_RELEASE_NOTES.md) |
| v3.24 | release-hub-orchestrator-skill | 2026-06-30 | Release-hub orchestrator skill — a net-new whole-release control plane (`release/skills/release-hub/`) that takes a milestone and drives it through the release pipeline by composing the stage skills, owning sequencing and readiness-gating but never the stage work itself. Two modes: **Mode R (Milestone Readiness, #2115)** is a pre-flight that confirms a bundled milestone is ready to START before a run is committed — it composes triage/dup, staleness/architecture, dependency, and bundle-coherence checks into one GO / NO-GO with a per-finding disposition list; **Mode O (Orchestrate Release, #2212)** runs the full hub-and-spoke orchestration — spawning the per-stage spokes in order, gating the operator only at the named pipeline checkpoints (plan-review GO/NO-GO, execute) rather than per action. The skill ships with 4 references (the readiness checklist, the orchestration playbook, the spoke-launch contract, and the decision-briefing format), a deploy-roster entry, and the built `.skill` package. `novel` class; both child issues delivered on one branch / one merge per the milestone-as-one-PR model. Claimed v3.24 at the Stage-12 atomic claim (anchor v3.23, `--bump minor`, no collision — the provisional and claimed version coincide, so no abandoned-version row). CHEAP / `git revert -m 1`; additive net-new orchestrator skill that composes existing stage skills and adds no new autonomous mutation surface, runtime blast radius none. | #2497 | [notes/v3.24_RELEASE_NOTES.md](notes/v3.24_RELEASE_NOTES.md) |
| v3.23 | 06-HEALTH-project-health-check | 2026-06-30 | Project health-check skill — a net-new intent-driven project-state drift auditor (`operations/skills/health-check/`) that audits one project for drift between its tracked state and its canonical sources (MCP-primary: Confluence/Jira/Smartsheet/SharePoint; local-fallback: trackers/PROJECT.md/PORTFOLIO.md/transcripts/emails/generated) and emits a categorized 5-section punch list — `## Confirmed` / `## Auto-Actionable` / `## Decisions` / `## Unknowns` / `## Rollup-Diffs` — that is never auto-applied (auto-actionable items stage a `TRACKER_UPDATES:` block for `/tracker-manager` approval; Rollup-Diffs are diff-only in `08-Generated/_health-check/`). Seven modes on one shared contract (4-intent block + 5-section output + S0–S3 confidence bands): `full` (total sweep, default), `timeline` (date/milestone drift with day-of-week validation, no generalized ranges), `attribution` (owner drift, no fabricated owners), `comms` (Communications-Tracker lifecycle; closures route to `/comms-writer` status-only), `plan <name>` (one named plan; requires the arg, prompts rather than guessing), `raid` (RAID-guardrail enforcement; never auto-closes a risk), `sources` (canonical-source inventory + graceful-degradation surface). **v1 foundation (#1125)** built the full contract once + the 3 foundation modes; **v2 (#1126)** implemented the 4 extended modes — both on one branch / one merge. Founding **ADR-051** records the MCP-primary/local-fallback source set + the graceful-degradation contract (unreachable connector → local-only run + banner; uncross-validated finding capped at MEDIUM, never auto-actionable). `novel` class; carried a provisional v2.42 label, claimed v3.23 at the Stage-12 atomic claim (no collision). Adds the `/health-check` command + registry row + deploy roster + `.skill` package. CHEAP / `git revert -m 1`; additive net-new skill + one ADR, runtime blast radius none, read-and-recommend only. | #2488 | [notes/v3.23_RELEASE_NOTES.md](notes/v3.23_RELEASE_NOTES.md) |
| v3.26 | 39-governance-drift-canonicalization-ci | 2026-06-29 | — | #2529 | [notes/v3.26_RELEASE_NOTES.md](notes/v3.26_RELEASE_NOTES.md) |
| v3.22 | 18-pmbok-coverage-and-project-schema | 2026-06-29 | PMBOK artifact coverage + PROJECT.md schema axes — PROJECT.md frontmatter gains three additive axes: the first-class `deliverable_type` deliverable-domain axis (open enum `{software, governance, web, data, enterprise-platform, hardware, process}` + lowercase-kebab open-escape; V13) with §5A Domain-Axis Consumption Pattern + G3-05 conditioning + founding ADR-050 (#351, keystone); `org_structure_type` (the PMBOK org-shape enum; V14, default `functional`) and a refs-only `team_roster` (`{person_ref, role_on_project}`; V15, no-inline-PII invariant) appended off the keystone's post-V13 tail (#262); five PMBOK artifact templates (charter, lessons-learned, change-log, RACI, stakeholder-register) + Tracker 8 (Stakeholder Register) + Tracker 9 (RACI) + OPERATIONS.md registration + deploy-sync wiring (#206); and a new `lifecycle-tailoring.md` mapping the 3-state agent model → PMBOK 5-process-group convention + 3 pointers (#371, foundation). `cross-cutting` class (Deep Stage-9 review; ADR-050 Proposed → Accepted at review). Carried a provisional v2.42 label; claimed v3.22 at the Stage-12 atomic claim (no collision). CHEAP / `git revert -m 1`; additive schema/template/reference fields, runtime blast radius none. | #2489 | [notes/v3.22_RELEASE_NOTES.md](notes/v3.22_RELEASE_NOTES.md) |
| v3.21 | 34-terminology-and-controlled-vocabulary | 2026-06-29 | Terminology & controlled-vocabulary canonicalization — the K1 terminology glossary is refreshed for AI-agent comprehension as autonomy expands: discoverable frontmatter, the Role term anchored to the Autonomy-Tier framework, and four first-class actor terms (Hub, Spoke, Skill, Sub-agent) (#68, keystone); the Tier Disambiguation Table in `autonomy-tiers.md` is extended to cover Hierarchy Tier with every other named convention's disposition stated (#128); and canonical `Initiative` / `Roadmap` terms are added to the glossary, reconciling it with the initiative-roadmap framework and the live `epic:*` label namespace, with Appendix B's "not modeled" assertion removed and ADR-049 recording the canonical vocabulary + the `initiative:` → `epic:`/`project:` label mapping (#432). `novel` class (#432 authors ADR-049 + carries the D-432-Wording canonical-vocabulary decision). Re-versioned v2.42 → v3.21 at the Stage-12 atomic claim (v3.20 spine frontier; see RELEASE_REVERSIONS). MODERATE / `git revert -m 1`; runtime blast radius none. | #2487 | [notes/v3.21_RELEASE_NOTES.md](notes/v3.21_RELEASE_NOTES.md) |
| v2.41 | 99-rca-and-corpus-anchor-hygiene | 2026-06-29 | Governance-hygiene — the root-cause-analysis method (#754) is wired by citation to the failure-owning surfaces its own §2 trigger table named but had no recorded binding (`ppm-agent` invoke-or-hand-off, `pmo-devops-sre` Mode-3 regression origination, the failure-owning pipeline stage(s)) (#1883); the hub's D-Version recurring-decision is reframed from an A/B/C click-gate to a recorded determination by default, escalating to an operator gate only on a deliberate version-less/forced-collision close-out (B) or a concurrent slot claim (C), with the (B)/(C) override valves retained (#1918); the extinct `pmo-platform/engineering/tools/` prefix in `practice-efficacy-framework.md` is dropped to the bare `health-check.sh` form (#1562); and a corpus-hygiene sweep fixes the malformed `(DT↔Engineering, )` cell in `handoff-coordinator-spec.md` and enumerates every PR-time durable-corpus gate + override marker in `core/ADRs/README.md` (#2218). `routine` class; slim cherry-picked milestone (release-corpus migration sliced off to #1549) — documentation/governance/skill-text only, no behavior/schema/runtime change. CHEAP / `git revert -m 1`. | #2438 | [notes/v2.41_RELEASE_NOTES.md](notes/v2.41_RELEASE_NOTES.md) |
| v2.40 | 17-per-project-processing-orchestration | 2026-06-29 | Transcript & meeting processing — the implicit transcript flow is named as one pipeline (Entry → Routing → Core-Tracker → PPM → Stakeholder-Review → Action-Triage) and its outputs are sorted into three automation tiers (auto-write trackers flow, stakeholder-facing/RAID changes stop for approval, action items surface for triage and are never auto-logged); the Open Meetings Tracker gains a governed `lifecycle_state` field (the frozen 3-state `scheduled → held \| cancelled` machine); and meeting materials (pre-reads, agendas, invites, context blocks) are redesigned for a 5-second scan. `routine` class; formalizes existing flow + adds a tracker field + scannable meeting materials — no data migration, no stored-content move, no new project-facing primitive. CHEAP / `git revert -m 1`. | #2426 | [notes/v2.40_RELEASE_NOTES.md](notes/v2.40_RELEASE_NOTES.md) |
| v2.39 | 60-audit-cadence-and-learning | 2026-06-29 | Audit-cadence-and-learning — the platform audits itself on documented event-bound cadences (process-fitness #167 + structural #168 axes) cross-referenced with the Anthropic Base-vs-Build framework as a 3-axis set, and captures recommendation↔choice deltas with a `--window` look-back + Stage-13 retro roll-up (#46), signal-only; `novel` class. Stage 12 completed as a remediation (#2428). CHEAP / `git revert -m 1`. | #2421 | [notes/v2.39_RELEASE_NOTES.md](notes/v2.39_RELEASE_NOTES.md) |
| v2.38 | 72-closeout-output-determinism | 2026-06-28 | — | #2398 | [notes/v2.38_RELEASE_NOTES.md](notes/v2.38_RELEASE_NOTES.md) |
| v2.37 | 91-release-notes-conformance | 2026-06-28 | Release-notes close-out conformance — the release-notes close-out is hardened so it is conformant on **every** authoring path (script AND hub-direct), and the published GitHub Release page can no longer silently drift from the in-repo source note; additive throughout — standard / pipeline-shard / close-runbook / lint / check edits plus one founding ADR, no data migration, no stored-content move, no project-facing behavior change. **Lint coverage for the missed posting defects (#2120):** `release-notes-standard.md` reconciles the three un-versioned title-format descriptions (§5.2 / §5.3 / §5.5 State-0) and the un-versioned `--title` cell in `stage-12-execute.md` to the single canonical `vX.Y — <headline>` form, fixes the §3.1 template plan link to the absolute `https://github.com/{REPO}/blob/main/...` form, and adds a whole-body link-purity lint (check 13) in `lint_release_corpus.py` flagging any repo-relative target in the frontmatter-stripped body (permitting `https://` / `#anchor` / `mailto:`); the Stage 13 close runbook (`hub-spoke-bridge.md` Procedure 7 + `stage-13-close.md` Phase B5.6) gains two `gh release view`-based LIVE-Release assertions (versioned title; no repo-relative body link); eval rubric synced 12→13. **§3.2 lint bound to the close EVENT (#2082):** the note-content conformance lint is wired into `automated-closeout.sh` (`phase_lint_release_notes`) AND named at the hub-direct Phase-B chore-PR fallback's Step 4 verification — on every path a finding naming the closing version blocks milestone close (a pre-existing finding for an unrelated version does not; audit-baseline discipline); the RCA fix for the v2.26 governance failure. **Deterministic Release-body re-emit + drift gate (#2085):** the published Release body is an enforced deterministic frontmatter-strip transform of the in-repo note (never hand-assembled), applied identically on the script and spoke paths, with §5.5 / §5.6 reconciled; a standing detective gate (`check-release-body-drift.sh`, surfaced by `deploy.sh` Check 47) asserts `published body == strip_frontmatter(note)` (flags, never re-emits). **Absorbed — rigor-invariance + Check 48 (#1290, ADR-048):** a named Rigor-Invariance Principle + canonical-checklist attestation at the Procedure 1 scaffolding seam (hub-direct ≡ spoke clause), plus `deploy.sh --check` **Check 48** + a `--check-close-completeness` CI probe (one shared body) asserting the complete Stage-13 output set on main for every `VERIFIED` `RELEASE_LOG` row at/after its cutover, delegating each sub-assertion to the engine that owns it (composes, never duplicates); reads main's state (scaffold-independent), warn-mode-initial + dormant behind a cutover sentinel. `routine` class; D-C SINGLE topology, one release PR (#2378), 14-commit history preserved; no sibling collision (RELEASE_REVERSIONS N/A). The release PR used safe-phrasing Issue-References (no auto-close keywords), so #2120 / #2082 / #2085 were closed manually in this close-out; the `.version` stamp (v2.36 → v2.37), CHANGELOG append, RELEASE_LOG/INDEX/DIGEST entries, and the release note are the Stage 13 close-out completions the automated tool drops for theme-named milestones. CHEAP / `git revert -m 1`. | #2378 | [notes/v2.37_RELEASE_NOTES.md](notes/v2.37_RELEASE_NOTES.md) |
| v2.36 | 69-triage-and-bundling-signals | 2026-06-28 | Triage-and-bundling signals — six Stage 1–6 signals the release pipeline used to re-derive each run (and one repeatedly hand-derived step) are converted into codified pipeline outputs and one tracked tool; additive throughout — spec/governance/schema edits plus a new tool, no data migration, no stored-content move, no behavior change to existing surfaces. **Directional-not-authoritative handoff (#500):** `stage-02-triage.md` §4 Inputs states that an approved ticket authorizes the problem/outcome, not its structural premise — downstream stages treat the ticket structure as a proposal to confirm/overturn at Solutioning; `stage-01-intake.md` §6 Outputs carries the forward-pointer and `stage-io-contracts.md` records the Stage 1 → Stage 2 boundary (principle only; enforcing gate is #501, ms `68-stage-gate-criteria-completeness`; compose, don't duplicate). **Priority-model decomposition (#283):** `stage-02-triage.md` splits priority's triple duty (urgency / importance / scope) into urgency vs value with a dependency-count triage signal, `improvement.yml` reconciled to match, public corpus kept methodology-neutral (no hardwired WSJF). **Native-dep mirror → tracked tool (#662):** the Stage 2 A3.5 native-dependency mirror is promoted from re-derived-each-run to a tracked tool (AS2) at `release/tools/native-dep-mirror.py` with a fixture test — line-directional parser, ignores relates-to/sibling edges, comma-safe. **Cluster threshold + tracking selection (#280):** `stage-03-bundle.md` documents a Stage-3 related-issue-cluster detection threshold + tracking-mechanism selection rule over the existing epic/cluster/sub-issue primitives. **A6 per-milestone + gate G3-17 (#292):** `release-process.md` makes the Stage-3 A6 New-Track Placement Rationale fire per-milestone and `gate-criteria-spec.md` adds enforcing gate **G3-17** (G3-13 retired per ADR-012; G3-14/G3-15 shipped via closed #293/#294; G3-16 reserved) — the edit that changed `release-planner`'s source and triggered its release-cut package rebuild. **Sub-task container threshold (#225):** `stage-06-engineering.md` defines a threshold for the Stage-6 sub-task container choice (sub-issues vs PR checklist); forward edge to #213, ships regardless. `novel` class; D-C SINGLE topology, one release PR (#2284), 12-commit history preserved. **Version note:** planned provisional v2.33; siblings claimed v2.33/v2.34/v2.35 mid-run so the atomic claim bound to **v2.36** above the shipped anchor v2.35 (recorded in `RELEASE_REVERSIONS.md`); the `.version` stamp, CHANGELOG append, RELEASE_LOG/INDEX/DIGEST entries, the release note, and the `release-planner` rebuild are the Stage 13 close-out completions the automated tool drops for theme-named milestones. CHEAP / `git revert -m 1`. | #2284 | [notes/v2.36_RELEASE_NOTES.md](notes/v2.36_RELEASE_NOTES.md) |
| v2.34 | 97-knowledge-and-decision-confidence | 2026-06-28 | Knowledge and decision confidence — new work gets classified the same way every time, captured learnings record the scope they came from, two more facilitation domains ship, the roadmap gains a curator skill, and a long-standing knowledge-eviction gap closes; additive throughout — new and edited reference docs, a new skill, and one build-check extension, no data migration and no stored-content move. **Architecture evaluative-lens layer (#1102):** a new `core/disciplines/` doc enumerates the design-time lenses — the triple-Venn (right skill ∩ right methodology ∩ right altitude) and the K1-universal vs K2–K5-install-specific plug-and-play classifier — wired as a Stage 2 Triage / Stage 4 Planning design check (graduated from observations #577 / #69 at N=2). **Organizational-altitude axis (#564):** `knowledge-architecture.md` gains a learnings-altitude axis (Unit → Task → Workstream → Project → Program → Portfolio → Organization) declared orthogonal to the K1–K5 universality tiers, with `km-protocols.md §3` recording altitude as a capture-time attribute and an adjacent-altitude rollup relation. **ADR-029 encode-and-evict closure (#2214):** the encode-and-evict lifecycle now re-points or drops downstream links to an evicted note and reconciles partially-absorbed notes at close; `deploy.sh` Check 36 gains two logged-not-blocking finding classes. **Facilitation-technique domains (#1945):** `core/standards/facilitation-techniques/` adds the retrospective and planning domains on the v2.22 estimation foundation, same 9-field schema, surfaced through the existing delivery-engine Mode D/E trigger. **Roadmap-curator skill (#453):** a new skill keeps the living roadmap current — default incremental maintenance against the ratified initiative set plus an operator-triggered re-baseline. `routine` class (`project:knowledge-corpus`); shipped as five separate integration PRs; the `.version` stamp, CHANGELOG append, RELEASE_LOG/INDEX entries, and the release note are the Stage 13 close-out completions the automated tool drops for theme-named milestones (#2048). CHEAP / `git revert` per PR. | #2356 | [notes/v2.34_RELEASE_NOTES.md](notes/v2.34_RELEASE_NOTES.md) |
| v2.35 | 104-agent-decision-confidence | 2026-06-27 | Agent decision-confidence — a written protocol lets an autonomous agent calibrate its own confidence at a non-obvious decision point and decide proceed / pause-to-learn / escalate from observable cross-check agreement (never self-reported confidence), scaled to the action's reversibility × autonomy; additive throughout — a new spec, a new ADR, and four edits wiring the gate into existing disciplines and one pipeline stage, no data migration or stored-content move. **Define — spec (#2286):** `core/specs/decision-confidence-protocol.md` defines the mechanism as a compose-not-redefine layer over five existing docs — (1) a confidence signal collapsed to three ordinal states (`CONVERGENT` / `DIVERGENT` / `UNGROUNDED`) from three observable sources (cross-check agreement, gap-namability, weakest evidence label), with verbalized self-confidence named and rejected as an input; (2) a reversibility × autonomy threshold matrix selecting PROCEED / PAUSE-TO-LEARN / ESCALATE per cell, refusing a single global numeric cutoff, with two cross-axis invariants (IRREVERSIBLE never auto-proceeds on a non-`CONVERGENT` signal; the matrix lowers ceremony but never raises an action's autonomy tier); and (3) a bounded pause-to-learn loop (default 1 cycle, hard cap 2) importing the discovery knowability triage as its gap-closer with four exit conditions guaranteeing termination — plus a six-domain application table, the named live consumer, and four FM entries. **Define — ADR (#2287):** `core/ADRs/ADR-047-decision-confidence-compose-posture.md` records the extend-and-compose posture with a per-doc relationship table agreeing cell-for-cell with the spec's §7. **Create — gate + loop (#2288):** `core/disciplines/autonomous-execution-model.md` registers a Pause-to-Learn Pattern as the third pre-action sibling to Retry/Escalate (competence-anchored, self-closing, chaining to Escalate only on budget-exhaustion), and `core/specs/reversibility-protocol.md` promotes its confidence-pairing from a label into an active self-gate as the cost-of-error axis. **Create — live consumer (#2289):** `release/references/pipeline/stage-04-planning.md` Phase A0 runs the gate before rendering the bundle-currency refresh outcome (no-op / amend / re-bundle / defer). **Create — anti-theater guard (#2290):** `core/disciplines/decision-discipline.md` adds G8 making the pause loop's three load-bearing properties (new-signal-injected / bounded / has-exit) observable, with a co-located falsification test-case pair. `routine` class; five separate integration PRs (one per deliverable); the `.version` stamp, CHANGELOG append, RELEASE_LOG/INDEX/DIGEST entries, and the release note are the Stage 13 close-out completions the automated tool drops for theme-named milestones. CHEAP / `git revert` per PR. | #2336, #2337, #2348, #2350, #2352 | [notes/v2.35_RELEASE_NOTES.md](notes/v2.35_RELEASE_NOTES.md) |
| v2.33 | 40-initiative-roadmap-vocabulary-and-home | 2026-06-27 | Initiative-roadmap vocabulary and home — roadmap instances get a shipped in-repo home that survives every platform update, and the label guide stops implying a hierarchy tier the platform does not have; additive — a new git-ignored `/roadmaps/` folder, a token-default rewire to it across the SSOT surfaces, and a one-line label-taxonomy reframe, no behavior or data change. **In-repo roadmap home (#416):** the platform ships a `/roadmaps/` folder — folder + README tracked, roadmap instances git-ignored and never committed, absent from the update/regeneration manifest so platform updates never overwrite operator content; the `<OPERATOR_INSTANCE_ROADMAPS_PATH>` token default moves to the in-repo folder across both sources of truth (the `depersonalization-spec.md` registry and the `operator.toml.template` resolver), staying repointable as an override; **ADR-046** records the decision and supersedes-in-part the operator-local placement of ADR-012 + ADR-017 (authored content ships in-repo + git-ignored, operator runtime state stays operator-local). **Work-hierarchy single-source + Initiative reframe (#1038):** `label-taxonomy.md` stops describing an initiative label as a binding tier between work items and states it is a *grouping*, citing the single methodology-invariant work-item-hierarchy source with `initiative-roadmap-framework.md` reconciled to it — a documentation-truth correction, no labels added, removed, or re-applied. `routine` class (`project:methodology-packs`); the `.version` stamp, CHANGELOG append, RELEASE_LOG/INDEX entries, and the release note are the Stage 13 close-out completions the automated tool drops for theme-named milestones (#2048). MODERATE / `git revert -m 1`. | #2280 | [notes/v2.33_RELEASE_NOTES.md](notes/v2.33_RELEASE_NOTES.md) |
| v2.32 | 104-skill-registry-identity-and-currency | 2026-06-27 | Skill-registry identity and currency — the already-shipped skill registry (`core/skills/registry.md`, ADR-038) is made self-checking against the deployed roster, and the consumer surface that cited a phantom catalog file is corrected; an additive build check plus citation corrections, no data migration or stored-content move. **Row-currency, always-enforce (#1811):** `deploy.sh --check` gains `Check 5(d): Registry-currency (rows)`, appended after Check 5(c) and reusing Check 5's roster machinery, asserting against the deployed roster (`OPERATIONS_SKILLS + RELEASE_SKILLS + CORE_SKILLS`, `CANARY_SKILLS` excluded per ADR-04): (i) every registry row name ∈ roster, (ii) every roster member ∈ registry rows (FAIL on asymmetry, both directions), (iii) every registry row name resolves to a live `SKILL.md`; structural / zero-FP, findings increment `ISSUES` directly, with the `cmd_check` guards inherited and a broken parse degraded to a single loud audit-baseline FAIL. **Field-currency, warn-mode-initial (#1658):** a per-row inner assertion extends the row loop at one marked nesting seam (both layers share one roster source, cannot fork) checking recorded modes/dependencies (set-equality) + trigger-surface presence against the live `SKILL.md`, emitting `WARN` without gating until `registry-field-currency.mode` flips to `enforce` (per the Check 14/15/18/19 shakedown precedent); a companion fix reconciled the `pmo-architect` composition set (frontmatter + registry row) to its true 2-skill set so the field layer reads clean. **Consumer re-point (#1211):** 13 files across `core/` / `operations/` / `release/` that cited a phantom `dependency-graph.md` (never shipped) now cite the live `registry.md`, and one net-new `registry.md` citation in `skill-workspace-location.md` is de-linked to avoid a forward-reference — pure reference correction. `routine` class; the shared `Check 5(d)` design ships as a design-note (no ADR) per the Stage 5 scope-lock; 4 drifted `.skill` packages rebuilt at release-cut. CHEAP / `git revert -m 1`. | #2249 | [notes/v2.32_RELEASE_NOTES.md](notes/v2.32_RELEASE_NOTES.md) |
| v2.31 | 16-knowledge-management-discipline | 2026-06-27 | Knowledge-management discipline — the platform's memory gets one written cross-surface read/write SSOT contract, and the QA platform-health audit gains systematic knowledge-management scanning; additive throughout (new docs + reviewed skill edits), no data migration or stored-content move. **Memory-type taxonomy + cross-surface contract (#1073 / #1074):** `knowledge-architecture.md §2.1` reconciles the four memory types (Work / Knowledge / People / Learning) against the K1–K5 universality, Context-Tier read-precedence, and Document-Tier write-approval axes — demonstrated as four orthogonal composing coordinates, not a fourth stacked axis; a new sibling discipline `memory-architecture.md` is the per-surface SSOT contract table (surface · memory-type · reader · writer · write-authority bound to the Autonomy Tier enum · cadence · class {read-only|auto-write|operator-write-only} · trigger), absorbing the no-shadow-SSOT invariant verbatim and generalizing it from the Knowledge cut to all four types, with `OPERATIONS.md` + `CLAUDE.md.template` linking it and `core/rules/governance-files.md` as the citing consumer. **ADR-045** records the decision and supersedes ADR-029 (its revisit trigger discharged). **People reconcile-and-cite (#1075):** the contract's People row cites the already-shipped functional people-graph — no new surface, no repo-tracked PII. **Operational-state lifecycle + CORRECTIONS graduation (#1076):** an additive surface-class-keyed §8 in `operational-artifact-inventory.md` (growth / cleanup / archive / retention; frozen §1–§7 untouched) plus a thin CORRECTIONS Graduation & Expiry Protocol in `OPERATIONS.md` binding into the existing observation-emergence bar + Pattern-Review cadence (graduate-then-evict / archive-first expiry / KEEP-active), no parallel mechanism and no new ADR. **Adoption audit + pilot (#1077):** a new `memory-adoption-requirement.md` standard (skill × reads/writes-memory audit + a forward-only SHOULD for new skills) with `daily-status` wired as the pilot (inputs + status-log append annotated against the contract). **KM scanning in QA Mode E (#249):** `pmo-qa-auditor` Mode E adds a four-scan KM check-set (doc-debt audit · staleness-by-criticality · artifact lifecycle-state · in-flight-capture) emitting a doc-debt register + a staleness report, all thresholds consumed from `km-protocols.md` + `lifecycle-states-canonical.md` (redefines nothing; INDETERMINATE on missing fields). `novel` class; the 2 edited skills (`pmo-qa-auditor`, `daily-status`) bump to v2.31 and rebuild packages at release-cut (theme-named milestone); the contract enforcement gate is deferred to a follow-up (ships doc-only). CHEAP / `git revert -m 1`. | #2188 | [notes/v2.31_RELEASE_NOTES.md](notes/v2.31_RELEASE_NOTES.md) |
| v2.30 | 38-governance-cross-reference-currency | 2026-06-26 | Governance cross-reference currency — display-text path citations across the corpus that still named the pre-reorganization `pmo-platform/{reference,skills,governance,engineering}/…` folders are reconciled to their verified live homes under `core/` / `release/` / `operations/`, so internal doc and skill-file links resolve instead of dead-ending; documentation/governance citation-accuracy only, no behavior/schema/runtime-check change. **Path-citation reconciliation (#121 / #399 / #677):** the 13 catalog `SKILL.md` files go from 67 stale `pmo-platform/…` citations to zero (#677); a 28-file non-corpus sweep rewrites genuine display-text drift while preserving legitimate occurrences — immutable ADR history, deploy-path globs, the rewriter's own examples, test fixtures, frozen audit line-refs, operator-instance roadmap paths (#399); one single-token QA↔Dev-Testing threshold `≥3` → `>3` is harmonized in `operating-model.md` (#121); and two `module-apis.md` standards files mis-listed under Specs with dead links move to the Standards list. The #677 scope correction (the drift was at `reference/standards/`, and the destination lives at `core/standards/`, not the issue's stated `core/specs/`) was verified against the live tree before any rewrite. **Two no-code dispositions (#2080 / #2081):** the #N-validity gate #2081 asks for is already enforced by the repository-integrity Issue-reference validity gate (no second check added); #2080's stale-#N sweep was re-measured live (176 unresolvable refs across 273 of 419 open issues) and the residual documented-and-kept since the re-version's old→new mapping is unrecoverable per reference; umbrella #753 closes on both children. `cross-cutting` class; additive/citation-correction throughout; the cosmetically text-edited skills' packages rebuilt at release-cut (theme-named milestone). CHEAP / `git revert -m 1`. | #2122 | [notes/v2.30_RELEASE_NOTES.md](notes/v2.30_RELEASE_NOTES.md) |
| v2.29 | 27-corpus-drift-reconciliation | 2026-06-26 | Corpus-drift reconciliation — ten places where a reference or governance doc had drifted from its canonical source are corrected to match; documentation/governance accuracy only, no behavior change. **Count/enumeration drift (#127 / #1100 / #2095 / #107):** `core/CLAUDE.md.template` Review-discipline preference drops the hardcoded "10 anti-laziness rules" for the count-agnostic "its anti-laziness rules" (the set grew 10→14; the count is owned by `review-discipline-principles.md`); `build-reviewer/SKILL.md` Section 1 drops the stale "(10 rules)" summary and defers to the canonical set (`version: v1.10 → v2.29`); `core/rules/skill-deployment.md` replaces its hand-maintained Drift Check enumeration (which left Checks 16/17/19/21/22/24/26/27/31/32/34–46 unlisted) with a `deploy.sh`-derivation rule + reads enforcement mode from `.claude/hooks/deploy-check.mode`; `lifecycle-states-canonical.md` §6.2 drops an already-satisfied forward-consumer row. **Reference-term / field-name reconciliation (#49 / #305 / #857):** `stage-06-engineering.md` corrects "CI/CD = pre-commit hooks" → "PreToolUse hooks (`.claude/hooks/`)"; `core/CLAUDE.md.template` syncs intake tier-selection wording to `stage-01-intake.md` § 5 Path A; `navigation-layer-schema.md` reconciles its remaining `methodology` reference → canonical `delivery_approach`. **Persona-card reconciliation (#881):** the Stage 8 QA persona "no conditional pass" anti-pattern is reconciled to the evolved `stage-08-qa-testing.md` verdict enum (a CONDITIONAL ACCEPT is a defined Phase E verdict with listed defects + an Override Record; only an unconditioned one is the anti-pattern). **Duplicate-basename single-source (#94):** `pmo-skill-editor`'s near-duplicate `regression-checks.md` is removed from source and single-sourced into the package at build time from `core/standards/regression-checks.md` (new `TEMPLATE_SYNC_MAP` entry + explicit-basename resolver rule). **Validator-surfaced cleanup (#498):** corpus-content drift surfaced by the v3.20 validators — 5 historical notes + a v1.03 INDEX-drift + filename findings. `routine` class; additive/text-correction throughout; the 2 changed-skill packages rebuilt at release-cut (theme-named milestone). CHEAP / `git revert -m 1`. | #2124 | [notes/v2.29_RELEASE_NOTES.md](notes/v2.29_RELEASE_NOTES.md) |
| v2.28 | 15-generated-vs-source-provenance | 2026-06-26 | Generated-vs-source provenance — instance-side provenance markers for AI-generated artifacts (single card #205; siblings #200 absorbed / #201 superseded, already closed). `core/schemas/frontmatter-schema.md` Category 3 adds the instance-population `generated_by` (skill **name + semantic version**, distinct from `created_by`) plus the reciprocal dual-semantics note that closes the `template-protocol.md` §8 dangling both-locations forward-reference, defines `source_inputs` as the canonical cross-domain provenance carrier (`TR-### \| MSG-### \| path`) replacing the "not yet defined" placeholder, adds a stable `id` (`<type>-<project>-<YYYY-MM-DD>-<nn>`, filename-independent, `Required: No`), marks Domain-C `synthesis_scope` DEPRECATED → alias of `source_inputs` (`trigger_source` kept distinct), and adds the §Scope generated-vs-source one-liner (`domain`/`folder` split confirmed present, #200 absorbed). The four emitting skills (`artifact-generator`, `comms-writer`, `daily-status`, `ppm-agent`) stamp `generated_by` + `source_inputs` on write (missing-header → regenerate rule); `OPERATIONS.md` records the marker requirement + forward-only no-back-fill policy; `pmo-qa-auditor` Mode D adds a `generated_by`-presence spot-check. All fields `Required: No` → purely additive, zero consumer breaks. 5 skills bumped to v2.28 + packages rebuilt at release-cut (theme-named milestone); structured `source_ref` + `relationships[]` routed to #1770. `novel` class; CHEAP / `git revert -m 1`. | #2091 | [notes/v2.28_RELEASE_NOTES.md](notes/v2.28_RELEASE_NOTES.md) |
| v2.27 | 22-ticket-information-architecture | 2026-06-26 | Ticket information architecture — three coupled cards establish how tickets carry confidence, titles, and information. **#436 (staleness-confidence standard + ADR-043):** a new `core/specs/staleness-confidence-standard.md` defines the ONE canonical four-band ordinal **depth** scale (`S0-NONE` < `S1-SUPERFICIAL` < `S2-SUBSTANTIVE` < `S3-STRUCTURAL`, optionally backed by a projected score) that every ticket-facing staleness mechanism maps onto — five incompatible shapes collapse to one vocabulary; founding slim-kernel ADR-043 records the ordinal-depth-over-time-decay decision (which reverses the originating issue's lean); `delivery-engine` backlog-aging reports on it (age alone never reaches `S3`). **#545 (human-informative titles + G1-01/Check-22 floor):** the three work-item templates drop the `[...]:` title prefix (type travels on the label), `gate-criteria-spec.md` repurposes **G1-01** in place into a title-informativeness floor (syntactic — no bracket prefix + substance floor; same ID, schema v1.15→v1.16), `deploy.sh` Check 22 enforces it **warn-tier** (legacy prefixed issues WARN, never FAIL — no forced bulk re-title), `intake-style-guide.md` §7 carries the semantic rubric, and `intake-desk` elicits to it. **#211 (ticket information model + Status/Stage invariant):** a new `release/references/specs/ticket-information-architecture.md` captures the Three-Layer Model (Source of Truth = issue body / Stage Reviews = comments|sub-issues / State Anchors = Projects-fields|labels|Milestones), and the backlog triage view (`github-projects-guide.md`) adds a **Stage** column so `Bundled` (Stages 3–5) no longer collapses into one group. `cross-cutting` class; additive throughout (in-place criterion-shape change + warn-tier gate + read-only/elicitation skill edits); the 2 changed-skill packages rebuilt at release-cut (theme-named milestone). CHEAP / `git revert -m 1`. | #2070 | [notes/v2.27_RELEASE_NOTES.md](notes/v2.27_RELEASE_NOTES.md) |
| v2.25 | 76-hub-spoke-orchestration-discipline | 2026-06-26 | Hub-spoke orchestration discipline — the orchestration failure-modes the platform learned by trial-and-error are codified into `release/references/how-to/hub-spoke-bridge.md`, the bridge reference the hub reads at runtime. **Spoke A (#2016)** adds a cascade-completeness chip block so a spoke whose spec changes a count / enumeration / threshold enumerates every stale occurrence (#78), a necessity/value-add 7th hub-eval dimension so the hub can diverge from accurate-but-inert spoke recommendations rather than rubber-stamp them (#210), a durable-corpus de-fragile authoring pre-check at the bridge + `reference-durability-standard.md` (#879), four chip disciplines lifted memory→corpus — verbatim chip-source, issues-as-spoke-contract, hook-safe git idioms, concurrent-spoke contention recovery (#1669), and a mid-Stage-5 cross-ticket scope-detection rule + hub escalation handler anchored by founding ADR-041 (#31). **Spoke B (#2017)** ships the #189 empirical hook-inheritance suite — a Layer-A CI regression + a Layer-B operator probe + the new Check 46 `agent-tools-list-conformance` `deploy.sh --check` + a `subagent-security-posture.md` §1/§3 reconcile, anchored by founding ADR-042; empirical finding: subagent hook-inheritance CONFIRMED at workspace root, the worktree-session hook-load residual surfaced and tracked as #1472. `novel` class; additive throughout (no `SKILL.md` edited); two disjoint single-writer spokes. CHEAP / `git revert -m 1` per PR. | #2016, #2017 | [notes/v2.25_RELEASE_NOTES.md](notes/v2.25_RELEASE_NOTES.md) |
| v2.24 | 26-corpus-conventions-and-standards-hygiene | 2026-06-26 | Corpus conventions and standards hygiene — a documentation/governance hygiene release (no executable surface). A blast-radius relocation hub (#77) moves `failure-mode-standard.md` + `process-flow-diagram-standards.md` into `core/standards/` with a live-verified 90-file inbound reference sweep re-grepped to 0 dangling `specs/` refs (ran first; rewrites a ref in `triage-design-rereview.md` (#81) + relocates the diagram standard (#113)). Four net-new corpus docs land: a minimal-addition authoring discipline (`core/disciplines/minimal-addition-discipline.md`, #751), the third domain-best-practices domain (`domain-best-practices/process.md`, #764), an audit-recommendation status-badge convention (`audit-recommendation-status-badges.md`, #171), and a Claude Code runtime-state catalog (`claude-code-runtime-state.md`, #163). Three drift fixes reconcile reference docs to live state: skill-deployment roster counts (#580), framework-catalog `canonical_doc` paths + km-governance count (#663), and the `triage-design-rereview §7` `size:XS` omission (#81). A `type:spike` substrate-neutrality audit (#1094) confirms the bare-toolkit substrate (no methodology vocabulary hardcoded as universal beyond `delivery_approach`; findings register under `pmo-platform/analysis/`; precursor to K1 methodology-pack authoring #1090). `novel` class; additive doc/governance only; the 37 changed-skill `.skill` packages rebuilt at release-cut (#77's ~30 inbound-ref `SKILL.md` rewrites + the version bump). 10/10 ACs PASS. **Re-versioned v2.23→v2.24** at Stage 12 (the `v2.23-functional-people-graph` sibling claimed v2.23; next-free minor per defer-to-merge). CHEAP / `git revert -m 1`. | #2019 | [notes/v2.24_RELEASE_NOTES.md](notes/v2.24_RELEASE_NOTES.md) |
| v2.26 | 85-people-graph-activation | 2026-06-26 | People-graph activation — install/update + setup-workspace seed the operator-instance people-roster create-once (never-clobber) at three sites behind a new `pmo_people_roster()` accessor (#2040); GETTING_STARTED gains a people-graph adoption section with the per-tier `ref->Person` owner flow (#2041); a binary-judge eval verifies the four consuming skills resolve read-only from a populated roster (#2042). Activates the structure shipped in v2.23. `routine` class; additive; CHEAP-MODERATE / `git revert -m 1`. | #2072 | [notes/v2.26_RELEASE_NOTES.md](notes/v2.26_RELEASE_NOTES.md) |
| v2.23 | 14-functional-people-graph | 2026-06-26 | Functional people-graph — PMO agents now resolve people from one maintained, never-committed functional people-graph. A closed-allow-list operator-instance roster (`people-roster.yaml`, `{value, source, last_verified}` provenance + a `status` tombstone; only a de-identified template ships in-repo) is the data surface (#315); a read-time composed VIEW over the roster + the frozen Person/Resource entities (compose-not-absorb, no committed store) answers who-does-what / who-covers-whom / coverage-by-capability, with Tier-1 ambient maintenance + a clarification queue (leg C, #1166); the four leadership-owner fields (project / portfolio / program owner + initiative sponsor) are type-lifted from free text to `ref→Person` with an external fallback, governed by ADR-040 (mutual-exclusion + field-conditional resolution; #1898); `comms-writer` / `tracker-manager` / `ppm-agent` / `delivery-engine` read names / owners / coverage read-only (leg D, #1899); the graph + its maintenance + its consumption are registered in OPERATIONS.md (leg E, #1900); spike-gated (#1897). The filled roster is protected by out-of-tree placement (primary) + a `.gitignore` stray-copy backstop + a PII pre-commit needle extractor. `novel` class; additive; functional-coordination only — explicitly not an HR/performance system. MODERATE / `git revert -m 1`. | #2020 | [notes/v2.23_RELEASE_NOTES.md](notes/v2.23_RELEASE_NOTES.md) |
| v2.22 | 25-comms-and-facilitation-reference-substrate | 2026-06-25 | Comms-and-facilitation reference substrate — facilitation techniques get a governed K1 home (`core/standards/facilitation-techniques/`, seeded with its first delivery-lifecycle domain, Estimation, and surfaced silently by `delivery-engine` Mode D; #56), the canonical meeting-agenda + meeting-recap output-format specs are authored once and single-sourced out of `comms-writer` (Types 2/3 + `channel-formats.md` + the `MTG-###` agenda field all cite-by-path, no inline duplicate; #317), and a recap↔follow-up boundary contract keeps a recap a reference to discrete follow-up records rather than their sole container (artifact-catalog split + `ppm-agent` emit-path rewire on canonical `operational-artifacts.md`; #378). 5 skills materially edited + bumped to v2.22 (packages rebuilt in-merge); `novel` class; #56 decomposed to a corpus-foundation card (9 per-domain cards deferred to epic #1173); QA PASS on all 3 cards, 0 defects. MODERATE / `git revert -m 1`. | #1919 | [notes/v2.22_RELEASE_NOTES.md](notes/v2.22_RELEASE_NOTES.md) |
| declarative-gating-model (version-less) | declarative-gating-model | 2026-06-24 | Declarative cross-methodology gate conditions — the work-item type-pack meta-schema can declare a gate whose condition is a *related* item's workflow status (a child can't start until its parent Epic is design-approved) or a *set-aggregate* over related items (a Kanban WIP limit), not just the item's own fields. Research-led: two spikes (#1875 gate-landscape, #1876 schema-feasibility) returned a one-extensible-model feasibility verdict; the story (#1870) adds the 3-kind `criteria.checks[].condition` discriminated union (related-item-status + set-aggregate built; control-field reserved), an EAD `x-pmo-aggregate` class, and detect-and-refuse cycle-safety — in `work-item-type-schema.md` only, plus founding ADR-039. Additive (meta-schema stays v1); `novel` class. CHEAP–MODERATE / `git revert`. | #1890 | [notes/declarative-gating-model_RELEASE_NOTES.md](notes/declarative-gating-model_RELEASE_NOTES.md) |
| v2.20 | 13-field-lifecycle-and-cmdb-automation | 2026-06-23 | Field-lifecycle and CMDB automation — entity fields get a governed, agent-driven lifecycle: shared + portfolio entity-lifecycle transition protocols (G8, #156/#1155), a G10 write-permission matrix governing who writes which field per stage aligned with the Agent Write Permissions table (#154/#208), and skills that emit/consume the transitions (#1156). The `artifact_state` model is reconciled onto the canonical `lifecycle_state` + Domain model (`promotion_state` carve-out; dual-read removed; artifact-generator + artifact-lint migrated; new `artifact-workflow-protocol.md`) (#1865/#1866). The skill registry is evolved into the platform CMDB (all deployed skills are CIs; Option A, **ADR-038 supersedes ADR-035 §Decision part 4**) with a per-project Artifact Register (#202), and a project-tracking integrity sweep adds dormancy (10bd) / overdue-decision (3–5bd) / evidence-backed-gate discipline (#752). 9 deliverables; `cross-cutting`; QA ACCEPT (~48/48 ACs, 0 defects); MODERATE / `git revert -m 1`. *(Numbering: `v2.20` tagged after `v2.21` — concurrent release claimed the higher number first; claim order non-monotonic by version; self-heals via the claimed-set.)* | #1878 | [notes/v2.20_RELEASE_NOTES.md](notes/v2.20_RELEASE_NOTES.md) |
| v2.21 | decision-rendering-standardization | 2026-06-23 | Hub Decision Briefings made auditable along the agent-operating-principles axis — information-sufficient (pre-load spec content, enumerate the full option space, render-before-AskUserQuestion), design-principle-conformance-scored via a new K1 register + per-option D-Gate subsection, and shaped by standing per-gate-class framing directives (ADD-never-SUBTRACT). 3 cards; `cross-cutting`; MODERATE. | #1877 | [notes/v2.21_RELEASE_NOTES.md](notes/v2.21_RELEASE_NOTES.md) |
| v2.19 | comanagement-shim-retirement | 2026-06-22 | Co-management governance cleanup — retires the legacy co-management compatibility shim (the operator-specific naming leaves `project-initiator` entirely; it survives only as a one-line `project-schema §7` migration breadcrumb + frozen test fixtures), genericizes the residual K4 operator-config field names to `co_management_*`, and **single-sources the Dual-Framing Bridge rule in `OPERATIONS.md`** — removing the redundant `core/CLAUDE.md.template` copy and repointing the 7 citations, closing the duplicate-source drift that had left the two copies disagreeing (the drift surfaced during this release). Follows v2.18's decouple; finishes de-broadcasting the operator-specific naming from the public codebase. **Migration:** a `PROJECT.md` on the legacy co-management key must rename it to `dual_framing_enabled` (one-line; identical semantics; the shim no longer reads it) — the 3 operator-local files were migrated alongside. 2 issues (#1846 shim + token, #1853 bridge single-source). Back-compat **contract change** (shim retirement) → minor, not patch; CHEAP / `git revert -m 1`. | #1851 | [notes/v2.19_RELEASE_NOTES.md](notes/v2.19_RELEASE_NOTES.md) |
| v2.18.1 | operator-instance-path-decoupling | 2026-06-22 | Operator-instance path decoupling (.env pattern) — a single resolver (`${CLAUDE_WORKSPACE_ROOT:-$HOME/Claude}` per ADR-032, no new variable) for the operator needle/instance path, a tracked `.env`-style template + create-once scaffold (gitignored content), persistence across `update.sh`, and a **fail-closed** PII guard (warn-mode-initial) that closes the silent fail-open. Sequenced onto #1847's path-portability enforcement (rebased + Check-43-allowlisted). 1 story (#1830). Patch off v2.18; MODERATE / `git revert -m 1`. | #1849 | [notes/v2.18.1_RELEASE_NOTES.md](notes/v2.18.1_RELEASE_NOTES.md) |
| public-flip-depersonalization-enforcement (version-less) | 43-public-flip-depersonalization-enforcement | 2026-06-21 | Public-flip depersonalization + path-portability **enforcement** — tokenizes the residual operator-handle leak, registers the GitHub-Projects token vocabulary first-class, and stands up four warn-mode-initial guards (path-portability `deploy.sh` Check 43, depersonalization-token Check 44, the gh-issue-ops path-leak hook, the draft-file hook + repo-integrity CI gate), plus `operator.toml` lossless round-trip and orphan `PMO_INSTANCE_PATH` convergence onto `CLAUDE_WORKSPACE_ROOT` — moving the public-repo boundary from manual vigilance to standing enforcement. 9 build issues + 1 verify-only (#376 KEEP). Additive governance + warn-mode hooks (no skills/packages); CHEAP / `git revert -m 1`. | #1847 | [notes/public-flip-depersonalization-enforcement_RELEASE_NOTES.md](notes/public-flip-depersonalization-enforcement_RELEASE_NOTES.md) |
| v2.18 | 86-hybrid-comanagement-decouple | 2026-06-21 | Hybrid co-management decouple + Hybrid-Two — the `Hybrid` methodology is now defined cleanly as a **user-configurable two-archetype combination** (`delivery_approach: [A, B]`, e.g. `[Scrum, Kanban]`) and decoupled from the co-management arrangement it was previously tied to. The legacy co-management field is renamed `dual_framing_enabled` (legacy key still accepted via a deprecation shim), the dual-framing status artifact is renamed the "Dual-Framing Bridge", and the operator-specific token survives only as operator-local (K4) config; dual-framing behavior is unchanged. Touches a K1 schema field, a registered artifact + registries, two governance files, 26 skills, and the XC-06 gate. Per-space methodology split deferred to #1788. Non-breaking (legacy-key shim); CHEAP–MODERATE / `git revert -m 1`. | #1797 | [notes/v2.18_RELEASE_NOTES.md](notes/v2.18_RELEASE_NOTES.md) |
| v2.17 | architecture-altitude-discipline | 2026-06-21 | Architecture-altitude discipline — every new capability is interrogated for its abstraction altitude (extend an existing platform seam, or solve point-wise?) as a forcing function at Stage 4/5, closing the gap that let a host-concrete design land where a host-agnostic capability + adapter was the right seam. A Stage-4 T3 obligation rider, an `altitude` distinctness axis (point-fix / extend-seam / new-abstraction) + ≥2-band rule, a blocking design-review §4.7 seam-composition gate, the `HOST-BINDING-LEAK` leakage class + a warn-mode Check 42 detector, a scoped A6.5 adversarial altitude exception, and `[adapters].repo_host` cross-references in the release specs. Additive governance-doc + one detector (no skills/packages); CHEAP / `git revert -m 1`. | #1812 | [notes/v2.17_RELEASE_NOTES.md](notes/v2.17_RELEASE_NOTES.md) |
| v2.16 | release-version-claim-determinism | 2026-06-21 | Concurrent-release version-claim determinism — a release's version is now claimed atomically at the merge tag (defer-to-merge + ref-CAS) instead of reserved early, so two releases in flight can no longer silently collide on a number. Defined host-agnostic via a config-selected `repo_host` adapter (GitHub/git the v1 reference); founding ADRs (ADR-036/037), host-adapter + grammar/semantics standards, the `claim-version.sh` atomic-claim tool, a CI freeness gate, and a machine-readable re-version ledger + recovery doctrine. Additive (no skills/packages touched); CHEAP / `git revert -m 1`. | #1768 | [notes/v2.16_RELEASE_NOTES.md](notes/v2.16_RELEASE_NOTES.md) |
| v2.14 | 71-autonomy-phaseout-foundation | 2026-06-21 | — | #1636 | [notes/v2.14_RELEASE_NOTES.md](notes/v2.14_RELEASE_NOTES.md) |
| v2.15 | 05-ROLE-sustain-coverage-router | 2026-06-20 | Role-Specialist suite GA — 5 Release-to-Sustain role agents (Release Manager, Tier-1/2 Support, OCM Lead, Knowledge Manager) + the capstone PMO Skill Router reading the new `core/skills/registry.md` logical skill registry. Closes epic #284 Phases 4–5; the suite reaches its full 19-Specialist roster, addressable as one routable unit. Additive (6 new skills + registry + ADR-035); CHEAP / `git revert -m 1`. | #1700 | [notes/v2.15_RELEASE_NOTES.md](notes/v2.15_RELEASE_NOTES.md) |
| v2.12 | 12-field-first-intake-enforcement | 2026-06-20 | — | #1699 | [notes/v2.12_RELEASE_NOTES.md](notes/v2.12_RELEASE_NOTES.md) |
| release-version-stamping (version-less) | release-version-stamping | 2026-06-20 | The platform version file stays accurate after a release — `.version` had frozen at `v2.08` inside the `v2.09`/`v2.10` tags, so the version-skew banner reported a perpetual "update available" no update could clear. #1643 fixes it in 3 layers: corrects the stale value (L1), makes the `.version` stamp an owned Stage 13 close-out step in `automated-closeout.sh` + declares it in `stage-13-close.md` (L2), and adds `deploy.sh` Check 39 to fail the build if `.version` drifts ≥2 minors behind the latest reachable release tag (L3). Version-less (no tag, no GitHub Release). | #1693 | [notes/release-version-stamping_RELEASE_NOTES.md](notes/release-version-stamping_RELEASE_NOTES.md) |
| v2.13 | 63-finding-disposition-discipline | 2026-06-20 | — | #1657 | [notes/v2.13_RELEASE_NOTES.md](notes/v2.13_RELEASE_NOTES.md) |
| v2.11 | 04-ROLE-delivery-coverage | 2026-06-20 | — | #1555 | [notes/v2.11_RELEASE_NOTES.md](notes/v2.11_RELEASE_NOTES.md) |
| v2.10 | 24-agent-artifact-disciplines | 2026-06-20 | — | #1556 | [notes/v2.10_RELEASE_NOTES.md](notes/v2.10_RELEASE_NOTES.md) |
| v2.09 | 37-deploy-sh-script-health | 2026-06-20 | **Deploy + release-tooling health — checks that silently skipped now run, scripts re-pointed at the live tree** — a `novel`-class release of 11 surgical fixes to the platform's own deploy and release scripts, sequenced D-C SINGLE (one branch, one commit per issue) because five issues edit `core/deploy/deploy.sh`. **deploy.sh cluster (5):** **#1104** corrected the Check-9 `MIRROR_PAIRS` source path (`release/rules/` → `release/governance/release-process.md`) so the rules-mirror pair is enforced instead of silently SKIPped, + a missing-source WARN guard; **#984** applied the #88 `remove_mirror_subtree`/root-cause pattern to the supplementary-copy loop so a read-only-target copy failure reports an actionable cause; **#661** added an additive `18d`-class framework-catalog `canonical_doc` path-resolution sub-check (warn-mode-initial) to the Check 18 block + `check-version-anchors.py`; **#1036** tombstoned the silent Check 24 numbering gap, attached reactivation anchors to the dormant Checks 11/30, and added a check-retirement registry (the issue body's wrong "Check 9 dormant" citation was corrected before Stage 5 — Check 9 is the live rules-mirror sync); **#86** was largely pre-resolved on live main (Check 19 `\|\| echo 0` → `0\n0` crash already fixed), scope reconciled (G-PL1) to a residual 2-site sweep + regression-confirm. **Independent files (6):** **#400** rewrote `blast-radius.sh detect_mirror_pairs` against the live `core/rules` topology (the detector guarded a dead `pmo-platform/engineering/rules` path, returning zero pairs) + protocol-doc typo fixes; **#758** fixed extinct-path resolution in `compute-cycle-time.sh` + `synthesize-release-learnings.sh` (REPO_ROOT depth + env→`operator.toml`→`${CLAUDE_WORKSPACE_ROOT}` ladder) and codified the `OPERATOR_INSTANCE_EVALS_RESULTS_PATH` token; **#760** replaced `check-doc-links.py`'s hardcoded `WORKSPACE_ROOT` with a param/env override for relocated/sandboxed trees; **#659** added absolute/worktree-glob allowlist forms for the 6 release tools lacking them; **#1089** extended `cleanup-orphan-state.sh --release-close` to match version-prefixed `release/vX.Y-<slug>` branches + `agent-*` worktrees; **#665** anchored/demoted `audit-mcp-usage.sh` per the operator's Stage-5 disposition (no runtime change). `novel` class, CHEAP–MODERATE reversibility / HIGH confidence (surgical corrections + additive checks; one commit per issue → per-issue `git revert`; whole-release rollback = `git revert -m 1` of PR #1551; new checks ship warn-mode-initial). All 11 verified file:line against live state at Stage 4 (3 scope-shaping findings: #86 pre-resolved, #1036 Check-9 citation corrected, #659 partially pre-resolved). Over-band note: 11 > the 5–8 band — operator-recorded override on capability-coherence grounds. PR #1551 merged to main via merge commit `581863b3d611356e4b88ed3891cc9e7e642a5dde`, `v2.09` tag at the merge SHA. The 11 parents are marked closed at Stage 13 (PR #1551 used safe phrasing; not auto-closed from the release PR). | #1551 | [notes/v2.09_RELEASE_NOTES.md](notes/v2.09_RELEASE_NOTES.md) |
| v2.04 | 62-close-out-registers | 2026-06-20 | **Close-out retrospective register template + a decided release-corpus public-vs-instance home (ADR-032 design; migration deferred)** — a `cross-cutting`-class release that ships the per-release close-out ceremony as a public template and ratifies which release-corpus surfaces are public capability vs operator-instance content, *without* executing the corpus move. **#1412 (DESIGN ONLY this release):** `core/ADRs/ADR-032-release-corpus-public-vs-instance-split.md` (Accepted) applies ADR-017's S1-Package-vs-S3-State cut to the release corpus — `RELEASE_LOG.md` / `RELEASE_DIGEST.md` / `notes/` / `plans/` are S3 operator-instance content; `CHANGELOG.md` + an empty `RELEASE_INDEX.md` seed are the thin S1 public surface; templates / schema / pipeline / tools / deploy-checks are S1 public mechanism (load-bearing rule: *the capability ships; the content instances*). It resolves #1412's 5 open questions, pins the instance-root resolver to the canonical `${CLAUDE_WORKSPACE_ROOT:-$HOME/Claude}` (not the ADR-017-named orphan `PMO_INSTANCE_PATH`), and chooses **HEAD-only removal over an irreversible history scrub** (non-PII operational detail; `git filter-repo` is force-push-denied + `refs/pull/*`-leaky + buys nothing the threat model needs). The corpus **migration execution** (`.gitignore` + `git rm --cached` + move + re-point 4 tools + 2 reference docs + converge 21 `deploy.sh` `PMO_INSTANCE_PATH` fallthroughs) is **DEFERRED** to a complete-by-construction follow-up issue filed at Stage 13. ADR authored as ADR-029 in Stage 5, renumbered to ADR-032 at Stage 6 (ADR-029/030/031 landed first; `check-adr-numbers.py` confirmed 032 gap-free). **#360 + #361 (combined template):** one `release/references/templates/release-learnings-register-template.md` ships PUBLIC carrying a full Kerth retrospective (Prime Directive + 3-question + ritual of closure) + full PMBOK 7 lessons-learned (Situation / Outcome / Lessons / Next-cycle Actions); `release/references/pipeline/stage-13-close.md` §6 names the register and states its **COEXIST** relationship to the machine-generated `#### Release Learnings v<X.Y>` triple H4 block (directional: triple → register; the triple stays authoritative for the cross-release pattern-detector and unaffected by the register's absence). **Half-migration friction (flagged):** the deferred migration means the operator-instance corpus root `${CLAUDE_WORKSPACE_ROOT:-$HOME/Claude}/personal/pmo-instance/releases/` is not yet bootstrapped (the `install.sh` skeleton is part of the deferred execution) and `<OPERATOR_INSTANCE_RELEASE_LOG_PATH>` still resolves to the in-repo `release/releases/RELEASE_LOG.md` per the `release-notes-standard.md` footnote — so all 5 corpus surfaces this release are written to their in-repo paths under the pre-extraction resolution rule (no corpus migration / `.gitignore` / toolkit re-point performed). The first filled close-out register (the v2.04 dogfood, satisfying #360/#361 AC6) was written to the canonical instance path, created at close because the bootstrap has not run. `cross-cutting` class, CHEAP reversibility / HIGH confidence (additive — a NEW template + a NEW design-only ADR + a `stage-13-close.md` §6 edit; whole-release rollback = `git revert -m 1` of PR #1533; the new template has no prior consumer and no release records were moved). **Version reclamation:** scoped `v2.07` at Stage 4 → provisional `v2.10` at Engineering Commit 0 → reclaimed the original abandoned `v2.04` slot at the Stage 12 gate (operator decision 2026-06-20; PR #1407 never merged; `git ls-remote --tags origin v2.04` confirmed the slot free) — a deliberate backwards-version reclamation numerically below the shipped v2.05–v2.08, operator-accepted `git describe` non-monotonicity. The release branch `release/v2.10-close-out-registers` is retained for traceability only (deleted at Stage 13 cleanup); the tag, plan filename, PR title, and commit scope all carry v2.04. PR #1533 merged to main via merge commit `cc10af42dfb2d4771b74107513c0e9cdd050486b`, signed-annotated (ED25519) `v2.04` tag at the merge SHA. The 3 cards are marked closed at Stage 13 (PR #1533 used safe phrasing; not auto-closed from the release PR). | #1533 | [notes/v2.04_RELEASE_NOTES.md](notes/v2.04_RELEASE_NOTES.md) |
| v2.07 | 10-ambient-intake-automation | 2026-06-20 | **Ambient intake automation — one operator-set automation dial, scheduled sweeps, a consolidated digest, and a runtime enforcement hook** — a `novel`-class release that turns the daily-processing cycle from a manual, unguarded ritual into a governed, scheduled, self-reporting capability bounded by a single plain-language ceiling. **#322 (C0 — the dial)** adds an `[automation]` table with `automation_level` (`off` / `recommend` / `bounded_auto`) to `core/config/operator.toml.template`, a workspace-wide ceiling read by all ambient automation per `effective = min(automation_level, per-action max)` — the irreducible-human set is never unlocked regardless of dial position. **#1159 (C1)** specifies a watched ambient inbox drop-zone + content-hash dedup cursor (`core/standards/c1-ambient-inbox-cursor.md`) so the same artifact is never ingested twice. **#1160 (C2 — Path A)** specifies the scheduled capture→classify→register intake sweep over the inbox + drive transcripts/emails (`core/standards/c2-intake-sweep-path-a.md`), clamped by `automation_level` (a `recommend`-ceiling sweep surfaces drafts and writes nothing above Tier 1). **#1161 (C3 — Path B)** specifies the snapshot-diff external-sync (Jira / Confluence / Smartsheet MCP) that reconciles tracker state and proposes **evidence-gated** auto-closes (`core/standards/c3-external-sync-path-b.md`). **#1162 (C4)** rolls every sweep into one consolidated daily-status digest with a **sweep-health heartbeat** so a silent sweep failure is impossible (`operations/skills/daily-status/SKILL.md` + `references/ambient-sweep-digest.md`). **#1163 (C5)** ships a unified payload-triggered PreToolUse **autonomy-ceiling hook** (`core/hooks/block-autonomy-ceiling.sh`, 442 lines, + cache-primer + tests), registered in `core/settings.json.template`, **warn-mode-initial** via `core/hooks/.autonomy-mode.template` — it blocks above-ceiling actions in the payload-detectable Tier-0 classes (governance / cross-domain) once the operator flips it to enforce after shakedown. The C5 reconciliation **R-C5RECON** resolved to this single unified hook (**ADR-031**, Accepted), which **supersedes** the `subagent-security-posture.md §4` subagent-tier design. `novel` class (plan-declared — the milestone carries no `release-class:` label). MODERATE reversibility / HIGH confidence — the config dial, the four C1–C4 specs, and the SKILL.md edit are additive (CHEAP); the C5 PreToolUse hook mutates the runtime guardrail surface and is the moderate element (it ships warn-mode-initial and is operator-flipped to enforce), so whole-release rollback = `git revert -m 1` of PR #1460 + delete the `v2.07` tag, after which `.autonomy-mode` returns to its pre-release state and the hook no longer fires. **Post-deploy operator actions:** run `update.sh` to deploy the C5 hook into the runtime; flip `.autonomy-mode` warn→enforce after shakedown. Dev Testing PASS-with-adjustments + QA 35/35 ACs PASS; Stage 9 Plan Review GO. **Combined Stage 12+13 close** — no separate Stage 12 chore PR landed a DEPLOYED row, so the RELEASE_LOG row is authored directly at VERIFIED (Phase A8 outcome-bound close; the binding is on the output set). The `daily-status.skill` package + content-hash sidecar are present on main (Check 7); runtime mirror sync via `./core/deploy/deploy.sh --deploy daily-status` is the operator/hub post-merge step. PR #1460 merged to main via merge commit `3d3dd508b5042e1bbde97e510ef14e00b7b750d5`, signed-annotated (ED25519) `v2.07` tag at the merge SHA. **Deferred from v2.07:** #1164 (C6 Teams connector) → backlog (unmet governed-Teams-MCP dependency, design in #1441); #296 (K4 onboarding) → milestone #52. Hook-scope observation: #1472. | #1460 | [notes/v2.07_RELEASE_NOTES.md](notes/v2.07_RELEASE_NOTES.md) |
| v2.08 | 11-artifact-lineage-and-generated-surface-governance | 2026-06-20 | **Artifact lineage graph + a recommend-only generated-surface lint** — a `novel`-class release that gives the platform's generated-artifact surface (the `08-Generated/` staging area + promoted folders) a lineage graph and a graph-integrity lint, so duplicate, orphaned, stale, or displaced AI-generated artifacts are surfaced for operator approval instead of silently accumulating. **#334** wires the horizontal-lineage scalar fields **`parent_artifact` / `sibling_topic` / `supersedes`** (with the `supersedes`/`superseded_by` inverse pair) into `core/schemas/frontmatter-schema.md` across **both Domain A (formal-baseline) and Domain C (synthesis)** so an A↔C chain resolves, adds the **lineage-vs-provenance distinction table** fixing the boundary (lineage = horizontal edges *between generated artifacts*; provenance = the *upstream human evidence* an artifact derives from — `source_inputs` scoped out as a separate provenance concept not yet in-schema), records the **field-vs-verb composition note** (the scalars compose against the 7 MVP `relationships[]` verbs — no new enum value, the 7-MVP-type constraint unchanged), and specifies **sidecar `.meta.yml` parity** plus a **WARN-not-BLOCK dangling-lineage rule**; syncs `core/standards/lifecycle-states-canonical.md §3.2` (the `DRAFT → REVIEWED → APPROVED → PROMOTED → ARCHIVED` Artifact Workflow restatement gains the three lineage fields, with the schema-home cross-pointer) and reconciles `core/standards/template-protocol.md` §9 (`topic` → `sibling_topic`). **#1165** authors the NEW **`operations/skills/artifact-lint/`** skill — **Autonomy Tier 1, recommend-only** — running **five graph-integrity checks (orphan / sibling-duplicate / stale-draft / displaced-content / version-chain)** that read the #334 lineage frontmatter and stage a single report to `08-Generated/artifact-lint-YYYY-MM-DD.md` with per-finding proposed action + reversibility tier + confidence + cited evidence, performing **no file moves and no deletes** (the operator approves every action); ships the **17-row work-plan taxonomy** (`references/work-plan-taxonomy.md` — Canonical-10 ≡ the Domain-C synthesis `artifact_type` set + Situational-7 ≡ the plan-type set + waterfall/cutover rows) the displaced-content Check 4 uses as the canonical-target-folder lookup; registered in `OPERATIONS_SKILLS`, shipping its own `.skill` package + `.sha256` content-hash baseline; the per-skill output contract added to `core/schemas/per-skill-output-contracts.md`. **FINDING-1 — dual state-read (the load-bearing input rule):** the state-keyed checks read **`artifact_state` primary → `lifecycle_state` fallback**, because `artifact_state` (the §3.2 Artifact Workflow enum stamped by `artifact-generator`) is absent from `frontmatter-schema.md` while `lifecycle_state` is the schema's REQUIRED field — keying on one silently mis-reads every artifact carrying the other. **FINDING-2 — 17-row taxonomy provenance:** no literal 17-row taxonomy survived in the canonical corpus (the original split card was deleted), so the taxonomy is an **evidence-grounded reconstruction operator-confirmed at Stage 9 Collective Review** as authoritative — Check 4 therefore treats a canonical-home mismatch as a firm (still recommend-only) finding. **Stage 5 caught a third #334 reconcile target** (`template-protocol.md`) beyond the two named at planning. `novel` class (plan-declared — the milestone carries no `release-class:` label), CHEAP reversibility / HIGH confidence (additive-only — a NEW skill + new optional schema fields + a synced restatement; whole-release rollback = `git revert -m 1` of PR #1468 + delete the `v2.08` tag; the new skill has no prior consumer, the optional lineage fields default-absent, and the schema additions have no external reader to unwind; re-run `./core/deploy/deploy.sh --deploy artifact-lint` post-revert to re-sync the skill mirror). **Combined Stage 12+13 close** — no separate Stage 12 chore PR landed a DEPLOYED row, so the RELEASE_LOG row is authored directly at VERIFIED (Phase A8 outcome-bound close; the binding is on the output set). The v2.08 branch merged current main in (incorporating #1461/#1474) before merge; the `artifact-lint.skill` package + content-hash sidecar are present on main (post-main-merge rebuild, Check 7). PR #1468 merged to main via merge commit `17955bc2cee02beac08606ac0f4bd56f1471ca74`, signed-annotated (ED25519) `v2.08` tag at the merge SHA. Forward-binding follow-up: `core/artifact-workflow-protocol.md` (planned co-located protocol doc; §3.2 pointer updates at its Stage 13). | #1468 | [notes/v2.08_RELEASE_NOTES.md](notes/v2.08_RELEASE_NOTES.md) |
| v2.06.1 | 36-ci-gate-trustworthiness-and-parallel-pr-safety (hotfix) | 2026-06-20 | **Deploy `--deploy` detection hotfix** — point release on the v2.06 milestone repairing two `deploy.sh --deploy` detection bugs that v2.06's new content-hash sidecar baselines exposed in the previously-untested `--deploy` path: (1) sidecar mis-enumeration (the `.skill` `sed` was unanchored, so the new `<name>.skill.sha256` content-hash baselines were mis-parsed) and (2) a BSD-`sed` alternation no-op in skill-name detection (the known R1). `cross-cutting` (same milestone), CHEAP reversibility / HIGH confidence — a deploy-script bug repair, additive to the gate apparatus shipped in v2.06; whole-release rollback = `git revert -m 1` of PR #1474. PR #1474 merged to main via merge commit `450b7e15d03774818fce6aa93590a605ea4e5517`, signed-annotated `v2.06.1` tag at the merge SHA. Fixes #1475. | #1474 | [notes/v2.06.1_RELEASE_NOTES.md](notes/v2.06.1_RELEASE_NOTES.md) |
| v2.06 | 36-ci-gate-trustworthiness-and-parallel-pr-safety | 2026-06-20 | **CI/deploy gate trustworthiness + parallel-PR safety** — a cross-cutting hardening of the platform's own automated-assertion gates so a green verdict *means* its claimed invariant holds, anchored on a new gate-efficacy contract; plus a hook-registry restructure that ends the parallel-PR cascade-conflict class. **Keystone #1101** authors `core/standards/gate-efficacy-standard.md` — the normative contract for the `deploy.sh --check` + `.github/workflows/*` gates (scoped apart from the LLM-judged pipeline stage-transition gates) — naming three requirements (**assert by content not proxy**, **self-declare required-vs-advisory**, **be auditable for false-confidence + coverage-gap**) plus a red-blocks posture rule; converts **Check 7 (package freshness)** from an `mtime` proxy to a **content-manifest-hash-vs-committed-baseline** assertion (the near-miss: a checkout-reset timestamp passing a stale package green), adds **required-vs-advisory declarations across the 14 workflow jobs**, and brings Checks 6/7/9/37/38 under the standard. **#673** adds a **Check-6 CI mirror** (`skill-canonical-structure-check.yml` invoking the single-sourced predicate `core/deploy/tools/check-canonical-structure.sh` shared with deploy-time Check 6) closing the run-context detection gap where a red-on-main structural breach sat undetected between deploys, bundled with a `daily-status` `references/` reconciliation (v2.01→v2.06). **#18 (ADR-030, Accepted)** breaks the ~32 KB `bypass-mode-readiness.md` monolith into **per-hook drop-in sources** + cross-cutting fragments with the canonical index **generated + committed** by `build-hook-registry.py`, adds a **completeness check (Check 37, advisory/warn-mode)** reconciling every `block-*.sh` against its declared owning doc — making the prior **5/7/9 three-way registry drift structurally impossible** — and an **index-freshness check (Check 38, always-enforce)**, extending Check 9's directory-walk; this kills the parallel-PR cascade-conflict class (concurrent hook-adding PRs no longer collide on one shared file, N=2 within one release). **#90** is a decision spike concluding the pre-commit localized-context layer is **NOT needed** (DC1/DC6 already gated pre-merge; residual stable), closed with a residual-risk register, not code. `cross-cutting` class, CHEAP reversibility / HIGH confidence (additive standard + checks + a documentation restructure into a generated index — whole-release rollback = `git revert -m 1`; the report-only Check 37 blocks nothing and the index regenerates deterministically). A follow-up **v2.06.1** (PR #1474, fixing #1475) repaired two `deploy.sh --deploy` detection bugs the new content-hash baselines exposed in the previously-untested `--deploy` path (sidecar mis-enumeration via an unanchored `.skill` `sed`; a BSD-`sed` alternation no-op — the known R1). **Combined Stage 12+13 close** — no separate Stage 12 chore PR landed a DEPLOYED row, so the RELEASE_LOG rows are authored directly at VERIFIED (Phase A8 outcome-bound close). PR #1461 merged to main via merge commit `a74f814baa736494231fd01bc06fa1c9ea03d32d`, signed-annotated `v2.06` tag at the merge SHA; the v2.06.1 hotfix tag points at `450b7e15d03774818fce6aa93590a605ea4e5517`. Follow-ups filed (reference): #1471 · #1476 · #1477. | #1461 | [notes/v2.06_RELEASE_NOTES.md](notes/v2.06_RELEASE_NOTES.md) |
| v2.05 | 35-agent-discipline-codification | 2026-06-19 | **Agent-discipline codification + the memory↔corpus SSOT boundary** — a cross-cutting cluster of 10 issues that promotes working agreements from operator memory into the durable corpus and establishes the contract for how knowledge graduates. **Keystone #530** ships `core/ADRs/ADR-029` (the *Memory SSOT model*, framed on a four-type memory architecture — Work / Knowledge / People / Learning — scoped to the Knowledge↔corpus cut), `core/disciplines/knowledge-architecture.md` §6 (SSOT assignment + no-shadow-SSOT invariant + ENCODE→ARCHIVE→VERIFY-CORPUS→EVICT lifecycle + three drift classes), a "Single-source-of-truth for knowledge" Universal Preference in `CLAUDE.md`, a warn-mode `deploy.sh` Check 36, and a drift-audit how-to. **#527 / #531** encode untied disciplines (read-before-edit, in-place bias, flag≠consent, +) and operator-flagged toolkit knowledge (engagement discipline, worktree boundary, system/toolchain references) across `git-workflow.md`, `depersonalization-spec.md`, `reversibility-protocol.md`, `initiative-roadmap-framework.md`, `pmo-skill-editor`, and a new `toolchain-operational-reference.md`. **#63** codifies AskUserQuestion timing; **#58 / #502** add failure-mode entries (chip-arithmetic-without-verification; perpetuate-structure-instead-of-redesign); **#426 / #427 / #546** codify repo-integrity authoring guidance, intake assumption-tag downstream resolution, and the advisory Solutioning pre-read convention; **#132** refreshes the CLAUDE.md tree. `cross-cutting` class, CHEAP reversibility / HIGH confidence (additive doc/governance only — whole-release rollback = `git revert -m 1`). Re-versioned v2.03→**v2.05** (mainline advanced mid-window) and the keystone ADR renumbered 028→**029** (main claimed 028). The 3 memory-tied References (#527/#530/#531) closed manually at Stage 13; their staging memories retired operator-side per ADR-029. | #1409 | [notes/v2.05_RELEASE_NOTES.md](notes/v2.05_RELEASE_NOTES.md) |
| v2.03 | 03-ROLE-factory-and-pilots | 2026-06-18 | **Role-skill factory foundation + first two pilots** — the role-Specialist factory exists end-to-end and ships its first two products; 2 skill-build cards (Phase 0 foundation + Phase 1 pilots), no application logic beyond the skills themselves. **#186 (Phase 0 — factory foundation)** wires a **feeding-document consumption** path into `release/skills/pmo-skill-refiner/SKILL.md` as the pre-Interview input branch of Mode 2 (Create New, authored via `pmo-skill-editor` discipline on the migrated skill), authors the 14-section format reference `release/skills/pmo-skill-refiner/references/pmo-feeding-document-v02.md`, authors the **5 `operations/skills/_shared/*.md`** building-block files (`behavioral-markers`, `anti-pattern-catalog`, `five-model-variations`, `deployment-strategies`, `lifecycle-gates`) every later role skill consumes, and adds a **fail-CLOSED `core/deploy/deploy.sh` Check 5(a) skip** as an exact-match `== _shared` guard (NOT a fail-open `_*` wildcard) + convention comment so the new `_`-prefixed sibling dir does not trip skill-roster-drift while a future stray `_`-prefixed dir still FAILs unless explicitly allowlisted. **#185 (Phase 1 — pilots + format lock)** builds the 2 pilot skills via `pmo-skill-refiner` consuming their feeding docs — `operations/skills/pmo-technical-program-manager/SKILL.md` (composes `pmo-technical-analyst` + `delivery-engine`) and `operations/skills/pmo-program-coordinator/SKILL.md` (composes `tracker-manager` + `daily-status`) — each composing by invocation (not re-implementation), carrying the `skill_discipline_migrated_v10_2` flag, registered in `OPERATIONS_SKILLS`, shipping its own `.skill` package; then **locks the feeding-document format** (a post-Phase-1 "changes require a governance update" note added to `pmo-feeding-document-v02.md`). Strict serialization **#186 → #185** (format-lock gate — #185 cannot start until #186's foundation exists). **Scope change: #450 (`pmo-architect`) DEFERRED to `04-ROLE-delivery-coverage`** — its compose-the-mode seam converges with a stub until `pmo-principal-engineer` (#1116) ships in milestone 04; decision recorded on #450. `novel` class (Standard engagement / Deep Stage-9 review / 30-day outcome window), CHEAP reversibility / HIGH confidence (additive-only — whole-release rollback = `git revert -m 1` of the release PR; the 5 `_shared/` files have no prior consumer to unwind, the deploy.sh guard reverts cleanly; re-run `./core/deploy/deploy.sh --deploy` post-revert to re-sync the skill mirrors). **Combined Stage 12+13 close** — no separate Stage 12 chore PR landed a DEPLOYED row, so the RELEASE_LOG row is authored directly at VERIFIED (Phase A8 outcome-bound close; the binding is on the output set). The 3 `.skill` packages (`pmo-skill-refiner` rebuilt + the 2 pilots built) were confirmed fresh on main at Stage 13 (a Stage-13 rebuild reproduced byte-identical content — no package re-commit). The 2 cards are marked closed at Stage 13 (PR #1280 used `References #186, #185`; not auto-closed from the release PR). PR #1280 merged to main via merge commit `cc094b4409825ed95f2e1dd1d60f7b5b4e9aa4be`, signed-annotated `v2.03` tag at the merge SHA. 30-day Stage 13 outcome window per the `novel` class. | #1280 | [notes/v2.03_RELEASE_NOTES.md](notes/v2.03_RELEASE_NOTES.md) |
| v2.02 | 61-bundling-capacity-and-sizing-gates | 2026-06-18 | **Bundling capacity + sizing/parse-rate gates** — the pipeline's Bundle stage enforces capacity and sizing as gates, not guidance; 4 governance/spec/schema cards (no application logic). **#290 (capacity TARGET)** adds a **risk-weighted release-capacity model** to `bundle-composition-doctrine.md §3 Step 5` where a higher-ceremony release's raw story points count for more against the same band (`effective_pts = round_half_up(Σpts × class_weight)`), **consolidates** the previously-scattered sizing guidance (item-count vs point-band) into that one cross-referenced source, **pins the rounding mode** `round_half_up` at the definitional home, and ships a `release_class_capacity_weights` inline table `{routine 1.0 / novel 1.15 / cross-cutting 1.3 / hotfix 0.9}` as explicitly-labelled MEDIUM-confidence `[CALIBRATE-AFTER-3]` seeds in `platform-config.toml.template` (+ config-schema doc + Stage-3 A4 cross-ref + `deploy.sh` Check 33 allowlist extension so the field is gated). The establishing **ADR-027 (Accepted)** records the risk-weight-keys-on-Release-Class decision (lowest-blast-radius canonicalization over a net-new per-issue Tier field). **#294 (per-milestone size gate — G3-15)** asserts `effective_pts ≤ band-upper` at a numbered gate in `gate-criteria-spec.md` and routes an oversize milestone to the split / tight-merge disposition (non-dead-end), with the enforcement cross-cite added to doctrine §3 Step 5 (serialized after #290). **#281 (RELEASE_LOG velocity instrument)** adds an additive velocity convention capturing per-release planned-vs-delivered points, files-changed, and allocation actuals alongside the already-shipped `Cycle-Time:` field — codified in NET-NEW `release/references/standards/release-velocity-tracking.md` (field schema + label→work-class map + N/A semantics + N=3 recalibration linkage) with the producing tool `release/tools/compute-release-velocity.sh`; the data instrument that feeds #290's recalibration. **#293 (Mode-A parse-rate gate — G3-14)** holds the `release-planner` Mode A dependency-graph parse rate to a **≥90% floor** against the conformant-bundleable denominator before Stage 3/4 may pass (documented operator override), with the Mode A AC cross-ref added to `release-planner` SKILL.md. `gate-criteria-spec.md` schema bumped v1.11 → v1.12 additive; warn-then-enforce gates with `[CALIBRATE-AFTER-3]` thresholds; gate-ID set {#293 → G3-14, #294 → G3-15, #292 → G3-16 reserved}. `novel` class (new RELEASE_LOG velocity instrument + two new gate criteria + multiple D-class planning decisions), CHEAP reversibility / HIGH confidence (additive-only — whole-release rollback = `git revert -m 1` of the release PR; every addition ships a default and no existing field/row/enum is mutated; ADR-027 + `release-velocity-tracking.md` have no external reader to unwind; re-run `./core/deploy/deploy.sh --deploy release-planner` post-revert to re-sync the skill mirror). **Combined Stage 12+13 close** — no separate Stage 12 chore PR landed a DEPLOYED row, so the RELEASE_LOG row is authored directly at VERIFIED (Phase A8 outcome-bound close; the binding is on the output set). The `release-planner.skill` package is rebuilt at Stage 13 (Check 7 package-freshness, resolving the deferred staleness). The 4 cards are marked closed at Stage 13 (not auto-closed from the release PR). PR #1278 merged to main via merge commit `a081fec36b6e5551df8564d7fa2457e27d8f00c5`, signed-annotated `v2.02` tag at the merge SHA. 30-day Stage 13 outcome window per the `novel` class. | #1278 | [notes/v2.02_RELEASE_NOTES.md](notes/v2.02_RELEASE_NOTES.md) |
| v2.01 | 02-FNH-est-lifecycle-status-hardening | 2026-06-16 | **Skill-function hardening (estimation / lifecycle / status)** — seven existing function-skills deepened across delivery-engine, ppm-agent, tracker-manager, weekly-status-rollup, daily-status, pmo-process-designer, and pmo-technical-analyst; 12 EXTEND-class stories, no new skills. The whole release is **enforcement-wiring, not logic-invention** — every story adds discipline-enforcement to skills whose reference substrate (estimation-standards, lifecycle-stages, gate-definitions, metric-registry, watermelon-detection, intake-governance, escalation-thresholds) was already built. **delivery-engine** (5-card serial chain on one SKILL.md) gains **estimation enforcement** establishing buffer/variance vocabulary (#264), **15-stage lifecycle tracking** (#265), **10-gate lifecycle enforcement** with PASS/CONDITIONAL/FAIL verdicts sitting between the stages (#266), a **tech-debt capacity floor + aging + rework-rate** with a "not computable" fallback when the rework-input source is absent (NET-NEW `references/tech-debt-capacity.md`, #366), and **Fowler-quadrant classification + Cost of Delay** ranking against that floor with a lexicographic sort (NET-NEW `references/tech-debt-classification.md`, registers a `framework-catalog.md` row, #180). **ppm-agent** (2-card serial chain) gains **intake governance + WSJF scoring** (NET-NEW `references/ppm-intake-governance.md`, renamed to avoid the intake-desk collision; WSJF consumed by reference, #253) and **org-model detection** (from `delivery_approach`) **+ stale-RAID escalation** wiring the existing `escalation-thresholds.md` (#254). **tracker-manager** gains **RAID deduplication** (≥0.70 similarity) **+ cascade detection** (#251). **weekly-status-rollup** gains **watermelon detection** consumed **via-public-api** from the canonical 8-signal `watermelon-detection.md` (owned by `pmo-qa-auditor` — referenced, not forked) **+ metric governance**, with an optional `investment_class` project-template field (absent → "Unclassified" coverage gap) (#256). **daily-status** gains **formula-driven RAG + buffer tracking**, wired to existing registries with no new `references/` dir (SKILL-only, #260). **pmo-process-designer** gains **INVEST scoring + Given-When-Then + NFR taxonomy** (ISO/IEC 25010) quality enforcement (#263). **pmo-technical-analyst** gains **ADR-immutability + FDD quality scoring** (4.0/3.0 thresholds) **+ Accelerate/DORA metrics** (#259). A supporting **ADR-028** (formerly ADR-024) records the #256 via-public-api-not-fork decision (operations consume a core safety-control by role-name rather than copying it), reversing the Stage-4 local-copy recommendation. `novel` class (≥1 new reference doc + ≥1 D-class decision; cross-cutting count-gates not met), CHEAP reversibility / HIGH confidence (additive-only — whole-release rollback = `git revert -m 1` of the release PR; the 3 NET-NEW reference docs + ADR-028 have no external reader to unwind; re-run `./core/deploy/deploy.sh --deploy` post-revert to re-sync the 7 skill mirrors). DT/QA: 12/12 PASS, 72/72 ACs. The 12 cards are marked closed at Stage 13 (not auto-closed from the release PR). PR #1240 merged to main via merge commit `97b66e12dd82ecb2131614577d9acf9314ba206e`, signed-annotated `v2.01` tag at the merge SHA. 30-day Stage 13 outcome window per the `novel` class. | #1240 | [notes/v2.01_RELEASE_NOTES.md](notes/v2.01_RELEASE_NOTES.md) |
| v2.00 | 01-FNH-qa-change-release-hardening | 2026-06-15 | **Skill-function hardening** — five existing function-skills deepened across change-management, release-executor, pmo-qa-auditor, artifact-generator, and release-planner; 11 build cards, all EXTEND except the release-planner RECONCILE card (no new skills). **change-management** gains a named **Mode G (Adoption Tracking)** surfacing ADKAR barrier-assessment + training-timing validation (#1127), champion-ratio / sponsor-coverage / valley-of-despair prep (#1128), and change-fatigue + outcome measurement (#1129) — all consuming the single-source change-methodology framework by reference (no duplicated scale/rule); and a **Mode D (Hypercare)** upgrade — a risk-classification step with a stable 3-tier set (HC-T1 Critical / HC-T2 Elevated / HC-T3 Routine, scored Severity × Likelihood × Blast-Radius; Severity+Likelihood adopted by reference from `delivery-engine/references/raid-templates.md`, only Blast-Radius net-new) (#1130) plus the tracking+reporting half on top of it: each hypercare incident recorded as an existing-prefix RAID entry with a met/breached verdict against the tier's committed SLA, a per-tier compliance report, and an **open SLA breach as a hard, non-waivable Mode-D exit gate** (#1131). **release-executor** gains a canonical 3-tier **Quality-Gate Ladder** (T1 schema validation → T2 cross-reference integrity → T3 stakeholder approval) that fires in order and short-circuits on the first hard fail, wired at both execution entry points (the git-native PR-merge pre-execution surface and the Cowork snapshot-apply path) from one source and reusing existing instruments (`lint_release_corpus.py --check schema` + `check-doc-links.py`; an unverifiable surface is a FAIL) (#244); a 3-cycle **progressive-rollout** model (shadow → warn → enforce) wrapping the ladder via a per-gate `rollout-cycle` attribute (default + fail-safe `enforce`; per-rule append-only outcome-log; operator-gated Advance Checklist; only an `enforce` gate short-circuits) (#245); and a **Mode D (Close Release)** documented build-out (not a rebuild) — an explicit issue-closure audit (non-zero open-issue count = blocking finding), carry-forward / deferred-item tracking as a named Step-6 output reported every close, and a new close-out checklist reference doc (#212). **pmo-qa-auditor** (P1) gains an **8-detector failure-mode battery** (automation complacency / faceless PMO / echo chamber / quality drift / SPOF / breadth burnout / AI hallucination / trust erosion) authored to the failure-mode 5-field template with corpus-precedent thresholds, plus a **RACI validation gate (G9)**; the auditor's own Domain-Specific Failure Modes section is untouched and the mode count stays 5 (#248). **artifact-generator** gains **lifecycle state tracking + zombie detection** adopting the platform-canonical Artifact Workflow vocabulary — the field **`artifact_state`** (replacing `status`) with the canonical 5 states DRAFT → REVIEWED → APPROVED → PROMOTED → ARCHIVED stamped DRAFT on emit, a documentation-debt register listing stale (>30-day, non-`ARCHIVED`) artifacts with recommended actions, and `references/lifecycle-states.md` re-specced as an application layer over the canonical state machine (the contained field rename verified to break no external reader) (#252). **release-planner** gains **typed artifact linking** — each in-release plan edge labelled with one of four §Category-4 relationship types (**GENERATES / DEPENDS_ON / BLOCKS / SUPERSEDES**) adopted verbatim from the ADR-018-frozen `frontmatter-schema.md` vocabulary, added as a **second, independent edge attribute** that the CPM longest-path math never reads (provably additive), with `bundle-issues-parser.py --json` read-only-extended by an additive `artifact_relationships` field (#246). `novel` class (≥1 new reference doc + ≥1 D-class decision; cross-cutting count-gates not met), MODERATE reversibility / HIGH confidence (whole-release rollback = `git revert -m 1` of the release PR; the one behavior change to unwind is the artifact-generator `artifact_state` field default, contained — no other skill reads the value; re-run `./core/deploy/deploy.sh --deploy` post-revert to re-sync the 5 skill mirrors). Key D-decision **D-246-Blocker resolved INCLUDE** — #246's "Blocked by #409" edge was reconciled-stale (the card needs only the already-frozen 7-type relationship vocabulary; #409 stays an open epic that does not close this release). **Version mapping:** the plan was drafted against the v3.x line (`.version` v3.19, latest tag v3.20) and **re-versioned to v2.00 at Engineering Commit 0** per the repo re-versioning lineage; `.version` reconciled v3.19 → v2.00 in the Stage 13 chore PR. The 11 cards are marked closed at Stage 13 (the release plan phrases per-card closure SAFE; not auto-closed from the release PR). PR #1200 merged to main via merge commit `42ee22cf1279ffff1c0172d95de092a159025baa`, signed-annotated `v2.00` tag at the merge SHA. 30-day Stage 13 outcome window per the `novel` class. | #1200 | [notes/v2.00_RELEASE_NOTES.md](notes/v2.00_RELEASE_NOTES.md) |
| v1.24 | pipeline-detection-bash-debt | 2026-06-14 | The Stage-2 Triage A6.5 management-task detection bash is corrected so its **automated backlog-hygiene checks stop silently returning zero** — the orphaned-dependency check (Pattern 1b) and the P1-blocked-by-lower check (Pattern 2a) now return correct results — plus a stale cross-spec citation in `release-process.md` is fixed. Resolves the three v1.02-triage carry-forward findings on issue **#309**. **CF-2 (awk silent-zero):** the detection table in `release/references/pipeline/stage-02-triage.md` used the range form `awk '/^### Dependencies/,/^### /'` (and the parallel `/^### Priority/,/^### /`), which is non-functional — the start regex is itself matched by the end regex `^### `, so the range collapses to the heading line and the body is dropped (the downstream `grep` then returns nothing). All **3 affected instances** (Pattern 1b orphaned-dependency extraction; Pattern 2a P1-blocked-by-lower dependency extraction; Pattern 2a blocker-priority extraction) replaced with the robust `awk 'BEGIN{flag=0} /^### <heading>/{flag=1;next} /^### /{flag=0} flag'` form (table-cell pipe-escaping preserved; Pattern 4a — a `grep -cE`, no awk — correctly untouched, correcting the issue body's stale "1b/2a/4a" inventory). **CF-3 (citation drift):** the Stage-2 Triage-cadence paragraph in `release/governance/release-process.md` cited the sister spec's "Stale issues" detection at `Phase A4 (ii) detection-pattern (1a)` — stale; the pattern lives at `Phase A6.5 Pattern (1a)` (A4 there is the unrelated feasibility quick-check). The bilateral P1-SLA contract is intact; only the anchor was wrong → corrected to `Phase A6.5 Pattern (1a)`. **CF-1 (yq toolchain) DE-SCOPED:** the `yq` dependency targeted the **G3-13 Roadmap-Cascade Validation** detection bash in `gate-criteria-spec.md`, which ADR-012 (roadmap-instance-descope, Accepted 2026-06-02) already retired — no live detection bash remains to fix, so CF-1 was closed at verification, not engineered. **3 CI-gate remediations rode along** on the touched lines: operator-handle tokenization, an `allow-link` marker (per-file reference-durability), and a `stage-02-triage.md` entry in `core/hooks/reference-durability-allowlist.txt` so the issue-ref detector does not false-positive on two pre-existing `#`-shaped patterns the fix re-surfaced (the `#5-process` anchor fragment + the `#[0-9]+` grep regex — not net-new fragile refs; mirrors the sibling stage-03/04/12 allowlist entries). `routine` class, CHEAP reversibility (single `git revert -m 1` of the release PR — corrective doc/spec-text edits only; no data migration, no consumer state; deploy no-op means nothing to re-sync post-revert). **Re-versioned planned v1.21 → v1.24** per the #769 version-collision class — `v1.21`/`v1.22`/`v1.23` were all claimed by concurrent releases (governance-as-code-quality-gates, deploy-toolchain-defect-cleanup, pmo-skill-reference-substrate) between planning and execute, so the release landed at next-free **v1.24**; the branch name (`release/v1.21-pipeline-detection-bash-debt`) and the build-commit messages retain the as-authored `v1.21` label (a cosmetic build-record artifact), every durable artifact ships v1.24. This release's Stage 4 plan was authored as a GitHub sub-task #1057 comment (no in-repo `*_RELEASE_PLAN.md` file). #309 stayed OPEN at merge (PR #1068 used `References #309` in its Issue References block — `closingIssuesReferences` empty — so #309 is closed manually at Stage 13). PR #1068 merged to main via merge commit `10977af642da65c1bbb2b35332ff3cba70cf5bb4`, signed-annotated `v1.24` tag at the merge SHA. 30-day Stage 13 outcome window per the `routine` class. | #1068 | [notes/v1.24_RELEASE_NOTES.md](notes/v1.24_RELEASE_NOTES.md) |
| v1.23 | pmo-skill-reference-substrate | 2026-06-14 | The change-management skill becomes a **pluggable methodology toolkit** — ADKAR consolidated as the single source of truth plus four newly-codified methodologies (`kotter-8-step.md`, `lewin-3-stage.md`, `bridges-transition.md`, `mckinsey-7s.md`) and a `methodology-selection.md` selector wired into SKILL.md Step 2.5 — and two more reference standards ship alongside (an intake-governance standard for `intake-desk` and a cross-pipeline sub-task methodology reference). **#267 (ADKAR SSOT):** `adkar-framework.md` authored as the single authoritative ADKAR scale + barrier rule, consolidating the two divergent sibling definitions (`impact-assessment.md` + `readiness-checklist.md` now reference it; one definition remains corpus-wide). **#1011/#1012/#1013/#1014:** Kotter / Lewin / Bridges / McKinsey-7S codified as structurally-consistent methodology references (each registered in `framework-catalog.md` — canonical_doc populated; ADKAR/Kotter/Bridges `evolving`/12mo, Lewin/7-S `stable`/36mo). **#1015 (keystone):** `methodology-selection.md` selector (axes = unit-of-change primary + dominant-risk tie-breaker; promotes the `impact-assessment.md` seed table to one authoritative selection view) + a SKILL.md Step 2.5 methodology-selection step. **#268 (intake-governance):** `intake-governance.md` (business-case tiering, WSJF formula, intake SLAs, 6-type demand taxonomy, anti-pattern reuse of the `gate-checklists.md` `>95%` threshold) for `intake-desk`, + the framework-catalog Cost-of-Delay `canonical_doc` pointer. **#224 (sub-task methodology):** `sub-task-methodology.md` cross-pipeline best-practices standard + a `release-process.md` Stage-6 cross-ref. `novel` class, CHEAP reversibility (additive K1 docs + two skill-version bumps to v1.23 + catalog rows; no prior consumers). **Re-versioned v1.21 → v1.22 → v1.23** under concurrent-release pressure (governance-as-code took v1.21, deploy-toolchain took v1.22); the release re-merged current `main` each time with no content conflict (only the version-named plan file collided). Scope grew mid-build from 3 to 8 issues by operator directive (the methodology suite). #224 auto-closed at merge; the other seven parents closed manually at Stage 13 with per-AC verification (the PR body's comma-list `Closes` closed only the first reference). PR #1043 merged to main via `23cafa4be9408be7d4320d458c780f1df0e0a08c`, signed-annotated `v1.23` tag at the merge SHA (ED25519 signature valid; zero signing bypasses). Consolidated independent Stage 7+8 review GO (all 8 ACs PASS, worked examples re-derived, three minor findings swept). 30-day Stage 13 outcome window per the `novel` class. | #1043 | [notes/v1.23_RELEASE_NOTES.md](notes/v1.23_RELEASE_NOTES.md) |
| v1.22 | deploy-toolchain-defect-cleanup | 2026-06-14 | The platform's deploy toolchain runs **deterministically**: `core/deploy/deploy.sh` and its governance checks no longer self-inflict a bash arithmetic crash, silently swallow a read-only install failure, clip a quoted path at a `#`, or skip `--report` parity, and `release/tools/blast-radius.sh` no longer crashes on non-ASCII input. **Eleven deploy-toolchain defects + one folded-in same-class defect**, dominated by one serialization surface — **7 of the 11 issues + the fold-in touch `core/deploy/deploy.sh`** on mutually-disjoint check regions, serialized by merge order under D-C SINGLE. Before this release, `deploy.sh --check` could exit on a `syntax error in expression` from a `grep -c … \|\| echo 0` double-emit (a `0\n0` two-line capture), the `references/` mirror cleanup swallowed a read-only-orphan failure with `2>/dev/null \|\| true` then hard-failed opaquely on the later `cp -R`, the `cowork_install_path` reader clipped a quoted path at a literal `#`, and several governance checks lacked `--report` parity or carried a stale env-var name. **#76 (Check-19 arithmetic double-emit):** the Check-19 pipeline-event-log capture replaced with the robust `… \|\| true); var=${var:-0}` form. **#1058 (Check-28/29 double-emit, folded in at re-baseline):** the identical defect class at the Check-28 (`_has_section`/`_has_field`) + Check-29 (`_has_h2`/`_has_xref`) sites, re-applied as a **signed** commit after the original unsigned foreign commit from a concurrent session was dropped on re-baseline. **#88 (references/ read-only self-heal):** the silent swallow replaced with a `remove_mirror_subtree` helper that self-heals a read-only Cowork-install orphan and surfaces an actionable RO-perms error + remediation. **#332 (`cowork_install_path` `#`-clip):** the `[^"#]+` capture pattern at the reader sites + the Check-8 reader fixed so a quoted `#`-bearing path passes through whole. **#111 (Check-17 env-var rename):** `C15_THRESHOLD_OVERRIDE_FILE` → `C17_*` (post-renumber namespace). **#104 (Check-18 `cmd_report` parity):** a Check-18 PASS/FAIL row added to `cmd_report` for Stage-13 evidence. **#26 (Check 35 mode-invocation-drift + Mode Provenance):** a brand-new **Check 35** appended after the shipped Check 34 (multi-mode SKILL.md mode-enum recognizability; reuses the existing `flag_warn_or_issue` / `DEPLOY_CHECK_MODE` warn-mode machinery — no new helper/flag/mode file) + its `cmd_report` parity block + a `### Mode Provenance` block in the Procedure 3 Spoke Template of `release/references/how-to/hub-spoke-bridge.md`. **#92 (blast-radius.sh locale):** `LC_ALL=C` pinned on the dependency-graph `sort` so a non-ASCII-bearing file no longer triggers `sort: Illegal byte sequence` + broken pipe. **#331 (four LOW findings):** `update.sh` EX_NOCHANGE Phase-5-redeploy, `validate-install.sh` A9 deploy-root override, `notify-version-skew.sh` dangling xref, `deploy.sh` DEPLOY_ROOT comment. `routine` class, CHEAP reversibility (single `git revert -m 1` of the release PR — all changes additive/corrective; the new Check 35 ships warn-mode-first so a false positive warns rather than breaking a deploy; no data migration, no consumer state to unwind; deploy no-op means nothing to re-sync post-revert). **Nine issues #88 / #76 / #332 / #111 / #104 / #26 / #331 / #92 / #1058 auto-closed at the release-PR merge**; two stale-against-the-tree issues #343 (skill-deployment session-UUID — both literal UUIDs absent tree-wide, `skill-deployment.md` fully tokenized to `[SESSION_UUID]`, ADR-013 re-pointed Check 8) and #122 (file-router Check 10 — last non-merge commit `be1db81` already carries a Skill-Editor-Audit-Trail trailer, so Check 10 already passes) were evidence-closed at Stage 13 with no code change; #106 (status-label) stays OPEN as the operator/hub's operational follow-up. PR #1081 merged to main via merge commit `edb99eb6313f6d991edc7ee0c231c918654c94fb`, signed-annotated `v1.22` tag at the merge SHA (ED25519 signature valid; zero signing bypasses); deploy no-op (no `skills/` / `packages/` / harness paths). **Re-baselined v1.20 → v1.21 → v1.22:** authored as v1.20, re-versioned to v1.21 when sior-escalation-discipline (v1.19) + health-and-raid-determinism (v1.20) consumed the tracked numbers, then to v1.22 when governance-as-code-quality-gates took v1.21 *and* its `Check 34` — so this release's mode-drift check is appended as **Check 35** after the shipped Check 34 (in-function locals renamed `c34_*`→`c35_*`). Unlike the v1.18→v1.20 / v1.20→v1.21 legacy-branch-name precedent, a fresh `release/v1.22-deploy-toolchain-defect-cleanup` branch was cut off current `main` so the version identity, branch, plan file, tag, and RELEASE_LOG row all agree on v1.22. 30-day Stage 13 outcome window per the `routine` class. | #1081 | [notes/v1.22_RELEASE_NOTES.md](notes/v1.22_RELEASE_NOTES.md) |
| v1.21 | governance-as-code-quality-gates | 2026-06-14 | The platform's automated quality-gate surface gains two detection capabilities: a template↔schema **contract-fidelity** check (`deploy.sh` Check 34, #318) and an L5 **cascade-completeness** gate (`pmo-qa-auditor` G8, #79). Before this release, a canonical template could silently drift from the schema governing its instances (Check 13 covered canonical↔mirror copy-fidelity only — no contract-fidelity axis), and a rename/restructure that swept most but not all OLD-value occurrences had no audit-time detector across the whole changed-file set (L1–L4 prevent at authoring/review time within the change matrix; L5 was deferred). **#318 (Check 34 — template↔schema conformance):** `core/deploy/deploy.sh` gains a `TEMPLATE_SCHEMA_MAP` manifest + a Check 34 block asserting each schema-bearing canonical template carries every section its governing schema mandates — the third fidelity axis over Check 13's copy-fidelity (the manifest mechanism mirrors the proven `TEMPLATE_SYNC_MAP` pattern; the in-template-frontmatter alternative is governance-barred by `template-storage.md` §3.4). Scope is opt-in by manifest presence — a template the manifest does not name is a structural skip, never a false failure (satisfying the no-false-positive AC). `operations/templates/open-meetings-tracker-template.md` was the first divergence caught (it shipped `ACTIVE` / `COMPLETED` against `tracker-schemas.md` Tracker 3's `Upcoming Meetings` / `Recently Completed` / `Recurring Cadences` structure) and was reconciled in the same release; the new axis is documented in `core/standards/template-storage.md` §3.5b. **#79 (G8 — L5 cascade-completeness gate):** `core/skills/pmo-qa-auditor/SKILL.md` gains a G8 check that, when the output under audit is a Stage 5 spec carrying a `### Cascade-Sweep` block, re-runs each declared sweep against the changed-file set on the release branch and verifies the swept-declaration table enumerates every OLD-value occurrence found — the L5 audit-time defense-in-depth complement to the cascade-omission failure mode's existing L1–L4 prevention surfaces, with the net-new supporting reference `core/skills/pmo-qa-auditor/references/cascade-completeness-detection.md` and the cascade-omission anti-pattern in `core/specs/failure-mode-standard.md` cross-referenced to name L5 as the automated-detection surface. The SKILL.md edit routed through `pmo-skill-editor` Mode A (Gate 2 hook discipline preserved). Both gates reuse the existing `flag_warn_or_issue` / `DEPLOY_CHECK_MODE` / `deploy-check.mode` warn-mode machinery and ship warn-mode-first per the established shakedown precedent. `novel` class (two D-class plan decisions + ADR-class Stage 5 design choices on deploy-time-vs-audit-time gate placement and the schema-scoping mechanism), CHEAP reversibility (per-commit `git revert`; both gates ship warn-mode-first so a false positive warns rather than breaking a deploy; no data migration, no consumer state to unwind). Both issues #318 + #79 auto-closed at the release-PR merge; PR #1040 merged to main via merge commit `62595481ef14bb4f37dd841809f5ca8b1568ce45`, signed-annotated `v1.21` tag at the merge SHA (ED25519 signature valid; zero signing bypasses). The `pmo-qa-auditor.skill` package was rebuilt at Stage 13 (Check 7 package-freshness) to carry the G8 check + new reference; the runtime skill-mirror sync (`./core/deploy/deploy.sh --deploy pmo-qa-auditor`) is flagged for the operator/hub to run from the primary post-merge. Version confirmed **v1.21** at the Stage 12/13 close — the version was free (no concurrent collision, unlike the v1.18→v1.20 chain), so no re-version was needed. 30-day Stage 13 outcome window per the `novel` class. | #1040 | [notes/v1.21_RELEASE_NOTES.md](notes/v1.21_RELEASE_NOTES.md) |
| parallel-launch-quota-budget-gate (version-less) | parallel-launch-quota-budget-gate | 2026-06-14 | The release pipeline becomes quota-aware: parallel spoke/subagent waves are estimated and re-validated against the operator's per-account usage-window envelope before launching, so a batch never half-completes from cumulative usage-window exhaustion. Before this release, parallel batches launched blind to the ~5-hour cumulative usage window and silently failed partway through once it was drawn down (the v11.27 9-spoke first-failure, 2026-05-24). **#24 (foundation):** authors the canonical `### Per-Account Usage Window Constraint` subsection in `release/references/how-to/hub-spoke-bridge.md § Spoke Launch Mechanisms` (renamed from the earlier "Quota Constraint" framing) — the cumulative-draw failure mode, a cumulative-vs-rate-limit contrast, the load-bearing mitigation set (pre-flight check / quota-budgeting / window-aware timing / serialize-on-failure / reduce-per-spoke), in-prompt `sleep` stagger demoted to a labeled secondary rate-limit-only defense (never the usage-window mitigation), and the v11.27 tail-of-window empirical anchor — plus a Stage-5-row note in the Procedure 2 Step 5 Parallelism Rules table and a Stage-5 persona anti-pattern in `release/references/specs/release-personas.md`. **#23 (active gate):** builds the dual-checkpoint enforcement — a plan-time terminal Checkpoint A (Phase A6 Quota-Budget Pre-Check, A2-sourced parallel-eligible count) in `release/references/pipeline/stage-04-planning.md` emitting a `### Quota Budget` plan section with a PASS / WARN / FAIL routing tree, and a runtime Checkpoint B (Procedure 2 Step 5.5 + § Spoke Launch Mechanisms launch-paragraph hook) in `hub-spoke-bridge.md` rendering a PROCEED / SERIALIZE / DEFER / REDUCE-scope verdict before each parallel launch, with a deviation-logged one-batch DEFER override exit (no deadlock, no reflexive-override training) and a Step 5 orthogonality note (parallel-safe = coordination, not usage-window; the two gates compose). The new `release/references/standards/quota-budget-protocol.md` is the protocol/schema home; `release/references/standards/pipeline-event-log-schema.md` § 3 adds the `spoke-launch` / `quota-reservation` event type carrying `tokens_used:` (reconciled to the real `payload`-convention surface — no fictional event), with `release/tools/append-pipeline-event.sh` carrying the data-driven enum + static fallback mirror (`--self-test` PASS, 11 event types). `release/ADRs/ADR-026-spoke-launch-quota-reservation-telemetry-event.md` (+ README index row, renumbered from a Stage-5 ADR-024 draft to dodge an in-flight collision) records the decision. `novel` class, CHEAP reversibility (single `git revert -m 1` of the release PR — additive text-only protocol change over the prior "launch all N" behavior; no data migration, no consumer state to unwind; deploy no-op means nothing to re-sync post-revert). **Issues #23 + #24 stayed OPEN at merge** (PR #911 used `References #23, #24` — `closingIssuesReferences` empty — so the two issues are closed manually at Stage 13 with per-AC verification evidence; Stage 8 QA ACCEPT 11/11 on #23 sub-task #787 and 6/6 on #24 sub-task #783). PR #911 merged to main via merge commit `b2b5f69873075de6f9516e3f1d053a7829416114` with live CI 22/22 green and MERGEABLE/CLEAN; signed-annotated version-less `parallel-launch-quota-budget-gate` tag points at the merge SHA (ED25519 signature valid; zero signing bypasses); deploy no-op (no `skills/` / `packages/` / harness paths). **Version-less identity:** authored in-pipeline as `v1.15`; `v1.15`/`v1.16`/`v1.17` (and subsequently `v1.18`–`v1.20`) were all claimed by concurrent releases, so this release ships **version-less** under the slug identity `parallel-launch-quota-budget-gate` (the #769 version-collision class) — the tag name IS the slug; every load-bearing `v1.15` anchor and the cutover clause were reconciled to the slug / introducing-release-SHA markerless form (no `allow-version-ref` marker needed). 30-day Stage 13 outcome window per the `novel` class. | #911 | [notes/parallel-launch-quota-budget-gate_RELEASE_NOTES.md](notes/parallel-launch-quota-budget-gate_RELEASE_NOTES.md) |
| v1.20 | health-and-raid-determinism | 2026-06-14 | Project health and RAID escalation become **deterministic**: a single canonical cross-level metric → RAG-threshold → decision-rule index, a watermelon-detection reference that consumes it, an escalation-thresholds doc-of-record, and a stale-RAID auto-escalation protocol on the `OPERATIONS.md` mirror pair, so the same situation produces the same color and the same escalation no matter which skill reports it. Health colors and escalation timing previously depended on which skill produced them and how each one decided where the lines fell — there was no single home for metric → threshold → decision mappings or for RAID age-escalation. **#271 (metric-registry):** NEW `operations/skills/weekly-status-rollup/references/metric-registry.md` — a cross-level (portfolio / program / project / team) INDEX of metric → RAG-threshold → decision-rule mappings that REFERENCES sibling-owned bands (`comms-writer/references/channel-formats.md` schedule/budget/risk/scope; `delivery-engine/references/capacity-model.md` capacity-utilization; `delivery-engine/references/backlog-health.md` portfolio cut-points; `escalation-thresholds.md` RAID-age) and OWNS only the metrics that exist nowhere else (Overdue RAID Count, cross-level roll-ups), with a lower-inclusive/upper-exclusive boundary convention so boundary values classify deterministically and a 🟡 YELLOW ≡ Amber vocabulary mapping. **#270 (watermelon-detection):** NEW `core/skills/pmo-qa-auditor/references/watermelon-detection.md` — eight named green-masking-risk signals (W1–W8) for status reported GREEN against a contradicting band reading, each with detection logic, data sources, false-positive filters, and an escalation action plus a verdict-composition rule; it consumes the registry thresholds by role-name (does not restate them) and is read in Mode A + Mode C. **#269 (escalation-thresholds):** NEW `operations/skills/ppm-agent/references/escalation-thresholds.md` — the PPM Agent doc-of-record for score → escalation-tier routing and the age-based auto-escalation thresholds; it references the delivery-engine 5×5 P×I scoring scale (`raid-templates.md §5`) rather than re-deriving it and reproduces the product grid only as a routing aid. **#261 (stale-RAID auto-escalation protocol):** a new `### Stale-RAID Auto-Escalation Protocol` H3 appended under `## Cross-Skill Protocols` in the `OPERATIONS.md` canonical+mirror pair (`core/governance/OPERATIONS.md` + `operations/OPERATIONS.md`, same commit) — age = `today − open-date` validated against the RAID Log, two warning/escalate bands by RAID type (Issue > 14 / > 30 days; Risk > 30 / > 60 days), a Nominal → Warning → Escalate action ladder, PPM Agent routing with Delivery Engine handing the tag back (no self-routing), and `escalation-thresholds.md` as the doc-of-record for threshold values. The `OPERATIONS.md` mirror pair (deploy Check 9) was reconciled in the same commit so mirror-drift holds on main. Consumption is reference-by-relative-link throughout (each consumer cites the canonical-source doc rather than re-deriving thresholds — duplicate-source-discipline), so the only cross-issue contention is avoided; the three new reference docs are consumed via their owning skills' `references/` directory, so the three `.skill` packages (`weekly-status-rollup`, `pmo-qa-auditor`, `ppm-agent`) rebuilt at Phase J.5 to carry the new docs (Check 7 package-freshness). `novel` class, CHEAP reversibility (single `git revert -m 1` of the release PR — additive-only: three new files + one append to the mirror pair; no data migration, no consumer state to unwind; re-run `./deploy.sh --deploy` post-revert to re-sync skill mirrors). All four issues #271 / #270 / #269 / #261 auto-closed at the release-PR merge; PR #933 merged to main on the 3rd Stage 12 attempt (prior attempts BLOCKED on two CI gates [FIXED] and a v1.19 version collision [FIXED — re-versioned to v1.20]) with the CI suite green (21 checks), signed-annotated `v1.20` tag at merge SHA `626b9926270216c639b7c7c13727c0648733028f` (ED25519 signature valid; zero signing bypasses); Stage 12 rebuilt the three `.skill` packages so Check 7 package↔source parity holds on main. Re-versioned **v1.18 → v1.19 → v1.20** in-pipeline after the concurrent `cross-release-impact-model` (#87, PR #928) claimed `v1.18` and `sior-escalation-discipline` (PR #941) claimed `v1.19`. 30-day Stage 13 outcome window per the `novel` class. | #933 | [notes/v1.20_RELEASE_NOTES.md](notes/v1.20_RELEASE_NOTES.md) |
| v1.19 | sior-escalation-discipline-across-the-comms-triage-technical | 2026-06-14 | Critical/High escalations across the skill suite now emit a single shared SIOR (Situation / Impact / Options / Recommendation) block — authored once in a canonical protocol doc, consumed by relative link — with a mandatory Recommendation + confidence and a named decision owner, instead of each skill formatting escalations ad hoc. SIOR escalation discipline previously lived inline across eight files with divergent structure, no shared severity-threshold policy, and (in comms-writer's case) no Recommendation component at all (the 2026-04-18 skill review P1 gap). **#179 (keystone):** authors the net-new `core/standards/sior-escalation-protocol.md` — the single source for the four mandatory ordered SIOR components, the canonical severity-threshold policy (CRITICAL = always / HIGH = SIOR + stakeholder-authority check / MEDIUM = conditional-on-blocks-downstream / LOW = no), five worked escalation-class examples, and the decision-owner-mapping pattern (read free-text `## Key People` + warn-and-route-to-PgM when authority data is absent); the doc is presentation-neutral so a future dual-format model can layer on it. comms-writer's named **Escalation** type is re-specified to emit named SIOR sections with an explicit mandatory Recommendation + confidence (the prior Ask-conflation stripped; the Ask demoted to a closing call-to-action), SIOR is carried into the name-keyed Escalation entry in `core/schemas/per-skill-output-contracts.md`, and one failure mode is added. **#178:** ppm-agent Section 5 `[COMMS]` routing emits a voice-neutral SIOR block, with the inline copies in `references/push-to-resolve.md` + `references/proactive-follow-up-tracking.md` reconciled to the canonical doc + one failure mode. **#177:** pmo-technical-analyst emits a severity-conditional SIOR block on CRITICAL/HIGH findings with an authority check + decision-owner mapping + one failure mode. **#934 (folded in at Stage 5 discovery):** delivery-engine reconciles the inline SIOR in `references/dependency-rules.md §4.2` + `references/raid-templates.md §7` to the canonical doc. Consumption is **LINK-REFERENCE** (ADR-025) — the canonical stays out of `skills/*/references/`, so `deploy.sh` and the `.skill` packages carry no injection of it and the only cross-issue file contention (all four issues touching `deploy.sh`) the Stage 4 plan flagged is eliminated; the four packages rebuilt only because each consumer skill's own SKILL.md/references content changed (Check 7 package-freshness). **`ADR-025-sior-escalation-canonicalization.md`** (CREATED, renumbered from the Stage 5 ADR-024 draft to avoid the in-flight PR #911 collision) records the decision. `novel` class, CHEAP reversibility (single `git revert -m 1` of the release PR — additive canonical doc + skill edits + output-contract carry + ADR; the net-new doc has no prior consumers, so deletion carries no orphaned-reference risk and a revert restores the consumers' pre-release inline SIOR in the same commit; re-run `./core/deploy/deploy.sh --deploy` post-revert to re-sync skill mirrors). All four issues #179 / #178 / #177 / #934 auto-closed at the release-PR merge; PR #941 merged to main with CI 22/22 green, signed-annotated `v1.19` tag at merge SHA `787029df87e2ea6e961e9cdc5a030ffa248fcfd2` (ED25519 signature valid; zero signing bypasses); Stage 12 rebuilt the four `.skill` packages so Check 7 package↔source parity holds on main, and the explicit-name `./core/deploy/deploy.sh --deploy comms-writer ppm-agent pmo-technical-analyst delivery-engine` deployed all four skills + four packages to the runtime targets (0 failures). Re-versioned **v1.18 → v1.19** in-pipeline after the concurrent `cross-release-impact-model` release (#87, PR #928) claimed the `v1.18` tag first. 30-day Stage 13 outcome window per the `novel` class. | #941 | [notes/v1.19_RELEASE_NOTES.md](notes/v1.19_RELEASE_NOTES.md) |
| v1.18 | cross-release-impact-model | 2026-06-14 | The release pipeline gains a durable, architecture-native, repo-agnostic model of the concurrent + planned release set and its mutual impact, consulted at Stages 3/4/5/9/12 so concurrent structural-blast-radius / same-file releases are detected and serialized and a Stage-9 GO is gated on baseline currency before Execute. **#87 (cross-release impact model):** the root-cause fix for the four 2026-05-16 four-milestone parallel-run failures (path-invalidation collision with zero ticket-dependency edge; silently-voided Stage-9 GO; stale-pinned cross-PR audit; un-flagged parallel-unsafe bundle). Adds a third contention axis — **structural-blast-radius** — alongside the existing ticket-dependency and same-path classifiers. `release/references/pipeline/stage-03-bundle.md` defines the `§ A9.6.1` axis: a 4-token mover-classifier (`RENAME` / `RELOCATE` / `DELETE-RECREATE` / `DIR-RESTRUCTURE`), each tied to a reproducible `git diff --name-status --find-renames` signal; the mover's break surface `SURFACE(R)` computed via the F1–F6 ref-form sweep (**F4 mover-internal-outbound EXCLUDED** so high-traffic governance files do not over-serialize); the intersection predicate `EDITSET(sibling) ∩ SURFACE(R) ≠ ∅`; and a `Tier-S` verdict + `structural-blast-radius` edge extending the live standing Parallelization Map. `release/references/pipeline/stage-04-planning.md` adds the authoritative A4 structural sub-audit (the first pipeline point a branch exists), the sibling-merge stale-pin self-invalidation trigger (a pinned A4 audit re-runs when a sibling parallel release merges after the pin), and Phase A0 Tier-S re-derivation. `release/references/pipeline/stage-05-solutioning.md` adds a cross-*release* coherence row to the Collective Review. `core/schemas/gate-criteria-spec.md` adds **`G-PR9`** — the Stage-9 GO records its baseline SHA + a sibling-merge revalidation predicate (CURRENT / STALE-REVALIDATE / STALE-VOID), composing with the live Phase-A6.5 divergence check as a second axis (registry now `G-PR1..G-PR7` + `G-PR9`, no renumber, added cleanly above the unregistered-G-PR8 gap owned by #887). `release/governance/release-process.md` adds the Stage 12 Phase A.5 semantic GO-invalidation check (catches a renamed-since-baseline referenced path even when a plain `git merge` reports no textual conflict); its byte-identical `.claude/rules/release-process.md` mirror syncs via `./deploy.sh --deploy`. `release/skills/release-planner/SKILL.md` is updated to consult the model (runtime-skill mirror; package rebuilt in-PR). **`ADR-024-cross-release-impact-model.md`** (CREATED) records the architecture decision. All three structural surfaces (A4 trigger / G-PR9 / Phase A.5) share one merge-style-agnostic `--name-status --find-renames` predicate; every new criterion carries the introducing-release-exempt cutover clause (reflexive-pipeline-loop discipline). `cross-cutting` class, CHEAP reversibility (single `git revert -m 1`; additive spec + gate criteria + ADR — no data migration; re-run `./deploy.sh --deploy` post-revert to re-sync the release-process rules mirror + the release-planner skill mirror). **#87 stayed OPEN through merge** (PR #928 used `References #87` in its Issue References block — `closingIssuesReferences` empty — so it was closed manually at Stage 13 with the per-AC verification evidence comment); PR #928 merged to main with all CI green, signed-annotated `v1.18` tag at merge SHA `7d9307bbfcf317e29bfe34a772f5fac08a8934ea` (zero signing bypasses); Stage 12 rebuilt the `release-planner.skill` package so Check 7 package↔source parity holds on main. AC-7 over/under-serialization pair validated both directions at Stage 8 QA (v11.08↔v11.11 caught/serialized; v16.02↔v11.11 correctly not over-serialized). 30-day Stage 13 outcome window per the `cross-cutting` class. | #928 | [notes/v1.18_RELEASE_NOTES.md](notes/v1.18_RELEASE_NOTES.md) |
| v1.17 | comms-writer-artifact-generator-anthropic-offload-refactor | 2026-06-14 | The two PMO authoring skills are narrowed to their PMO-unique surface and given an Anthropic-voice offload path that runs at design time (no runtime Anthropic dependency), and a duplicate-ADR collision is resolved. **#174 (narrow comms-writer to 6 PMO-unique primary types):** `comms-writer` SKILL.md narrowed to the 6 genuinely PMO-unique communication types, with the PMO-critical rules (audience calibration, escalation discipline, status-theater guardrails) preserved through the narrowing rather than dropped. **#173 (comms-writer owns exec-brief + stakeholder-email — own-with-harvest):** `comms-writer` takes ownership of executive-brief + stakeholder-email generation under the **own-with-harvest** pattern — the Anthropic voice is harvested into the skill at design time so generation carries no runtime Anthropic dependency. **#176 (narrow artifact-generator catalog — Part A+B):** the `artifact-generator` catalog narrowed to its PMO-unique artifact set (27 artifacts after the narrowing + own-with-harvest additions), removing commodity artifacts an Anthropic scaffolding skill produces better. **#175 (artifact-generator Anthropic offload Wrapper Mode + Part-D routing tree — Part C+D):** a NEW **Wrapper Mode** in `artifact-generator` that offloads commodity artifact scaffolding to an Anthropic skill, plus a **Part-D routing decision tree** in `CLAUDE.md` routing an artifact request to the PMO catalog vs. the Anthropic offload path. **#791 (resolve duplicate ADR-022 collision):** the duplicate `ADR-022` basename collision in `core/ADRs/` resolved by renumbering the skill-sourcing-coupling-posture ADR to **`ADR-023-skill-sourcing-coupling-posture.md`** (the platform-config-vs-operator-toml-split ADR retains `ADR-022`), with cross-references reconciled and the skill-sourcing registry trigger-(d) wired. `novel` class, CHEAP reversibility (single `git revert -m 1`; skill-scope narrowing + offload wrapper + routing tree + ADR renumber — no data migration; re-run `./deploy.sh --deploy` post-revert to re-sync skill mirrors). Re-versioned **v1.15 → v1.17** in-pipeline after the concurrent `platform-self-measurement-and-quality-method` release claimed `v1.15` (and `delivery-capacity-and-lifecycle-gating` landed `v1.16`) first; the signed `v1.17` tag points at this release's merge SHA `79d8827d0fb57f671ad49c0a80acf9e376bc5a55`. All 5 issues #173 / #174 / #175 / #176 / #791 auto-closed at the release-PR merge; PR #864 merged to main with CI green, signed-annotated `v1.17` tag at merge SHA `79d8827d0fb57f671ad49c0a80acf9e376bc5a55` (zero signing bypasses); Stage 12 rebuilt the `comms-writer.skill` + `artifact-generator.skill` packages so Check 7 package↔source parity holds on main, and the explicit-name `./deploy.sh --deploy comms-writer artifact-generator` deployed both skills + both packages to the runtime targets (0 failures). 30-day Stage 13 outcome window per the `novel` class. | #864 | [notes/v1.17_RELEASE_NOTES.md](notes/v1.17_RELEASE_NOTES.md) |
| v1.16 | delivery-capacity-and-lifecycle-gating | 2026-06-13 | The delivery-engine gains four reference foundations that turn sprint planning + release-readiness from rules-of-thumb into governed method across capacity, estimation, lifecycle, and gates. **#272 (capacity-model.md):** a NEW `operations/skills/delivery-engine/references/capacity-model.md` modeling *effective* delivery-team capacity — the capacity formula (§1) + context-switching penalties across concurrent projects (§3) + Brooks's-Law late-addition thresholds (§4) + the 60/20/20 effort split (§5) + team-stability / vendor-ramp thresholds (§6–§7) + staffing-concentration / bus-factor risk (§8) + demand-vs-supply gap RAG bands (§9); wired into `delivery-engine` Mode D (formula + penalties) + Mode E (demand-supply gap); the focus factor + the platform bus-factor rule are referenced (owned by `estimation-standards.md` §3 + `core/disciplines/km-protocols.md` §4), not restated. **#273 (estimation-standards.md):** a NEW `.../references/estimation-standards.md`, the **canonical owner of the focus-factor table** (§3, promoted unchanged from its sole prior occurrence in `sprint-defaults.md` §1.2) — plus Cone-of-Uncertainty band widths bounding precision by lifecycle phase (§1, normative cone rule rejects an over-tight range), planning-horizon commit-vs-forecast rules, the three-zone buffer model, the velocity-and-estimates-as-ranges enforcement rule (§5), and the contingency-vs-management-reserve distinction; wired into Mode D (bands + horizon + table + buffer) + Mode E (range enforcement). **#274 (lifecycle-stages.md):** a NEW `.../references/lifecycle-stages.md` enumerating the **15-stage universal delivery lifecycle** (temporal axis) five-phase super-grouped (Capture → Prepare → Build → Validate → Deliver) with per-stage entry/exit criteria + consumed/produced artifacts + a 5-of-8 methodology mapping; wired into Mode C (execution-stage entry) + Mode F (validation-stage exit, incl. the QA/Acceptance exit predicate blocking Plan Review while a P1 defect is open); explicit boundary statement (project delivery lifecycle ≠ the platform release pipeline). **#275 (gate-definitions.md):** a NEW `.../references/gate-definitions.md`, the canonical sequence of **eleven project-lifecycle gates** `LG-0` (Idea Screen) → `LG-10` (Closure), each with entry/exit criteria + key decisions + authority holder + required artifacts + escalation; wired into Mode F (per-gate exit + transition rule) + Mode G (decision attribution) + Mode C (sprint DoR = LG-4); cross-references the sibling gate-TYPE taxonomy (`gate-checklists.md`) not restated; the `LG-` prefix is namespaced to avoid collision with the release-pipeline stage gates. The three governance docs (`lifecycle-stages.md` temporal · `gate-definitions.md` gate-instance · `gate-checklists.md` gate-type) form a clean three-axis model. `novel` class, CHEAP reversibility (single `git revert -m 1`; additive reference docs + Mode C/D/E/F/G wiring + one-line `sprint-defaults.md` single-source edit — no data migration). A concurrent v1.15 merged into main mid-pipeline; the `delivery-engine` SKILL.md conflict was resolved at Stage 12 with BOTH edit-sets preserved (v1.15 RCA additions + this release's mode wiring). Two CI gates (durable-corpus `#N` fragility) flagged at Stage 9 Plan Review were de-fragiled before merge. PR #859 merged to main with CI green, signed-annotated `v1.16` tag at merge SHA `a915daa94caa68423b42cbf7e7db69a4dfbc37e1`; Stage 12 rebuilt the `delivery-engine.skill` package so Check 7 package↔source parity holds; runtime skill-copy deferred per content-release precedent (canonical deployment = merge + tag). 30-day Stage 13 outcome window per the `novel` class. | #859 | [notes/v1.16_RELEASE_NOTES.md](notes/v1.16_RELEASE_NOTES.md) |
| v1.15 | platform-self-measurement-and-quality-method | 2026-06-13 | The platform measures and improves its own agent quality. **#358 (Platform Health Audit mode):** a NEW `platform-health` mode integrated into `core/skills/pmo-qa-auditor/SKILL.md` that audits base-vs-build compatibility drift on a registered cadence, consuming the re-homed predecessor inputs `release/references/protocols/platform-health-audit-framework.md` + `core/specs/anthropic-base-vs-build-registry.md`, with the deferred v11.01f body ACs resolved against the live §4 placeholder. **#359 (data-driven failure-mode target range):** `core/specs/failure-mode-standard.md` § Minimum Count amended with a 6–10 target-range addendum for complex skills (the issue's cutover risk was MOOT — v1.10 `failure-mode-coverage-completion` shipped and all 5 per-category remediation issues #6/#138/#139/#140/#141 are CLOSED). **#754 (invokable discipline methods):** NEW `core/disciplines/root-cause-analysis.md` (the invokable RCA method) + NEW `core/references/how-to/migration-playbook.md` (the migration playbook), wired with intake/pipeline handoff via `core/rules/operations-bridge.md` + the two discipline/how-to README indexes (docs-as-disciplines path, not a skill). **#125 (verified triage — analysis-class):** a verified triage of the 34-day-stale 2026-05-10 harness audit (NOT a restatement) — F2 RESOLVED (link-check CI exists), F4 RESOLVED-by-pruning, F1 PARTIALLY-RESOLVED (residual `evals/results/`), F3 UNVERIFIABLE-HERE (runtime `.mode` lives install-root), F5 UNVERIFIED→install-root; only F1-residual + F5-triage survive as follow-up proposals (Dev Testing + QA SKIP — no runtime surface). `novel` class, CHEAP reversibility (single `git revert -m 1`; additive skill mode + standard addendum + 2 discipline docs + analysis artifact — no data migration). PR #858 merged with the full CI suite green, signed-annotated `v1.15` tag at merge SHA `dfb3836a2b8ac9807113ae1beb7a91629a3e2874`; Stage 12 rebuilt 3 `.skill` packages (`pmo-qa-auditor`, `delivery-engine`, `intake-desk`) so Check 7 package↔source parity holds. 30-day Stage 13 outcome window per the `novel` class. | #858 | [notes/v1.15_RELEASE_NOTES.md](notes/v1.15_RELEASE_NOTES.md) |
| v1.14 | adapter-config-foundation | 2026-06-13 | The adapter-config foundation — the canonical platform-config surface + onboarding spine every host adapter (#10/#11/#12/#13) composes into. **#22 (unified platform-config surface + 5-rung resolver):** reframed off the issue body's stale "new YAML/MD file" premise (the surface is NOT greenfield — operator-identity template + composition-surface spec + deploy rung-reader + ADR-017 §162 pre-existed); split into two Layer-1 templates by concern — NEW `core/config/operator.toml.template` (operator ENVIRONMENT/IDENTITY: identity, paths, `[adapters]` host-selectors, `[methodology].default_delivery_approach`) + NEW `core/config/platform-config.toml.template` (platform BEHAVIOR: `[bundling]`, `[release_class]`, `[relationship_mapping]`, `[calibration]`) — with NEW field schema `core/schemas/platform-config-schema.md`, NEW human-readable catalog `docs/platform-config-reference.md`, and the **Platform-Config Resolution Protocol** authored into `core/governance/OPERATIONS.md` (5-rung most-specific-wins cascade + 3-level default-fallback that never hard-fails + single-resolution-at-hub + two-track update governance on the Layer-1/Layer-2 boundary). Three consumers wired as worked examples (`release-planner` Mode A + `release-executor` Mode A resolve named fields at session start; `core/deploy/deploy.sh` rung-reader extended to the behavior surface). Scope = mechanism + resolver + 3 wired examples, NOT a full migration; split rationale in NEW `core/ADRs/ADR-022-platform-config-vs-operator-toml-split.md`. **#703 (onboarding-umbrella spine):** NEW `docs/ONBOARDING_JOURNEY.md` — the ordered clone→working-install arc with 4 uniform Extension-Point blocks naming where each host choice (repo, ticketing, KB, AI tool) plugs in, linked from `README.md` Getting Started; Stage 5 boundary = journey-spine + extension-point contract (NOT a re-statement of GETTING_STARTED / FIRST_STEPS), Stage 8 verified non-duplication. The release PR stamped both release-skill SKILL.md at `v1.13` in error; the Stage 12 close-out re-stamped both to `v1.14`, rebuilt the 2 `.skill` packages to embed v1.14, and anchored the resolution-protocol cutover SHA to the literal PR #759 merge SHA `71047a5` (this release itself exempt per reflexive-pipeline-loop discipline). `novel` class, CHEAP reversibility (single `git revert -m 1`; additive templates + schema + catalog + onboarding doc + resolution protocol — no data migration). Both issues #22 + #703 closed COMPLETED at the release-PR merge; PR #759 merged with the full CI suite green, signed-annotated `v1.14` tag at merge SHA `71047a5` (ED25519; zero signing bypasses); Stage 12 deployed 2 skills + 2 packages to both runtime targets (0 failures). 30-day Stage 13 outcome window per the `novel` class. | #759 | [notes/v1.14_RELEASE_NOTES.md](notes/v1.14_RELEASE_NOTES.md) |
| v1.13 | runtime-test-gating | 2026-06-13 | Runtime test gating + the platform's first standing regression corpus. **Re-versioned v1.12 → v1.13** — planned/branched/committed/PR-titled as `v1.12`; the concurrent `corpus-durability-enforcement` release (PR #748) claimed `v1.12` first, so the canonical version is **v1.13** (signed `v1.13` tag at merge SHA `fba3df4`); PR #747's title + merged commit messages retain the immutable "v1.12" as the as-authored historical record. **Deploy no-op** (no skill/package/harness changes — 17 paths confined to `core/hooks/tests/` + `core/deploy/tests/` + `.github/workflows/` + `release/references/**` + `release/tools/**`). **#319 (test-suite rot — hook/deploy tests under CI):** the 8 committed hook tests (`core/hooks/tests/*.test.sh` + `test-runner.sh`) made portable + CI-gated via NEW `core/hooks/tests/setup-ci-layout.sh` + 4 refreshed hook tests, wired into `.github/workflows/install-tests.yml` so a PR breaking a wired hook test fails CI regardless of checkout location; `core/deploy/tests/test_lib_composition.sh` composition manifest-count assertion made manifest-derived (no hardcoded literal). Stage 5 AC-currency reconciliation (G-PL1) had confirmed 2 of 3 original defects already FIXED on main + a third a phantom — build targeted the still-live core. **#430 (runtime code-test gating + machine-readable `test-run` tracking):** NEW `release/references/standards/runtime-suite-selection-map.md` (changed-path → gating-suite map) + select/run steps in `stage-06-engineering.md` + `stage-07-dev-testing.md`; NEW `test-run` event type in `release/references/standards/pipeline-event-log-schema.md` + DT↔QA handoff test-results block; the event-log writer/reader pair `release/tools/append-pipeline-event.sh` + `release/tools/query-pipeline-event.sh` repaired so `test-run` events emit + read back (resolving the dedup survivor #590 — extinct `pmo-platform/engineering/` path — which closed on the fix); `stage-09-plan-review.md` updated. **#706 (standing install/onboarding/update regression suite):** NEW sandboxed CI-gated suite `core/deploy/tests/run-install-regression.sh` + `test_upgrade_config_durability.sh` wired into `install-tests.yml`, productizing the pre-public-flip QA sweep (Suites A–H under a `HOME`→`/tmp` sandbox: fresh sandboxed install, post-install token-substitution + file-link/reference integrity, two-phase deploy success, user-config durability across a simulated upgrade); deterministic scoring (no judge layer). The #325 behavioral / judge-graded scenario engine (C2–C5) + BYO scenarios (C7) stay out of scope, sequenced to a later slice. `cross-cutting` class (re-classified novel → cross-cutting at the Stage 4 C8 scope expansion; trigger (c) — 3 in-bundle compositional edges), CHEAP reversibility (single `git revert -m 1`; additive CI workflows + test suites + 2 pipeline tools + spec text; deploy no-op means nothing to re-sync post-revert). Three issues #319 / #430 / #706 closed COMPLETED at the release-PR merge (+ #590 closed by the #430 repair). PR #747 merged with 22/22 CI green — including the gates from the two concurrent releases (cross-reference integrity, intra-repo link verification, skill-count/IMP-XXX delta, durable-corpus fragile-reference delta) + the new hook-test / install-regression / shell harnesses; Phase A6.5 sync `c09fbd1` re-validated against the concurrent releases' new gates at 22/22 pre-merge. Fast-follows filed at close: #758 (extinct-path resolution in release-analytics tools) / #760 (`check-doc-links.py` cannot validate a sandboxed/deployed tree). Process improvement: #769 (Stage 12 / A6.5 pre-merge must verify the version number is still unclaimed). 30-day Stage 13 outcome window per the `cross-cutting` class. | #747 | [notes/v1.13_RELEASE_NOTES.md](notes/v1.13_RELEASE_NOTES.md) |
| v1.12 | corpus-durability-enforcement | 2026-06-13 | Corpus durability enforced by two file-disjoint CI gates (warn-mode-initial). **#316 (single-source shared references + enforced rebuild — Candidate B):** the two hand-duplicated shared references are single-sourced to NEW canonical homes `core/standards/output-format.md` (was 6 byte-identical copies, md5 `c43de7d8…`) and `core/standards/operational-artifacts.md` (was 2 copies), the 6+2 per-skill mirror files deleted, and `core/deploy/deploy.sh` extended to inject both canonicals via `TEMPLATE_SYNC_MAP` with a **Check 13b template-injection collision sub-assertion** that fails on any reintroduced same-basename copy — identical OR divergent (the operator-added divergent prong) — via a narrow explicit-basename resolver extension, NOT the broad catch-all and NOT the issue's deferred `depends_on:` frontmatter (verified CONFLICT vs the Anthropic skill-creator schema per `template-storage.md` §3.4/D5, so 0 SKILL.md frontmatter edits — only a one-line `comms-writer` cross-path body fix). The single-source contract is documented in `core/standards/template-storage.md` + `core/rules/skill-deployment.md` + `core/standards/universal-vs-localized-context.md`. **#311 (raw-URL prohibition + named ledger allowlist — Delta-2-primary build):** NEW Class U raw `github.com/.../{issues,pull,milestone}` URL detector `BLOCK-FRAGILE-REF-004` added to `core/hooks/block-fragile-refs.sh` and the byte-identical `.github/workflows/reference-durability.yml` CI (with an `is_ledger_exempt()` tracking-surface exemption covering top-level `CHANGELOG.md`), the named **5-surface ref-permitted allowlist** formalized in `core/standards/universal-vs-release-pipeline-split-rule.md` (adding CHANGELOG to the prior 4), the new URL class registered in `core/standards/reference-durability-standard.md`, and fixtures wired into `core/hooks/run-fragile-ref-fixtures.sh` + `core/hooks/testdata/cutover-fixtures.txt`; Delta-3 (version-cutover-clause prohibition) folded **verify-only** with R3 evidence citing the live `CUTOVER_RE` lines (already enforced in both hook + CI). `core/rules/git-workflow.md` documents the new raw-URL prohibition. `novel` class, MODERATE reversibility (single `git revert -m 1`; #316 restores the 8 deleted mirror files, #311's detector ships warn-mode so there is no enforce-state to unwind; re-run `./deploy.sh --deploy` post-revert to re-sync skill mirrors). Both issues #316 / #311 marked closed at Stage 13 on the DT PASS ×2 / QA ACCEPT ×2 verdicts + merge SHA + live gate per issue (release PR used `References` → manual close). Check 14 flip-to-enforce assessment: DEFER — keep warn-mode-initial through the standard shakedown window; the two new gates (Check 13b; raw-URL `BLOCK-FRAGILE-REF-004`) are brand-new with zero warn-log history, below the bypass-mode-readiness ≥3-day threshold. Adversarial review #733 absorbed at Stage 5 (3 corrections, 2 design-mandates); #746 filed out-of-scope. | #748 | [notes/v1.12_RELEASE_NOTES.md](notes/v1.12_RELEASE_NOTES.md) |
| cross-reference-integrity-ci (version-less) | cross-reference-integrity-ci | 2026-06-13 | Cross-reference & reference-durability CI — the platform's PR-time reference-integrity enforcement is hardened across three file-disjoint CI surfaces (warn-mode-initial). **#314 (reference-durability positional `#N` parity):** the position-blind CI existence check is replaced by a true line-position classifier extracted to NEW `core/hooks/lib/positional-issueref.awk`, invoked by both the pre-commit hook and `.github/workflows/reference-durability.yml`, closing the one divergent shape (a self-describing `#N` in body prose with the reference block BELOW it) where the hook flagged `OUTSIDE-BLOCK` while CI passed — a flip-to-enforce false-negative; the divergence fixture is wired into the fixture suite (25 passed / 0 failed). The diff-hunk mapper was inlined in the CI step (no second lib — no current consumer) and `deploy.sh` was not touched. **#169 (link-check PR gate):** NEW `.github/workflows/link-check.yml` promotes the `core/deploy/tools/check-doc-links.py` primitive to a PR-time gate with `<OPERATOR_INSTANCE_*>` token-class exclusion in the engine + a repo-resident allowlist (NEW `core/deploy/allowlists/skip-doc-link-check-ci.txt`); the 13 pre-existing genuine broken refs were drained to 0 (baseline 221 raw → 0 real residual over the #169 scope); it is the SAME primitive over the SAME `--target-paths` as deploy-time Check 14, so deploy-time and PR-time verdicts cannot drift. **#130 (forward IMP-XXX / skill-count drift CI, re-scoped C+D):** NEW `core/deploy/tools/check-skill-count-imp.py` (detector) + NEW `.github/workflows/skill-count-imp-check.yml` (added-lines-delta workflow), new files only — the absent `imp-references.tsv` (confirmed absent from working tree and full git history) made the literal TSV-consumer ACs dead-on-arrival, so the issue was re-scoped to forward CI and the routing-conflict-detection portion split to #704 (OPEN, off-milestone). `novel` class, CHEAP reversibility (single `git revert -m 1`; all deliverables additive CI/tooling; warn-mode-initial means no PR is hard-blocked during shakedown). Three issues #314 / #169 / #130 marked closed at Stage 13 on the Stage 8 ACCEPT/PASS verdicts (#697 / #698 / #699) + merge SHA + live gate per issue. Check 14 flip-to-enforce assessment: keep warn-mode-initial through the standard shakedown window (0-baseline, no warn-log history). Pre-flip-to-enforce hardening follow-up filed off-milestone. | #745 | [notes/cross-reference-integrity-ci_RELEASE_NOTES.md](notes/cross-reference-integrity-ci_RELEASE_NOTES.md) |
| v1.11 | cleanup-orphan-state-reliability | 2026-06-12 | Cleanup-orphan-state reliability — the workspace sweep `release/tools/cleanup-orphan-state.sh` becomes safe to run from anywhere and complete in one pass. #333 SELF action class (self-cwd computation + primary-clause robustification; the script's own runtime worktree reads `SELF — script's own runtime worktree (protected)` in both emitters and is apply-inert including under `--force`) + worktree dedup guard; #326 all-process `lsof` cwd liveness oracle (absolute-path binary, self-canary, fail-closed when the oracle is unavailable) + live clause at precedence slot 3 + apply-time re-verification (A-Recheck) + fail-closed counter + `liveness_oracle` JSON field, with the oracle-selection record at NEW `release/ADRs/ADR-021-liveness-oracle-selection.md` (Accepted at the 2026-06-11 scope-lock; 30-day re-triage criterion toward the all-fd predicate); #53 `resolve_freed_branches` — ONE bounded re-evaluation pass between apply and verify (freed set derived from REMOVED worktree rows; unrowed `--historical` branches fresh-classified) so a single `--apply` reaches fixed point; #57 VERIFY-ONLY (apply-path hardening confirmed shipped, re-evidenced by `--self-test` PASS at every commit; marked closed at Stage 13 with the three-link evidence comment per D-VerifyOnly-57). Self-test grows three end-to-end fixtures (SELF guard / synthetic live holder / fixed-point apply) on the preserved apply/verify/prune guards; the two invoker docs updated to the four-phase apply vocabulary (`core/rules/git-workflow.md` § Step 10 + `release/references/pipeline/stage-13-close.md` Phase C4). `novel` class, CHEAP reversibility (single `git revert -m 1`; dogfood guard — this release's own Stage 13 cleanup runs the NEW script, halt-revert-rerun on misbehavior). Follow-ups routed at plan/stage boundaries: #654 (worktree-agent-* sweep coverage) / #655 (per-branch gh-call batch-prefetch) / #656 (protect-list install seeding) / #659 (script-execution allowlist invocation forms, DL-8) / #669 (--help usage window) / #670 (duplicate branch rows under --all) / #679 (append-pipeline-event.sh pre-restructure path) | #668 | [notes/v1.11_RELEASE_NOTES.md](notes/v1.11_RELEASE_NOTES.md) |
| v1.10 | failure-mode-coverage-completion-across-all-five-categories | 2026-06-12 | Failure-mode coverage completion across all five categories — **45 fresh-authored domain-specific failure-mode entries** (35 PRIMARY + 9 TRIG SECONDARY + the operator-included comms-writer send-boundary HAND SECONDARY per adversarial CD-2) across all 23 SKILL.md files take every category to 100% live-catalog coverage from 52.2 (TRIG) / 78.3 (INPUT) / 78.3 (PROC) / 82.6 (OUT) / 65.2 (HAND) percent; catalog entry census 102 → 147. Five per-category issues shipped as ONE release on a single branch with per-issue sequential commits in the approved OUT → INPUT → PROC → TRIG → HAND order (D-C SINGLE; fresh-authoring release — the source audit's drafts were unreachable post re-versioning, 12 one-liner seeds survived), every entry Stage-5-designed against the skill's live operational surface, adversarially reviewed (5 independent reviews, zero Blockers), and Stage-8 G7-graded with G7-06 LLM means ≥4.0 on every modified file. Every deployed modified skill bumps `version: v1.10` (canary `v1.10-canary`, fixture-scoped per D-CanaryScope/ADR-04); eval-writer / pmo-qa-auditor / prompt-builder ADD the previously-missing `version:` field; project-initiator + weekly-status-rollup gain `references/` adjacencies per the Check 6 D-Refs disposition. Stage 12 deployed 22 skills + 22 rebuilt packages to both runtime targets (0 failures). `novel` class, CHEAP repo-level reversibility (runtime MODERATE — re-deploy at reverted HEAD). Follow-ups filed at close: #673 (Check 6 detection gap) / #674 (release-executor Dry-Run-Record mis-route) / #675 (blast-radius.sh locale) / #676 (canary § Skill 12 reconciliation) / #677 (15-file stale-path sweep) | #658 | [notes/v1.10_RELEASE_NOTES.md](notes/v1.10_RELEASE_NOTES.md) |
| v1.09 | agent-to-script-promotion | 2026-06-11 | The agent-to-script promotion framework — a governed answer to "when does repeated agent work earn a script, and what does a script owe once it exists." NEW tracked standard `core/standards/agent-script-promotion-framework.md`: the AS0–AS4 promotion ladder (establishing ADR-020, Accepted — agent procedure → documented command → tracked tool → checkpoint-wired → autonomous guard; deterministic caller-type AS2/AS3 boundary test; split promotion for judgment-class steps per the mixed-determinism rule) with six governed decision-table sections (triggers + counter-signals, authoring/review with the PR-bound discovery anchor, testing, drift monitoring, agent-script interface, versioning/deprecation), registered as an INTERNAL framework-catalog row (Check 18-validated) and grounded in an executed operator-instance audit per D-AuditPlacement (71-script census across 9 coded columns, runtime-mirror identity confirmed, 22-skill mechanical-vs-judgment segmentation) + a ranked 8-candidate opportunity inventory (4 READY; #287/#286 mapped, #234 documented-excluded). Worked examples SP-1 + SP-4 inline the inventory substance; 4 upstream-catalog entries re-verified; OPERATIONS.md mirrors untouched per the ratified D-4. `novel` class, CHEAP reversibility. Follow-ups queue post-release per D-AuditSynthesisGranularity (4 READY candidates 1:1 via standard intake; catalog `canonical_doc` reconciliation + km-governance narrative as one item) | #636 | [notes/v1.09_RELEASE_NOTES.md](notes/v1.09_RELEASE_NOTES.md) |
| memory-to-corpus-codification (version-less) | memory-to-corpus-codification | 2026-06-10 | Verification-only corpus-codification close-out — all nine targeted knowledge encodings (5 workspace guardrails in `core/CLAUDE.md.template` + 4 git-workflow rules in `core/rules/git-workflow.md`) verified already shipped in the tracked corpus, with provenance recorded (two encodings from the original repository's 2026-04-18 / 2026-05-12 lineage; seven from the 2026-06-05 public-seed first commit; tickets migrated open and bundled unverified — caught by the Stage 4 currency gate). The release's only substantive mutation was the Stage 12 operator-instance eviction of the three superseded feedback memories (plus three index lines and the codification-ledger row), archive-first. Both tickets marked closed at Stage 13 on reconciled per-AC evidence. `routine` class, CHEAP reversibility. Out-of-scope discovery filed: #633 (rules-mirror deploy gap); #429 owns the migration-playbook scope | #604 | [notes/memory-to-corpus-codification_RELEASE_NOTES.md](notes/memory-to-corpus-codification_RELEASE_NOTES.md) |
| v1.08 | skill-suite-architecture-spine | 2026-06-08 | The skill-suite architecture spine (decomposition axis + factory guard) — a binding decomposition rule (the establishing ADR-019: role-named Specialists COMPOSE shared function-skills rather than absorb them, a role may span several Specialists per the skill-boundary test, relying on the existing cascade rule C1 depth bound; `status: Proposed`); the audited + codified skill↔pipeline alignment as a new `core/standards/` standard (decision-tests DT-1/2/3); and a fail-loud Anthropic-scaffolder-drift guard in `pmo-skill-refiner` so the factory degrades loudly, not silently. `novel` class, MODERATE reversibility (governance + tooling, additive). #8 premise-rejected per ADR-012 (tracked roadmap instances are operator-local; the framework already ships) — no tracked deliverable. Follow-ups: SPA-001 census fix #63 · proof-of-mechanism #571 | #570 | [notes/v1.08_RELEASE_NOTES.md](notes/v1.08_RELEASE_NOTES.md) |
| v1.07 | declarative-workitem-type-model | 2026-06-07 | The work-item type layer (methodology-agnostic, best-practice TEMPLATE) — a thin generic `Work Item` entity beneath Milestone/Workstream as the hierarchy node (with the establishing ADR-018 and a Tier-2 SCOPE-CHANGE re-freeze of the FROZEN entity roster); a domain-neutral work-organization mapping framework (concept → hierarchy-by-methodology → best-practice schemas / user plug-and-play); and a declarative type-pack meta-schema (methodology-projected kinds, EAD-materialized per-type schemas) that parameterizes the entity so a user brings their work types and an agent understands the work structure by nature. K1 governance + K4 user config, decoupled from the release-pipeline ticket model. Deferred to later milestones under #409: C3/C5/C6/C7/C8/C9 | #526 | [notes/v1.07_RELEASE_NOTES.md](notes/v1.07_RELEASE_NOTES.md) |
| domain-aware-stage5-design (version-less) | domain-aware-stage5-design | 2026-06-07 | Domain-aware Stage 5/7 design (design no longer pmo-markdown-bound) — a keystone `domain:` class field on the `domain_practice` label classified once at Planning A3; a governed design-exploration step (divergent generation → convergent narrowing) before the trade-off matrix; a per-domain impact-analysis method selector (markdown `blast-radius.sh` default, non-markdown fan-out admitted, documented opt-out); a domain-best-practice review criterion with an honest not-assessed flag; and the first two per-domain best-practice guides (software, governance). Deferred: #504 (Check-18 path) + #505 (domain-fan-out tool) | #503 | [notes/domain-aware-stage5-design_RELEASE_NOTES.md](notes/domain-aware-stage5-design_RELEASE_NOTES.md) |
| v3.20 | v3.20-release-corpus-verification-surface | 2026-06-07 | Release-corpus verification surface (real checks, not grep proxies) — the two corpus tools rewired to the live `core/`/`release/` layout with deploy-checks made fail-loud (no more green-on-broken); bundle-issues parser to ≥90% combined-clean on the conformant-bundleable substrate; release-executor on live-taxonomy labels only; deploy-check class exits non-zero on path-resolution failure | #496 | [notes/v3.20-release-corpus-verification-surface_RELEASE_NOTES.md](notes/v3.20-release-corpus-verification-surface_RELEASE_NOTES.md) |
| v1.06 | v1.06-solutioning-and-related | 2026-06-06 | Pipeline-discipline capability slice — the platform's first fix-now/defer/accept finding-disposition framework for Dev Testing + QA, single-sourced via an Option-C extraction to a shared standard; plus a Stage-5 design-template BLOCK-DESTRUCTIVE-022 guard for new in-tree CLIs (#31 deferred to v1.22) | #449 | [notes/v1.06_RELEASE_NOTES.md](notes/v1.06_RELEASE_NOTES.md) |
| intake-elicitation-skill (version-less) | intake-elicitation-skill | 2026-06-06 | Conversational intake front door (`intake-desk` — meets an idea at any altitude, proposes the work-item type and its place in the intake hierarchy with re-route on reclassification, elicits type/level-appropriate fields, applies the 5-test rule live, and logs a well-formed typed item to the work tracker — never a scratch file; MVP over the current three types via a parameterization seam; intake-front-door architectural boundary recorded in ADR-016) | #424 | [notes/intake-elicitation-skill_RELEASE_NOTES.md](notes/intake-elicitation-skill_RELEASE_NOTES.md) |
| v1.05 | v1.05-planning-and-related | 2026-06-05 | Pipeline-discipline capability slice (design-artifact pilot + centralized `core/diagrams/` location via ADR-015; reusable 6-form ref-form checklist + gated design-review check; release-plan frontmatter backfill complete) | #401 | [notes/v1.05-planning-and-related_RELEASE_NOTES.md](notes/v1.05-planning-and-related_RELEASE_NOTES.md) |
| public-flip-install-blockers (version-less) | public-flip-install-blockers | 2026-06-04 | Fresh-install & onboarding blockers gating the private→public flip (zero-skills fresh-install fix; no-Cowork deploy; config-first install-path resolver via ADR-013; honored sandbox overrides; two-hash managed-section tamper detection via ADR-014; wired no-change update exit; validate-install false-positive repairs; deploy `--check/--report` fixes; SKILL_LIST + `.version` reconciliation; deploy-test harness hygiene; repaired-and-wired version-skew notifier) | #627 | [notes/public-flip-install-blockers_RELEASE_NOTES.md](notes/public-flip-install-blockers_RELEASE_NOTES.md) |
| v3.19 | v3.19-close-out-reliability-backstops | 2026-06-03 | Stage 13 close-out reliability backstops (release-corpus completeness gate; outcome-bound mandatory-close mandate + documented merge-ahead path; CI smoke gate for the close-out tooling; schema-aware slug + locked-Keychain degradation; working orphan-branch `--apply` with chore-branch scope + stale-ref prune + post-apply verify) | #588, #591, #592, #598 | [notes/v3.19-close-out-reliability-backstops_RELEASE_NOTES.md](notes/v3.19-close-out-reliability-backstops_RELEASE_NOTES.md) |
| v3.18 | v3.18-corpus-integrity-enforcement | 2026-06-03 | Corpus-integrity enforcement (reference-durability standard + three-primitive enforcement spine; three repository-integrity PR gates — depersonalization, issue-reference validity, dead-file references) | #562 | [notes/v3.18-corpus-integrity-enforcement_RELEASE_NOTES.md](notes/v3.18-corpus-integrity-enforcement_RELEASE_NOTES.md) |
| v1.04 | v1.04-planning | 2026-06-02 | Stage-4 planning discipline (mandatory currency + crisping gates, release-plan versioning protocol, analysis-class methodology-design Stage 5 persona variant via ADR-011) | #540 | [notes/v1.04-planning_RELEASE_NOTES.md](notes/v1.04-planning_RELEASE_NOTES.md) |
| v1.03 | v1.03-bundle-and-related | 2026-06-02 | Bundle protocols (domain best-practice sourcing-or-flag step, standing milestone Parallelization Map, cascading-rebundle protocol) | #515 | [notes/v1.03-bundle-and-related_RELEASE_NOTES.md](notes/v1.03-bundle-and-related_RELEASE_NOTES.md) |
| v1.02 | v1.02-triage-and-related | 2026-06-02 | Triage/Bundle protocol codification (triage cadence, observation pattern-review cadence + Mode D/G split, roadmap-to-bundle cascade gate G3-13, management-task identification A6.5) | #493 | [notes/v1.02-triage-and-related_RELEASE_NOTES.md](notes/v1.02-triage-and-related_RELEASE_NOTES.md) |
| v1.01 | v1.01-intake | 2026-06-01 | Pipeline-discipline capability slice (intake symmetry, signed-annotated tag, mid-pipeline divergence, temporal-AC, AC-drift) | #460 | [notes/v1.01_RELEASE_NOTES.md](notes/v1.01_RELEASE_NOTES.md) |
