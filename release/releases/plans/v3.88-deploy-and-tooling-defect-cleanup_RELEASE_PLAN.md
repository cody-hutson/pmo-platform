<!-- repo-integrity: allow-issue-ref -->
# v3.88 Release Plan — deploy-and-tooling-defect-cleanup

Milestone: deploy-and-tooling-defect-cleanup (#281)
Version: v3.88 (recorded determination — next-free, bump-class MINOR; binds atomically at the Stage-12 tag-claim)
Release class: routine · Bump class: MINOR · Reversibility: MODERATE / HIGH (CHEAP for five of six; MODERATE only for #2232 path-a)
Topology: D-C SINGLE — one release branch, one PR, one merge (plan lands as Engineering Commit 0)
Identity: versioned
Scope: 6 issues · no hard dependency edges · one soft file adjacency · P0 fully-serial

> **Engineering Commit-0 version re-verify (2026-07-24, Friday).** Recomputed next-free for
> bump-class MINOR against **origin tags** (not the RELEASE_LOG tail — the ledger lags because a
> shipped version's row lands in a separate chore PR). `git fetch --tags origin && git fetch origin main`
> at `origin/main = 03f2450`: highest tag `v3.85`, fully claimed (tag `v3.85` +
> `v3.85-close-out-hardening-wave-2_RELEASE_PLAN.md` + a VERIFIED/DEPLOYED RELEASE_LOG row). `v3.88`
> has **no tag, no plan file, and no release branch** → unclaimed and equal to next-free ⇒ **PROCEED**. `[SOURCE]`
>
> **Version lineage.** The Stage-4 plan comment and Plan Addendum recorded provisional **v3.85**;
> that version **shipped 2026-07-24** (tag `v3.85`; RELEASE_LOG DEPLOYED row via the Stage-12 chore PR).
> The hub-determination comment on the Stage-4 planning sub-task corrected the provisional to **v3.88**
> (rule-computed next-free). This Commit-0 re-verify independently confirms v3.88 is next-free. The binding
> claim remains the Stage-12 atomic ref-CAS; re-version-up is expected under parallel release work.

## Summary (30 seconds)

Six independent, small, CHEAP-reversible deploy/tooling/test defect fixes — no dependency edges, no hard
file contention, no cross-PR contention. All six reproduce LIVE on `main`; every hub-supplied line-ref
correction verified accurate. Recommended sequence by ascending blast-radius:
**#2341 → #3210 → #2439 → #3578 → #3722 → #2232**. Stage 5 Solutioning activates for **#2232 only** (genuine
path-a-vs-b design choice, issue-flagged "do not pre-decide"); the other five SKIP Stage 5. **#2341 SKIPs
Stage 7** (docs-only, no behavior change). One Cross-Issue Acceptance Criterion (deploy-check-surface
consistency, spanning #2439 × #3578). Quota Budget: **PASS**. No merge/split within the six. The six ship
as one PR / one merge (single-branch); issues are marked closed at Stage 13 via the block-close protocol
(the PR body does not auto-close them).

**Scope note (#3722 admitted).** The Stage-4 plan was authored against **five** deploy/test defects
(#2439 · #3578 · #2232 · #3210 · #2341). A sixth `status: bundled` member — **#3722** (eval_review.html DOM
refactor / CodeQL `js/xss-through-dom` hardening) — was surfaced by the plan's audit-baseline finding as
bundled AFTER the committed scope was frozen. The operator rendered the scope fork at the plan-approval gate
→ **Admit #3722**; the milestone Release Outcome Statement / Scope / Parallelization-Map were amended
(2026-07-24) to name #3722. Scope is now **six**. This plan integrates #3722 as an authoritative slice.

## Dependency Graph

Directional edges — **none**. All six are independent tooling/asset defects (consistent with the milestone
Parallelization Map: "Tier-A clean — independent tooling fixes; no cross-milestone dep or shared surface").

```
#2341  canonical-skill-structure.md        (no edges — docs-only)
#3210  test_sandbox_roots.sh               (no edges)
#3722  eval_review.html + .skill rebuild   (no edges — disjoint file)
#2232  update.sh / manifest / CLAUDE.md.template   (no edges)

#2439  deploy.sh  ──edits region R1 (injected_ref_basenames / Check 1+13)──┐
                                                                            ├─ shared file: core/deploy/deploy.sh
#3578  check-skill-count-imp.py  ──READS region R2 (roster arrays)─────────┘   (soft read/write adjacency, NOT a dep edge)
```

- **No hard dependency edges.** No issue's fix is a prerequisite for another's.
- **One soft adjacency:** #2439 *edits* `deploy.sh` (the `injected_ref_basenames()` / Check-1/Check-13
  reference-drift region, ~L842-864); #3578's fix *reads* `deploy.sh` (parses the
  `OPERATIONS_SKILLS`/`RELEASE_SKILLS`/`CORE_SKILLS` arrays, ~L40-70) but per its AC edits only
  `check-skill-count-imp.py`. Different regions; #2439 does not touch the arrays. No write-write conflict →
  sequencing is a soft preference (build #2439 before #3578 so #3578 parses post-#2439 `deploy.sh`), not a
  dependency.

## Implementation Sequence

Single-branch topology: six delivery slices on **one** release branch, built in the order below, **one** PR /
one merge gate (per the milestone-one-PR discipline). Order is by ascending blast-radius so low-risk changes
accumulate first and the M-sized #2232 lands last where it gets the most review scrutiny.

| # | Issue | Size | Stage 5? | Slice intent | Why here |
|---|---|---|---|---|---|
| 1 | **#2341** | S (docs) | skip | Correct singular `reference/` → `references/` subdir name in canonical-skill-structure.md (spec-defining occurrences L77, L112, L179) | Trivial, zero behavior/gate impact; warms the branch + authors this plan as Engineering Commit 0 |
| 2 | **#3210** | XS | skip | Make `test_sandbox_roots.sh` line-63 `printf\|grep\|head` SIGPIPE-safe (audit L94 for the same idiom) | Smallest functional fix, fully isolated |
| 3 | **#2439** | S | skip | Make `deploy.sh --check` credit nested `references/templates/` injections (fix `injected_ref_basenames()` / Check-13 reference-drift, L842-864) | Deploy-tooling logic; before #3578 (soft deploy.sh adjacency) |
| 4 | **#3578** | S | skip | Derive `check-skill-count-imp.py` roster from the deploy.sh arrays (parse, not filesystem walk) | Adjacent to #2439 (both deploy-check integrity); reads post-#2439 deploy.sh |
| 5 | **#3722** | S | skip* | eval_review.html row render → scanner-verifiable DOM construction; drop 2 `nosemgrep` suppressions (L88, L106) | Independent (disjoint file); among the S-sized fixes, before the M-sized #2232 |
| 6 | **#2232** | M | APPLY | Reconcile `update.sh` regen-vs-marker contract for CLAUDE.md (path a: register as composition surface / path b: drop the false marker) | Largest surface + only design-latitude slice → last, most scrutiny |

\* **#3722 Stage-5 = SKIP:** the refactor target is prescribed (build rows via DOM APIs + `textContent` rather
than assigning an escaped HTML string to the row element) — a known-pattern hardening, not a path-a/b design
fork like #2232. If the Engineering spoke finds genuine idiom latitude (e.g. keep `escapeHtml()` as
defense-in-depth vs pure DOM APIs), it escalates to a LIGHT Stage-5 note; not gated at entry.

## Stage Applicability Matrix

Stages 6 (Engineering), 9 (Plan Review), 12 (Execute), 13 (Close) apply once at release level for all six.
Per-issue applicability for the decision-relevant stages 5/7/8 (routine class → Stage-5 bias SKIP-where-trivial;
skip 7/8 only if no functional impact):

| Issue | S5 Solutioning | S7 Dev Testing | S8 Acceptance | Evidence |
|---|---|---|---|---|
| **#2341** | **SKIP** | **SKIP** | LIGHT | Docs-only; "documentation-only correction; no behavior or gate change." No functional impact → S7 skips cleanly. S8 = grep-verifiable AC (sections say `references/`; matches check) — foldable into Stage-9 PR review. |
| **#3210** | **SKIP** | APPLY | LIGHT | Well-understood SIGPIPE fix (no design choice). It IS a test → S7 verifies determinism (run the suite repeatedly, no broken-pipe). S8 light (AC = deterministic pass). |
| **#2439** | **SKIP** | APPLY | APPLY | Fix well-understood (recurse into / credit nested `references/` per injection map). Functional impact on `--check` output → S7 must verify no false-positive AND no masking of real drift. |
| **#3578** | **SKIP** | APPLY | APPLY | Fix pre-ratified by ADR-008 + registry.md; the compliant sibling `check-canonical-structure.sh` is the exact pattern to copy → no design uncertainty. Functional (roster parse); AC mandates a falsification test → S7/S8 apply. |
| **#3722** | **SKIP*** | APPLY | APPLY | Functional render change → S7 verifies the review page still renders rows correctly AND a local scanner pass (semgrep/CodeQL) is clean. S8 grades the DOM-construction AC. **Final CodeQL-alert-closed verification is Stage 13** (next `main` scan post-merge — see R8). |
| **#2232** | **APPLY** | APPLY | APPLY | **Only Stage-5 activation.** Genuine design choice: path (a) register CLAUDE.md as a composition surface (HTML-comment markers + `[OPERATOR_*]` token substitution) vs path (b) remove the false "regenerated by update.sh" marker + name the true refresh path; issue explicitly says "do not pre-decide the fix." Adjacent settings.json case decided here too. |

Stage-5 activation count: **1** (#2232). Stage-7 parallel-eligible: **5** (#3210, #2439, #3578, #3722, #2232).
Stage-8 parallel-eligible: **5-6** (#2341 light/foldable).

## Contention Map

**Within-release contention: NONE hard.** No file is *edited* by two issues. The only shared file is
`core/deploy/deploy.sh` — edited by #2439 (reference-drift region), read-only by #3578 (arrays region);
different regions, no write-write conflict. Under single-branch topology both are sequential commits on one
branch regardless.

**Cross-PR contention: NONE.** The Stage-4 baseline pin was `origin/main @ f531b9b` (2026-07-24); the only open
PR at pin time (a Stage-13 corpus chore) had zero overlap with any #281 file. The v3.85 close-out has since
merged (base advanced to `03f2450`) touching only RELEASE_LOG/INDEX/DIGEST/NOTES/CHANGELOG — **no overlap** with
any of the six defect files. Anything merging after this Commit-0 base but before Stage 12 is caught by the
Stage 7/8 entry re-checks + Stage 9 release-integration check + Stage 12 pre-merge freeness re-verify.

**File Change Matrix (machine-readable — one path per line; Stage 7/8/9 chips extract this block):**

```
core/deploy/deploy.sh
core/deploy/tools/check-skill-count-imp.py
core/deploy/tests/test_sandbox_roots.sh
core/standards/canonical-skill-structure.md
release/skills/pmo-skill-refiner/assets/eval_review.html
packages/pmo-skill-refiner.skill
core/deploy/composition-surface-manifest.sh
update.sh
core/CLAUDE.md.template
docs/scripts/setup-workspace.sh
core/standards/composition-surface-spec.md
core/settings.json.template
```

Per-path intent (add/edit/delete) and issue ownership:

| Path | Intent | Issue | Notes |
|---|---|---|---|
| `core/deploy/deploy.sh` | edit | #2439 | `injected_ref_basenames()` L842-864 (nested `references/*/*` skip at L860) + Check-13 reference-drift path. **Reconcile the in-code comment L849-851** that currently asserts the skip is intentional. |
| `core/deploy/tools/check-skill-count-imp.py` | edit | #3578 | Replace filesystem directory-walk (docstring L20-42) with a parse of the deploy.sh roster arrays. |
| `core/deploy/tests/test_sandbox_roots.sh` | edit | #3210 | Line 63 `printf … \| grep … \| head -1` SIGPIPE (and audit L94 for the same idiom). |
| `core/standards/canonical-skill-structure.md` | edit | #2341 | L77, L112, L179 only (singular `reference/` subdir name → `references/`). Preserve genuine prose uses (L118/L122/L125/L127 already plural). |
| `release/skills/pmo-skill-refiner/assets/eval_review.html` | edit | #3722 | Row render escaped-string templating → DOM construction (`createElement`/`textContent`); remove the two `nosemgrep: dom-htmlinjection-sink-unreviewed` suppressions (L88, L106) once the sink is structurally scanner-verifiable. |
| `packages/pmo-skill-refiner.skill` | rebuild | #3722 | Repackage at release-cut (`build-skill-packages.sh pmo-skill-refiner`) — the asset ships inside the `.skill`. |
| `core/deploy/composition-surface-manifest.sh` | edit (path a) | #2232 | Add CLAUDE.md entry (currently 18 entries, none CLAUDE.md). Path-b: no change. |
| `update.sh` | edit (path a) | #2232 | Sole regen loop at L220 `for entry in "${COMPOSITION_SURFACE_FILES[@]}"`. Repo-root path (NOT `core/deploy/update.sh`). Path-a: teach it the CLAUDE.md marker+token surface type. |
| `core/CLAUDE.md.template` | edit | #2232 | Path-a: keep marker (now honored). Path-b: drop the false "regenerated by update.sh" claim. |
| `docs/scripts/setup-workspace.sh` | edit (conditional) | #2232 | `substitute_templates()` L968-976 — the actual install-time refresh path; path-b names it in the contract. |
| `core/standards/composition-surface-spec.md` | edit (conditional) | #2232 | Reconcile the surface-spec to whichever path is chosen. |
| `core/settings.json.template` | edit (conditional) | #2232 | Adjacent case (also install-composed, absent from manifest, no false marker) — resolve-or-defer per AC. |

Read-only references (NOT edited): `core/skills/registry.md`, `core/ADRs/ADR-008-*`,
`core/deploy/tools/check-canonical-structure.sh` (the compliant sibling #3578 copies).

## Risk Register

| ID | Class | Risk | Severity | Owner | Mitigation |
|---|---|---|---|---|---|
| R1 | Scope | #3722 bundled after the committed scope was frozen (audit-baseline invalidation). | High → **RESOLVED** | Operator | **Admitted** at the plan-approval gate; milestone Outcome/Scope/Parallelization-Map amended 2026-07-24. Premise re-verified (CodeQL alert OPEN). No longer open. |
| R2 | Scope | #2232 path (a) can pull in `docs/scripts/setup-workspace.sh`, `composition-surface-spec.md`, and the `settings.json` adjacent case → M can creep. | Med | Stage-5 spoke | Stage-5 Solutioning fixes the fix-path + the settings.json resolve-or-defer decision before Engineering; AC is fix-path-agnostic so scope is bounded by the design decision. |
| R3 | Contention | `deploy.sh` shared by #2439 (edit) + #3578 (read). | Low | Sequence | Build #2439 before #3578; different regions, single branch → no merge conflict. |
| R4 | Rollback | #2232 path (a) changes install/update composition behavior (heavier blast radius than the others). | Med | Stage-9 | Reversibility **MODERATE** for #2232-path-a (touches install+update paths); **CHEAP** for path-b and all other five. Single-branch → revert the branch/commit; verify a clean `install.sh`/`update.sh --dry-run` post-revert. |
| R5 | Rollback | #3578 + #2232 ACs mandate **falsification** regression tests (a test that fails against the pre-fix impl) → new/expanded test files. | Low | Stage-6/7 | Confirm at Engineering whether tests are new files vs additions to existing suites; either way stays `routine` (no new file *class*). |
| R6 | Verification | #2439 in-code comment (deploy.sh L849-851) asserts the nested-skip is *intentional*; a logic-only fix leaves the comment contradicting behavior. | Low | Stage-6 | Reconcile-don't-annotate: the #2439 slice must correct that comment alongside the logic. |
| R7 | Verification | Stale in-body line refs across all six issues (cosmetic; hub-flagged). | Low | Stage-6 | Refresh when the slice touches each file (verified corrections: #2439 202-211→220-229; #3578 L52→docstring L20-42; #3210 :53→:63/:94; #2341 69/84/140→77/112/179). Defects — not the numbers — are authoritative. |
| R8 | Verification | #3722 AC (CodeQL `js/xss-through-dom` alert → `fixed`) resolves only on the next scheduled `main` CodeQL scan post-merge → a Stage-13-timed verification dependency, plus a `packages/pmo-skill-refiner.skill` rebuild at release-cut. | Med | Stage-13 | Grade at Stage 13 Close per the issue AC; not a Stage-9 blocker. |
| R9 | Verification | The repo's PreToolUse security-reminder hook fires on the DOM HTML-sink property token; #3722 intermediate edits may trip it. | Low | Stage-6 | The #3722 Engineering spoke must expect it on intermediate edits and land a sink-free end state (DOM APIs + `textContent`); a residual flagged assignment on an untrusted path blocks the edit by design. |

## Cross-Issue Acceptance Criteria

One genuine cross-issue cohesion constraint (traceable to the milestone Success Indicator: "each named
check/tool/test behaves correctly; no false-positive, no roster drift"):

**Cross-Issue Acceptance Criteria**
- [ ] **CIAC-1 (#2439 × #3578 on the deploy-tooling check-integrity surface):** After both slices land, a
  single clean-tree pass shows the deploy skill-roster/drift check surface mutually consistent — `deploy.sh
  --check` exits 0 with **no** project-initiator `references/` false-positive drift, AND
  `check-skill-count-imp.py` agrees with `deploy.sh` Check 5(c) on the **53** public roster (canary excluded).
  Neither issue's own AC asserts the *joint* green state of the two axes on the integrated tree. *Shared
  surface:* the deploy-tooling skill-count + reference-drift check surface (`core/deploy/deploy.sh --check` +
  `core/deploy/tools/check-skill-count-imp.py`). *Method:* `bash core/deploy/deploy.sh --check` (expect exit
  0, no project-initiator drift line) **and** `python3 core/deploy/tools/check-skill-count-imp.py --mode
  count` (expect count == 53 == Check 5(c)). *Graded at Stage 9 on the merged PR.*

(No other CIAC. #2341 [standard-doc naming] and #2439 [--check nested-crediting] both touch the `references/`
convention but on disjoint files/concerns — grading them as one predicate would be forced; kept as separate
per-issue ACs. #3722 is disjoint — no new cross-issue cohesion constraint.)

## Quota Budget

**Verdict:** PASS (per quota-budget-protocol Checkpoint A)
**Parallel-eligible spokes per parallel stage:** Stage 5: 1 · Stage 7: 5 · Stage 8: 5-6
**Per-spoke cost estimate:** size-bucket band — 1×XS + 4×S + 1×M (source: size-bucket heuristic, MEDIUM
confidence; no telemetry medians yet)
**Estimated cumulative draw % (worst parallel batch):** default topology is **D-C SINGLE = P0 fully-serial**
(parallelism is opt-IN) → worst *serial* batch = 1 spoke → draw trivial. Even under opt-in parallel posture,
5-6 small (XS/S/M) spokes estimate **< 50%** of a fresh-to-moderate 5-hour window.
**Routing:** PASS → proceed. Checkpoint B re-validates at every parallel wave (runtime, load-bearing).

## Recommendations

**Release Class declaration.** **`routine`** — CONFIRMED (matches the milestone-declared class). Fires on the
Class-Enum trigger (d) *zero new D-class decisions in the release plan* (only the two standing recurring-Ds,
D-ReleaseClass + D-Version) and substantially (c) *zero new file classes* (edits to existing files; any
falsification-test additions are not a new file class). No `novel` trigger (no new reference doc / schema /
skill — #2341 edits an existing standard, #2232 edits existing manifest/template, #3722 edits an existing skill
asset) and no `cross-cutting` trigger. *Differentiation posture (routine):* Engagement density **Light** ·
Stage 9 depth **Standard** · Stage 5 bias **SKIP-where-trivial** (→ only #2232 activates) · Stage 13
outcome-window **30-day**. **Admitting #3722 does not change the class** — 6 small independent fixes; ≈18 pts
(inside the 15-25 band).

**Domain classification.** `domain_practice: { source: N/A — pipeline-internal release, date: 2026-07-24,
domain: software }`. Dominant domain **software** (deploy/test tooling — shell + python + one HTML asset) with
**governance** secondary (one standards doc `canonical-skill-structure.md`; and CLAUDE.md.template if #2232
path-a). Sourcing-**exempt** (entire matrix is internal pmo-platform artifacts — pipeline-internal release);
still domain-classified per the mandatory `domain:` field.

**Recurring-D determinations (recorded determinations — NOT operator click-gates):**
- **D-Version → recorded determination:** **v3.88** (rule-computed next-free; bump-class = minor; latest
  shipped v3.85). No operator judgment to render; re-verification rungs = Engineering Commit 0 (this file) +
  Stage 12 atomic claim.
- **D-ReleaseClass → recorded determination:** `routine` (evidence above); confirmed at Phase B3 scope-commit.

**Merge/split:** No split (each issue is atomic and small). No merge among the six (independent, disjoint edit
files; merging would blur root-cause traceability). Keep #2439 and #3578 as **adjacent** slices (both
deploy-check integrity) but **separate** commits. The six ship as one PR / one merge (single-branch).

**Out-of-scope discoveries (Recommendations, not this release — route to hub, do not act):**
- `registry.md` reportedly carries stale prose roster counts (per #3578's "Adjacent" note: 19/30/49 vs live
  20/32/52) — not verified here; file separately if confirmed (do not fold into #3578, whose AC is scoped to
  the tool).
- `release/skills/pmo-skill-refiner/eval-viewer/viewer.html` (L923) uses the same escaped-interpolation pattern
  class as #3722 (no CodeQL alert; different taint source) — #3722 explicitly defers it; a candidate for a
  future hardening pass, not this release.

## Change Description

*(Per `release/governance/RELEASE_PROTOCOL.md` § Change Description Protocol. Authored at Engineering Commit 0,
slice 1 of 6; per-issue statuses refresh as subsequent slices land, finalized before the Stage-9 draft→ready
transition. `[SOURCE]` for the #2341 row landed this commit; the remaining five are PENDING and accrue on this
branch.)*

**Outcome.** v3.88 is a deploy/tooling/test defect-cleanup release: it removes six small, independent
correctness defects across the deploy-check surface, the skill-count tool, a sandbox test, a skill standard, a
skill asset's XSS-hardening, and the update-vs-composition contract. No new capability, schema, or skill —
each fix makes an existing check/tool/test/doc behave as already specified. Operator-facing effect: `deploy.sh
--check` stops mis-crediting nested `references/` injections, the skill-count tool derives its roster from one
source of truth, and the canonical skill standard's prose matches the gate it documents.

**Issues resolved.**

| Issue | One-line outcome | Status |
|---|---|---|
| #2341 | canonical-skill-structure.md names the skill source subdir `references/` (plural) consistently — prose matches Check 6 | DONE |
| #3210 | `test_sandbox_roots.sh` SIGPIPE-safe (no spurious broken-pipe failure) | DONE |
| #2439 | `deploy.sh --check` credits nested `references/templates/` injections (no false reference-drift) | DONE |
| #3578 | `check-skill-count-imp.py` derives its roster from the deploy.sh arrays (no roster drift) | DONE |
| #3722 | eval_review.html row render is scanner-verifiable DOM construction; CodeQL `js/xss-through-dom` alert cleared | DONE |
| #2232 | `update.sh` ↔ CLAUDE.md regen-vs-marker contract reconciled (path a/b per Stage 5) | DONE |

**Key decisions.**
- **D-Version = v3.88** — rule-computed next-free (bump-class MINOR; latest shipped v3.85). Re-verified at this
  Commit 0; binds atomically at Stage 12.
- **D-ReleaseClass = routine** — 6 small independent fixes; no new D-class decision, no new file class.
- **#3722 admitted** — operator scope-fork decision at the plan-approval gate; milestone Outcome/Scope amended
  2026-07-24; premise re-verified (CodeQL alert OPEN before admit).

**Reversibility.** **MODERATE / HIGH** overall — CHEAP for five of six (docs/tooling/test/asset edits, revert
the commit); MODERATE only for #2232 path-a (touches install+update composition). Rollback mechanism:
single-branch → `git revert` the release PR or the specific slice commit; verify a clean `install.sh` /
`update.sh --dry-run` post-revert for the #2232 slice.

**Downstream impact.**
- Enables a clean deploy-check surface for the next release (no false reference-drift, no roster drift masking
  real drift).
- #3722 carries a Stage-13-timed CodeQL-alert-closed verification (next `main` scan post-merge) + a
  `packages/pmo-skill-refiner.skill` rebuild at release-cut.
- Carry-forward candidates (not this release): registry.md stale prose roster counts; the eval-viewer
  viewer.html same-pattern-class hardening.

**Cross-references.**
- Release plan: this file (top).
- Milestone: deploy-and-tooling-defect-cleanup (#281).
- User-facing release note (authored at Stage 13): `release/releases/notes/v3.88_RELEASE_NOTES.md`.

## Model Provenance

Engineering Commit 0 authored as **claude-opus-4-8** (Opus 4.8), max reasoning effort, Software Engineer —
Implementation persona (Stage 6). This file transcribes the approved Stage-4 plan (Stage-4 planning comment +
Plan Addendum on the planning sub-task) at the hub-corrected version **v3.88**, integrating the #3722 admit.
The plan content is the operator-approved release plan; this Commit 0 re-verifies the version and lands the
plan on the release branch per the D-C SINGLE topology.

## Re-version Note (2026-07-24)

**Version lineage: v3.86 → v3.87 → v3.88 (two concurrent-claim losses).** Authored against provisional v3.86 → claimed by the concurrent `structure-records-and-review-signals` release (#3797, VERIFIED). Re-versioned to v3.87 → claimed by the concurrent `design-artifact-backfill` release (#3796; #3441/#3798, VERIFIED) during the Stage-12 pre-merge window. This release now takes next-free **v3.88**, claimed atomically at Stage 12 via `claim-version.sh` ref-CAS (authoritative — the actual tag may advance further under contention). Both prior slots shipped unrelated work touching none of this release's source files. The sole merge conflict — `packages/pmo-skill-refiner.skill`, from #3798's concurrent package rebuild vs. this release's #3722 rebuild — was resolved by rebuilding the package from the merged source (main's SKILL.md markers + this release's `eval_review.html` DOM refactor both present). CIAC-1 roster predicate = 53 (agreement).
