# v1.21 Release Plan — deploy-toolchain-defect-cleanup

> **Re-versioned (v1.20 → v1.21)** — this release's Stage-4 gate selected v1.20 as next-free, but during the release's flight two concurrent releases shipped to `main` and consumed the versions this release was tracking: `sior-escalation-discipline` took **v1.19** (#941, tag + plan on `main`) and `health-and-raid-determinism` took **v1.20** (#933, tag + `v1.20_RELEASE_PLAN.md` on `main`). The #88 Stage-6 Engineering chip caught the collision at Commit 0 (fresh `origin/main` fetch) and BLOCKED rather than overwrite the shipped v1.20 plan or silently re-version. The operator re-versioned **v1.20 → v1.21** (next-free; the original next-free rule re-applied to updated facts) on 2026-06-14. The branch name (`release/v1.20-deploy-toolchain-defect-cleanup`) is retained as a legacy/cosmetic identifier per the `health-and-raid` v1.18→v1.20 precedent; the **v1.21 identity** lives in this plan file, the Stage-12 tag (`v1.21`), and the RELEASE_LOG row. Historical commit messages on the branch (`v1.20: #88 …` at `6274882`) are left as-authored. [VERIFIED: git tag v1.19+v1.20 present; `release/releases/plans/v1.20_RELEASE_PLAN.md` = health-and-raid content; RELEASE_LOG v1.19+v1.20 rows present; v1.21 plan/tag FREE]

| Field | Value |
|---|---|
| **Version** | v1.21 |
| **Milestone** | deploy-toolchain-defect-cleanup (#171) |
| **Release Class** | `A` / `routine` (Light engagement / Standard Stage 9 review / Stage 5 activation bias SKIP-where-trivial / 30-day outcome-window) |
| **Branch topology** | D-C SINGLE — one `release/v1.20-deploy-toolchain-defect-cleanup` branch (name legacy/cosmetic; release identity is v1.21), write-serialized, one release PR |
| **Baseline** | `main` re-checked at Engineering Commit 0; the #88 fix landed first at `6274882` (Commit 1 ahead of this plan Commit 0 on the legacy-named branch) |
| **Scope** | Eleven issues: #26, #76, #88, #92, #104, #106, #111, #122, #331, #332, #343 (see Implementation Sequence + Risk Register R-STALE rows) |
| **Rollback** | Single `git revert -m 1` of the merged release PR (additive/corrective on a feature branch; one revertable unit). Reversibility: **CHEAP** |

## Summary

Eleven deploy-toolchain defects, mostly independent, dominated by one serialization surface: **`core/deploy/deploy.sh` is touched by 7 of the 11 issues** (#76, #88, #104, #111, #332, #331-F4, and #26). Under the **D-C SINGLE** topology these Engineering chips serialize on that one file. The release is **Class A `routine`** (all P3/P4 — the lone P2 #88 is a CHEAP-reversibility deploy-diagnostic change; size:S/XS; zero new files; zero new D-class architectural decisions — the deploy.sh checks already exist; this is defect-repair on a high-touch file).

**Critical staleness findings — three issues were re-scoped against the current tree at the Stage-4 gate before any Engineering fired:**

1. **#343 is OBSOLETE — mark as closed at Stage 13 with verification evidence, no code change.** Both literal UUIDs the issue names (`3ad19e63-…` and `0989ef14-…`) are GONE from the entire tree; `core/rules/skill-deployment.md` is fully tokenized to `[SESSION_UUID]`; and **ADR-013 already re-pointed deploy.sh Check 8** away from the doc-UUID-grep mechanism. The issue's entire premise (refresh a stale literal UUID) no longer exists. [VERIFIED grep 3ad19e63/0989ef14 core/ release/ operations/ .claude/ → NOT FOUND] [VERIFIED Check-8 block core/deploy/deploy.sh → ADR-013 re-point comment present]
2. **#26 → use Check 34, NOT Check 28.** #26's body names "Check 28" but Check 28 already exists (Doc-impact resolution at Stage 13 close, deploy.sh:3259). The highest check is **33**; the correct free number for #26's mode-drift check is **Check 34**. The rest of #26's premise holds (`### Model Provenance` block exists at hub-spoke-bridge.md:927; no `### Mode Provenance` equivalent). [VERIFIED grep "Check 28" → exists at :3259 + skill-deployment.md:191; highest=33]
3. **#106 → drop the stale Check-16 framing.** Check 16 has been re-scoped to scan `--label improvement` issues ONLY; it no longer scans `sub-task` issues at all. So #106's stated mechanism ("≥22 open sub-tasks each fire a Check 16 warn-mode emission; AC3 baseline unsatisfiable") is **no longer true** — the scope decision was already made in the exclude-sub-tasks direction. The residual live gap is narrower: the Stage-6 creation-path stamp + Rule 6 clarification are still unimplemented. [VERIFIED Check-16 block → `--label improvement` scope; gh sub-task scan → 12 open, 12 with no status label]

The other 8 issues verify **LIVE** and are real defects (see Risk Register R-STALE rows for the per-issue verification ledger).

---

## Dependency Graph

Directional edges (`A → B` = A should land before B). Most issues are independent defects on disjoint regions; only a few genuine edges exist.

```
#343 ──(OBSOLETE: no edge; close-with-evidence)──> (none)

deploy.sh contention cluster (no hard functional deps, but FILE-serialized — see Contention Map):
   #76  (Check-19 arithmetic)        ─┐
   #88  (references/ RO swallow)      ─┤
   #104 (Check-18 cmd_report parity)  ─┼──> all touch core/deploy/deploy.sh; serialize by merge order, not by logic
   #111 (C15_→C17_ env rename)        ─┤
   #332 (capture [^"#]+ clip)         ─┤
   #331-F4 (DEPLOY_ROOT comment)      ─┘
   #26  (add Check 34 + Mode Prov.)   ───> APPENDS a new check at end-of-file; lowest contention risk in the cluster

#122 (file-router Check 10 trailer)   ──(operator a/b choice RENDERED: option (a))──> self-contained
#92  (blast-radius.sh locale)         ──(independent; different file: release/tools/blast-radius.sh)──> (none)
#106 (sub-task status label)          ──(independent; touches label-taxonomy.md + Stage-6 spec, NOT deploy.sh check-16 logic)──> (none)
#331 (4 LOW findings, 4 files)        ──(F1 update.sh / F2 validate-install.sh / F3 notify-version-skew.sh / F4 deploy.sh)──> F4 only joins deploy.sh cluster
```

**Soft edge — #88 ↔ #343 (session-path coupling, SEVERED).** Both reference the Cowork session-UUID churn class (`skill-deployment.md` "Session instability note"). But since #343 is obsolete (tokenized) and #88's fix is a deploy.sh diagnostic-hardening change (surface an actionable error on read-only `references/` subtree failure) that does NOT depend on the canonical-UUID doc, **there is no live dependency edge.** They share a root-cause *narrative* (session churn leaves orphaned read-only dirs) but no shared file or ordering constraint. [VERIFIED: #88's affected region is the references/ mirror block at deploy.sh:~1173; #343's was a doc-grep that no longer exists.]

**No GitHub native "Blocked by" edges within this milestone.** The roadmap-deps footers list cross-milestone blocks (#76 blocks #237/#238/#242/#243; #88 blocks #172; #106 blocks #112; #332 blocks #260) — all OUTSIDE this milestone, so they do not constrain intra-release sequencing. They are downstream consumers, captured in the Risk Register as "unblocks-on-merge."

---

## Implementation Sequence

Dependency- and contention-ordered. The load-bearing factors are (a) the deploy.sh serialization surface and (b) #88's priority. Sequence assumes **D-C SINGLE**: one `release/v1.20-deploy-toolchain-defect-cleanup` branch (legacy name; v1.21 identity), Engineering chips run strictly serially in this order, each rebasing on the prior.

| Order | Issue | Region of deploy.sh (or other file) | Why this position |
|---|---|---|---|
| **0** | (plan) | `release/releases/plans/v1.21_RELEASE_PLAN.md` | Engineering Commit 0 — this plan file (SINGLE topology). Authored after the #88 fix landed (the re-version blocker delayed Commit 0; see header). |
| **1** | **#88** | references/ mirror block (~deploy.sh:1163–1183) | Highest priority. Distinct region (deploy block, not a check). Landed first (commit `6274882`) so the diagnostic-hardening is in the tree before the check-region edits pile up. |
| **2** | **#76** | Check-19 block (deploy.sh:2424/2426/2435) | Self-inflicted arithmetic crash currently makes `--check` exit 1 — fixing it early restores a clean `--check` baseline that every subsequent chip can verify against. Distinct region (Check 19). |
| **3** | **#332** | capture pattern `[^"#]+` at 4 sites (deploy.sh:106, 393, 734, 1680) | Touches 4 scattered sites incl. detect_install_path + Check-8 readers. Land after #88/#76 so its multi-site edit rebases onto a clean check baseline. Distinct from the check-region edits. |
| **4** | **#111** | Check-17 env var (deploy.sh:2253/2261/2262) | Env-var rename `C15_*`→`C17_*` inside Check 17 region. Independent region; pure rename (+ optional shim per D-#111). |
| **5** | **#104** | `cmd_report` function (deploy.sh:~3831–3920, add a Check-18 block) | Additive report block in the cmd_report function — distinct region from all check-region edits above. Append-pattern (low conflict). |
| **6** | **#331** (4 findings) | F1 `update.sh`, F2 `docs/scripts/validate-install.sh`, F3 `core/hooks/notify-version-skew.sh`, F4 `core/deploy/deploy.sh:78` comment | F1/F2/F3 are non-deploy.sh files (zero contention). F4 is a 1-line comment fix at deploy.sh:78 (DEPLOY_ROOT) — trivial, lands late to avoid churning the file mid-sequence. |
| **7** | **#26** | APPEND new **Check 34** at end-of-checks + edit hub-spoke-bridge.md Procedure 3 Spoke Template (add `### Mode Provenance`) | Appends a brand-new check (lowest merge-conflict risk — end of file) + a doc edit in a different file. Land LAST so the new check number (34) is stable after all other check-region edits settle. **Uses Check 34, NOT Check 28.** |
| **8** | **#106** | label-taxonomy.md Rule 6 + Stage-6 sub-task-creation spec | NOT a deploy.sh edit (Check 16 already excludes sub-tasks). D-1 = BOTH (Rule 6 states mirror-at-creation AND Check-16-exclusion); D-2 = Option 1 (mirror the parent's current status label at scaffold time). Independent of the deploy.sh cluster. |
| **9** | **#122** | `operations/skills/file-router/SKILL.md` (D-#122 = option (a)) | Operator rendered **option (a)** — pmo-skill-editor Mode A no-op commit on file-router; the resulting commit carries the trailer; Check 10 passes. Sequence-independent of the deploy.sh cluster. |
| **parallel-safe** | **#92** | `release/tools/blast-radius.sh` (the `sort` in the dependency-graph pipeline, ~line 761) | Zero deploy.sh contention — different file. Can be engineered in parallel with the deploy.sh cluster, or slotted anywhere under SINGLE. One-line `LC_ALL=C sort` fix. |
| **close-with-evidence** | **#343** | (no code change) | OBSOLETE — verify the tree is tokenized + ADR-013 re-point holds, attach evidence, mark as closed at Stage 13. |

**Sequencing rationale (Localization Check, M1):** The serialization order is driven by *current* deploy.sh line regions verified at Stage 4, not by the issue bodies' stale line hints (e.g., #76 says "~line 2216" but the live defect is at 2424/2426/2435; #111 says "≈L2060-2061" but the live sites are 2253/2261/2262). Sequencing on verified-live regions, not issue-body hints. [VERIFIED grep per issue, against HEAD of the worktree at Stage 4]

---

## Stage Applicability Matrix

Stages 5–13. Default = all apply. Skips justified per issue. (Stage 5 Solutioning skipped only when trivial; Stages 7–8 Dev-Testing/QA skipped only when no functional impact.)

| Issue | S5 Solutioning | S6 Eng | S7 DevTest | S8 QA | S9 PlanReview | S10–13 | Skip justification |
|---|---|---|---|---|---|---|---|
| **#76** | SKIP | YES | YES | YES | YES | YES | Trivial mechanical fix (idiom `c=$(grep -cE … 2>/dev/null); c=${c:-0}`); functional impact (Check-19 verdict) → DT/QA required. |
| **#88** | **APPLY** | YES | YES | YES | YES | YES | HOW is a genuine design choice (pre-flight writability probe vs. detect-and-report-on-rm-failure vs. cause-classified DRIFT). Functional → DT/QA. (DONE at `6274882`.) |
| **#92** | SKIP | YES | YES | YES | YES | YES | Trivial (`LC_ALL=C` on one sort); but functional (script crashes today) → DT/QA required (verify exit 0 on non-ASCII + ASCII regression). |
| **#104** | SKIP | YES | YES | SKIP-eligible | YES | YES | Additive `cmd_report` block, no `cmd_check` behavior change. DT verifies `--report` emits Check-18 evidence; QA optional (no judgment surface — pure parity). |
| **#106** | **APPLY** | YES | YES | YES | YES | YES | The scope decision (D-1 BOTH / D-2 Option 1) IS a Stage-5 determination per the issue. Functional (creation-path stamp). |
| **#111** | SKIP | YES | YES | SKIP-eligible | YES | YES | Cosmetic rename; the only judgment is D-#111 (A/B/C) rendered at the gate. DT verifies env var works under new name (AV-7 reproducer). QA optional. |
| **#122** | SKIP | YES | SKIP | SKIP | YES | YES | No functional/code impact (option (a) = no-op Mode-A commit). Pure hygiene. DT/QA N/A — verification is "Check 10 returns clean on file-router." |
| **#331** | SKIP | YES | YES (F1/F2 behavioral) / SKIP (F3/F4 comment-only) | partial | YES | YES | F3 + F4 are comment/docstring-only (no runtime impact) → DT/QA skip for those two. F1 (update.sh exit-code) + F2 (validate-install.sh A9 deploy-root) are behavioral → DT required. |
| **#332** | SKIP | YES | YES | SKIP-eligible | YES | YES | Regex fix is mechanical; functional (path clip) → DT required (verify `#`-bearing quoted path passes through). QA optional. |
| **#343** | SKIP | **SKIP** | **SKIP** | **SKIP** | (verify) | S13 only | **OBSOLETE — no engineering.** Verification-only: confirm tokenization + ADR-013 re-point, attach evidence, mark as closed at Stage 13. (Per intake-pre-rendered-artifacts discipline: all-AC-already-satisfied → invert to verify+evidence+closure, never re-author.) |
| **#26** | **APPLY** | YES | YES | YES | YES | YES | New check (Check 34) + new Spoke-Template block + multi-mode-skill audit = design surface (warn-mode posture, mode-enum format, which skills are multi-mode). Functional → DT/QA. |

**Doc-only / data-only skip candidates assessed:** #343 (doc-only AND obsolete → S5–S8 all skip). #122 (hygiene-only → S5,S7,S8 skip). #106's *operational backfill* portion (label data, not code) is auto-write-class and skips S7/S8 for the backfill action itself, but its *creation-path stamp* (code) keeps S7. #331-F3/F4 (comment-only) skip S7/S8.

---

## Contention Map

**`core/deploy/deploy.sh` is the dominant serialization surface — 7 of 11 issues touch it.** Mapped to precise regions (verified at Stage 4) so SINGLE-topology Engineering chips can be ordered to minimize rebase conflicts:

| Issue | deploy.sh region (verified line[s]) | Function / Check | Conflict class with siblings |
|---|---|---|---|
| **#88** | ~1163–1183 (references/ mirror block; the `rm -rf "$INSTALL_PATH/$skill/references" 2>/dev/null \|\| true` at :1173) | `deploy_skill` references mirror | Isolated region (deploy block). LOW conflict. |
| **#76** | 2424, 2426, 2435 (`c19_log_rows`/`c19_write_lines`/`c19_header_ok` via `grep -cE … \|\| echo 0`) | Check 19 | Isolated (Check-19 block). LOW. |
| **#332** | 106, 393, 734, 1680 (`sed -E 's/.*=…([^"#]+)"?.*/\1/'`) | detect_install_path readers + Check-8 reader + a 4th reader | **4 scattered sites** — :1680 is in Check-8 region, :106/:393/:734 in path-reader helpers. MEDIUM (touches Check-8 area; no overlap with #76/#111 check blocks). |
| **#111** | 2253, 2261, 2262 (`C15_THRESHOLD_OVERRIDE_FILE`) | Check 17 | Isolated (Check-17 block). LOW. |
| **#104** | ~3831–3920 (add a block inside `cmd_report`, parallel to the Check-16 block at :184 and Check-17 block at :207 of the function) | `cmd_report` | Isolated (report function, not check function). LOW. |
| **#331-F4** | 78 (`DEPLOY_ROOT="${PMO_PLATFORM_DEPLOY_ROOT:-$HOME}"` comment) | top-of-file | 1-line comment. LOW, but lands late to avoid mid-sequence churn. |
| **#26** | APPEND new Check 34 after Check 33 (current end at :3731+) | new check | Append-only at end-of-file. LOWEST conflict. Lands last so the number 34 is stable. |

**Non-deploy.sh files (zero cross-contention):**

| File | Issue(s) | Region |
|---|---|---|
| `release/tools/blast-radius.sh` | #92 | the `sort` in the dependency-graph pipeline (~line 761) |
| `release/references/how-to/hub-spoke-bridge.md` | #26 | Procedure 3 Spoke Template (add `### Mode Provenance` near the `### Model Provenance` block at :927) |
| `operations/skills/file-router/SKILL.md` | #122 | per operator a/b choice — RENDERED option (a): Mode-A no-op commit |
| `core/specs/label-taxonomy.md` + Stage-6 sub-task-creation spec | #106 | Rule 6 (:113) + creation-path stamp |
| `update.sh` (repo root) | #331-F1 | EX_NOCHANGE exit-code decision (~:380) |
| `docs/scripts/validate-install.sh` | #331-F2 | `check_a9_skill_roster` `skills_dir="${HOME}/.claude/skills"` (~:657) |
| `core/hooks/notify-version-skew.sh` | #331-F3 | docstring spec xref (line 9) |

**Contention verdict:** Under **SINGLE** the deploy.sh cluster (#88, #76, #332, #111, #104, #331-F4, #26) MUST serialize — sequence per Implementation Sequence above. The four deploy.sh *check regions* touched (Check 19 / Check 17 / Check 8 / cmd_report) are mutually disjoint, so the only real rebase risk is #332's 4 scattered sites overlapping a sibling's edit window; the sequence places #332 third (after #88/#76 settle, before #111/#104) precisely to bound that. **Hub-surfaced ordering note:** `cmd_report()` (~3831) is touched by #88 (:3878), #104 (Check-18 report block), and possibly #26 (Check-34 row); the sequence #88 → #104 → #26 orders them so the rebase stays clean. #92, #122, #106, #331-F1/F2/F3 carry **zero** deploy.sh contention and are parallel-safe.

---

## File Change Matrix

Per-file add/edit/delete intent with owning issue(s). (Intent = what the change does; HOW for #88/#106/#26 is Stage-5-determined.)

| File | Intent | Issue(s) | Notes |
|---|---|---|---|
| `core/deploy/deploy.sh` | EDIT | #88, #76, #332, #111, #104, #331-F4, #26 | 7-issue serialization surface. Regions: references-mirror (#88, DONE at `6274882`), Check-19 (#76), capture-pattern x4 (#332), Check-17 env var (#111), cmd_report (#104), DEPLOY_ROOT comment (#331-F4), new Check 34 append (#26). |
| `release/tools/blast-radius.sh` | EDIT | #92 | Pin `LC_ALL=C` on the failing `sort` (~line 761). |
| `release/references/how-to/hub-spoke-bridge.md` | EDIT | #26 | Add `### Mode Provenance` block to Procedure 3 Spoke Template. **Governance/reference file — PR diff IS the dry-run gate.** |
| `core/specs/label-taxonomy.md` | EDIT | #106 | Clarify Rule 6 "inherit" contract — D-1 BOTH: mandatory mirrored label at creation AND sub-tasks excluded from Check 16. **Governance/spec file.** |
| Stage-6 sub-task-creation spec (`release/references/pipeline/stage-06-engineering.md` and/or hub-spoke-bridge scaffolding) | EDIT | #106 | Stamp parent `status:` label on new sub-tasks at creation — D-2 Option 1: mirror parent's current status at scaffold time. Confirm exact path at Stage 5. |
| `update.sh` | EDIT | #331-F1 | EX_NOCHANGE decision also considers whether Phase-5 redeploy deployed anything. |
| `docs/scripts/validate-install.sh` | EDIT | #331-F2 | A9 honor `PMO_PLATFORM_DEPLOY_ROOT` deploy-root override. |
| `core/hooks/notify-version-skew.sh` | EDIT | #331-F3 | Comment-only: fix dangling "composition-surface-spec.md §6" xref (line 9). |
| `operations/skills/file-router/SKILL.md` | EDIT | #122 | Mode-A no-op commit to emit trailer — D-#122 RENDERED option (a). |
| `core/rules/skill-deployment.md` | NO CHANGE | #343 | OBSOLETE — file already tokenized; verification-only, no edit. |

**Machine-readable path list (downstream Stage 7/8/9 chip extraction — one path per line):**

```
core/deploy/deploy.sh
release/tools/blast-radius.sh
release/references/how-to/hub-spoke-bridge.md
core/specs/label-taxonomy.md
release/references/pipeline/stage-06-engineering.md
update.sh
docs/scripts/validate-install.sh
core/hooks/notify-version-skew.sh
operations/skills/file-router/SKILL.md
```

> Note: D-#122 RENDERED option (a), so `operations/skills/file-router/SKILL.md` ships and `core/config/allowlists/skill-editor-exemption-list.txt` does NOT (option (b) not taken). `release/references/pipeline/stage-06-engineering.md` is the #106 creation-path-stamp candidate (confirm exact file at Stage 5). `core/rules/skill-deployment.md` is intentionally absent (#343 = no change).

---

## Risk Register

| ID | Risk | Severity | Owner | Mitigation |
|---|---|---|---|---|
| **R-1** | **deploy.sh concurrent-edit collision.** 7 issues edit one file; under SINGLE, two Engineering spokes on the same release branch in parallel cause rebase/staging races. | HIGH (likelihood) / LOW (impact if serialized) | Hub | **Serialize** the deploy.sh cluster strictly per Implementation Sequence; run ONE Engineering spoke at a time on `core/deploy/deploy.sh`; each chip rebases on prior. Do NOT launch two-or-more deploy.sh chips concurrently. |
| **R-2** | **#332 multi-site edit (4 scattered sites) overlaps a sibling's edit window**, incl. the Check-8 reader at :1680. | MEDIUM | Hub / #332 spoke | Sequence #332 third (after #88/#76 settle; before #111/#104). #332's sites are in path-readers + Check-8; siblings touch Check-19/Check-17/cmd_report — disjoint, but verify rebase cleanliness after #332 lands. |
| **R-3 (rollback)** | **No rollback strategy = release-blocker per Stage-4 anti-pattern.** | — | — | **Rollback = `git revert -m 1 <release-merge-SHA>` (D-C SINGLE single-PR convention).** All 11 changes are additive/corrective on a feature branch; the release PR is one revertable unit. Reversibility: **CHEAP** (repo-level — single revert restores deploy.sh + blast-radius.sh + all doc/spec edits). Runtime: re-run `./deploy.sh --deploy` at reverted HEAD if any skill mirror was refreshed (only #122 option-a touches a skill). #106 label backfill (if done) is independently reversible via `gh issue edit`. Per-issue CHEAP reversibility confirmed in #88/#104/#106/#332 bodies. |
| **R-STALE-#343** | Acting on #343 as written would author a UUID refresh into a tokenized file — **theater.** | HIGH (avoided) | Gate | [VERIFIED] both UUIDs absent tree-wide; ADR-013 re-pointed Check 8. **Mark as closed at Stage 13 with verification evidence; zero code change.** |
| **R-STALE-#26** | #26 says "add Check 28" but Check 28 exists; building it as Check 28 would clobber the doc-impact check. | HIGH (avoided) | #26 spoke | [VERIFIED] highest check = 33. **Use Check 34.** Rest of premise (Mode Provenance gap) holds. |
| **R-STALE-#106** | #106's "Check 16 fires on 22-plus sub-tasks" framing drives an unneeded backfill + a Check-16-scope edit that's already made. | MEDIUM | Stage 5 | [VERIFIED] Check 16 scoped to `--label improvement`; sub-tasks excluded. Re-scoped to (a) Rule-6 clarification (D-1 BOTH) + (b) Stage-6 creation-path stamp (D-2 Option 1); backfill of the 12 live no-status sub-tasks is OPTIONAL hygiene, not a Check-16 unblock. AC3 ("Check 16 baseline 0") is already structurally satisfiable. Do NOT backfill the 330 closed no-status sub-tasks (archaeology; no live consumer). |
| **R-4** | **#122 blocks its own Triage** — needs operator a/b BEFORE Engineering. Building either path without the decision is rework. | MEDIUM (resolved) | Operator (D-#122) | D-#122 RENDERED **option (a)** (Mode-A no-op commit). file-router IS migrated (`skill_discipline_migrated_v10_2: true`), so option (a) is viable. [VERIFIED] |
| **R-5** | **Stale line-number hints in issue bodies** (#76 "~2216", #111 "≈L2060-2061") mislead a spoke that doesn't re-verify. | MEDIUM | Eng spokes | This plan supplies VERIFIED-live regions (#76 → 2424/2426/2435; #111 → 2253/2261/2262). Chips cite plan regions, not body hints. |
| **R-6** | **#88 fix incomplete** — a bounded-target guard (D5) was already added at deploy.sh:1170–1171 (public-flip work), but the core `2>/dev/null \|\| true` silent-swallow at :1173 remained. | RESOLVED | #88 spoke | [VERIFIED] AC-1 was UNMET at Stage 4; **#88 fix landed at `6274882`** — a shared `remove_mirror_subtree` helper replaces the silent swallow at both mirror sites (chmod -R u+w self-heal of read-only orphans, then guarded conditional that emits the captured rm root cause + path + remediation and routes into the FAILURES/return-1 path); AC-3 writability-probe annotations added at the DRIFT-emit + cmd_report FAIL sites (diagnostic-only). |
| **R-7** | **#92 false-negative under operator's default locale.** Verified it reproduces (`LC_ALL=en_US.UTF-8` → `sort: Illegal byte sequence`; `LC_ALL=C` → exit 0). A spoke testing only under `LC_ALL=C` would see a false PASS. | LOW | #92 spoke | DT must reproduce under a UTF-8 locale, not C. [VERIFIED both directions at Stage 4] |
| **R-8 (audit-baseline)** | Cross-PR overlap audit (Stage-4 A4) is baseline-pinned; concurrent releases merging to main during this release's Engineering window aren't caught. | LOW (REALIZED) | Hub | **Baseline pinned: worktree HEAD at Stage 4; recent-window = tags through v1.18 + open PRs at plan time.** Re-check at Stage 9 (Phase A6.5) + Stage 12 (Phase A.5). **This risk REALIZED:** concurrent `sior-escalation-discipline` (v1.19) and `health-and-raid-determinism` (v1.20) shipped to main mid-flight and consumed the tracked versions — caught at #88 Commit 0; re-versioned v1.20 → v1.21 (see header + D-Version). |
| **R-9 (cross-milestone unblock)** | On merge, this release unblocks downstream issues in OTHER milestones (#76→#237/#238/#242/#243; #88→#172; #106→#112; #332→#260). | INFO | Hub | No intra-release constraint; note for post-merge routing. Native "Blocked by" links resolve automatically. |
| **R-10 (governance scope)** | #26 edits hub-spoke-bridge.md, #106 edits label-taxonomy.md + a pipeline spec — **governance/reference files.** Per "No ungoverned changes," each needs its tracked Issue (have it) + the PR diff as the dry-run gate. | LOW | Hub | All edits flow through the release PR; PR review IS the governance gate (Claude Code path). No separate per-step approval needed beyond Stage 9 GO. |

---

## Decisions Rendered (Stage 4 + Collective Review + D-Version)

Recorded for downstream stages — all gate decisions are now CLOSED through Stage 9 (scope hard-locked; Engineering authorized).

| Decision | Outcome | Reversibility / Confidence |
|---|---|---|
| **Release Class** | **A — `routine`** (Light engagement / Standard Stage 9 / Stage 5 SKIP-where-trivial / 30-day outcome-window) | CHEAP / HIGH |
| **D-Version** | **v1.21** — re-rendered v1.20 → v1.21 mid-flight (concurrent releases took v1.19 + v1.20); next-free rule re-applied to updated facts (2026-06-14) | CHEAP / HIGH |
| **D-C Branch Topology** | **SINGLE** — one legacy-named `release/v1.20-deploy-toolchain-defect-cleanup` branch (v1.21 identity), write-serialized, one release PR | CHEAP / HIGH |
| **D-#122** | **Option (a)** — pmo-skill-editor Mode A no-op commit on `file-router` (resolves Check-10 finding without weakening trailer discipline) | CHEAP / HIGH |
| **#106 D-1** (Rule 6 contract) | **BOTH** — Rule 6 states mirror-at-creation AND Check-16-exclusion | CHEAP / HIGH |
| **#106 D-2** (stamp value source) | **Option 1** — mirror the parent's current status label at scaffold time | CHEAP / HIGH |
| **Scope-lock** | **APPROVED** — scope hard-locked through Stage 9; Engineering authorized (2026-06-14, operator in main-thread chat) | — |
| **RC-5** (adversarial review) | **OVERRIDE** — hub-side adversarial verification accepted in lieu of a formal pmo-adversarial pass (Class A routine; deviation logged for Stage 13 retrospective) | — |

**Collective Review result:** Bullets 1–6 all PASS (deps satisfied; no design conflict — shared files edited in disjoint regions; cumulative risk CHEAP/HIGH concentrated on deploy.sh; scope valid; cross-D upstream-compat N/A-with-rationale; N-way consistency all aligned; R1 evidence-grounding present for #26, #106). Bullet 7 (RC-5) overridden as above.

**Hub-surfaced Stage-6 implementation note (#26):** the Check-34 desc-fallback grep (`^[[:space:]]*Modes:`) returns 0 for the 2 desc-only skills (pmo-skill-editor, project-initiator) — their `Modes:` token is inline in the `description:` frontmatter. Engineering must match the actual form, else those skills are silent false-negatives. Warn-mode contains it.

---

## Release Outcome Statement

**AFTER** — `core/deploy/deploy.sh` and its governance checks run deterministically for the platform engineer: the Check-19 arithmetic crash, the silently-swallowed read-only `references/` failure, and the `#`-clipping path readers are fixed; `release/tools/blast-radius.sh` completes on non-ASCII files; `--report` carries Check-18 parity; the Check-17 override env var is consistently named; a Mode-Provenance drift check (Check 34) and the four public-flip cleanup findings land; and the skill-deployment session-path documentation is confirmed already-canonical (tokenized per ADR-013).

**BEFORE** — The toolchain carries latent defects that make `deploy.sh --check` self-fail (Check-19 emits 0-newline-0 then a `syntax error in expression`, exit 1), let `blast-radius.sh` crash with `sort: Illegal byte sequence` under a UTF-8 locale, silently mask a read-only `references/` subtree failure behind `|| true` before failing opaquely at `cp -R`, clip an unquoted `#`-bearing `cowork_install_path`, omit Check-18 from `--report`, and carry a misnamed `C15_*` override env var on the renumbered Check 17.

**Actor(s):** Stage 6 Engineering spokes (deploy.sh chain + blast-radius.sh + doc/spec edits); Operator at the D-Version / D-C / D-#122 gates.

**Success Indicator:** `./core/deploy/deploy.sh --check` returns exit 0 (no `syntax error in expression` at Check 19) AND `bash release/tools/blast-radius.sh core/schemas/tracker-schemas.md` exits 0 under the operator's default UTF-8 locale, immediately after this release's merge SHA lands on main.

---

## Out-of-scope discoveries (routed; filed as separate intake)

- **#88:** supplementary-copy loop at deploy.sh:1145 — same silent-swallow family (weaker; no `rm -rf || true`) → improvement.
- **#26:** deploy.sh Check-24 gap (sequence skips 24) → observation; skill mode-declaration non-uniformity → improvement (enables Check 34 enforce-flip).
- **#106:** do NOT backfill the 330 closed no-status sub-tasks (archaeology; no live consumer).
- **v1.x version-counter non-contiguity** is a release-identity hygiene signal that overlaps milestone #152 `release-identity-and-spec-hardening` — confirm there, not in this defect-cleanup release.

---

_Verification ledger (Stage 4, against worktree HEAD): #26 [Check 28 exists @:3259; highest=33; free=34] · #76 [grep -cE then echo-0 @2424/2426/2435 — LIVE] · #88 [silent-swallow @:1173 LIVE at Stage 4; FIXED at `6274882`] · #92 [REPRODUCED: UTF-8 to crash, C to exit 0] · #104 [cmd_report has 16/17 not 18 — LIVE] · #106 [Check 16 = improvement-only scope; 12 sub-tasks no-status — framing STALE, source-gap LIVE] · #111 [C15_THRESHOLD_OVERRIDE_FILE @2253/2261/2262 — LIVE] · #122 [file-router migrated; D-#122 = option (a)] · #331 [F1 update.sh:380 / F2 validate-install.sh:657 hardcoded HOME / F3 notify-version-skew.sh:9 / F4 deploy.sh:78 — all LIVE] · #332 [caret-quote-hash capture @106/393/734/1680 — LIVE] · #343 [both UUIDs absent tree-wide; ADR-013 re-pointed Check 8 — OBSOLETE]._

### Source(s)
- Stage 4 Release Plan, Collective Review scope-lock decision record, and D-Version re-render record — milestone #171 `deploy-toolchain-defect-cleanup` planning issue #943.
