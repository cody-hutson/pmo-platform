---
version: v1.08
date: 2026-06-08
type: plan
issues: ["#406", "#2", "#193"]
pr: null
links:
  note: null
  log_archive: "logs/v1.08.md"
reversibility-tier: MODERATE
themes: ["initiative:skill-suite", "cluster:skill-suite-architecture-spine"]
---

# v1.08 Release Plan — skill-suite-architecture-spine

**Milestone:** `skill-suite-architecture-spine`
**Release Class:** `novel` (Stage 4 spoke-proposed; operator renders at scope-commit). Basis: two new tracked files (the establishing ADR-019 + a new `core/standards/` alignment standard), ≥4 D-class decisions, and a clean Tier-0 premise rejection (#8).
**Branch Topology:** `D-C SINGLE` — one release branch `release/v1.08-skill-suite-architecture-spine`; sequential Engineering commits; one release PR (shared-release-PR append model). This plan is committed as the first Engineering commit (Commit 0).
**Differentiation posture (per `novel`):** Engagement density Standard; Stage 9 review depth Deep (Collective Review N-way consistency + cross-D upstream-compatibility scan + blast-radius); Stage 5 activation bias ALL (all surviving issues activate Stage 5); Stage 13 outcome-window 30-day.

**Stage 4 plan content of record:** the Stage 4 Release Planning comment on sub-task #547 (operator-reviewed at the Stage 4 Decision Briefing). This file transcribes that comment as Engineering Commit 0.

---

## Stage 4 Release Planning — skill-suite-architecture-spine

> Release-scoped Stage 4 plan for Milestone #206 (`skill-suite-architecture-spine`). Produced under the operator directive that the four milestone issues are **work-item hypotheses, not specifications** — each load-bearing claim was re-verified against the live `core/`+`release/`+`operations/` modular-monolith tree (post-v3.x restructure) and live `gh` state. Disposition is per-issue: not all four ship as authored.

### Summary (30 seconds)

- **4 work items** in the milestone (#406, #2, #8, #193) + the Stage 4 sub-task #547. The milestone's `open_issues:5` counts #547 itself; the milestone-description's "~30 pts across 5 issues" overcounts by one work item — there are **4 work-item deliverables**, not 5. (See Work-Item Disposition note.)
- **Dispositions:** **#406 → re-scope** (author ADR-019 in `core/ADRs/`, not a stale path; forward-reference #2 — no hard ordering). **#2 → re-scope** (re-ground every cited path; tracked deliverable shrinks to one standard in `core/standards/`; audit/recommendations registers are operator-local read-once artifacts). **#8 → REJECT-PREMISE** (C3 premise-invalidation → **Tier 0 Premise Rejection**: ADR-012 already de-scoped tracked roadmap instances and removed `core/governance/roadmaps/`; the framework already exists in-repo). **#193 → re-scope** (the "#160 block" is a confirmed mislink; real dependency `pmo-skill-refiner` is satisfied; ship the smallest fail-loud guard, composed with the existing upstream-reference-catalog drift-check, not a compat-shim framework).
- **The highest-stakes call (#8)** is a clean premise rejection backed by an Accepted ADR + a `No such file or directory` on the target dir. Surviving in-repo content from #8 is at most a one-line cross-reference into the existing `initiative-roadmap-framework.md`; the roadmap *instances* themselves are operator-local per ADR-012 and out of the tracked release.
- **Surviving release = 3 issues** (#406, #2, #193). Two are analysis/design-class (#406 ADR authoring is design-class; #2 is analysis-class) → **Stage 5 ACTIVATES** for the release (ADR-011 analysis-class research-methodology-design variant applies to #2; #406 hits the structural-design trigger).
- **Proposed Release Class: `novel`** (introduces a new ADR record + a new `core/standards/` doc + ≥1 D-class decision). **Domain: `governance`, sourcing-exempt** (pipeline-internal release). **Version: `v1.08`** (next sequential after the just-shipped v1.07).

---

### Work-Item Disposition

**Milestone-count reconciliation (flagged per HUB-VERIFIED CONTEXT).** `gh issue list --milestone … --state all` returns 5 open: the 4 work items {#406, #2, #8, #193} **plus the Stage 4 sub-task #547**, which is itself assigned to Milestone #206 (`gh issue view 547 --json milestone` → `number:206`). `gh api …/milestones/206` reports `open_issues:5, closed_issues:0` for the same reason. The milestone-description tagline "~30 pts across 5 issues" therefore either (a) coincidentally matches the issue-count-including-sub-task, or (b) was authored expecting a 5th work item that was never filed / was absorbed. **Load-bearing conclusion: there are 4 work-item deliverables.** The "5" is not a missing-issue signal — it is the sub-task being milestone-tagged. `[VERIFIED 2026-06-07: gh issue view 547 --json milestone → 206; gh issue list --milestone … --state all → {547,406,193,8,2}]`

---

#### #406 [ADR] — DISPOSITION: **RE-SCOPE** (author the ADR record at the correct live home; decouple the #2 dependency)

- **Premise validity (A0):** VALID. The decision ("Specialists are role-named thin skills that COMPOSE shared function-skills, not absorb them; a role may span several Specialists per the skill-boundary test") was operator-adopted 2026-06-06 per the issue body. The deliverable is to author the ADR markdown record of that adopted decision. `[VERIFIED 2026-06-07: gh issue view 406 — body states "operator-adopted 2026-06-06"; "authoring the ADR markdown file is downstream pipeline work"]`
- **File-absence re-verification:** No ADR for this decision exists. `core/ADRs/` holds ADR-003…ADR-018 (016 = intake-front-door-architectural-boundary, 018 = work-item-type-layer); `release/ADRs/` holds 001/002/005/011. None covers "Specialists compose not absorb." `[VERIFIED 2026-06-07: ls core/ADRs/ release/ADRs/ → no specialists/compose ADR]`
- **Correct ADR home + number (re-scoped from stale path):** This is a platform-architecture/governance decision about skill decomposition (sibling to ADR-004 "five-function-spine", ADR-016 "intake-front-door-architectural-boundary") → home is **`core/ADRs/`**, NOT a `pmo-platform/...` path. Next sequential core number is **ADR-019** (core set is contiguous through 018). Recommended filename: `core/ADRs/ADR-019-specialists-compose-not-absorb.md`, house format per ADR-005 template, `status: Proposed` promoting to `Accepted` at Collective Review per the ADR governance convention.
- **#2 coupling (the named question):** The ADR's Consequences say overlapping role/function pairs are "reconciled at #2." This is a **forward reference, not a hard ordering dependency.** The ADR records a decision already made; it does not consume #2's audit output. Author the ADR with a forward-reference to #2 ("reconciliation of specific overlapping pairs is performed by the #2 alignment audit"). **#406 does NOT need #2 to land first.** Authoring-order recommendation: #406 *before* #2, so the audit (#2) can cite the adopted ADR as its governing principle. `[VERIFIED 2026-06-07: gh issue view 406 — Consequences reference #2 as the reconciliation venue, not as an input to the decision]`
- **Stale dep-link note:** The issue's `Blocks: #181,#182,#183,#184,#185,#186,#284,#297,#298,#407,#408` is roadmap-consolidation metadata about the *downstream role-skill build*, not intra-release. Those are out-of-release; #406 ships the ADR record only. #284 (epic) and #410 (initiative) both resolve OPEN. `[VERIFIED 2026-06-07: gh issue view 284 → OPEN; gh issue view 410 → OPEN]`

#### #2 [Protocol] — DISPOSITION: **RE-SCOPE** (re-ground all cited paths; split the tracked deliverable from the operator-local read-once artifacts)

- **Premise validity (A0):** VALID. No skill-pipeline alignment standard exists (`find … -name '*skill-pipeline-alignment*'` → empty; `find … -name '*pipeline-alignment*'` → empty). The misalignment the issue names (skills define parallel mode vocabularies that run alongside, not driven by, the pipeline) is real and current. `[VERIFIED 2026-06-07: no skill-pipeline-alignment file exists anywhere in tree]`
- **AC-currency (A0.5) — every cited path re-grounded:**
  | #2 cites (pre-restructure) | Live home (re-grounded) | Evidence |
  |---|---|---|
  | `CLAUDE.md § Execution Framework Reference` (4-layer Process/Framework/Methodology/Tool model) | **`core/disciplines/execution-framework.md`** — the section name "Execution Framework Reference" no longer exists in `core/CLAUDE.md.template`; the 4-layer model is now its own discipline doc | `grep "Execution Framework" core/CLAUDE.md.template` → empty; `execution-framework.md` head declares the Process↔Tool layering |
  | `OPERATIONS.md § Mode Selection Protocol` | **`core/governance/OPERATIONS.md:135` AND `operations/OPERATIONS.md:135`** (mirror pair; both carry the section) | `grep "Mode Selection Protocol"` → both files line 135 |
  | `pmo-platform/reference/schemas/gate-criteria-spec.md` | **`core/schemas/gate-criteria-spec.md`** (exists, 77 KB) | `ls core/schemas/gate-criteria-spec.md` → present |
  | `pmo-platform/reference/standards/skill-pipeline-alignment.md` (the deliverable's likely home) | **`core/standards/skill-pipeline-alignment.md`** (K1 standards live under `core/standards/`) | sibling K1 standards confirmed under `core/standards/` (e.g., `initiative-roadmap-framework.md`, `upstream-reference-catalog.md`) |
  | `pmo-platform/analysis/skill-pipeline-alignment-audit-YYYY-MM-DD/SUMMARY.md` + `RECOMMENDATIONS.md` + `issue-drafts/` | **`<OPERATOR_INSTANCE_ANALYSIS_PATH>/skill-pipeline-alignment-audit-YYYY-MM-DD/`** (operator-local, untracked) | stage-04-planning.md A4 + multiple specs reference `<OPERATOR_INSTANCE_ANALYSIS_PATH>` as the analysis home; analysis artifacts are read-once, not tracked corpus |
  | `deploy.sh SKILL_LIST` (cross-check method in AC) | **`core/deploy/deploy.sh`** | `find -name deploy.sh` → `core/deploy/deploy.sh` |
  | "≥1 row per of the 20 deployed skills" | **22–23 skills** now deployed (23 SKILL.md dirs; one is `pmo-skill-refiner-selftest-canary` test harness) | `find …/skills -name SKILL.md | wc -l` → 23 |
- **Re-scope substance:** Of #2's three deliverables, only **deliverable #2 (the alignment standard) lands in the tracked tree** — at `core/standards/skill-pipeline-alignment.md`. **Deliverable #1 (audit register / SUMMARY.md)** and **deliverable #3 (RECOMMENDATIONS.md + issue-drafts/)** are read-once analysis artifacts → operator-local `<OPERATOR_INSTANCE_ANALYSIS_PATH>/`. This is the modular-monolith + ADR-012-class consequence: the *convention/standard* is template-corpus; the *operator's specific audit findings* are operator-instance. The two CLAUDE.md cross-link ACs re-ground to: cross-link FROM `core/disciplines/execution-framework.md` (not "CLAUDE.md § Execution Framework Reference") and FROM both OPERATIONS.md mirrors' Mode Selection Protocol sections.
- **Scope-realism (split assessment per task item):** The AC "≥1 row per of the ~22 deployed skills + ≥1 finding per dimension + standard + recommendations + 1 end-to-end remediation proof" is a **large single milestone** but coherent — the audit, the standard, and the recommendation register are one analytical pass over one corpus; splitting them fragments the evidence→principle→disposition chain (anti-pattern per intermediate-artifact discipline). **Recommend: keep as ONE issue for the audit+standard+recommendations**; the AC's final clause ("one representative finding remediated end-to-end as proof-of-mechanism") is the natural **split point** — that remediation should be a *spawned follow-up issue*, not in-release, because it depends on audit output that does not yet exist. So: ship audit+standard+recommendations in v1.08; the proof-of-mechanism remediation becomes a `requires-new-issue` follow-up. This is a **scope-reduction**, not a split-into-parallel.

#### #8 [Architecture] — DISPOSITION: **REJECT-PREMISE** → **Tier 0 Premise Rejection** (C3 premise-invalidation; highest-stakes call)

- **A0 verdict: C3 — premise invalidated.** #8 asks to author **3 TRACKED roadmap instances** at `pmo-platform/governance/roadmaps/` (`role-skills-family.md`, `role-skills-pmo.md`, `role-skills-software.md`). **ADR-012 (Accepted 2026-06-02) explicitly de-scopes roadmap *instances* from the tracked tree** — they are authored operator-local at `<OPERATOR_INSTANCE_ROADMAPS_PATH>`, untracked, and the enforcement surfaces were *removed*, not made dormant. The target directory **does not exist**. `[VERIFIED 2026-06-07: ls core/governance/roadmaps/ → "No such file or directory"; core/governance/ holds only OPERATIONS.md + README.md; ADR-012 Decision §1 "Untrack in place (forward-only)" + §3 "Enforcement dropped, not dormant"]`
- **The issue's own banner concedes this:** the 2026-06-06 reconciliation header on #8 states the `v13.01–v13.06` milestone scheme "was never created," the framework dep `#976` "does not exist — the framework shipped," and "per ADR-012, roadmap *instances* are operator-local / untracked … the Affected Files paths below are stale." The issue is self-aware that its premise is void. `[VERIFIED 2026-06-07: gh issue view 8 — top banner]`
- **What survives in-repo (the "what, if anything" question):** The reusable **framework already exists** at `core/standards/initiative-roadmap-framework.md` — authoring it was the in-repo deliverable and it is done. `[VERIFIED 2026-06-07: find -name initiative-roadmap-framework.md → core/standards/initiative-roadmap-framework.md]` The *only* conceivably-surviving in-repo artifact from #8 is a **navigational pointer** — e.g., a one-line "role-skills roadmap family (PMO + software archetypes) is authored operator-local per ADR-012" entry, IF a consumer needs it. But ADR-012 §4 ("References re-pointed, not deleted") already re-pointed every live reference to the `<OPERATOR_INSTANCE_ROADMAPS_PATH>` token or the framework. **No tracked authoring deliverable survives.** The actual roadmap-instance *content* (archetype split, Now/Next/Later) is operator-local planning work, out of the tracked release entirely.
- **Recommendation:** **Reject the premise and mark #8 as closed against ADR-012 at Stage 13** (transition #8 to closed at Stage 13 with a comment citing ADR-012 §Decision + the missing-directory evidence). Do NOT author the 3 tracked files — doing so would directly violate an Accepted ADR and re-introduce the planning-metadata leak ADR-012 removed. If the operator wants the operator-local roadmap instances authored, that is **operator-instance work outside this tracked release** (and outside Claude Code's worktree-PR surface per the no-worktree-for-operator-work boundary). This is the cleanest, most defensible call in the release. Reversibility of the rejection: **CHEAP** (re-opening #8 is one `gh issue reopen`); confidence **HIGH** (Accepted ADR + filesystem evidence + the issue's own concession).

#### #193 [Risk] — DISPOSITION: **RE-SCOPE** (correct the mislinked blocker; ship the smallest fail-loud guard)

- **Premise validity (A0):** VALID. `pmo-skill-refiner` wraps `anthropic-skills:skill-creator` at runtime; if the scaffolder's output format drifts, the PMO-injection step can silently misfire (silent factory degradation). The risk is real and the skill is live. `[VERIFIED 2026-06-07: ls release/skills/pmo-skill-refiner/ → SKILL.md (28KB) + references/ (eval-framework.md, schemas.md, …) + scripts/ (10 scripts)]`
- **Real dependency SATISFIED:** #193's stated dependency is "v9.1 #598 must merge first (pmo-skill-refiner must exist)." The skill exists in full. That dependency is met. `[VERIFIED 2026-06-07: pmo-skill-refiner SKILL.md + references/ + scripts/ all present]`
- **"#160 block" — CONFIRMED MISLINK:** The native `Blocked by #160` is a roadmap-consolidation mislink. **#160 = "[Initiative]: Project Data Architecture — entity model, storage, automation, interface, governance (5-layer framework)"** (`initiative:project-data-architecture`, OPEN, milestone:null) — entirely unrelated to scaffolder version-compat. It is **not a genuine blocker.** `[VERIFIED 2026-06-07: gh issue view 160 → title "Project Data Architecture …", initiative:project-data-architecture]`
- **AC-currency (A0.5):** The "Affected Files" paths are already current-correct (`release/skills/pmo-skill-refiner/SKILL.md`, `.../references/eval-framework.md`, `.../scripts/`) — they were authored *post-restructure* and resolve live. No re-grounding needed beyond removing the #160 link.
- **Simplicity-first re-scope (the smallest-robust-guard question):** #193's "Proposed Change" lists three mechanisms — (1) detection, (2) compat-shim layer, (3) pinning strategy. **The smallest robust guard is mechanism #1 alone: a structure-assertion pre-injection check that captures the expected skill-creator output skeleton (required frontmatter `name:`+`description:`, the `references/` directory convention, body anatomy per `core/standards/upstream-reference-catalog.md`) and FAILS LOUDLY on mismatch.** Mechanisms #2 (compat-shim framework) and #3 (version-pinning) are **over-engineering for current state** — flag them: a shim layer presupposes a drift that has not occurred and would carry maintenance burden for a hypothetical; pinning depends on plugin-version machinery the issue itself marks "consider whether … the plugin system supports versioning." **Compose the guard with the EXISTING `upstream-reference-catalog.md` drift-check protocol** (its entries already define `drift_check_protocol: "Verify Anthropic skill-creator scaffolder output on framework release; surface drift via Stage 13 Close re-verification"`) — harden that into a runtime assertion rather than inventing a parallel compat-shim subsystem. This is the SIMPLICITY-FIRST + BEST-PRACTICE posture. `[VERIFIED 2026-06-07: core/standards/upstream-reference-catalog.md — 3 entries (skill-md-frontmatter / skill-references-directory / skill-md-body-anatomy), each with drift_check_protocol; last_verified 2026-05-16/23]`

---

### Dependency Graph

Directional, intra- and cross-issue, **with rotted links corrected**:

```
#406 (ADR record)
   └─(forward-reference, SOFT — not a build dependency)→ #2 (audit cites the adopted ADR as governing principle)

#2 (alignment audit + standard)
   └─ independent for authoring; downstream remediation spawns its own follow-up issue (out of release)

#8  ──[PREMISE REJECTED]──  no edges into the surviving release (closed against ADR-012)

#193 (scaffolder-drift guard)
   └─ depends-on: pmo-skill-refiner EXISTS  [SATISFIED]
   └─ depends-on: core/standards/upstream-reference-catalog.md EXISTS  [SATISFIED — guard composes with it]
   ✗ "Blocked by #160"  [DELETED — mislink; #160 is Project Data Architecture, unrelated]
```

**Corrections applied:**
- **#193 `Blocked by #160` → removed** (mislink; #160 unrelated). #193 has **no remaining blocker**.
- **#406 `Blocks #181…#408` → out-of-release** (downstream role-skill build metadata; not intra-release ordering).
- **#406 → #2 reclassified** from implied hard-dependency to **soft forward-reference** (the ADR records a made decision; the audit cites it — recommended authoring order #406→#2, but not a hard gate).
- **#8 edges (`Blocks #206,#286`) → void** (premise rejected; #8 produces no tracked artifact).

**Net surviving-release dependency posture:** the three surviving issues (#406, #2, #193) are **effectively independent** for authoring, with one soft ordering preference (#406 before #2). No hard cross-issue blocker remains.

### Implementation Sequence

Over the **3 surviving issues** (#8 excluded — premise rejected):

1. **#406** — author `core/ADRs/ADR-019-specialists-compose-not-absorb.md` (records the 2026-06-06 adopted decision; forward-references #2). *First, so #2 can cite it.*
2. **#2** — author `core/standards/skill-pipeline-alignment.md` (the tracked standard) + the operator-local audit/recommendations registers; cross-link from `core/disciplines/execution-framework.md` + both OPERATIONS.md Mode Selection Protocol sections. *Cites ADR-019 as governing principle.*
3. **#193** — add the structure-assertion drift guard to `pmo-skill-refiner` (composed with `upstream-reference-catalog.md` drift-check). *Independent; can run in parallel with 1–2, sequenced last only because it is the narrowest and lowest-coupling.*

Rationale: #406→#2 is the one soft ordering edge; #193 is independent. Single-operator PMO — this is a **sequence, not a schedule** (no dates per persona anti-pattern).

### Stage Applicability Matrix

Stages 5–13. Per `core/standards/planning-solutioning-handoff.md` (all-or-nothing release-level Stage 5 activation, logical-OR over T1–T6) and ADR-011 (analysis-class research-methodology-design is a **Stage 5 persona variant, not a skip**).

| Issue | Stage 5 Solutioning | Stage 6 Eng | Stage 7 DevTest | Stage 8 QA | Stage 9–13 | Stage 5 trigger evidence |
|---|---|---|---|---|---|---|
| **#406** (ADR record) | **ACTIVATE** | ✓ | ✗ (no functional/runtime impact — ADR is a governance record; DT has nothing to execute) | ✓ (QA = conformance to ADR house format + governance-file discipline) | ✓ all | **T1** (new file: ADR-019) + **T3** (structural design decision — the decomposition-axis rule constrains downstream role-skills) |
| **#2** (audit + standard) | **ACTIVATE** — *ADR-011 analysis-class research-methodology-design variant applies* (deliverable is a research artifact: audit sampling frame over ~22 skills + evidence-grading + the alignment principle) | ✓ | ✗ (analysis/governance artifact; no runtime behavior to test) | ✓ (QA = standard completeness: decision-tests present, worked examples, register conformance) | ✓ all | **T1** (new file: `skill-pipeline-alignment.md`) + **T3** (the alignment principle is a structural/naming decision) + **T6** (blast-radius uncertain — audit surfaces unknown finding count across the corpus) |
| **#193** (drift guard) | **ACTIVATE** | ✓ | **✓** (functional impact — the guard is executable assertion code; DT must demonstrate it FIRES on simulated scaffolder drift per the AC) | ✓ | ✓ all | **T2** (skill-logic change to `pmo-skill-refiner` SKILL.md + scripts) + **T4** (multiple valid approaches: detection-only vs shim vs pin — Stage 5 selects detection-only per simplicity-first) |

**Release-level rollup: ACTIVATE.** Every surviving issue fires ≥1 trigger → Stage 5 activates for the whole release (no per-issue mixed routing in current contract). **Collective Review fires** (≥2 issues with Solutioning activated). Per ADR-011, #2's Stage 5 sub-task uses the **analysis-class research-methodology-design variant** of the Stage 5 persona (defines the audit's sampling frame / evidence-grading rubric / validity threats), with the in-stage gate-teeth (§7 methodology-design gate rows + §12 handoff schema).

**DT/QA skip notes:** #406 and #2 skip Stage 7 Dev Testing (no executable/runtime impact — they are governance + analysis artifacts; "test" has no runtime target). #193 retains Stage 7 — its guard is code and the AC explicitly requires "test demonstrating detection fires when scaffolder output structure diverges." All three retain Stage 8 QA (document/standard conformance review).

### Contention Map

Files touched by multiple surviving issues:

| File | #406 | #2 | #193 | Collision risk |
|---|---|---|---|---|
| `core/ADRs/ADR-019-*.md` | **create** | — | — | none (single author) |
| `core/standards/skill-pipeline-alignment.md` | — | **create** | — | none (single author) |
| `core/disciplines/execution-framework.md` | — | **edit** (add cross-link to the standard) | — | none |
| `core/governance/OPERATIONS.md` (Mode Selection Protocol §) | — | **edit** (cross-link) | — | **mirror-pair risk** — must mirror byte-identically to `operations/OPERATIONS.md` per skill-deployment Check 9 |
| `operations/OPERATIONS.md` (Mode Selection Protocol §) | — | **edit** (cross-link) | — | mirror pair of the above — edit both in the same change |
| `release/skills/pmo-skill-refiner/SKILL.md` + `scripts/` | — | — | **edit/add** | none |
| `core/standards/upstream-reference-catalog.md` | — | — | **read** (compose with; possibly bump `last_verified`) | low — read-mostly |

**Verdict:** Near-zero contention. As the task framing predicted, an ADR file, a standard file, and a skill-guard rarely collide. The **one real contention is the `core/governance/OPERATIONS.md` ↔ `operations/OPERATIONS.md` mirror pair** that #2's cross-link edit touches — both mirrors must change byte-identically in the same PR (Check 9). No two issues write the same file.

### Risk Register

No passive voice — each risk names owner-action + mitigation.

| ID | Class | Risk | Owner-action + Mitigation |
|---|---|---|---|
| R1 | **Dependency (mislink)** | #193 carries native `Blocked by #160`; if a tool reads the native link literally, #193 is falsely blocked by an unrelated initiative. | **Operator/Hub** removes the `Blocked by #160` native link at Stage 6 (or now) via `gh issue edit 193 --remove-…`; the body's `Blocked by #160` line is reconciled at Engineering per reconcile-don't-annotate. #193 then has no blocker. |
| R2 | **Scope (premise-rejection regret)** | Rejecting #8 might later be regretted if the operator *did* want a tracked navigational stub. | **Operator** renders the #8 D-gate (below). Mitigation: rejection is CHEAP-reversible (`gh issue reopen 8`); ADR-012 §4 already re-pointed references, so nothing dangles if #8 stays closed. A framework-index stub, if wanted, is a 1-line follow-up — not a reason to author 3 instance files. |
| R3 | **Scope (over-engineering)** | #193 as authored implies a compat-shim framework + version-pinning subsystem — disproportionate to a hypothetical drift. | **Stage 5 spoke (#193)** selects detection-only (structure-assertion that fails loudly), composed with the existing `upstream-reference-catalog.md` drift-check; shim + pinning are deferred-with-rationale to follow-ups if/when real drift is observed. |
| R4 | **Contention (mirror pair)** | #2's OPERATIONS.md cross-link edit must hit both `core/governance/OPERATIONS.md` and `operations/OPERATIONS.md` byte-identically; a one-sided edit fails Check 9. | **Stage 6 spoke (#2)** edits both mirrors in the same commit; verifies with the Check-9 mirror diff before PR. |
| R5 | **Scope (analysis sizing)** | #2's audit spans ~22 skills × 3 dimensions; finding count is unknown until the audit runs (T6 blast-radius uncertainty). | **Stage 5 spoke (#2)** defines the sampling frame + a `D-AuditSynthesisGranularity` decision up front (per hub-spoke-bridge G-Hybrid rule) so the recommendation register doesn't explode; the proof-of-mechanism remediation is split to a follow-up issue, not in-release. |
| R6 | **Rollback complexity** | All three deliverables are additive (new ADR, new standard, new guard code) + 3 small cross-link edits. | **Low rollback complexity.** Revert = `git revert` the release PR(s); new files vanish, cross-links revert, guard code removed. No data migration, no destructive edit, no enum/stage-count change. The #8 rejection is a no-op on the tree (nothing authored). |

**Aggregate rollback strategy:** Additive-only release. Per-issue revert is a single `git revert` of the issue's commit/PR; no inter-issue rollback ordering required (issues are file-disjoint). The #8 premise-rejection leaves zero tree footprint to roll back.

### Release Class & Domain

**Release Class: `novel`** (per `release-class-taxonomy.md` § Class Enum + Classification Procedure).
- **Trigger evidence (novel fires on any one):** (a) **≥1 issue introduces a new reference doc / schema / skill** — #406 creates ADR-019, #2 creates `skill-pipeline-alignment.md`. ✓ (b) **≥1 D-class decision in the release plan** — the #8 premise-rejection, the #193 "#160-block" verdict, the version stamp, and the #2 scope-reduction are all D-class. ✓ (c) **≥1 Stage 5 ADR** — ADR-011 variant activation for #2. ✓
- **Why not `routine`:** routine requires zero new files AND zero new D-class decisions; this release adds 2 new tracked files and renders ≥4 D-decisions. Disqualified.
- **Why not `cross-cutting`:** cross-cutting needs ≥3 `pipeline/stage-*.md` edits OR ≥3 of the named governance surfaces OR ≥3 in-bundle compositional edges. This release edits **zero** pipeline-stage shards, touches OPERATIONS.md (1 mirror-pair) + execution-framework.md (1) = 2 governance surfaces (< 3), and has near-zero compositional edges. Disqualified — it is `novel`, not `cross-cutting`.
- **Why not `hotfix`:** no P1/P2 defect against a deployed release; not corrective. Disqualified.
- **Differentiation posture (novel):** Engagement density **Standard** · Stage 9 review depth **Deep** · Stage 5 activation bias **ALL** · Stage 13 outcome-window **30-day**.

**Domain classification (per stage-04 §A1.5/§A3):** **`domain: governance`**, **sourcing-exempt**.
- A3-time deliverable-domain classification from the File-Change-Matrix: the matrix consists entirely of internal pmo-platform artifacts — an ADR, a `core/standards/` doc, governance cross-links, and skill-internal guard code. No external/application/web surface. → `domain: governance` (the `#193` guard code is `software`-flavored skill-internal; governance dominates the matrix — secondary `software` noted in rationale).
- **Sourcing-exempt** — pipeline-internal release; the platform encodes its own internal-deliverable best-practice in `core/standards/domain-best-practices/governance.md`. Provenance label for the release plan:
  `domain_practice: { source: N/A — pipeline-internal release, date: 2026-06-07, domain: governance }`
  (Secondary `software` for the #193 guard noted in the classification rationale; the dominant matrix domain is `governance`.)

### Build-Principle Assessment

Per surviving deliverable (SCALABLE / SIMPLICITY-FIRST / MAINTAINABLE / BEST-PRACTICE):

- **#406 (ADR-019):** SCALABLE ✓ (an ADR is the platform's standing decision-record mechanism; sibling to 18 prior ADRs). SIMPLICITY-FIRST ✓ (records an already-made decision; smallest possible deliverable — one markdown file). MAINTAINABLE ✓ (house ADR format, immutable-by-convention, forward-reference not hard-coupling). BEST-PRACTICE ✓ (ADR is the canonical architecture-decision pattern). **No over-scope.**
- **#2 (audit + standard):** SCALABLE ✓ (the standard governs all future SKILL.md authoring; one decision-test set scales across the suite). SIMPLICITY-FIRST ⚠️ **flag** — the AC's "≥1 row per ~22 skills + ≥1 per dimension + standard + recommendations + end-to-end proof" is the heaviest item in the release; **recommend reducing in-release scope to audit+standard+recommendations and splitting the proof-of-mechanism remediation to a follow-up** (it depends on audit output that doesn't exist yet). MAINTAINABLE ✓ (decision-table + worked-examples form per prefer-durable-structures). BEST-PRACTICE ✓ (analysis-folder convention; tracked standard vs operator-local registers split is correct). **Over-scope flagged → re-scoped (split the proof).**
- **#193 (drift guard):** SCALABLE ✓ (a structure-assertion guard scales to any scaffolder-output check). SIMPLICITY-FIRST ⚠️ **flag** — *as authored* it implies compat-shim + version-pinning subsystems = **over-engineering**; **re-scoped to detection-only fail-loud assertion composed with the existing upstream-reference-catalog drift-check**. MAINTAINABLE ✓ (one assertion + one catalog read beats a bespoke shim framework). BEST-PRACTICE ✓ (fail-loud-on-drift is the correct posture for silent-degradation risk; reuses existing catalog rather than inventing parallel machinery). **Over-engineering flagged → re-scoped to smallest robust guard.**
- **#8:** N/A — premise rejected; no deliverable to assess. (Authoring it would itself be an over-scope / ADR-012 violation — the rejection IS the build-principle-correct outcome.)

### Operator Decisions (D-gates)

Rendered per the hub-spoke-bridge D-Gate Template. The operator must render these before scaffolding.

#### D-8-Disposition: Reject #8's premise (do not author the 3 tracked roadmap files)?
**Gate input:** A0 re-review C3 finding — #8 asks for 3 TRACKED instances at `core/governance/roadmaps/`; ADR-012 (Accepted 2026-06-02) de-scoped tracked roadmap instances and removed that directory; the framework already exists at `core/standards/initiative-roadmap-framework.md`; #8's own banner concedes its paths/milestones are void. `[VERIFIED: ls core/governance/roadmaps/ → No such file or directory; ADR-012 §Decision]`
**Gate decision:** (A) **REJECT-PREMISE** — mark #8 as closed against ADR-012 at Stage 13, author nothing in the tracked tree (RECOMMENDED); (B) re-scope to a single 1-line framework-index/navigational stub pointing at the operator-local instances + ADR-012; (C) defer #8 to operator-instance work (author the instances operator-local, outside this tracked release). Author the 3 tracked files: **NOT an option** (violates Accepted ADR-012).
**Blocks:** #8's place in the implementation sequence; the milestone close (Stage 13) cannot complete while #8's disposition is unrendered.
**Upstream compatibility:** N/A — this D-decision does not modify skill-authoring surface (it is a roadmap-instance placement decision governed by ADR-012). Upstream compatibility check does not apply.
**Reversibility / Confidence:** **CHEAP / HIGH** — option A reverses via `gh issue reopen 8`; ADR-012 §4 already re-pointed all references so nothing dangles. Confidence HIGH (Accepted ADR + filesystem `No such file` + issue's own concession).

#### D-193-Block: Is "#160" a genuine blocker for #193?
**Gate input:** #193 native link `Blocked by #160`. `[VERIFIED: gh issue view 160 → "[Initiative]: Project Data Architecture …", initiative:project-data-architecture — unrelated to scaffolder version-compat]` #193's real dependency (`pmo-skill-refiner` exists) is satisfied `[VERIFIED: ls release/skills/pmo-skill-refiner/ → full skill present]`.
**Gate decision:** (A) **Confirm #160 is a mislink → remove the `Blocked by #160` native link; #193 has no blocker and is release-eligible now** (RECOMMENDED); (B) treat #160 as a genuine blocker (would require evidence #160's data-architecture work gates scaffolder-compat — none found).
**Blocks:** #193's release-eligibility; whether #193 can be scaffolded in v1.08 at all.
**Upstream compatibility:** REQUIRED (the underlying #193 work touches skill-authoring surface — scaffolder output structure). Anthropic `skill-creator` convention per `core/standards/upstream-reference-catalog.md`: required frontmatter `name`+`description`, `references/` dir, body anatomy (last_verified 2026-05-16/23). The #193 guard ASSERTS this convention — it is upstream-compat-aligned by construction (it exists to detect upstream drift, not to diverge from it). No conflict. Note: catalog `last_verified` is >14 days old; Stage 5 should re-verify the skill-creator schema before finalizing the assertion targets.
**Reversibility / Confidence:** **CHEAP / HIGH** — removing a native link reverses via re-adding it; confidence HIGH (#160's subject is unambiguous and unrelated).

#### D-ReleaseClass: What Release Class does this release carry?
**Gate input:** Spoke-proposed class + trigger evidence per `release-class-taxonomy.md`.
**Gate decision:** (A) routine, (B) **novel** [RECOMMENDED], (C) cross-cutting, (D) hotfix.
**Blocks:** milestone-description `## Release Class` authoring + downstream per-class posture (Stage 9 review depth, Stage 5 bias).
**Upstream compatibility:** N/A — Release Class is PMO platform internal taxonomy; no Anthropic upstream surface. Upstream compatibility check does not apply.
**Reversibility / Confidence:** **CHEAP / HIGH** — re-classifiable later per Re-Classification Protocol. Recommendation: **novel** (new ADR + new standard + ≥1 D-class decision fire novel triggers (a)+(b)+(c); cross-cutting's ≥3-stage-file / ≥3-governance-surface thresholds are not met).

#### D-VersionStamp: What version does this release carry?
**Gate input:** No `vX.Y` stamp visible on Milestone #206. Latest shipped tag is **v1.07** (prior milestone `declarative-workitem-type-model`). Plans directory shows a contiguous v1.0x line through v1.07 (plus older v3.18/v3.20 corpus plans). `[VERIFIED: ls release/releases/plans/ → v1.07_RELEASE_PLAN.md latest in the v1.x line]`
**Gate decision:** (A) **v1.08** [RECOMMENDED — next sequential in the active v1.x line]; (B) other operator-chosen stamp.
**Blocks:** release-plan filename (`release/releases/plans/v1.08_RELEASE_PLAN.md`), branch name (`release/v1.08-skill-suite-architecture-spine`), milestone `## Release Class` block, tag.
**Upstream compatibility:** N/A — versioning is PMO-internal. Does not touch skill-authoring surface.
**Reversibility / Confidence:** **CHEAP / HIGH** — version stamp is a label; re-nameable before tag. Confidence HIGH that v1.08 is the next sequential.

#### D-2-Scope: Reduce #2's in-release scope (split the proof-of-mechanism remediation to a follow-up)?
**Gate input:** #2's AC includes "one representative finding remediated end-to-end as proof-of-mechanism" — that remediation depends on audit output that does not yet exist (T6). Splitting it keeps the release additive and the audit→standard→recommendations chain intact.
**Gate decision:** (A) **Ship audit+standard+recommendations in v1.08; spawn the proof-of-mechanism remediation as a `requires-new-issue` follow-up** [RECOMMENDED]; (B) keep the full AC in-release (larger, depends on not-yet-existing findings).
**Blocks:** #2's Stage 5/6 scope; the milestone close criterion for #2.
**Upstream compatibility:** N/A — #2 edits governance/standards + analysis artifacts, not skill-authoring frontmatter/layout. Does not touch skill-authoring surface.
**Reversibility / Confidence:** **CHEAP / HIGH** — the follow-up can be pulled back in; confidence HIGH that the remediation is naturally downstream of the audit.

*(Note on #406↔#2 ordering: this is a SOFT authoring preference, not a D-gate — author #406 before #2 so the audit cites the adopted ADR. Surfaced in Implementation Sequence; no operator decision required.)*

### Recommendations

1. **Ship v1.08 with 3 issues — #406 (re-scoped home ADR-019), #2 (re-scoped + split), #193 (re-scoped to smallest guard).** Reject #8's premise.
2. **#8 is the clean win:** rejecting it is fully defensible (Accepted ADR-012 + missing directory + the issue's own concession). Authoring the 3 tracked files would *violate* governance — the build-principle-correct action is to author nothing and close against ADR-012 at Stage 13.
3. **Fix the #193 native `Blocked by #160` link** (mislink) at Engineering or now — it is the only dependency-graph defect that could falsely gate the release. Reconcile the body's `Blocked by #160` line per reconcile-don't-annotate.
4. **Out-of-scope discoveries to log (not in this release):**
   - The roadmap-consolidation pass that authored the native dep-links injected at least 2 verified-wrong links into this milestone (#193→#160 mislink; #8→stale roadmap targets). Worth a sweep of native `Blocked by` links across the `initiative:skill-suite` set for similar rot — file as an observation, not in v1.08.
   - The milestone-description tagline "~30 pts across 5 issues" miscounts (4 work items + sub-task). Minor; the milestone description is operator-owned — flag at Stage 13 for a one-line correction if desired.
   - `upstream-reference-catalog.md` `last_verified` dates (2026-05-16/23) are aging; #193's Stage 5 should re-verify the skill-creator schema and bump the catalog `last_verified` as a side-effect — keeps the catalog and the new guard honest.
5. **Per the milestone Value statement,** the spine still delivers what it promised even with #8 rejected: the binding architectural rule (#406 ADR), the alignment audit (#2), and the version-compat guard (#193) all ship. The "navigable roadmap family" clause of the Value statement is satisfied by the operator-local instances + the existing in-repo framework per ADR-012 — not by tracked instance files.

### Issue References

References #406, #2, #8, #193 (the four milestone work items analyzed). References #547 (this Stage 4 sub-task; mark #547 as closed at completion of this planning pass). References #160 (verified unrelated — Project Data Architecture initiative), #284 + #410 (role-skill epic + initiative, both open), ADR-011 / ADR-012 (governing ADRs).

