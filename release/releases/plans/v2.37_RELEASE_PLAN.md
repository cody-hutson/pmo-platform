---
version: v2.37
date: 2026-06-28
type: plan
issues: ["#2120", "#2082", "#2085"]
pr: null
links:
  note: null
  log_anchor: "#v2-37-release-notes-conformance"
reversibility-tier: CHEAP
themes: ["cluster:release-notes-conformance"]
---

# v2.37-release-notes-conformance Release Plan

**Milestone:** 91-release-notes-conformance
**Release Class:** `routine` (operator-rendered at the Stage 4 Decision Gate, 2026-06-28). Basis: three related defect fixes on the release-notes close-out path — bounded, single-surface; no new component, no public-API change, no data migration, no cross-module blast radius; all edits internal governance/tooling.
**Branch Topology:** `D-C SINGLE` (operator-rendered, 2026-06-28) — one release branch `release/91-release-notes-conformance`; #2120 → #2082 → #2085 serialized as sequential commits on the shared lint module + standard + close-runbook surfaces; one release PR carrying each issue's reference in the dedicated Issue References block only. This plan is committed as the first Engineering commit (Commit 0).
**Differentiation posture (per `routine`):** Engagement density Light; Stage 9 review depth Standard; the one elevated-attention item is the cross-milestone merge contention on `automated-closeout.sh` (R1) — surfaced at Stage 9, does not lift the class. Reversibility CHEAP / Confidence HIGH.

**Domain Practice Provenance (Phase A1.5, per `stage-04-planning.md` §5.7):** `domain_practice: { source: N/A — pipeline-internal release, date: 2026-06-28, domain: software }`. This release's entire File Change Matrix consists of internal pmo-platform artifacts — the release-notes close-out **lint/drift-check tooling** (`core/deploy/tools/lint_release_corpus.py`, `core/deploy/deploy.sh`, `release/tools/automated-closeout.sh`) plus the governance docs that codify what that tooling enforces (`release-notes-standard.md`, `stage-12-execute.md`, `stage-13-close.md`, `hub-spoke-bridge.md`). A pipeline-internal release whose "domain" is the platform's own internals → **sourcing-exempt** (no external best-practice step fires; the `core/standards/domain-best-practices/` guides encode the practice directly). **Domain-classified `software`**: the load-bearing behavior change is the lint coverage + drift-check/re-emit *code*; the governance-doc edits document/mirror that tooling behavior (secondary domain: `governance`). Dominant-domain rule applied per §5.7; mirrors `cross-reference-integrity-ci` (CI + Python/bash tooling → `software`). [SOURCE: File Change Matrix below; stage-04-planning.md § Software/governance/pipeline-internal exemption — sourcing-exempt but still domain-classified.]

> Provisional version **v2.37**. Repo `cody-hutson/pmo-platform`. Milestone #234. Scope: #2120 (story), #2082 (bug), #2085 (bug). Concrete version atomic-claimed at Stage 12. Branch: `release/91-release-notes-conformance` (theme-named).

---

## Stage 4 Release Planning — 91-release-notes-conformance

> Spoke: Release Manager (Release Planning persona). Read-only analysis. Provisional version **v2.37**. Repo `cody-hutson/pmo-platform`. Milestone #234. Scope: #2120 (story), #2082 (bug), #2085 (bug).
> **Config-resolution fallback note:** `operator.toml` / `platform-config.toml` are not present in this worktree (operator-instance, outside the tree). Planning used the hub-injected documented defaults — `default_release_class=routine`, `release_size_target_pts≈15–25` band, `bundle_doctrine_frame=F1` (default). [CONTEXT]

### Summary (30 seconds)

A tight, coherent 3-issue close-out-conformance release (12 raw / 12 effective pts — under the 15–25 band, acceptable per bundle-composition-doctrine § 3 Step 5 "capability boundary is clear"). The three issues harden the release-notes close-out path so it is conformant on **every** authoring path (script AND hub-direct): extend the §3.2 lint to catch the defects it misses (#2120), run that lint on the hub-direct close event (#2082), and deterministically re-emit + drift-check the published Release body from the in-repo source note (#2085). **No native dependency links exist** among the three [SOURCE: GraphQL — all `trackedIssues`/`trackedInIssues` empty]; the milestone's #2120→#2082→#2085 order is a recommended build sequence (acyclic), confirmed. The release **plans and builds now** but **MUST MERGE after #1290** (OPEN, M72 #167) and serialize with M92 (#235, OPEN) — both are merge-contention on the shared file `release/tools/automated-closeout.sh`, **not** logical prerequisites [SOURCE: gh + file grep]. Recommended D-decisions: **D-Class = routine**, **D-Version = v2.37** (provisional; binds at Stage-12 atomic claim), **D-C = SINGLE-branch** (default). Quota Budget: **PASS** (worst parallel wave = 3 size-M spokes; well under envelope).

**Load-bearing correction to issue Affected-Files (carry into Stage 5/6):** #2120's 4th AC names a "Stage 13 close runbook" posted-surface-verify step. There is **no** `stage-13-closeout.md` file — the canonical Stage-13 output-set verification surface is the **Procedure 7 Step 4 completion-verification table in `release/references/how-to/hub-spoke-bridge.md`** (and its mirror in `release/references/pipeline/stage-13-close.md`). The posted-surface verify step must land there. [SOURCE: filesystem — `stage-13-closeout.md` absent; `stage-13-close.md` present; hub-spoke-bridge.md:1589 Procedure 7]

---

### Dependency Graph

**Native links: NONE.** [SOURCE: GraphQL on #2120/#2082/#2085 — every `trackedIssues` and `trackedInIssues` node-set empty 2026-06-28; cross-references are narrative-only: #2082↔#2085 (sibling mention), all↔#2360 (Stage-4 sub-task).]

```
#2120  (story — extend lint coverage: 6b link-purity + versioned-title reconcile)
   │  (recommended-build edge: a complete lint is the thing #2082 wires onto the close event)
   ▼
#2082  (bug — bind §3.2 lint to the close EVENT, incl. hub-direct path)
   │  (recommended-build edge: lint runs at close first; body re-emit/drift sits downstream of it on the same close path)
   ▼
#2085  (bug — deterministic Release-body re-emit + drift check on the same close path)
```

- **Edge class:** all edges are **soft / sequencing-only** (recommended build order), **acyclic**. No edge is a hard blocker — each issue is independently buildable; the order minimizes rework (build the lint's coverage once, then wire it, then add the body guard that reuses the same close-path plumbing). [INFERRED from issue bodies + Internal Sequence in milestone description]
- **Cross-milestone (merge-contention, NOT logical deps):**
  - **#1290** (OPEN, M72 `72-closeout-output-determinism` #167) — "MUST SERIALIZE AFTER" per the milestone Parallelization Map. Contention surface: `release/tools/automated-closeout.sh` (B1 touches it; M91 is B2). [SOURCE: gh issue view 1290 — state OPEN, milestone 72]
  - **M92** (`92-version-identity-and-parser-ssot` #235, OPEN, 6 open issues) — "SERIALIZE WITH" on the close-out/version surface (`automated-closeout.sh` + Stage-13 close path). [SOURCE: gh api milestones]
  - Both are **merge-order** constraints only — M91 can plan/build/PR now; only the **merge to main** must follow #1290 and coordinate with M92.

### Implementation Sequence

**Confirmed: #2120 → #2082 → #2085** (matches milestone Internal Sequence). Rationale — verified against the live code, not just the issue prose:

1. **#2120 (foundation — lint coverage).** Extend `lint_release_corpus.py` to scan the **whole note body** for repo-relative links (currently `check_note_content()` extracts **only** Section 6a via `extract_section_6a()`; there is no 6b extractor at all) and reconcile the conflicting title-format wording to the single `vX.Y — <headline>` form across `release-notes-standard.md` + `stage-12-execute.md:138/:180`. Building this first means #2082 wires a lint that already catches the full defect class. [SOURCE: `core/deploy/tools/lint_release_corpus.py:310,347` — `extract_section_6a` is the sole section extractor]
2. **#2082 (infrastructure — bind lint to close event).** Make any Stage-13 close that authors a note run `lint_release_corpus.py --check note-content` and block milestone-close on a finding for the new version, **regardless of path** (script OR hub-direct). Depends-for-rework-minimization on #2120 (wire the complete lint, not a partial one). [SOURCE: `automated-closeout.sh` close path has zero `lint_release_corpus` invocation — confirmed by grep]
3. **#2085 (skill-core — body re-emit + drift check).** Emit the published Release body via the §5.1 deterministic frontmatter-strip transform **from** the in-repo note, and add a drift check (`published body == frontmatter-stripped in-repo note`). Sits downstream of #2082 on the same close path; `automated-closeout.sh:publish_github_release` (phase 15.5) **already** does the frontmatter-strip + versioned-title in the SCRIPT path — so #2085's net-new work is (a) the **drift check** and (b) extending the deterministic emit to the **hub-direct** path + the post-correction re-emit-all-three-surfaces rule. [SOURCE: `automated-closeout.sh:~1357` — `notes_body="$(sed '1,/^---$/d; 1,/^---$/d' …)"` + `--title "$VERSION — $headline"`]

**No revision to the proposed order.** The order is rework-optimal and the edges are soft, so a constrained run could also build #2120 ‖ #2085 in parallel (disjoint enough — see Contention Map), but the recommended serial order is cleaner for a single-operator release.

### Stage Applicability Matrix

Per-issue applicability for Stages 5–13. **REDUCE** = applies but lightweight. Rationale localized to this release (governance/tooling-internal, `domain: governance` + `software` for the lint code).

| Stage | #2120 (story) | #2082 (bug) | #2085 (bug) |
|---|---|---|---|
| **5 Solutioning** | **APPLY** — issue explicitly defers mechanism to "Stage 5 work" (the "renders on the Release surface" link-rule design + title-reconcile method). [SOURCE: #2120 Risks line "Mechanism is Stage 5 work"] | **APPLY (REDUCE)** — one design call: *where* to bind the lint (close-event hook in `automated-closeout.sh` vs `deploy.sh` Check-20 path vs a Stage-13 runbook gate) so it fires on the hub-direct path. | **APPLY (REDUCE)** — design the drift-check siting (deploy.sh/CI vs Stage-13 close) + the post-correction re-emit-all-surfaces rule. Carry the hub-flagged Stage-5 design note: §3.2's "lint only from Mode E" wording is imprecise (lint also lives in deploy.sh Check 20) — design against the real surface. |
| **6 Engineering** | **APPLY** — edits to standard + stage-12 doc + lint code (+ Procedure 7 / stage-13-close runbook for the AC-4 verify step). | **APPLY** — close-path wiring (script + runbook). | **APPLY** — emit-transform + drift-check code. |
| **7 Dev Testing** | **APPLY** — AC-3 is an **executable lint-fixture test** (add a 6b-relative-link fixture note; assert non-zero exit). This is a real Stage-7 surface. [SOURCE: #2120 AC-3 method] | **APPLY** — reproduce the hub-direct close → assert lint now runs + blocks on a non-conformant note. | **APPLY** — fixture: published-body vs in-repo-note divergence → assert drift check flags it. |
| **8 QA Testing** | **APPLY (REDUCE)** — grep-based AC verification (title-format single-form; 6b link rule present). | **APPLY (REDUCE)** — confirm the gate blocks close on both paths; no regression to the script path. | **APPLY (REDUCE)** — confirm three-surface parity for a corrected note. |
| **9 Plan Review** | **APPLY (release-level)** — single GO/NO-GO for the bundle (human gate); not per-issue. | (same release-level gate) | (same release-level gate) |
| **10** (n/a in 13-stage close set) | — | — | — |
| **11** (n/a) | — | — | — |
| **12 Execute** | **APPLY (release-level)** — version atomic-claim + tag + Surface-1 emit; one event for the bundle. Re-verify v2.37 freeness at Engineering Commit 0 and at the Stage-12 claim. | (release-level) | (release-level) — note this release **changes** the Stage-12/13 emit path it rides on; verify the changed `publish_github_release` against its own release at Stage 12. |
| **13 Close** | **APPLY** — and this release **modifies** the Stage-13 close surface (Procedure 7 verify table). Dogfood caution: run the close with the **pre-merge** runbook for v2.37 itself (reflexive-loop — the new gate cannot bind its own close). | **APPLY** — same reflexive caution (the lint-on-close gate ships here; it does not retroactively gate v2.37's own close). | **APPLY** — same reflexive caution (drift check ships here). |

> **Stage 10/11 note:** the 13-stage pipeline's close arc is 9 (Plan Review) → 12 (Execute) → 13 (Close); stages 10/11 are not separate gate surfaces in the close set used here, so they are marked n/a for all three. [INFERRED from stage-13-close.md + hub-spoke-bridge Procedure 5/6/7 structure]
> **Reflexive-pipeline-loop discipline applies to all three at Stages 12/13:** every fix in this bundle hardens the very close path v2.37 itself rides. Per the cutover discipline stamped throughout the pipeline specs, the introducing release is exempt from its own new gate — v2.37's close runs under the pre-merge rules. Carry as an Engineering/Close note.

### Contention Map

**Within-release file contention** (files claimed by >1 issue — sequencing resolves all; no scope-split needed):

| File | #2120 | #2082 | #2085 | Resolution |
|---|---|---|---|---|
| `release/references/standards/release-notes-standard.md` | edit (§5.3 title wording; Check-12 link-purity beyond 6a) | edit (§3.2 "lint runs on close regardless of path") | edit (§5.1 deterministic-transform + drift-check rule) | **Serialize per build order** (#2120→#2082→#2085). Three disjoint sections (§5.3/§3.2/§5.1) — low real conflict, but same file → serialize edits. |
| `core/deploy/tools/lint_release_corpus.py` | edit (whole-body link check; new 6b coverage) | edit (close-event invocation binding, if lint gains a close-path entrypoint) | edit-or-new-check (drift check may live here or as a new check) | **Serialize.** #2120 owns the structural extension; #2082/#2085 extend the same module. Primary contention surface — sequence strictly. |
| `core/deploy/deploy.sh` | edit (Check-20 wiring if lint signature changes) | edit (Check-20 close-path wiring) | edit (drift-check wiring if added to deploy.sh) | **Serialize.** Check-20 block is the shared hunk. |
| `release/references/pipeline/stage-12-execute.md` | edit (`:138`/`:180` title-form reconcile) | — | edit (Surface-1 emit-from-note path) | Serialize #2120 then #2085; different regions. |
| `release/tools/automated-closeout.sh` | — | edit (lint invocation on close path) | edit (`publish_github_release` drift check + hub-direct emit) | **Serialize + CROSS-MILESTONE FLAG (see below).** Both #2082 and #2085 touch the same `publish_github_release` / close region. |
| `release/references/how-to/hub-spoke-bridge.md` (Procedure 7) **and/or** `release/references/pipeline/stage-13-close.md` | edit (AC-4 posted-surface verify step) | edit (close-event lint gate in the runbook) | edit (re-emit-all-surfaces rule in the runbook) | **Serialize.** All three add steps to the Stage-13 close runbook surface. |

**CROSS-MILESTONE CONTENTION — `release/tools/automated-closeout.sh` (HIGH attention):**
- **#1290** (M72 #167, OPEN) edits `automated-closeout.sh` (B1). M91's #2082+#2085 edit the same file (B2). **Merge order: #1290 BEFORE M91.** [SOURCE: milestone Parallelization Map + gh issue 1290 state OPEN]
- **M92** (#235, OPEN) shares the close-out/version surface (`automated-closeout.sh` + Stage-13 close path). **Serialize with M92** — coordinate merge windows; whichever lands first, the other re-baselines its `automated-closeout.sh` hunks.
- This is a **structural merge edge, not a logical dep** — M91 builds in parallel; the constraint binds at PR-merge time. Recorded in the Risk Register as the top contention risk. Re-run the A4 sibling-merge stale-pin check at Stage 9 entry (`git log <pin>..origin/main --name-status` ∩ this release's `automated-closeout.sh`).

**File Change Matrix** (machine-readable path list — one repo-relative path per line, deduped across all three issues, for downstream Stage-7/8/9 overlap extraction per stage-04-planning.md Cross-PR Overlap Audit):

```
core/deploy/deploy.sh
core/deploy/tools/lint_release_corpus.py
release/references/how-to/hub-spoke-bridge.md
release/references/pipeline/stage-12-execute.md
release/references/pipeline/stage-13-close.md
release/references/standards/release-notes-standard.md
release/tools/automated-closeout.sh
```

> **Path-list provenance / corrections vs the issue bodies:**
> - `release/references/pipeline/stage-13-close.md` and `release/references/how-to/hub-spoke-bridge.md` are **added** to the matrix beyond what the issue bodies named — #2120 AC-4's "Stage 13 close runbook" verify step has no home in the issue's Affected-Files list; the real surface is Procedure 7 (hub-spoke-bridge.md) + stage-13-close.md. [SOURCE: filesystem; `stage-13-closeout.md` does NOT exist]
> - #2082/#2085 name `release/tools/automated-closeout.sh` and the "hub-direct Stage-13 close procedure (hub-spoke-bridge.md Procedure 7)" in prose — matrix makes both explicit.
> - All 7 paths **verified present on disk** 2026-06-28 except the non-existent `stage-13-closeout.md` (replaced by the two real surfaces above). [SOURCE: `ls`]

### File Change Matrix

| File | #2120 intent | #2082 intent | #2085 intent | Net intent |
|---|---|---|---|---|
| `core/deploy/tools/lint_release_corpus.py` | **EDIT** — whole-body link-purity check; flag `](../`, `](release/`, `](core/` outside 6a; require absolute `https://github.com/...` for Release-surface links | **EDIT** — bind/invoke note-content lint to the close event (entrypoint/exit-code contract for the close path) | **EDIT or ADD-check** — drift check (`published body == frontmatter-stripped note`) may be a new `--check` here | EDIT |
| `core/deploy/deploy.sh` | **EDIT** — Check-20 wiring if lint signature changes | **EDIT** — Check-20 / close-path wiring | **EDIT** — drift-check wiring (if sited in deploy.sh) | EDIT |
| `release/references/standards/release-notes-standard.md` | **EDIT** — single versioned title rule (§5.3); extend Check-12 link-purity beyond §6a | **EDIT** — §3.2 "lint runs on close regardless of authoring path" | **EDIT** — §5.1 deterministic-transform enforcement + drift-check + re-emit-all-surfaces | EDIT |
| `release/references/pipeline/stage-12-execute.md` | **EDIT** — reconcile `:138` vs `:180` to one versioned `--title "v<X.Y> — $HEADLINE"` form | — | **EDIT** — Surface-1 body emitted via §5.1 transform from the in-repo note | EDIT |
| `release/tools/automated-closeout.sh` | — | **EDIT** — invoke note-content lint on the close path; block close on a finding | **EDIT** — `publish_github_release` drift check; extend deterministic emit to hub-direct path | EDIT |
| `release/references/how-to/hub-spoke-bridge.md` | **EDIT** — Procedure 7 Step 4: add posted-surface verify (title-format + no repo-relative body link) before milestone close | **EDIT** — Procedure 7 close-event lint gate | **EDIT** — Procedure 7 re-emit-all-§5.1-surfaces rule | EDIT |
| `release/references/pipeline/stage-13-close.md` | **EDIT** — mirror the Procedure-7 posted-surface verify into the Stage-13 close spec | **EDIT** — mirror the close-event lint gate | **EDIT** — mirror the drift/re-emit rule | EDIT |

- **No DELETEs. No NEW files** (a new `--check` in the existing lint module is an in-file add, not a new file). All intents are EDIT against existing, on-disk surfaces. [SOURCE: filesystem verification]

### Risk Register

| ID | Class | Risk | Owner | Mitigation | Reversibility |
|---|---|---|---|---|---|
| R1 | **Contention (cross-milestone)** | `automated-closeout.sh` is edited by **#1290 (M72, OPEN)** and **M92 (OPEN)** as well as M91's #2082/#2085 → merge conflict / re-baseline if M91 merges out of order. | Release Mgr | **Merge M91 AFTER #1290.** Coordinate merge window with M92 (whichever lands first, the other re-baselines its `automated-closeout.sh` hunks). Re-run A4 sibling-merge stale-pin check at Stage 9 + Stage 12 entry (`git log <pin>..origin/main --name-status --find-renames` ∩ `automated-closeout.sh`). | MODERATE (re-baseline = hours) |
| R2 | **Dependency (rework)** | Building #2082/#2085 before #2120's lint coverage lands → wire/test a partial lint, redo on #2120 merge. | Release Mgr | Hold the recommended serial order #2120→#2082→#2085. Edges are soft, so this is rework-avoidance, not a hard block. | CHEAP |
| R3 | **Scope (stale-premise)** | #2082's claim "lint invoked ONLY from release-executor Mode E" is **imprecise** — the lint also lives in `deploy.sh` Check 20; designing the close-event binding against the wrong premise mis-sites the gate. | Stage-5 design | Carry the hub's Stage-5 design note: bind against the **real** surfaces (Mode E Step 4 **and** deploy.sh Check 20 **and** the hub-direct close). Substantive defect (no §3.2 content lint on hub-direct close) still holds — verified. [SOURCE: lint grep + #2082 comment thread] | CHEAP |
| R4 | **Scope (AC homing)** | #2120 AC-4 names a "Stage 13 close runbook" with no matching file (`stage-13-closeout.md` absent) → the verify step could be authored to a non-existent or wrong surface. | Stage-5/6 | Home the verify step in **hub-spoke-bridge.md Procedure 7 Step 4** (+ mirror in `stage-13-close.md`). Stated in the File Change Matrix. | CHEAP |
| R5 | **Rollback-complexity (reflexive loop)** | All three fixes harden the close path **v2.37 itself rides** → a bug in the new gate could block v2.37's own close. | Engineering/Close | Apply reflexive-pipeline-loop exemption: v2.37 closes under **pre-merge** rules; the new gate does not bind its own introducing release. Stamp the cutover in the release-log entry. | CHEAP (gate is opt-in until cutover SHA) |
| R6 | **False-positive (lint over-reach)** | #2120's "no repo-relative links" rule could false-positive on links that are legitimately relative in the **file** context. | Stage-5 design | Key the rule on "renders on the Release surface" (require absolute URLs in the body that publishes to Surface 1), **not** a blanket ban on relative links in the file. Lint-fixture test (AC-3) must include a legitimate-relative-in-file negative case. [SOURCE: #2120 Risks line] | CHEAP |
| R7 | **Currency (under-band)** | 12 pts is below the 15–25 target band → "is this a real standalone release?" challenge. | Operator (D-Class) | Bundle-composition-doctrine § 3 Step 5 permits under-band for a clear capability boundary; close-out-conformance is one coherent capability (all three fix the same close path). Merge/split analysis (below) confirms coherent + distinct. | N/A (planning judgment) |

**Rollback strategy (release-level):**
- **Mechanism:** every change ships via the standard release branch + PR (D-C SINGLE). Rollback = `git revert <merge-SHA>` of the v2.37 merge (single-branch → one revert restores prior close-path behavior). [SOURCE: core/rules/git-workflow.md — all changes via PR]
- **Granularity:** because the three issues land as commits on one branch and are sequenced, a partial rollback (e.g., revert only the #2085 drift check while keeping #2120 lint coverage) is possible by reverting the specific commit range — but the **recommended** rollback is whole-release revert given the shared `automated-closeout.sh` / lint surfaces.
- **Reversibility tier:** **CHEAP–MODERATE.** Pure governance/tooling edits (lint, runbook, standard) revert in hours with no data loss (CHEAP). The one MODERATE factor is the cross-milestone `automated-closeout.sh` contention (R1) — a revert after #1290/M92 have layered on the same file requires conflict-aware reverting (still hours, no stakeholder/data impact).
- **Blast radius:** internal close-path tooling + governance docs only; no public-facing API, no operations-module surface, no data migration. A regression manifests as a close-out lint false-positive/negative or a release-body emit error — caught at the next release's Stage 12/13, recoverable via `gh release edit` + re-run.

### Quota Budget

**Verdict:** **PASS** (per `quota-budget-protocol.md` Checkpoint A — § 3.2 bands)
**Parallel-eligible spokes per parallel stage (from A2 Stage Applicability Matrix):** Stage 5: **3** · Stage 7: **3** · Stage 8: **3** (all three issues apply at each parallel-safe stage; Stage 6 Engineering and Stage 13 Close are write-serialized → excluded from the parallel-batch surface per protocol § 1).
**Per-spoke cost estimate:** size-bucket ordinal band — all three issues are **size:M** (milestone scope marks each `M, ~4 [est]`) → **low–moderate** band (source: § 5 heuristic; no telemetry medians available yet). [SOURCE: milestone Scope section]
**Assumed/stated remaining usage-window envelope:** conservative default — operator quota state not stated to this spoke; **concurrent releases M97 and M104 draw on the same per-account 5-hour window** (both have Stage-4 sub-tasks in flight), so the realizable envelope for M91's waves is *shared*, not exclusive. [CONTEXT — hub note + gh milestone list]
**Estimated cumulative draw % (worst parallel batch):** worst wave = **3 × size:M (low–moderate)** = a small cumulative draw; **< 50% of a single fresh window** for M91's own batch. **Routing: PASS** for M91 in isolation.
**Routing:** **PASS** — proceed parallel; no warning required for M91's own waves.
**Cross-release caveat (advisory):** because M91/M97/M104 share the window, the **aggregate** of three concurrent releases each firing a 3-spoke wave could push the *combined* draw into WARN territory at runtime. That is a **Checkpoint-B (runtime, load-bearing)** concern, not a Checkpoint-A blocker — flag it so the hub's per-wave Checkpoint B re-validates M91's Stage-5/7/8 waves against the **remaining** (shared, possibly-depleted) envelope and renders SERIALIZE/DEFER if M97/M104 have already drawn the window down.
**Note:** Checkpoint B re-validates at every parallel wave (runtime, load-bearing) with PROCEED/SERIALIZE/DEFER/REDUCE-scope; STAGGER is a secondary rate-limit-only defense, not a usage-window mitigation. Bands + cumulative-draw budget are `[CALIBRATE-AFTER-3]` MEDIUM.

### Operator Decisions

#### D-ReleaseClass: What Release Class governs v2.37?
**Gate input:** Milestone description declares `Class: routine` (Rationale: "Three related defect fixes on the release-notes close-out path — bounded, single-surface"). Spoke concurs.
**Pre-decided (if applicable):** Milestone-declared `routine` — citing milestone #234 description `## Release Class` block. Hub default `default_release_class=routine`.
**Gate decision:** {routine | significant | major}. **Recommend: routine.**
**Trigger-condition evidence:** Bounded scope (3 issues, 12 pts, under-band); single capability surface (release-notes close-out conformance); no new component, no public-API change, no data migration, no cross-module blast radius; all edits internal governance/tooling. This matches the routine trigger profile (defect-cluster hardening on one path).
**Differentiation posture:** Routine engagement density — standard single Stage-9 GO/NO-GO; standard Stage-9 review depth (no Collective-Review escalation, no extra design gate). The one elevated-attention item is the **cross-milestone merge contention (R1)** — surface it at Stage 9 but it does not lift the class.
**Blocks:** Stage-9 review depth + engagement cadence; does not block sub-task start.
**Upstream compatibility:** **N/A — this D-decision does not modify skill-authoring surface.** Upstream compatibility check does not apply (PMO-internal release-class taxonomy; no Anthropic skill-creator coupling).
**Design-Principle Conformance:** N/A — no design-principle `scope_predicate` matches a class-selection decision (pure ceremony-level classification, no change surface).
**Reversibility / Confidence:** **CHEAP / HIGH** — class is re-selectable at Stage 9 if scope shifts.

#### D-Version: What version does v2.37 claim?
**Gate input:** Authoritative anchor = latest published GitHub Release. Verified live: latest published = **v2.36** (2026-06-28T07:26) → rule-determined next-free = **v2.37**. [SOURCE: `gh release list` 2026-06-28 — v2.36 is `Latest`; no v2.37 present]
**Pre-decided (if applicable):** OMIT (no operator pre-decision on record; provisional v2.37 is rule-determined, not directed).
**Gate decision:** Claim **v2.37** (provisional). Binds only at the **Stage-12 atomic claim**.
**Authoritative-selection note:** v2.37 is provisional — **concurrent release activity exists** (M97 #233-class and M104 both have Stage-4 sub-tasks in flight; M92 #235 also active). A sibling may claim v2.37 first. **Re-verify at Engineering Commit 0** (first Engineering spoke, immediately before writing the plan file: `git fetch --tags origin && gh release list`; recompute next-free; HALT-and-re-render if v2.37 is taken) and again at the **Stage-12 atomic claim**. Detection rung 1 (Commit-0 re-verify) + Stage-9 divergence re-check + Stage-12 freeness check + atomic claim per hub-spoke-bridge Procedure 0 § Commit-0 version re-verify. [SOURCE: hub-spoke-bridge.md:212]
**Blocks:** Stage-12 tag/emit; the plan-file name (`v2.37_RELEASE_PLAN.md`) is a narrative identifier, not load-bearing.
**Upstream compatibility:** **N/A — this D-decision does not modify skill-authoring surface.** Version-slot selection is a release-internal allocation; no Anthropic coupling.
**Design-Principle Conformance:** N/A — no register `scope_predicate` matches a version-allocation decision.
**Reversibility / Confidence:** **CHEAP / HIGH pre-Engineering** (re-selectable until the Stage-12 atomic claim; the theme-named branch insulates the branch from a version rename). Becomes EXPENSIVE only post-tag.

#### D-C Branch Topology: Single-branch or per-issue branches?
**Gate input:** File-contention profile (above) + Delivery Strategy. Three issues, heavy **within-release** file overlap (all three touch `release-notes-standard.md`, `lint_release_corpus.py`, `deploy.sh`, the Stage-13 runbook surface; #2082+#2085 co-touch `automated-closeout.sh`).
**Pre-decided (if applicable):** OMIT. (D-C SINGLE is the documented default per hub-spoke-bridge Procedure 0 § Canonical location.)
**Gate decision:** {SINGLE-branch (default) | OPTION-A per-issue branches+PRs}. **Recommend: SINGLE-branch.**
**Justification:** The three issues share most of their edit surface (same lint module, same standard, same close runbook, same `automated-closeout.sh` region). Per-issue branches (OPTION-A) would generate **within-release** merge conflicts on every shared file and force constant re-baselining — OPTION-A's value (independent early-merge) is defeated when the issues are this tightly coupled. SINGLE-branch with the sequenced commit order (#2120→#2082→#2085) lets each issue's edits layer cleanly on the prior. The plan file commits as **Engineering Commit 0** on the release branch. OPTION-A's per-issue-PR isolation buys nothing here and costs conflict overhead.
**Blocks:** Plan-file commit mechanism (Engineering Commit 0 vs Stage-4 chore-PR); Procedure 1 scaffolding shape.
**Upstream compatibility:** **N/A — this D-decision does not modify skill-authoring surface** (branch topology is a release-mechanics decision). Upstream compatibility check does not apply.
**Design-Principle Conformance:** N/A — no register `scope_predicate` matches a branch-topology decision (pure sequencing/branch-naming surface, explicitly an N/A case per the D-Gate applicability note).
**Reversibility / Confidence:** **CHEAP / HIGH** — topology is re-selectable before Engineering Commit 0.

### Recommendations

1. **APPROVE the bundle as a routine v2.37 release** — 3 issues, coherent close-out-conformance capability, 12 pts (under-band, justified). No merge/split change.
2. **Merge/split verdict: keep all three distinct + together (Mode R confirmed).** They are **distinct** (lint-coverage / lint-on-close-event / body-drift-check — three different mechanisms) and **coherent** (all harden the same close path so notes are conformant on every authoring path). Do **not** merge into one issue (loses the bug-vs-story typing and the per-defect AC traceability); do **not** split into separate releases (they share `automated-closeout.sh` + the lint module — splitting multiplies the cross-milestone contention). [SOURCE: issue bodies + sibling-defect framing in #2085]
3. **Build order #2120 → #2082 → #2085** (confirmed; rework-optimal; soft edges).
4. **D-decisions:** D-Class **routine**, D-Version **v2.37** (provisional — re-verify at Commit 0 + Stage 12), D-C **SINGLE-branch**.
5. **Top operational watch-item: R1 cross-milestone `automated-closeout.sh` contention.** Gate the **merge** (not the build) behind #1290 (M72, OPEN) and coordinate with M92 (#235). Re-run the A4 sibling-merge stale-pin check at Stage 9 and Stage 12 entry.
6. **Carry into Stage 5:** (a) #2120 link-rule keys on "renders on Release surface," not blanket relative-link ban (R6); (b) #2082 design against the **real** lint surfaces — Mode E Step 4 + deploy.sh Check 20 + hub-direct close — not the imprecise "Mode E only" premise (R3); (c) #2120 AC-4 verify step homes in **hub-spoke-bridge Procedure 7 Step 4** + `stage-13-close.md`, not the non-existent `stage-13-closeout.md` (R4).
7. **Reflexive-loop caution (R5):** v2.37 closes under pre-merge rules — the new lint-on-close / drift / verify gates do not bind their own introducing release. Stamp the cutover SHA in the release-log entry.
8. **Quota: PASS for M91's own waves**, but flag the shared-window aggregate (M97/M104 concurrent) for Checkpoint-B runtime re-validation.

---
*Stage 4 Release Planning complete. Read-only analysis — no files edited, no branches created. Provisional v2.37 binds at Stage-12 atomic claim. Evidence labels: [SOURCE] = verified against live ref/file; [INFERRED] = derived from cited inputs; [CONTEXT] = hub-provided/situational; [ASSUMPTION – CONFIRM] = stated assumption needing operator confirmation.*

---

## Approval Record

### Stage 4 Plan Approval (Procedure 0 Step 7)

**Operator verdict:** APPROVE as-is → proceed to Procedure 1 (scaffolding). [2026-06-28, main-thread Decision Briefing]

**Authorized:**
- Release Class: **routine**
- D-Version: **v2.37** (provisional — rule-determined next-free above v2.36 anchor; binds at the Stage-12 atomic claim; re-verify at Engineering Commit 0)
- Branch topology: **SINGLE-branch** (D-C SINGLE)
- Build sequence: **#2120 → #2082 → #2085**
- Scope expansion accepted: **+2 files** (`hub-spoke-bridge.md` Procedure 7, `stage-13-close.md`) homing #2120 AC-4 posted-surface verify
- Release Outcome Statement: **ratified** (already present in milestone #234 description)

**Top watch-item carried:** R1 cross-milestone contention on `automated-closeout.sh` — merge M91 AFTER #1290 (M72, OPEN); coordinate with M92 (#235, OPEN); A4 sibling-merge stale-pin re-check at Stage 9 + Stage 12 entry.

Reversibility: CHEAP (plan-level) · Confidence: HIGH.

### Stage 5 Solutioning Accepted (Procedure 4)

**Operator verdict** [2026-06-28]: ACCEPT all 3 Stage-5 designs → route to Stage 6. **#2085: KEEP FULL SCOPE** (operator override of the proposed trim — build the emit-path hardening explicitly, not just the detective drift-check; the v2.26 incident showed a convention-level mandate is not enforcement).

- **#2120** — whole-body link rule **floored at v2.37 + warn-mode-initial** (load-bearing: 42/64 historical notes carry `](../RELEASE_LOG` 6b links); single versioned-title reconcile incl. `release-notes-standard.md:575` + `:133` template; posted-surface verify in Proc 7 + `stage-13-close.md`. No ADR.
- **#2082** — defense-in-depth: `phase_lint_release_notes` in `automated-closeout.sh` + runbook content-conformance row; caller-side grep version-scoping; legacy debt not retroactively blocked. No ADR.
- **#2085** — **FULL scope**: enforce deterministic emit on all paths (build on the existing transform, do not duplicate) + new gh-guarded `check-release-body-drift.sh` (warn-mode deploy.sh check + automated-closeout verification phase) + §5.6 three-surface re-emit rule. Detective-only. No ADR.

Batched [ASSUMPTION-CONFIRM] proposed answers accepted. Next: Stage 6 Engineering, write-serialized #2120→#2082→#2085 on one release branch. Reversibility MODERATE · Confidence HIGH.
