<!-- repo-integrity: allow-issue-ref -->
# v3.95 Release Plan — runtime-config-and-posture

Milestone: 56-runtime-config-and-posture (milestone #183) · Release **v3.95** (recorded determination — rule-computed next-free minor; binds atomically at the Stage-12 tag-claim)
Release class: **novel** (operator-adjudicated 2026-07-25) · effective **25 pts** (raw 22 × 1.15) · band 15–25 · **at capacity (25/25)**
Reversibility: **MODERATE** (branch, git-revertable; nothing on `main` until Stage 12)
Topology: **D-C SINGLE** — one release branch, one PR, one merge (this plan lands as Engineering Commit 0)
Concurrency posture: **P0 fully-serial** (write-serialized on the shared branch)
Identity: versioned
Scope: **#1212 → #102 → #340 → #310 → #339** (serial build order) · one hard dependency edge (**#340 → #339**) · one real file-contention pair (**#310 ↔ #339**) · a ratified three-issue config-surface convergence (**#1212 / #340 / #310** on `core/config/platform-config.toml.template`, disjoint sections)
Branch base: cut from `origin/main` `627b4776` (the release branch equals `main` at Commit-0; origin carries `release/v3.93-runtime-config-and-posture` from the first Engineering push forward). Prepared + Commit-0 authored 2026-07-25 (Sat).

> **Provenance.** This file transcribes the operator-approved Stage-4 release plan (the Release Manager planning-spoke comment on Stage-4 sub-task #3943, 2026-07-25) and the Collective-Review scope-lock decisions (the operator decision comment on #3943, 2026-07-25) into the standard release-plan file format at Engineering Commit 0. The plan content is a transcription, not a re-plan; the three Collective-Review-ratified design decisions are carried in § Collective Review Scope-Lock Decisions and reflected in the File Change Matrix + Contention Map.

## Summary (30 seconds)

- **Five-issue runtime-config-and-posture bundle**, target **v3.95** (recorded determination). Built as **one release branch → one PR → one merge** (milestone-one-PR model) in strict dependency order under **D-Concurrency Posture P0 (fully-serial)** + **D-C SINGLE** topology. Rollback = revert.
- **One hard dependency edge (#340 → #339):** #339's SessionStart hook reads the canonical surface #340 establishes — build #340 first (eliminates the dual-source interim).
- **One real file-contention pair (#310 ↔ #339):** both edit `core/rules/bypass-mode-readiness.md` — sequenced #310 → #339; Check 9 byte-identity green after the final commit.
- **Ratified config-surface convergence (Collective Review):** `[spoke_runtime]` (#340) + `[security_hooks]` (#310) converge on `core/config/platform-config.toml.template` as **separate sections** — the same file #1212 adds `[failure_mode_detectors]` + the rollout-log surface to. Three writers, one file, disjoint sections, resolved by serial build-order.
- **Release Class — governed check flags a cross-cutting near-miss; operator adjudicated `novel`** at the plan-approval gate (see § Release Class Declaration). Effective 25 pts (raw 22 × 1.15), at the 15–25 band ceiling.
- **Two security postures locked at Collective Review:** master-OFF is **SECURITY-EXCLUDED** (never silently disables the always-enforce floor / public-surface security hooks); G2-12 **folds into #102**.
- **Top risk:** #310 blast radius across 13–14 hooks (R2) — mitigated by a shared-lib master-enable + default-OFF + the SECURITY-EXCLUDED scope.

## Dependency Graph

```
#1212  platform-config [failure_mode_detectors] (D6/D8) + rollout-log surface   (isolated, additive)
   │   (no edges — pure additive config)
   ▼
#102   G2-06 + G2-12 reworded → config-resolved backlog surface                  (isolated, gate-wording)
   │   (no edges)
   ▼
#340   spoke model+effort canonical surface + [spoke_runtime] config section     (first of the trio)
   │   HARD edge: #340 → #339  (#339 reads #340's canonical surface)
   │   DESIGN-CONVERGENCE: #340 ↔ #310  (both write platform-config.toml.template — ratified separate sections)
   ▼
#310   security-hook master-enable + [security_hooks] config section             (durable carrier + master-OFF gate)
   │   COORDINATION edge: #310 ↔ #339  (shared .mode / CLAUDE_HOOK_BYPASS + bypass-mode-readiness mirror)
   ▼
#339   SessionStart verify-session-config hook (reads #340's surface; honors #310's master-OFF)
```

Directional / coordination edges (evidence-cited from the Stage-4 plan):

- **#340 → #339** (directional, soft-blocking): #340 "Blocks #339 … sequence #340 → #339"; #339 reads #340's canonical surface. Build #340 first to eliminate the dual-source interim. [SOURCE]
- **#310 ↔ #339** (coordination, non-blocking): shared `.mode` / `CLAUDE_HOOK_BYPASS` carrier + the `bypass-mode-readiness.md` mirror. [SOURCE]
- **#310 ↔ #340** (design-convergence, non-blocking → **ratified at Collective Review**): the durable carrier converges with the canonical surface — both write `platform-config.toml.template` as separate sections. [SOURCE]
- **#102, #1212** isolated (no edges). External **#22 CLOSED** — no leak. **No cycles.**

## Implementation Sequence

Strict serial, dependency-ordered, on one release branch:

1. **#1212** — add `[failure_mode_detectors]` (D6 concurrency-ceiling + D8 walk-back-rate keys) + the rollout-log config surface to `core/config/platform-config.toml.template`, on the existing `[bundling]` sibling-section pattern; additive + disjoint. *Isolated — first because it clears the config file's first new-section beachhead and unblocks nothing/blocks nothing.* **← THIS SPOKE (Stage 6, first engineering spoke; also authors this plan file at Engineering Commit 0).**
2. **#102** — reword **G2-06 AND G2-12** (symmetric twins — G2-12 folded in per Collective Review) in `core/schemas/gate-criteria-spec.md` to the config-resolved backlog surface; edit `release/references/pipeline/stage-02-triage.md` so decision-date resolves the backlog surface from config. *Isolated gate-wording.*
3. **#340** — establish the spoke model+effort canonical surface (`core/config/allowlists/agents-model-overrides.txt` + effort field), register it in the Governance File Map (`core/CLAUDE.md.template`), teach the chip-prompt to read it (`release/references/how-to/hub-spoke-bridge.md`), author the ADR (reverses #2535 "two surfaces by design"; next-free number), and add the `[spoke_runtime]` section to `platform-config.toml.template`. *First of the trio — establishes the surface #339 reads.*
4. **#310** — add the security-hook master-enable (shared-lib `core/hooks/lib/master-enable.sh` collapsing the 13–14 hook edits to one lib), gate the block-* hooks + `git-pre-commit-pii.sh` on it (master-OFF scope **SECURITY-EXCLUDED** per Collective Review), wire the opt-in (default OFF) through `docs/scripts/setup-workspace.sh` + `install.sh` / `update.sh`, add the `[security_hooks]` section to `platform-config.toml.template`, and edit `core/rules/bypass-mode-readiness.md` **first** (C1 writer 1). *Durable carrier + master-OFF gate.*
5. **#339** — add the SessionStart `core/hooks/verify-session-config.sh` (block-skill-direct-edit.sh shape), register the matcher in `core/settings.json.template` (alongside the **two** existing SessionStart hooks — substrate note below), and layer its `VERIFY-SESSION-CONFIG` RULE-IDs onto #310's `bypass-mode-readiness.md` edit (C1 writer 2). *Last — reads #340's surface, honors #310's master-OFF.*

**Merge-ordering discipline for the shared files:**
- `core/rules/bypass-mode-readiness.md` is written #310 → #339 (each later writer layers on the prior); Check 9 byte-identity vs the deployed mirror is green only after #339's final commit.
- `core/config/platform-config.toml.template` is written #1212 → #340 → #310 (disjoint sections; each later writer appends its own section without disturbing prior ones). Because this is one branch built in sequence, contention resolves by build-order, not by merge conflict.

## Stage Applicability Matrix

Per-issue for Stages 5–8; Stages 9–13 are **release-scoped** (run once for the whole PR).

| Issue | S5 Solutioning | S6 Engineering | S7 Dev Testing | S8 QA Testing | Note |
|---|---|---|---|---|---|
| **#1212** | **SKIP** (justified) | APPLY | APPLY | APPLY | Pure additive config on the `[bundling]` sibling pattern; zero design uncertainty, no new file class. S7/S8 still apply — the D6/D8 detectors flip INDETERMINATE→determinate = functional impact. |
| **#102** | **LIGHT** | APPLY | APPLY | APPLY | Stage 5 = scope-boundary confirm (#102 gate-wording vs #11 adapter mechanics). |
| **#340** | **APPLY** | APPLY | APPLY | APPLY | ASSUMPTION-CONFIRM at S5: mechanism (+ ADR authored). Effort-as-pointer (no shadow-SSOT) ratified at Collective Review. |
| **#310** | **APPLY** | APPLY | APPLY | APPLY | ASSUMPTION-CONFIRM at S5: durable carrier + precedence + scope. Master-OFF = SECURITY-EXCLUDED ratified at Collective Review. |
| **#339** | **APPLY** | APPLY | APPLY | APPLY | ASSUMPTION-CONFIRM at S5: SessionStart payload exposes model+effort? Register alongside BOTH existing SessionStart hooks (substrate drift). |

**Stages 9–13 (release-scoped, all APPLY):** S9 Plan Review (Deep per `novel` Release Class), S10 Dry Run, S11 Snapshot, S12 Execute (atomic version-claim binds v3.95), S13 Close (RELEASE_LOG row, CHANGELOG, RELEASE_NOTES, INDEX/DIGEST). No stage skipped except the one justified #1212 Stage-5 skip above.

## Contention Map

Shared write-surfaces (the reason the build serializes on the file axis, independent of the logical chain):

| ID | File | Writers | Class | Resolution |
|---|---|---|---|---|
| **C1** | `core/rules/bypass-mode-readiness.md` | #310, #339 | **REAL** | Sequence #310 → #339; master-enable toggle (#310) then `VERIFY-SESSION-CONFIG` RULE-IDs (#339). Check 9 byte-identity vs the deployed mirror green after the final commit. |
| **C2** | `core/hooks/` | #310, #339 | **NOMINAL** | Add-set vs edit-set: #310 edits 13 `block-*.sh` + `git-pre-commit-pii.sh` and adds `lib/master-enable.sh`; #339 adds `verify-session-config.sh`. No same-file overlap. |
| **C3** | `core/settings.json.template` (SessionStart) | #339 (+ #310 conditional) | **CONDITIONAL** | #339 registers the SessionStart matcher; #310 touches it only if its carrier is stored there. Register alongside the **two** existing SessionStart hooks (substrate note). |
| **C4** | `core/config/platform-config.toml.template` | #1212, #340, #310 | **REAL (ratified convergence)** | Was NOMINAL/convergence at Stage 4 (carrier home TBD at Stage 5); Collective Review ratified `[spoke_runtime]` (#340) + `[security_hooks]` (#310) converge here as separate sections. Three writers, disjoint sections (`[failure_mode_detectors]` + rollout-log #1212 / `[spoke_runtime]` #340 / `[security_hooks]` #310), resolved by serial build-order #1212 → #340 → #310. |

- **Cross-PR / cross-release contention:** single-branch model; no open sibling milestone declares a mover intersecting this surface at plan time. Structural-blast-radius: #310's 13–14-hook edit is the largest surface — bounded by the shared-lib master-enable + default-OFF.

## Risk Register

| ID | Class | Risk | Sev | Reversibility | Mitigation |
|---|---|---|---|---|---|
| **R2** | Blast radius | #310 edits 13–14 hooks (master-OFF gate) | **HIGH-likelihood** | MODERATE | Prefer the shared-lib `master-enable.sh` (collapses 13–14 edits → 1 lib); default-OFF bounds impact; master-OFF **SECURITY-EXCLUDED** (R9). |
| **R4** | Regression | #340 must preserve the #2535 detection composite (Check 27 + Model Provenance + Stage 8) | **MODERATE** | MODERATE | The ADR reverses #2535's "two surfaces by design" intentionally; the effort-as-pointer design keeps a single SSOT — verify the composite still fires at Stage 7/8. |
| **R5** | Design uncertainty | #339 SessionStart payload-schema uncertainty (does the payload expose model+effort?) | **MODERATE** | CHEAP | Verify the payload schema first at Stage 5 (ASSUMPTION-CONFIRM). |
| **R6** | Design convergence | #310/#340 carrier convergence | **MODERATE** | CHEAP | Resolved together at Collective Review — converge on `platform-config.toml.template` (separate sections). |
| **R8** | Scope | G2-12 shares G2-06's GitHub-Projects coupling | **LOW/informational** | CHEAP | **Resolved: FOLD into #102** (Collective Review) — #102 rewords both symmetric twins. |
| **R9** | Security | #310 master-OFF on credential/egress/PII hooks could regress public-repo safety | **MODERATE (security)** | MODERATE | **Resolved: master-OFF = SECURITY-EXCLUDED** (Collective Review) — the always-enforce floor + `block-egress` / `block-gh-path-leak` / `git-pre-commit-pii` / `block-credential-reads` are never silently disabled; #310 AC-2/AC-4 re-worded from "all 14" accordingly. Public-surface security paramount. |

**Rollback strategy:** single release branch, single squash-merge → rollback = one `git revert` of the merge (or delete-branch pre-merge). All issues CHEAP–MODERATE; **no IRREVERSIBLE actions**. Config additions are inert defaults (no data migration, no runtime state). Overall rollback: **MODERATE**.

## Release Class Declaration

**Governed classification: cross-cutting trigger (b) fires by strict letter; operator adjudicated `novel`.**

- **Cross-cutting trigger (b)** = ≥3 of the named governance surfaces edited. The plan touches **three** — `core/CLAUDE.md.template`, `release/references/how-to/hub-spoke-bridge.md`, `core/schemas/gate-criteria-spec.md` (Hub Verification Addendum CONFIRM). By strict letter, cross-cutting > novel on multi-trigger.
- **Operator adjudication (2026-07-25): `novel`.** The three touches are shallow single-line registrations, and cross-cutting's signature (≥3 `pipeline/stage-*.md` edits) does not fire — only `stage-02-triage.md` is touched. `novel` → cross-cutting stays **CHEAP to tighten** if Stage 5/7 surfaces wider blast radius (Re-Classification Protocol).
- **Effective points:** raw 22 × novel weight 1.15 = 25.3 → round-half-up = **25 pts**, at the 15–25 band ceiling (**at capacity, 25/25**).

**Posture (novel):** Stage 9 review depth = **Deep**; Stage 5 activation bias = ALL (all four non-#1212 issues carry genuine open design questions); engagement density = Standard.

## Operator Decisions (D-Gate Block)

| D-decision | Kind | Rendering | Value / Outcome |
|---|---|---|---|
| **D-Version** | rule-determined | **RECORDED DETERMINATION** (not a click-gate) | **v3.95** — rule-computed next-free minor; re-versioned at Stage 12 from the Stage-4 v3.93 determination after sibling releases claimed v3.93 and v3.94 on `main`. Binds atomically at the Stage-12 tag-claim (`claim-version.sh` ref-CAS). |
| **D-ReleaseClass** | judgment | **Operator gate — RENDERED** | **`novel`** (cross-cutting trigger (b) fired by strict letter on 3 governance surfaces; operator adjudicated `novel` — shallow registrations, no `stage-*.md` signature). Reversibility CHEAP / HIGH. |
| **D-Concurrency Posture** | judgment | **Operator ratifies — RENDERED** | **P0 (fully-serial)** + **D-C SINGLE** topology. Driver: one real contention (C1) + single-branch model. Force-push on the shared branch prohibited under any non-serial posture (moot at P0). |

## Collective Review Scope-Lock Decisions

The three design decisions ratified at Collective Review (operator decision comment on #3943, 2026-07-25), carried here as binding design constraints for Stage 6. Design is **LOCKED** — proceed to Engineering.

1. **Scope-lock — LOCK & proceed to Engineering.** Integrated design ratified: `[spoke_runtime]` (#340) + `[security_hooks]` (#310) converge on `core/config/platform-config.toml.template` (separate sections); **effort-as-pointer** (no shadow-SSOT); `build-hook-registry.py` glob generalized to `[!_]*.md`; warn-mode shakedowns for the new gates; **ADR authorized** (reverses the #2535 "two surfaces by design" decision — authored during #340 Engineering with a next-free number).
2. **D-R9 master-OFF scope = SECURITY-EXCLUDED.** Master-OFF governs warn-mode workflow hooks only; the always-enforce floor + public-surface security hooks (`block-egress` / `block-gh-path-leak` / `git-pre-commit-pii` / `block-credential-reads`) are never silently disabled. #310's AC-2 / AC-4 re-worded from "all 14" accordingly. Public-surface security paramount.
3. **D-G2-12 = FOLD into #102.** #102 rewords G2-06 **and** G2-12 (symmetric twins) to the config-resolved backlog surface.

## Cross-Issue Acceptance Criteria

Release-scoped cohesion constraints spanning ≥2 issues (graded at Stage 9 QC3.5 on the merged PR; distinct from per-issue ACs):

- [ ] **CIAC-1 (#339 × #340):** after both land, editing the spoke-model value in #340's canonical surface makes #339's SessionStart hook flag a model mismatch at launch (proves #339 reads the same surface #340 establishes). *Graded at Stage 9 QC3.5.*
- [ ] **CIAC-2 (#310 × #339):** one durable master-enable + `.mode` + `CLAUDE_HOOK_BYPASS` governs both #310's block-* hooks and #339's hook (master-OFF → exit 0 for both **workflow** hooks; the SECURITY-EXCLUDED floor stays enforced); the `bypass-mode-readiness.md` mirror is byte-identical (Check 9). *Graded at Stage 9 QC3.5.*

## File Change Matrix

Structurally machine-readable — one path per line; first token after the intent marker is the path; owning issue(s) in trailing parens; `#` starts a comment.

```
# ADD (create)
core/hooks/lib/master-enable.sh (#310)                          # shared-lib — collapses 13-14 hook edits to one lib
core/hooks/verify-session-config.sh (#339)                      # new SessionStart hook (block-skill-direct-edit.sh shape)
core/ADRs/ADR-NNN-spoke-model-effort.md (#340)                  # ADR — reverses #2535 "two surfaces by design"; next-free number at build
# EDIT
core/config/platform-config.toml.template (#1212,#340,#310)     # #1212 [failure_mode_detectors]+rollout-log · #340 [spoke_runtime] · #310 [security_hooks] — disjoint sections
core/schemas/gate-criteria-spec.md (#102)                       # reword G2-06 AND G2-12 → config-resolved backlog surface
release/references/pipeline/stage-02-triage.md (#102)           # decision-date resolves backlog surface from config
core/config/allowlists/agents-model-overrides.txt (#340)       # canonical surface anchor (+ effort field)
core/CLAUDE.md.template (#340)                                  # register canonical file in Governance File Map
release/references/how-to/hub-spoke-bridge.md (#340)            # chip-prompt reads canonical surface
docs/scripts/setup-workspace.sh (#310)                          # hook-activation prompt, default OFF
install.sh (#310)                                               # opt-in wiring + toggle survives update
update.sh (#310)                                                # opt-in wiring + toggle survives update
core/hooks/block-*.sh (#310)                                    # 13 scripts — master-OFF gate (workflow hooks)
core/hooks/git-pre-commit-pii.sh (#310)                         # 14th enforcement hook — SECURITY-EXCLUDED from silent master-OFF
core/rules/bypass-mode-readiness.md (#310,#339)                 # C1 (sequenced) — master-enable toggle (#310) + VERIFY-SESSION-CONFIG RULE-IDs (#339)
core/settings.json.template (#339)                              # register SessionStart matcher (alongside the 2 existing hooks)
# BUILD / CLOSE-OUT ARTIFACTS (release-scoped — Stage 6/12/13)
release/releases/plans/v3.95_RELEASE_PLAN.md (plan)             # this file — Engineering Commit 0
release/releases/RELEASE_LOG.md (Stage 12/13)
CHANGELOG.md (Stage 13)
release/releases/notes/v3.95_RELEASE_NOTES.md (Stage 13)
# DELETE (none)
# READ-ONLY INPUTS (no write)
core/skills/pmo-qa-auditor/references/failure-mode-detectors.md (#1212 read — names the D6/D8 config keys)
release/skills/release-executor/references/progressive-rollout.md (#1212 read — names the rollout-log surface)
```

> **Deployed-mirror note.** `core/rules/bypass-mode-readiness.md` is the tracked source; its deployed mirror is a build artifact, not a tracked write (Check 13 byte-identity). Only the source path is in the write-set.

## Recommendations

1. **Release Class `novel`** ratified at the D-ReleaseClass gate (cross-cutting near-miss adjudicated `novel`). Deep Stage 9 over the config-surface + hook blast-radius changes.
2. **D-Concurrency Posture P0 (fully-serial) + D-C SINGLE** ratified. Build as **one PR**, delivery slices in dep order #1212 → #102 → #340 → #310 → #339, one merge gate.
3. **Honor the ratified integrated config-surface design** — #1212 / #340 / #310 write `platform-config.toml.template` as disjoint sections; each writer appends its own section without disturbing prior ones. Build-order #1212 → #340 → #310 resolves the C4 contention without merge conflict.
4. **Master-OFF is SECURITY-EXCLUDED** — #310 must never gate the always-enforce floor / public-surface security hooks behind master-OFF. AC-2 / AC-4 re-worded from "all 14".
5. **Run Stages 5, 7, 8 for real** — do not collapse into CI + Stage 9. #1212's Stage 5 is a justified skip (pure additive config); Stage 7/8 verify the D6/D8 INDETERMINATE→determinate flip via fixture.
6. **#340 authors the ADR in-slice** — reverses #2535 "two surfaces by design"; allocate a next-free ADR number at build (global-monotonic across `core/ADRs/` + `release/ADRs/`).

**Discoveries outside scope** (surface to hub; do not action here):
- **(a) SessionStart substrate drift** — #339's body assumes ONE existing SessionStart hook (`notify-version-skew.sh`); the live `core/settings.json.template` SessionStart has **two** (`notify-version-skew.sh` + `prime-autonomy-ceiling-cache.sh`). #339 must register the new matcher alongside BOTH. Non-blocking (Stage-5 substrate note).
- **(b) platform-config schema + catalog drift (from #1212 — see § Deviation Log (b)).** The new sections #1212 adds to the template are NOT mirrored in `core/schemas/platform-config-schema.md` or `docs/platform-config-reference.md`; #1212's completion conditions are template-scoped only. Candidate for an in-release follow-up slice or a fast-follow intake.

## Deviation Log

Departures from the Stage-4 plan comment (#3943), captured at Engineering Commit 0 per the write-first-speak-second discipline. Each is a plan-transcription reconciliation, not a build defect.

**(a) Collective-Review scope-lock (amends the Stage-4 "convergence TBD at Stage 5" items).** Stage 4 recorded #310/#340 carrier convergence and R9 master-OFF scope as Stage-5 questions, and G2-12 as an R8 "fold or defer to #11" scope question. Collective Review resolved all three: convergence → `platform-config.toml.template` separate sections; R9 → SECURITY-EXCLUDED; G2-12 → FOLD into #102. This plan reflects the resolved (as-built) design in § Contention Map (C4 REAL), § Risk Register (R6/R8/R9 resolved), and § Collective Review Scope-Lock Decisions.

**(b) #1212 implementation scope — template-only (schema/catalog flagged, not edited).** #1212's completion conditions and the Stage-4 File Change Matrix scope the change to `core/config/platform-config.toml.template` (+ the already-correct pmo-qa-auditor / release-executor references, which name the config keys and need no edit). Adding fields is a Track-A change that the platform-config schema's own §5 expects to also update `core/schemas/platform-config-schema.md` + `docs/platform-config-reference.md`; those are OUTSIDE #1212's ratified completion conditions and the change matrix, and neither `deploy.sh` Check 33 (closed field allowlist) nor Check 34 (platform-config not in the TEMPLATE_SCHEMA_MAP manifest) fails on their absence. Flagged as § Recommendations discovery (b); not silently expanded.

**(c) Version — recorded v3.93 at Stage 4; re-versioned to v3.95 at Stage 12.** The Stage-4 plan recorded target v3.93 (rule-determined next-free). At Commit-0 the branch base was `origin/main` `627b4776` with no interim reversion; at the Stage-12 conflict-resolve, sibling releases having claimed v3.93 and v3.94 on `main`, this release re-versioned to v3.95 (next-free). The binding claim remains the Stage-12 atomic ref-CAS.

## Change Description

*Authored at Stage 6 Phase C1 (PR assembly), after all five slices land, per `release/governance/RELEASE_PROTOCOL.md` § Change Description Protocol and `release/references/pipeline/stage-06-engineering.md` C1. Committed on the release branch before the PR is transitioned draft→ready at the Stage 9 gate, so the operator reviews it in the PR diff. Not authored at Engineering Commit 0 (the build is not yet complete) — this heading is the reserved home for the Phase-C author.*
