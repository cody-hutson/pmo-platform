<!-- repo-integrity: allow-issue-ref -->
<!-- reference-durability: allow-link -->
# v3.62 Release Plan — 31-immutable-adr-system

> Durable release-plan record for milestone `31-immutable-adr-system` (Release Class **`novel`** — Deep Stage 9). Transcribes the operator-approved Stage-4 release plan + the Collective-Review scope-lock (all 8 Stage-5 D-decisions ratified as-is). Version **v3.62** provisional (recorded determination; binds atomically at Stage 12 per the repo versioning lineage). Three cards — #431 (canonical ADR authoring guide + reference repoints), #15 (`adr-helper` skill), #541 (ADR-017 + ADR-019 index rows) — ship as **one branch / one PR / one merge**, built in dependency order **#431 → #15 → #541**.

---

## Version determination (recorded — not gated)

- **Provisional version: v3.62.** Hub-verified free at plan time; re-verified SHIPPED-free on all three surfaces at Engineering Commit 0 (this file) — no `v3.62` git tag, `gh releases/latest` = **v3.61** (anchor), and no `release/releases/plans/v3.62_RELEASE_PLAN.md` on `origin/main` at branch-create time. Next-free = anchor + 1 = v3.62. PROCEED.
- **Version handling note:** per the repo versioning lineage the provisional binds **atomically at Stage 12**, not at Engineering. If a concurrent release consumes v3.62 before this release reaches execute, the Stage-12 atomic claim re-verifies next-free and self-resolves forward — the provisional number recorded here is not load-bearing at merge time.
- **Branch topology: SINGLE** — one branch `release/v3.62`, one PR, one merge for all three issues. Stage 6 Engineering is write-serialized on the single branch (P0-serial default posture); the parallel surface is Stages 5/7/8 only.

---

## Tier-A activated design artifacts

Per the design-artifact standard, the Stage 5 spoke declares each design artifact this release produces so Stage 13 Close can scope its refresh-gate (G-CL6) detection. #431 produces exactly one Tier-A artifact — the canonical ADR authoring guide (a net-new K1 reference doc). #15's SKILL.md and #541's index-row edits are not Tier-A design artifacts.

| Artifact path | Flow class | Activation trigger | Tier-A (new) / Tier-B (refresh) |
|---|---|---|---|
| `core/standards/adr-authoring-guide.md` | Reference doc (K1 standard) | New K1 reference doc (the canonical ADR authoring guide — when-to-write rubric + copy-paste template + supersede-not-edit policy) per design-artifact-standard §7 Tier-A activation | **Tier-A (new)** |

The guide references `core/schemas/adr-schema.md` for the field/section data contract (one source — not re-inlined) and cites both ADR READMEs (Status-enum + Reversibility + Repo-integrity authoring discipline) rather than duplicating them. Stage 13 G-CL6 scopes its refresh-gate detection to this one declared artifact path.

---

## Stage 4 Release Planning — 31-immutable-adr-system (transcribed)


### Summary (30 seconds)

Release **v3.62** (provisional) bundles 3 open issues to close the ADR-governance gap: **#431** (size:L) authors the canonical ADR guide — when-to-write rubric + copy-paste template + supersession/immutability policy — and repoints the dangling `repo-hygiene-standards`/`adr-policy-cluster` references at it; **#15** (size:S) builds a NEW ADR-creation helper skill (`adr-helper`) via `pmo-skill-refiner` that consumes #431's template; **#541** (size:S) backfills the ADR-017 + ADR-019 index rows in `core/ADRs/README.md`.

Proposed Release Class: **`novel`** (VALIDATED — new skill build + new reference doc, both fire the `novel` trigger). Raw size **12 pts** → `effective_pts` **14** (novel weight 1.15) → **ACCEPTABLE band** (below the 15–25 target; ships because the capability boundary is clear). Implementation sequence: **#431 → #15 → #541** (dep-ordered).

**Two validation findings vs the injected Mode R read** (neither blocks): (1) the predicted `core/ADRs/README.md` contention between #431 and #541 is **DOWNGRADED to LOW / conditional** — the core README has **no** live "when-to-write → decision-discipline" pointer for #431 to repoint (only `release/ADRs/README.md` does), so #431's only touch on `core/ADRs/README.md` is an *optional* link-add to the new guide, in a different file region (prose "Authoring new ADRs" section) from #541's edits (cross-numbering table + detail sections); (2) `#431`'s AC #5 names both READMEs but the load-bearing repoint lives in `release/ADRs/README.md`, not `core/ADRs/README.md`.

### Dependency Graph

Directional edges (`A → B` = A must land before B):

```
#431 (canonical ADR guide + template + supersession policy)
   │
   └──soft-compose──▶ #15 (adr-helper skill CONSUMES #431's canonical template)

#541 (ADR-017 + ADR-019 index rows)  — INDEPENDENT (no edges in/out)
```

- **#431 → #15 edge: VALIDATED as a soft-compose (sequence, not hard-block).** #15's AC requires the helper to scaffold `ADR-NNN.md` "with the canonical sections present (Context / Decision / Alternatives / Consequences / Status)." That canonical structure is exactly what #431 codifies as the copy-paste template. #15's body states "composes with `decision-discipline.md`… No blocking deps at intake," and #431's body lists #15 as "Relates to (composes-with): #15 (helper skill consumes the template)." The edge is real but **soft**: #15 *could* be built against the existing hand-authored ADR shape (ADR-005 exemplar) even if #431 slipped — but building #15 first would risk the skill scaffolding a structure that #431 then revises. **Sequence #431 first** so the skill encodes the canonical (not the ad-hoc) structure. Confidence HIGH.
- **#541: no dependency edges.** Pure index-hygiene; both target ADR files (`ADR-017-distribution-architecture.md`, `ADR-019-specialists-compose-not-absorb.md`) already exist on `main` (verified). Independently shippable in any position.
- **No cross-milestone leaks** (VALIDATED): #431's external refs #426 (CLOSED) and #144 (CLOSED) both resolved — confirmed via `gh issue view`. No open external dependency edges leave this bundle. External-dep count = 0, well within the ≤2 target (bundle-composition-doctrine §3 Step 7).

### Implementation Sequence

Dependency-ordered execution order (bundle-composition-doctrine §9 layer sequence: foundation → infra → skill-core → eval):

1. **#431** — canonical ADR guide + reference repoint. `layer:foundation`. Authors the template/rubric/policy that #15 consumes; ships the K1 reference doc first. **Foundation layer — lands first.**
2. **#15** — `adr-helper` skill build via `pmo-skill-refiner`. `layer` (skill-suite). Consumes #431's template. **Skill-core layer — lands after its foundation dependency.**
3. **#541** — ADR-017 + ADR-019 index rows in `core/ADRs/README.md`. `layer:foundation` (documentation/structure). **Independent** — sequenced LAST to let #431's (optional) `core/ADRs/README.md` guide-link edit land first, so #541 rebases onto a known README state and there is zero same-file merge risk. (If #431 ends up NOT touching `core/ADRs/README.md` — see Contention Map — #541 may land in any position; last is the safe default.)

Single-branch topology (milestone = one PR / one merge): build in the order above on one release branch, one merge gate. Within-layer tie-break not exercised (each layer has ≤1 blocking member on the critical path).

**Critical path:** #431 → #15 (the only chain with a dep edge). #541 is off the critical path (parallelizable in principle, but see Contention Map — kept serial-last for same-file safety since parallelism posture defaults to P0 fully-serial when undeclared).

### Stage Applicability Matrix

Default = all of Stages 5–13 apply. Skip Stage 5 only if trivial; skip Stages 7–8 only if no functional impact. Justification given per skip.

| Issue | S5 Solutioning | S6 Eng | S7 DevTest | S8 QA | S9 Review | S10 Prep | S11 Deploy | S12 Execute | S13 Close |
|---|---|---|---|---|---|---|---|---|---|
| **#431** (doc/gov: guide + repoint) | **APPLY** — real design decisions: final guide path (candidate `core/standards/`), rubric threshold set, template structure, supersession-policy wording. #431 body explicitly defers "Mechanism (final guide path, rubric thresholds, template structure) to Stage 5." Non-trivial. | APPLY | **APPLY (doc-verify)** — grep-based ACs (template fence present, ≥1 trigger + ≥1 non-trigger, 0 `repo-hygiene-standards`/`adr-policy-cluster` live refs, both README pointers gone, scope banner present). No code, but functional verification via grep is the test. | **APPLY** — governed-doc QA: reference-durability + repo-integrity gates on every changed markdown file; dead-file-ref gate on new guide's links. | APPLY (Deep — novel class) | APPLY | APPLY | APPLY | APPLY |
| **#15** (NEW skill build) | **APPLY** — genuine design surface: auto-detection mechanism (LLM-graded vs explicit-trigger), scaffold-only vs drafts-content, dynamic ADR-home read. #15 body defers all three to Stage 5. Built via `pmo-skill-refiner` Interview→Create. Non-trivial. | APPLY | **APPLY** — functional skill test: trigger via test prompt → verify `ADR-NNN.md` written to `core/ADRs/` with canonical sections; verify auto-allocated number = max+1 (**current max = ADR-070 → expect ADR-071**); verify `deploy.sh --check` structure + roster pass. Direct functional impact. | **APPLY** — skill QA: `pmo-qa-auditor` against Principal Standard; ≥3 domain-specific failure modes; deploy `--check` roster/structure. | APPLY (Deep) | APPLY | APPLY | APPLY | APPLY |
| **#541** (index-hygiene chore) | **SKIP — trivial.** Zero design uncertainty: mechanical single-file insert of two table rows + two `### ADR-NNN` detail sections, structure dictated by the existing ADR rows (Status / Decision / File pattern). #541 body: "Pure index-hygiene chore — no behavior change." No D-class decision, no new file, no new pattern. Meets the Stage-5 SKIP-where-trivial bias for the letter-only case. | APPLY | **SKIP — no functional impact.** README index rows are documentation; there is no runtime behavior to dev-test. AC is grep-verified at QA (`grep ADR-017`/`ADR-019` returns row + heading + `File:` link). | **APPLY** — governed-doc QA only: repo-integrity dead-file-ref gate confirms the two `File:` links resolve; grep-AC verification. No functional QA. | APPLY (Light-ish; rolls into the bundle's Deep review) | APPLY | APPLY | APPLY | APPLY |

Note: S9–S13 apply to the release as a whole (single PR / single merge / one close-out), not per-issue — the per-issue columns for S9+ reflect the bundle's shared gates.

### Contention Map

Shared-file analysis across the 3 issues (from issue-body Affected-Files / Completion-Condition sections + live tree inspection):

| File | #431 | #15 | #541 | Contention verdict |
|---|---|---|---|---|
| `core/ADRs/README.md` | **MAYBE (optional link-add)** — prose "Authoring new ADRs" section (lines ~222–283) | — | **YES (heavy)** — cross-numbering table (lines ~19–30) + new `### ADR-017` / `### ADR-019` detail sections | **LOW / conditional** — see finding below |
| `core/standards/initiative-roadmap-framework.md` | **YES** — repoint 9 `repo-hygiene-standards`/`adr-policy-cluster` refs (lines 18,19,46,48,173,190,333,454,455) | — | — | None (sole editor) |
| `release/ADRs/README.md` | **YES** — repoint when-to-write pointer (lines 77, 94) at new guide | — | — | None (sole editor) |
| `core/disciplines/decision-discipline.md` | **YES** — add ADR-scope-disclaimer banner | — | — | None (sole editor) |
| `release/references/pipeline/stage-05-solutioning.md` | **YES** — repoint ADR row off `decision-discipline.md § 3` | — | — | None (sole editor) |
| `.github/ISSUE_TEMPLATE/adr.yml` | **YES** — link the new guide from description | — | — | None (sole editor) |
| NEW ADR guide (candidate `core/standards/<adr-guide>.md`) | **YES** — creates | (reads at build) | — | None (creator) |
| `<skill-home>/adr-helper/SKILL.md` (candidate `core/skills/adr-helper/`) | — | **YES** — creates | — | None (creator) |
| `core/deploy/deploy.sh` | — | **YES** — register in per-module skill array | — | None (sole editor) |
| `core/governance/OPERATIONS.md` | — | **YES** — add to skill catalog | — | None (sole editor) |

**PRIMARY CONTENTION FINDING — `core/ADRs/README.md` (#431 ∩ #541): CONFIRMED-BUT-DOWNGRADED.**
The hub's Mode R read flagged this as "likely file contention to CONFIRM: #431 repoints when-to-write pointers, #541 adds ADR-017/019 rows." Validation against live state **downgrades** it:
- The live `core/ADRs/README.md` has **NO** "when to write an ADR → `decision-discipline.md`" pointer (grep returned zero matches). That pointer lives only in `release/ADRs/README.md` (lines 77, 94). So #431 does **not** need to repoint anything *in the core README* — the premise behind the predicted contention is partially stale.
- #431's residual touch on `core/ADRs/README.md` is at most an **optional** link-add from the "Authoring new ADRs" prose section (lines ~222–283) to the new guide. That is a different file region from #541's edits (the cross-numbering table at lines ~19–30 and the appended `### ADR-017`/`### ADR-019` detail sections).
- **Net:** if both touch the file, it is a low-risk insert-in-different-regions overlap (unlikely to textually conflict), not a same-line collision. **Mitigation:** sequence #541 **after** #431 (already the plan) so #541 rebases onto the post-#431 README; one branch, one merge means no cross-PR merge at all. Confidence HIGH. Reversibility CHEAP.

No other cross-issue file contention exists — #15's files (SKILL.md, deploy.sh, OPERATIONS.md) are disjoint from #431's and #541's edit sets.

### File Change Matrix

Per issue, one path per line inside the fenced block. `A`=add, `E`=edit, `D`=delete. Skill-home + guide-home paths are **candidates** (final path is a Stage-5 decision per each issue body — do NOT freeze here).

**#431 — canonical ADR guide + reference repoint** (`domain: governance`; pipeline-internal, external-sourcing-exempt):
```
A core/standards/adr-authoring-guide.md            # CANDIDATE path (final = Stage 5 D-decision; #431 body: candidate core/standards/)
E core/standards/initiative-roadmap-framework.md   # repoint 9 repo-hygiene-standards / adr-policy-cluster refs → new guide
E release/ADRs/README.md                           # repoint when-to-write pointer (lines 77, 94) → new guide
E core/disciplines/decision-discipline.md          # add ADR-scope-disclaimer banner (grep "ADR" returns ≥1 banner match)
E release/references/pipeline/stage-05-solutioning.md  # repoint ADR row off decision-discipline.md § 3 → new rubric
E .github/ISSUE_TEMPLATE/adr.yml                    # link the new guide from description
E core/ADRs/README.md                              # OPTIONAL guide-link from "Authoring new ADRs" section (may be omitted; see Contention Map)
```

**#15 — adr-helper skill build** (`domain: software`; new-capability skill via pmo-skill-refiner):
```
A core/skills/adr-helper/SKILL.md                  # CANDIDATE home (final = core/skills/ vs governance-tooling module — Stage 5 / architecture-binding decision)
E core/deploy/deploy.sh                            # register adr-helper in the correct per-module skill array
E core/governance/OPERATIONS.md                    # add adr-helper to the skill catalog
```
(Skill may also add `core/skills/adr-helper/references/*.md` support files — determined at build by pmo-skill-refiner; not pre-frozen.)

**#541 — ADR-017 + ADR-019 index rows** (`domain: governance`; index-hygiene):
```
E core/ADRs/README.md                              # add cross-numbering rows + ### ADR-017 and ### ADR-019 detail sections
```

### Risk Register

| # | Risk | Class | Severity | Mitigation | Reversibility |
|---|---|---|---|---|---|
| R1 | **#431 → #15 sequence inversion** — if #15 is built before #431's template is final, the skill scaffolds an ad-hoc ADR structure that #431 then revises, forcing a skill rebuild. | Dependency | MED | Enforce sequence #431 → #15 on the single release branch (build in dep order). Stage 6 must land #431's template before #15's skill-build commit. | CHEAP (re-edit skill template) |
| R2 | **`core/ADRs/README.md` same-file edits (#431 optional link-add ∩ #541 rows).** | Contention | LOW | Sequence #541 after #431 (plan already does this); single-branch/single-merge eliminates any cross-PR merge. Edits are in different file regions. If #431 omits the core-README link-add entirely, risk → nil. | CHEAP |
| R3 | **#15 candidate skill home unresolved** (`core/skills/` vs a governance-tooling module) — picking wrong home churns deploy.sh array + OPERATIONS.md catalog. | Scope | MED | #15 AC requires reading the ADR home **dynamically** (no hardcode) + architecture-binding step at build (roadmaps/skill-matrix + ADR-019/ADR-004). Resolve home at Stage 5 before the deploy-array edit. Also: #15 body flags POSSIBLE-WRONG-INITIATIVE (skill-suite vs decision-discipline epic) — non-blocking, but confirm home at Stage 5. | MODERATE (re-home skill = cross-file move) |
| R4 | **#15 auto-detection false-positive** — helper offers ADR scaffolding unprompted and misfires, annoying the operator. | Rollback-complexity | LOW | Per #15 body: the offer is non-blocking; operator declines without friction. Stage 5 decides LLM-graded vs explicit-trigger-only (defer to explicit-trigger if uncertain — lower false-positive surface). | CHEAP (skill trigger tuning) |
| R5 | **#431 rubric-threshold subjectivity** — "when to write an ADR" thresholds are judgment calls; a too-loose or too-strict rubric mis-guides authors. | Scope | LOW | Ground the rubric in the shipped ADR corpus (70 ADRs, ADR-005 exemplar) + the Nygard status enum already live. Deep Stage 9 review (novel class) validates the rubric against real ADRs. | CHEAP (edit guide prose) |
| R6 | **Governed-doc gate failures** — #431 edits 6+ markdown files + a new guide; each must clear repo-integrity (issue-ref validity, dead-file-ref, depersonalization) + reference-durability gates. A dangling link or bare `#N` outside a reference block red-fails CI. | Dependency (CI) | MED | Author every changed file to the gates up front (bare `#N`, file-level override markers where needed, self-describing prose) per `core/ADRs/README.md` § Repo-integrity authoring discipline. New guide's internal links must resolve (dead-file-ref gate). Never merge red CI. | CHEAP (fix + re-push) |
| R7 | **Baseline staleness** — #541's ADR-017/019 files verified present at HEAD 99a9476; a concurrent ADR renumber (the ADR sequence is active — max is ADR-070) could shift ADR-017/019 filenames before merge. | Contention | LOW | Baseline pinned at HEAD 99a9476 (2026-07-03). Re-check `ls core/ADRs/ADR-017*.md core/ADRs/ADR-019*.md` at Stage 6 Commit 0 and at Stage 12 Phase A.5 before the row text is frozen. | CHEAP |

### Release Class (proposed)

**Proposed class: `novel`** — VALIDATED against the hub's provisional read (`novel`) with evidence.

- **Trigger evidence (release-class-taxonomy.md § Class Enum, `novel` row — "any one fires"):**
  - **(a) ≥1 issue introduces a new reference doc, schema, or skill** — FIRES **twice**: #431 introduces a NEW K1 reference doc (the canonical ADR guide), AND #15 introduces a NEW skill (`adr-helper`). Either alone fires `novel`.
  - **(b) ≥1 D-class decision in release plan** — FIRES: #431 defers guide-path/rubric-threshold/template-structure to Stage 5 (D-class); #15 defers auto-detection-mechanism/scaffold-vs-draft/skill-home to Stage 5 (D-class). The D-Concurrency Posture D-Gate also fires at Stage 4.
- **Not `routine`:** #431 + #15 both add new files (violates routine trigger (c) "zero new files") and introduce new D-class decisions (violates (d)). Aggregate scope disqualifies routine even though 2 of 3 issues are size:S.
- **Not `cross-cutting`:** does NOT reach the ≥3 threshold — the File Change Matrix touches **0** `pipeline/stage-*.md` files as *primary targets* (#431 edits exactly ONE, `stage-05-solutioning.md`, well under the ≥3 trigger (a)); it touches only 2 of the 7 named governance surfaces (`OPERATIONS.md` via #15, plus `decision-discipline.md` which is a discipline not on the cross-cutting list) — under the ≥3 trigger (b); in-bundle compositional edges = 1 (#431→#15) — under the ≥3 trigger (c). Challenge-checked and REJECTED cross-cutting.
- **Not `hotfix`:** no P1/P2 deployed-release defect; scope is additive-governance + new-capability, not corrective.
- **Multi-trigger resolution:** only `novel` triggers fire (routine disqualified, cross-cutting/hotfix not reached) → `novel` is unambiguous. No `cross-cutting > novel` escalation needed.

**Differentiation posture (per-class mapping table):**
- Engagement density (REQUIRED): **Standard** — per-D-decision Operator Decision Gate + per-Stage-5 Decision Briefing on completion.
- Stage 9 Plan Review depth (REQUIRED): **Deep** — Collective Review N-way consistency + cross-D upstream-compat scan + design-spec conformance; PR diff review includes blast-radius assessment (justified: new skill + new reference doc + 6-file repoint).
- Stage 5 Activation bias (OPTIONAL): **ALL** — bias toward activating Stage 5 (both #431 and #15 carry genuine deferred design decisions; #541 still SKIPs per its trivial-letter-only status).
- Stage 13 Outcome-window (OPTIONAL): **30-day** — standard window (not a hotfix).

### Size Roll-up

Point scale (bundle-composition-doctrine §3 Step 5): XS=1 / S=2 / M=4 / L=8 / XL=16.

| Issue | size label | pts |
|---|---|---|
| #431 | size:L | 8 |
| #15 | size:S | 2 |
| #541 | size:S | 2 |
| **raw `sum(member_pts)`** | | **12** |

**Risk-weighting (§3 Step 5 Risk-Weighting; ADR-027):** `effective_pts = round_half_up(sum(member_pts) × class_weight)`.
- `class_weight` for `novel` = **1.15** (per-class seed in `[bundling].release_class_capacity_weights`; ADR-027: routine 1.0 / **novel 1.15** / cross-cutting 1.3 / hotfix 0.9; MEDIUM-confidence `[CALIBRATE-AFTER-3]`).
- `effective_pts = round_half_up(12 × 1.15) = round_half_up(13.8) = ` **14**.

**Band disposition vs `release_size_target_pts = 15-25`:**
- `effective_pts` = **14** → falls in the **10–15 "Acceptable band"** (below the 15–25 target band, above the <10 merge-floor).
- **Disposition: SHIP AS-IS.** Per §3 Step 5 disposition table, the 10–15 band "ships if capability boundary is clear; consider merging if drift-prone." The capability boundary here is crisp (one coherent capability: "record an architecture decision without friction" — the guide + the helper skill + the index hygiene that supports it), so it ships as one release. Not oversized (well under the 25-pt ceiling even weighted), not undersized (above the 10-pt merge-floor). No split, no tight-merge, no additional bundling needed.

### Recommendations

1. **Merge/split: NEITHER — ship the 3-issue bundle as-is.** Size (14 effective pts, Acceptable band) and capability coherence both support a single release. No split (not oversized); no merge-in of adjacent issues (boundary is clear); no tight-merge (bundle-composition §4 applies only to >15-ticket parents). **Single PR / single merge** on one release branch per the milestone = one-PR rule.
2. **Sequence is load-bearing: #431 → #15 → #541.** #431 first (foundation: ships the template #15 consumes); #15 second (consumes the template); #541 last (independent, sequenced last for same-file safety on `core/ADRs/README.md`). Do NOT build #15 before #431's template lands.
3. **Resolve two Stage-5 D-decisions before Engineering freezes the matrix:** (a) **#431 final guide path** — candidate `core/standards/adr-authoring-guide.md`; confirm `core/standards/` is the home (no existing ADR-guide file there today — it is net-new). (b) **#15 skill home** — candidate `core/skills/adr-helper/`; run the architecture-binding step (skill-matrix + ADR-019/ADR-004) and resolve `core/skills/` vs a governance-tooling module BEFORE the deploy.sh-array edit. #15 also flags a POSSIBLE-WRONG-INITIATIVE (skill-suite epic vs decision-discipline epic) — non-blocking, but the operator may want to confirm the home epic at the Stage-5 gate.
4. **Validation finding to carry forward (not a blocker):** the predicted `core/ADRs/README.md` contention between #431 and #541 is **LOW / conditional**, not the medium collision the Mode R read implied — the core README has no live "when-to-write → decision-discipline" pointer for #431 to repoint (that pointer is in `release/ADRs/README.md`). #431's core-README touch is at most an optional guide-link. Single-branch sequencing (#541 after #431) fully mitigates it.
5. **Out-of-scope discovery (note only, do not action here):** `core/ADRs/README.md`'s cross-numbering table is itself incomplete beyond ADR-017/019 — it also omits rows for ADR-010/012/013/014/015/016/020/022/023 (each has a `### ` detail section but no table row). #541's body explicitly scopes to ADR-017 + ADR-019 only and defers the full backfill as "a separate hygiene task." **Recommend leaving that broader table backfill out of this release** (scope discipline) and, if the operator wants it, filing a separate `improvement.yml` for the full cross-numbering-table reconciliation. Not filed here (planning-only spoke; no recursion).
6. **Governed-doc gate readiness:** #431 touches 6+ markdown files + a new guide; author all to the repo-integrity + reference-durability gates up front (bare `#N`, override markers, resolving links) per `core/ADRs/README.md § Repo-integrity authoring discipline` so Stage 8 QA does not red-fail on CI. #15's new SKILL.md is equally durable-corpus and equally scanned.
7. **#15 Stage-7 test anchor:** current max ADR = **ADR-070**; the helper's "auto-allocated number = max+1" AC should scaffold **ADR-071** at test time. Re-verify the live max at Stage 7 (the ADR sequence is active — it may advance before this release reaches DevTest).

