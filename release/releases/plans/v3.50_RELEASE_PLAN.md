# v3.50 Release Plan — 68-stage-gate-criteria-completeness

> Release-scoped plan for Milestone **68-stage-gate-criteria-completeness**. Authored at Stage 6 (Engineering Commit 0) from the Stage-4 spoke output (sub-task #2859) + the hub R1 D-Version correction. D-C **SINGLE** (one release branch → one PR → one merge). Reversibility **MODERATE**. **Shipped as v3.50** — the provisional **v3.49** was claimed by a concurrent release (`08-INTAKE-governed-file-movement`) at Stage 12, so this milestone re-claimed forward to the next-free minor **v3.50** at the atomic tag claim (forward-only per bundle-composition-doctrine §5.3; the durable declaration is the bump-class, minor). The § D-Version section below records the build-time provisional determination unchanged for audit.

## Release Class declaration

- **Release Class:** cross-cutting (operator-rendered; in milestone description). class_weight **1.3**. Raw 25 pts → effective_pts = round_half_up(25 × 1.3) = **33** (above the 15–25 band); operator-accepted size-target override recorded in the milestone description (capability-coherence; doctrine §3 Step 5 Shape-1 precedent). Scope **LOCKED at 7**.
- **Domain:** governance (governance/schema/doc-authoring release). `source: N/A — pipeline-internal release` (sourcing-exempt, not classification-exempt).
- **Domain Practice Provenance:** `domain: governance | date: 2026-07-02 | source: N/A — pipeline-internal release`

## Delivery Strategy

- **Branch topology (D-C):** SINGLE — one release branch `release/68-stage-gate-criteria-completeness` (slug-based; version provisional churning). All 7 issues committed as delivery slices in Implementation-Sequence order, **one PR → one merge**. Posture **P0 fully-serial** (undeclared → default P0). Force-push prohibited on the shared branch under multi-chip activity.
- **Merge approach:** single squash/merge PR into `main` at Stage 12; the merge SHA is the release SHA. Stage 9/12 gates own the merge — Engineering does NOT merge.
- **Rollback strategy:** git-native — `git revert -m 1 <merge-sha>` reverts the whole release atomically (single PR/single merge). No DB/external state; Stages 10/11 PLATFORM-SATISFIED (git-native).

## D-Version (RECORDED DETERMINATION — not a gate)

- **anchor()** = highest claimed minor in the v3.x mainline = **v3.47** (`gh release list`: v3.47 marked *Latest*, published 2026-07-02).
- **Bump-class** = minor (capability release) — the durable declaration.
- **claimed_set** (per bundle-composition-doctrine §5.1) = released tags through v3.47 ∪ in-flight `release/v3.4x-*` branches. In-flight branches hold **v3.46** (open PR #2861), **v3.47** (released; plan on main), **v3.48** (open PR #2918). Lowest free minor > anchor not in the claimed set = **v3.49**.
- **Provisional = v3.49.** Re-claims atomically at Stage 12 (forward-only) if a sibling takes v3.49 first. Detection rungs: Engineering Commit 0 re-verify (done — this file) → Stage 9 G-PR9 baseline-currency → Stage 12 Phase A.5 pre-merge freeness → atomic tag claim.
- **Lineage note:** the Stage-4 spoke computed provisional v3.46; hub R1 corrected to v3.48 (in-flight v3.46/v3.47 held their minors). At Engineering Commit 0, v3.48 is ALSO taken (open PR #2918) and v3.47 is now released, so the recomputed next-free is **v3.49**. `[SOURCE: git fetch --tags origin; gh release list; gh pr list; git branch -a]`

## Implementation Sequence

Layer-ordered, reconciled with the two shared-table ordering constraints. One Engineering commit per issue on ONE release branch (write-serialized Stage 6).

| # | Issue | Size | Layer | Why here |
|---|---|---|---|---|
| 1 | **#28** — G4 + G6 discrete gate criteria | M(4) | foundation (structural IDs) | Foundation-first: #118/#29 reference G4/G6 IDs. First edit to the shared `gate-criteria-spec.md` table. v1.20→v1.21. |
| 2 | **#96** — G1-04 bidirectional | XS(1) | skill-core (criterion text) | Surgical one-cell edit to the shared table; land early while table churn is low, before #119's refactor. v1.21→v1.22. |
| 3 | **#118** — Stage 9/12/13 judgment dimensions | S(2) | skill-core | Anchors to EXISTING G-PR/G-EX/G-CL (stable). Edits `gate-evaluation-spec.md` (+3 shards), NOT the gate-criteria table — parallel-safe. v1.1→v1.2. |
| 4 | **#29** — 5 Stage-IO boundary contracts | L(8) | skill-core | Edits `stage-io-contracts.md` (+ shards). 6→7 cites #28's G6 (forward-pointer, same PR). |
| 5 | **#501** — Stage-5 structural-premise review gate | M(4) | skill-core (protocol) | Own surfaces (stage-05 §7.2, planning-solutioning-handoff §3.2, stage-io 5→6, checklist 4.8). Reserves two hooks for #889. **SEAM.** |
| 6 | **#889** — ticket↔architecture reconciliation discipline | S(2) | skill-core (discipline) | NEW `core/disciplines/` doc + README + build-philosophy + process.md index + stage-04/05 wiring. **FILLS #501's two seam hooks. SEAM.** Lands after #501. |
| 7 | **#119** — CLI out of criterion cells (Validation method column) | M(4) | skill-core (schema refactor) | **LAST on the shared table** — adds a column + migrates ~14 CLI-bearing rows. v1.22→**v2.0 MAJOR**. Re-enumerate CLI rows against the POST-#28/#96 file. |

**Load-bearing ordering:** (a) **#28 before #118/#29** (structural IDs before consumers); (b) **#119 last** among the three `gate-criteria-spec.md` editors (#28, #96, #119) to keep shared-table churn monotonic; (c) **#501 before #889** (the §3.2 anchor + SR-G1 clause must exist for #889 to fill).

## File Change Matrix (machine-readable)

One path per line; `intent` ∈ {add, edit}. No deletes.

```
# path                                                        intent   issues            note
core/schemas/gate-criteria-spec.md                            edit     #28,#96,#119      SHARED TABLE — serialize #28→#96→#119; #28 adds Gate4/Gate6 sections+self-repair+2 inheritance rows (v1.21); #96 makes G1-04 criterion bidirectional (v1.22); #119 adds Validation-method column + migrates ~14 CLI cells (v2.0)
core/schemas/gate-evaluation-spec.md                          edit     #118              #118 adds Per-Boundary Judgment-Dimension Anchors (Stage9/12/13 → G-PR*/G-EX*/G-CL*) + line-87 enumeration + v1.2
core/schemas/stage-io-contracts.md                            edit     #29,#501          #29 authors 5 boundary sections (2->3,6->7,7->8,8->9,9->12) + prunes Future Boundaries; #501 adds Stage5->6 Structure-review determination row + Validation Rule 4 + resolves the 1->2 forward-pointer
release/references/pipeline/stage-04-planning.md              edit     #28,#889          #28 cross-references G4 (§7); #889 binds the reconciliation discipline as a Phase A0 pre-build step
release/references/pipeline/stage-05-solutioning.md           edit     #501,#889         SEAM — #501 adds §7.2 SR-G1..SR-G4 gate; #889 fills SR-G1 Method pointer + adds §5 Phase 0.5 binding (compose, not duplicate)
release/references/pipeline/stage-06-engineering.md           edit     #28               #28 cross-references G6 (§7)
release/references/pipeline/stage-09-plan-review.md           edit     #118              cross-reference the Stage-9 judgment-dimension anchors
release/references/pipeline/stage-12-execute.md               edit     #118              cross-reference the Stage-12 judgment-dimension anchors
release/references/pipeline/stage-13-close.md                 edit     #118              cross-reference the Stage-13 judgment-dimension anchors
core/standards/planning-solutioning-handoff.md               edit     #501,#889         #501 adds §3.2 Structural-Premise-Review Obligation (rider on T3) w/ reserved #889 hook; #889 fills the reserved slot with a live link
release/references/templates/design-review-checklist.md      edit     #501              #501 adds blocking check 4.8 Structural-premise review + Section 4 pass/fail update
core/disciplines/build-philosophy.md                         edit     #889              re-scope BP-process Skills cell + enforcer line to add ticket-architecture-reconciliation (cite-only; NOT a GAP->cited edit)
core/disciplines/ticket-architecture-reconciliation.md       add      #889              NEW discipline doc (when-fires + 4-step procedure + ticket-age staleness signal + composition map + failure modes)
core/disciplines/README.md                                   edit     #889              +1 discipline row
core/standards/domain-best-practices/process.md              edit     #889              +1 "Practice concept" index row citing the discipline as enforcing artifact (index-only, no body)
```

## Risk Register

| ID | Risk | Type | Sev | Mitigation | Reversibility |
|---|---|---|---|---|---|
| R-1 | `gate-criteria-spec.md` shared-table churn: #28/#96/#119 edit one table. | contention | HIGH | Serialize #28 → #96 → #119; #119 last. One commit per issue, staged paths only. | CHEAP (re-sequence) |
| R-2 | #889 ↔ #501 Stage-5 SEAM on `stage-05-solutioning.md` + `planning-solutioning-handoff.md`. | scope/dependency | HIGH | #501 authors §7.2 + §3.2 reserved slots; #889 FILLS them (does not re-author). #501 before #889. Non-overlapping sections. | MODERATE |
| R-3 | #119 column-addition is a MAJOR bump per the spec's own rule; mis-calling corrupts change-log. | scope | MED | Version block → v2.0 MAJOR (rule names "column additions"). Assert ID count identical across #119's diff. | MODERATE |
| R-4 | #118 anchors to G-PR/G-EX/G-CL IDs #28 might renumber. | dependency | MED | #28 adds G4/G6 ONLY (no renumber of existing IDs); #118 edits gate-evaluation-spec + 3 shards, not the gate-criteria table. | CHEAP |
| R-5 | Stale AC premises (J-* #118, "6 boundaries" #29, empty BP-process GAP #889). | scope (currency) | MED | Reconciled in Stage-5 designs — build from the Stage-5 specs (prose #118, target=5 #29, home-a #889). | CHEAP |
| R-6 | D-Version slot contention — provisional v3.49 may be claimed before merge. | contention (version) | MED | v3.49 is a provisional display; durable = bump-class (minor). Re-claims atomically at Stage 12. | CHEAP pre-Eng / MODERATE post-Commit-0 |
| R-8 | A bad merge could leave the gate-criteria table half-refactored. | rollback | LOW | `git revert -m 1 <merge-sha>` reverts the whole release atomically. Stages 10/11 PLATFORM-SATISFIED. | CHEAP (git revert) |

**Audit-baseline note:** the concurrent-release population driving R-6 was assessed against origin/main @ fetch 2026-07-02, anchor v3.47. Re-check the claimed set at Stage 12 before the atomic claim.

## Verification Plan (AC → method; Stage 7/8 runs these)

| Issue | Verification Method | Expected Result |
|---|---|---|
| #28 | `grep -cE '^\| G4-0[0-9]' core/schemas/gate-criteria-spec.md` ≥ 4; `grep -cE '^\| G6-0[0-9]' …` ≥ 4; self-repair rows = criterion rows; `grep -E 'Plan Readiness\|Engineering Completeness' …` ≥ 2; `grep -c 'G4-0' …stage-04-planning.md` ≥ 1; `grep -c 'G6-0' …stage-06-engineering.md` ≥ 1; version block v1.21 + warn-mode line. | Gate 4 + Gate 6 sections with 5 rows each, matched self-repair, inheritance rows, shard cross-refs, versioned. |
| #96 | `grep G1-04` shows BOTH under-spec and over-spec directions in the Criterion cell; optional rubric-note grep in gate-evaluation-spec. | Bidirectional criterion; v1.22. |
| #118 | new subsection names ≥1 dim per Stage 9/12/13; each dim cites G-PR*/G-EX*/G-CL*; `grep 'Per-Boundary Judgment-Dimension Anchors' stage-{09,12,13}-*.md` = 3; change-log "no schema change needed" (calibration); v1.2. | Judgment dims anchored, shards cross-ref, calibration resolved, versioned. |
| #29 | `grep -cE '^## Boundary: Stage [12678]\|^## Boundary: Stage 9' core/schemas/stage-io-contracts.md` ≥ 10; `### Validation Rules` count rose by 5; 5 pruned from Future Boundaries; per-boundary Required-Artifacts exist in matching shard §6; 8→9↔G-PR, 9→12↔G-EX cite IDs; 7→8 references DT↔QA Handoff Payload. | 5 new boundary sections authored, Future-Boundaries pruned to header+note. |
| #501 | `grep 'SR-G1' stage-05-solutioning.md` returns §7.2 gate table (Authority=Gate-blocking); `grep 'Structure-review determination' stage-io-contracts.md` returns Path A row + Validation Rule 4; `grep 'Structural-Premise-Review Obligation' planning-solutioning-handoff.md` returns §3.2; check 4.8 present; §7 base metrics + §7.1 MD-G unchanged. | Gate-with-teeth present; handoff field required; activation rider fires; no duplication; regressions pass. |
| #889 | `core/disciplines/ticket-architecture-reconciliation.md` exists (when-fires + procedure + ticket-age signal); README row present; build-philosophy BP-process cell + enforcer cite it; process.md index row; stage-04/05 reference it; #501's §3.2 slot + SR-G1 pointer filled. | Reconciliation discipline authored, seam hooks filled, wired. |
| #119 | Criterion cells carry no `grep`/`gh `/`git `/`deploy.sh` tokens (G3-05/G-CL3 excluded); Validation method column holds migrated commands; `grep -oE 'G[1-3]-[0-9]{2}\|G-(PR\|EX\|CL\|BR)[1-9]'` count identical before/after #119's diff; version block v2.0 MAJOR. | No inline CLI in criterion cells; all IDs resolve unchanged; versioned. |

## Deferred Items

None. Scope LOCKED at 7. Peel-#29 (→ effective 22) recorded as a Stage-9-capacity contingency only (not actioned). #354 excluded (shipped).

## Out-of-scope discoveries (Recommendations, not this release)

- G-PR8 is now registered (v1.20). No action.
- build-philosophy BP-governance + BP-coding cells carry the same *skill-wiring pending* marker as BP-process; #889 touches only BP-process. Sibling-cell wiring is a separate (#365-family) item.

---
_Stage 6 Engineering Commit 0. Plan authored from Stage-4 spoke (#2859) + hub R1 D-Version correction. PROV = v3.49._
