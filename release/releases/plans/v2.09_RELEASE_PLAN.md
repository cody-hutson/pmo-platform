## Stage 4 Release Planning — 04-ROLE-delivery-coverage

> **Working version:** v2.09 · **Milestone:** #214 (04-ROLE-delivery-coverage; 14 open / 6 closed) · **Survey commit:** `c4700b7` (HEAD; `git describe` → `v2.03-2-gc4700b7`) · **Survey date:** 2026-06-18
> **Persona:** Release Manager — Release Planning (Tier 2 Recommend; sequencing + change-spec + risk, no calendar dates)
> **Platform-config (hub single-resolution, not re-resolved):** `bundle_doctrine_frame = F1` · `release_size_target_pts = 15-25` · `default_release_class = novel` · capacity weights {routine 1.0, novel 1.15, cross-cutting 1.3, hotfix 0.9}

### Summary (30 seconds)

This release builds **all 12 greenfield role-Specialist skills** for the core delivery roles, each a thin Specialist that **composes** already-shipped function-skills (ADR-019). I verified on disk: **all 12 targets are GREENFIELD** (no SKILL.md, no dir, zero deploy.sh refs) and **all 9 composition-target function-skills EXIST and are deployed**. The format-lock blocker (`03-ROLE-factory-and-pilots`) **shipped as v2.03**, so this release is **UNBLOCKED**.

**Dependency posture:** 11 of 12 builds compose only *shipped* skills → they are **mutually independent**. The single intra-milestone edge is **#450 → #1116** (architect composes principal-engineer). The 03-cycle design (D-450 = COMPOSE-THE-MODE, sub-task #1271, verdict "CLEAN SEAM FOUND / PASS") **softens** this edge: #450 can compose the Principal-Engineer *spec-surface* (`stage-05-solutioning.md` Phase A4) even if #1116 is not yet built. **New fact for v2.09 (changes the 03-cycle calculus):** #1116 is now IN THIS milestone, so the architect's composition target upgrades from spec-surface to a real deployable skill-chain **if #1116 is sequenced first**. I recommend **soft-ordering** (#1116 before #450 in the Engineering sequence), not a hard gate.

**The headline decision is capacity, not dependency.** Raw size ≈ **55 pts** (1×L + 7×M + 4×S) against a **15-25 band** → ~2.2-2.7× over-band. The bundle is a fixed roadmap slice (04/09) and splitting it touches the 04→05 roadmap sequence. I resolve this as an explicit **D-Capacity/Split** decision and recommend **execute-as-one-v2.09-with-internal-waves** under D-C SINGLE — but the quota-budget verdict is **WARN/FAIL-sensitive** at the worst parallel wave (12-way Stage 5), so the wave plan caps parallelism.

- **Proposed Release Class:** `novel` (12 new skills + ≥1 D-class decision; matches v2.03 precedent for this milestone family).
- **Proposed topology:** **D-C SINGLE** (one release branch, one PR) — consistent with v2.03; rollback is CHEAP (`git revert -m 1`).
- **Capacity verdict:** **WARN** (split the worst parallel wave) — see § Capacity & Quota Budget.
- **Special row:** #1341 is operator-attested Layer-2 validation of the *v2.03 pilots* — NOT a standard Stage 5-13 build. Recommend **disposition as operator hand-off, descoped from the build sequence**.

---

### Dependency Graph

Directional edges (`A → B` = "A depends on / composes B"; **bold** = intra-milestone, plain = shipped/external).

```
SHIPPED FUNCTION-SKILLS (all exist + deployed — external to this milestone, no build):
  ppm-agent · weekly-status-rollup · delivery-engine · pmo-process-designer
  pmo-technical-analyst · implementation-planner · release-executor
  pmo-qa-auditor (core) · build-reviewer

INDEPENDENT BUILDS (compose only shipped skills — no intra-milestone edge):
  #1109 pmo-portfolio-manager      → ppm-agent, weekly-status-rollup
  #1110 pmo-program-manager        → ppm-agent, delivery-engine
  #1111 pmo-release-train-engineer → delivery-engine, ppm-agent        (SAFe-conditional)
  #1112 pmo-project-manager        → delivery-engine
  #1113 pmo-product-owner          → pmo-process-designer, delivery-engine   ⟂ co-deconflict #1114
  #1114 pmo-business-analyst       → pmo-process-designer, delivery-engine   ⟂ co-deconflict #1113
  #1115 pmo-scrum-master           → delivery-engine
  #1116 pmo-principal-engineer     → pmo-technical-analyst
  #1117 pmo-software-engineer      → implementation-planner
  #1118 pmo-devops-sre             → release-executor
  #1119 pmo-qa-lead                → pmo-qa-auditor, build-reviewer

SINGLE INTRA-MILESTONE EDGE (softened):
  #450  pmo-architect              → **#1116 pmo-principal-engineer** (in-milestone)
                                   + pmo-technical-analyst (shipped)
```

**The #450 → #1116 edge — recommend SOFT-ORDER (parallel-with-surface-seam), not hard gate.**
- Per #1271 (03-cycle, operator decision D-450 = COMPOSE-THE-MODE, "CLEAN SEAM FOUND / PASS"): the Principal-Engineer capability is a **real canonical instruction surface** (`stage-05-solutioning.md` Phase A4 + the persona card), self-declared composable. So #450 has a **valid composition target regardless of #1116's build state** — the seam is reference-the-spec-surface today, upgrade-to-skill-chain when #1116 ships.
- For v2.09 the upgrade target (#1116) is in-bundle. If #1116's Engineering lands before #450's, #450 composes the **live skill** directly (the stronger seam). If routing reaches #450 first, #450 composes the **spec-surface** (CHEAP one-line upgrade later). Either is correct and ships.
- **Recommendation:** sequence **#1116 before #450** in the Engineering order (soft-order). This is free under D-C SINGLE (Engineering is write-serialized anyway), and it lets #450 compose the live skill. It is NOT a hard blocker — if #1116 stalls, #450 proceeds on the spec-surface seam per #1271. **Cost of soft-order: zero** (it is a sequence preference inside an already-serialized stage).

**Logical co-dependency (not a blocking edge):** #1113 ↔ #1114 (PO/BA) share the identical compose-pair (`pmo-process-designer` + `delivery-engine`) and MUST be **trigger-deconflicted against each other** at Stage 7. This is a *deconfliction adjacency*, not a build-order dependency — neither blocks the other's Engineering; they must be evaluated as a pair at DT. Same pattern (weaker) across the `delivery-engine`-composing cluster {#1110, #1112, #1115} — deconflicted by primary-role + trigger.

---

### Implementation Sequence

Dependency-ordered. Under **D-C SINGLE** topology, Stage 6 Engineering is **write-serialized** — the hub routes **one Engineering chip at a time** (they all edit the shared `deploy.sh` + registry; see § Contention Map). The sequence below is the Engineering commit order. (Stages 5/7/8 are parallel-safe and wave-batched — see § Capacity & Quota Budget.)

**Engineering Commit 0:** the v2.09 release plan file commits to the release branch as the first commit (D-C SINGLE convention).

| Order | Issue | Skill | Module | Composes (shipped unless noted) | Rationale for position |
|---|---|---|---|---|---|
| 1 | **#1116** | pmo-principal-engineer | `release/skills/` | pmo-technical-analyst | **First** — soft-ordered before #450 so the architect can compose the live skill. Carries RECONCILE (see Risk R-RECON). |
| 2 | **#450** | pmo-architect | `release/skills/` | **#1116** (live or spec-surface) + pmo-technical-analyst | **After #1116** (soft-order). The only intra-milestone composer. size:L. |
| 3 | #1119 | pmo-qa-lead | `release/skills/` | pmo-qa-auditor + build-reviewer | Independent. RECONCILE (vs auditor-hardening). |
| 4 | #1117 | pmo-software-engineer | `release/skills/` | implementation-planner | Independent. Dead-path fix in source #298. |
| 5 | #1118 | pmo-devops-sre | `release/skills/` | release-executor | Independent. |
| 6 | #1109 | pmo-portfolio-manager | `release/skills/` * | ppm-agent + weekly-status-rollup | Independent. (* module flag — see § Module Placement) |
| 7 | #1110 | pmo-program-manager | `operations/skills/` | ppm-agent + delivery-engine | Independent. |
| 8 | #1111 | pmo-release-train-engineer | `operations/skills/` | delivery-engine + ppm-agent | Independent. SAFe-conditional activation. |
| 9 | #1112 | pmo-project-manager | `operations/skills/` | delivery-engine | Independent. |
| 10 | #1113 | pmo-product-owner | `operations/skills/` | pmo-process-designer + delivery-engine | **Build adjacent to #1114** (PO/BA deconfliction pair). |
| 11 | #1114 | pmo-business-analyst | `operations/skills/` | pmo-process-designer + delivery-engine | **Build adjacent to #1113.** |
| 12 | #1115 | pmo-scrum-master | `operations/skills/` | delivery-engine | Independent. |

**Sequencing logic:** (1) #1116 → #450 soft-order honored first; (2) `release/`-module skills grouped early so the `RELEASE_SKILLS` array edits cluster; (3) `operations/`-module skills grouped late so the `OPERATIONS_SKILLS` array edits cluster; (4) PO/BA (#1113/#1114) kept adjacent for paired deconfliction. **Within the two independent blocks (3-6 and 7-12) the order is free** — any permutation ships; the grouping minimizes array-edit thrash, it is not a dependency constraint.

**#1341 (validation story): NOT in the build sequence.** Disposition is operator hand-off — see § Stage Applicability Matrix special row + § Recommendations.

---

### Stage Applicability Matrix

Default = **all stages 5-13 apply** to every build issue (each is a NEW skill needing design / engineering / dev-test / qa). The 12 builds share **one release lifecycle** (D-C SINGLE), so Stages 9-13 are **release-level (run once for the bundle)**, not per-issue. Per-issue stages are 5-8.

| Issue | S5 Solutioning | S6 Engineering | S7 Dev-Test | S8 QA | S9-13 (release-level) | Skip justification |
|---|---|---|---|---|---|---|
| #1116 principal-eng | ✅ | ✅ | ✅ | ✅ | bundle | RECONCILE design surface (auditor subsumption + #297 dead-path) → S5 required |
| #450 architect | ✅ | ✅ | ✅ | ✅ | bundle | size:L; composition-seam design (harvest #1271) → S5 required |
| #1119 qa-lead | ✅ | ✅ | ✅ | ✅ | bundle | RECONCILE (vs auditor-hardening) → S5 required |
| #1117 software-eng | ✅ | ✅ | ✅ | ✅ | bundle | dead-path fix (#298) + 1-mode design → S5 |
| #1118 devops-sre | ✅ | ✅ | ✅ | ✅ | bundle | decision-vs-execution boundary → S5 |
| #1109 portfolio-mgr | ✅ | ✅ | ✅ | ✅ | bundle | RECONCILE (overlap pair) → S5 |
| #1110 program-mgr | ✅ | ✅ | ✅ | ✅ | bundle | program/project/portfolio boundary → S5 |
| #1111 RTE | ✅ | ✅ | ✅ | ✅ | bundle | SAFe-conditional activation design → S5 |
| #1112 project-mgr | ✅ | ✅ | ✅ | ✅ | bundle | delivery-engine-cluster deconfliction → S5 |
| #1113 product-owner | ✅ | ✅ | ✅ | ✅ | bundle | PO/BA split — paired deconfliction → S5+S7 |
| #1114 business-analyst | ✅ | ✅ | ✅ | ✅ | bundle | PO/BA split — paired deconfliction → S5+S7 |
| #1115 scrum-master | ✅ | ✅ | ✅ | ✅ | bundle | delivery-engine-cluster deconfliction → S5 |

**No Stage skips for any build.** Rationale: every issue is a NEW skill (S5 design genuinely applies — composition seam + boundary + ≥3 failure modes + reversibility are real design content); every issue ships functional behavior (S7/S8 verify trigger-deconfliction + eval pass, which is the load-bearing risk for a 12-skill suite). The `novel` Release Class biases Stage 5 activation to **ALL** (per release-class-taxonomy.md), reinforcing no-skip.

**Special non-standard row — #1341 (validation story):**

| Issue | Disposition | Stages | Rationale |
|---|---|---|---|
| **#1341** | **Operator hand-off (descope from build pipeline)** | **NONE of S5-13** as a build; a single operator-attested validation action instead | #1341 is `observation`-shaped carried-forward validation of the **v2.03 pilots** (`pmo-technical-program-manager`, `pmo-program-coordinator`) — L2 Full-tier evals (5+5, ≥90%) + cross-skill harness + feeding-doc instances + L1 report. These run against **operator-local git-ignored Skills-Map staging** (Layer-2), so a repo-bound spoke **cannot produce or machine-verify them** (same Layer-2 boundary as v2.03 R5). It is NOT a Specialist build — it has no SKILL.md to author, no deploy.sh entry, no package. |

**#1341 recommended disposition (operator decides):** Three live options — **(A)** run the L2 validation operator-local now and attach evidence, then **mark #1341 as closed** independent of the v2.09 build pipeline; **(B)** carry #1341 forward to the `05-ROLE-sustain-coverage-router` milestone (which is the natural home for cross-skill harness sustainment); **(C)** keep it open as a standing operator obligation tracked outside the pipeline. **Recommendation: (A) if the operator can run the eval this cycle**, because #1341's own "what good looks like" states the format-lock should be behaviorally validated on the two extreme pilots **BEFORE Wave-2/3/4 builds consume the locked format** — and v2.09 IS those Wave-2/3 builds. If (A) is not feasible this cycle, **(B)** preserves the intent without blocking v2.09 (the 12 builds consume the *structurally-locked* format, which shipped QA-passed at v2.03; the behavioral validation is a confidence gate, not a structural blocker). Reversibility: **CHEAP / MEDIUM** (validation can run any time; deferral does not invalidate prior structural QA).

---

### Contention Map

Every build creates a **new `skills/<name>/` directory** (zero cross-build collision — disjoint paths). The contention is on the **shared registration surfaces** every build must edit. These are the **write-serialization points**.

| Shared surface | Who edits | Edit type | Contention class | Resolution |
|---|---|---|---|---|
| **`core/deploy/deploy.sh`** — `RELEASE_SKILLS` array | #1116, #450, #1119, #1117, #1118 (+#1109 if `release/`) | +1 array entry each | **EDIT-EDIT, same region (array)** | **Write-serialized.** Under D-C SINGLE every Engineering chip rebases on branch HEAD; each appends its entry. The array is the primary serialization driver — Stage 6 routes ONE chip at a time. |
| **`core/deploy/deploy.sh`** — `OPERATIONS_SKILLS` array | #1110, #1111, #1112, #1113, #1114, #1115 (+#1109 if `operations/`) | +1 array entry each | **EDIT-EDIT, same region (array)** | **Write-serialized**, same mechanism. |
| **Core skill registry** (per `skill-deployment.md` registration tax; logical-registry surface) | ALL 12 | +1 entry each | **EDIT-EDIT, same file** | **Write-serialized.** Each build registers its own node in the same commit as its SKILL.md. |
| **Module README rosters** (`release/skills/README.md` / `operations/skills/README.md` skill-roster sections, if maintained) | `release/` builds → release roster; `operations/` builds → operations roster | +1 roster row each | **EDIT-EDIT, per-module** | **Write-serialized within each module's README.** Two disjoint files (release vs operations), so the two module groups don't collide with each other — but builds within a module group serialize on their shared README. |
| **`packages/<name>.skill`** | ALL 12 (build own package) | ADD (new artifact each) | **No contention** | Each skill owns its own `.skill` file — disjoint paths. |
| **`operations/skills/_shared/*.md`** (the v2.03 shared dependency surface) | ALL `operations/` role builds | **READ-ONLY (runtime consume)** | **No write contention** | Shipped at v2.03 (#186); v2.09 builds are downstream consumers, not writers. |
| Composed function-skills (ppm-agent, delivery-engine, etc.) | ALL | **READ-ONLY (runtime compose)** | **No contention** | Composition is invocation, not modification (ADR-019). Zero edits to composed skills. |

**What MUST be write-serialized:** the **`deploy.sh` array edits** + the **core skill registry edits** + the **per-module README roster edits**. These are why Stage 6 Engineering is NO-parallel (write-serialized) under D-C SINGLE — the canonical Parallelism Rule (Procedure 2 Step 5): "Engineering — write-serialized; concurrent chips race on branch HEAD; file-disjoint commits still serialize at git's push level." The new `skills/<name>/` dirs and `.skill` packages are **disjoint and parallel-safe**; only the three shared registration surfaces force serialization.

**Cross-PR overlap audit — baseline pin (per audit-baseline discipline):** Audit-start baseline SHA = **`c4700b7`** (HEAD; `git describe` → `v2.03-2-gc4700b7`); recent-window = last 10 merged PRs + all open PRs at this SHA. **At this baseline the open-PR population is observably EMPTY** (`gh pr list --state open` → NO OPEN PRS) for the in-scope paths (`core/deploy/deploy.sh`, `release/skills/`, `operations/skills/`, the core registry). Per audit-baseline discipline this default-to-no-cross-PR-contention is **pinned to SHA `c4700b7` + the last-10-merged-PR window** and MUST be re-checked at **Stage 9 Phase A6.5** (PRIMARY, HALT-eligible) and at **Stage 7/8 entry** (warn-only) — a single PR appearing later (e.g., a concurrent release editing `deploy.sh`) silently invalidates this classification.

**File Change Matrix (machine-readable — one path per line; deterministic extraction for S7/8/9 chips):**

```
ADD    release/skills/pmo-principal-engineer/SKILL.md                  #1116
ADD    release/skills/pmo-architect/SKILL.md                           #450
ADD    release/skills/pmo-qa-lead/SKILL.md                             #1119
ADD    release/skills/pmo-software-engineer/SKILL.md                   #1117
ADD    release/skills/pmo-devops-sre/SKILL.md                          #1118
ADD    release/skills/pmo-portfolio-manager/SKILL.md                   #1109  (module flag — see § Module Placement)
ADD    operations/skills/pmo-program-manager/SKILL.md                  #1110
ADD    operations/skills/pmo-release-train-engineer/SKILL.md           #1111
ADD    operations/skills/pmo-project-manager/SKILL.md                  #1112
ADD    operations/skills/pmo-product-owner/SKILL.md                    #1113
ADD    operations/skills/pmo-business-analyst/SKILL.md                 #1114
ADD    operations/skills/pmo-scrum-master/SKILL.md                     #1115
EDIT   core/deploy/deploy.sh                                           ALL (RELEASE_SKILLS / OPERATIONS_SKILLS array entries — write-serialized)
EDIT   core/specs/anthropic-base-vs-build-registry.md                  ALL (registry node entries — IF this is the registration surface; confirm at S5/S6)
ADD    packages/pmo-principal-engineer.skill                           #1116
ADD    packages/pmo-architect.skill                                    #450
ADD    packages/pmo-qa-lead.skill                                      #1119
ADD    packages/pmo-software-engineer.skill                           #1117
ADD    packages/pmo-devops-sre.skill                                  #1118
ADD    packages/pmo-portfolio-manager.skill                           #1109
ADD    packages/pmo-program-manager.skill                             #1110
ADD    packages/pmo-release-train-engineer.skill                      #1111
ADD    packages/pmo-project-manager.skill                             #1112
ADD    packages/pmo-product-owner.skill                               #1113
ADD    packages/pmo-business-analyst.skill                            #1114
ADD    packages/pmo-scrum-master.skill                                #1115
EDIT   release/skills/README.md                                        release-module builds (roster rows — IF maintained; confirm at S6)
EDIT   operations/skills/README.md                                     operations-module builds (roster rows — IF maintained; confirm at S6)
```

**Domain classification (Phase A3, per stage-04-planning.md §5.7):** `domain_practice: { source: N/A — pipeline-internal release, date: 2026-06-18, domain: software }`. The File Change Matrix is entirely internal pmo-platform skill-authoring artifacts (SKILL.md files + deploy infra + packages) → **sourcing-exempt** (pipeline-internal); **domain-classified `software`** (skill/tooling code; the `core/standards/domain-best-practices/software.md` guide encodes the practice). No external best-practice sourcing step. **No file moves/renames** → no A3.2 ref-form sweep, no structural-blast-radius sub-audit (pure ADD + array-append edits).

---

### Risk Register

| ID | Risk (named — owner, mitigation) | Severity | Reversibility |
|---|---|---|---|
| **R-CAP** | **Over-scope / capacity overrun.** ~55 raw pts vs 15-25 band (~2.2-2.7×). The worst parallel wave is a **12-way Stage 5** batch drawing cumulatively against the 5-hour usage window. Owner: Hub (routing) + Operator (D-Capacity/Split). Mitigation: cap parallelism per wave (see § Capacity wave plan — max 4-6 concurrent Stage-5 spokes, batched across waves); Checkpoint B re-validates before every wave (load-bearing). | **HIGH** | CHEAP (sequence/batch is re-tunable mid-release; no artifact invalidated by re-batching) |
| **R-SERIAL** | **Engineering serialization stall.** All 12 builds edit shared `deploy.sh` + registry → Stage 6 is ONE-chip-at-a-time. 12 sequential Engineering commits is the long pole of the release. Owner: Hub. Mitigation: D-C SINGLE git-serializes by design; each chip rebases on HEAD + appends its disjoint array entry; the array edits are append-only (low merge-conflict risk). Accept the serial Engineering cost as inherent to the shared-registration surface. | **MEDIUM** | CHEAP (re-runnable; each commit independent) |
| **R-450** | **#450 → #1116 ordering.** If #1116 stalls, #450 must fall back to the spec-surface seam (#1271 C2). Owner: #450 Engineering spoke. Mitigation: soft-order #1116 first; #450's SKILL.md cites the PE capability such that it composes the live skill if present, else the `stage-05-solutioning.md` Phase A4 spec-surface (CHEAP one-line upgrade later). NOT a hard blocker — #450 ships either way. | **LOW** | CHEAP (spec-surface→skill-chain is a 1-line text edit) |
| **R-RECON** | **Unresolved RECONCILE obligations.** #1116 (re-scope `pmo-technical-analyst` hardening #259/#177; fix #297 dead-path `pmo-platform/skills/…`→`release/skills/…`) and #1119 (keep QA-Lead Specialist distinct from auditor-hardening #219/#222/#248/#249/#250) carry RECONCILE flags that MUST resolve at Stage 5 BEFORE Engineering. Owner: #1116/#1119 Stage-5 spokes. Mitigation: Stage 5 Solutioning fires for both (novel → bias ALL); the RECONCILE resolution is a named S5 deliverable + an AC. | **MEDIUM** | MODERATE (a wrong subsumption boundary forks a skill — caught at S5/S7, but rework is more than a text edit) |
| **R-DECONF** | **Trigger collision across 12 new skills.** This is the load-bearing risk for a role-suite: 12 Specialists composing 9 function-skills, several sharing compose-pairs (PO/BA identical pair; 3-way delivery-engine cluster; architect/principal-eng solution-vs-system). False-positive cross-fire degrades routing. Owner: DT (Stage 7) spokes. Mitigation: every build's AC mandates `pmo-skill-refiner` cross-skill false-positive detection; the PO/BA pair (#1113/#1114) and the delivery-engine cluster ({#1110,#1112,#1115}) get **explicit pairwise deconfliction** at S7; architect/principal-eng get the solution-vs-system boundary check. **Run the full 12×12 cross-trigger harness at S8 QA** (cross-output coherence). | **MEDIUM-HIGH** | CHEAP (description/trigger tuning is editable) |
| **R-REG** | **deploy.sh / registry / package registration drift.** A NEW skill dir without its array entry fails Check 5; missing `version:` fails Check 6; stale/absent `.skill` fails Check 7; unregistered mirror fails Check 12/13. ×12 builds = 12× the registration tax. Owner: each Engineering spoke. Mitigation: each build does its OWN full registration (array entry + `version:` frontmatter + `.skill` rebuild + registry node) in the same commit, then `deploy.sh --check` before close. | **MEDIUM** | CHEAP (re-runnable; `deploy.sh --check` is the safety net) |
| **R-L2** | **Layer-2 verification boundary (#1341 + per-build eval ACs).** #1341 and each build's "passes eval tier" AC resolve partly to operator-local git-ignored eval runs. A repo-bound spoke produces repo files + repo-observable verification (`deploy.sh --check`, package rebuild, structural QA) but CANNOT run the L2 behavioral eval. Owner: Operator (hand-off) + Hub (surfaces the gap). Mitigation: repo spokes flag L2 eval as operator-attached evidence; DT/QA verify *presence* of attested evidence, not its production. #1341 descoped to operator hand-off. | **MEDIUM** | N/A (boundary discipline) |
| **R-SAFe** | **#1111 RTE SAFe-conditional activation.** The RTE Specialist is only meaningful under a SAFe `delivery_approach`; the platform default is Scrum. Risk: shipping a skill that never fires under the operator's config, or that cross-fires with `pmo-scrum-master`/`pmo-program-manager`. Owner: #1111 Stage-5/7 spokes. Mitigation: AC mandates "does not fire under non-SAFe `delivery_approach`" + deconfliction vs scrum-master/program-manager. Ship it as config-gated (methodology-as-config); behavioral validation is conditional on a SAFe instance. | **LOW** | CHEAP (config-gated; dormant under non-SAFe is correct behavior) |
| **R-ROLLBACK** | **Release-level rollback complexity.** 12 skills + 2 array regions + registry + 2 READMEs + 12 packages land together. Owner: Operator (Stage 12/13). Mitigation: under D-C SINGLE the entire release is ONE PR → rollback = `git revert -m 1` of the merge commit (CHEAP; git-native, no data migration, no schema change). All 12 dirs + packages are purely additive (no prior consumer) → revert cleanly. Per-skill partial rollback: remove that skill's array entry + dir + package + registry node, re-run `deploy.sh --check`. | **LOW** | **CHEAP** (git-native `git revert`) |

**Rollback strategy (consolidated):** D-C SINGLE ⇒ one release branch, one PR. Full rollback = `git revert -m 1` of the merge commit. No data migration, no schema change, no Document-Tier-1 governance-file mutation (`deploy.sh` is infra, not governance; the registry is a spec doc, additively edited). Per-skill partial rollback is supported (additive dirs). **Reversibility tier: CHEAP / Confidence HIGH.**

---

### Capacity & Quota Budget

This is the headline decision. Raw sizing vs the band:

| Size | Issues | Count | Weighted (novel ×1.15 applied at release level, not per-issue) |
|---|---|---|---|
| L (≈8) | #450 | 1 | ~8 |
| M (≈5) | #1109,#1110,#1111,#1112,#1116,#1118,#1119 | 7 | ~35 |
| S (≈3) | #1113,#1114,#1115,#1117 | 4 | ~12 |
| **Total** | | **12** | **~55 raw pts** |

**~55 pts vs the 15-25 band → ~2.2-2.7× over-band.** This is unambiguously over-band. **Localization Check (decision-discipline Mechanism 1):** the generic "split anything >25 pts" heuristic is reconciled against THIS release's context — (a) the bundle is a **fixed roadmap slice (04/09)**; splitting it touches the **04→05 roadmap sequence** (`05-ROLE-sustain-coverage-router` is blocked-by 04); (b) 11/12 builds are **independent and mechanically identical** (thin Specialist composing shipped skills) — the per-issue cost is low and uniform, unlike a 55-pt release of heterogeneous deep work; (c) v2.03 (the immediate predecessor in this family) shipped 2 skills as one `novel` D-C SINGLE release with CHEAP rollback. The over-band figure is driven by **count, not per-item complexity** — which is exactly the case the band heuristic over-penalizes.

#### Phase A6 — Quota-Budget Pre-Check (Checkpoint A, plan-time estimate)

Per quota-budget-protocol.md §3. The load-bearing input is the **worst parallel wave** at the parallel-safe stages (5/7/8). Stage 6 is write-serialized → no concurrent-batch draw.

```
### Quota Budget

Verdict: WARN (per quota-budget-protocol.md Checkpoint A)
Parallel-eligible spokes per parallel stage (from Stage Applicability Matrix):
  Stage 5: 12 · Stage 7: 12 · Stage 8: 12   (all 12 builds apply at every parallel stage)
Per-spoke cost estimate: size-bucket band — 1×L (moderate–high) + 7×M (low–moderate) + 4×S (lowest)
  (source: quota-budget-protocol.md §5 size-bucket heuristic; no telemetry medians yet → [CALIBRATE-AFTER-3] MEDIUM)
Assumed/stated remaining usage-window envelope: conservative default (operator quota state UNSTATED at hub start
  — operator should state fresh/partial-N%/near-tail at Stage 12; absent a statement, plan assumes a single
  fresh 5-hour window cannot absorb a 12-way parallel wave of mixed M/L spokes)
Estimated cumulative draw % (worst parallel batch — 12-way Stage 5, all spokes in one response):
  12 concurrent spokes (1L+7M+4S) against one window = HIGH cumulative draw, estimated 50–80%+ of a fresh window
  → lands in WARN, FAIL-sensitive if the window is partial/near-tail at wave time
Routing: WARN → window-aware timing + quota-budgeting (SPLIT the 12-way wave into capped sub-waves).
  Recommended cap: max 4–6 concurrent Stage-5 spokes per sub-wave (3 sub-waves of ~4, or 2 of ~6).
Note: Checkpoint B re-validates at every parallel wave (runtime, load-bearing) with
  PROCEED/SERIALIZE/DEFER/REDUCE-scope; STAGGER is secondary rate-limit-only. Bands + cumulative-draw budget
  are [CALIBRATE-AFTER-3] MEDIUM.
```

**Verdict: WARN — split the worst parallel wave.** A single 12-way parallel Stage-5 (or Stage-7, or Stage-8) batch is the capacity risk. It is NOT a reason to split the *release* (the builds are independent and cheap per-item); it is a reason to **cap parallelism per wave**. The mitigation is wave-batching, not bundle-splitting.

**Split recommendation:** **DO NOT split the bundle.** Execute as **one v2.09 release (D-C SINGLE) with internal waves** that cap per-wave parallelism. Splitting the bundle would touch the 04/09 roadmap slice and the 04→05 blocked-by edge for no capacity benefit the wave-cap doesn't already deliver. See D-Capacity/Split below.

#### Parallelism / Wave Plan

Stages **5 / 7 / 8 are parallel-safe** (output = sub-task comments; no file-contention surface). Stage **6 Engineering is write-serialized** (shared `deploy.sh`/registry/README edits). The wave structure caps the parallel stages to respect R-CAP; Engineering runs one chip at a time per the Implementation Sequence.

| Wave | Stage | Issues (capped batches) | Parallelism | Note |
|---|---|---|---|---|
| **W1** | Stage 5 Solutioning | Batch 5a: {#1116, #450, #1119, #1117} · Batch 5b: {#1118, #1109, #1110, #1111} · Batch 5c: {#1112, #1113, #1114, #1115} | **≤4 concurrent per batch** (3 batches) | #1116 in 5a (RECONCILE early); #450 in 5a (composes #1116's design output). PO/BA (#1113/#1114) in 5c together. Checkpoint B before each batch. Collective Review fires after ALL Stage-5 close (multi-issue S5 → ratifies ADR-019 status per the ADR). |
| **W2** | Stage 6 Engineering | All 12, in Implementation-Sequence order | **1 at a time (write-serialized)** | The serial long pole. #1116 → #450 → release-module builds → operations-module builds. Each chip: author via `pmo-skill-refiner`, register (array + version + package + registry), `deploy.sh --check`, commit. |
| **W3** | Stage 7 Dev-Test | Batch 7a/7b/7c (same ≤4 cap) **+ mandatory pairwise deconfliction**: {#1113,#1114} same batch; {#1110,#1112,#1115} same batch; {#450,#1116} same batch | **≤4 concurrent per batch** | Cross-skill false-positive detection per build; the named pairs/clusters deconflicted together. Checkpoint B before each batch. |
| **W4** | Stage 8 QA | Per-build QA batches (≤4 cap) **+ one release-level 12×12 cross-trigger coherence run** | **≤4 concurrent per batch** | Cross-output coherence (pmo-qa-auditor Mode B) over all 12 — the suite-level deconfliction gate. R-DECONF lands here. |
| **W5** | Stages 9-13 | Release-level (one Plan Review → Dry-run → Snapshot → Execute → Close) | serial | Deep Stage-9 (novel). Stage 12 = one release PR. Stage 13 = one chore PR (INDEX+DIGEST+NOTES) + Milestone close + orphan cleanup. |

**Cap rationale:** ≤4 concurrent spokes per parallel batch keeps the worst-wave cumulative draw in PROCEED/low-WARN territory against a fresh window, and gives Checkpoint B headroom to SERIALIZE/DEFER if the operator's stated window is partial at wave time. The cap is a **plan-time recommendation**; Checkpoint B is the load-bearing runtime gate (re-validates remaining envelope before every batch). Operator should **state quota window at hub start** (fresh / partial-N% / near-tail) so Checkpoint B has a real envelope rather than the conservative default.

---

### Operator Decisions (D-Gate blocks)

#### D-ReleaseClass: What Release Class does v2.09 carry?
**Gate input:** Spoke-proposed class + trigger evidence per release-class-taxonomy.md § Class Enum.
**Pre-decided (if applicable):** None. (Hub-resolved `default_release_class = novel` is the default, not a pre-decision.)
**Gate decision:** Choose between (A) routine, (B) **novel** ← recommended, (C) cross-cutting, (D) hotfix.
**Blocks:** Downstream per-class differentiation posture (engagement density / Stage 9 depth / Stage 5 bias / Stage 13 window). Does not block Engineering.
**Upstream compatibility:** N/A — Release Class is PMO platform internal taxonomy; no Anthropic upstream surface. Upstream compatibility check does not apply.
**Reversibility / Confidence:** CHEAP / HIGH (re-classifiable later with operator approval per Re-Classification Protocol; cheaper-to-stricter is CHEAP).
**Recommendation:** **`novel`.** Trigger evidence: novel(a) ≥1 issue introduces a new skill — **12 new skills** fire this maximally; novel(b) ≥1 D-class decision in the plan — **3 D-decisions** (this block). NOT `cross-cutting`: the File Change Matrix touches **zero** `pipeline/stage-*.md` files and **zero** of the {CLAUDE.md, OPERATIONS.md, …} governance set; the in-bundle compositional edges are 1 (#450→#1116), well under the cross-cutting(c) ≥3 threshold. NOT `routine`: 12 new files fired (routine(c) zero-new-files fails). Differentiation posture: **Engagement Standard · Stage 9 Deep · Stage 5 activation bias ALL · Stage 13 outcome-window 30-day** — identical to the v2.03 precedent for this milestone family.

#### D-Capacity/Split: Execute v2.09 as one release with internal waves, or split the bundle?
**Gate input:** Phase A6 quota-budget estimate (WARN) + raw sizing (~55 pts vs 15-25 band) + the fixed-roadmap-slice constraint (04/09) + the 04→05 blocked-by edge.
**Pre-decided (if applicable):** None.
**Gate decision:** Choose between **(A) execute-as-one-v2.09-with-internal-waves** ← recommended · **(B) split-the-bundle** (e.g., release the Strategy-to-Execution roles as one version and Technical-Delivery roles as another) · (C) reduce per-spoke cost (compact prompts) within (A).
**Blocks:** The wave plan + the topology (D-C SINGLE assumes one release) + Procedure 1 scaffolding scope. Renders before scaffolding.
**Upstream compatibility:** N/A — this D-decision does not modify skill-authoring surface (it is release-scope / wave-batching / branch topology). Upstream compatibility check does not apply.
**Reversibility / Confidence:** **MODERATE / HIGH.** MODERATE because once scaffolding creates 12 per-issue sub-tasks under one milestone+branch, retro-splitting into two versions means re-bundling (touches the milestone + the 05 blocked-by edge) — undo in days, minor coordination loss. HIGH confidence the wave-capped single-release path works (v2.03 precedent + independent/uniform builds + CHEAP git rollback).
**Recommendation:** **(A) execute as one v2.09 release with internal waves.** Localization Check: the over-band figure is **count-driven, not complexity-driven** — 11/12 builds are independent, mechanically identical thin Specialists composing shipped skills; the per-item cost is low and uniform. The capacity risk is the *worst parallel wave*, which the **≤4-concurrent wave cap + Checkpoint B** already mitigate without splitting. Splitting the bundle would touch the **fixed 04/09 roadmap slice** and the **04→05 blocked-by edge** (a roadmap-sequence mutation, MODERATE reversibility) to buy a capacity reduction the wave-cap delivers for free. Split (B) only becomes correct if the operator's quota window is structurally near-tail for an extended period (Checkpoint B would DEFER repeatedly) — in which case **defer waves across windows**, still within one release, before splitting the bundle.

#### D-450-Order: Hard-order #1116-before-#450, or parallel-with-surface-seam?
**Gate input:** The single intra-milestone edge #450→#1116 + the 03-cycle design D-450 = COMPOSE-THE-MODE (#1271, "CLEAN SEAM FOUND / PASS") + the new fact that #1116 is now in-bundle.
**Pre-decided (if applicable):** D-450 = COMPOSE-THE-MODE was operator-rendered 2026-06-17 (03 cycle); it governs the *seam mechanism* (architect composes the PE capability). It did NOT pre-decide the v2.09 *build order* (#1116 was not yet in a build milestone when D-450 was rendered).
**Gate decision:** Choose between **(A) soft-order #1116 before #450 (parallel-with-surface-seam fallback)** ← recommended · (B) hard-gate #450 on #1116 (block #450 Engineering until #1116 ships) · (C) no ordering (#450 composes spec-surface regardless).
**Blocks:** The Engineering sequence position of #450 vs #1116. Renders at/before Engineering.
**Upstream compatibility:** **REQUIRED — this D-decision touches skill-authoring surface** (it determines what #450's SKILL.md `## Composition` section cites: a live skill-chain to `pmo-principal-engineer` vs a spec-surface reference to `stage-05-solutioning.md` Phase A4).
- Anthropic skill-creator convention: Frontmatter is `name:` + `description:` only per the `anthropic-skills:skill-creator` schema (re-verified in the 03-cycle at commit `353ad8b` per #1271 Evidence-Grounding; matches `upstream-reference-catalog.md` `skill-md-frontmatter` entry). The `## Composition` / `## Role` body sections are a **registered PMO body-anatomy extension** (`canonical-skill-structure.md` + ADR-019), NOT an upstream frontmatter field — so citing a composed skill in the body does not touch the upstream frontmatter contract.
- Upstream-compatible: no conflict. The composition seam lives in the SKILL.md **body** (PMO-extension surface), not in upstream-governed frontmatter. Compatible-path evidence: skill-creator schema review carried forward from #1271 (2026-06-17, `name`+`description` only confirmed); the body-anatomy extension is the same one v2.03's pilots used.
- Sourcing posture: **own** (per ADR-023). `pmo-architect` and `pmo-principal-engineer` author governance-binding ADRs (HIGH blast-radius) and are judgment Specialists (LOW commodity-stability) → own-with-harvest; no runtime Anthropic dependency. Justification: blast-radius HIGH × commodity-stability LOW = own-only quadrant.
**Reversibility / Confidence:** **CHEAP / HIGH.** The spec-surface→skill-chain upgrade is a one-line text edit in #450's `## Composition`; soft-order vs no-order is a sequence preference inside an already-serialized Engineering stage (zero cost).
**Recommendation:** **(A) soft-order #1116 before #450.** Rationale: under D-C SINGLE Engineering is write-serialized anyway, so sequencing #1116 first is **free**, and it lets #450 compose the **live skill** (the stronger seam) rather than the spec-surface. NOT a hard gate (B) — if #1116 stalls, #450 ships on the spec-surface seam per #1271 (the 03-cycle already proved this path is valid). NOT no-order (C) — there is no reason to leave #450 on the weaker spec-surface seam when #1116 is one slot earlier in the same serialized stage.

---

### Recommendations

1. **Topology — D-C SINGLE** (one `release/v2.09-role-delivery-coverage` branch, write-serialized Engineering, one release PR at Stage 12). Consistent with v2.03; rollback is CHEAP (`git revert -m 1`). This is the implied topology of D-Capacity/Split option (A); surfaced here as the standing recurring **D-C topology** decision — recommend **D-C SINGLE / CHEAP / HIGH**.
2. **Approve the three D-decisions** as recommended: D-ReleaseClass = `novel`; D-Capacity/Split = (A) one release with internal waves; D-450-Order = (A) soft-order #1116 before #450.
3. **State the quota window at hub start** (fresh / partial-N% / near-tail). The Phase A6 estimate used a conservative default because the window was unstated; Checkpoint B needs a real envelope to gate the ≤4-concurrent waves accurately. The 12-skill count makes this the most quota-sensitive release in the family to date.
4. **#1341 disposition** (operator decides): recommend **(A) run the L2 validation operator-local this cycle and mark #1341 as closed**, because v2.09 IS the Wave-2/3 build wave that #1341's "what good looks like" wants the format validated *before*. If not feasible this cycle, **(B) carry #1341 to `05-ROLE-sustain-coverage-router`**. Either way, **descope #1341 from the v2.09 build pipeline** — it is not a Specialist build.
5. **Resolve the two RECONCILE flags at Stage 5 before Engineering** (R-RECON): #1116 (re-scope `pmo-technical-analyst` hardening #259/#177; fix #297 dead-path) and #1119 (QA-Lead Specialist distinct from auditor-hardening #219/#222/#248/#249/#250). Both are named S5 deliverables + ACs; the `novel` Stage-5-bias-ALL guarantees S5 fires.
6. **Run the suite-level 12×12 cross-trigger coherence harness at Stage 8** (R-DECONF) in addition to per-build false-positive detection at Stage 7. With 12 Specialists over 9 function-skills and three named collision surfaces (PO/BA identical pair; 3-way delivery-engine cluster; architect/principal-eng solution-vs-system), suite-level deconfliction is the load-bearing quality gate — do not rely on per-build checks alone.

**Discoveries outside scope (noted, NOT added to the Milestone):**

- **D1 — Registration surface ambiguity.** Each build's AC says "register in the `core/` skill registry," but the canonical registry file is not unambiguous on disk — the closest candidate is `core/specs/anthropic-base-vs-build-registry.md` (the base-vs-build registry), while the "logical skill-registry broker" the ACs reference for *composition* (ADR-007 runtime skill-chaining) is a conceptual surface, not a single editable node-list file. **Recommend the hub confirm the exact registration target at Stage 5/6** (it may be that "registration" = `deploy.sh` arrays + `version:` + package + mirror, with the "registry" being the deploy.sh per-module arrays themselves — in which case the File Change Matrix `core/specs/anthropic-base-vs-build-registry.md` EDIT line should be dropped). Flagged so a spoke does not author a parallel registry file.
- **D2 — #1109 module placement conflict (load-bearing — see § Module Placement below).** #1109's issue body AC says `pmo-portfolio-manager/SKILL.md` and labels the role `release/`-bound in one place, but the role is a PMO operations role (portfolio-tier health/rollup, composes `ppm-agent` + `weekly-status-rollup`, both `operations/skills/`). This conflicts with the sibling operations-role pattern. **Recommend confirming #1109 → `operations/skills/` at Stage 5** (consistent with #1110-#1115 and the v2.03 pilots). Marked with a module flag in the File Change Matrix.
- **D3 — Milestone #214 has no Parallelization Map section.** The milestone predates the standing `## Parallelization Map (recorded YYYY-MM-DD)` convention, so the Phase A0 currency check is suppressed per the standing-applicability clause (not a defect). After operator approval of this plan, the hub may optionally backfill the Parallelization Map + the `### Release Outcome Statement` H3 into the milestone description at Phase B1 (per Procedure 0 Step 7) — noted, not required for this plan's validity.

### Module Placement (per skill — target module path)

| Issue | Skill | Module path (per issue body) | Confidence |
|---|---|---|---|
| #450 | pmo-architect | `release/skills/pmo-architect/` | **CONFIRMED** (body: "pipeline-bound / Release & Engineering → `release/`") |
| #1116 | pmo-principal-engineer | `release/skills/pmo-principal-engineer/` | **CONFIRMED** (body: "Module: `release/` (pipeline-bound, Stage 5)") |
| #1117 | pmo-software-engineer | `release/skills/pmo-software-engineer/` | **CONFIRMED** (body: "Module: `release/` (pipeline-bound, Stage 6)") |
| #1118 | pmo-devops-sre | `release/skills/pmo-devops-sre/` | **CONFIRMED** (body: "Module: `release/` (pipeline-bound, Stage 12)") |
| #1119 | pmo-qa-lead | `release/skills/pmo-qa-lead/` | **CONFIRMED** (body: "Module: `release/` (pipeline-bound, Stage 8)") |
| #1110 | pmo-program-manager | `operations/skills/pmo-program-manager/` | **CONFIRMED** (body: "Module: `operations/` (PMO role)") |
| #1111 | pmo-release-train-engineer | `operations/skills/pmo-release-train-engineer/` | **CONFIRMED** (body: "Module: `operations/` (PMO role)") |
| #1112 | pmo-project-manager | `operations/skills/pmo-project-manager/` | **CONFIRMED** (body: "Module: `operations/` (PMO role)") |
| #1113 | pmo-product-owner | `operations/skills/pmo-product-owner/` | **CONFIRMED** (body: "Module: `operations/` (PMO role)") |
| #1114 | pmo-business-analyst | `operations/skills/pmo-business-analyst/` | **CONFIRMED** (body: "Module: `operations/` (PMO role)") |
| #1115 | pmo-scrum-master | `operations/skills/pmo-scrum-master/` | **CONFIRMED** (body: "Module: `operations/` (PMO role)") |
| #1109 | pmo-portfolio-manager | **`operations/skills/pmo-portfolio-manager/`** [ASSUMPTION – CONFIRM] | **FLAG (D2)** — body AC shows `pmo-portfolio-manager/SKILL.md` without a clean module prefix; the role is operations-tier (composes two `operations/` skills). Recommend `operations/` at Stage 5; placed in `release/` group in the sequence only provisionally. |

**Net module split:** 5 confirmed `release/skills/` (#450, #1116, #1117, #1118, #1119) + 6 confirmed `operations/skills/` (#1110-#1115) + 1 flagged (#1109, recommend `operations/`). This split is why the **module README roster edits** divide into two disjoint files (release vs operations) in the Contention Map — and why grouping the sequence by module minimizes array-edit thrash.

---

*Plan authored by the Stage 4 Release Planning spoke (sub-task #1348). The hub adversarially verifies against live state and presents to the operator. Per-issue closure actions occur at Stage 13 — each build issue is marked as closed at Stage 13 via the release PR / chore PR per the Implementation Sequence.*
