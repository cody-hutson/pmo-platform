---
version: v1.10
date: 2026-06-10
type: plan
issues: ["#6", "#138", "#139", "#140", "#141"]
pr: null
links:
  note: null
  log_anchor: "#v1-10-failure-mode-coverage-completion-across-all-five-categories"
reversibility-tier: MODERATE
themes: ["epic:skill-suite", "cluster:eval-quality"]
---

# v1.10 Release Plan — failure-mode-coverage-completion-across-all-five-categories

**Milestone:** `failure-mode-coverage-completion-across-all-five-categories`
**Release identifier:** `v1.10` (D-VersionIdentifier, operator-modified 2026-06-09 from the spoke-recommended v1.09 — v1.09 was claimed by another actively-running release at decision time). Branch `release/v1.10-failure-mode-coverage`; plan file `release/releases/plans/v1.10_RELEASE_PLAN.md`; signed-annotated tag `v1.10` at Stage 12; every modified deployed skill bumps `version: v1.10` (canary: `v1.10-canary`; eval-writer / pmo-qa-auditor / prompt-builder ADD the missing field).
**Release Class:** `novel` (D-ReleaseClass, operator-rendered 2026-06-09). Basis: novel trigger (b) — the plan renders 5 release-specific D-class decisions — plus a catalog-wide fresh-authoring effort with every entry LLM-quality-gated at Stage 8 G7-06; multi-trigger resolution selects the higher-ceremony class.
**Branch Topology:** `D-C SINGLE` (D-C = B, operator-rendered 2026-06-09) — one release branch `release/v1.10-failure-mode-coverage`; per-issue sequential Engineering commits in the Implementation Sequence order (OUT → INPUT → PROC → TRIG → HAND), one Engineering chip at a time; one release PR. This plan file commits as Engineering Commit 0. Spokes re-read each skill's full failure-mode section before authoring (same-skill duplicate check).
**Differentiation posture (per `novel`):** Engagement density Standard; Stage 9 review depth Deep (Collective Review N-way consistency + cross-D upstream-compatibility scan + blast-radius); Stage 5 activation bias ALL; Stage 13 outcome-window 30-day.

**Stage 4 plan content of record:** the Stage 4 Release Planning comment on sub-task #581 (operator-reviewed at the Stage 4 Decision Briefing; plan approved 2026-06-09). This file transcribes that comment verbatim as Engineering Commit 0, followed by the two decision records rendered on the same sub-task: the Stage 4 Plan Review decision record (all 7 D-gates) and the Collective Review post-Solutioning scope-lock decision record (2026-06-10). Where the verbatim Stage 4 transcript states a spoke recommendation that the operator subsequently modified — the v1.09 identifier and its derived branch / plan-file paths — the rendered value in the Decision Records below (**v1.10** and its derived paths) is authoritative.

---

## Stage 4 Release Planning — failure-mode-coverage-completion-across-all-five-categories

**Baseline pin:** live tree at `3f91d05` (merge of PR #576), re-derived 2026-06-09. All gap lists, counts, and percentages below are derived from this baseline via the stated grep commands, per the derivation-logic pattern. Re-check the greps at Stage 6 entry if main advances first.

---

### Phase A0 — Triage→Design Re-Review (per triage-design-rereview.md §1)

**Header:** issues `#6 / #138 / #139 / #140 / #141` · release_milestone: milestone #213 `failure-mode-coverage-completion-across-all-five-categories` (version identifier pending D-VersionIdentifier below) · stage: 4 · spoke_author: Stage 4 Release Planning spoke (hub-spawned) · re_review_date: 2026-06-09 · issue_body_revision: all five bodies `2026-06-08T23:53Z` (`gh issue view --json updatedAt`) · triage_decision_date: [CONTEXT] not directly recorded; all five carry `status: bundled` and were milestone-bound at the 2026-06-07 roadmap consolidation (milestone created `2026-06-07T04:40:33Z`) · effort_tier: **complex** (33 fresh-authored entries across 23 files, 7 D-gates).

| Requirement | D1 finding (best practices) | D2 finding (platform-localized) | D3 finding (learnings) | Class | Delta / PT |
|---|---|---|---|---|---|
| All 5 issues: AC denominators "22 skills" + audit-era % targets (54.5/63.6/77.3/77.3/81.8 → 100) | failure-mode-standard.md §Scope applies to *every* skill — the AC must track the live catalog, not the audit-era roster | live tree has **23** skill directories (`ls -d {core,operations,release}/skills/*/ \| wc -l` = 23 at `3f91d05`); deploy.sh module arrays are the roster source of truth per core/rules/skill-deployment.md §Tracked Skills | v1.08 RELEASE_INDEX row + intake-elicitation-skill release added `intake-desk` post-audit — roster moved after the audit snapshot | **C2** | Tier 1 [ADJUST]: re-state every per-issue coverage AC against denominator 23 with the grep cited as method, not a hardcoded sum. Live baselines: TRIG 12/23 (52.2%), INPUT 18/23 (78.3%), PROC 18/23 (78.3%), OUT 19/23 (82.6%), HAND 15/23 (65.2%). |
| #6 Affected Files: 5 skills incl. `release-executor` | verify-before-recommend: gap claims re-verified against canonical source before planning | `grep -cE '^### .+ — OUT *$' release/skills/release-executor/SKILL.md` = 1 (line 654, "Mode E Section 6a banned-jargon leakage from operator-grade inputs — OUT") — release-executor **has** an OUT entry | post-audit releases materially edited release-executor (its `version:` is v3.20) | **C2** | Tier 1 [ADJUST]: drop release-executor from #6 scope with the grep evidence; #6 covers **4** skills. |
| #141 Affected Files: 10 skills / #140 Affected Files: 4 skills | same | live grep: **11** skills lack TRIG and **5** lack INPUT — `intake-desk` (shipped post-audit) lacks both (its section carries 6 entries, categories HAND/OUT/PROC only) | intake-elicitation-skill release (2026-06-06) postdates the 2026-05-02 audit | **C2** | Tier 1 [ADJUST] conditional on D-IntakeDeskScope: add intake-desk to #141 + #140 scope (recommended) or file a follow-up issue. |
| #139 Affected Files (5) / #138 Affected Files (8) | same | live grep matches both issue lists exactly | — | **C1** | (verbatim) |
| All 5 issues, Proposed Change: "audit recommendation files propose entries; Stage 5 expands them" | review-discipline: stale evidence is not load-bearing until re-verified | `git cat-file -t bc4cfb8e8a173816ff85e341e3c9d53039c3a1d1` → fatal (exit 128) at `3f91d05`; no local copy of the audit folder exists in the tree | clean-seed re-versioning (2026-06) removed pre-rewrite objects; the one-line Evidence summaries quoted in the issue bodies are the only surviving seeds | **C2** | Delta: every entry is **fresh authoring** grounded in the live skill's operational surface; 12 surviving one-liner seeds cover 8 of 33 skill×category cells (OUT 3, HAND 4, PROC 4-covering-2-skills, INPUT 1, TRIG 0). #139's "PROC first — minimum fresh-authoring" sequencing rationale is void; sequence re-derived below. |
| #139 footer: "Blocked by: #106 — must ship first" (native link) | decision-discipline §2.1 Mechanism 1 — localize the link against current state before honoring it | #106's own body disclaimer states its `#139` references **predate the 2026-06 re-versioning** (old-#139 = a Check 16 organic-emissions subset, remediated in the 2026-05-15 triage run per #106's body); #106's subject (sub-task `status:` labels / Check 16 / label-taxonomy Rule 6) has no read/write interaction with PROC content authoring in 5 SKILL.md files; #139's own Dependencies section says "None blocking" | known backlog-wide pre-renumber link rot from the consolidation that mirrored body references as native links | **C3 / PT-1 (stale assumption)** | Tier 0 decision surface = **D-106Disposition** gate below; #139 holds for that operator decision (not for #106's shipping). Recommended resolution: WAIVE + stamp `status:` labels on this release's own sub-tasks (the only real, narrow interaction). |
| Milestone #213 description: "~18 pts across 6 issues"; no `## Release Class` H2; no `### Release Outcome Statement` H3; no `## Parallelization Map` | release-class-taxonomy.md §Classification Procedure + release-outcome-statement-template.md §2 + stage-04-planning.md Parallelization-Map currency check | milestone holds **5** issues (+1 pipeline sub-task #581, which does not count); the named sixth outcome maps to #142 (OPEN, no milestone, `type:observation`, `status: proposed`) | milestone created 2026-06-07 by roadmap consolidation, **after** the v1.03 Parallelization-Map cutover (2026-06-02) and after the Class/Outcome conventions — post-cutover milestone missing all three sections | **C2** | Tier 1 [ADJUST]: one Phase B3 description PATCH adds Outcome Statement H3 (top), Release Class H2, Parallelization Map H2, and corrects the issue count per **D-142Scope** below. |

**G-PL1 (AC/substrate currency):** FAIL pre-reconciliation on the denominator/percentage ACs and #6's file list — all routed Tier 1 [ADJUST] above; no AC premise is invalidated (no Tier 2 scope change beyond the D-gated intake-desk question). **G-PL2 (crisping):** all five bodies PASS G1-02/G1-04/G1-05 — descriptions actionable, Proposed Change names files, ACs verifiable by stated method; plan-ready once the currency deltas land.

---

### Summary (30 seconds)

Five per-category issues (#141 TRIG, #140 INPUT, #139 PROC, #6 OUT, #138 HAND) put ≥1 domain-specific failure-mode entry in every missing category of every skill. Re-baselined at `3f91d05`: **33 new entries across all 23 SKILL.md files** (every skill in the catalog is missing ≥1 category) — TRIG 11, INPUT 5, PROC 5, OUT 4 (release-executor drops out), HAND 8; intake-desk joins TRIG + INPUT pending D-gate. The audit's per-entry recommendation drafts are unreachable (unfetchable commit) — this is a **fresh-authoring release** seeded by 12 surviving one-liners. No hard inter-issue dependencies; heavy file contention (8 skills appear in 2-3 issues) drives the **D-C SINGLE branch, one-Engineering-chip-at-a-time** recommendation, sequenced OUT → INPUT → PROC → TRIG → HAND (small-to-large for authoring calibration; HAND last per #138's own note and its cross-skill authoring weight). The #139→#106 blocked-by link is renumber rot — recommend WAIVE. Proposed identifier **v1.09**; proposed class **novel** (Deep Stage 9, Stage 5 ALL). Seven D-gates need operator decisions before scaffolding.

### Dependency Graph

**In-bundle (5 issues):** no hard content dependencies — each issue adds entries in a distinct category and can be authored independently. Two soft edges:
- `#141 → #138` (soft ordering): #138's Dependencies section asks to sequence HAND after TRIG. Honored in the sequence below.
- `#139 ⇢ {#6, #138}` (verification edge, not a build edge): #139's count≥5 ACs for pmo-process-designer and pmo-technical-analyst are satisfied by the *sum* of #139's PROC entries with #6's OUT entry (pmo-process-designer) and #138's HAND entry (pmo-technical-analyst). Minimum-entry math: pmo-process-designer 3+1(PROC)+1(OUT)=5; pmo-technical-analyst 3+1(PROC)+1(HAND)=5. Verify the AC release-scoped after all three land, regardless of intra-release order.

**Cross-milestone:**
- `#106 → #139` (native blocked-by, OPEN in milestone `deploy-toolchain-defect-cleanup`): assessed as a renumber-rot artifact, not a genuine gate — see Phase A0 row (C3/PT-1) and **D-106Disposition**.
- `#6 → #288` (blocks; #288 OPEN in milestone `knowledge-management-discipline`): downstream-only. No effect on intra-release order; merging this release unblocks #288. Note: under single-branch topology #288 unblocks at release merge, not at per-issue completion.
- `#142` (OPEN, no milestone): named by the milestone value statement but not bundled — see **D-142Scope**.

### Implementation Sequence

Single release branch, one Engineering chip at a time (per D-C recommendation). Order:

| Seq | Issue | Category | Skills (per re-baseline) | New entries | Rationale for position |
|---|---|---|---|---|---|
| 1 | #6 | OUT | pmo-process-designer, project-initiator, tracker-manager, pmo-skill-refiner-selftest-canary | 4 | Smallest bundle post-rebaseline; 3 of 4 cells seeded; settles the **canary ADR-04 boundary** (D-CanaryScope) at the first touch, which #138 re-applies later |
| 2 | #140 | INPUT | artifact-generator, delivery-engine, pmo-qa-auditor, weekly-status-rollup, (+intake-desk per D-gate) | 4-5 | Small bundle; establishes the cross-category-duplication discipline (#140's named risk) early |
| 3 | #139 | PROC | daily-status, file-router, pmo-process-designer, pmo-technical-analyst, ppm-agent | 5 (option +2 if the two high-complexity skills take double entries per Stage 5 judgment) | Mid-size; both count≥5 expansions in flight by mid-release |
| 4 | #141 | TRIG | build-reviewer, comms-writer, daily-status, eval-writer, implementation-planner, pmo-skill-editor, pmo-skill-refiner, release-executor, tracker-manager, weekly-status-rollup, (+intake-desk per D-gate) | 10-11 | Largest bundle, zero surviving seeds — runs after the authoring pattern is G7-calibrated by seqs 1-3 |
| 5 | #138 | HAND | change-management, comms-writer, daily-status, pmo-skill-refiner-selftest-canary, pmo-technical-analyst, prompt-builder, release-planner, weekly-status-rollup | 8 | Honors #138's "after TRIG" note; HAND entries cite consumer-skill input contracts (heaviest per-entry lift) — maximum calibration + 4 of its 8 skills already touched earlier in-release |

Capacity assessment: 33 (minimum) to 35 entries, uniform 5-field template, additive-only — within a single release for a single operator. The cost center is not edit mechanics but **authoring + review quality** (every entry must clear G7-06 LLM-graded domain-specificity, mean ≥4.0); the sequence above front-loads calibration on small bundles. Not over-scoped; flagged MEDIUM for Stage 9 review surface (~33 entries ≈ 600-900 added lines).

### Stage Applicability Matrix

| Issue | S5 Solutioning | S6 Engineering | S7 Dev Testing | S8 QA | S9-S13 |
|---|---|---|---|---|---|
| #6 OUT | **YES** | YES | YES | YES | release-scoped, once |
| #140 INPUT | **YES** | YES | YES | YES | ″ |
| #139 PROC | **YES** | YES | YES | YES | ″ |
| #141 TRIG | **YES** | YES | YES | YES | ″ |
| #138 HAND | **YES** | YES | YES | YES | ″ |

No stage skips. Stage 5 is **not** skippable for any issue: the audit recommendation drafts the issues assumed ("Stage 5 expands them into full 5-field templates") are unreachable, so Stage 5 is where each skill's entry is *designed* against its live operational surface — per-issue Solutioning produces the per-skill entry specs (name, category-correct classification per failure-mode-standard.md §How to Choose, Conditional in G7-05 grammar, fixture-scoping for the canary). Stage 7 verifies structure (G7-03/G7-04/G7-05 regex + per-file grep counts + no-deletion diff scan); Stage 8 runs pmo-qa-auditor Mode A G7 incl. G7-06 (mean ≥4.0, individual ≥3) per the ACs. This matches the `novel`-class Stage 5 ALL bias.

### Contention Map

Full skill×issue matrix from the re-baseline greps (`X` = issue adds an entry to that SKILL.md). 8 of 23 skills are multi-issue; **every skill in the catalog is touched**.

| Skill (SKILL.md path) | #6 OUT | #140 INPUT | #139 PROC | #141 TRIG | #138 HAND | Issues |
|---|---|---|---|---|---|---|
| operations/skills/daily-status | | | X | X | X | **3** |
| operations/skills/weekly-status-rollup | | X | | X | X | **3** |
| operations/skills/comms-writer | | | | X | X | **2** |
| operations/skills/tracker-manager | X | | | X | | **2** |
| operations/skills/pmo-process-designer | X | | X | | | **2** |
| operations/skills/pmo-technical-analyst | | | X | | X | **2** |
| release/skills/pmo-skill-refiner-selftest-canary | X | | | | X | **2** |
| operations/skills/intake-desk (per D-gate) | | X | | X | | **2** |
| operations/skills/project-initiator | X | | | | | 1 |
| operations/skills/artifact-generator | | X | | | | 1 |
| operations/skills/delivery-engine | | X | | | | 1 |
| core/skills/pmo-qa-auditor | | X | | | | 1 |
| operations/skills/file-router | | | X | | | 1 |
| operations/skills/ppm-agent | | | X | | | 1 |
| core/skills/eval-writer | | | | X | | 1 |
| release/skills/build-reviewer | | | | X | | 1 |
| release/skills/implementation-planner | | | | X | | 1 |
| release/skills/pmo-skill-editor | | | | X | | 1 |
| release/skills/pmo-skill-refiner | | | | X | | 1 |
| release/skills/release-executor | | | | X | | 1 |
| operations/skills/change-management | | | | | X | 1 |
| core/skills/prompt-builder | | | | | X | 1 |
| release/skills/release-planner | | | | | X | 1 |

Contention class: every edit appends an H3 entry into the **same** `## Domain-Specific Failure Modes` section of the shared file. Across concurrent branches this is line-range overlap at the section tail (NOT benign append-pattern in the ADR-005 sense — same insertion point), which is what rules out parallel per-issue PRs. Under the recommended single-branch sequential execution, contention reduces to "later spoke must read the section as already extended by earlier spokes" — made an explicit Stage 6 step (see D-C gate). Cross-PR (external) contention at baseline `3f91d05`: no open PRs touch `{core,operations,release}/skills/*/SKILL.md` [VERIFIED 2026-06-09: open-PR population = 0 at baseline — per audit-baseline discipline this is pinned to the SHA + date and must be re-checked at Stage 6 entry and at the Stage 9 A6.5 divergence checkpoint].

### Risk Register

| # | Risk | Class | Likelihood / Impact | Mitigation | Owner |
|---|---|---|---|---|---|
| R1 | **Fresh-authoring quality at scale.** 25 of 33 cells have no surviving seed; the source audit's core finding was floor-filling generic authoring — recreating it at scale defeats the release | Scope/quality | M / H | Stage 5 per-skill design grounded in live operational surface; small→large sequence calibrates through G7 early; Stage 8 G7-06 LLM-graded gate (mean ≥4.0) is the hard backstop; #141's own anti-floor note (consider 2+ entries where surface justifies) applied as Stage 5 judgment | Stage 5/6 spokes + Stage 8 QA |
| R2 | **Audit recommendation files unreachable** (`git cat-file -t bc4cfb8e…` → fatal; no local copy). Issue Notes' effort framings ("light lift", "minimum fresh-authoring", "4 of 5 drafted") are stale | Evidence | certain / M | Plan re-baselines effort: ALL entries fresh-authored, 12 one-liner seeds covering 8 cells; Tier 1 [ADJUST] deltas recorded in Phase A0 table | hub (issue-body deltas) |
| R3 | **Contention on 8 shared SKILL.md files** (2-3 issues each) | Contention | H under parallel PRs / eliminated by D-C SINGLE sequential | D-C SINGLE + one-Engineering-chip-at-a-time (hub-spoke-bridge write-serialization); later spokes re-read extended sections; concurrent-spoke race patterns (4 known git races on shared release branches, promoted 2026-06-04 N=4) enumerated in every Stage 6 chip prompt | hub Procedure 2 |
| R4 | **Canary fixture-scope breach** (ADR-04): production-grade OUT/HAND entries on a report-only fixture | Scope | M / M | D-CanaryScope gate; entries authored to fixture surface (canary self-status OUT; `[CANARY EXPECTED]` drift-signal HAND per surviving seeds); Stage 8 boundary check: entry text references fixture role only | Stage 6 spoke + Stage 8 |
| R5 | **Edit-path compliance drift.** 21/23 skills carry `skill_discipline_migrated_v10_2: true`; direct edits bypass the pmo-skill-editor audit trail; Check 10 (warn-mode) emits on missing trailers; spokes must never self-create `.editor-session` sentinels | Process | M / M | Plan-stated convention (below): pmo-skill-editor Mode A routing for ALL SKILL.md edits (incl. the 2 unmigrated: intake-desk, canary); warn-mode direct edit is fallback-with-named-consequence, never sentinel synthesis; under enforce-mode, escalate via Hook-Blocked → User-Side Handoff | Stage 6 spokes |
| R6 | **Version-field + package staleness.** Every modified skill must bump `version:` (release-tag-at-last-material-edit) and get a `.skill` rebuild; Check 6/7 gate. Discovery: the 3 core-module skills (eval-writer, pmo-qa-auditor, prompt-builder) currently have **no `version:` field at all** | Compliance | M / M (release-blocking if missed) | All 22 deployed modified skills bump to the release tag (decisive input to D-VersionIdentifier); canary uses `-canary` sentinel; the 3 core skills get the field **added** during their material edits (mandated by version-field-semantics.md — log as deviation-record adjacency, not scope creep); rebuild all modified deployed packages (canary excluded per ADR-04); `./deploy.sh --check` pre-merge | Stage 6 + Stage 12 |
| R7 | **#106 blocked-by honored literally** would gate this release on an unrelated milestone (`deploy-toolchain-defect-cleanup`) | Dependency | resolved by D-gate | D-106Disposition: WAIVE with rot evidence; narrow real interaction (this release's own sub-task labels) mitigated by stamping `status:` labels at scaffolding | operator |
| R8 | **Stage 9 review surface** (~33 entries, 23 files, one PR under D-C SINGLE) | Review | M / M | Per-issue commits give 5 reviewable commit-level diffs inside the single PR; `novel`-class Deep review; additions-only invariant verified by diff scan (no deletions/modifications of existing entries per every issue's regression AC) | Stage 9 |
| R9 | **Milestone metadata gaps** (no Outcome Statement / Release Class / Parallelization Map; "6 issues" mismatch) leave downstream consumers (G3-11, readiness scan dim-3, G-PR7, QC4-06) blind | Governance | certain / L-M | Single Phase B3 description PATCH after the D-gates render (Outcome H3 top, Class H2, Parallelization Map H2, count corrected per D-142Scope) | hub at Phase B3 |
| R10 | **Rollback complexity** | Rollback | L / L | Repo level CHEAP: additive-only content on one branch → single `git revert` of the release PR restores all 23 files (version: fields revert with the files). Runtime level MODERATE: after a post-deploy rollback, re-run `./deploy.sh --deploy` + rebuild packages at reverted HEAD so Cowork/user-local mirrors and `packages/*.skill` match the reverted source (Check 7/12/13 verify) | Stage 12 |

### Recommendations

1. **Render the 7 D-gates below**, then PATCH the milestone description (Outcome Statement + Release Class + Parallelization Map + corrected issue count) at Phase B3, then apply the Tier 1 [ADJUST] issue-body deltas from the Phase A0 table (denominator 23 ACs; #6 drops release-executor with evidence; #141/#140 add intake-desk if D-gated IN). Only then scaffold (Procedure 1).
2. **Merge/split:** keep all 5 issues separate (per-category ACs and traceability are clean) and ship them as ONE release on one branch — the bundling benefit the issues invite (#140 "could combine with another bundle"; #138 "operator may bundle TRIG + HAND") is fully captured by single-release execution; merging issue *bodies* would only blur per-category ACs. Do not split #141 by skill-bucket: single-operator review of one bundled release beats 10 micro-releases (per #141's own risk note), and the sequence + per-issue commits keep the diff reviewable.
3. **intake-desk:** prefer scope-adjustment of #141/#140 over a new issue (duplicate-discipline: enrich the existing owner; the milestone outcome statement already promises catalog-wide coverage).
4. **Edit-path posture (plan-stated convention, not a D-gate):** all SKILL.md edits route through `pmo-skill-editor` Mode A (its session protocol owns the `.editor-session` sentinel and the material-vs-cosmetic version-bump call per version-field-semantics.md §Determination Mechanism). Runtime hook `block-skill-direct-edit.sh` + shared mode file = warn [CONTEXT: hub-verified 2026-06-09; source hook present at `core/hooks/block-skill-direct-edit.sh` and 21/23 migrated flags verified at `3f91d05`]. Under warn-mode a direct edit proceeds with a logged warning and a Check 10 warn emission (missing editor audit-trail trailer) — documented fallback, never preferred. Spokes NEVER self-create `.editor-session` to satisfy the hook; if enforce-mode lands mid-release, escalate per Hook-Blocked → User-Side Handoff.
5. **Stage 12 obligations:** rebuild `.skill` packages for every modified deployed skill (22 if intake-desk IN; canary has no package per ADR-04) via `core/deploy/tools/build-skill-packages.sh`; `./deploy.sh --deploy` post-merge; `./deploy.sh --check` clean (Checks 5/6/7/10/12/13 are the relevant surfaces).
6. **Out-of-scope discoveries** (flag, do not execute here): (a) the 3 core-module skills lack the mandated `version:` frontmatter field — handled in-release as part of their material edits per R6, but the *Check 6 scope question* (why deploy-check did not surface 3 missing fields) deserves a follow-up intake ticket; (b) #106's body (and likely other consolidation-era native links) carries pre-renumber reference rot — the WAIVE decision here should feed the known backlog-wide reconciliation effort rather than a one-off fix.
7. **Safe-phrasing note for Engineering:** when this plan is transcribed into PR bodies, per-issue closure prose must read "mark #N as closed at Stage 13" — close-family verbs + `#N` stay confined to the PR's Issue References block.

### Operator Decisions (D-Gates)

#### D-ReleaseClass: What Release Class does this release carry?
**Gate input:** Spoke-proposed class + trigger-condition evidence per release-class-taxonomy.md.
**Gate decision:** (A) routine, (B) **novel** ← recommended, (C) cross-cutting, (D) hotfix.
**Blocks:** Phase B3 milestone-description authoring; downstream per-class differentiation posture.
**Upstream compatibility:** N/A — Release Class is PMO platform internal taxonomy; no Anthropic upstream surface. Upstream compatibility check does not apply.
**Reversibility / Confidence:** CHEAP / HIGH (re-classifiable later per Re-Classification Protocol).
**Recommendation: `novel`.** Trigger evidence: novel (b) fires — this plan renders 5 release-specific D-class decisions (beyond the 2 recurring ones). Routine triggers (a: all P3, no size labels) and (c: zero new files) also fire → multi-trigger resolution selects the higher-ceremony class (`novel` > `routine`). Substantive justification beyond the mechanical trigger: 33 fresh-authored entries all facing an LLM-graded quality gate + the canary-boundary design judgment warrant Deep Stage 9 and Stage 5 ALL. Honest counter-note for the operator: the D-decisions are scope/metadata disposition (rot-driven), not protocol novelty, and the edit pattern itself is routine-shaped — operator MAY override to `routine` with documented rationale per taxonomy §Scope; the spoke recommends `novel`. Differentiation posture: Engagement density **Standard** · Stage 9 review depth **Deep** · Stage 5 activation bias **ALL** · Stage 13 outcome-window **30-day**.

#### D-C: Branch Topology
**Gate input:** Contention map above — 8 skills shared across 2-3 issues; all edits append into the same H2 section per file; lineage default D-C SINGLE; v1.07 (#526) / v1.08 (#570) single-release-PR precedent.
**Gate decision:** (A) Option-A per-issue branches + 5 PRs; (B) **SINGLE release branch, per-issue sequential commits** ← recommended; (C) SINGLE branch, per-skill grouped commits (issue-spanning).
**Blocks:** Procedure 0 §Canonical location (where this plan file lands — Engineering Commit 0 under SINGLE vs. chore PR under Option-A); Procedure 1 scaffolding; all Stage 6 routing.
**Upstream compatibility:** N/A — branch topology does not modify skill-authoring surface. Upstream compatibility check does not apply.
**Reversibility / Confidence:** CHEAP until scaffolding / HIGH.
**Recommendation: B.** Option-A is dominated: 3 PRs would race on daily-status and weekly-status-rollup SKILL.md at the same section-tail insertion point (true line-range overlap, not benign append-pattern), plus 5 review cycles and cross-PR audit overhead. C (per-skill commits) buys authoring coherence per skill but breaks issue↔commit traceability and per-issue Stage 7/8 scoping; its real benefit — avoiding cross-category duplicate entries within a skill (#140's named risk) — is recovered procedurally under B by requiring each Engineering spoke to re-read the target skill's full failure-mode section (including earlier in-release additions) before authoring, with same-skill-duplication check as an explicit Stage 6 step. Under B the plan file commits as Engineering Commit 0 on the release branch; hub routes ONE Engineering chip at a time in the Implementation Sequence order.

#### D-IntakeDeskScope: Is intake-desk (TRIG + INPUT) in scope?
**Gate input:** Live re-baseline — intake-desk (shipped post-audit, 2026-06-06) lacks TRIG and INPUT (`grep -cE '^### .+ — TRIG *$' operations/skills/intake-desk/SKILL.md` = 0; same for INPUT; section exists with 6 entries in HAND/OUT/PROC). Milestone outcome statement: "Every skill in the catalog carries ≥1 … in all five categories."
**Gate decision:** (A) **IN — Tier 1 [ADJUST] adds intake-desk to #141 and #140 scope** ← recommended; (B) OUT — file a follow-up issue; release ships 100% of the audit-era roster but 2 known gaps remain on the live catalog and the milestone outcome statement must be re-worded.
**Blocks:** #141/#140 issue-body deltas; File Change Matrix final form; AC totals.
**Upstream compatibility:** N/A — does not modify skill-authoring surface rules; the added entries conform to the existing failure-mode-standard.md body convention (upstream `anthropic-skills:skill-creator` constrains frontmatter only — `name` + `description` per upstream-reference-catalog.md `skill-md-frontmatter`, last_verified 2026-06-08 @ `f7d882a`; body sections unconstrained — no conflict).
**Reversibility / Confidence:** CHEAP (scope adjustable until Stage 6 starts) / HIGH.
**Recommendation: A.** +2 entries (~6% effort delta) keeps the shipped state equal to the promised outcome; duplicate-discipline prefers enriching the existing per-category owners over a parallel issue.

#### D-106Disposition: Does #106 gate #139?
**Gate input:** Native blocked-by link on #139 (from the 2026-06 consolidation) vs. the Phase A0 C3/PT-1 finding: #106's body references a **pre-renumber** "#139" (per #106's own disclaimer), its subject (sub-task `status:` labels / Check 16 baseline) has no interaction with PROC content authoring, and #139's original Dependencies section reads "None blocking."
**Gate decision:** (A) WAIT — hold #139 until #106 ships; (B) **WAIVE — operator removes the native link with rationale** ← recommended; (C) RESHAPE — re-point the concern at this release's own Stage 6 sub-task labeling.
**Blocks:** #139's Stage 5/6 sub-tasks (and, under the recommended single-branch topology, sequence position 3).
**Upstream compatibility:** N/A — issue-dependency disposition does not modify skill-authoring surface. Upstream compatibility check does not apply.
**Reversibility / Confidence:** CHEAP (link re-addable in seconds) / HIGH.
**Recommendation: B, plus C's mitigation regardless:** remove the link citing renumber rot, AND have the hub stamp `status:` labels on this release's sub-tasks at scaffolding (label-taxonomy Rule 6 interim compliance) so this release does not add to the Check 16 warn baseline that #106 will eventually drive to zero.

#### D-142Scope: How is the milestone's third outcome clause satisfied?
**Gate input:** Milestone description says "~18 pts across **6 issues**" while holding 5; its value statement's third clause ("the cross-phase skill-automation maturity pattern is documented as a roadmap observation") is verbatim #142's outcome. #142 is OPEN, un-milestoned, `type:observation`, `status: proposed` (NOT approved/bundled), and its own body says it is "NOT promoted to single milestone-bundled work"; its deliverable is a roadmap-instance entry — operator-local per ADR-012 (precedent: #8 premise-rejected at v1.08, "no tracked deliverable" per RELEASE_INDEX).
**Gate decision:** (A) pull #142 into the milestone; (B) **correct the description to 5 issues and re-home the third value clause as an operator-local roadmap action** ← recommended; (C) fold a roadmap-observation deliverable into an existing issue.
**Blocks:** Phase B3 milestone-description PATCH wording; release-scope finality.
**Upstream compatibility:** N/A — milestone metadata; no skill-authoring surface. Upstream compatibility check does not apply.
**Reversibility / Confidence:** CHEAP / MEDIUM (A remains coherent if the operator first routes #142 through approval; B is the only option that doesn't bundle an unapproved observation or invent a tracked deliverable ADR-012 says is operator-local).
**Recommendation: B.** C has no natural host — none of the 5 content issues is a methodology-axis documentation home, and grafting it would violate single-responsibility of the per-category ACs.

#### D-VersionIdentifier: What identifier does this release carry?
**Gate input:** Milestone title is version-less. RELEASE_INDEX lineage: v1.08 (2026-06-08) is the latest in the active v1.x line; v1.07/v1.08 both paired thematically-named milestones with sequential v1.x identifiers; version-less precedents exist (intake-elicitation-skill, domain-aware-stage5-design, public-flip-install-blockers) but created **no git tag** (live tags: v1.05-v1.08, v3.20 only).
**Gate decision:** (A) **v1.09** ← recommended; (B) version-less `failure-mode-coverage-completion-across-all-five-categories`.
**Blocks:** release branch name, plan-file path (`release/releases/plans/v1.09_RELEASE_PLAN.md`), Stage 12 tag, and the `version:` value written into every modified SKILL.md.
**Upstream compatibility:** Diverged-with-rationale (registered extension) — the `version:` frontmatter field this decision feeds is a PMO extension registered in upstream-reference-catalog.md `skill-md-frontmatter` (`pmo_extensions: version → version-field-semantics.md`), last_verified 2026-06-08 @ `f7d882a`; upstream `anthropic-skills:skill-creator` emits `name` + `description` only — no conflict.
**Reversibility / Confidence:** CHEAP pre-Engineering, MODERATE after (identifier propagates into branch, plan path, and 22 frontmatter fields) / HIGH.
**Recommendation: A — v1.09.** Decisive argument: this release materially edits every deployed skill, so every `version:` field bumps to the release tag (format `^v[0-9]+\.[0-9]+(-[a-z]+)?$` — a version-less identifier has **no conformant value**). The version-less precedent already produced semantic damage: intake-desk shipped in `intake-elicitation-skill` and carries `version: v3.19`, a borrowed adjacent tag. A version-bearing tag also keeps deploy.sh's tag-diff change-detection window correct. Canary bumps to `v1.09-canary` (sentinel permitted for the canary only). Same-epic precedent: v1.08 was also `epic:skill-suite`.

#### D-CanaryScope: How far do the canary's OUT + HAND entries go? (ADR-04 boundary)
**Gate input:** pmo-skill-refiner-selftest-canary is a report-only permanent fixture ("no recommendations, no actions" — SKILL.md lines 8/22, per ADR-04); it needs OUT (#6) and HAND (#138) entries to complete 5-category coverage. Surviving seeds are already fixture-scoped: OUT "canary self-status emitted as standalone consumer-facing artifact"; HAND "`[CANARY EXPECTED]` annotation on fixture-induced drift signal." Its existing 3 entries (INPUT/PROC/TRIG) prove fixture-scoped authoring is achievable.
**Gate decision:** (A) **author both entries fixture-scoped** ← recommended; (B) exempt the canary (coverage lands 22/23 in OUT+HAND; milestone outcome unmet); (C) author production-grade entries (breaches ADR-04 fixture role).
**Blocks:** #6 and #138 Stage 5 design for the canary; Stage 8 boundary check definition.
**Upstream compatibility:** N/A — body content per the existing failure-mode-standard.md convention; no change to skill-authoring surface rules (upstream frontmatter contract unaffected per `skill-md-frontmatter` catalog entry, last_verified 2026-06-08). Upstream compatibility check does not apply.
**Reversibility / Confidence:** CHEAP / HIGH.
**Recommendation: A.** Stage 8 verifies the boundary: entry text must reference the fixture's own surface (self-status silence, drift-signal annotation), not production review behaviors.

### Release Class Declaration

Proposed for the milestone-description `## Release Class` H2 (pending D-ReleaseClass):

```
## Release Class

Class: novel
Rationale (max 2 sentences): Novel trigger (b) fires — the release plan renders 5 release-specific D-class decisions — and the release is a catalog-wide fresh-authoring effort (33 entries, all LLM-quality-gated) rather than a pattern-replay; routine triggers (a)/(c) also fire and multi-trigger resolution selects the higher-ceremony class.
Differentiation posture:
  - Engagement density: Standard
  - Stage 9 review depth: Deep
  - Stage 5 activation bias: ALL
  - Stage 13 outcome-window: 30-day
```

`domain_practice: { source: N/A — pipeline-internal release, date: 2026-06-09, domain: governance }` — A3 classification rationale: the File Change Matrix is 100% internal platform artifacts (23 SKILL.md governance-content files + the release-plan file); the platform encodes this domain's practice directly (core/specs/failure-mode-standard.md is the content standard; core/standards/domain-best-practices/governance.md is the domain guide). Sourcing-exempt, domain-classified.

### Release Outcome Statement (draft)

For the Phase B1 briefing and the top of the milestone description (novel-shape, per release-outcome-statement-template.md; wording assumes D-IntakeDeskScope = IN and D-142Scope = B):

```
### Release Outcome Statement

**AFTER** — Every skill in the live catalog (23 directories under {core,operations,release}/skills/) carries ≥1 domain-specific failure-mode entry in each of the five categories (TRIG/INPUT/PROC/OUT/HAND) per core/specs/failure-mode-standard.md, taking per-category coverage to 100% from 52.2/78.3/78.3/82.6/65.2 percent respectively. All six high-complexity skills sit at total count ≥5, and the canary's two new entries stay inside the ADR-04 fixture boundary. Every modified skill carries a bumped version: field and a fresh .skill package.

**BEFORE** — Coverage is uneven: 11 skills lack TRIG, 8 lack HAND, 5 lack PROC, 5 lack INPUT, and 4 lack OUT (live-tree grep at 3f91d05, 2026-06-09); every one of the 23 skills is missing at least one category, and the boundary categories (TRIG + HAND) are the platform's biggest authoring blind spot.

**Actor(s):** Stage 5/6 spokes via pmo-skill-editor Mode A; pmo-qa-auditor Mode A G7 at Stage 8; operator at Stage 9 Deep review.

**Success Indicator:** `for CAT in TRIG INPUT PROC OUT HAND; do grep -LE "^### .+ — $CAT *$" {core,operations,release}/skills/*/SKILL.md; done` returns empty post-merge, and pmo-qa-auditor Mode A G7 passes (G7-06 mean ≥4.0) on every modified skill.
```

### Version-Identifier Recommendation

**v1.09** — see D-VersionIdentifier above. Branch `release/v1.09-failure-mode-coverage`; plan file `release/releases/plans/v1.09_RELEASE_PLAN.md` committed as Engineering Commit 0 under D-C SINGLE; signed-annotated tag `v1.09` at Stage 12; all modified deployed skills bump `version: v1.09` (canary: `v1.09-canary`; the 3 core skills *add* the missing field).

### File Change Matrix

One path per line (machine-readable per the Stage 4 baseline-pin awareness clause). All entries are EDIT (append ≥1 H3 entry to `## Domain-Specific Failure Modes`; plus `version:` frontmatter bump — ADD of that field for the 3 core skills) except the final line (ADD). intake-desk lines apply if D-IntakeDeskScope = IN. Package rebuilds (`packages/*.skill`) and the Stage 12/13 corpus files (RELEASE_LOG row, RELEASE_INDEX row, CHANGELOG, release notes) follow at their owning stages and are not change-spec lines here.

```
core/skills/eval-writer/SKILL.md
core/skills/pmo-qa-auditor/SKILL.md
core/skills/prompt-builder/SKILL.md
operations/skills/artifact-generator/SKILL.md
operations/skills/change-management/SKILL.md
operations/skills/comms-writer/SKILL.md
operations/skills/daily-status/SKILL.md
operations/skills/delivery-engine/SKILL.md
operations/skills/file-router/SKILL.md
operations/skills/intake-desk/SKILL.md
operations/skills/pmo-process-designer/SKILL.md
operations/skills/pmo-technical-analyst/SKILL.md
operations/skills/ppm-agent/SKILL.md
operations/skills/project-initiator/SKILL.md
operations/skills/tracker-manager/SKILL.md
operations/skills/weekly-status-rollup/SKILL.md
release/skills/build-reviewer/SKILL.md
release/skills/implementation-planner/SKILL.md
release/skills/pmo-skill-editor/SKILL.md
release/skills/pmo-skill-refiner-selftest-canary/SKILL.md
release/skills/pmo-skill-refiner/SKILL.md
release/skills/release-executor/SKILL.md
release/skills/release-planner/SKILL.md
release/releases/plans/v1.09_RELEASE_PLAN.md
```

---

*Model provenance: invocation model parameter `fable` (passed explicitly by hub); agent-definition default: N/A — no pmo-planning agent definition deployed in this environment; runtime-reported model: Fable 5 (`claude-fable-5[1m]`).*

---

## Decision Record — Stage 4 Plan Review (Phase B1) — 2026-06-09

**Decision:** Release plan APPROVED with one operator modification. All 7 D-gates rendered by operator in main-thread chat (hub session, 2026-06-09).

| D-Gate | Operator decision | Note |
|---|---|---|
| D-VersionIdentifier | **v1.10** (operator-modified from spoke-recommended v1.09) | v1.09 is claimed by another actively-running release (`memory-to-corpus-codification`, branch `release/memory-to-corpus-codification`). Branch: `release/v1.10-failure-mode-coverage`; plan file `release/releases/plans/v1.10_RELEASE_PLAN.md`; tag `v1.10`; skill `version:` bumps to `v1.10` (canary `v1.10-canary`). |
| D-ReleaseClass | **novel** (as recommended) | Deep Stage 9 review · Stage 5 ALL · Standard engagement · 30-day outcome window. |
| D-C Branch Topology | **B — SINGLE release branch, per-issue sequential commits** (as recommended) | Plan file commits as Engineering Commit 0; one Engineering chip at a time per Implementation Sequence; spokes re-read each skill's full failure-mode section before authoring (same-skill duplicate check). |
| D-IntakeDeskScope | **A — IN** (as recommended) | intake-desk added to #141 (TRIG) + #140 (INPUT) scope via Tier 1 [ADJUST]. |
| D-106Disposition | **B — WAIVE + C's mitigation** (as recommended) | Native blocked-by link #106→#139 removed citing renumber rot (#106's own body: its `#139` refs predate the 2026-06 re-versioning; old-#139 subset resolved 2026-05-15). Hub stamps `status:` labels on this release's sub-tasks at scaffolding (label-taxonomy Rule 6 interim compliance). |
| D-142Scope | **B — correct description to 5 issues** (as recommended) | Third value clause re-homed as an operator-local roadmap action per ADR-012; #142 stays un-bundled (`status: proposed` observation). |
| D-CanaryScope | **A — fixture-scoped both entries** (as recommended) | Canary OUT + HAND entries authored to the fixture surface; Stage 8 verifies the ADR-04 boundary. |

**Hub empirical verification (per R3):** all load-bearing spoke claims independently verified-matches before briefing — re-baselined gap lists (hub's pre-launch grep = spoke's grep, exact match); release-executor OUT entry at `release/skills/release-executor/SKILL.md:654`; `git cat-file -t bc4cfb8e…` fatal (audit drafts unreachable); #106 body pre-renumber disclaimer + old-#139 remediation text; intake-desk 6 entries (PROC/OUT/HAND only) + borrowed `version: v3.19`; eval-writer / pmo-qa-auditor / prompt-builder carry no `version:` field; #142 labels `status: proposed` + `type:observation`, no milestone; tags v1.05–v1.08 + v3.20 only; open-PR population = 0 at baseline `3f91d05` (pinned 2026-06-09; re-check at Stage 6 entry + Stage 9 A6.5).

**Concurrency note (operator-surfaced at gate):** a concurrent release is in flight (`memory-to-corpus-codification`, carries v1.09; branch at `3f91d05` = no divergence yet at recording). Mid-pipeline divergence risk is LIVE — Stage 6 chips re-run the baseline greps + divergence check at entry; Stage 7/8 entry checks Tier 1 informational; Stage 9 Phase A6.5 HALT-eligible per G-PR8.

**Deviation note (cosmetic):** the Stage 4 spoke omitted the one-line model-provenance note from its output comment. Hub records authoritatively: invocation model parameter `fable` (passed explicitly at the Agent call site); agent-definition default N/A (no `pmo-planning` agent definition deployed in this environment — spoke ran as `general-purpose` with the Stage 4 persona card embedded per the bridge template); parent-session model `claude-fable-5[1m]`.

**Routing:** hub proceeds under this approval to (1) D-106 link removal, (2) Phase B3 milestone-description PATCH, (3) Tier 1 [ADJUST] issue-body deltas on #6/#138/#139/#140/#141, (4) Procedure 1 scaffolding with `status: bundled` stamped on sub-tasks. Stage 5 routing follows scaffolding per Procedure 2.


---

## Decision Record — Collective Review (Post-Solutioning Scope-Lock) — 2026-06-10

**Decision:** Scope-lock **APPROVED** by operator in main-thread chat. Engineering authorized per the Implementation Sequence (OUT → INPUT → PROC → TRIG → HAND, one chip at a time, D-C SINGLE on `release/v1.10-failure-mode-coverage`). Scope is hard-locked through Stage 9.

| Decision | Operator verdict | Binding effect |
|---|---|---|
| Scope-lock | **Approve full 44-entry set** (35 PRIMARY + 9 TRIG SECONDARY) | All five Stage 5 specs adopted as the Stage 6 contracts (#605/#606/#607/#608/#609 output comments) |
| Check 6 D-Refs disposition | **references/ adjacency, BOTH files** | Seq-1 chip (#610): create `operations/skills/project-initiator/references/` with one small single-sourced doc (pre-existing over-threshold — hub finding, 659 ln/34 KB today). Seq-5 chip (#614): create `operations/skills/weekly-status-rollup/references/` with the roll-up input-coverage checklist (spoke finding F1 — crosses at seq 5). Same authorized-adjacency shape as the plan R6 version-field ADDs. Lean-squeeze and exemption-listing REJECTED. |
| #632 CD-2 (comms-writer send-boundary SECONDARY HAND entry) | **INCLUDE** | Seq-5 chip (#614) adds the entry per the reviewer's sketch — release total becomes **45 entries** |

**Protocol check results (hub-verified):** Cross-issue deps satisfied · zero design conflicts on the 8 shared skills (bidirectional sibling verification) · N-way consistency ALL ALIGNED (no disagreement rows) · cross-D upstream-compat scan clean (no `**CONFLICT.**`; catalog `skill-md-frontmatter` last_verified 2026-06-08, <90 days) · R1 Evidence-Grounding present ×5 · adversarial outputs present ×5 (verdicts: OUT Major-conditional-go / INPUT Major-discharged / PROC Minor / TRIG Minor / HAND Major-procedural; **zero Blockers**).

**Adversarial-delta routing (Minor/Cosmetic — apply at Stage 6 as text deltas, no redesign; this list is the deviation log seed):**
- Seq 1 (#610 / #6 OUT): canary SKILL.md:117 count-free reconciliation as DECLARED scoped prose change; #628 FM-1 three word-level precision fixes; `:960` routing = explicit-carry branch; Evidence-Grounding survey-count record fix (26 → 25).
- Seq 2 (#611 / #140 INPUT): #629 FM-2/FM-4 + cosmetic (a) spec-text deltas; restate F-E with the full 11-occurrence grep; weekly-status-rollup mitigation convergence → HARMONIZE (default) at seq 2/seq 5.
- Seq 3 (#612 / #139 PROC): #630 one-line category note on 4b naming G7-06 as arbiter; re-pin baseline at current origin/main.
- Seq 4 (#613 / #141 TRIG): #631 three one-clause fixes (8.1 cross-ref to defuse same-file mitigation contradiction; 3.2 resolvable CLOSED-prong referent; 7.2 TRIG-vs-HAND category note).
- Seq 5 (#614 / #138 HAND): #632 FM-2/FM-3/FM-4/FM-5 wording deltas; D-Refs disposition bound into row 8 (w-s-r); CD-2 send-boundary entry INCLUDED per operator.

**Follow-up intake tickets (file at Stage 13 close; do NOT execute in-release):**
1. Check-6-red-on-main investigation — why the always-enforce gate did not surface the 3 missing `version:` fields (eval-writer / pmo-qa-auditor / prompt-builder) + project-initiator's D-Refs breach before this release's sweep (the release FIXES all 4 instances in-flight; the ticket covers the detection gap).
2. #631 FM-1 — pmo-skill-editor Dry-Run-Record PROC entry pre-existing git-native mis-route.
3. #631 F6 — blast-radius.sh locale issue.
4. #632 PR-2/CD-3 — Skill 12 reconciliation (one ticket).
5. #630 R3 — file-router F2 stale-path drift.

**Concurrency note:** main moved `3f91d05` → `eecbb5e` during Stage 5; concurrent v1.09 PR #604 MERGED touching only its own plan file — zero overlap with this release's 23-file matrix (hub-verified via `gh pr view 604 --json files`). Stage 6 chips re-pin + divergence-check at entry.

**Routing:** seq-1 Engineering chip (#610, issue #6 OUT) launches now — creates `release/v1.10-failure-mode-coverage`, commits the plan file as Engineering Commit 0, implements the 4 OUT entries + project-initiator references/ adjacency. Stage 7 DT chips fire per-issue as commits land (parallel-safe); Engineering stays write-serialized.


---

### Issue References

- References #6 (OUT), #140 (INPUT), #139 (PROC), #141 (TRIG), #138 (HAND) — the five per-category milestone work items this release ships; each stays open through the release and is marked closed at Stage 13.
- References #581 — the Stage 4 Release Planning sub-task whose comments (plan + the two decision records above) this file transcribes.
- References #605, #606, #607, #608, #609 — the five Stage 5 Solutioning sub-tasks whose output comments are the adopted Stage 6 implementation contracts (per the Collective Review scope-lock).
- References #628, #629, #630, #631, #632 — the five Stage 5 adversarial design reviews whose Minor/Cosmetic deltas the Collective Review routed to Stage 6 as text deltas (deviation-log seed above).
- References #610, #611, #612, #613, #614 — the five Stage 6 Engineering sub-tasks, one per sequence position.
- References #106 (waived blocked-by link on #139, renumber rot), #142 (un-bundled observation per D-142Scope), #288 (downstream issue unblocked at release merge).

---

## Change Description

### Outcome

v1.10 completes domain-specific failure-mode coverage across the entire skill catalog: every one of the 23 skills now carries ≥1 entry in each of the five categories (TRIG / INPUT / PROC / OUT / HAND), up from a state where all 23 were missing at least one category and the boundary categories (TRIG 52.2%, HAND 65.2%) were the platform's biggest authoring blind spot. The release adds **45 entries** (35 PRIMARY + 9 TRIG SECONDARY + 1 operator-included send-boundary SECONDARY) across all 23 SKILL.md files — each grounded in the skill's live operational surface, citing its specific cross-skill handoff mechanism by name, and structurally conformant to the G7-03/04/05 gates. The release-level success indicator (`grep -LE` per category across `{core,operations,release}/skills/*/SKILL.md`) returns empty on the release branch.

### Issues resolved

| # | Outcome (one line) | Status |
|---|---|---|
| #6 | OUT-category gap closed — 4 entries (pmo-process-designer, project-initiator, tracker-manager, canary fixture-scoped per D-CanaryScope); project-initiator `references/` adjacency created (pre-existing D-Refs breach fixed in-flight) | DONE |
| #140 | INPUT-category gap closed — 5 entries incl. intake-desk (added to scope per D-IntakeDeskScope) | DONE |
| #139 | PROC-category gap closed — 7 entries (5 skills + second entries on the two high-complexity skills per the Stage 5 anti-floor judgment); the #106 blocked-by link was waived as renumber rot per D-106Disposition | DONE |
| #141 | TRIG-category gap closed — 20 entries (11 PRIMARY + 9 SECONDARY per the approved anti-floor set) incl. intake-desk | DONE |
| #138 | HAND-category gap closed — 9 entries (8 PRIMARY + the CD-2 comms-writer send-boundary SECONDARY the operator included at Collective Review); weekly-status-rollup `references/` adjacency created (roll-up input-coverage checklist; Check 6 conformant over-threshold state) | DONE |

All five close at PR merge via the Issue References block (the PR fully resolves each).

### Key decisions

- **D-VersionIdentifier: v1.10** (operator-modified from spoke-recommended v1.09 — v1.09 was claimed by a concurrent release). See § Decision Record — Stage 4 Plan Review.
- **D-ReleaseClass: novel** — Deep Stage 9, Stage 5 ALL, 30-day outcome window.
- **D-C: SINGLE release branch**, per-issue sequential commits (OUT → INPUT → PROC → TRIG → HAND), one Engineering chip at a time; this plan file is Engineering Commit 0.
- **D-IntakeDeskScope: IN** — intake-desk joined #141 + #140 scope (+2 entries; catalog-wide outcome kept true).
- **D-106Disposition: WAIVE** — blocked-by link removed citing renumber rot; `status:` labels stamped on this release's sub-tasks as the narrow real mitigation.
- **D-142Scope: B** — milestone description corrected to 5 issues; roadmap-observation clause re-homed operator-local per ADR-012.
- **D-CanaryScope: fixture-scoped** — both canary entries (OUT + HAND) reference only the fixture's own report surface; `[CANARY EXPECTED]` annotation per the surviving audit seed.
- **Collective Review dispositions:** Check 6 D-Refs = references/ adjacency on BOTH files (project-initiator at seq 1, weekly-status-rollup at seq 5; lean-squeeze and exemption-listing rejected); CD-2 send-boundary SECONDARY entry = INCLUDE (release total 45); the five adversarial reviews' Minor/Cosmetic deltas applied at Stage 6 as text deltas per the § Decision Record routing list.

### Reversibility

**CHEAP — HIGH confidence.** Single `git revert` of the release PR restores all 23 SKILL.md files, both new `references/` docs, and every `version:` field (additive-only content, no data migration); runtime level is MODERATE per plan R10 — after a post-deploy rollback, re-run `./deploy.sh --deploy` and rebuild packages at the reverted HEAD so mirrors and `packages/*.skill` match the reverted source.

### Downstream impact

- Stage 8 QA runs pmo-qa-auditor Mode A G7 (G7-06 LLM-graded, mean ≥4.0 / individual ≥3) on every modified skill — the release's hard quality backstop per every issue's AC.
- Stage 12 rebuilds `.skill` packages for all 22 modified deployed skills (canary excluded per ADR-04), runs `./deploy.sh --deploy`, and verifies `--check` clean — including the four previously-red Check 6 surfaces this release fixes in-flight (3 missing `version:` fields ADDed on eval-writer / pmo-qa-auditor / prompt-builder; project-initiator + weekly-status-rollup D-Refs).
- #288 (knowledge-management-discipline) unblocks at release merge via the #6 downstream edge.
- Five follow-up intake tickets queue for Stage 13 close per the Collective Review record (Check-6-red-on-main detection gap; pmo-skill-editor Dry-Run-Record mis-route; blast-radius.sh locale defect; canary § Skill 12 contract reconciliation; file-router stale-path drift).
- Future skill authoring starts from a 23/23 five-category baseline — the failure-mode floor (≥3 per skill) is now exceeded everywhere, and the boundary categories are no longer the catalog's blind spot.

### Cross-references

- Release plan: top of this file (`release/releases/plans/v1.10_RELEASE_PLAN.md`).
- Milestone: `failure-mode-coverage-completion-across-all-five-categories` (v1.10) on the repository milestone surface.
- User-facing release notes: `release/releases/notes/v1.10_RELEASE_NOTES.md` (authored at Stage 13 Close; does not exist yet at Engineering time).

---

### Issue References

References #6 (OUT), #140 (INPUT), #139 (PROC), #141 (TRIG), #138 (HAND) — the five per-category work items; each is fully resolved by the release PR and closes at merge via the PR's Issue References block. References #581 (Stage 4 planning sub-task — plan + decision records transcribed above). References #609 (Stage 5 HAND spec) and #632 (its adversarial review — source of the FM-2/FM-3/FM-4/FM-5 deltas and the CD-2 entry sketch applied at seq 5). References #106 (waived link), #142 (un-bundled observation), #288 (unblocks at merge).
