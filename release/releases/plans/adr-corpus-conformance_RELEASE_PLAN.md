---
title: Release Plan — adr-corpus-conformance (reconcile the ADR standard, bring the corpus to it, and make concurrent ADR authoring collision-safe)
type: release-plan
plan_type: release
status: ACTIVE
release: slug-only (ADR-092 — the concrete version binds at the Stage-12 atomic claim)
milestone: adr-corpus-conformance
release_class: cross-cutting
reversibility: CHEAP / Confidence HIGH
---
# Release Plan — `adr-corpus-conformance`

**Milestone:** `adr-corpus-conformance` (milestone 286) · hub sub-task #4576 = Stage 4 plan source · #4666 / #4670 / #4674 / #4678 / #4682 / #4686 = Wave-1 Stage 5 Solutioning sources, #4690 = the Wave-2 source · #4667 = Stage 6 Engineering sub-task for the first member (#3914), which lands this plan as Commit 0.
**Version identity:** **slug-only** per **ADR-092**. The plan file is `adr-corpus-conformance_RELEASE_PLAN.md` and the branch is `release/adr-corpus-conformance`; no `vX.Y` stem appears in the plan filename, the branch name, or this plan's own identity prose. Bump class is `minor` (recorded determination, not a gate). The concrete number binds at the **Stage-12 atomic claim** (`git mv` to `plans/v<MAJOR>/{{RELEASE_VERSION}}_RELEASE_PLAN.md`).
**Topology:** D-C **SINGLE** — one release branch, one PR, one merge; this plan lands as **Engineering Commit 0**.
**Concurrency posture:** **P0 fully-serial.** Stage-6 chips route one at a time in the approved sequence on the single branch; force-push (including `--force-with-lease`) is prohibited on the shared branch under any multi-chip activity.
**Release class:** **`cross-cutting`** (operator verdict, Stage-4 gate). Posture: engagement density **Tight** · Stage-9 review depth **Deep** · Stage-5 activation bias **ALL** · Stage-13 outcome window **30-day**.

> **Provenance.** This file transcribes the Stage-4 Release Planning output posted on hub sub-task #4576, reconciled to the **Stage-4 gate** verdicts (2026-08-03), the **consolidated Wave-1 gate** verdicts (2026-08-03), and the approved Stage-5 Solutioning designs. Where a later disposition superseded a Stage-4 assumption, the transcribed section carries the **decided** state and the **§ Deviation Log** records the delta against the Stage-4 plan of record. Authored at Engineering Commit 0 by the Stage-6 Engineering spoke for #3914 (#4667).

---

## Header

| Field | Value |
|-------|-------|
| **Version** | `{{RELEASE_VERSION}}` — slug-only pre-claim (ADR-092); bump class `minor` |
| **Date Created** | 2026-08-03 (Monday) |
| **Release Manager** | Agent-assisted (`release-hub` Mode O) |
| **Status** | Executing (Stage 6 Engineering) |
| **Branch** | `release/adr-corpus-conformance` |
| **PR** | (populated at PR creation — hub-owned, after the final slice) |
| **Milestone** | `adr-corpus-conformance` (286) |
| **Baseline** | `origin/main` @ `41d12ed8` (Commit-0 re-pin **confirms** the Stage-4 pin — zero commits of drift) |

---

## Change Description

*Authored at Stage 6 Phase C1 per RELEASE_PROTOCOL § Change Description Protocol. Operator-facing. **Refreshed by the final Engineering slice (6d) — this is that refresh.***

**Outcome.** The platform's own architecture-decision records stop contradicting the standard that governs them. Seven cards in three movements: **the standard is made self-consistent** (one requirement level for the alternatives section, stated once and cited everywhere; an immutability rule that distinguishes recording a fact from revising a decision), **the corpus is brought to it** (heading normalization, missing-section backfill, durability-anchor hygiene), and **concurrent authoring is made collision-safe** (a tooled merge-time renumber, so two releases can author an ADR at the same time without one of them stalling). After this release, "author to the ADR standard" and "bring the corpus into conformance" name the same target instead of three conflicting ones.

**The through-line.** Every card is an instance or a prevention of the same failure: *a rule stated in more than one place, where the copies have drifted apart and nothing reconciles them.* The requirement level was stated on five surfaces at four different levels. The immutability rule said one thing and the shipped lint assumed another. The release ADR index and the ADR file set disagreed. The fix in each case is the same shape — one authority, everything else cites it — and the release ships the checks that keep it that way.

**Key decisions.** Release class held at **`cross-cutting`**. Entry disposition **two-wave** (Wave 1 now; Wave 2 merge-order-gated on the sibling release PR #4568). `## Alternatives rejected` **satisfies** the alternatives requirement — the contract is decision-recall content, not the heading string — and applying that principle consistently re-sizes the authoring set. `core/ADRs/README.md` is declared a **curated thematic doc, not an index**, so index-completeness scopes to the release index only. ADR-number collision resolves by **tooled merge-time renumber**, not pre-allocation. The ADR `deciders:` carve-out is ratified **name-only** — never the GitHub handle. All four Stage-5 scope changes accepted.

**Key decisions — added at Wave 2.** The release ADR index becomes a **generated projection with every column derived**, registered under the repository's existing Derived-Surface Contract rather than a new mechanism. Deriving the *cells*, not merely the rows, is the decision that matters: the defect was four independent hand-maintained enumerations of one file set with four different maxima, and half the rows that did exist contradicted the record they linked to. A **delta-scoped structural-conformance guard** ships in warn-mode, narrowed on measurement to net-new records and lost sections so its population over the existing corpus is empty by construction. The **completeness claim is scoped to a named baseline** rather than asserted as a universal, and its residual is stated in one sentence rather than hedged.

**Reversibility.** **CHEAP** for the standard-reconciliation half (prose and one additive self-test case; no rule behavior changes and no new findings over the corpus), **CHEAP** for the index projection (the generated content is already the correct content, so reverting to plain markdown loses nothing and touches no record), and **MODERATE** for the corpus half (a wide body-edit sweep whose revert restores a non-conformant corpus) and for the numbering mechanism (later releases may adopt the tooled path). Whole-release rollback is `git revert` of the merge commit. **The durability lint's `WARN_MODE` is deliberately NOT flipped in this release** — see § Rollback; the structural guard ships warn-mode under that same unflipped switch.

**Downstream impact.** No runtime behavior changes for end users. One ADR-authoring skill package is rebuilt once, at release-cut.

Two changes a contributor will notice. **Adding an ADR to the release module now means running a command, not typing a row** — and a hand-typed row fails a check rather than drifting silently, which is the intended behaviour and is stated in the region's own text. **The durability lint now reports a structural finding** on a net-new ADR that is missing a required section, or on a change that removes one; it stays silent on a section a record was already missing, so the pre-existing gaps do not become anyone's surprise problem. Both are warn-mode.

The lint's corpus verdict is unchanged at zero. One behavioural correction inside it widens recall: a word-boundary defect had been silently suppressing live counts preceded by any word ending in `g` or `v`.

**What this release does NOT claim.** The corpus is not fully conformant to the section set afterwards, and the plan says so rather than implying otherwise. Sections whose backfill would be *authoring* a decision rather than *recording* one stay out of scope with their blocking authority named, and one superseded record can never conform because the immutability policy forbids the two edits it needs. The named residual: **an ADR merged from a sibling branch may be non-conformant and nothing in the repository will say so — the index cannot drift; the corpus can.** That residual materialized during this release, before it merged.

**Cross-references.** Stage-4 plan #4576 · Stage-5 designs #4666 / #4670 / #4674 / #4678 / #4682 / #4686 (Wave 1) and #4690 (Wave 2) · Wave-2 Engineering #4691 · milestone 286. Version identity per **ADR-092**; overlap classes per **ADR-005**; canonical-spec-edit-wins per **ADR-062**. Records authored by this release: **ADR-114** (section set + hygiene carve-out, amended at Wave 2), **ADR-115** (number binds at merge), **ADR-112** (deciders carve-out is name-only), **ADR-113** (index as derived surface + scoped conformance claim).

---

## Scope

### Summary

Seven members (one folded), **43 raw pts** at the Stage-4 pin, one branch, one PR. **Three verified HARD dependency edges**, all running out of #3914. Build posture: **SINGLE branch, P0 fully-serial**, two waves.

Capability outcome: **an ADR author has one unambiguous standard, the corpus conforms to it, and two releases can author into the sequence concurrently.**

**Bundle sizing.** Raw sum = 43 pts; `class_weight`(cross-cutting) → `effective_pts` = 56 against a 15–25 band. The override is operator-accepted on the coherence argument recorded in § Bundle-Coherence Read — two of the three capabilities are gates on each other's *correctness*, not merely adjacent. **#3713 is the recorded clean cleave point** if a split is ever forced.

### Members

| # | Issue | Type | Size | Wave | Surface |
|---|-------|------|------|------|---------|
| 1 | **#3914** reconcile the ADR standard's internal contradictions | story | M | 1 | `adr-schema.md`, `adr-authoring-guide.md`, `adr-template.md`, `check-adr-durability.py`, `.github/ISSUE_TEMPLATE/adr.yml`, +1 new ADR |
| 2 | **#3915** ADR issue-reference rule + reference block | story | M | 1 | `adr-authoring-guide.md`, `.github/workflows/repo-integrity.yml` |
| 3 | **#3708** R2-COUNT recall bound (measured pre-sweep) | story | M | 1 | `check-adr-durability.py`, `adr-authoring-guide.md` |
| 4 | **#3707** ADR `deciders:` name-vs-handle gate reconcile | story | **M** | 1 | `adr-authoring-guide.md`, `core/rules/git-workflow.md` |
| 5 | **#3916** adr-helper scaffold conforms to the reconciled standard | story | M | 1 | `core/skills/adr-helper/**`, `operations/templates/adr-template.md`, `packages/adr-helper.skill` |
| 6 | **#3713** concurrent ADR-number collision resolution | story | L | 1 | `check-adr-numbers.py`, `adr-helper`, index sites, `stage-12-execute.md`, +1 new ADR |
| 7 | **#1488** ADR corpus structural-conformance sweep (**#3383 folded in**) | story | XL | **2** | `core/ADRs/**`, `release/ADRs/**`, both ADR READMEs |

**#3383 is folded into #1488** as a named sub-deliverable of its index-lockstep criterion — it is not a parallel write. It is **marked as closed at Stage 13** with its criteria satisfied by #1488's README pass.

### Wave Structure (Stage 6 Engineering)

Single branch, serial commit order. The hub surfaces one Stage-6 chip at a time.

| Order | Member | Rationale |
|-------|--------|-----------|
| 0 | *(this plan)* | **Commit 0** — plan file lands before any member edit. Carries the version re-verify and both ADR-number derivations. |
| 1 | **#3914** | Foundation. Blocks two cards, and its immutability carve-out is what makes Wave 2's edits *legal*. Writes the § ADR template prose in the hottest file in the release while the branch is clean. |
| 2 | **#3915** | Foundation. Shares `adr-authoring-guide.md` with #3914 → serialized immediately after it, in its own reserved subsection. |
| 3 | **#3708** | **Moved forward from the Stage-4 milestone's "eval last" framing.** Its measurement must be taken against a **pre-sweep** corpus — running it after #1488 would measure a drained population and yield a vacuously clean recall bound. |
| 4 | **#3707** | Placed here so all `check-adr-durability.py` writes land contiguously under one serialization window. |
| 5 | **#3916** | Consumes #3914 + #3915. Skill + template + one package rebuild. |
| 6 | **#3713** | Independent of all six at design; serialized at engineering on the shared skill and index sites. Authors the release's second ADR. |
| — | **GATE** | **Merge-order constraint, not a stop.** Wave 2 may *build* at any time; it must not *merge* before sibling PR #4568 unless the operator accepts the residual. |
| 7 | **#1488** (+#3383) | The corpus wave. Denominator re-derived at execution; criteria expressed as glob predicates. |

Each issue is **marked as closed at Stage 13** on this plan's schedule; no issue closes at merge.

---

## Dependency Graph

Edge legend: **HARD** = downstream cannot correctly start until upstream lands · **SOFT** = ordering preference with a named cost · **CONTENTION** = no dependency; same-file write collision (drives commit order, not dependency).

```
  WAVE 1 (no corpus coupling, no sibling-PR coupling)

   #3914 ─────HARD───────────────────────────────────► #1488   (WAVE 2)
  (standard reconcile)  └──HARD──► #3916                 ▲
                                     ▲                   │
   #3915 ──────HARD───────────────────┘            FOLD-IN│
  (issue-ref rule)                                  #3383 ┘

   #3707  (gate reconcile — INDEPENDENT)
   #3708  (recall bound — MEASURE PRE-SWEEP) ──SOFT──────┘
   #3713  (numbering — INDEPENDENT of all six)
```

**Verified edges (3 HARD):**

| Edge | Evidence |
|---|---|
| `#3914 → #1488` | The conformance sweep cannot author to a self-contradictory standard; and without #3914's immutability carve-out, editing an Accepted ADR body **is a policy violation**, so the sweep is not merely mis-targeted, it is unauthorized. |
| `#3914 → #3916` | #3916's first criterion scaffolds the full required-section set *as defined by the reconciled standard*. |
| `#3915 → #3916` | #3916 requires the scaffold to model the reference block whose form #3915 defines. |

**Edges asserted by the milestone that do NOT exist:** `#1488 → #3707` (its own dependency note states either order is fine, and the population it would collide over is empty) and `#1488 → #3708` (the real edge is **inverted** — see § Implementation Sequence Finding S1).

---

## Implementation Sequence

**Entry disposition: two-wave** (operator verdict, Stage-4 gate D-A). Waves are branch-internal commit ranges on **one release branch, one PR, one merge**; "wave" is a sequencing unit, not a separate PR.

**Finding S1 — the milestone's internal sequence inverts #3708.** The milestone sequenced #3708 as "eval / verification — gated on the conformance pass." That is backwards: #3708's first criterion runs the shipped checker over the corpus with its vocabulary constraint lifted and subtracts the shipped result. #1488's sweep exists to **drain live population counts from durable ADR prose**. Running #3708 after #1488 measures a drained corpus and yields a **vacuously clean recall bound** — precisely the false-coverage reading #3708 was filed to prevent. **Resolution: #3708 sequenced into Wave 1**, measuring live pre-sweep state, with no baseline pinning required. This deviates from the `bundle-composition-doctrine.md` § 9 "eval last" layer rule; the deviation is deliberate — that rule assumes eval *observes* the shipped work, whereas here eval measures a population the shipped work destroys. **Layer-order yields to measurement validity.**

**Second-order sequencing note.** If #3708 must ever run late, its measurement is taken against the release-branch base SHA via `git show ${base}:<path>` — note the shell hazard: brace the ref as `${rev}:path`, never `"$rev:path"`, which mangles silently under zsh and produces a false MISSING.

---

## Stage Applicability Matrix

Default all of Stages 5–13. **Zero skips** — every card carries either a decision gate or a functional gate change; none is trivial. This is consistent with the `cross-cutting` Stage-5 activation bias (ALL) but is not *caused* by it; each activation is independently justified.

| Issue | S5 | S6 | S7 | S8 | S9–S13 | Rationale for S5 activation |
|---|---|---|---|---|---|---|
| **#3914** | YES | YES | YES | YES | YES | Renders the requirement-level decision **and** the heading-equivalence call that sizes the sweep. Functional: binds the durability lint's cited section set. |
| **#3915** | YES | YES | YES | YES | YES | The narrow-marker criterion is a judgment call; the identity-field lint is a functional deliverable. |
| **#3708** | YES | YES | YES | YES | YES | Widen-vocabulary vs. document-the-bound is a genuine design fork with a measured precision cost. S7/S8: self-test cases with mutation-kill controls are the deliverable. |
| **#3707** | YES | YES | YES | YES | YES | Public-surface policy call (name-only vs. name+handle) — operator-class. S7: the fixture pair (handle fires / name passes on both gates) is the test. |
| **#3916** | YES | YES | YES | YES | YES | Scaffold shape consumes two upstream reconciliations. S7: a scaffolded ADR must pass the durability lint with zero findings and without a marker. |
| **#3713** | YES | YES | YES | YES | YES | Three-way direction choice + an ADR is required. S7/S8: "two branches author concurrently and both merge" is behavioral and needs a real two-branch fixture. |
| **#1488** | YES | YES | YES | YES | YES | Per-module wave decomposition over the whole corpus + the index-generation design. S7: all required CI gates pass. |

---

## Contention Map

### Within-release

| ID | File | Written by | Severity | Resolution |
|---|---|---|---|---|
| **C1** | `core/standards/adr-authoring-guide.md` | #3914, #3915, #3708, #3707 | **HIGH** — the hottest file in the release | **Serialize in the Wave-1 order, on the binding disjoint partition below.** Four sequential writers on one file with no coordination is how a reconciliation release ships a self-contradictory guide. **CIAC-1 grades the outcome.** |
| **C2** | `release/tools/check-adr-durability.py` | #3914 (scope + section-set citation), #3708 (R2-COUNT vocabulary + self-test), #3707 (R3) | MEDIUM | Serialize; contiguous in Wave 1 by design. **Three disjoint docstring blocks** — one per writer. One `--self-test` run must pass after all three (**CIAC-3**). |
| **C3** | `core/skills/adr-helper/**` | #3916, #3713 | MEDIUM | Serialize #3916 → #3713. Skill-edit discipline runs before push; the package rebuilds **once** at release-cut, not per-card. |
| **C4** | `release/ADRs/README.md` | #1488 (+#3383 folded), #3713 | **HIGH** | The fold converts a 3-way write into 2-way. #3713's index touch is *mechanism* (the renumber tool updates indices), not content. |
| **C5** | `core/ADRs/README.md` | #1488, #3713 | MEDIUM | Same resolution as C4. Per the ratified scope call, this file is a **curated thematic doc, not an index** — completeness criteria do not target it. |
| **C6** | `operations/templates/adr-template.md` | #3914, #3916 | LOW | Serialize. #3914 sets the requirement level; #3916 realizes it in the scaffold. |
| **C7** | `packages/adr-helper.skill` + `.sha256` | #3916, #3713 | LOW (build artifact) | Rebuild **once** at release-cut. Per-card rebuilds are the known package-drift cluster. |
| **C8** | The ADR corpus files | #1488 only | — | Not contention. Recorded to make the negative explicit: no other card edits ADR bodies. |

**C1 — the binding disjoint partition.** Four Stage-5 spokes independently converged on this partition with zero coordination; it is now binding for Engineering.

| Writer | Exclusive write scope in `adr-authoring-guide.md` | MUST NOT touch |
|---|---|---|
| **#3914** | § ADR template **prose** (the body-section-set sentence) + the fenced template's alternatives **guidance comment** + a **new H3** carve-out appended inside § Supersession + immutability | the `### Durability rules` table rows; the H3 slot reserved for #3915 |
| **#3915** | a **new H3** for the issue-reference rule, placed **after** `### Durability rules` and **before** `## Worked example` — **plus** the reference-block statement | § ADR template prose; the § Supersession carve-out H3; the durability-rules table rows |
| **#3708** | the `### Durability rules` table's **"No stale anchors"** row **only**, plus the exemption paragraph immediately following the table | the other two rows; § Supersession; § ADR template |
| **#3707** | the `### Durability rules` table's **"No operator handle"** row **only** | the other two rows; § Supersession; § ADR template |

**Shared invariant binding all four writers (this is what CIAC-1 grades):** the guide's stated **required-section set appears exactly once**, in § ADR template, and every other surface *cites* it. No writer may introduce a second statement of the set or a second requirement-level claim. A guide that states the level twice is self-contradictory by construction even when both statements happen to agree on the day they are written.

**C2 — the docstring partition.** `SCOPE — WHAT THIS LINT DOES NOT CHECK` is #3914's block; `R2 EXEMPTIONS` is #3708's; `NEVER HARDCODE THE HANDLE` is #3707's. Three disjoint blocks, so CIAC-3's three coexisting statements hold.

### Cross-PR — baseline-pinned

**Open-PR population at the pin: exactly 1** (sibling release PR #4568). A single-member population is fragile — **re-check before the Stage-9 GO**; one new PR silently invalidates the contention finding.

Same-path intersection is **one conditional, append-pattern file** (`core/schemas/gate-criteria-spec.md`, only if #3713's direction lands a named gate ID) — informational, not a serialization point. **The coupling that matters is not same-path.** Four derived-value couplings, none visible to a path-intersection audit:

| # | Coupling | Effect on merge |
|---|---|---|
| **X1** | **ADR namespace.** The sibling PR claims two ADR numbers that are unmerged. **Only `origin/main` binds** — an unmerged claim does not bind the sequence. | Both of this release's ADR numbers are derived at Commit 0 against `origin/main` **only** (see § ADR Number Derivation). Binding at the `main` next-free is safe under **both** merge orders. |
| **X2** | **Corpus denominator.** The corpus grows when the sibling merges. | Wave 2's completeness criteria are **glob predicates over the live corpus**, re-derived at execution — never a frozen count. |
| **X3** | **Index completeness.** The sibling adds release ADRs and no README change, re-creating the exact defect the folded card fixes. | A **generated** index makes the recurrence structurally impossible; plus a coordination request to the sibling. |
| **X4** | **Version slot.** Both releases compute the same next-free minor. | **Expected runtime behavior, not an error.** The atomic claim resolves it: first to merge takes the slot, the other re-versions. Recorded, not mitigated. |

**Not contention — process currency.** The sibling PR edits several pipeline and skill specs that this release **reads** but does not write. If it merges mid-flight, downstream spokes read updated specs — a currency consideration for the Stage-9 re-check, not an edit collision.

---

## ADR Number Derivation (Commit 0)

**The binding anchor is `origin/main` ONLY.** Scanning other branches is for *detection*, never for *binding*. `core/ADRs/README.md` § Renumber log states the reasoning verbatim: an unmerged claim does not bind the sequence, and merging into a gap would land that gap on `main`, where the contiguity gate then fails **every subsequent PR**. Pre-reserving a higher slot is no remedy, because the checker fails a gap as readily as a duplicate. The corpus already carries the precedent — a release ADR was renumbered **down** for exactly this reason.

| Consumer | Number | Home | Derivation |
|---|---|---|---|
| **#3914** (this slice) | **ADR-114** | `core/ADRs/` — cross-cutting platform decision | `anchor(origin/main)` + 1 |
| **#3713** (Wave 1, order 6) | **ADR-115** | its slice's determination | `anchor(origin/main)` + 2, sequenced after this allocation |

Derivation command: `git ls-tree -r --name-only origin/main -- core/ADRs release/ADRs | grep -oE 'ADR-[0-9]{3}' | sort -n | tail -1`, with the contiguity gate (`release/tools/check-adr-numbers.py`) confirming the sequence is gap-free at the anchor. If the sibling PR merges first, the ratified **tooled merge-time renumber** resolves the collision — that mechanism is itself a deliverable of this release, exercised by this release.

---

## Risk Register

| ID | Risk | Class | Sev | Owner-stage | Mitigation | Reversibility · Confidence |
|---|---|---|---|---|---|---|
| **R1** | Four cards write `adr-authoring-guide.md`; a *reconciliation* release ships a self-contradictory guide | Contention | **HIGH** | S6 | The binding C1 partition + the state-it-once invariant; **CIAC-1** grades guide↔scaffold agreement at S9 | CHEAP · HIGH |
| **R2** | The sibling PR merges after Wave 2 → the release ships an index and a completeness claim that are false on arrival | Dependency (cross-release) | **HIGH** | S4/S9 | Two-wave entry; generated index makes it structurally impossible; Stage-9 re-check | MODERATE · HIGH |
| **R3** | This release authors ADRs into a namespace with in-flight claims; binding into a gap fails every subsequent PR | Dependency (cross-release) | **HIGH** | S6 | **Resolved at Commit 0** — derive against `origin/main` only, never pre-allocate; see § ADR Number Derivation | CHEAP · HIGH |
| **R4** | The folded card was sized against a two-row premise and a much larger reality | Scope | **HIGH** | S4 | Re-sized and folded; the index becomes a generated projection | CHEAP · HIGH |
| **R5** | The sweep's headline omission figure is three different jobs — mechanical renames, a judgment call, and genuine decision-recall authoring on Accepted records | Scope | **HIGH** | S5 | The heading-equivalence call is ratified and the authoring set is re-sized (§ Deviation Log Δ-resize); slice the authoring third as its own wave with its own budget | MODERATE · MEDIUM |
| **R6** | The recall bound run after the sweep is **vacuous** — the exact false-coverage reading it was filed to prevent | Sequence | **HIGH** | S4 | Finding S1 — sequenced into Wave 1, measured pre-sweep | CHEAP · HIGH |
| **R7** | Both this release and the sibling compute the same next-free version slot | Contention (version slot) | MEDIUM | S12 | Expected: the atomic claim resolves it; the loser re-versions. Recorded, **not** mitigated | CHEAP · HIGH |
| **R8** | The sweep touches the whole corpus in one PR; post-merge rollback restores a non-conformant corpus | Rollback complexity | **HIGH** | S12 | **Do not flip `WARN_MODE` in this release** (see § Rollback). Wave-structured commits give pre-merge wave-granular rollback | MODERATE · HIGH |
| **R9** | Planning against the milestone's unreproducible corpus-wide citation figure over-scopes the numbering card toward an expensive direction | Scope | MEDIUM | S5 | The decision-relevant figure is **per-ADR**, not corpus-wide; a renumber always targets the newest, least-cited record | CHEAP · HIGH |
| **R10** | Theme breach: three capabilities in one bundle, operator-accepted but unresolved | Scope | MEDIUM | S5 | The two-wave structure is the mitigation; Collective Review re-surfaces it | MODERATE · MEDIUM |
| **R11** | Open-PR population is one; a single new PR silently invalidates the contention finding | Evidence | MEDIUM | S9 | Audit-baseline discipline: baseline pinned; **re-check before GO** | CHEAP · HIGH |
| **R12** | The ADR-authoring skill package rebuilt per-card instead of once at release-cut → package drift | Contention | LOW | S12 | C7: a single rebuild at release-cut | CHEAP · HIGH |

---

## Cross-Issue Acceptance Criteria

Four release-scoped cohesion constraints, graded at Stage 9 on the merged PR.

- [ ] **CIAC-1 (#3914 × #3915 × #3916 on the authoring guide + the ADR-authoring skill):** the authoring guide states **exactly one** requirement level for the alternatives section and **exactly one** issue-reference rule, and the scaffold emits a section set **identical** to the guide's stated required set. *Method:* extract both lists; assert set **and order** equality; **control** — remove one section from the scaffold and confirm the assertion fails.
- [ ] **CIAC-2 (#3914 × #1488 × #3915 on the ADR corpus):** every corpus-scoped acceptance criterion in the merged release is expressed as a **predicate over the live ADR glob**, carrying **zero** hardcoded corpus counts. *Method:* sweep the merged plan and criterion text for three-digit numbers; each survivor must be an ADR identifier or this plan's own recorded baseline pin; **control** — the baseline-pin line MUST match, proving the probe is not returning zero for lack of input.
- [ ] **CIAC-3 (#3707 × #3708 × #3914 on the durability lint):** after all three land, a single `--self-test` run passes, **and** the module docstring states all three writers' statements — the ratified name-vs-handle reading, the R2-COUNT recall bound, and the structural-conformance scope declaration. *Method:* run `--self-test` (expect exit 0); grep the docstring for all three; **control** — mutate a predicate to a no-op and confirm the new cases fail.
- [ ] **CIAC-4 (#1488 × #3383 × #3713 on the release ADR index):** the release ADR index is a **projection** of the file set — index rows ≡ the release ADR glob, zero missing and zero orphan rows — machine-checked. *Method:* set-difference the index-row set against the file-glob set (expect empty both directions); **control** — delete one row and confirm the check fires; **and** the check's own output must state its denominator.

---

## File Change Matrix

Machine-readable — **one path per line**, for deterministic extraction by Stage 7/8/9 chip prompts. Intent in the trailing comment.

```paths
release/releases/plans/adr-corpus-conformance_RELEASE_PLAN.md   # new  — this plan, Engineering Commit 0
core/schemas/adr-schema.md                                      # edit — #3914 § 3 gains the alternatives row + the requirement-level contract (§3.1) + the authority-chain table (§3.2)
core/schemas/README.md                                          # edit — #3914 forced cardinality cascade on the schema index row (Stage-6 finding; see Deviation Log Δ-cascade)
core/standards/adr-authoring-guide.md                           # edit — #3914 § ADR template prose + template guidance comment + § Supersession carve-out H3; #3915 issue-reference H3 + reference block + the post-fence scope sentence + the durability-rules intro clause; #3708 stale-anchor row + exemption paragraph; #3707 operator-handle row
operations/templates/adr-template.md                            # edit — #3914 requirement level (project-ADR population) + body-section cardinality; #3916 de-cardinalizes the platform-contract parenthetical (population boundary preserved; see Deviation Log Delta-decard)
release/tools/check-adr-durability.py                           # edit — #3914 scope block + section-set citation + self-test drift case; #3915 R4 IDENT-REF + shared frontmatter_bounds() + R4 docstring block + 16 self-test cases; #3708 R2-COUNT widened vocabulary + qualifier clause + identifier-prefix/ceiling/thousands-separator guards + 2 measured historical anchors + R2-COUNT RECALL BOUND docstring block + repointed source_observation_lines() + 19 self-test cases with 2 mutation-kill arms; #3707 R3
.github/ISSUE_TEMPLATE/adr.yml                                  # edit — #3914 fifth requirement-level surface (accepted scope change SC-4)
.github/workflows/repo-integrity.yml                            # edit — #3915 job `issue-ref` REFBLOCK_RE + its failure-summary block (accepted scope change SC-3), and job `adr-durability` findings/fixes message for R4; #3708 GATE 9 header comment only — R2 narration + de-cardinalized rule roster (no job logic, WARN_MODE untouched); #3707 job `depersonalization` (GATE 1) — DISJOINT regions, serialization suffices
core/rules/git-workflow.md                                      # edit — #3707 self-contradiction on the ADR deciders carve-out (accepted scope change SC-2); #3915 forced cascade — the durability-job rule enumeration (see Deviation Log Delta-cascade-gw)
core/standards/depersonalization-spec.md                        # edit — #3707 §1.2 narrowed to name-only in BOTH clauses + the enforcement residual recorded (absent from the Stage-4 matrix; see Deviation Log Delta-spec-path)
release/tools/lib/deciders-carveout.sh                          # new  — #3707 the shared per-line carve-out predicate, sourced by BOTH the gate and its fixture suite (PV-2d; see Deviation Log Delta-libextract)
release/tools/tests/test_deciders_carveout.sh                   # new  — #3707 two-gate agreement suite, invocable locally AND by CI
release/tools/tests/fixtures/deciders-carveout/*                # new  — #3707 synthetic fixtures + README (GLOB; FX-4 deliberately absent — see Deviation Log Delta-fx4)
.github/workflows/release-tooling-smoke.yml                     # edit — #3707 CI wiring for the suite + a repo-integrity.yml path trigger so the structural binding assertion re-runs (absent from the Stage-4 matrix; see Deviation Log Delta-smoke-ci)
core/skills/adr-helper/SKILL.md                                 # edit — #3916 derive-not-copy contract, reference block, self-check + index-registration steps, amended additive-only invariant (6 sites), 3 appended failure modes; #3713 allocation surface (reserved regions byte-untouched — see Deviation Log Delta-c3)
core/skills/adr-helper/references/scaffolding-procedure.md      # edit — #3916 section-set cardinality cascade (handed over from #3914) resolved as derive-from-schema; NEW 2.1 merge-binding anchor, recast 4, 5, extended 7, NEW 8 conformance self-check, NEW 9 release-index registration; #3713 allocation (1, 2 body, 6 reserved and byte-untouched)
packages/adr-helper.skill                                       # edit — ONE rebuild at release-cut, after both #3916 and #3713 land
packages/adr-helper.skill.sha256                                # edit — content-baseline sidecar re-emitted by the package builder
release/tools/check-adr-numbers.py                              # edit — #3713 next_free() the binding oracle + 2 self-test fixtures; main()/CI/verdict semantics byte-unchanged
release/tools/renumber-adr.py                                   # new  — #3713 the tooled six-step merge-time renumber (absent from the Stage-4 matrix; see Deviation Log Delta-tool-path)
release/tools/tests/test_renumber_adr.sh                        # new  — #3713 two-branch hermetic git fixture, 40 assertions incl. T0 root-cause + A7 negative control + A10 renumber-down
.github/workflows/repo-integrity.yml                            # edit — #3713 CI wiring: tool self-test + fixture, beside adr-number-integrity (see Deviation Log Delta-ci-two-layer)
release/references/pipeline/stage-12-execute.md                 # edit — #3713 Phase A.5.7 (ADR-identifier dimension of the A.5 pre-check) + the Section 7 metrics line
core/schemas/gate-criteria-spec.md                              # edit — #3713 CONDITIONAL, and it FIRED: G-EX9 row + self-repair row + schema version re-derived to 2.6 + versioning block
core/schemas/stage-io-contracts.md                              # edit — #3713 the live G-EX1..G-EX8 range claim (absent from the Stage-4 matrix; see Deviation Log Delta-io-contracts)
core/ADRs/ADR-114-adr-section-set-and-durability-hygiene-carve-out.md   # new — #3914's ADR (number derived at Commit 0 against origin/main; LANDED)
release/ADRs/ADR-115-adr-number-claim-binds-at-merge.md         # new  — #3713's ADR (release-home: release-pipeline mechanics; see Deviation Log Delta-adr-home)
core/ADRs/ADR-112-adr-deciders-carve-out-is-name-only.md        # new  — #3707's decision, which had no durable home (core-home: platform-wide public-surface policy; see Deviation Log Delta-adr-count)
core/ADRs/ADR-*.md                                              # edit — #1488 corpus sweep (GLOB, deliberately — the corpus is the population); W2.1 heading normalization LANDED (slice 6a); W2.2 durability LANDED (slice 6b); W2.4 identity + W2.5 alternatives authoring LANDED (slice 6c)
release/ADRs/ADR-*.md                                           # edit — #1488 corpus sweep (GLOB); W2.1 heading normalization LANDED (slice 6a); W2.3 durability LANDED (slice 6b); W2.4 identity LANDED (slice 6c; W2.5 is core-only by population)
core/deploy/allowlists/count-structure-baseline.txt             # edit — #1488 / W2.2 forced cascade: one Check-63 debt row pruned after its pair stopped being examined (see Deviation Log Δ-w22-countstruct)
core/ADRs/README.md                                             # edit — #3915 DEF-1 marker-suppression correction + authoring-discipline item 2 (third writer, ESC-2; region-disjoint); #1488 declare the curated-thematic-doc contract (ratified: NOT an index)
release/ADRs/README.md                                          # edit — #1488 (+#3383 folded) generated index projection — LANDED (slice 6d): derived-surface marker + managed ADR-INDEX region, all five columns projected; the prose roster, cross-numbering table and scope narrative COLLAPSED to a rule statement
release/tools/generate-adr-index.py                             # new  — #1488 / W2.6 the index projector: --verify (set-difference both ways + per-cell equality, prints its denominators) / --write / --stdout / --self-test with 3 control arms + 3 structural-failure arms
release/references/standards/release-corpus-schema.md           # edit — #1488 / W2.6 register the ADR index in § Derived-Surface Contract (roles + per-field provenance + verification posture); the contract is declared to govern the PATTERN, not only the ledger family
release/ADRs/ADR-113-adr-index-derived-surface-and-scoped-conformance-claim.md  # new — #1488's ADR (release-home: the surface it governs is the release module's index; number bound against origin/main and chosen by merge-union simulation)
release/releases/RELEASE_LOG.md                                 # edit — Stage-13 close-out ledger row [Stage 13, not this build]
```

**Notes.** (0) `.github/workflows/repo-integrity.yml`, `release/tools/renumber-adr.py`, `release/tools/tests/test_renumber_adr.sh`, `core/schemas/adr-schema.md` and `core/ADRs/ADR-114-*.md` gained a **further** #1488 / W2.6 writer in slice 6d — the index-projection CI step and the R5 findings text; R4's projector redirect and its re-expressed A3 fixture arm; the §3.2 authority-chain row reconciled to the lint's new delta-only structural enforcement; and ADR-114 §Decision(3) amended in place (still `Proposed`, so an amendment and not a supersession). (1) The two `ADR-*.md` glob rows are **globs, deliberately** — the corpus is the population, and a hardcoded file list is the very rot class this release exists to eliminate. (2) `core/schemas/gate-criteria-spec.md` is **conditional**. (3) The two new-ADR rows carry their derived numbers per § ADR Number Derivation; if the sibling PR merges first, the ratified tooled renumber resolves the collision. (4) The canonical tool path is `release/tools/`, **not** `core/tools/` — the latter does not exist.

**Explicitly NOT edited:**
- **`core/skills/adr-helper/**` by #3914** — the cardinality cascade landing there is **handed to #3916**, which owns that surface. #3914 sets the contract; #3916 realizes it.
- **`release/releases/**` historical artifacts** (shipped plans, notes, digest, archive) — historical record, untouched by design.
- **The durability lint's `WARN_MODE`** — see § Rollback. No card in this milestone carries the flip as an acceptance criterion; this is a boundary to hold, not a scope change.

---

## Domain Practice Provenance

The File Change Matrix is entirely internal pmo-platform artifacts — governance standards, schemas, the ADR corpus, CI checkers, issue templates, and one skill. Sourcing-exempt (no external practice governs ADR section-set reconciliation for an internally-governed corpus), but the domain class is mandatory in every mode and travels unchanged into Solutioning and Engineering.

domain_practice: { source: N/A — pipeline-internal release, date: 2026-08-03, domain: governance }

---

## Bundle-Coherence Read

The bundle carries a **real, unresolved theme breach**: the capability test is answered three times (an author has one unambiguous standard; a reader finds every ADR conformant and indexed; two concurrent releases can author without colliding). The declined split would have put all three in-band.

**Why hold it anyway.** The three capabilities are not merely adjacent — **two of the three are gates on each other's correctness.** The standard must land before the corpus can be *authored against at all*, not as a sequencing nicety but because the immutability carve-out is what makes editing an Accepted ADR **legal**. And the numbering fix is exercised *by this very release* authoring into a contended namespace. Splitting into three milestones would trade an in-bundle edge for a cross-milestone edge without reducing coupling.

**Recorded cleave point.** **#3713 is the clean cleave line** if a split is ever forced: zero dependency edges to the other six, separable capability, second-largest card. Extracting it leaves a bundle whose remaining members are genuinely one capability chain.

---

## Deviation Log

Deltas between the Stage-4 plan of record (#4576) and the ratified dispositions (Stage-4 gate, consolidated Wave-1 gate), plus Stage-6 implementation findings.

| # | Stage-4 / spec record | Ratified / implemented delta | Basis |
|---|---|---|---|
| **Δ-D-A** | Hub framed two-wave entry as "#3914, #3915 only" | **Wave 1 is six cards, not two.** #3916, #3707, #3708 and #3713 also have zero sibling-PR coupling and zero corpus-count dependency. Holding them would repeat the defer-everything error at smaller scale. The gate is a **merge-order constraint, not a wait** — Wave 2 may build at any time. | Stage-4 gate, D-A |
| **Δ-D-B1** | Card recommended "conditional-required" (presence conditional on ≥2 options) | **`## Alternatives rejected` satisfies the requirement** — the contract for the section is the *decision-recall content*, not the heading string. Normalize the heading; do not author against records whose content is already present. | Stage-4 gate, D-B1 |
| **Δ-D-B2** | Both sweep cards assumed `core/ADRs/README.md` is an index | **Declared a curated thematic doc, not an index.** It is thematic by construction; converting it to a flat index would destroy a useful curation and create a second hand-maintained duplicate of the file set. **Index-completeness criteria scope to `release/ADRs/README.md` only.** | Stage-4 gate, D-B2 |
| **Δ-D-C** | Card stated a preference for defer-to-claim (pre-allocation) | **(C) tooled merge-time renumber.** Pre-allocating from a moving population is the failure, not the specific oracle; any design-time reservation against a globally-sequenced, concurrently-claimed namespace inherits it. The observed failure was a **missing provenance note** — a tooling gap, not a design gap. The version-adapter analogy is real but asymmetric: a tag is a single ref with an atomic compare-and-swap; an ADR number is a filename plus N citations. | Stage-4 gate, D-C |
| **Δ-SC** | Stage-4 File Change Matrix covered neither the fifth requirement-level surface nor three sibling-card surfaces | **All four Stage-5 scope changes accepted.** SC-1 frontmatter-detector repair incl. its stale-anchor half (#3708) · SC-2 `core/rules/git-workflow.md` self-contradiction (#3707) · SC-3 the reference-block pattern + the repo-integrity workflow (#3915) · **SC-4 `.github/ISSUE_TEMPLATE/adr.yml` — the fifth requirement-level surface, now #3914's** (a ~2-line consistency edit under the ratified level). Reconciling 3 of 5 surfaces would have left the contradiction live. | Wave-1 gate |
| **Δ-blocker** | #3916 required a reference-block form that no card owned | **#3915 owns stating the reference block**, landing inside its already-reserved H3, so the four-way C1 partition survives intact. #3916 binds to the outcome via a set-equality criterion. | Wave-1 gate |
| **Δ-resize** | Stage-4 sized the sweep's authoring set at **32** under the ratified heading-equivalence call | **Re-sized to 21.** Applying the ratified content-over-heading principle *consistently* moves 11 files out of the authoring set: **10** record their options under an `Options considered` / `Options Considered` / `Decision Drivers` H2, and **1** records them at H3. The decisive witness: the authoring guide's **own named exemplar for "rejected alternatives recorded"** is scored an omission by a heading-string test — the standard's own exemplar failing the standard's own test is a demonstration that the test measures the wrong property. Net effect: normalize the heading family + promote one H3 + author the residual. **Partition re-verified at Commit 0** against the live corpus with both control arms; sensitivity and specificity arms clean, extraction shown non-empty. **This applies the ratified call; it does not re-open it.** | Wave-1 gate; **pinned at Stage 6 Commit 0** |
| **Δ-size** | #3707 carried `size:S` | **Re-sized to `size:M`** once its accepted scope change (the git-workflow self-contradiction) landed in its surface. | Wave-1 gate |
| **Δ-D1** | Public-surface policy for the ADR `deciders:` carve-out was open | **NAME-ONLY ratified.** The carve-out permits the operator's literal **name**, never the GitHub handle. The spec narrows; the durability lint's handle rule is not touched. | Wave-1 gate |
| **Δ-anchor** | Stage-4 standing constraint read *"derive against `main` **and** all remote branches"* → next-free two above the `main` anchor | **CORRECTED: the binding anchor is `origin/main` ONLY.** The Stage-4 wording conflated **detection** with **binding** and yielded the exact gap-landing value it was written to prevent — binding two above the `main` anchor lands a 2-wide gap on `main` if this release merges first, and the contiguity gate then fails every subsequent PR. Scanning other branches is for *detection*, never for *binding*. **Binding at the `main` next-free is safe under both merge orders.** Raised at Stage 5; hub-verified against the corpus; both numbers derived accordingly at Commit 0. | Stage-5 finding; Wave-1 gate |
| **Δ-baseline** | Stage-4 pinned `origin/main` at the milestone-readiness commit | **Re-pinned at Commit 0 — unchanged, zero commits of drift.** Every chip still re-locates its edit anchors by content, not by line number. | Commit-0 re-verify |
| **Δ-cascade** | The Stage-5 consumer table classified `core/schemas/README.md` as "cites, does not restate → NO update" | **UPDATE — the row does restate the cardinality.** The Commit-0 cascade sweep found the schema index row carrying the literal body-section count, which the §3 change makes stale. Owned by no other card and forced by this card's change, so it is repaired here rather than left to ship a stale count inside the release that exists to eliminate stale counts. Smallest correct edit: the one cardinality token. The adjacent frontmatter-field count is **correct** and was deliberately left alone — making index-row counts structurally durable is the count-vs-structure lint's job, not this card's. **Sweep verdict: 3 UPDATE (1 handed to #3916) / 2 PRESERVE / 2 N/A**, both arms clean. | Stage-6 implementation finding (#3914 chip) |
| **Δ-adr** | Stage-4 left both ADR numbers to be derived at Commit 0 | **ADR-114 allocated and landed** for #3914 (`core/ADRs/`, cross-cutting platform decision), covering both reconciliations in one record — they are a single coherent decision (*what the standard requires, and what may be edited to reach it*), and splitting them would produce two records that must cite each other to be understood. **ADR-115 is #3713's** to author in its slice. The new record is the first authored under the reconciled set and carries all seven sections; it passes the durability lint with **zero** findings and **without** an override marker. | Commit 0; #3914 chip |
| **Δ-version** | Stage-4 recorded a rule-computed provisional next-free minor | **Commit-0 re-verify: PROCEED.** `anchor()` re-derived from authoritative host state (remote tags + the ledger read from `origin/main`, not the worktree copy); the `minor`-floor next-free is **unchanged** from the Stage-4 recorded determination and is still free. Per ADR-092 the concrete number does **not** bind here — it binds at the Stage-12 atomic claim. The sibling release will compute the same slot; that is expected, and the atomic claim resolves it. | Commit-0 re-verify |
| **Δ-refblock** | SC-3 was framed as widening the issue-ref gate's reference-block pattern so `## Related ADRs` is recognized, on the read that it is "the single largest unblocking of the rung-5 rule" | **Implemented as `Source(s)` only; `Related ADRs` deliberately NOT added.** Measured on the live corpus: recognizing `## Related ADRs` moves **one** marker-carrying file out of the still-misplaced set, and the tokens it un-flags are, on inspection, bare `#N` sitting **inside** `## Related ADRs` sections — precisely the zone-1 violations the new rule prohibits and the sweep needs to see. Because these gates treat the first recognized heading as a *cut point*, recognizing it would lift the cut above that section and make the gate accept exactly the placement this release's own rule forbids. The `Source(s)` arm **is** a genuine defect: the gate's own failure message, the core ADR README and the durability standard all named `### Source(s)` as recognized while the regex rejected it — specification-versus-implementation, now corrected. The real unblocking is naming `## References` as the ADR reference block, which is what the guide now does. **Evidence-grounded deviation from the hub's framing, surfaced for Stage 9 — a one-line regex either way, CHEAP to reverse.** | Stage-6 implementation finding (#3915 chip) |
| **Δ-refblock-2gates** | Both the guide draft and the surrounding discussion assumed one recognized reference-block heading set | **There are TWO independent `REFBLOCK_RE` declarations and they already disagree.** The issue-ref gate declares its own inline; the reference-durability gate, its PreToolUse hook and its fixture runner all source a single shared pattern library. `Related` and `Source(s)` are recognized by the issue-ref gate **only**. `## References`, `## Issue References`, `## Provenance`, `## Sources` sit in the **intersection**. The guide is authored to the intersection and states why, rather than offering the union as a menu. **Unifying the two sets is NOT done here** — it is a corpus-wide behavioural change to a shared hook library with its own fixture suite, outside SC-3, and it needs an operator call on which set is canonical. Recorded as a residual. | Stage-6 implementation finding (#3915 chip) |
| **Δ-cascade-gw** | The Stage-5 File Change Matrix did not list `core/rules/git-workflow.md` for #3915 | **UPDATE — forced by R4.** Its § Repository-Integrity Gates paragraph enumerated the durability job's rules with a frozen cardinality, which adding a fourth rule makes false. Repaired as the smallest correct edit: the cardinality is **de-cardinalized** (it now cites the module docstring as the enumerating authority) and the fourth rule is added to the list. The `deciders:` carve-out sentence — the sibling card's declared SC-2 surface — is **byte-untouched**; the two edits are region-disjoint and serialization suffices. | Stage-6 implementation finding (#3915 chip) |
| **Δ-E1** | The binding C1 partition row for #3915 named only "a new H3 … plus the reference-block statement" | **Two further in-scope prose edits were required and made.** (a) The post-template-fence sentence that over-generalised the cross-ADR `never issue #N` note into a blanket ban on body **and frontmatter** references is the contradiction the card exists to remove — leaving it would fail the card's own no-contradiction criterion while the new H3 permits a bare `#N` in `source_observations:` two screens below. (b) The `### Durability rules` **intro clause** stated a frozen count of enforced rules, which R4 falsifies. **Neither is another writer's declared region:** the landed slice-1 edits (§ ADR template prose, template guidance comment, § Supersession H3) and the two reserved table **rows** plus the exemption paragraph are all byte-untouched, verified with a sensitivity control. | Stage-6 implementation finding (#3915 chip) |
| **Δ-E3-dropped** | The Stage-5 spec proposed appending a cross-reference sentence to the trailing prose of `### Durability rules` | **DROPPED.** The consolidated Wave-1 gate assigned that paragraph — the stale-anchor exemption prose — to the sibling stale-anchor card. The cross-reference now lives inside the new H3 instead, which is wholly within this card's region. The spec's "deliberate seam" is closed rather than contested. | Wave-1 gate; #3915 chip |
| **Δ-derive-src** | The Stage-5 spec bound the scaffold's derive-not-copy contract to `adr-authoring-guide.md § ADR template` — the only candidate visible when both upstream cards had reported nothing | **Re-bound to `core/schemas/adr-schema.md` §3.** The upstream reconciliation landed an authority-chain table making §3 the sole DEFINING surface, with every other surface CITING it — and that table **names `scaffolding-procedure.md` as a citing member by path**. Binding to the guide would have made the scaffold a citer-of-a-citer, a two-hop chain whose middle link can drift. The schema is also the surface with a **shipped, self-tested extractor** (`parse_schema_section_set()` in the durability lint, pinned by a `--self-test` case), so reuse beats re-derivation. The integration criterion is unaffected: schema §3 and the guide's rendered template were verified **set- and order-identical**, with a sensitivity control (dropping one schema row makes the assertion fail). | Stage-6 implementation finding (#3916 chip) |
| **Δ-c3** | The Stage-5 reservation gave `scaffolding-procedure.md` § 2 wholly to #3713 | **Resolved by narrowing, not by relocating.** The merge-binding rule belongs at allocation time, and § 2 is the allocation walkthrough — so filing it under a scaffold-output section would have misplaced it. #3916 therefore takes a **new H3 § 2.1** appended beneath § 2, leaving § 2's heading, algorithm, worked example, union rationale and closing re-check sentence **byte-identical** (asserted by diff). **#3713's reservation is unchanged in substance and now reads: § 1, § 2's body, § 6, plus SKILL.md's Mode steps 1–2, § Immutable-Numbering Rule, Output Contract item 2, the allocation PROC failure mode, and the two allocation invariant bullets — all verified byte-untouched.** #3713 should **cite** § 2.1 rather than restate it, and two residual imprecisions are handed over with exact strings (see Δ-livetree). | Stage-6 implementation finding (#3916 chip) |
| **Δ-region** | The Stage-5 write set named SKILL.md Mode steps 4–5, Output Contract items 1/3/6, appended failure modes, non-allocation bullets; reference §§ 4, 5, new 8 | **Widened, and forced by the change itself.** The additive-only invariant is stated in **six** places in SKILL.md and the hand-off contract in **two** (SKILL.md's Mode hand-off step and the reference's § 7); editing a subset would have shipped a skill that contradicts itself — the exact defect this release exists to remove. Actual set adds: § Role's act list (its "three things" lead-in became an undercount), Mode steps 6–8 (the hand-off step renumbered from 6; no surface references the step numbers — verified), Output Contract new items 7–8, § Autonomy Tier, § Dependency Graph Node, § Reversibility Discipline, § Guardrails, one What-This-Skill-Does-NOT-Do addition; reference § 7 extended, new § 9. **All additions are region-disjoint from #3713's reservation.** | Stage-6 implementation finding (#3916 chip) |
| **Δ-additive** | `adr-helper` declared itself **additive-only — its sole write is a NEW `ADR-NNN.md`; it writes NOTHING else** | **Amended, not violated.** Index registration makes the skill write a second path, so the invariant is restated as *additive-only across a declared two-file write scope* at every site that states it. Autonomy tier is **unchanged at Tier 1**: one explicit trigger still authorizes one drafted package (the record plus the index row projecting it), and a write outside the declared scope does not proceed. Recorded as the release's one named debt-incurred item, surfaced for Stage 9 rather than absorbed silently. | Stage-6 implementation finding (#3916 chip) |
| **Δ-index-mech** | The Stage-5 design left the registration mechanism open pending the sweep card's index question | **Question is no longer open, and the answer shaped the build.** This plan's own **CIAC-4** and **R2** commit the release ADR index to being a **generated projection** in Wave 2. A hardcoded row-append would therefore have been built wrong on arrival. The scaffold states the **outcome** ("leave the index consistent with the file set it extended") and resolves the **mechanism at runtime**, preferring the index's own generator where one exists. **D-B2 honored:** registration targets the release index only; the core-module README is explicitly excluded in both the skill and the reference. The recurrence is live — the sibling PR adds two `release/ADRs/` records and touches that index **zero** times. | Stage-6 implementation finding (#3916 chip) |
| **Δ-decard** | The Stage-5 cascade sweep marked the project template's platform-contract parenthetical UPDATE on the then-current (wrong) body-section count | **De-cardinalized rather than re-counted.** The sibling card had already corrected the count once inside this release — which is the demonstration that the frozen form rots, not an argument that the new number is safer. The count is replaced by a pointer to the defining schema section. The **adjacent frontmatter-field count is deliberately left alone**, mirroring the boundary the sibling card stated: this release changes the body-section definition, not the field set, and making index-row counts structurally durable is the count-vs-structure lint's job. The § 5 population boundary and the shared alternatives predicate are byte-preserved. | Stage-6 implementation finding (#3916 chip) |
| **Δ-ac5-probe** | AC-5's separation evidence was "the scaffold references the project template **zero** times" | **The probe shape changes and Stage 8 must grade the new predicate.** The reference now names the boundary explicitly ("governs a different population … the scaffold never reads it and never applies it"), so the mention count is **one**, by design. Silence is the weaker instrument — an explicit non-conflation statement is what AC-5 actually asks for. A grader running the old zero-mentions probe would read the intended strengthening as a regression. | Stage-6 implementation finding (#3916 chip) |
| **Δ-livetree** | Not foreseen at Stage 5 | **Two residual imprecisions handed to #3713, deliberately not crossed.** `scaffolding-procedure.md` § 6 and SKILL.md § Immutable-Numbering Rule each say the skill *"allocates against the live tree"*, which § 2.1 now sharpens to the mainline anchor. **Both sit inside #3713's reserved regions**, and that card must edit both anyway for the tooled merge-time renumber, so they are handed over with the exact strings rather than reached into — the same discipline the four-way guide partition held. The third occurrence, in § Dependency Graph Node, was inside this card's region and **is** repaired. If #3713 does not land, Stage 9 owns the residual. | Stage-6 implementation finding (#3916 chip) |
| **Δ-frontmatter-split** | The Stage-5 spec made repointing `source_observation_lines()` at the shared bound an in-scope prerequisite for this card | **Split: the helper is built here, the repoint is left to the stale-anchor card.** That card owns the frontmatter-detector repair as an accepted scope change, and repointing the exemption here would shift the **R2** corpus verdict from this card's commit, muddying attribution for a rule this card does not own. `frontmatter_bounds()` therefore ships as a shared, comment-tolerant helper consumed by R4 only; `source_observation_lines()` is **byte-identical** to its pre-change form, with a docstring note naming the helper the repair should consume. Measured: the tolerant bound is worth **10** additional identity-field tokens over the shipped line-0 rule. | Stage-6 coordination (#3915 → #3708) |
| **Δ-frontmatter-join** | The split above left the repoint outstanding | **Repoint landed.** `source_observation_lines()` now resolves through the shared `frontmatter_bounds()`; no second bounds parser was added, per the handoff. The R2 verdict moves as predicted: one false stale-count finding and **two** false commit-SHA findings are removed — the SHA half being the case the operator explicitly admitted with SC-1 and that no card previously owned. The helper's docstring note pointing forward to this repair is reconciled to name its two consumers rather than left describing a state that no longer holds. | Stage-6 implementation finding (#3708 chip) |
| **Δ-widen-premise** | The card framed the fork as widen-**vs**-document, with widening carrying a measured precision cost, and the Stage-5 spec adopted Option C on figures measured at the pre-slice corpus | **The premise is falsified, and the Stage-5 figures are superseded by the as-built measurement.** Built and measured on the live corpus: the widened rule is better than the shipped rule on **both** axes, not traded off. Full delexicalisation was also built — with *every* repaired guard retained, so vocabulary was the only variable — and still lands near two thirds precision, because its residual false positives are fixed-cardinality design constants that no structural guard can separate from a live count. **That asymmetry is the whole argument for keeping a vocabulary and widening it rather than dropping it**, and it is now recorded in the checker's docstring so the question is not re-litigated. The as-built figures differ from the Stage-5 spec's in both directions (the recall denominator is larger because the repaired guards make more of the lifted arm's output classifiable, and the qualifier clause was implemented hyphen-tolerant, reaching more compounds than specced); the **as-built** measurement governs. | Stage-6 implementation finding (#3708 chip) |
| **Δ-anchors-measured** | The Stage-5 spec proposed **eight** additional historical-framing anchors | **Two admitted, five rejected, one dropped — each on measurement, not on plausibility.** Every candidate was run against the whole corpus and its suppression set inspected. Three (`live survey`, `empirical survey`, `corpus probe returned`) suppress **genuine** live counts: they name a survey *method*, not a date, and a method phrase is not a historical anchor. Two (`live-state reconciliation`, `live registry drift`) match **nothing** — a vocabulary entry that suppresses nothing is not an exemption, it is noise, and it defeats the closed-vocabulary falsifiability the exemption rests on. One (`conducted 20`) is redundant on this corpus with an admitted anchor and is a bare year-prefix match, which is the loose predicate class this card exists to discipline. **`at survey baseline` and `audit-baseline` admitted**; the rejected `live survey` is retained as a self-test case asserting it must NOT suppress. | Stage-6 implementation finding (#3708 chip) |
| **Δ-guards-3-to-5** | The Stage-5 spec specified the lookbehind completion plus vocabulary widening | **Two further structural guards were added, each tied to a measured false-positive family, and one specified guard was narrowed on measurement.** (a) A **ceiling-prefix** guard — a number introduced by a bound word states a limit the corpus is authored to stay under, not a measurement of it. **Ceilings only:** the symmetric *floor* words were implemented, measured, and **reverted** — they suppressed empirical lower-bound measurements, which are live counts that do rot, and killed no false positive the ceiling half had not already killed. A guard that looks symmetric is not thereby correct. (b) **Thousands-separator tolerance** in the number — without it the predicate began inside the number and reported its tail, understating the very figure it was flagging. Net: it corrects three mis-reported findings and admits one new false positive (a threshold whose bound word is too far from the number for the ceiling guard to reach), and is kept because a finding that misquotes its own subject is not citeable. | Stage-6 implementation finding (#3708 chip) |
| **Δ-digest-frozen** | The card requires the introducing record's precision claim to be corrected where the build record lives | **The in-repo home is the shipped release digest, which this plan's File Change Matrix explicitly forbids editing** (historical artifacts — shipped plans, notes, digest, archive — are untouched by design). The correction is therefore recorded in the **live** artifact instead: the checker's own docstring states that the introducing record reported precision only, reported it as perfect, and that re-measurement found two of its six findings were not genuine — with both causes fixed in the same commit. **Surfaced for the hub**: the frozen digest line still carries the superseded figure, and whether a corrections pointer belongs elsewhere in the release corpus is a call above this chip. | Stage-6 implementation finding (#3708 chip) |
| **Δ-spec-path** | The Stage-4 File Change Matrix did not list `core/standards/depersonalization-spec.md` for #3707 | **UPDATE — it is the card's PRIMARY surface.** §1.2 is the carve-out's governed home and the origin of the ambiguity; the matrix listed the two *derivative* prose surfaces (`git-workflow.md`, the workflow comment) and the checker, but not the rule's own home. Repaired here: the permission clause and the blocked-list clause now state the same boundary, the enforcement sentence describes the name-scoped skip, and the rule is stated once with the other surfaces citing it. **Declared deviation, not silent scope** — the issue body named this file first in its own Affected Files, so the matrix was the artifact that was incomplete, not the scope. | Stage-6 implementation finding (#3707 chip) |
| **Δ-libextract** | The Stage-5 spec specified the fix as a **4-line predicate inline** in the workflow's existing `case` branch, and listed no library path | **Extracted to a shared sourced library — `release/tools/lib/deciders-carveout.sh` — consumed by BOTH the gate and the committed fixture suite.** The spec's own D-9 sizing rationale already required this (PV-2d: a probe reachable only by a remote CI trigger needs an entry point invocable locally **and** by CI, and *"where the CI job asserts inline rather than delegating to such an entry point, extracting the assertion into one is part of the work, not an exemption"*), but the change matrix never gained the path. An inline guard plus a re-implemented test would pass its own tests while the real gate diverged — the shadow-source failure this very card exists to end. `esc_lines` moved **byte-identical** (verified by diffing the extracted body against `HEAD`'s inline copy; only its one-line comment was promoted to a docstring), so the extraction is provably behaviour-preserving. Placement **extends** the existing `release/tools/lib/` convention (`schema-v1-emit.sh`), not a net-new directory — SR-G6 satisfied. | Stage-6 implementation finding (#3707 chip) |
| **Δ-smoke-ci** | `.github/workflows/release-tooling-smoke.yml` was in neither the Stage-4 nor the Stage-5 File Change Matrix | **UPDATE — required by PV-2d's "invoked by the CI job" half.** A committed fixture suite that only a human ever runs is not a gate. The suite is added as a `closeout-smoke` step, and `.github/workflows/repo-integrity.yml` is added to the workflow's `paths:` triggers **specifically** so that a change dropping the sourcing (or the verdict call) re-runs the structural binding assertion that catches it — the library itself is already covered by the existing `release/tools/**` trigger. Runner is `macos-latest`; the suite is Bash-3.2-clean and was executed on macOS before commit. | Stage-6 implementation finding (#3707 chip) |
| **Δ-D5-declined** | The Stage-5 spec **recommended D-5**: close the operator-email and collaborator over-suppression with the same guard, at "zero marginal cost" | **Offered explicitly at the consolidated Wave-1 gate and DECLINED. Plain NAME-ONLY, not "name-only plus close the adjacent gap."** As-built, the un-carved set holds the **handle only**. The declined half is therefore **recorded as an enforcement residual rather than closed**, in three places so it cannot be mistaken for an oversight: a named paragraph in `depersonalization-spec.md` §1.2 (*"the enforcement is narrower than the rule … a stated gap, not a second reading"*), an in-code `KNOWN RESIDUAL` comment on the branch itself, and fixtures **FX-5 / FX-6**, which pin the current behaviour as **characterization, not endorsement**. **Consequence for the ACs: refined criterion AC-6′ (which asserted the gate blocks the email and every collaborator literal on a `deciders:` line) is WITHDRAWN** — it encoded the declined variant. Its fixtures are retained inverted, as residual pins. | Wave-1 gate; #3707 chip |
| **Δ-fx4** | The Stage-5 fixture matrix specified **eight** fixtures, FX-1..FX-8 | **Seven committed; FX-4 deliberately absent.** FX-4 (the operator *name* on an ADR **body** line) is a depersonalization-only dimension with **no durability counterpart**, so it cannot participate in a two-gate *agreement* assertion — the property this suite exists to assert. Including it would have added a row whose "agreement" column is meaningless. The absence and its reason are recorded in the fixture README, and fixture IDs stay stable with the Stage-5 matrix rather than being renumbered, so the two artifacts remain diffable. Suite denominator is emitted as **7** by the suite itself (PV-6), never asserted as a frozen count in prose. | Stage-6 implementation finding (#3707 chip) |
| **Δ-guide-zero** | The C1 partition reserved `### Durability rules` **row 3** for #3707, and `check-adr-durability.py`'s R3 block as its C2 region | **Both writes are genuinely ZERO lines, re-verified at the post-slice HEAD rather than assumed from the Stage-5 baseline.** Row 3 already states the ratified reading verbatim (*"The GitHub handle is never sanctioned, on any line, including `deciders:`"*) with a matching write-this/not-this pair; `git blame` dates it to the durability lint's own introducing commit, an ancestor of the release baseline, and the three sibling writes since did not touch it. The R3-dependent sentence #3707 handed to the stale-anchor card as a constraint (*"The marker never suppresses the handle rule"*) **survived** that card's exemption rewording — verified present exactly once. **A zero-line write is the correct result here and is reported as one; authoring an edit to look productive on the release's most contended file would have been manufactured scope.** | Stage-6 verification (#3707 chip, C1 writer 4 of 4) |
| **Δ-ciac1** | #3707 owns the release-level **CIAC-1** cross-check as the last of four writers on `adr-authoring-guide.md`: the required-section set must be stated exactly once | **VERDICT: PASS on the invariant, with a named residual the hub should dispose.** As landed, the invariant is satisfied in a *stronger* form than specified: `adr-schema.md` **§3.2 Section-set authority chain** makes §3 the sole DEFINING surface and obliges every other surface to CITE, and § ADR template is the guide's single citing render. A normalized repo-wide probe (all tracked files; ADR instances excluded as renderings) found **no second statement of the requirement level**, with a working specificity arm. **Residual:** §3.2's chain table enumerates four surfaces, but two further surfaces render the seven-section set and are not in it — `release/tools/check-adr-durability.py` (whose copy is *mechanically* pinned to §3 by a self-test asserting membership **and** order, so it cannot drift) and `core/skills/adr-helper/SKILL.md` § Output Contract (which restates "all required body sections" and is **outside this release's surface**, untouched by any card). §3.2's own rule says a later surface "joins this table and inherits the CITES obligation". **Not closed here** — `adr-schema.md` is another writer's region and `adr-helper/SKILL.md` is outside the release. **Surfaced for Stage 9 disposition.** | Stage-6 verification (#3707 chip, CIAC-1 owner) |
| **Δ-gate9-comment** | The Stage-5 File Change Matrix listed the workflow edit for this card as the R2 narration only | **The same comment block also carried a frozen rule cardinality that the sibling card's fourth rule had already made false.** Repaired as the smallest correct edit inside the block being opened anyway: de-cardinalized to cite the module docstring as the enumerating authority, and the fourth rule added in outline rather than restated in full (restating it would fork the contract the sibling card owns). **Comment-only** — no job logic, and `WARN_MODE` is untouched. Region-disjoint from every other writer's declared workflow region. | Stage-6 implementation finding (#3708 chip) |

| **Δ-adr-count** | The plan expected **one** ADR from #3713 (ADR-115) | **Two authored — ADR-115 and ADR-112 — on the operator's ratified allocation.** ADR-112 records the `deciders:` **name-only** public-surface decision, which was rendered at the Wave-1 gate, implemented by #3707 and gated in CI, and had **no durable corpus home**: the sibling slice verified that ADR-114 carries the alternatives requirement level and the durability-hygiene carve-out but *not* this decision or its rejected name+handle alternative. It is the one thing in the bundle that was decided and not written down. Its determining argument — the handle is an account identifier, the record is supersede-not-edit, it ships to every downstream deployment, and **widening is the one cost in this bundle a `git revert` does not undo** — is exactly the class of rationale that gets re-litigated when it lives only in stage commentary. The **declined** operator-email/collaborator variant is recorded inside it as an enforcement residual, so a future reader does not read it as an oversight. | Wave-1 gate allocation; #3713 chip |
| **Δ-adr-home** | The Stage-4 matrix listed the second ADR as `core/ADRs/ADR-115-*.md` | **Homes split, and the matrix row was the artifact that was wrong.** ADR-115 (the merge-time binding rule) is **release**-scoped — it governs pipeline mechanics, Stage-12 Phase A.5.7 and a Gate-12 criterion — so it lands in `release/ADRs/`. ADR-112 (the public-surface policy) is platform-wide and lands in `core/ADRs/`. The matrix itself declared home "that slice's determination", so this is the determination, not a departure from it. | Stage-6 implementation finding (#3713 chip) |
| **Δ-tool-path** | The Stage-4 matrix listed no path for the new tool, and `core/schemas/stage-io-contracts.md` appeared in neither the Stage-4 nor the Stage-5 matrix | **Three paths added.** `release/tools/renumber-adr.py`, its fixture `release/tools/tests/test_renumber_adr.sh`, and the CI wiring in `.github/workflows/repo-integrity.yml` — all three are load-bearing for the direction the operator chose, and a tool with no committed test invoked by no job is not a mechanism. Python is not stylistic: it is what makes the `import` of the number-space contract possible, and `BLOCK-DESTRUCTIVE-022` intercepts `bash\|sh\|source\|.` only, so the Python form needs zero allowlist entries where a shell sibling would need four. | Stage-6 implementation finding (#3713 chip) |
| **Δ-io-contracts** | The plan's matrix did not list `core/schemas/stage-io-contracts.md` | **UPDATE — it carries a LIVE `G-EX1..G-EX8` range claim plus a parenthetical eight-item enumeration**, in a different file from the gate registry, which is precisely the row a same-file cascade sweep misses. Adding G-EX9 without it would have shipped a contract statement that under-enumerates the gate it hands to. Twenty-two further `G-EX1..G-EX8` occurrences in the versioning block are **historical** snapshots and were deliberately PRESERVED — rewriting them would falsify the schema's own version history. | Stage-6 implementation finding (#3713 chip) |
| **Δ-schema-version** | Stage 5 scored the `gate-criteria-spec.md` schema-version line as a **two-way** contention with the sibling PR, resolved by re-deriving | **Re-derived to 2.6 against the mainline, and the contention is now THREE-way, not two.** The audit baseline moved between Stage 5 and Commit 0: the open-PR population went **1 → 4**, and a newly-opened non-draft PR also edits `gate-criteria-spec.md` and also claims **2.6**. Both sibling claims are **detection, never binding** — the same rule this card ratifies for ADR numbers — so this release takes `mainline(2.5) + 1 = 2.6` and the later merger re-derives. The instruction is recorded **in the versioning block itself** rather than left to the merge: re-derive from the mainline and restate the ID enumeration to include every landed addition; never take either side's value. This sharpens the Stage-5 Tier-2 observation rather than resolving it — a globally-sequenced identifier bound at authoring against a moving population is the same defect class this card fixes, at one line of blast radius. | Stage-6 Commit-0 re-derivation (#3713 chip) |
| **Δ-p12-moved** | Stage 5 pinned the cross-PR intersection against an open-PR population of **1**, with an explicit re-check condition | **Re-check FIRED and the finding changed.** Population is **4**. Re-derived intersection over all four: **two** now touch `gate-criteria-spec.md` (the known sibling plus one new non-draft PR); the other two intersect nothing. The first intersection probe run at Commit 0 returned "no intersection" for **all four** PRs — a **broken probe**, caused by zsh not word-splitting an unquoted variable, so the surface list collapsed to a single unmatchable string. It was rebuilt with a bash array and re-run with a specificity control, and the corrected result is the one recorded here. Reported rather than silently fixed: a probe that returns a clean zero because it compared nothing is the exact failure this release exists to eliminate. | Stage-6 Commit-0 re-derivation (#3713 chip) |
| **Δ-live-contention** | The card's premise — that concurrent releases are forced onto the same number — was argued from three historical instances | **It is live at this commit, and this release is inside it.** `--detect` at Commit 0 reports `anchor(origin/main) = 109` with branch-only claims on **110, 111, 112, 113 and 114** across four sibling branches: one holds 110/111, one holds 112/113, one holds 114. This release took **111 and 112** because only the mainline binds; under the rejected `max(claimed_set) + 1` reading it would have taken 115 and landed a **four-number hole** on `main`, failing every subsequent PR. **Prediction, recorded so Stage 12 is not surprised:** if the branch holding 112/113 merges first, this release's ADR-112 becomes a DUPLICATE and Phase A.5.7 fires — this release will dogfood its own mechanism pre-merge, which is the intended behaviour and not a defect. | Stage-6 Commit-0 derivation (#3713 chip) |
| **Δ-r6-union** | The Stage-5 spec's R6 verify asserted `check-adr-numbers.py` is clean over the tree | **As-specced it evaluated the WORKING TREE, and the fixture caught it.** A branch that has not merged the mainline is legitimately missing the numbers its siblings claimed while it was away, so a working-tree evaluation reports a **confident GAP the merge does not have** — the identical answer-computed-over-the-wrong-population defect this card exists to fix, reproduced inside the fix. R6 now evaluates the simulated **merge union**. Recorded prominently because the design spec contained the bug and only the behavioural fixture surfaced it: this is the concrete argument for why AC-1 needed a real two-branch topology and not a function test. | Stage-6 implementation finding (#3713 chip) |
| **Δ-historical-exempt** | The Stage-5 spec's R3 sweep rewrote every `ADR-<old>` token in branch-diff scope | **Two surfaces RECORD the old number on purpose and must be exempt: the `## Status` provenance note and the § Renumber log.** Unexempted, the sweep erased the audit trail the renumber exists to create, and — sharper — the tool's own R5 and R4 output then failed its own R6 verify, so no renumber could ever complete. The rewrite is now line-wise with a `is_historical_numbering_line()` predicate, and the self-test pins it with a sensitivity arm (an ordinary citation on the same input DOES move). A record of a number is not a reference to one. | Stage-6 implementation finding (#3713 chip) |
| **Δ-completion-mode** | The Stage-5 spec had the tool refuse when `<old>` is absent | **It refuses only when neither the old nor the new number is this branch's claim; when the rename is already in place it enters COMPLETION MODE and performs the outstanding steps.** This is the ADR-088 shape made repairable: a hand-performed renumber that landed the rename and skipped the provenance note is exactly the observed defect, and a tool that refuses to touch it leaves the one reliably-skipped step still skipped. It also makes the tool genuinely idempotent (assertion A8) rather than merely safe to re-run. The "ours" test throughout is a **path** test, not a number test — at a duplicate the working tree holds two files for one number, and a number test picks the mainline's record and renumbers the wrong file. That was a real bug, caught by the fixture. | Stage-6 implementation finding (#3713 chip) |
| **Δ-fixture-a7b** | The Stage-5 spec specified assertions A1–A10 | **40 assertions across 13 named arms (T0, ACT2, A1–A10, A7b) — A7b added.** A7b runs **after** A7's failure arms are recorded, so the negative control keeps its meaning, and asserts that completion mode repairs the hand-moved record. **Mutation-tested, and the result is reported honestly:** M1 (drop the provenance note) and M2 (drop the citation sweep) each kill 27 assertions; M3 (bind against `max(claimed_set)+1`) and M4 (drop the index update) each kill 2 and are named. **M5 (remove the right-hand citation boundary) does NOT fail the fixture** — its number range is too small to contain the `ADR-010` inside `ADR-104` case — but it DOES fail the tool's pure-function self-test. The two layers cover different properties and **CI runs both**, deliberately; a single-layer wiring would have shipped M5's class uncaught. | Stage-6 implementation finding (#3713 chip) |
| **Δ-index-rows** | The scaffolding procedure's § 9 obliges a release-home record to leave the release ADR index consistent | **Deliberate NON-action: ADR-115 gets no `release/ADRs/README.md` row, and this is a decision rather than an omission.** The four most recent release-home ADRs are all already unregistered — that is the #1488 gap — so adding one row for the newest record would produce an *inconsistent partial* index that skips four older ones, which is worse than the uniform gap #1488 backfills wholesale. #1488 is in this same release, owns that surface, and backfills by enumerating the file glob, so ADR-115 is picked up automatically. Appending by hand now would also collide with that slice's write on the same lines and would hardcode a row into a surface the plan's own CIAC-4 makes a **generated projection**. | Stage-6 implementation finding (#3713 chip) |
| **Δ-ac1-reading** | AC-1 requires two branches to merge "without a duplicate number **or a manual renumber**" | **Read on the "manual" qualifier, and the reading is stated in ADR-115's Consequences rather than left for Stage 8 to infer.** Direction (C) *tools* the renumber; it does not eliminate it. Eliminating it outright needs an arbitration primitive that does not exist for a filename-plus-citations object — the finding recorded in the ADR's Portability CONFLICT. Left ambiguous, Stage 8 would grade an AC nothing in this direction can satisfy. **Fixture assertion A5 is the graded surface**: both branches merge, the mainline is contiguous, no duplicate, no hand edit. | Stage-6 implementation finding (#3713 chip) |
| **Δ-bundle-held** | Stage 4 named #3713 the release's clean cleave point if composition coherence forced a split | **No reason surfaced to change the operator's call; the bundle holds, and the case for holding is now stronger than at Stage 5.** #3713 corrects the `max(union)+1` algorithm on the same `adr-helper` surface #3916 rewrote — shipping them apart would leave the gap-landing defect live for a release. As-built, #3713 also authors the durable home for #3707's decision (Δ-adr-count), which is a second, unforeseen coherence edge to a Wave-1 sibling. Cleaving now would strand it. | Stage-6 judgement (#3713 chip) |
| **Δ-w21-inventory** | The Stage-5 spec sized W2.1 at **56 edits / 51 files** and instructed re-derivation at execution, the corpus figure having moved five times (30 → 82 → 104 → 109 → 112) | **Re-derived at execution: unchanged — 56 edits / 51 files, and the four-class partition reproduces exactly** (`OK` 41 · `RENAME-H2` 39 · `RENAME-OPTS` 10 incl. the frozen record · `PROMOTE-H3` 1 · `AUTHOR` 21 = 112, both arms clean). Corpus re-derived independently: **112 = core 82 + release 30**, contiguous 001..112, **111 sweepable** after the frozen exclusion. This is the first Wave-2 slice at which a re-derivation confirmed a Stage-5 figure rather than superseding it; recorded because a *confirmed* count is evidence the spec's partition is stable, not evidence the re-derivation was skipped. | Stage-6 implementation finding (#1488 slice 6a) |
| **Δ-w21-ep1-scope** | The Stage-5 exit predicate for W2.1 reads `grep -hoE '^#{2,4} .*[Aa]lternativ' $(SWEEPABLE) \| sort -u \| wc -l` → **1** | **Unsatisfiable at the `#{2,4}` bound without destroying legitimate structure; the correct predicate is H2-scope, and it PASSES at 1.** The literal bound also captures **three H3 sub-headings** — `### Decision 1 alternatives`, `### Decision 2 alternatives`, `### Decision 3 alternatives` in `ADR-003` and `ADR-004` — which sit *beneath* an already-canonical `## Alternatives Considered` and pair one-to-one with those records' `### Decision N` blocks. They are sub-structure, not section headings; renaming them would collapse which-decision-was-weighed information, and `adr-schema.md` §3.1's "**MINIMUM, not a closed vocabulary**" covers them explicitly. **Reported both ways rather than redefined to green:** H2 scope = **1** (PASS); literal `#{2,4}` scope = **4**, with all three exceptions enumerated. Stage 8 should grade the H2-scope predicate. | Stage-6 implementation finding (#1488 slice 6a) |
| **Δ-w21-dup-related** | The spec's `## Related` → `## Related ADRs` class was sized at **4**, and the corpus separately carried **1** `### Related` H3 that no class owned | **`ADR-091` carries both, stacked with zero content between them** (`## Related`, blank, `### Related`, then the section's bullets) — one heading duplicated, not two sections. Normalized to the single canonical `## Related ADRs`; **zero prose touched**. Admitted under the carve-out's **open** permitted list ("normalizing a section heading to its canonical string") on the record-vs-revise test: it records nothing and revises nothing. Counted **separately as the 57th line operation**, not folded into the spec's 56, so the divergence is visible rather than absorbed. **Without it AC-5′c is unsatisfiable** (the `Related` form count floors at 2). The single judgment call in an otherwise mechanical slice; **CHEAP** to reverse (one line). | Stage-6 implementation finding (#1488 slice 6a) |
| **Δ-w21-provenance-drop** | The spec listed `## Alternatives considered (from the Stage 5 design-exploration)` (`ADR-032`) as a plain `RENAME-H2` | **Canonicalization drops the heading's parenthetical, and that is a real six-word provenance loss — disclosed, not hidden.** The canonical string is exact, so no rename preserves it, and relocating the phrase into the body would be prose authoring, which this slice does not do. Left to the operator/Stage 8 as a named, trivially reversible item. **This is the only edit in the slice that removes information**; the other 55 renames are supersets or case-folds (`Alternatives rejected` / `and rejected` → `Considered` widens, never narrows, and the rejected-options content is untouched). | Stage-6 implementation finding (#1488 slice 6a) |
| **Δ-w21-position** | The Stage-5 spec scoped **repositioning OUT** (ADR-114 permits heading normalization and H3→H2 promotion, not re-ordering two H2 sections) | **Honored, and it leaves three records with `## Alternatives Considered` at position 6 rather than 4.** In `ADR-023` / `ADR-027` / `ADR-028` the promoted `## Reversibility` now precedes the normalized alternatives section, because `### Reversibility` sat *inside* `## Consequences` and the options section followed it. Presence is the conformance assertion per §3.1 ("structurally checkable… never that nothing else is"), so this conforms; recorded so a position-sensitive grader does not read it as a defect. `ADR-018` and `ADR-057` carry the same out-of-position shape from before the sweep. | Stage-6 implementation finding (#1488 slice 6a) |
| **Δ-w21-frozen** | Every completeness predicate carries the frozen-record exclusion | **`ADR-029` verified byte-identical at `522961d6` and absent from the changed-file set**, with a working control (`ADR-030`, in the edit set, reports MODIFIED by the same check). It retains the two edits it can never receive — a `RENAME-OPTS` and a Reversibility H3→H2 — which is why the post-sweep corpus still shows exactly one `## Options considered` and one `### Reversibility`. **Those two residuals are correct, permanent, and must not be "fixed" by a later slice.** | Stage-6 implementation finding (#1488 slice 6a) |
| **Δ-w22-inventory** | The Stage-5 spec sized W2.2 at **76 findings / 17 core files** and W2.3 at **17 / 5 release files**, and instructed re-derivation at execution | **Re-derived at execution: unchanged on every axis** — core **71** R2-COUNT + **5** R2-SHA = 76 across 17 files; release **17** R2-COUNT + **0** R2-SHA across 5 files; **93 findings over 70 distinct lines in 22 files**. The line-level figure is the one the spec did not carry and the one the work is actually sized by: 23 of the 93 findings share a line with another, so a single edit clears up to four. Second consecutive Wave-2 slice at which re-derivation **confirmed** the Stage-5 figure. | Stage-6 implementation finding (#1488 slice 6b) |
| **Δ-w22-remedy-classes** | The Stage-5 spec set the exit predicate (`R2 → 0`) but named no remedy; the authoring guide's stale-anchor row offers three, as alternatives | **Each finding is classified to exactly one of the three, and the classification is the slice's substance rather than a formatting choice.** **H — historical anchor (55 lines):** the line records a survey, probe, baseline or corpus state observed when the decision was made, so its already-implicit historical framing is made explicit and machine-recognizable. The number **stays**, because it is the evidence the decision rested on and deleting it would falsify the record — ADR-114's record-vs-revise boundary read in the direction that protects the audit trail. **R — cite the authority, not the number (11 lines):** the count duplicated a fact the record already states structurally, so the restatement is dropped. **C — fixed-cardinality constant (4 lines):** not a live population at all — a matrix's row count, a per-stage file rule, a ratified volume trigger. The lint's own docstring names this as its acknowledged residual false-positive class. Values and meaning preserved exactly; **none is anchored, because a constant is not a dated fact.** No finding was left unremediated and **nothing was suppressed** — the per-file `adr-durability: allow-anchor` marker was available on every one of these files and was deliberately **not used**: a marker leaves the prose making a live claim and silences the report, whereas an anchor changes the claim itself. | Stage-6 implementation finding (#1488 slice 6b) |
| **Δ-w22-dedupe** | The Stage-5 spec treated every R2 finding as an in-place rewrite | **Ten lines were better served by deletion than by anchoring, because the count was a *duplicate* rather than a claim.** The clearest case is `ADR-007`'s § Context category list: it restated five populations that the record's **own `source_observations:` survey** and its **own § Decision section headings** already carry verbatim (`### Standards (= 34 of 45)`, `### Specs (= 12 of 21)`, and so on). Dropping the parenthetical counts from the Context list loses **nothing** and removes five rot sites — duplicate-source-discipline's register-or-remove rule applied inside a single record. `ADR-012`'s two quotations of `ADR-007`'s clause were shortened in lockstep so the quoting record does not carry a count the quoted record no longer states; `ADR-030` point 4 now cites the hook set enumerated in its own point 1. | Stage-6 implementation finding (#1488 slice 6b) |
| **Δ-w22-countstruct** | Not foreseen at Stage 5. The File Change Matrix listed no Check-63 surface | **The baseline-differenced gate run caught a regression this slice introduced, and the fix forced one path into the matrix.** `ADR-012` line 24 is a colon-terminated count preamble sitting above a three-item list that enumerates something else, and **Check 63 (count-vs-structure) carries it in its accepted-debt register keyed on a hash of the preamble text**. Anchoring the count re-keyed the hash, the row stopped matching, and the pair became a **hard FAIL** — which is the check working exactly as designed (*"editing a count preamble is exactly the moment to re-verify its count"*). Re-verified: the count is correct and the list beneath is not an enumeration of it, so the remedy is to take the count **off the preamble** rather than anchor it; it survives in the record's own `source_observations:` and at line 36, and the finding's class moves **H → R**. With the pair no longer examined its register row went stale, and the stale arm's own stated remedy is to prune — done, with the self-describing `ENTRIES` header re-derived. The register is declared *"a DEBT REGISTER, NOT A PERMANENT EXEMPTION… expected to shrink"*; it is one row smaller. **Check 63 whole-tree: exit 0, zero FAIL, zero STALE, 423 pairs examined, in-run controls sensitivity 1/1 and specificity 1/0.** | Stage-6 implementation finding (#1488 slice 6b) |
| **Δ-w22-recall-g** | The guide documents the R2-COUNT rule as a high-precision **partial net** whose misses are a *vocabulary* problem — nouns outside the closed set | **There is a second, structural miss class the documented bound does not describe, and it is measured here rather than asserted.** `REFERENCE_PREFIX_RE` — the guard that demotes a number to an ordinal reference — carries bare single-letter alternatives `g` and `v` **with no word boundary**, so a count preceded by *any* word ending in `g` or `v` is suppressed. Measured over the swept corpus: **3** such suppressions (`addin`**`g`** ` 24 cross-references`, `containin`**`g`** ` 14 files`, `Existin`**`g`** ` 115 refs`) against **6** genuine reference-word suppressions as the control arm. All three are cleared as a **declared 3-line extension** counted separately from the 70 findings, because two sit on lines *adjacent* to lines this slice anchored and leaving them would make `ADR-002` and `ADR-003` internally inconsistent about the same figure. Post-sweep the bare-letter class measures **0** with the control arm still at **6**. **The checker is NOT modified** — the byte-unchanged control over the lint is INT-4's, and a recall gap is a finding for the rule's owner, not a licence to edit it mid-sweep. **Surfaced for Stage 9 / the R2-COUNT card:** a one-token `\b` on those two alternatives closes it, and the guide's stated bound should name the shape class alongside the vocabulary class. | Stage-6 implementation finding (#1488 slice 6b) |
| **Δ-w22-commit-shape** | The established slice shape is two commits — implementation, then plan | **Four, and each additional one is a self-caught finding rather than a split of the same work.** Implementation (70 lines) · the Check-63 re-classification and register prune · the declared 3-line extension · this plan update. Recorded so a reviewer reads the commit count as an audit trail of what the gates caught, not as churn. **No commit was amended, rebased or force-pushed.** | Stage-6 implementation finding (#1488 slice 6b) |
| **Δ-w23-adr100-shared-line** | The spec's slice split puts W2.3 (durability) in 6b and W2.4 (identity R4) in 6c, on the assumption the two populations are line-disjoint | **They overlap on exactly one line, and it is handed over rather than reached past.** `release/ADRs/ADR-100` line 6 is a `deciders:` identity field carrying **both** an R2-COUNT (`all 8 consumers`) **and** two R4 issue references. This slice edited **only** the count token — rewritten to `all event-log consumers`, which is what the record's own `source_observations:` supports — leaving both issue references **byte-identical** for 6c. Verified: R4 is emitted before the line loop and is not gated on the historical-anchor or exempt paths, so a durability anchor can never mask an identity finding; the R4 verdict is **25 before and 25 after**. | Stage-6 implementation finding (#1488 slice 6b) |
| **Δ-w24-inventory** | The Stage-5 spec sized W2.4 at **25 findings / 14 files** and instructed re-derivation at execution | **Re-derived at execution: unchanged — 25 findings across 14 files, core 16/9 and release 9/5, matching the spec's split exactly.** Third consecutive Wave-2 slice at which re-derivation confirmed the Stage-5 figure rather than superseding it. The figure the spec did not carry, and the one the remedy is actually shaped by: the 25 findings resolve to **23 distinct tokens** on **17 identity-field lines** (`ADR-092`'s `#279` appears in both `release:` and `deciders:`, so one rewrite pair clears two findings). | Stage-6 implementation finding (#1488 slice 6c) |
| **Δ-w24-remedy-classes** | The Stage-5 spec set the exit predicate (`R4 → 0`) and inherited slice 2's `## References` contract, but named no rule for *which* findings relocate | **Each token is classified to exactly one of two remedies, and the discriminator is whether the record already carries the reference in a sanctioned provenance home.** Both classes first rewrite the identity field to name the value by its slug / role / literal name, which is the authoring guide's zone-3 remedy and is what actually clears R4. **S — strike-only (13 tokens / 14 findings):** the token already survives in the record's own `source_observations:` (10) or in an existing `### Issue References` block recognized by *both* placement gates (3). Nothing is added, because a second copy would be the duplicate-source defect this release exists to remove. **R — relocate (10 tokens / 11 findings):** the token has no other home, so it moves into a `## References` block, pairing the bare number with a summary noun phrase drawn from the identity field's own description of it. **Zero information lost:** every one of the 13 struck tokens was verified still resolving inside its own record, with a working control. | Stage-6 implementation finding (#1488 slice 6c) |
| **Δ-w24-issue-references-h3** | The ratified `## References` contract is a single H2 spelled exactly, placed after `## Related ADRs` as the last section | **Three records already carry a designated reference block at H3 — `### Issue References`, nested inside `## Related ADRs` — and they were left alone rather than duplicated.** `ADR-086` / `ADR-087` / `ADR-089` each carry the relocation candidate (`#261` ×2, `#291`) on a `- #N — <summary>` line inside that H3 block. Adding a `## References` H2 for the same token would have put two reference blocks in one file; the honest remedy was strike-only, since the reference is already homed in a heading both gates recognize. **Surfaced rather than absorbed:** `### Issue References` is a *heading-normalization* divergence from the ratified contract (3 files), which is slice 6a's edit class, already closed. It is a real conformance gap against the reference-block contract and is **left for 6d to route** — it is not an R4 finding and not this slice's class. | Stage-6 implementation finding (#1488 slice 6c) |
| **Δ-w25-authoring-split** | The Stage-5 spec forecast the authoring set as **one clean single-forced-approach candidate (`ADR-092`) and 20 where evidence exists to be recalled**, on an option-vocabulary probe | **Measured record by record, the split is 21 recovered / 0 forced — and the divergence is reported rather than bent to the forecast.** Every one of the 21 names at least one specific alternative path *and* why it was not taken, each pinned to a **verbatim anchor verified present in the pre-edit record**. `ADR-092` in particular does weigh a real fork — § Context asks whether the number resolves before or after the CAS win and rejects the pre-CAS reading on two independent grounds — which the vocabulary probe could not see because the record states it without the words *option*, *alternative* or *rejected*. The section's **shape follows its evidence class**, so thin evidence is not dressed as a rich comparison: **A (8)** the record enumerates a candidate option set → a table; **B (2)** the record already carries a dedicated rejected-alternatives block → recalled into the canonical section; **C (11)** no option set, but specific paths named with reasons → a cited bullet list. Where the *decision itself* was forced (`ADR-008`'s array split, `ADR-045`'s mandated supersession, `ADR-070`'s required grammar additions, `ADR-049`'s constrained wording) the section says so plainly and records only the shaping alternatives the record evidences. | Stage-6 implementation finding (#1488 slice 6c) |
| **Δ-w25-no-fabrication-gate** | ADR-114's no-fabrication clause is *"a hard rule, not guidance"*, and AC-5′e is the one criterion in this card a machine cannot grade | **The clause was made mechanically enforceable at authoring time rather than left to reviewer discipline.** Every authored section declares a **verbatim evidence anchor** that must already exist in its own record; the applier aborts the whole run if any anchor is absent, so a section can only be written from text the record already carried. All 21 verified against the pre-edit tree, with a working control (a falsified anchor aborts; a synthetic anchor resolves in 0 of 21 base records). This does not make AC-5′e machine-gradable — a reviewer still judges whether the recalled content is faithful — but it does make *fabrication from nothing* structurally impossible, which is the half that was previously unfalsifiable. | Stage-6 implementation finding (#1488 slice 6c) |
| **Δ-w25-position** | The canonical position for `## Alternatives Considered` is H2 at position 4 (`adr-schema.md` §3), and slice 6a scoped **repositioning OUT** for records that already carried the section | **A section being *added* is inserted at its canonical position; a section that already exists is still not moved.** All 21 authored blocks sit immediately before `## Consequences`, verified with a per-file assertion, so the seven required sections stay in relative order in every one. The 27 records whose pre-existing `## Alternatives Considered` sits *after* `## Consequences` are untouched and remain out of scope — that is 6a's declared `Δ-w21-position` residual, not a defect this slice introduced. | Stage-6 implementation finding (#1488 slice 6c) |
| **Δ-w25-countstruct** | Not foreseen at Stage 5 | **Check 63 caught a regression this slice introduced — the same gate, and the same class, that caught 6b's.** The `ADR-031` section lead read *"§ Context weighs the **two** competing hook designs…"* and terminated in a colon directly above a **five-row** table, so the count-vs-structure check read it as a count preamble and hard-FAILed (`stated=[2] items=5`). The check is right: a reader would take the count as introducing the table. Repaired by removing the cardinality from the lead. **It was invisible to every ADR-scoped probe** — durability, headings, numbering, the strip-and-compare and the block bijection were all green while it was live — which is the second instance this release of a content edit correct in its own file breaking a structural consumer elsewhere. The other 20 leads were then swept for the same shape: 10 carry a number above an enumeration, all verified consistent by inspection, none colon-terminated. **Check 63 whole-tree after the fix: exit 0, FAIL 0, STALE 0, in-run controls sensitivity 1/1 and specificity 1/0.** | Stage-6 implementation finding (#1488 slice 6c) |
| **Δ-w25-anchor-prose** | 6b drained R2 to zero, so any prose this slice adds is a fresh opportunity to re-seed stale-anchor debt | **Two authored sentences carried survey-derived population figures recalled from their own record, and both were historically anchored rather than shipped bare.** `ADR-044`'s go/no-go and RAID producer counts and `ADR-089`'s flow-type and artifact-bearing-file figures now read as *"at the counts the review recorded at the time"* / *"at the current-state survey § Context cites"*. The durability lint reports **0** on both before and after, so neither was a live finding — the edit is made **because the lint's own docstring says a green gate is not evidence a record carries no live count**, and because 6b surfaced a shape-class recall gap in the same rule. Recorded so a later reader knows the anchoring was deliberate, not incidental. | Stage-6 implementation finding (#1488 slice 6c) |
| **Δ-w24-frozen** | Every completeness predicate carries the frozen-record exclusion | **`ADR-029` verified untouched across both halves of this slice**, with a working control (`ADR-006` and `ADR-030`, both in the edit set, report MODIFIED by the same check). It remains the sole `EXEMPT` row and still carries `## Options considered` and `### Reversibility` — the permanent residuals 6a declared. **`AC-5′a` therefore reads 111 / 111 sweepable and can never read 112 / 112**; the corpus-wide alternatives-family H2 form count is **1** over the sweepable population and **2** over the full glob, and both figures are correct. | Stage-6 implementation finding (#1488 slice 6c) |
| **Δ-w26-anchor-moved** | § ADR Number Derivation binds against `origin/main`, and the Wave-2 GATE names sibling PR #4568 | **The anchor MOVED mid-slice and the gate's condition fired.** #4568 merged during Engineering, landing two release ADRs on the mainline at the two numbers this release's Wave-1 records already hold. The merge union therefore carries **two duplicates**, and both pre-date this slice's first commit. This slice's own record was bound by SIMULATING the merge union against the shipped contiguity rule rather than by incrementing: the lower adjacent candidate adds a third duplicate, the higher lands a gap, and only the selected slot adds **zero** new problems — the working tree stays contiguous and the union's problem set is unchanged at the two pre-existing duplicates. **The reconciliation is Wave-1-record scope plus a branch-wide citation sweep and is escalated, not performed here.** The tooled remedy is a deliverable of this same release. | Stage-6 implementation finding (#1488 slice 6d) |
| **Δ-w26-title-derivation** | #3383 framed the index defect as missing rows, and the Stage-5 re-derivation added the stale-`Status` half | **A THIRD drift axis was measured and it is the largest: `Title`.** Comparing every indexed row against its record's own `title:`, the rows split five correct on every column, eight carrying a stale `Status`, and **nine carrying a stale `Title`** — several of them strings the record had since shortened. The projection derives `Title` from the record's `title:` field, which the schema defines as authoritative and required to match the H1, so nine index titles change in this slice. **This is a correction, not a rewrite:** the README's versions were not summaries, they were copies that drifted. Recorded because a reviewer scanning the diff will see title text change on rows nobody reported as broken. | Stage-6 implementation finding (#1488 slice 6d) |
| **Δ-w26-sc5-scope** | The ratified SC-5 reads "delta-scoped … over **added-or-changed** ADR files only" | **Narrowed to net-new-or-lost, on measurement, and the narrowing is what makes the rule admissible.** As of this slice's exit, **29 of 111** sweepable records still carry a §3 section gap this release scoped OUT with a named blocking authority. A changed-file rule would therefore fire on roughly a quarter of ADR-touching PRs against records nobody is permitted to fix — the exact guarding-before-cleaning condition ADR-114 §Decision(3) rejected, at lower volume. **R5 fires on a NET-NEW record missing a section, or a CHANGED record that has LOST one it carried at the base.** Both limbs have an empty population over the existing corpus by construction, which is the property Stage 5's own admissibility argument rests on, and together they still catch every one of the sibling records measured non-conformant. **Both scopings are reported; the rejected one is in ADR-114's amended alternatives table with its number.** | Stage-6 implementation finding (#1488 slice 6d) |
| **Δ-w26-h3-population** | Slice 6c routed **three** `### Issue References` H3 blocks to this slice (`ADR-086` / `087` / `089`), and the operator approved normalizing them | **Re-derived at execution the population is ELEVEN, and nine were normalized.** 6c measured within its own identity-reference population; a corpus-wide sweep found eight more of the identical shape. Normalizing three of nine would have shipped a completeness claim the corpus contradicts in six further places, so all nine are normalized — declared rather than absorbed, on the same basis as slice 6a's 57th operation. **Two records are deliberately NOT touched because they are a different shape, not the same defect:** in `ADR-071` the H3 sits *beneath* a correct `## References` H2 as legal sub-structure, and in `ADR-079` a separate `## References` H2 already follows it, so promoting either would create a duplicate H2. Merging those two blocks is content relocation, not heading normalization. Corpus after: **34** records carry the block, it is the last H2 in **34 of 34**, zero duplicated, and the marker ratchet did not increase. | Stage-6 implementation finding (#1488 slice 6d) |
| **Δ-w26-regex-scope** | The approved fix was "a one-token `\b`" on the bare `g` / `v` alternatives of `REFERENCE_PREFIX_RE` | **Anchored the whole word group instead, after measuring the two forms equivalent.** Over the mainline ADR corpus, **97** COUNT_RE hits clear the floor and ceiling guards; the unanchored predicate suppresses **9** and both anchored forms suppress **6** — the same three findings recovered, the same six genuine suppressions preserved. `\b` can only ever narrow a match set, so anchoring the group closes the whole shape class at **zero observed cost** rather than patching the two members that happened to be observed. The section-symbol alternative is deliberately left OUTSIDE the anchor: `§` is not a word character, so a boundary assertion before it fails against a preceding space and would disable that arm entirely. A mutation-kill arm asserts the recall case is SILENT against the pre-fix predicate. | Stage-6 implementation finding (#1488 slice 6d) |
| **Δ-w26-int4-broken** | **INT-4's control is `git diff` over `check-adr-durability.py` being EMPTY across Wave 2**, and slices 6a–6c each preserved it | **This slice BREAKS that control, deliberately and with notice.** SC-5 was ratified as a limb on the existing durability job, and the section-set constant plus its schema parser already live in that file and are self-test-pinned — so the reuse-first answer is to extend it, and a second checker would have needed a second frontmatter parser, a second frozen-record exemption, a second CI job and a second §3 citation to keep pinned. **INT-4's underlying question survives in a stronger form and should be graded that way:** the R2 verdict reached zero because the SWEEP drained the population, not because the rule was weakened — and the diff proves it, since the only R2-affecting change *widens* recall (three findings recovered, zero suppressions added). The byte-unchanged proxy is no longer available; the substantive assertion is. | Stage-6 implementation finding (#1488 slice 6d) |
| **Δ-w26-adr110-amend** | ADR-114 §Decision(3) declares structural conformance unenforced, and SC-5 contradicts it | **Amended in place, not superseded — the record is still `Proposed`, so in-place editing is what its own three-state model permits.** The amendment preserves the original rejection verbatim in force (a whole-corpus enforcing rule stays rejected, on its own stated ground) and records that the ground reaches exactly one of two possible scopings. The alternatives table gains a four-row scoping comparison so the rejected options carry their reasons. `adr-schema.md` §3.2's authority-chain row is reconciled in the same commit, because it asserted the lint "declares it does **not** enforce the set" — a statement this slice made false. **Reconciled rather than annotated.** | Stage-6 implementation finding (#1488 slice 6d) |
| **Δ-w26-title-prefix** | Not foreseen at Stage 5 | **The projector surfaced a schema-conformance residual on a different axis and does NOT fix it.** Three release records carry a `title:` with no `ADR-NNN — ` prefix, and three carry a `title:` that does not match their own H1 — both contrary to `adr-schema.md` §2. `--verify` emits a visible `NOTE` per record and uses the field verbatim; normalizing an identity field to suit a projector would be the tool dictating to the record. **Surfaced for disposition, not absorbed.** | Stage-6 implementation finding (#1488 slice 6d) |
| **Δ-w26-countstruct** | Check 63 caught a regression in each of the two preceding slices, and a generated table with a preamble is that check's trigger shape | **Ran BEFORE committing, and it is clean: exit 0, FAIL 0, STALE 0, in-run controls sensitivity 1/1 and specificity 1/0.** The region preamble was deliberately written to avoid the shape — it states no cardinality and does not terminate in a colon above the table. **The durability lint DID catch a regression this slice introduced**, in the ADR-114 amendment prose: a live corpus count in durable text. Repaired by historical anchoring, with a control confirming the rule fires on the pre-anchor line and is silent on the post-anchor one. **Third consecutive slice in which a gate, not review, caught the slice's own defect.** | Stage-6 implementation finding (#1488 slice 6d) |

---

## Rollback

Per `RELEASE_PROTOCOL.md § Rollback protocol` (operator-authorized). One branch, one PR, one merge → release-level rollback is `git revert` of the merge commit on a `fix/` branch through the normal PR path — never a force-push to `main`, never a history rewrite.

**Pre-merge, rollback is wave-granular.** The branch is built in wave order, so each wave is a contiguous commit range. Dropping Wave 2 before merge is a revert of that range or a reset to the Wave-1 tip — Wave 1 has **no** dependency on Wave 2 (all three HARD edges point *into* Wave 2, never out of it). This is a deliberate property of the sequencing: **the recommended order makes the expensive card the last and most droppable one.**

| Surface | Rollback | Tier |
|---|---|---|
| #3914 standard reconciliation + its ADR | `git revert` — prose + one additive self-test case | **CHEAP** |
| #3915, #3707 | `git revert` — documentation + spec text | **CHEAP** |
| #3708 | `git revert` — additive checker vocabulary + self-tests, warn-mode, delta-scoped | **CHEAP** |
| #3916 | `git revert`, then rebuild the ADR-authoring package or it drifts from source | **CHEAP–MODERATE** |
| #3713 + its ADR | `git revert` — but it adds a Stage-12 step other releases may adopt; reverting strands anyone mid-flight on the tooled path | **MODERATE** |
| #1488 (+#3383) | `git revert` is textually clean, **but** it restores a non-conformant corpus | **MODERATE — the binding constraint** |

**Rollback-driven design constraint — the one thing that must not ship in this release.**

**Do NOT flip the durability lint's `WARN_MODE` to enforce in this PR.** The flip is hard-gated on the conformance pass, so it is *tempting* to land both together. If it did, a revert of the sweep would leave an **enforcing gate over a corpus that just reverted to non-conformant** — every subsequent PR red, with the fix already reverted. That is a rollback trap, and the recovery would be a two-step manual operation under CI pressure. Verified as a boundary to hold rather than a scope change: **no issue in this milestone carries the flip as an acceptance criterion** — three cards mention it as forward context, none as a criterion. The flip belongs in a thin follow-up release **after** this one has soaked, where it is a one-line change with a trivial revert.

**Partial-ship safety.** Within #3914, the immutability carve-out is independently revertible from the requirement-level change — though reverting the carve-out alone would re-block the sweep.

No IRREVERSIBLE actions. **No tag is claimed by this plan** — version identity binds at the Stage-12 atomic claim, so there is no tag-rollback surface at Stage 6.

---

*Closure-phrasing note: every per-issue closure reference in this plan is written as "mark #N as closed at Stage 13" — no close-family verb is bound to an issue number, so transcription into PR bodies will not trip GitHub's auto-close parser.*
