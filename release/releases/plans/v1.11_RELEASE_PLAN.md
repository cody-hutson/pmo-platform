---
version: v1.11
date: 2026-06-11
type: plan
issues: ["#333", "#326", "#53", "#57"]
pr: null
links:
  note: null
  log_anchor: "#v1-11-cleanup-orphan-state-reliability"
reversibility-tier: CHEAP
themes: ["epic:portability-distribution", "cluster: automation"]
---

<!-- repo-integrity: allow-memory-ref -->

# v1.11 Release Plan — cleanup-orphan-state-reliability

**Milestone:** `cleanup-orphan-state-reliability` (milestone 170)
**Release identifier:** v1.11 (D-C naming, operator-rendered 2026-06-10 with the Stage 4 plan approval). Plan file `v1.11_RELEASE_PLAN.md` · branch `release/v1.11-cleanup-orphan-state-reliability` · tag v1.11 at Stage 12.
**Release Class:** `novel` (D-ReleaseClass, operator-rendered 2026-06-10). Basis: ≥1 D-class decision in the plan (4 D-gate blocks — trigger (b)); trigger (c) confirmed at Stage 5 (the liveness-oracle selection produced an ADR, filed as ADR-021 after the A-ADR renumber).
**Branch Topology:** `D-C SINGLE` — one release branch `release/v1.11-cleanup-orphan-state-reliability`; ONE Engineering spoke; serialized per-issue commits in protective-gates-first order (#333 → #326 → #53); this plan committed as Engineering Commit 0; one release PR.
**Differentiation posture (per `novel`):** Engagement density Standard; Stage 9 review depth Deep — scoped to `classify_worktree` clause precedence + liveness-oracle design + apply-phase re-evaluation; Stage 5 activation bias ALL; Stage 13 outcome-window 30-day.

**Stage 4 plan content of record:** the Stage 4 Release Planning comment on the Stage 4 planning sub-task (operator-reviewed; plan approved 2026-06-10). This file transcribes that comment verbatim as Engineering Commit 0, followed by the Decision Log (Stage 4 plan-review decision record + the Collective Review scope-lock and amendment record) and the Engineering Commit 0 Amendments (refined File Change Matrix, Deviation Log).

**Stage 5 spec of record:** the two-part combined Stage 5 Solutioning comment on the Solutioning sub-task, **APPROVED with amendments 1–3** at the operator's 2026-06-11 Collective Review scope-lock (Decision Log below). The approved spec — as amended (A-ADR, A-Fixture, A-Recheck) and extended by operator scope calls 4–5 — is the implementation spec Engineering executes.

---
## Stage 4 Release Planning — cleanup-orphan-state-reliability

### Phase A0 — Triage→Design Re-Review (per triage-design-rereview.md § 1)

```
issue_number: release-scoped (#57, #53, #333, #326)
release_milestone: cleanup-orphan-state-reliability (milestone 170; version unassigned — see D-C)
stage: 4
spoke_author: Stage 4 Release Planning spoke (release-scoped)
re_review_date: 2026-06-10
issue_body_revision: #57 2026-06-08T23:52:06Z · #53 2026-06-08T23:52:05Z · #333 2026-06-08T23:52:37Z · #326 2026-06-08T23:52:34Z
triage_decision_date: 2026-05-23 (filed/triaged, pre-re-versioning) / 2026-06 re-bundled into milestone 170 [ASSUMPTION – CONFIRM exact Stage 2 date; pre-seed record]
effort_tier: standard (size:S issues, but scope = functional engineering tool with destructive-action surface)
```

| Requirement | D1 finding | D2 finding | D3 finding | Cls | Delta/PT |
|---|---|---|---|---|---|
| #57 AC set (apply-path dead code) | decision-discipline.md § 2.1.1 audit-snapshot reconciliation applies — the issue body is a pre-seed snapshot; primitives run (see Disposition) | Current script: plain-`if` apply gate + apply-before-emit ordering + selftest guards (line cites in Disposition table) | Confirmed operator pattern: intake pre-rendered artifacts → verify + evidence + closure routing, never re-author (promoted 2026-06-09, N=2) | **C3 / PT-1** | Stale-assumption: defect no longer present. Cite: seed commit `64c9c7e` (2026-06-05) is the file's entire history; live `--self-test` PASS 2026-06-10. Tier 0 operator decision folded into Phase B plan review as **D-VerifyOnly-57** (§ Operator Decisions) per § 10 Phase 1 operator-approved protocol. |
| #53 AC set (fixed-point apply) | review-discipline root-cause: apply ORDERING (branches before worktrees) structurally guarantees the residual — stronger than the body's "enumeration timing" framing | `apply_removals` order branches→remote→worktrees (lines 484–526); `classify_local` line 206 attached-skip; `verify_apply` (587–622) re-checks REMOVED rows only | v11.19 empirical evidence (4 surviving orphan branches) retained as class evidence; pre-seed release IDs are historical record | **C1** (+C2-light) | Defect current. C2-light: regression-AC wording cites pre-seed releases (v11.19) not reproducible on the re-versioned tree → Tier 1 [ADJUST] A1. |
| #333 AC set (SELF guard) | Defense-in-depth discipline per bypass-mode-readiness.md precedent (tools self-protect); failure-mode conditional: do NOT rely on `git worktree remove` dirty-refusal when tree is clean | `classify_worktree` (241–255) has no SELF class; no self-cwd computation anywhere in script; live demo: this spoke's own worktree meets REMOVE conditions (below) | `feedback_worktree_reaped_mid_execution` memory (operator-instance) is the interim guidance this issue retires; v11.19 + v11.10 reap events | **C1** (+C2-light) | Defect current. C2-light: (a) regression-AC pre-seed release refs → ADJUST A2; (b) affected-file bullet targets "no self-destruction risk" language in hub-spoke-bridge.md § Procedure 7 — that language is ABSENT from the current tree [VERIFIED 2026-06-10: grep -n "self-destruction\|no self" over hub-spoke-bridge.md + stage-13-close.md → 0 hits] → sub-item moot, ADJUST A4. |
| #326 AC set (liveness gate) | discovery-discipline: mechanism explicitly deferred = T3 auto-fire; body's own top risk (oracle under-detection) is the release's top risk | Zero process-liveness logic in script [VERIFIED 2026-06-10: grep ps/pgrep/lsof/lock over the script → 0 hits]; classification keys on path/dirty/protected/merged only (248–251) | Operator-observed 2026-06-04 [CONTEXT]: worktree lock files may be ABSENT for live sessions; this workspace TODAY: 4 of 24 worktrees locked despite ≥2 live sessions | **C1** | Premise current; mechanism design correctly routed to Stage 5 (by the body itself). |

**G-PL1 (AC/substrate currency):** PASS with 4 Tier 1 [ADJUST] items (A1–A4, § Operator Decisions). **G-PL2 (Gate-1 crisping):** PASS — all four bodies actionable, name files, carry verifiable ACs (#53/#333 missing `### Priority` field is a recorded G1 residual from bundling; does not block planning). **Parallelization-Map currency:** suppressed — milestone 170's description predates the convention and carries no map section (standing-applicability rule, stage-04-planning.md § Parallelization-Map currency check). **A4 Cross-PR Overlap Audit:** baseline-pinned at `origin/main` = `eecbb5e` (merge of PR #634, 2026-06-10T18:29:43Z); see Risk Register R3/R4.

---

### Summary (30 seconds)

Four issues, one affected code file (`release/tools/cleanup-orphan-state.sh`, 799 lines). Current-state verification against the re-versioned seed shows **#57 is already remediated in the shipped tree** (plain-`if` apply gate, apply-before-emit ordering, self-test regression guards; live `--self-test` PASS today) → **VERIFY-ONLY**. The other three are **STILL-PRESENT defects → BUILD**: #53 (apply order local-branches → remote → worktrees with no re-evaluation = no fixed-point), #333 (no SELF action class — this spoke's own worktree is REMOVE-eligible right now), #326 (zero process-liveness logic). All three BUILD issues edit overlapping functions (`classify_worktree`, emitters, self-test), so the recommended shape is: **one release branch (D-C SINGLE), one combined Stage 5 design spec, serialized per-issue commits in protective-gates-first order (#333 → #326 → #53), one PR**. Stage 5 is REQUIRED (T3 auto-fires on #326; T4 on #53). Proposed Release Class: **novel**. Cross-PR contention on the code file is ZERO at baseline `eecbb5e`; the in-flight v1.09/v1.10 releases overlap only on Stage 12/13 append-corpus files.

### Per-Issue Disposition (BUILD vs VERIFY-ONLY)

All line numbers cite `release/tools/cleanup-orphan-state.sh` at `eecbb5e` (file history = single seed commit `64c9c7e`, 2026-06-05 — issue bodies filed 2026-05-23 predate the seed; defect CLASS verified, not cited line numbers).

| Issue | Disposition | Line-level evidence (current tree) |
|---|---|---|
| #57 — apply-mode dead code (`set -e` + `[[ ]] && echo`) | **VERIFY-ONLY** | (1) `emit_markdown` final statement is a plain `if` block (lines 426–428) with set-e-immunity comment (422–425) naming "the v1.02 apply-path no-op defect" and "Do not 'simplify' this back". (2) Apply now runs BEFORE emit: main flow lines 782–792 (`if [[ "$MODE" == "apply" ]]; then apply_removals; verify_apply; prune_remote_tracking; fi`), so an emitter failure can no longer no-op the apply phase — the defect class is eliminated structurally, not just at one line. (3) Pattern audit across the script: every `[[ ]] &&` increment carries `|| true` (335–349, 485, 499, 514, 593–613); `apply_removals`/`verify_apply`/`prune_remote_tracking` end `return 0` (528, 575, 621); `emit_json` ends in `printf` (463); top-level `[[ ]] && die/self_test` (763–769) are mid-script && lists (set-e-exempt). (4) AC3's self-test exists: `selftest_apply_path` (632–676) runs the REAL apply path against a throwaway branch, Guard 1 = emitters set-e-safe in --apply, Guard 2 = branch actually deleted; invoked from `self_test` (737). (5) [SOURCE — live run 2026-06-10, this worktree, baseline `eecbb5e`]: `--self-test` → "apply-path check PASS — throwaway branch deleted and emitters set -e-safe in --apply … verify-path check PASS … prune-path check PASS … self-test: PASS", exit 0, 2.6 s. All four ACs satisfied by the shipped tree. Per the confirmed intake-pre-rendered pattern: verification + evidence + closure routing; no re-authoring. |
| #53 — orphan branches survive same-run worktree removal | **BUILD** | (1) `apply_removals` processes LOCAL branches first (484–496), then REMOTE (498–511), then WORKTREES (513–526) — branch removal precedes worktree removal, so a branch freed by a same-run worktree removal can NEVER be caught; the ordering guarantees the residual. (2) `classify_local` line 206 marks "SKIP — active worktree attached" at detection; nothing revisits SKIP rows post-apply. (3) `verify_apply` (587–622) re-checks REMOVED rows only — it catches survivors, never promotes newly-eligible branches. (4) No re-enumeration or second pass exists; `apply_removals` is invoked exactly once (789). |
| #333 — script removes its own runtime worktree | **BUILD** | (1) `classify_worktree` (241–255) action classes: REMOVE / "SKIP — primary checkout" (248) / "SKIP — uncommitted changes" (249) / "SKIP — protected branch" (250) / "SKIP — branch not fully merged" (251). No SELF class. (2) No self-cwd computation anywhere: the only `git rev-parse` calls are `--verify HEAD` inside self-tests (634, 686); `--show-toplevel` appears nowhere. (3) [SOURCE — live workspace 2026-06-10]: this spoke's own worktree (`.claude/worktrees/agent-a129…`, clean, branch fully merged at `eecbb5e`) satisfies every REMOVE condition under `--historical`/`--all` scope — the running session's cwd is classified REMOVE today. (4) Useful invariant for the design: the apply loops act only on exact `action == "REMOVE"` (488, 503, 518), so a new SELF class is automatically inert in apply, including under `--force` — the guard belongs in classification. |
| #326 — no process-liveness gate | **BUILD** | (1) [VERIFIED 2026-06-10: `grep -nE "ps |pgrep|lsof|lock"` over the script → 0 process-liveness hits] — eligibility derives only from branch-name patterns (`detect_*`, 257–315), merged-state (`is_fully_merged`, 151–156), dirty-check (`worktree_is_clean`, 181–185), attached-test (`branch_has_worktree`, 167–170). (2) Live-host evidence for the body's own top risk: only 4 of 24 worktrees carry git locks despite ≥2 concurrently live sessions (this spoke + the v1.10 Stage 9 spoke) — a lock-keyed oracle under-detects, re-introducing the reap risk [CONTEXT, operator-observed 2026-06-04: the validated liveness primitive on this host is `lsof -a -d cwd -p <pid>` over live claude PIDs]. Recorded here as Stage 5 design input — mechanism design stays at Stage 5 per the issue body. |

### Dependency Graph

**Hard blocking edges inside the milestone: none.** All four bodies declare "Dependencies: none"; native links are composes-with. [SOURCE: issue bodies + roadmap-deps blocks]

**Soft design-composition edges (directional, drive sequencing):**
- #333 → #326: SELF is the degenerate "self is live" case but must remain a DISTINCT action class (deterministic, oracle-independent — defense-in-depth when the liveness oracle under-detects). The two new `classify_worktree` clauses and their precedence order are one design decision.
- (#333 + #326) → #53: the re-evaluation pass must preserve the invariant "only worktrees actually REMOVED free their branches" — SELF/live/dirty-skipped worktrees keep their branches attached-and-skipped.
- #57 (shipped) → constrains all three: set-e discipline, apply-before-emit ordering, and the selftest guards are regression surfaces every edit must preserve (#53 touches exactly the control flow #57 hardened).

**Cross-milestone (downstream, all verified OPEN 2026-06-10):** completing this milestone unblocks the deploy-decomposition wave (#341, #45, #44, #43, #42 list #57 in their Blocks chain) and #257 (blocked by #326). No upstream blockers.

### Implementation Sequence

Single Engineering pass on one release branch, serialized per-issue commits, protective-gates-first:

| # | Commit | Content | Why this position |
|---|---|---|---|
| 0 | Engineering Commit 0 | Release plan file `release/releases/plans/<version>-cleanup-orphan-state-reliability_RELEASE_PLAN.md` (this plan, transcribed) | D-C SINGLE topology convention |
| 1 | #333 SELF guard | `script_worktree` computation + SELF clause in `classify_worktree` + SELF row/count in both emitters + self-test fixture | Smallest, fully-specified, purely additive; establishes the new action-class reporting pattern the next commits reuse |
| 2 | #326 liveness gate | Liveness-oracle helper (per Stage 5 design) + live-session clause adjacent to SELF + distinct skip label + synthetic-live-holder fixture | Lands the second protective gate before removal scope expands; reuses commit 1's reporting pattern |
| 3 | #53 fixed-point apply | Post-worktree-removal re-evaluation (option A/B per Stage 5) + pass-2 verify coverage + fixed-point fixture | Most invasive (apply-phase control flow); written once against final classification semantics; its fixture validates the composed behavior |
| 4 | Docs | Conditional edits: `release/references/pipeline/stage-13-close.md` Phase C4 choreography, `core/rules/git-workflow.md` § Step 10 phase description (Stage 5 confirms exact scope) | After behavior is final |

Rationale (M2-tested): #53 EXPANDS per-run removal aggressiveness; landing SELF + liveness first means the expanded removal logic is born inside the protected envelope. This supersedes the archived design-composition note on #333 ("corrective fixes first, guard last") — that note's premise included #57 as a pending corrective fix, which is now shipped. #57 contributes no commit; its verification evidence is captured at Stage 7 on the post-edit branch.

### Stage Applicability Matrix

**Stage 5 activation (T1–T6 per core/standards/planning-solutioning-handoff.md § 3–4):**

| Issue | T1 | T2 | T3 | T4 | T5 | T6 | Verdict | Rationale |
|---|---|---|---|---|---|---|---|---|
| #57 | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ | (verify-only) | No build; enters the combined spec only as a regression-constraints section |
| #53 | ✗ | ✗ | ✓ | ✓ | ✗ | ✗ | ACTIVATE | T4: body enumerates options A/B/C with trade-offs; T3: apply-phase contract change (re-evaluation semantics constrain report + verify behavior) |
| #333 | ✗ | ✗ | ✓ | ✗ | ✗ | ✗ | ACTIVATE | T3: new action-class contract in markdown+JSON output (consumed by chip prompts) + path-normalization decision (`pwd -P` vs `git worktree list` path forms, macOS `/private` symlink prefix) |
| #326 | ✗ | ✗ | ✓ | ✓ | ✗ | ✓ | ACTIVATE | T3 AUTO: literal "deferred to Stage 5" framing; T4: oracle candidates (lock-holder∩process-table vs `lsof`-cwd vs mtime heuristics); T6: portability of the oracle is uncertain |
| **Release rollup** | | | | | | | **ACTIVATE** | Any ✓ on any issue → release-wide activation (all-or-nothing, § 2) |

**Per-stage applicability (5–13):**

| Stage | #57 (VERIFY-ONLY form) | #53 | #333 | #326 |
|---|---|---|---|---|
| 5 Solutioning | Converts: regression-constraints section in the combined spec (no design work) | YES — option A/B/C selection + invariant design | YES — clause precedence + path normalization + report schema | YES — REQUIRED; oracle mechanism TBD by the issue body itself |
| 6 Engineering | Converts: no commit; evidence capture only | YES (commit 3) | YES (commit 1) | YES (commit 2) |
| 7 Dev Testing | Converts: re-run `--self-test` on post-edit branch; capture PASS evidence (R3 evidence requirement) | YES — fixed-point fixture + live-workspace check | YES — SELF fixture incl. `--force` survival | YES — synthetic live-holder fixture + real-session check |
| 8 QA | Converts: evidence-completeness check vs the 4 ACs | YES | YES | YES |
| 9 Plan Review | GATE — release-scoped, operator GO/NO-GO (depth per D-ReleaseClass) | ← same | ← same | ← same |
| 10 Dry Run / 11 Snapshot | Compressed for git-native releases: PR diff IS the dry-run; git history IS the snapshot [SOURCE: core/rules/harness-deployment.md § Cross-Reference] | ← | ← | ← |
| 12 Execute | Merge + post-merge verification; then the #333 operator-instance memory retirement step (sequenced AFTER verification — see Rollback) | ← | ← | ← |
| 13 Close | Mark #57 as closed with the verification-evidence comment (manual closure; PR carries References-only) | Mark #53 as closed at Stage 13 | Mark #333 as closed at Stage 13 | Mark #326 as closed at Stage 13 |

Phase C4 note: this release's own Stage 13 orphan cleanup dogfoods the NEW script — its first live exercise (the SELF guard protects the Stage 13 spoke's own worktree).

### Contention Map

Single shared file; function-level claims (current line ranges at `eecbb5e`):

| Region | #53 | #333 | #326 | #57 (shipped — regression surface) |
|---|---|---|---|---|
| Header docs/usage (1–57) | edit (fixed-point semantics) | edit (SELF doc) | edit (liveness doc) | preserve apply-phase doc (26–34) |
| Globals + helpers (59–199) | possible tracking accumulator | ADD `script_worktree` computation | ADD liveness-oracle helper | — |
| `classify_worktree` (241–255) | consumes final semantics | **ADD SELF clause** | **ADD live-session clause** | — |
| `detect_*` (257–315) | possible re-evaluation entry point | — | — | — |
| `emit_markdown` (319–429) | pass-2/resolved counters | SELF row + summary count | distinct liveness-skip label | last-statement plain-`if` (422–428) MUST survive |
| `emit_json` (431–464) | same | `"action":"self"` | distinct skip reason | set-e-safe `printf` ending |
| `apply_removals` (476–529) | **re-evaluation pass / ordering** | inert (acts on REMOVE only) | inert (same) | `return 0` + PASS/FAIL log discipline |
| `verify_apply` (587–622) | verify pass-2 removals | — | — | AC4 survivor reclassification |
| self-test (624–742) | fixed-point fixture | SELF fixture | live-holder fixture | `selftest_apply_path` + `selftest_verify_and_prune` preserved |
| main flow (766–799) | apply-block sequencing (788–792) | — | — | apply-before-emit ordering preserved |

**Hot spots:** (1) `classify_worktree` — #333 + #326 insert adjacent clauses; precedence order (primary → SELF → live → dirty → protected → not-merged, or variants — e.g. does a dirty SELF tree report SELF or dirty?) is ONE design decision → combined Stage 5. (2) Both emitters — three issues add rows/labels/counters to the same functions. (3) Self-test region — three fixtures appended (append-pattern, low conflict). Serialized commits on one branch reduce all three hot spots to ordinary sequential edits.

### File Change Matrix

Engineering scope (release branch; machine-readable for Stage 7/8/9 chip extraction):

```
release/tools/cleanup-orphan-state.sh
release/releases/plans/v1.11-cleanup-orphan-state-reliability_RELEASE_PLAN.md
release/references/pipeline/stage-13-close.md
core/rules/git-workflow.md
```

- `release/tools/cleanup-orphan-state.sh` — EDIT (all three BUILD issues; the only code file).
- `release/releases/plans/v1.11-cleanup-orphan-state-reliability_RELEASE_PLAN.md` — ADD (Engineering Commit 0; version prefix is [ASSUMPTION – CONFIRM] — v1.09/v1.10 are claimed by in-flight releases, v1.11 is next; hub assigns at scaffolding and renames the path token accordingly).
- `release/references/pipeline/stage-13-close.md` — EDIT, conditional (Phase C4 "detach BEFORE invoking --apply" choreography simplifies once SELF ships; Stage 5 confirms exact wording; #333's original doc target — the "no self-destruction risk" assertion in hub-spoke-bridge.md — is already absent from the current tree, so hub-spoke-bridge.md is NOT in the matrix).
- `core/rules/git-workflow.md` — EDIT, conditional (§ PR Process Step 10 describes the apply phases; gains the re-evaluation pass + SELF/liveness skip vocabulary if Stage 5 lands options A/B for #53).
- Stage 12/13 close-out appends (NOT release-branch scope; listed for the cross-release contention picture): `release/releases/RELEASE_LOG.md`, `RELEASE_INDEX.md`, `RELEASE_DIGEST.md`, `release/releases/notes/<version>_RELEASE_NOTES.md`, `CHANGELOG.md`.
- DELETE intent: none in-repo. One operator-instance retirement at Stage 12/13 per #333's body: the `feedback_worktree_reaped_mid_execution` memory file (workspace-owner home dir) — both its named preconditions (self-cwd protection AND cross-session detection) ship together in this release.

### Risk Register

| # | Risk | Severity | Evidence | Mitigation |
|---|---|---|---|---|
| R1 | Single-file contention: 3 BUILD issues edit overlapping functions | MED | Contention map | D-C SINGLE; ONE engineering spoke (or strictly serialized spokes); combined Stage 5 spec; `--self-test` re-run at every commit. Confirmed pattern cited: concurrent same-branch Stage 6 spokes produce 4 git-race classes (promoted N=4) — avoided by serialization, not recovery |
| R2 | Liveness-oracle under-detection re-introduces the reap risk (#326's own named risk) | **HIGH** | 4/24 worktrees locked despite ≥2 live sessions TODAY; lock files may be absent for live sessions [CONTEXT, operator-observed 2026-06-04] | Stage 5 design inputs: do NOT key the oracle on lock files; validated host primitive is `lsof -a -d cwd -p <pid>` over live claude PIDs; fail-closed (oracle unavailable → conservative SKIP); SELF guard stays oracle-independent (defense-in-depth) |
| R3 | Concurrent-release divergence: v1.09 (Stage 9 open) + v1.10 (sub-tasks #625/#626/#627 open) merge to main mid-window | MED | [VERIFIED 2026-06-10: `git diff --name-only origin/main...origin/release/v1.09-…` → plan file only; `…origin/main...f80b418` (v1.10 local tip, 1 ahead) → plan file only; post-seed main history for the cleanup script = seed commit only] → ZERO overlap on this release's code file at baseline | Baseline pinned (below); Stage 7/8 entry warn-checks + Stage 9 Phase A6.5 re-check catch later divergence; only shared surface is Stage 12/13 append-corpus (append-pattern class — structurally HIGH, operationally LOW per ADR-005) |
| R4 | Audit baseline rests on an EMPTY open-PR population | LOW | `gh pr list --state open` → 0 rows at audit time | Per the audit-baseline discipline: baseline = `origin/main` @ `eecbb5e` (PR #634 merge, 2026-06-10T18:29:43Z); bounded window = last 12 merged PRs (#522–#634, 2026-06-07→06-10, none touch `release/tools/`); population re-checked at Stage 7/8/9 entry |
| R5 | Fixture realism: a synthetic live-holder fixture could pass while the real oracle misses live sessions | MED | Same lock-absence evidence as R2 | Stage 7 adds a REAL-session check: classify the live workspace in dry-run while ≥1 sibling session is live; assert its worktree classifies live-SKIP (this workspace reliably has live sibling sessions) |
| R6 | #53 re-evaluation could loop unboundedly against racing sessions | LOW-MED | Fixed-point phrased as "common case" in the AC | Bound to exactly ONE re-evaluation pass (worktree removal frees branches; branch removal frees nothing further) — Stage 5 encodes the bound |
| R7 | Rollback + dogfood wrinkle: this release's own Stage 13 runs the NEW script; operator-instance memory retired at Stage 12/13 | LOW | Stage 13 Phase C4 invokes the script | Rollback = single `git revert -m 1 <merge-sha>` (one code file + docs; no data migration; append-corpus rows stay as history) — CHEAP. Memory retirement sequenced AFTER post-merge verification; if a revert follows retirement, guidance text is reconstructible from the #333 body — MODERATE residual |
| R8 | Classification cost at scale: ~2 `gh` calls per branch; 107 local branches on this host → 200+ calls per `--all` run | LOW (this release) | Script lines 212–215, 231–235; current workspace branch count | Out-of-scope for these 4 issues; noted in Recommendations (batch-prefetch candidate); Stage 7 live checks use scoped runs (`--spawn-task`/`--release-close`) to stay cheap |
| R9 | Silent regression of SELF/liveness inertness-in-apply if a future refactor changes the `action == "REMOVE"` exact-match | LOW | Apply loops key on exact REMOVE (488/503/518) | #333 AC3 fixture asserts SELF + `--apply --force` survival explicitly; #326 AC2 asserts live-held set unchanged across apply |
| R10 | Coverage gap (discovery): 51 `worktree-agent-*` harness branches match NO sweep pattern except `--historical` | LOW | Branch listing on this host; `detect_spawn_task` scans `claude/*` only (282–294) | Out of scope here — flagged in Recommendations for operator routing (possible Stage 5 scope note on #326 or separate intake) |

### Operator Decisions (D-Gate blocks)

#### D-ReleaseClass: What Release Class does this release carry?
**Gate input:** Spoke-proposed class + trigger-condition evidence per release/references/specs/release-class-taxonomy.md. Milestone 170's description carries NO `## Release Class` H2 yet (Stage 3 predates the convention's enforcement on this bundle) — operator decision here lands it via the milestone-description PATCH.
**Gate decision:** Choose between (A) routine, (B) novel, (C) cross-cutting, (D) hotfix.
**Recommendation: novel** — trigger (b) fires (this plan carries ≥1 D-class decision: 4 blocks here); trigger (c) likely (liveness-mechanism selection is an architectural choice that may warrant a Stage 5 ADR per decision-discipline § 3); and the liveness gate is a first-of-kind protective protocol for platform tooling (design uncertainty is the *subject* of #326). cross-cutting does not fire (1 tool file; no pipeline-stage-file ≥3 / governance-surface ≥3). hotfix does not fire (no P1/P2-against-deployed; 4 issues; corrective+protective mix). routine counter-read (M2 Opposing View): the D-decisions here are routing-class, not protocol-class ("ticket-newness is not protocol-newness" anti-pattern) — if Stage 5 yields no ADR and the design reduces to mechanical clause insertion, routine was right; evidence that would confirm: an empty Stage 5 open-question register. Operator may re-classify stricter-to-cheaper later with explicit risk acceptance (MODERATE per the Re-Classification Protocol). M3 Pattern Cache: no confirmed pattern bears on class selection; the intake-pre-rendered pattern bears on #57's disposition, not the class.
**Differentiation posture (if novel):** Engagement density Standard; Stage 9 review depth Deep — scoped to the `classify_worktree` clause-precedence + oracle design + apply-phase re-evaluation surface; Stage 5 activation bias ALL (already ACTIVATE); Stage 13 outcome-window 30-day (fixed-point + no-reap properties prove out across subsequent releases' Stage 13 runs).
**Domain practice provenance (Phase A1.5):** `domain_practice: { source: N/A — pipeline-internal release, date: 2026-06-10, domain: software }` — A3 classification rationale: the File Change Matrix is a bash tool under `release/tools/` plus pipeline-doc edits (secondary: governance); sourcing-exempt as pipeline-internal, domain-classified `software`.
**Blocks:** milestone-description Release Class section; downstream per-class posture (Stage 9 depth).
**Upstream compatibility:** N/A — Release Class is PMO platform internal taxonomy; no Anthropic upstream surface. Upstream compatibility check does not apply.
**Reversibility / Confidence:** CHEAP / MEDIUM-HIGH (re-classifiable per Re-Classification Protocol).

#### D-C: Branch Topology — SINGLE vs OPTION-A
**Gate input:** Contention map (3 BUILD issues × overlapping functions × 1 file); confirmed pattern (M3): concurrent same-branch spokes → 4 git-race classes (promoted N=4); concurrent releases v1.09/v1.10 in flight.
**Gate decision:** (1) SINGLE — one release branch, ONE engineering spoke producing serialized per-issue commits, one PR [RECOMMENDED]; (2) OPTION-A — per-issue branches + per-issue PRs.
**Why SINGLE (M1 localization):** generic heuristic says "independent issues → parallel per-issue PRs"; localized context overrides — all three BUILD changes edit the same functions, so OPTION-A produces 3 PRs in rebase-chain conflict on one file AND puts intermediate tool states (a half-protected destructive tool) on main between merges. SINGLE serializes naturally and ships the composed safety model atomically; rollback = one revert. What would invalidate: if operator splits #53 out to a later release (then 2 remaining issues still share `classify_worktree` — SINGLE still wins). M2 Opposing View: OPTION-A gives smaller review units; dismissed — Stage 9 Deep review scoped to one file is tractable, and the inter-commit boundaries give per-issue reviewability inside one PR.
**Version naming:** [ASSUMPTION – CONFIRM] `release/v1.11-cleanup-orphan-state-reliability` (v1.09/v1.10 claimed by in-flight releases).
**Blocks:** Procedure 1 scaffolding; Engineering Commit 0 placement.
**Upstream compatibility:** N/A — branch topology does not modify skill-authoring surface. Upstream compatibility check does not apply.
**Reversibility / Confidence:** CHEAP / HIGH.

#### D-Stage5-Topology: combined design spec vs per-issue Stage 5 spokes
**Gate input:** All-or-nothing ACTIVATE verdict; hot-spot analysis (clause precedence in `classify_worktree` spans #333+#326; #53's invariant depends on both); bundling record (archived comments on #53/#333 explicitly anticipated "a single Stage 5 Solutioning pass to design coherent remediation").
**Gate decision:** (1) ONE combined Stage 5 spoke producing one design spec covering #53+#333+#326 with a #57 regression-constraints section [RECOMMENDED]; (2) default per-issue Stage 5 spokes (3 specs).
**Why combined:** the load-bearing design decisions are CROSS-issue (clause precedence; report-label vocabulary; apply-phase invariant; one self-test architecture for 3 fixtures). Per-issue specs would each re-derive the same decisions with drift risk. The Phase A6.5 adversarial review then fires once over the composed design — reviewing exactly the interaction surface. Mitigation for traceability (M2): the combined spec MUST carry a per-issue AC-mapping section so Stage 7/8 can verify per-issue. M3: no confirmed pattern opposes; the single-file-contention orientation supports.
**Blocks:** Stage 5 chip creation at Procedure 1.
**Upstream compatibility:** N/A — does not modify skill-authoring surface. Upstream compatibility check does not apply.
**Reversibility / Confidence:** CHEAP / HIGH.

#### D-VerifyOnly-57: confirm VERIFY-ONLY disposition + closure routing
**Gate input:** Disposition evidence above (line cites + live `--self-test` PASS at `eecbb5e`, 2026-06-10); Phase A0 C3/PT-1 classification (Tier 0 decision rendered HERE per Phase 1 operator-approved protocol). M3 Pattern Cache: confirmed operator pattern — intake pre-rendered artifacts → verify + evidence + closure routing; D-NO-RE-AUTHORING; R3 evidence still required (promoted 2026-06-09, N=2) — this is its third instance if confirmed.
**Gate decision:** (1) VERIFY-ONLY confirmed; PR carries `References #57` (no close-family verb); Stage 7 captures fresh post-edit self-test evidence; evidence comment posted on #57; mark #57 as closed at Stage 13 [RECOMMENDED]; (2) VERIFY-ONLY confirmed but auto-closed at merge via the PR's Issue References block; (3) reject — operator sees a residual defect (state it; returns #57 to BUILD).
**Why (1) over (2) (M1):** the PR lands no #57-specific diff; the closing artifact is the verification evidence, not the merge — References-only keeps the Development-sidebar link honest and matches the git-workflow rule for PRs that do not implement the issue. M2 Opposing View: "a remaining instance of the `[[ ]] &&` pattern lurks elsewhere in the script" — tested and dismissed: full-script pattern audit (evidence item 3 in the Disposition table) + live self-test PASS; would-confirm evidence: any emitter returning non-zero in --apply during Stage 7 re-run.
**Ripple:** milestone 170 description's "Today" paragraph still asserts the apply-dead-code defect — ADJUST A3 below.
**Blocks:** Procedure 1 (whether a #57 build sub-task chain is scaffolded); PR-body Issue References composition.
**Upstream compatibility:** N/A — does not modify skill-authoring surface. Upstream compatibility check does not apply.
**Reversibility / Confidence:** CHEAP / HIGH.

#### Tier 1 [ADJUST] queue (operator approves; hub applies via `gh issue edit` / milestone PATCH)
- **A1 (#53):** regression-AC bullet citing "v11.19-style historical cleanup" (pre-seed, not reproducible) → refine to: "run `--historical --apply` against a fixture workspace; assert zero orphan `claude/*` branches survive from same-run-removed worktrees."
- **A2 (#333):** same class — "v11.19-style … AND v11.10-style release-close cleanup" regression bullet → fixture-based + live-workspace dry-run form.
- **A3 (milestone 170 description):** "Today" paragraph asserts the apply-dead-code defect; refresh to reflect #53/#333/#326 only once D-VerifyOnly-57 renders.
- **A4 (#333):** affected-file bullet targeting the "no self-destruction risk" language in hub-spoke-bridge.md is moot — language absent from current tree [VERIFIED 2026-06-10, grep → 0 hits]; redirect the doc intent to stage-13-close.md Phase C4 (conditional, Stage 5 confirms).

### Verification Plan + Rollback Strategy

**Verification (per issue AC → mechanism):**
- **#333:** self-test fixture — `git worktree add` temp branch → invoke classification with cwd inside it → assert action = SELF; assert survival under `--apply` AND `--apply --force`; assert markdown row + `"action":"self"` JSON + distinct summary count; primary-invocation no-op check (AC4).
- **#326:** synthetic live-holder fixture (self-test, exit 0); report labels three distinct skip reasons (grep); Stage 7 real-session check — dry-run classification of the live workspace while a sibling session is live, assert live-SKIP; before/after live-held set unchanged across an `--apply` (AC2).
- **#53:** fixture — 1 clean worktree on a merged throwaway branch → `--apply` → assert the branch is gone in the SAME run; current-workspace regression (post-ADJUST A1 form).
- **#57:** re-run `--self-test` on the post-edit release branch; capture PASS output as the R3 evidence comment (baseline PASS already captured 2026-06-10 at `eecbb5e`, 2.6 s).
- **Cross-cutting:** `--self-test` re-run at EVERY commit on the branch (cheap; guards the #57 regression surface while #53 rewires the apply block); Stage 8 QA produces the AC-by-AC evidence table + emitter schema checks (JSON parses; tables well-formed).
- **Stage 12 post-merge:** dry-run from inside a worktree on the live workspace; confirm SELF + live-session rows; only then any operator-approved apply; only then the #333 memory-retirement step.

**Rollback:**
- Pre-merge: delete the release branch (no shared state).
- Post-merge: single `git revert -m 1 <merge-sha>` — one code file + doc edits revert cleanly; no data/state migration; close-out corpus rows are append-only history. **Reversibility: CHEAP / HIGH.**
- Sequencing guard: the operator-instance memory retirement (#333 ship-step) runs only AFTER post-merge verification; if a later revert occurs, the guidance is reconstructible from the #333 body (MODERATE residual, accepted).
- Dogfood guard: if the new script misbehaves at this release's own Stage 13 Phase C4, halt cleanup, revert, re-run cleanup on the reverted script.

### Recommendations

1. **Approve the plan with D-gates rendered:** D-ReleaseClass (novel), D-C (SINGLE + v1.11 naming), D-Stage5-Topology (combined), D-VerifyOnly-57 (option 1), ADJUST queue A1–A4. Then Procedure 1 scaffolding (per-issue sub-tasks; ONE combined Stage 5 chip if D-Stage5-Topology lands as recommended; no #57 build chain).
2. **Release Outcome Statement (draft for Phase B1):** "A single `cleanup-orphan-state.sh --apply` run executes its removals, reaches fixed-point in one pass, and never removes a worktree that is the script's own cwd or is held by a live session — verified by self-test fixtures and a live-workspace regression at Stage 12, with the apply-path hardening confirmed shipped and evidenced." [RECOMMENDED — hub formats per release-outcome-statement-template.md at the B1 gate.]
3. **Out-of-scope discoveries (hub routes; not filed by this spoke per spoke scope):**
   - **Coverage gap:** 51 `worktree-agent-*` harness branches match no sweep pattern except `--historical` (the `--spawn-task` scope scans `claude/*` only) — candidate Stage 5 scope note on #326 or separate intake.
   - **Performance:** classification issues ~2 `gh` calls per branch (107 branches here → 200+ calls per `--all` run) — batch-prefetch (`gh pr list --state all --json headRefName,number,state` once) is a clean follow-up.
   - **Deploy wiring check:** `core/config/allowlists/cleanup-protect-list.txt` exists in-repo and the runtime instance exists on this host (2026-06-05), but `install.sh` / `core/deploy/deploy.sh` greps show no reference to deploying it — verify the seeding path before relying on protect-list semantics on fresh installs [ASSUMPTION – CONFIRM: may be wired via a directory-level copy not traced here].
   - **Stage-13 doc nuance:** stage-13-close.md Phase C4 says the spoke's own worktree is auto-skipped under `--release-close` scope — true for that scope, but the `--spawn-task`/`--all` paths documented in git-workflow.md § Step 10 remain exposed until #333 ships (no doc change needed if this release lands; noted for review sequencing only).
4. **Stage 5 design inputs to carry forward:** clause precedence in `classify_worktree` (does SELF outrank dirty?); oracle = NOT lock-file-keyed; `lsof -a -d cwd -p <pid>` host-validated primitive [CONTEXT]; fail-closed on oracle-unavailable; ONE bounded re-evaluation pass for #53; preserve the `action == "REMOVE"` exact-match invariant; per-issue AC-mapping section mandatory in the combined spec.

### Model Provenance

- **Invocation model parameter:** `fable` (passed explicitly by hub per the workspace model-preference rule — highest-tier, version-agnostic; supersedes the bridge doc's era-specific `opus` literal)
- **Agent-definition default:** N/A — no .claude/agents/pmo-*.md definitions exist in this deployment; spoke runs as general-purpose with persona embedded
- **Parent-session model:** claude-fable-5[1m] (Fable 5, 1M context) per runtime session metadata
- **Designated-model match:** YES — fable family is the workspace's designated highest tier

### Issue References

References #57, #53, #333, #326 (the four Milestone issues this plan covers — dispositions: #57 VERIFY-ONLY; #53, #333, #326 BUILD). References #635 (this Stage 4 sub-task). Cross-milestone context: References #625, #626, #627 (concurrent-release sub-tasks, divergence risk R3); References #341, #45, #44, #43, #42, #257 (verified-OPEN downstream dependents unblocked by this milestone).


---

## Decision Log

### Stage 4 Plan Review (Phase B1) — rendered 2026-06-10, main-thread chat (operator)

**Decision:** Release plan **APPROVED as recommended** (operator, 2026-06-10, rendered in main-thread chat).

**D-gates rendered:**
- **D-ReleaseClass: novel** — Stage 9 review depth Deep, scoped to `classify_worktree` clause precedence + liveness-oracle design + apply-phase re-evaluation; engagement density Standard; Stage 13 outcome-window 30-day.
- **D-C Branch Topology: SINGLE** — branch `release/v1.11-cleanup-orphan-state-reliability`; one Engineering spoke; serialized per-issue commits in protective-gates-first order (#333 → #326 → #53); one release PR.
- **D-Stage5-Topology: COMBINED** — one Stage 5 design spec covering #53 + #333 + #326 with a #57 regression-constraints section; per-issue AC-mapping section mandatory; one adversarial design review over the composed spec.
- **D-VerifyOnly-57: Option 1** — verification + evidence + closure routing; release PR carries `References #57` only; fresh post-edit self-test evidence at Stage 7; evidence comment on #57; manual closure at Stage 13.

**ADJUST queue approved (Tier 1):** A1 (#53 regression-AC refresh), A2 + A4 (#333 regression-AC refresh + moot doc-target redirect), A3 (milestone description "Today" refresh — folded into the Outcome Statement / Release Class PATCH).

**Hub empirical verification:** 8 spoke claims verified-matches (line-level reads of `apply_removals` / `classify_worktree` / `emit_markdown`; `--self-test` PASS independently reproduced at `eecbb5e`, exit 0; zero file overlap with in-flight v1.09/v1.10 branches confirmed by direct diff; v1.11 confirmed next in version sequence).

**Risk acceptance:** R2 (liveness-oracle under-detection, HIGH) routed to Stage 5 design inputs — oracle must not key on lock files, host-validated primitive `lsof -a -d cwd -p <pid>`, fail-closed on oracle-unavailable. R7 (memory-retirement residual, MODERATE) accepted with retirement sequenced after post-merge verification.

**Outstanding items:** Out-of-scope discoveries (worktree-agent-* sweep coverage gap; per-branch gh-call cost; protect-list deploy wiring) — hub files/routes as separate intake per the auto-logging rule; not added to this Milestone per Scope Control.

### Collective Review Scope-Lock + Amendment Record — rendered 2026-06-11, main-thread chat (operator)

**Decision:** Combined design **APPROVED with amendments 1–3** (operator, 2026-06-11, rendered in main-thread chat). Scope hard-locked through Stage 9. Engineering authorized (Tier 3 post-scope-lock per the Stage-Conditional Launch Policy). Engineering implements the spec AS AMENDED here.

#### Amendments (operator-required)

1. **A-ADR (from adversarial PRF-2):** The liveness-oracle ADR is **ADR-021**, not ADR-020 — v1.09 (merged 2026-06-11, before this scope-lock) filed `core/ADRs/ADR-020-agent-script-promotion-ladder.md`. Apply everywhere: file name `release/ADRs/ADR-021-liveness-oracle-selection.md`, title, frontmatter, `release/ADRs/README.md` index row, cross-numbering row. **Stage 6 entry step:** re-verify the highest ADR number across `core/ADRs/ release/ADRs/` immediately before filing (the number is a shared mutable sequence resource file-overlap diffs cannot see).
2. **A-Fixture (from adversarial PRF-4/FMF-2/CDF-3):** `selftest_fixed_point` bases its throwaway branch at `base=$(git merge-base HEAD "origin/main")` (existing loud-SKIP guard when empty) — NOT at `origin/main`. Merge-base is an ancestor of BOTH baselines: REMOVE-eligible (0 unique commits vs origin/main) AND `git branch -d`-deletable from any invoking checkout. Removes the dominant environmental flakiness from the per-commit gate.
3. **A-Recheck (from adversarial PRF-1/FMF-1/CDF-1 — adopted):** Apply-time liveness re-verification. Rebuild the liveness map once at the worktree-apply-loop entry (after the branch/remote loops in `apply_removals`) and re-check `worktree_is_live "$path"` for each worktree row still `REMOVE`; a fresh hit rewrites the row in place to `SKIP — live session (${LIVE_HIT})` and the loop continues; oracle-unavailable at re-check time converts residual REMOVEs fail-closed (same label). **Correct the spec/ADR backstop sentence** — git porcelain does NOT refuse removal of a clean live-held tree and `verify_apply` detects survivors, never wrongful removals; with the re-check, the residual window is the seconds between re-check and porcelain call. **ADR-021 Consequences additions:** (a) non-covered holder classes — open-fd holders and absolute-path operators whose cwd is elsewhere; (b) the self-canary is necessary-not-sufficient (witnesses scan-ran, not scan-complete); (c) 30-day outcome-window monitoring criterion — any reap incident where the affected session's cwd was outside the removed tree re-triages the oracle definition toward the all-fd predicate (the adversarial CDF-2 why-not record carries the cost/starvation rationale).

#### Operator scope calls (rendered at the same gate)

4. **Dedup guard → commit 1:** skip a worktree path already present in `WORKTREE_CANDIDATES` (one loop + plain-`if`, RC-conformant) — keeps the new SELF/live counters truthful under the default `--all` scope (pre-existing duplicate-row defect verified live: 6 paths × 2 rows).
5. **Fail-closed counter → commit 2 (adversarial FMF-5):** count fail-closed conversions (`fcc`); Summary suffix becomes `— liveness oracle UNAVAILABLE (fail-closed; $fcc removal(s) blocked)`; plain-`if` Totals line gated on `fcc > 0`.

#### Ratified as-spec'd

R-4 (full-label JSON action values incl. `SELF — script's own runtime worktree (protected)`), R-5 (primary-clause robustification folded into commit 1), R-6 (live skip is force-immune; operator release path = quit the holder), R-7 (ADR ships as a repo FILE in the release PR; acceptance = this scope-lock), R-8 (self-test budget ~15 s accepted; per-commit re-run discipline retained).

#### Deviation log rows (transcribed per the scope-lock instruction)

| # | Item | Disposition |
|---|---|---|
| DL-1 | JSON action label full-string vs the SELF-guard issue body's bare-`"self"` sketch | Ratified deviation (R-4; R1-grounded, adversarially verified) |
| DL-2 | ADR ships as repo file, not `adr`-labeled GitHub Issue | Ratified process deviation (R-7; practiced convention) |
| DL-3 | Duplicate worktree rows under `--all` (pre-existing) | Fixed in commit 1 per operator scope call (dedup guard) |
| DL-4 | Oracle definitional boundary (cwd-holding ≠ fd-holding) | Accepted residual; ADR-021 Consequences + 30-day monitoring criterion (adversarial FMF-3/CDF-2) |
| DL-5 | Self-canary witnesses scan-ran, not scan-complete | Accepted residual; ADR-021 Consequences note (adversarial FMF-4) |
| DL-6 | Fixture environmental dependence | Resolved by amendment A-Fixture (merge-base anchoring; adversarial FMF-6) |
| DL-7 | `has_merged_pr` vestigial helper | Accepted residual (spec R-9) |
| DL-8 | Script-execution allowlist carries 2 of 4 invocation forms | Routed as next-release intake (spec R-9; hub files the issue) |
| DL-9 | TOCTOU backstop sentence corrected; residual window = seconds between re-check and porcelain call | Amendment A-Recheck (adversarial PRF-1) |

#### Cross-reference

Adversarial review: the 12-finding advisory record is the first comment on the adversarial-review sub-task. Stage 4 plan + plan-approval decision record: comments on the Stage 4 planning sub-task (transcribed above). Scope-lock rendered against the two-part combined spec comments on the Solutioning sub-task.

---

## Engineering Commit 0 Amendments

Recorded by the Stage 6 Engineering spoke at plan transcription (Commit 0). The transcribed Stage 4 sections above are the historical record of the approved plan; this section is the live state after the Collective Review scope-lock.

### File Change Matrix — refined (supersedes the transcribed Stage 4 matrix)

Per the approved combined Stage 5 spec's Blast Radius matrix, as amended by the scope-lock (A-ADR renumber):

| # | Path | Intent | Commit | Reversibility |
|---|---|---|---|---|
| 1 | `release/releases/plans/v1.11_RELEASE_PLAN.md` | ADD (Commit 0 — this file) | 0 | CHEAP |
| 2 | `release/tools/cleanup-orphan-state.sh` | MODIFY (SELF + dedup + oracle + apply-time re-check + resolve + 3 fixtures + header) | 1, 2, 3 | CHEAP (single revert) |
| 3 | `release/ADRs/ADR-021-liveness-oracle-selection.md` | NEW (renumbered per A-ADR) | 2 | CHEAP |
| 4 | `release/ADRs/README.md` | MODIFY (index row + cross-numbering row) | 2 | CHEAP |
| 5 | `core/rules/git-workflow.md` | MODIFY (§ PR Process Step 10 automated-path bullet) | 4 | CHEAP |
| 6 | `release/references/pipeline/stage-13-close.md` | MODIFY (§ Phase C4 final sentences) | 4 | CHEAP |

Machine-readable block (one path per line; supersedes the transcribed block above):

```
release/releases/plans/v1.11_RELEASE_PLAN.md
release/tools/cleanup-orphan-state.sh
release/ADRs/ADR-021-liveness-oracle-selection.md
release/ADRs/README.md
core/rules/git-workflow.md
release/references/pipeline/stage-13-close.md
```

### Deviation Log

All entries Tier 1 [ADJUST] (minor adjustment, committed with rationale per the inter-stage feedback protocol) unless marked otherwise. The nine scope-lock deviation rows DL-1..DL-9 are transcribed in the Decision Log above; the entries below are Commit-0-time additions.

**(a) Plan-file path normalization.** The transcribed Stage 4 File Change Matrix names the plan file `v1.11-cleanup-orphan-state-reliability_RELEASE_PLAN.md`; it lands as `v1.11_RELEASE_PLAN.md` per the sibling bare-version convention (`v1.07_RELEASE_PLAN.md` / `v1.08_RELEASE_PLAN.md` / `v1.09_RELEASE_PLAN.md`) — an approved path normalization directed at the Stage 6 chip. The refined matrix above carries the real path.

**(b) A-ADR re-verification at Stage 6 entry (performed).** `ls core/ADRs/ release/ADRs/` at Commit 0 (2026-06-11): highest existing number is ADR-020 (`core/ADRs/ADR-020-agent-script-promotion-ladder.md`, v1.09); ADR-021 is the next free number across both directories. The renumbered file path stands as amended; no further renumber needed.

**(c) Baseline re-check at Commit 0 (audit-baseline discipline).** Plan baseline was pinned at `origin/main` = `eecbb5e` (2026-06-10) with an EMPTY open-PR population. At Commit 0 (2026-06-11): `origin/main` = `081f07c` (v1.09 Stage 12/13 close-out corpus merges — commits touch `CHANGELOG.md` + `release/releases/` corpus only; zero overlap with this matrix); open-PR population = 1 (the concurrent v1.10 release PR, 26 files, **zero overlap** with this release's File Change Matrix — verified by direct file-list comparison). Branch point for this release: `081f07c`.

**(d) Liveness-oracle ADR status at filing.** The spec drafted the ADR `Proposed` ("promotes to Accepted at the v1.11 Collective Review scope-lock"). That gate rendered 2026-06-11 (Decision Log above) and ratified R-7 (acceptance = the scope-lock); per the v1.09 ADR-020 precedent, ADR-021 therefore files with status **Accepted** at commit 2, citing the gate.

**(e) Stage 6 entry divergence check (Tier 1 informational).** Commits on `origin/main` since the Stage 5 survey tree (`eecbb5e`): v1.09 Stage 12/13 close-out corpus only — no commits touch `release/tools/`, `core/rules/git-workflow.md`, or `release/references/pipeline/stage-13-close.md`. Spec line anchors verified intact at the branch point.

---

## Change Description

### Outcome

v1.11 makes `release/tools/cleanup-orphan-state.sh` safe to run from anywhere and complete in one pass. The sweep can no longer remove the worktree it is running from (a new `SELF` action class, immune to `--force`), can no longer remove a worktree held by any live process (an all-process `lsof` cwd liveness gate — fail-closed when the oracle is unavailable, re-verified at apply time seconds before removal), and no longer leaves orphan branches behind for a second run (a bounded resolve pass removes local branches freed by the same run's worktree removals). The self-test grows three end-to-end fixtures (SELF guard, synthetic live holder, fixed-point apply) on top of the preserved apply-path/verify/prune guards, and the two invoker docs that enumerate the apply phases are updated to the four-phase vocabulary.

### Issues resolved

| # | One-line outcome | Status |
|---|---|---|
| #333 | SELF action class + self-worktree computation + primary-clause robustification + worktree dedup guard; own runtime worktree reads `SELF — script's own runtime worktree (protected)` in both emitters and is apply-inert including under `--force` | DONE |
| #326 | All-process lsof cwd liveness oracle (absolute-path binary, self-canary, fail-closed) + live clause at precedence slot 3 + apply-time re-verification (A-Recheck) + fail-closed counter + `liveness_oracle` JSON field + ADR-021 | DONE |
| #53 | `resolve_freed_branches` — ONE bounded re-evaluation pass between apply and verify; freed set derived from REMOVED worktree rows; unrowed `--historical` branches fresh-classified; a single `--apply` reaches fixed point | DONE |
| #57 | VERIFY-ONLY — no code change shipped for it; the apply-path hardening it tracks is confirmed present and re-evidenced by `--self-test` PASS at every commit on this branch (#57 transitions to closed at Stage 13 with the evidence comment) | DONE (verify-only) |

### Key decisions

- **D-ReleaseClass: novel** — Stage 9 review depth Deep (clause precedence + oracle design + apply-phase re-evaluation); 30-day outcome window. Rendered at the Stage 4 plan review (Decision Log above).
- **D-C: SINGLE topology** — one branch, one Engineering spoke, serialized per-issue commits in protective-gates-first order; one PR.
- **D-Stage5-Topology: COMBINED** — one design spec across the three BUILD issues + a #57 regression-constraints section; one adversarial review over the composed design.
- **D-VerifyOnly-57: Option 1** — References-only in the PR; manual closure at Stage 13 with verification evidence.
- **Collective Review scope-lock (2026-06-11): APPROVED with amendments** — A-ADR (liveness ADR renumbered ADR-021), A-Fixture (fixed-point fixture anchored at the HEAD↔origin/main merge-base), A-Recheck (apply-time liveness re-verification; backstop sentence corrected); operator scope calls: dedup guard → commit 1, fail-closed counter → commit 2; R-4..R-8 ratified as-spec'd (full-label JSON vocabulary; primary-clause robustification; live skip force-immune; ADR ships as repo file Accepted at the gate; ~15 s self-test budget accepted).

### Reversibility

CHEAP / HIGH confidence — a single `git revert -m 1` of the release PR restores prior behavior (one code file + one new ADR + three doc/registry edits; no data migration; classification-side protection only), with the dogfood guard that this release's own Stage 13 cleanup runs the NEW script (halt-revert-rerun on misbehavior).

### Downstream impact

- The Stage 13 Phase C4 choreography simplifies permanently: the spoke's own worktree is protected in-script (SELF), and detaching before `--apply` matters only to release the RELEASE BRANCH for same-run draining via the resolve pass.
- Completing this milestone unblocks the deploy-decomposition wave (#341, #45, #44, #43, #42 list #57 in their Blocks chain) and #257 (blocked by #326).
- The operator-instance worktree-reaped-mid-execution memory retires at Stage 12/13 (both its preconditions — self-cwd protection AND cross-session detection — ship together here), sequenced AFTER post-merge verification per plan R7.
- ADR-021's 30-day monitoring criterion arms the Stage 13 outcome window: a reap incident with the affected session's cwd OUTSIDE the removed tree re-triages the oracle toward the all-fd predicate.
- Routed out-of-scope follow-ups (hub files): script-execution allowlist missing 2 of 4 invocation forms (DL-8); `worktree-agent-*` sweep coverage gap; per-branch gh-call batching.

### Cross-references

- Release plan: top of this file (`release/releases/plans/v1.11_RELEASE_PLAN.md`).
- Milestone: `cleanup-orphan-state-reliability` (v1.11, milestone 170) on the repository milestone surface.
- ADR: `release/ADRs/ADR-021-liveness-oracle-selection.md` (Accepted at the 2026-06-11 scope-lock).
- User-facing release notes: `release/releases/notes/v1.11_RELEASE_NOTES.md` (authored at Stage 13 Close; does not exist yet at Engineering time).

---

### Issue References

References #333 (SELF guard — implemented at commit 1). References #326 (liveness gate — implemented at commit 2 as amended). References #53 (fixed-point apply — implemented at commit 3). References #57 (VERIFY-ONLY; regression constraints RC-1..RC-6 held at every commit; transitions to closed at Stage 13). References #635 (Stage 4 planning sub-task — transcribed plan + decision record). References #637 (Stage 5 Solutioning sub-task — two-part spec + scope-lock amendment record). References #638 (adversarial-review sub-task — the 12-finding advisory record behind the amendments). References #639, #640, #641 (Stage 6 Engineering sub-tasks). References #341, #45, #44, #43, #42, #257 (downstream dependents unblocked by this milestone).
