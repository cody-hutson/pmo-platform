## Stage 4 Release Planning — v1.03-bundle-and-related

### Summary (30 seconds)

**Bundle scope (work-item glosses on first mention):**
- **Domain-best-practice sourcing step** — Add a domain-best-practice sourcing-or-flag step to Planning / Solutioning / Dev-Testing.
- **Standing Parallelization Map** — Standing milestone Parallelization Map + mandatory Stage 3 / Stage 4 reconfirm gate (addresses the recurring operator re-derivation gap).
- **Cascading-rebundle protocol** — When a re-milestone event removes a donor milestone's namesake deliverable, trigger donor-milestone re-evaluation.

3-issue capability slice in the Pipeline Discipline arc — all 3 are protocol/governance edits with no skill-logic changes, no new source files, mostly text-shaped diffs. The bundle is structurally consistent with the milestone description's `routine` declaration BUT touches Stage 3, Stage 4, Stage 5, and Stage 7 spec files plus release-process.md — surfacing a `routine` vs `novel` reaffirmation question (Hub-A). Sequence sourcing-step → Parallelization-Map → cascading-rebundle (per description) is verified with one caveat: item D (the Parallelization-Map work and the sourcing-step work both edit stage-04-planning.md) requires deliberate within-file section discipline, and item B (the sourcing step's domain auto-detection substrate dep) requires the SHIP-WITH-FLAG decision. Recommended D-C: SINGLE topology, D-Stage5Activation: NO (skip — no new files, doctrine-encapsulation only).

### Dependency Graph

**Direct edges (within-milestone):** none — all 3 work items are independently shippable per body declarations.

**Substrate edges (out-of-milestone, informational):**

| From | To | Type | Status in current repo |
|---|---|---|---|
| Sourcing step | Domain auto-detection follow-up | substrate-soft | **NOT in current milestone, NOT a tracked issue in pmo-platform — planning shorthand only** |
| Parallelization Map | Cross-milestone-sequence (G3-07) harmonization | compose-with (adjacent) | **Legacy tracking-repo reference — not present in pmo-platform repo** |
| Parallelization Map | File-contention always-emit | compose-with (adjacent) | **Legacy tracking-repo reference — not present in pmo-platform repo** |
| Cascading-rebundle | Precipitating taxonomy issue in the legacy tracking repo | precipitating-event-ref | **Legacy tracking-repo reference — not actionable in pmo-platform repo** |
| Cascading-rebundle | The donor milestone named in the originating finding | subject-of-finding | **That milestone does NOT exist in pmo-platform repo (current track is the v1.* line)** |

**Graph topology:**
```
  sourcing-step ─────┐
                     ├──→ (independent — no in-bundle FS+0d hard edges; merge in any order)
  Parallelization ───┤
                     │
  cascading-rebundle ┘
```

**No native blocked-by mirroring required** — body Dependencies fields declare no FS+0d substrate edges in current repo. Cross-milestone refs do NOT mirror to native deps per Stage 2 A3.5 protocol.

### Implementation Sequence

**Doctrine § 9 application (foundation → infrastructure → skill-core → eval):**

| Order | Work item | Layer assignment | Rationale |
|---|---|---|---|
| 1 | Domain-best-practice sourcing step | `skill-core` (Protocol — pipeline-spec edit) | Adds sourcing-step convention to Planning/Solutioning/Dev-Testing; foundation-adjacent because subsequent stages would consume the convention |
| 2 | Standing Parallelization Map | `skill-core` (Protocol — Stage 3/Stage 4 convention) | Adds Parallelization Map convention + reconfirm gate; depends on Stage 3/4 spec stability |
| 3 | Cascading-rebundle protocol | `infrastructure` (Structure — process-protocol amendment per disposition) | Codifies cascading-rebundle protocol; lands last so it can reference the Stage 3/4 substrate left after the first two work items |

**Within-layer tie-break (Kahn's BFS):** priority-desc → issue-asc. The sourcing step (P2) precedes the Parallelization Map (P3); the cascading-rebundle protocol (P2) ties with the sourcing step on priority but lands LAST per "synthesis-layer placement" per doctrine § 9 (it codifies a meta-pattern composing-with the Parallelization Map's reconfirm step).

**Reaffirmed:** sourcing-step → Parallelization-Map → cascading-rebundle per milestone description.

**Critical-path note (degraded mode):** No typed-dep substrate populated; CPM in degraded mode. Schedule-determining chain = the 3 sequential work items themselves (no parallelism opportunity given mutual-edit territory on stage-04-planning.md per item D).

### File Change Matrix

```
release/references/pipeline/stage-04-planning.md
release/references/pipeline/stage-05-solutioning.md
release/references/pipeline/stage-07-dev-testing.md
release/references/pipeline/stage-03-bundle.md
release/governance/release-process.md
release/references/standards/ (new domain-practice provenance standard — sourcing-step candidate)
release/skills/release-planner/SKILL.md
release/skills/release-planner/references/dependency-analysis.md
```

**Per-work-item change matrix:**

| Work item | File | Intent | Mirror-pair? | Rationale |
|---|---|---|---|---|
| Sourcing step | `release/references/pipeline/stage-04-planning.md` | edit (add sourcing/provenance step in Phase A) | NO (single canonical in new repo) | Phase A sourcing-or-flag step for unfamiliar-domain detection |
| Sourcing step | `release/references/pipeline/stage-05-solutioning.md` | edit (add sourcing/provenance step) | NO | Stage 5 design with sourced domain practice |
| Sourcing step | `release/references/pipeline/stage-07-dev-testing.md` | edit (add provenance verification step) | NO | DT verifies sourced-or-flagged design |
| Sourcing step | `release/references/standards/<domain-practice-provenance>.md` | add (new standard) | NO | Provenance label schema with dated field |
| Sourcing step | `release/governance/release-process.md` | edit (cite new sourcing step under Stage 4/5/7 sections) | NO (NB: no `core/rules/release-process.md` mirror in current modular repo — single canonical) | Process anchor for new step |
| Parallelization Map | `release/references/pipeline/stage-03-bundle.md` | edit (A6-adjacent "Parallelization Map" section convention in milestone descriptions) | NO | Standing-reference convention (the standing-reference acceptance criterion) |
| Parallelization Map | `release/references/pipeline/stage-04-planning.md` | edit (Phase A0 currency-check additions for parallelization-map reconfirm) | NO | Reconfirm gate (the reconfirm-gate acceptance criterion — mutual-edit territory with the sourcing step — see Contention Map + Item D) |
| Parallelization Map | `release/governance/release-process.md` | edit (Stage 3 A7 refresh-trigger + Stage 4 Phase A0 currency-check additions for parallelization map; cite hard-vs-soft classifier) | NO | Reconfirm-mandate (the reconfirm-mandate + reproducible-classifier acceptance criteria) |
| Parallelization Map | `release/skills/release-planner/SKILL.md` | edit (auto-populator candidate) | NO | Skill consumes the standing convention |
| Parallelization Map | `release/skills/release-planner/references/dependency-analysis.md` | edit OPTIONAL (hard-vs-soft classifier note) | NO | The reproducible-classifier acceptance criterion |
| Cascading-rebundle | `release/references/pipeline/stage-03-bundle.md` | edit (cascading-rebundle protocol — when a re-milestone event removes the namesake deliverable, trigger re-bundle re-evaluation for the donor milestone) | NO | Codifies the cascade per disposition (Item C) |
| Cascading-rebundle | `release/governance/release-process.md` | edit (Stage 3 A7 Bundle Mutability Protocol amendment: add new trigger condition "namesake-deliverable removed via re-milestone") | NO | Cascade-protocol process anchor |

**Note on mirror-pair discipline:** The current modular repo (`core/`, `release/`, `operations/`) does NOT carry the legacy paired-rules mirror that the issue bodies reference. The 3 work items' "affected files" lists reference legacy paths. Stage 5 (if activated) or Stage 6 Engineering MUST translate legacy paths to current modular paths — captured in Risk Register R-PathTranslation below.

### Contention Map

| File | Work items touching it | Sequencing implication |
|---|---|---|
| `release/references/pipeline/stage-04-planning.md` | **Sourcing step** (sourcing step in Phase A) + **Parallelization Map** (Phase A0 reconfirm currency-check addition) | **MUTUAL-EDIT — disjoint sections (Phase A vs Phase A0) but same file. Sequence the sourcing step first to establish the Phase A surface; the Parallelization Map then adds the Phase A0 reconfirm wording. Stage 6 under D-C SINGLE topology → serial Engineering chips on this file** |
| `release/references/pipeline/stage-03-bundle.md` | **Parallelization Map** (Parallelization Map convention near A6) + **Cascading-rebundle** (cascading-rebundle protocol near A7) | Disjoint sections (A6 vs A7). Sequence Parallelization-Map then cascading-rebundle per implementation sequence; append-pattern-adjacent (different sub-sections), low merge-conflict risk under D-C SINGLE |
| `release/governance/release-process.md` | **Sourcing step** (Stage 4/5/7 step citations) + **Parallelization Map** (Stage 3 A7 + Stage 4 A0 reconfirm) + **Cascading-rebundle** (Stage 3 A7 cascading-rebundle trigger) | **3-way contention — all 3 work items edit this file in Stage 3 / Stage 4 sections.** Sequence sourcing-step → Parallelization-Map → cascading-rebundle maintains layered ordering; each chip pulls latest release/governance/release-process.md before committing |
| `release/references/standards/<domain-practice-provenance>.md` | **Sourcing step only** | New file — no contention |

**Cross-PR Overlap Audit (A4 extension):** Baseline-pinned at this sub-task commit SHA. No other open PRs detected against the v1.03 milestone (this milestone has not yet entered Stage 6 Engineering). Baseline-pin temporal limitation per stage-04-planning.md applies — mid-pipeline divergence caught at Stage 9 Phase A6.5 and Stage 12 Phase A.5.

**Overlap-class enrichment (per ADR-005 append-pattern detection):** Both 2-way and 3-way contentions are `line-range-overlap` class (sequencing-sensitive), NOT `append-pattern` (which would be informational-only). Mitigation = sequencing per Implementation Sequence + worktree-discipline `git fetch` before each Engineering chip per hub-spoke-bridge.md Procedure 3 § Worktree discipline.

### Stage Applicability Matrix

| Work item | Stage 5 (Solutioning) | Stage 6 (Engineering) | Stage 7 (Dev Testing) | Stage 8 (QA Testing) | Stage 9 (Plan Review) | Stage 12 (Execute) | Stage 13 (Close) |
|---|---|---|---|---|---|---|---|
| Sourcing step | SKIP-borderline (see Stage 5 Activation rationale below) | APPLY | APPLY | APPLY | APPLY | APPLY | APPLY |
| Parallelization Map | SKIP-borderline | APPLY | APPLY | APPLY | APPLY | APPLY | APPLY |
| Cascading-rebundle | SKIP-borderline | APPLY | APPLY | APPLY | APPLY | APPLY | APPLY |

**Recommendation:** apply all-or-nothing per Stage 5 release-level rule. Borderline-SKIP is the spoke recommendation per § Stage 5 Solutioning Activation below; the release-level D-Stage5Activation gate locks the all-or-nothing posture.

**Rationale (Stage 7/8 retention even for protocol-only edits):**
- AC verification (e.g., the sourcing step's acceptance criterion "Verify a design/review output can carry a domain-practice provenance label with a date") requires Stage 7 grep + Stage 8 acceptance — protocol-only does NOT mean no-functional-impact at the documentation-corpus layer.

### Stage 5 Solutioning Activation

**Verdict: NO (recommended SKIP — release-level all-or-nothing).**

**Per-work-item trigger evidence:**

| Work item | Triggers? | Evidence |
|---|---|---|
| Sourcing step | borderline-YES | (a) "new reference doc" possibly added: `release/references/standards/<domain-practice-provenance>.md` — IF authored as a new file rather than a sub-section in an existing standard, this trips the `novel` trigger (a). (b) [ASSUMPTION-CONFIRM] flag on "acquisition mechanism (web sourcing vs. authored domain pack vs. operator prompt) is deferred to Stage 5" — the body explicitly cites Stage 5 as the mechanism decision gate. **This makes Stage 5 ACTIVATION naturally-suggested by the issue body itself.** |
| Parallelization Map | NO | Doctrine-encapsulation pattern per stage-03-bundle.md A6 precedent (rationale-in-description). No new file; no skill-logic change; no D-class decision required beyond reconfirm-gate mechanics |
| Cascading-rebundle | NO | Process-protocol amendment to existing stage-03-bundle.md A7 / release-process.md. No new file; no skill-logic change. Disposition (Item C) chooses the cascade-protocol path (not the donor-milestone-doc-update path) which is purely process-protocol-amend |

**Recommendation rationale:** The sourcing step's body declares "[ASSUMPTION – CONFIRM] mechanism TBD at Stage 5" — this is a direct invitation to activate Stage 5 Solutioning. **However**, the work item can be successfully scoped at Planning level by treating it as a "Phase A sourcing-step-or-flag" minimal addition (Planning-level wording) with the acquisition mechanism deferred to a follow-up issue (the domain auto-detection follow-up — see Item B). Under the recommended posture for Item B (SHIP-WITH-FLAG), Stage 5 SKIPS — design uncertainty parks in a follow-up issue rather than expanding the release scope.

**If operator opts for FULL-SOLUTION at Item B**, then Stage 5 ACTIVATES, Collective Review fires post-Stage 5, and the sourcing step carries acquisition-mechanism design content.

### Risk Register

| ID | Risk | Severity | Mitigation |
|---|---|---|---|
| R-MutualEdit | The sourcing step + the Parallelization Map both edit stage-04-planning.md in disjoint sections (Phase A vs Phase A0). Concurrent Engineering chips race on file HEAD under D-C SINGLE | MEDIUM | Implementation Sequence puts the sourcing step first; the Parallelization Map chip pulls latest after the sourcing step lands per Worktree Discipline `git fetch origin release/<milestone>` |
| R-3WayContention | release/governance/release-process.md touched by all 3 work items in Stage 3/4 sections | MEDIUM | Sequence enforces layered ordering; each chip pulls latest before commit. Cross-D scan at Collective Review (if activated) catches any convention divergence |
| R-PathTranslation | Issue bodies reference legacy paths — the current modular repo uses `release/references/pipeline/...` and single-canonical `release/governance/release-process.md` (no paired-rules mirror in new repo) | LOW-MEDIUM | Stage 6 Engineering spoke applies path-translation when reading the release plan's File Change Matrix. Acceptance criteria re-stated against current-modular paths at Engineering chip prompt |
| R-DomainDepFollowup | The sourcing step declares "Requires a domain-parameter-keystone capability to know when a domain is unfamiliar" — that capability is NOT in current milestone, NOT a tracked issue in pmo-platform repo | MEDIUM | **Item B handling — SHIP-WITH-FLAG**: the sourcing step ships with a sourcing-step-or-flag mechanism = "operator-prompt OR explicit Unsourced-Domain flag in design output". Domain auto-detection deferred. Follow-up issue filed for the auto-detection mechanism |
| R-NewStandardScope | The sourcing step may add a new `release/references/standards/<domain-practice-provenance>.md` file. Adding a new standard mid-bundle expands scope and is a `novel` trigger (a) | MEDIUM | Engineering decision: (a) author as new standard file (triggers re-classification to `novel`); OR (b) inline the provenance schema into an existing standard or into stage-05-solutioning.md Cascade-Sweep block. Recommended (b) at Planning to keep `routine` posture |
| R-CascadeDisposition | The cascading-rebundle work has 3 possible dispositions (donor-milestone-doc-update / cascade-protocol-amend / both); the donor milestone does NOT exist in current repo — eliminates the doc-update option by construction | LOW | **Item C handling — cascade-protocol-amend (only viable option)**: Codify the cascading-rebundle protocol in stage-03-bundle.md A7 / release-process.md A7 so future re-milestone events automatically trigger donor-milestone re-evaluation. NO donor-milestone-doc-update fires (no such milestone in this repo) |
| R-MapStandingApplicability | The Parallelization Map mandates a map on "every milestone reaching Stage 3 Bundle" — applying it retrospectively to milestones that predate its adoption would be a disproportionate scope expansion | LOW | State the convention unconditionally with a standing-applicability clause expressed as plain rule semantics: it binds milestones going forward and does not retroactively bind milestones that predate it (they carry no map by construction) |
| R-SourcingStandingApplicability | The new sourcing step likewise should not fire retroactively on already-planned work | LOW | State the step unconditionally; the pipeline-internal exemption already covers governance/software releases by construction, so the rule reads correctly with no further qualification |
| R-CascadeStandingApplicability | The cascade-protocol governs future re-milestone events only | LOW | State the protocol unconditionally with a standing-applicability clause expressed as plain rule semantics: it governs future re-milestone events, and events predating its adoption do not retroactively trigger donor-milestone re-evaluation |
| R-ReleaseClassReaffirm | Milestone declares `routine`. 3-issue protocol-shard edits historically classified as `novel` (governance-shard precedent) | LOW-MEDIUM | **Item A handling — REAFFIRM `routine` with explicit rationale**: doctrine-encapsulation pattern (extending existing A6/A7 conventions, NOT introducing new ones); no new pipeline stage; no new D-class architectural decision rendered; acceptable single-step edits per `routine` trigger (a)+(d). Hub-A scan acknowledged but evidence supports REAFFIRM |
| R-Rollback | Rollback complexity: text-only edits → `git revert` per the release commit. No state-mutating defaults; no new files (under R-NewStandardScope mitigation b); no migrations | LOW | Standard git-revert rollback per CHEAP reversibility; documented in Verification Plan |

### Recommendations

**Hub-surfaced item A — Release Class reaffirmation:**

**REAFFIRM `routine`.** Evidence supporting reaffirmation:
- All 3 work items are `cluster: process-protocol` or `cluster: gate-handoff` doctrine-encapsulation work — extending existing A6/A7/Stage-N-A0 conventions rather than introducing new pipeline stages or schemas.
- File Change Matrix touches **3 pipeline/stage-*.md files** (stage-03, stage-04, stage-05/07) — exactly at the `cross-cutting` trigger (a) threshold of ≥3. **However**, the edits in stage-05 and stage-07 are the sourcing step's addition only; the bulk of work concentrates on stage-03 + stage-04. Counter-evidence: this is on the boundary, leaning toward `cross-cutting` if Engineering authors substantive content in all 4 stage files.
- Zero new D-class architectural decisions at Stage 4 (the D-decisions surfaced here are routing — D-ReleaseClass, D-C, D-Stage5Activation — NOT new architectural axes).
- Zero new files added under recommended R-NewStandardScope mitigation (b).
- All 3 reversibility postures: CHEAP (text edits, `git revert`).

**Counter-recommendation (if operator prefers stricter posture):** PROPOSE-RE-CLASS to `novel` cites: the sourcing step introduces a provenance-tracking convention (new pattern in the corpus); the Parallelization Map mandates a new milestone-description schema section; the cascading-rebundle protocol introduces a cascade-protocol trigger (new Bundle Mutability trigger). These are 3 first-of-kind pattern additions, which IS `novel` trigger (a) "introduces a new reference doc, schema, or skill" interpreted broadly (schema = milestone-description section schema).

**Operator decision recommended: REAFFIRM `routine` per doctrine-encapsulation rationale.** Differentiation posture per `routine` mapping table: Engagement density = Light; Stage 9 Plan Review depth = Standard; (optional) Stage 5 activation bias = SKIP-where-trivial; (optional) Stage 13 outcome-window = 30-day.

**Hub-surfaced item B — domain auto-detection dep handling:**

**Recommendation: SHIP-WITH-FLAG.** The domain-parameter-keystone capability is a planning-shorthand reference to "domain auto-detection capability" — not a tracked issue in the current pmo-platform repo. Three options:

| Option | Verdict | Rationale |
|---|---|---|
| **(B1) SHIP-WITH-FLAG (recommended)** | RECOMMEND | Author the sourcing step with `operator-prompt OR Unsourced-Domain flag` mechanism (no auto-detection). The acquisition mechanism degrades gracefully without auto-detection. The provenance-label acceptance criterion is satisfied via the provenance label; the explicit-flag acceptance criterion is satisfied via the explicit Unsourced-Domain flag. Domain auto-detection tracked as a follow-up issue linked to the sourcing step |
| **(B2) DEFER the sourcing step entirely** | NOT-RECOMMEND | Removes 1 of 3 work items from the release; pushes the sourcing step to a future milestone that depends on the auto-detection capability being filed/triaged first. Adds 1+ release cycle delay |
| **(B3) SPLIT the sourcing step** | NOT-RECOMMEND | Splits into "sourcing-step convention" (in this release) + "domain-detection mechanism" (deferred). Adds ceremony without meaningful release-shape improvement vs. B1 |

**Hub-surfaced item C — cascading-rebundle disposition:**

**Recommendation: cascade-protocol-amend (the doc-update option is eliminated by repo state; "both" reduces to cascade-only).** Three a priori options:

| Option | Verdict | Rationale |
|---|---|---|
| **(C1) donor-milestone-doc-update only** | **IMPOSSIBLE** | The donor milestone named in the originating finding does NOT exist in pmo-platform repo (current track is the v1.* line). That milestone was in the legacy tracking repo. There is no description to update in this repo |
| **(C2) cascade-protocol-amend (recommended)** | RECOMMEND | Codify the cascading-rebundle protocol in stage-03-bundle.md A7 (add the trigger "namesake-deliverable removed via re-milestone") and release-process.md A7 Bundle Mutability table. Future re-milestone events automatically trigger donor-milestone re-evaluation. Satisfies the cascade-codification + composition acceptance criteria; the doc-update acceptance criterion becomes informational (no donor-milestone description exists to update) |
| **(C3) "both" — protocol + (informational) historical-note** | NOT-RECOMMEND | The historical-note path would need to live in `release/governance/RELEASE_LOG.md` or a release-archive surface; the work is already an archival auto-log per its body. Adding a second historical-note surface is duplication |

**Hub-surfaced item D — Parallelization-Map + sourcing-step mutual-edit territory on stage-04-planning.md:**

**Confirmed mutual-edit on `release/references/pipeline/stage-04-planning.md`** at disjoint Phase A vs Phase A0 sections. Under D-C SINGLE topology, Engineering chips serialize at file HEAD. Recommendation:

| Option | Verdict | Rationale |
|---|---|---|
| **(D1) Sequenced chips per Implementation Sequence (sourcing-step → Parallelization-Map) (recommended)** | RECOMMEND | The sourcing step lands first establishing the Phase A surface; the Parallelization Map chip pulls latest before adding Phase A0 reconfirm wording. Standard Worktree Discipline `git fetch origin release/<milestone>` covers the sequencing |
| **(D2) Merge the sourcing step + Parallelization Map into one ticket** | NOT-RECOMMEND | Combines two distinct AC sets (the sourcing step + the parallelization map) under one issue — destroys per-issue traceability; would require ad-hoc Stage 2 Triage reversal |
| **(D3) Split the Parallelization Map into "Stage 3 Parallelization Map" + "Stage 4 reconfirm gate"** | NOT-RECOMMEND | Adds ceremony; the Parallelization Map's two parts are conceptually one (the standing reference AND its reconfirm gate are coupled). The mutual-edit can be cleanly sequenced |

**Other recommendations:**

- **Standing-applicability framing for all 3 work items.** Each new convention is stated unconditionally with plain rule semantics — it binds milestones / releases going forward and does not retroactively bind work that predates its adoption (pre-existing milestones carry no Parallelization Map by construction; the sourcing step's pipeline-internal exemption already covers governance/software releases). The going-forward-vs-pre-existing distinction is expressed as rule behavior — version history lives in git, not in the rule text (R-MapStandingApplicability, R-SourcingStandingApplicability, R-CascadeStandingApplicability).
- **Path translation discipline (R-PathTranslation):** Stage 6 Engineering spoke prompts MUST reference current modular paths (`release/references/pipeline/...`, `release/governance/release-process.md`), NOT the legacy paths embedded in issue bodies.
- **Domain-best-practice sourcing step for this release ITSELF:** the current Stage 4 Planning protocol does NOT itself source authoritative external domain best-practice (which IS the sourcing step's whole point). This release is a pipeline-internal / governance release, so it falls under the sourcing step's pipeline-internal exemption by construction — no operator action needed.

### Operator Decisions (D-Gate blocks per hub-spoke-bridge.md D-Gate Template)

#### D-ReleaseClass: What Release Class does this release carry?
**Gate input:** Spoke-proposed class `routine` with REAFFIRM evidence (doctrine-encapsulation pattern; 0 new files under R-NewStandardScope mitigation b; 0 new D-class architectural axes). Counter-evidence available for `novel` (3 first-of-kind pattern additions).
**Pre-decided (if applicable):** Milestone description already declares `routine`.
**Gate decision:** Choose between (A) REAFFIRM `routine` (recommended), (B) PROPOSE-RE-CLASS to `novel`, (C) PROPOSE-RE-CLASS to `cross-cutting`.
**Blocks:** Stage 9 Plan Review depth posture; per-class engagement-density routing in Procedure 2.
**Upstream compatibility:** N/A — Release Class is PMO platform internal taxonomy; no Anthropic upstream surface. Upstream compatibility check does not apply.
**Reversibility / Confidence:** CHEAP / HIGH — re-classifiable later per Re-Classification Protocol.
**Recommendation:** **(A) REAFFIRM `routine`** per doctrine-encapsulation rationale; differentiation posture = Engagement density Light / Stage 9 Standard / Stage 5 SKIP-where-trivial / Stage 13 30-day outcome-window.

#### D-C: Branch Topology
**Gate input:** Default for capability-slice routine releases per stage-04-planning.md precedent; 3-issue bundle with mutual-edit territory on stage-04-planning.md and 3-way contention on release-process.md.
**Pre-decided (if applicable):** None.
**Gate decision:** Choose between (A) SINGLE — one release branch `release/v1.03-bundle-and-related`, sequential Engineering chips serialized at file HEAD; (B) OPTION-A — per-issue branches + per-issue PRs.
**Blocks:** Procedure 1 Scaffolding sub-task wording; Stage 6 chip prompt Worktree Discipline; Stage 4 release-plan commit location (release branch Engineering Commit 0 vs Stage 4 release-plan chore PR on main).
**Upstream compatibility:** N/A — release branch topology does not modify skill-authoring surface. Upstream compatibility check does not apply.
**Reversibility / Confidence:** CHEAP / HIGH — re-electable later per hub-spoke-bridge.md.
**Recommendation:** **(A) SINGLE.** 3-issue bundle, mutual-edits clean under sequenced chips per Implementation Sequence + Worktree Discipline. OPTION-A would add overhead without contention benefit (the 3-way release-process.md edit serializes at merge anyway).

#### D-Stage5Activation: Does Stage 5 Solutioning fire for this release?
**Gate input:** Spoke per-work-item trigger evidence (the sourcing step borderline-YES via [ASSUMPTION-CONFIRM] flag; the Parallelization Map NO; the cascading-rebundle protocol NO). Recommendation hinges on Item B decision.
**Pre-decided (if applicable):** None.
**Gate decision:** Choose between (A) NO — release-level SKIP; the sourcing step ships at Planning-level scope per Item B SHIP-WITH-FLAG; (B) YES — release-level ACTIVATE; the sourcing step carries acquisition-mechanism design content; Collective Review fires post-Stage 5.
**Blocks:** Procedure 1 Scaffolding (Stage 5 sub-tasks created+skipped or created+populated); Procedure 2 Routing (Collective Review check); Stage 6 Engineering input shape (Planning-level vs Solutioning-level spec).
**Upstream compatibility:** N/A — Stage 5 activation decision does not modify skill-authoring surface. Upstream compatibility check does not apply.
**Reversibility / Confidence:** MODERATE / HIGH — re-electable up to Procedure 1 Scaffolding completion; after sub-tasks scaffolded, switching costs sub-task reshuffling.
**Recommendation:** **(A) NO** under Item B SHIP-WITH-FLAG. If operator picks Item B FULL-SOLUTION (B2/B3), switch to (B) YES.

#### D-DomainDepHandling: How does the sourcing step handle the substrate dep on domain auto-detection?
**Gate input:** The domain-parameter-keystone capability is a planning-shorthand reference, NOT a tracked issue in pmo-platform repo. The sourcing step declares it as a substrate dep to "know when a domain is unfamiliar".
**Pre-decided (if applicable):** None.
**Gate decision:** Choose between (B1) SHIP-WITH-FLAG (operator-prompt OR Unsourced-Domain flag in design output; auto-detection deferred to follow-up); (B2) DEFER the sourcing step; (B3) SPLIT the sourcing step.
**Blocks:** The sourcing step's Stage 5/6 scope; Stage 9 acceptance for its provenance-label + explicit-flag acceptance criteria.
**Upstream compatibility:** N/A — internal protocol decision.
**Reversibility / Confidence:** MODERATE / HIGH — if SHIP-WITH-FLAG proves insufficient at Stage 9, the domain auto-detection follow-up issue can land in a future milestone.
**Recommendation:** **(B1) SHIP-WITH-FLAG.**

#### D-CascadeDisposition: How does the cascading-rebundle work resolve given the donor milestone does not exist in current repo?
**Gate input:** The donor milestone named in the originating finding does NOT exist in pmo-platform repo. The original disposition (donor-milestone description update) is structurally impossible.
**Pre-decided (if applicable):** None.
**Gate decision:** Choose between (C1) IMPOSSIBLE (donor-milestone-doc-update-only path); (C2) cascade-protocol-amend (recommended); (C3) cascade-protocol-amend + historical-note (NOT-RECOMMEND).
**Blocks:** The cascading-rebundle work's File Change Matrix; the doc-update acceptance criterion's interpretation.
**Upstream compatibility:** N/A — internal protocol decision.
**Reversibility / Confidence:** CHEAP / HIGH.
**Recommendation:** **(C2) cascade-protocol-amend.** Adds the Stage 3 A7 trigger "namesake-deliverable removed via re-milestone" to release-process.md; the cascade-codification + composition acceptance criteria are satisfied; the doc-update acceptance criterion is marked informational (no donor milestone to update).

### Release Class declaration

**Class:** `routine`

**Rationale (max 2 sentences):** 3-issue capability slice in the Pipeline Discipline arc, all doctrine-encapsulation edits (extending existing A6/A7 conventions); zero new D-class architectural axes, CHEAP reversibility, text-only diffs. Trigger conditions (a) "all issues are P3/P4+size:S/M" partially holds (P2/P3 mix, sizes S/L) but (d) "zero new D-class decisions" holds — multi-trigger resolution lands on `routine` per minimal-ceremony posture and doctrine-encapsulation precedent.

**Differentiation posture:**
- Engagement density: Light
- Stage 9 review depth: Standard
- Stage 5 activation bias: SKIP-where-trivial (OPTIONAL)
- Stage 13 outcome-window: 30-day (OPTIONAL)

### Model Provenance

- **Invocation model parameter:** `claude-opus-4-7[1m]` (model identifier of this spawned spoke session per env-block)
- **Agent-definition default:** opus (expected per agent definition for Stage 4 Release Planning — `release-planner` retirement-target skill)
- **Parent-session model:** unknown (spawned via Agent tool — hub did not surface parent model identifier in spoke launch payload)
- **Designated-model match:** MATCH (invocation `claude-opus-4-7[1m]` ≡ agent-definition default `opus`)
