<!-- reference-durability: allow-issue-ref -->
<!-- reference-durability: allow-link -->
<!-- repo-integrity: allow-issue-ref -->
# Release Plan — v2.00 (01-FNH-qa-change-release-hardening)

**Milestone:** 01-FNH-qa-change-release-hardening (parent epic: skill-function-hardening) · **Release Class:** novel · **Topology:** D-C SINGLE
**Version target:** v2.00 (set at Engineering Commit 0; the Stage-4 plan was authored against a draft `v3.21` string on the then-current v3.x line — the operator set the released version to **v2.00** at scope-commit per the repo re-versioning lineage).
**Scope:** 11 open build cards hardening 5 existing function-skills (all EXTEND except the release-planner RECONCILE card — no new skills created).
**Stage 4 source:** the Release Planning sub-task comment (Stage 4 spoke output), reproduced below as the committed release plan (Engineering Commit 0). The draft version target `v3.21` in that comment is updated to **v2.00** throughout this committed copy; the empirical anchors the plan recorded at authoring time (`.version` = v3.19, latest tag = v3.20) are preserved as the as-observed state at plan time.

---

## Stage 4 Release Planning — 01-FNH-qa-change-release-hardening

### Summary (30 seconds)

Release-scoped plan for the `01-FNH-qa-change-release-hardening` milestone (parent epic: skill-function-hardening). **Scope = 11 open build cards** hardening **5 existing function-skills** (all EXTEND except the release-planner RECONCILE card — no new skills created). The 3 CLOSED provenance cards are provenance only — re-decomposed into the open set, not re-planned. Version target: **v2.00** (set at Engineering Commit 0; the plan was drafted against a `v3.21` string on the v3.x line — `.version` = v3.19 [SOURCE: `.version` at plan time], latest tag = v3.20 [SOURCE: `git tag --sort=-v:refname` at plan time] — and re-versioned to v2.00 at scope-commit).

**Proposed Release Class: `novel`** (≥1 new reference doc + ≥1 D-class decision; cross-cutting triggers do NOT fire — touches skill capability surfaces, not ≥3 pipeline-stage or ≥3 governance files).

**Dominant constraint = file contention, not capacity.** Under D-C SINGLE topology (default), Stage 6 Engineering commits serialize on the release branch [SOURCE: hub-spoke-bridge.md Parallelism Rules]. `change-management/SKILL.md` is edited by 5 cards; `release-executor/SKILL.md` by 3. The Implementation Sequence groups by target skill so each shared SKILL.md is touched by one coherent commit-run.

**Key D-decision — D-246-Blocker: INCLUDE the typed-artifact-linking card this release.** The "Blocked by #409" edge is **stale**. That card needs only the **already-built** typed-relationship vocabulary, which exists in-corpus today; #409 is an open epic that will NOT close this release and whose own body declares the card "composes against (not redefined)" the built vocabulary. Evidence in the D-gate below.

**Quota Budget: PASS** — worst parallel wave (Stage 8 QA, 11 spokes) is the binding wave; under the size-bucket heuristic it sits well under the 50% usage-window band on a fresh envelope.

---

### Dependency Graph

Directional edges (`A → B` = A must land before B). Derived from issue bodies (`gh issue list --milestone ... --json body`) + the corpus investigation below.

```
INTERNAL (in-bundle) edges:
  #1130 ──BLOCKS──▶ #1131      (hypercare tier vocabulary + committed SLAs that #1131 measures against)
                                [SOURCE: #1130 body "Blocks: #1131"; #1131 body "Blocked by: #1130"]

EXTERNAL edge (does NOT block this release — see D-246-Blocker):
  #246 ──stale "blocked by"──▶ #409   (RESOLVED: #246 needs only the already-built
                                        frontmatter-schema §Category 4 vocabulary; #409 stays open)

COHERENCE clusters (no hard dependency — same-SKILL.md serialization + design coherence):
  { #1127, #1128, #1129 }   change-management adoption-science (land coherently)
  { #1130, #1131 }          change-management Mode-D hypercare (#1130 first)
  { #244, #245 }            release-executor execution-flow (same SKILL.md + execution-checklist.md;
                            #245 rollout-cycle should be applicable to the #244 gates)
  { #212 }                  release-executor Mode-D close-out (same SKILL.md — sequence after #244/#245)

INDEPENDENT (no shared file, no dep):
  #248  pmo-qa-auditor      (core/skills — sole editor of that SKILL.md)
  #252  artifact-generator  (operations/skills — sole editor of that SKILL.md)
```

Only **one hard in-bundle edge** exists (#1130→#1131). All other ordering is contention-driven (shared SKILL.md) or coherence-driven (split-card design consistency), not dependency-driven.

---

### Implementation Sequence

Dependency- and contention-ordered Stage-6 execution order. **Grouped by target skill** because 5 cards share `change-management/SKILL.md` and 3 share `release-executor/SKILL.md` — under D-C SINGLE topology all Engineering commits serialize on `release/v2.00`, so same-file cards MUST run as one consecutive run to avoid stale-base/non-fast-forward races [SOURCE: hub-spoke-bridge.md File-contention boundary rules, D-C SINGLE = FILE-level serialization]. Independent-file cards (#248, #252) may slot anywhere; placed last as natural breakpoints.

| Order | Card | Skill (target SKILL.md) | Why here |
|---|---|---|---|
| 1 | #1127 | change-management | Start the CM run; ADKAR slice of the adoption-science split |
| 2 | #1128 | change-management | champion/sponsor/valley slice — same SKILL.md, coherent with #1127 |
| 3 | #1129 | change-management | fatigue/outcome slice — completes the adoption triad |
| 4 | #1130 | change-management (Mode D) | **must precede #1131**; defines hypercare tier vocabulary |
| 5 | #1131 | change-management (Mode D) | **after #1130** — tracks SLAs against #1130's tiers; closes the CM run |
| 6 | #244 | release-executor | Start the RE run; tiered quality gates into execution-checklist |
| 7 | #245 | release-executor | progressive rollout — same SKILL.md + same checklist; rollout-cycle applies to #244 gates |
| 8 | #212 | release-executor (Mode D) | close-out build-out — same SKILL.md; sequence after #244/#245 land |
| 9 | #248 | pmo-qa-auditor | Independent file — 8 detectors + RACI gate (P1-Critical) |
| 10 | #252 | artifact-generator | Independent file — lifecycle + zombie detection |

**Rationale for ordering #244/#245 before #212:** all three edit `release/skills/release-executor/SKILL.md`; #244+#245 also co-edit `references/execution-checklist.md` and add `references/progressive-rollout.md` (tightly coupled — land together). #212 builds out Mode D (a distinct SKILL.md section) and adds `references/close-out-checklist.md` — least entangled with the #244/#245 execution-flow edits, so it lands last in the RE run.

**Critical path** = the change-management run (5 serialized cards, one true edge #1130→#1131). It dominates because it is the longest same-file serialized chain.

---

### Stage Applicability Matrix

Default: all stages apply. These are **skill-edit cards**, so Stages 7/8 are `pmo-skill-editor` Regression + `pmo-qa-auditor` gates — **not** code tests [CONTEXT: every card's build rule mandates "edit via pmo-skill-editor discipline (impact + coherence + regression)"]. Stage-5 Solutioning is recommended where the card carries a genuine design choice (scoring scale, tier set, threshold, detector signature, gate ladder, typed-edge schema) and SKIP-where-trivial otherwise — consistent with `novel` class's Stage-5-activation-bias = ALL [SOURCE: release-class-taxonomy.md].

| Card | S5 Solutioning | S6 Eng | S7 Dev-Test (skill-regression) | S8 QA (qa-auditor) | S9 Plan Review | S10–S13 | Stage-5 rationale |
|---|---|---|---|---|---|---|---|
| #1127 | **YES** | ✅ | ✅ | ✅ | ✅ Deep | ✅ | ADKAR scoring scale + barrier-identification rule + training-timing logic = design choice |
| #1128 | **YES** | ✅ | ✅ | ✅ | ✅ Deep | ✅ | champion-ratio target/formula + valley-prep timing model + sponsor signals = design choice |
| #1129 | **YES** | ✅ | ✅ | ✅ | ✅ Deep | ✅ | fatigue saturation threshold + counting rule + deployed-vs-adopted method = design choice |
| #1130 | **YES** | ✅ | ✅ | ✅ | ✅ Deep | ✅ | hypercare tier SET + classification scoring (sev×likelihood×blast) + tier→support map = design choice |
| #1131 | SKIP-where-trivial | ✅ | ✅ | ✅ | ✅ Deep | ✅ | measurement mechanics on #1130's tiers; record-fields + report-format are largely determined once #1130 sets the tiers |
| #244 | **YES** | ✅ | ✅ | ✅ | ✅ Deep | ✅ | 3-tier gate ladder + per-tier pass/fail conditions + short-circuit order = design choice |
| #245 | **YES** | ✅ | ✅ | ✅ | ✅ Deep | ✅ | shadow→warn→enforce model + rollout-cycle attribute + persistence surface = design choice |
| #246 | **YES** | ✅ | ✅ | ✅ | ✅ Deep | ✅ | typed-edge schema (which 4 of the 7; how derived/classified; render form) = design choice — see D-246 |
| #248 | **YES** | ✅ | ✅ | ✅ | ✅ Deep | ✅ | 8 detector signatures + thresholds + RACI gate rule = design choice (P1-Critical; failure-mode 5-field conformance G7) |
| #252 | SKIP-where-trivial | ✅ | ✅ | ✅ | ✅ Deep | ✅ | State set + transitions fixed by the canonical Artifact Workflow (§3.2; 5 states post-re-spec — see Change Description row); the only choice (30-day zombie cutoff) is stated in the AC — low design uncertainty |
| #212 | SKIP-where-trivial | ✅ | ✅ | ✅ | ✅ Deep | ✅ | RECONCILE of an already-declared mode onto an existing script; close-out checklist sequence is determined by Stage-13 spec — wiring, not design |

**S9 review depth = Deep** for every card per `novel` class (Stage-9 depth = Deep) [SOURCE: release-class-taxonomy.md].

**Note (release-level):** Stage 4 Phase A1.5 domain classification → `domain: governance` (entire File Change Matrix is internal pmo-platform skill/reference artifacts) → **external-sourcing-exempt**, domain-classified [SOURCE: stage-04-planning.md]. `domain_practice: { source: N/A — pipeline-internal release, date: 2026-06-14, domain: governance }`.

---

### Contention Map

Files shared across cards, with overlap classification. `append-pattern` = new sections/reference-doc additions (structurally HIGH, operationally LOW merge-conflict risk per ADR-005); `line-range-overlap` = edits to the same existing region (retains sequencing mitigation) [SOURCE: stage-04-planning.md].

**Hotspot 1 — `operations/skills/change-management/SKILL.md` (5 editors): #1127, #1128, #1129, #1130, #1131**
- Overlap class: **append-pattern** within the modes/reference-table region — each card adds a capability block + a reference-docs-table row. #1130/#1131 both edit the **Mode D** section (line-range-overlap on the Mode-D exit-criteria + hypercare blocks).
- Mitigation: single serialized CM run (sequence 1→5); #1130 before #1131 keeps the Mode-D tier vocabulary stable. The three adoption-science split cards (#1127/#1128/#1129) must keep adoption-tracking framing consistent (shared Output-Format conventions).

**Hotspot 2 — `operations/skills/change-management/references/hypercare-plan.md` (2 editors): #1130, #1131**
- Overlap class: **line-range-overlap** — #1130 adds tier set + classification rule + tier→support map; #1131 adds SLA-record fields + compliance-report format to the same doc.
- Mitigation: #1130 first (defines tiers), then #1131 (adds tracking against them). True dependency edge — not just contention.

**Hotspot 3 — `release/skills/release-executor/SKILL.md` (3 editors): #244, #245, #212**
- Overlap class: **append-pattern** — #244 adds the gate-ladder block, #245 adds the rollout-model block, #212 builds out the existing **Mode D** section. #244/#245 are adjacent (execution-flow); #212 is a separate mode section.
- Mitigation: serialized RE run (sequence 6→8). #245's rollout-cycle is specified to apply to #244's gates → land #244 then #245.

**Hotspot 4 — `release/skills/release-executor/references/execution-checklist.md` (2 editors): #244, #245**
- Overlap class: **line-range-overlap** — both wire mechanism into the same execution-checklist (gates before applying changes / rollout-cycle read).
- Mitigation: #244 then #245, same commit-run.

**New shared file — `release/skills/release-executor/references/progressive-rollout.md` (#245 creates; #244 AC references execution-checklist OR this file):** #245 owns creation; #244 wires gates into execution-checklist (not this file) — no true co-creation contention, but coordinate so cross-links resolve.

**No cross-card contention** for #248 (`core/skills/pmo-qa-auditor/SKILL.md` + new `references/failure-mode-detectors.md`) or #252 (`operations/skills/artifact-generator/SKILL.md` + new `references/lifecycle-states.md`) — each is the sole editor of its skill.

**File Change Matrix (machine-readable — one path per line per Baseline-pin awareness):**

EDIT (existing files):
```
operations/skills/change-management/SKILL.md
operations/skills/change-management/references/hypercare-plan.md
core/skills/pmo-qa-auditor/SKILL.md
operations/skills/artifact-generator/SKILL.md
release/skills/release-planner/SKILL.md
release/skills/release-planner/references/dependency-analysis.md
release/skills/release-planner/references/release-plan-template.md
release/skills/release-planner/references/output-templates.md
release/skills/release-executor/SKILL.md
release/skills/release-executor/references/execution-checklist.md
```

ADD (new reference docs):
```
operations/skills/change-management/references/adkar-assessment.md
operations/skills/change-management/references/adoption-tracking.md
operations/skills/change-management/references/fatigue-and-outcomes.md
core/skills/pmo-qa-auditor/references/failure-mode-detectors.md
operations/skills/artifact-generator/references/lifecycle-states.md
release/skills/release-executor/references/progressive-rollout.md
release/skills/release-executor/references/close-out-checklist.md
```

DELETE: none.

(Per-card file ownership — for sequencing the serialized runs:)
| Card | EDIT | ADD |
|---|---|---|
| #1127 | change-management/SKILL.md | references/adkar-assessment.md |
| #1128 | change-management/SKILL.md | references/adoption-tracking.md |
| #1129 | change-management/SKILL.md | references/fatigue-and-outcomes.md |
| #1130 | change-management/SKILL.md, references/hypercare-plan.md | — |
| #1131 | change-management/SKILL.md, references/hypercare-plan.md | — |
| #248 | pmo-qa-auditor/SKILL.md | references/failure-mode-detectors.md |
| #252 | artifact-generator/SKILL.md | references/lifecycle-states.md |
| #246 | release-planner/SKILL.md, references/dependency-analysis.md, references/release-plan-template.md, references/output-templates.md | — |
| #244 | release-executor/SKILL.md, references/execution-checklist.md | (references/progressive-rollout.md per #245) |
| #245 | release-executor/SKILL.md, references/execution-checklist.md | references/progressive-rollout.md |
| #212 | release-executor/SKILL.md | references/close-out-checklist.md |

---

### Risk Register

| # | Risk | Type | Sev | Mitigation | Owner |
|---|---|---|---|---|---|
| R1 | **5-way contention on `change-management/SKILL.md`.** Concurrent or out-of-order Engineering chips race on branch HEAD (stale-base / non-fast-forward) under D-C SINGLE. | Contention | HIGH | Serialize the CM run (seq 1→5) as ONE chip-at-a-time per hub-spoke-bridge.md; hub confirms each commit landed on `release/v2.00` before routing the next [SOURCE: hub-spoke-bridge.md]. | Hub (Procedure 2) |
| R2 | **#1130→#1131 hard ordering violated.** If #1131 lands first, it tracks SLAs against a tier vocabulary that doesn't yet exist → incoherent Mode-D. | Dependency | HIGH | Enforce #1130 before #1131 in the sequence; both edit hypercare-plan.md (line-range-overlap) so order is doubly load-bearing. | Hub / Stage 6 |
| R3 | **#212 mode-stub-vs-build-out — INVERTED risk.** Mode D already exists in `release-executor/SKILL.md` and wraps `automated-closeout.sh` (which already enumerates open milestone issues at Phase 4 `detect_open_issues`). The gap is NOT a missing mode — it's the AC's *specific documented elements*: issue-closure-audit as a narrated Mode-D step, **carry-forward/deferred-item tracking** (grep: absent), and **`references/close-out-checklist.md`** (absent). Risk = builder either (a) re-implements existing mechanism, or (b) marks the card done because "Mode D exists" without adding the missing AC elements. | Scope | MEDIUM | Stage 5/6 must scope #212 as a **documentation/reference build-out onto existing Mode D** (wire the audit + add carry-forward output + add close-out-checklist.md), reusing `automated-closeout.sh` Phase 4 rather than duplicating it. Verify all 6 AC checkboxes individually. | Stage 5/6 |
| R4 | **#212 AC cites a stale RELEASE_LOG path.** AC names `pmo-platform/governance/RELEASE_LOG.md`; the file actually lives at `release/releases/RELEASE_LOG.md` [SOURCE: `find . -name RELEASE_LOG.md`]. | Scope (AC currency / G-PL1) | MEDIUM | **G-PL1 CURRENCY-MISMATCH finding** → Tier 1 [ADJUST]: correct the path in #212's AC (via `gh issue edit --body`) before Stage 6, OR the builder uses the live path and notes the deviation. The existing Mode D already targets the correct path, so the mechanism is fine — only the AC text is stale. | Operator / Hub (G-PL1) |
| R5 | **#246 stale "Blocked by #409".** Acting on the blocker as-written would defer a buildable card indefinitely (#409 is an open epic). | Dependency (stale) | MEDIUM | **D-246-Blocker resolves to INCLUDE** (see D-gate). Tier 1 [ADJUST]: clear/annotate the #246 blocker field; #246 references the existing `frontmatter-schema §Category 4` vocabulary. | Operator (D-gate) |
| R6 | **#248 is P1-Critical + carries the heaviest design + conformance load** (8 detectors, each must pass failure-mode 5-field G7 structural conformance + the LLM-graded content check). Generic/non-actionable detector entries fail G7. | Scope / Quality | MEDIUM | Stage 5 Solutioning ON; each detector authored to the 5-field template per `failure-mode-standard.md`; Stage 8 verifies G7 + G1–G7 regression [SOURCE: #248 AC]. | Stage 5/8 |
| R7 | **Reference-doc overlap (#1127).** An `adkar-framework.md` already exists in change-management/references; #1127 adds a NEW `adkar-assessment.md`. Risk of duplicated ADKAR content / reader confusion. | Scope (dup-source) | LOW | #1127 builder reconciles: `adkar-framework.md` = methodology reference; `adkar-assessment.md` = the assessment-capability spec — cross-link, don't duplicate (duplicate-source-discipline register-or-remove). | Stage 6 |
| R8 | **Rollback complexity — low.** Every change is a skill-file/reference-doc edit on a single release branch; no schema migration, no data, no external system. Whole-release rollback = `git revert <merge-SHA>`; per-card rollback = revert the card's commit. | Rollback | LOW | Standard release-branch rollback per RELEASE_PROTOCOL. Reversibility tier of the release as a whole: **MODERATE / HIGH** (skill edits revertable; no irreversible state). | Release-executor Mode C |
| R9 | **`novel`-class Stage-5 fan-out (8 of 11 cards).** 8 Solutioning spokes is the widest non-trivial parallel wave; the change-management Stage-5 spokes reason about the same SKILL.md design surface. | Capacity / Coherence | LOW | Quota PASS (below). Coherence: the 3 adoption-science + 2 hypercare split Stage-5 outputs should be cross-checked for adoption/hypercare-framing consistency before Stage 6 (Collective-Review-style N-way check). | Hub |

---

### Quota Budget

**Verdict:** PASS (per `quota-budget-protocol.md` Checkpoint A) [SOURCE: stage-04-planning.md routing tree]
**Parallel-eligible spokes per parallel stage (from the Stage Applicability Matrix; parallel-safe stages = 5/7/8 per hub-spoke-bridge.md):** Stage 5: **8** (#1127,#1128,#1129,#1130,#244,#245,#246,#248 — the YES rows) · Stage 7: **11** · Stage 8: **11**
**Per-spoke cost estimate:** size-bucket band (mostly size:S, three size:M) — heuristic, no telemetry medians available in this worktree [SOURCE: quota-budget-protocol.md § size-bucket band until telemetry; `[CALIBRATE-AFTER-3]` MEDIUM]
**Assumed/stated remaining usage-window envelope:** conservative default — fresh full per-account 5-hour window (operator did not state quota state at hub start) [ASSUMPTION – CONFIRM]
**Estimated cumulative draw % (worst parallel batch = Stage 8, 11 QA spokes):** 11 × size-S/M skill-regression spoke << 50% of a fresh window. Skill-regression/QA spokes are read-heavy + bounded (no large code-build); cumulative draw estimated <50% band.
**Routing:** PASS → proceed parallel; no warning in plan.
**Note:** Checkpoint B re-validates at every parallel wave at runtime (load-bearing) with PROCEED/SERIALIZE/DEFER/REDUCE-scope [SOURCE: hub-spoke-bridge.md Step 5.5]; STAGGER is a secondary rate-limit-only defense, not a usage-window mitigation. If the operator's actual remaining envelope at the Stage-8 wave is <full, re-run Checkpoint B — 11 concurrent spokes is the binding wave and could cross into WARN on a depleted window. Bands + cumulative-draw budget are `[CALIBRATE-AFTER-3]` MEDIUM.

**Important — Stage 6 is NOT a parallel wave here.** Under D-C SINGLE topology Engineering serializes (one chip at a time), so Stage 6 draws no cumulative parallel cost — but it is the longest *wall-clock* stage because of the 5-card serialized change-management run (R1). The quota constraint is the Stage-8 fan-out; the throughput constraint is the Stage-6 CM serialization.

---

### Recommendations (merge/split + Release Class)

**Engineer the same-skill cards as coherent commit-runs, NOT as a single squashed commit, and NOT bin-packed by count** [applying composition-is-judgment-not-bin-packing — merge by capability-coherence + dep-edges, never by issue-count]:

- **change-management run (#1127→#1128→#1129→#1130→#1131):** keep as **5 separate commits** in one serialized run on `release/v2.00`. Each card is a distinct capability (ADKAR / adoption-tracking / fatigue-outcome / hypercare-tiering / hypercare-SLA) with its own reference doc and its own AC checklist + regression gate. Do NOT merge into one commit — that would erase per-card traceability and per-card regression. The coherence requirement (cards reference shared Output-Format + Mode-D vocabulary) is satisfied by *sequencing + cross-review*, not by collapsing commits. **#1130 before #1131 is mandatory** (true edge).
- **release-executor run (#244→#245→#212):** keep as **3 separate commits**, one run. #244+#245 are tightly coupled (same execution-checklist; #245's rollout-cycle applies to #244's gates) — land adjacent. #212 (Mode-D build-out) is a separate mode section — land last in the run.
- **#248, #252:** standalone single-card commits; independent files; slot at sequence 9–10.

**No split recommended.** The oversized upstream cards were already split (into #1127–#1131); the open cards are correctly atomic (size:S/M). No card needs further decomposition.

**Out-of-scope discoveries (noted, NOT added to the milestone per scope):**
1. **#212 AC path-staleness** (R4) and **#246 stale blocker** (R5) are AC-currency defects (G-PL1). They warrant a Tier 1 [ADJUST] `gh issue edit --body` before Stage 6 — surfaced for operator action, not added as new issues.
2. **#1127 reference-doc overlap** (R7, `adkar-framework.md` vs new `adkar-assessment.md`) — a duplicate-source-discipline reconciliation the #1127 builder should handle inline.

**Release Class declaration:**
```
## Release Class
Class: novel
Rationale: ≥1 issue introduces a new reference doc (7 new reference docs across the bundle — novel trigger (a)) AND ≥1 D-class decision in the release plan (D-246-Blocker, D-ReleaseClass — novel trigger (b)). Cross-cutting triggers do NOT fire: the File Change Matrix touches skill SKILL.md + skill-reference files, not ≥3 pipeline/stage-*.md files and not ≥3 of the named governance surfaces; only one in-bundle compositional edge exists (#1130→#1131), below the ≥3-edge cross-cutting trigger (c).
Differentiation posture:
  - Engagement density: Standard
  - Stage 9 review depth: Deep
  - Stage 5 activation bias: ALL
  - Stage 13 outcome-window: 30-day
```
[SOURCE: release-class-taxonomy.md novel triggers (a)+(b); cross-cutting triggers (a/b/c) none fire; multi-trigger resolution; per-class mapping]

---

### Operator Decisions (D-gates)

#### D-246-Blocker: Include #246 (typed artifact linking) in v2.00, or defer until #409 ships?
**Gate input:** #246 body states "Blocked by: **#409** — must ship first." Empirical corpus investigation of whether #246 needs the full #409 epic or only the existing in-corpus typed-relationship vocabulary.
**Pre-decided (if applicable):** None — the hub flagged this as the release's key D-decision for empirical determination.
**Gate decision:** (A) **INCLUDE #246 this release** using the existing in-corpus vocabulary, clear/annotate the stale #409 blocker; OR (B) **DEFER #246** until #409 closes.
**Recommendation: (A) INCLUDE #246 this release.** Evidence:
  - The **7 MVP relationship types are already built and frozen.** `core/ADRs/ADR-018-work-item-type-layer.md`: "The **7 MVP relationship types** (`GENERATES` / `DEPENDS_ON` / `BLOCKS` / `SUPERSEDES` / `BELONGS_TO` / `RELATES_TO` / `ASSIGNED_TO`), built and frozen in…".
  - The **SSOT defining all four types #246 needs exists today**: `core/schemas/frontmatter-schema.md` (§Category 4) defines GENERATES/DEPENDS_ON/BLOCKS/SUPERSEDES with directionality + examples. #246 needs exactly these 4 (a subset of the frozen 7).
  - **#409's own body declares #246 composes against the BUILT vocabulary, not the epic's remaining work**: #409 body — "**Composes against (not redefined) — relationship vocabulary already built:** #246 / #159 / #334 / #362 adopt the canonical 7 MVP types (`frontmatter-schema §Category 4`)."
  - **#409 will NOT close this release**: it is an OPEN `type:epic` tracking-umbrella (`status: approved`, no milestone) with deferred children under a separate milestone `declarative-workitem-type-model` [SOURCE: `gh issue view 409 --json state,labels,body`].
  - The vocabulary is **already in use in #246's own edit-target file**: the typed family appears in `release/skills/release-planner/references/release-plan-template.md` and 18 other corpus files [SOURCE: `grep -rlE 'DEPENDS_ON|BELONGS_TO|BLOCKS|SUPERSEDES' --include='*.md'`].
  Conclusion: the "Blocked by #409" edge is **stale** — #246's AC ("consistent with the typed-linking vocabulary used elsewhere in the corpus, BELONGS_TO/DEPENDS_ON/BLOCKS family") is satisfiable today by referencing `frontmatter-schema §Category 4`. Deferring would block a buildable card on an epic that will not close.
**Blocks:** #246's Stage 5/6 (and the v2.00 scope-commit — whether the release contains 10 or 11 cards).
**Upstream compatibility:** N/A — this D-decision does not modify skill-authoring surface (it is a scope/dependency determination about an existing skill's planned mode-add; it does not change required/optional frontmatter, file layout, or naming for NEW skills). Upstream compatibility check does not apply.
**Reversibility / Confidence:** CHEAP / HIGH — including #246 is reversible (drop it from scope at Stage 9 if Solutioning surfaces an unexpected #409 coupling); the evidence that the vocabulary is built-and-frozen is direct (ADR-018 + frontmatter-schema + #409's own body), so confidence is HIGH.

**Decision-discipline mechanisms applied** (D-class → full M1/M2/M3 per `decision-discipline.md`):
- **M1 Localization:** The load-bearing "platform context" (the #409 blocker) is itself a point-in-time artifact in #246's body → Audit-Snapshot Reconciliation fires. Reconciled against canonical sources (ADR-018, frontmatter-schema, live `gh issue view 409`) rather than trusting the body's blocker field. Result: blocker stale.
- **M2 Opposing View:** Strongest case for DEFER — "#409 is literally listed as a hard blocker; honoring it is the safe reading." Rebuttal: #409's own body *explicitly carves #246 out* as composing-against-built-vocab; the blocker predates the build that froze the 7 types. The opposing view rests on the stale field; the evidence post-dates and overrides it.
- **M3 Pattern Cache Scan** (domain: release-ops): Applicable confirmed patterns — verify-before-recommend (verify non-canonical input vs git/grep/issue-state before recommending) → directly applied (grepped corpus + read ADR + checked live #409 state). Re-evaluate-ticket-against-architecture (reconcile ticket against live architecture/ADRs; ticket-age-vs-architecture = staleness signal) → directly applied (#246's blocker is stale vs the ADR-018-frozen vocabulary). Both imply INCLUDE. No new observation to log (no operator correction this session).

---

#### D-ReleaseClass: What Release Class does this release carry?
**Gate input:** Spoke-proposed class + trigger-condition evidence per `release/references/specs/release-class-taxonomy.md`.
**Gate decision:** Choose between (A) routine, (B) **novel**, (C) cross-cutting, (D) hotfix.
**Recommendation: (B) novel.** Triggers fired: novel (a) ≥1 new reference doc (7 new docs in the bundle); novel (b) ≥1 D-class decision in the plan (D-246-Blocker). Cross-cutting triggers (a) ≥3 pipeline/stage-*.md — NOT met (0); (b) ≥3 named governance surfaces — NOT met (0); (c) ≥3 in-bundle compositional edges — NOT met (1: #1130→#1131). routine disqualified (new files + D-decisions present). hotfix disqualified (not corrective/defect-driven). Multi-trigger resolution: only novel fires → novel [SOURCE: release-class-taxonomy.md].
**Blocks:** Milestone-description "## Release Class" section; downstream per-class differentiation posture (Stage 9 depth = Deep; Stage 5 bias = ALL).
**Upstream compatibility:** N/A — Release Class is PMO platform internal taxonomy; no Anthropic upstream surface. Upstream compatibility check does not apply.
**Reversibility / Confidence:** CHEAP / HIGH — re-classifiable later with operator approval per release-class-taxonomy.md Re-Classification Protocol (cheaper-to-stricter, e.g., novel→cross-cutting, is CHEAP/HIGH if Stage 5/7 surfaces wider blast radius).

**Decision-discipline mechanisms applied:**
- **M1 Localization:** classification localizes to *this platform's* taxonomy (the `cross-cutting` value is PMO-specific). Applied the platform's own trigger table, not a generic ITIL change-class.
- **M2 Opposing View:** Strongest case for `cross-cutting` — "11 cards across 5 skills is a lot of surface." Rebuttal: the cross-cutting triggers are explicitly *count-gated* (≥3 stage files / ≥3 named governance surfaces / ≥3 in-bundle edges) and the `cross-cutting` anti-pattern warns against classifying by aggregate issue-count alone; this release touches skill-capability surfaces, none of the three count-gates met → novel is correct.
- **M3 Pattern Cache Scan** (domain: release-ops): composition-is-judgment-not-bin-packing applies to the adjacent merge/split recommendation (don't bin-pack by count) and reinforces NOT reading the 11-card count as a cross-cutting signal. No new observation to log.

---

#### NOTE (not a decision) — Version mapping
The milestone uses the `01-FNH-…` naming convention. The plan was drafted against the then-current v3.x line: `.version` = **v3.19** [SOURCE: `.version` at plan time], latest tag = **v3.20** [SOURCE: `git tag --sort=-v:refname` at plan time], with a likely-next draft string of `v3.21`. **The operator set the released version to `v2.00` at scope-commit** (repo re-versioning lineage); this committed plan uses **v2.00** throughout. This NOTE does not gate Stage 6; the version string is fixed at Engineering Commit 0 / Stage 12.

---

*Plan authored by Stage 4 spoke (Release Manager — Release Planning persona). Read-only on the code tree; the only writes at Stage 4 were the planning comment + closing the Stage-4 sub-task. Per-issue closure phrased SAFE throughout (cards are marked closed at Stage 13, never auto-closed from this plan). Scope = the 11 open build cards; the 3 closed cards are provenance only.*

---

## Change Description

Outcome: this release hardens five existing PMO function-skills with structured adoption-science, hypercare, execution-gate, close-out, and detector capability — 11 build cards across change-management, release-executor, pmo-qa-auditor, and artifact-generator, all EXTEND/RECONCILE (no new skills). Engineering Commit 0 (this PR) lands the committed release plan and the first card of the serialized change-management run (#1127). Before this release, change-management exposed ADKAR only as a methodology lens inside the impact/readiness/hypercare modes; after #1127, it carries a named **Mode G — Adoption Tracking** that surfaces an operator-runnable ADKAR barrier-assessment plus training-timing validation, with the methodology consumed by reference from the existing single-source framework doc (no duplicated scale or rule).

Issues resolved (this PR): #1127 (change-management ADKAR barrier-assessment + training-timing validation — Mode G surfacing) is implemented. It is marked closed at Stage 13 (not at this PR's merge), preserving the ordered close-out. The remaining 10 cards land in their own Stage-6 commits in the sequence above. The committed release plan (Engineering Commit 0) is established.

Per-card delta — issues resolved on the release branch (status: DONE = built + committed; close at Stage 13):

| # | Outcome | Status |
|---|---------|--------|
| #1130 | change-management **Mode D (Hypercare)** gains a risk-classification step + a stable 3-tier set (**HC-T1 Critical / HC-T2 Elevated / HC-T3 Routine**). Scoring = `Severity × Likelihood × Blast-Radius` (1–75); Severity+Likelihood adopted **by reference** from `delivery-engine/references/raid-templates.md §5.1/§5.2` (single-source — scale not restated), only Blast-Radius (1–3) net-new; band edges 30/10/1 are `[INFERRED]` for Stage-9 ratification. New `## Hypercare Risk Classification` section in `references/hypercare-plan.md` **references, does not replace** the existing 3-Tier Support model + S1–S4 severity→SLA matrix (orthogonal management-posture axis). Running Mode D now produces a tiered hypercare risk register. The frozen tier tokens `{HC-T1, HC-T2, HC-T3}` + per-tier committed (response/resolution) SLA are the contract sibling card #1131 measures against. | DONE |
| #1131 | change-management **Mode D (Hypercare)** gains the **tracking + reporting half** on top of #1130's tiers. New Mode-D **step 4.5** records each hypercare incident as an existing-prefix `R-CM-###`/`I-CM-###` RAID entry (HC-tier + open/resolve time + **met/breached verdict** computed against the tier's committed SLA, read **by reference** from #1130's §Tier → Support Model / Escalation / Committed SLA — not restated), then produces a **hypercare compliance report** (per tier: events, met-vs-breached, breach detail). **Open SLA breaches are a hard, non-waivable Mode-D exit gate** (added to SKILL.md Mode D + `references/hypercare-plan.md` §Exit Criteria + Exit Decision Model). New `### SLA Tracking and Compliance Reporting` subsection in `hypercare-plan.md` (record fields + report format) + a 10th Required-Sections row; Reversibility Discipline extended to the SLA verdicts/report (G4); one new PROC failure mode ("SLA verdict asserted without the timestamp pair"; 8→9). No new tracker; tier vocabulary unchanged (frozen by #1130). | DONE |
| #244 | **release-executor** gains a canonical `## Quality-Gate Ladder` — a **three-tier pre-apply gate ladder (T1 schema validation → T2 cross-reference integrity → T3 stakeholder approval)** that fires **in order and short-circuits** (a T1 hard-fail HALTs before T2/T3 run; the testable headline AC). The ladder is wired at **two entry points** from one source (per the Stage-5 F1 finding): the `references/execution-checklist.md` Pre-Execution surface (live **git-native** PR-merge path) **and** Mode A Step 5 (Cowork snapshot-apply lineage) — neither lineage left ungated. It is a **shift-left** of the existing Mode B verification dimensions and **reuses existing instruments** (F2): T1 = `lint_release_corpus.py --check schema` + a SKILL-frontmatter field-set assertion; T2 = `check-doc-links.py --target-paths '<changed-files>' --require-targets` (broken-refs; **exit 1 or exit 3 = FAIL** per F4 — an unverifiable surface is not a passing gate) — no new detectors invented. Each FAIL emits a **5-field actionable finding** (tier / what failed / where / what to fix / short-circuit-skipped). Each gate row carries a **`rollout-cycle` column defaulting to `enforce`** — the explicit seam sibling card #245 attaches its shadow→warn→enforce progressive-rollout to (F5; build #244 first, #245 wraps). Additive change (new section + two in-mode pointers); no existing Mode A–G step semantics altered; `version:` bumped v1.14 → **v2.00**. Files: `release-executor/SKILL.md`, `references/execution-checklist.md`. | DONE |
| #245 | **release-executor** gains a **three-cycle progressive-rollout model** — **shadow** (evaluate + log, never block) → **warn** (evaluate + surface a notice, never block) → **enforce** (evaluate + block / short-circuit) — that wraps #244's quality-gate ladder. Each rule/gate carries a **`rollout-cycle` attribute** (values `{shadow, warn, enforce}`; **default `enforce`**; **fail-safe to `enforce`** on an absent/unparseable value — adoption is behavior-preserving) that the executor reads to dispatch log-only / notice / block. Shadow+warn hits persist to a per-rule append-only **outcome-log** at `core/hooks/<rule-id>-rollout-log.jsonl` so the operator reviews hit-rate before advancing; advancement is an **operator-gated Advance Checklist** (never auto-promoted by hit-count). **Seam with #244:** the rollout-cycle attaches per gate Tier — a **shadow/warn gate does NOT short-circuit** the ladder (observe / notice + continue); only an **enforce** gate fails-and-short-circuits — so #244's "fires in order, short-circuits on first failure" holds, scoped to enforce gates, and each gate becomes roll-out-able. **Generalizes** the existing `.claude/hooks/.mode` warn/enforce/off precedent + the gate-criteria per-criterion warn→enforce rollout; the **one net-new tier is `shadow`** (silent, persisted-but-not-surfaced observation). New `references/progressive-rollout.md` (canonical model + attribute + outcome-log + Advance Checklist + precedent note + seam dispatch routine); SKILL.md sharpens the seam paragraph + adds a Step-5 dispatch read + a Reference-Files entry; execution-checklist adds the dispatch table. Additive; no existing Mode A–G step semantics altered; `version:` stays **v2.00**. Built via pmo-skill-editor Mode A. Files: `release-executor/SKILL.md`, `references/execution-checklist.md`, `references/progressive-rollout.md`. | DONE |
| #248 | **pmo-qa-auditor** (P1-Critical) gains an **8-detector failure-mode battery** plus a **RACI validation gate (G9)**. New reference doc `references/failure-mode-detectors.md` carries the catalog `FMD-1..FMD-8` (surfaced **D1–D8**): automation complacency (PROC), faceless PMO (OUT), echo chamber (INPUT), quality drift (OUT), SPOF (TRIG), breadth burnout (PROC), AI hallucination (INPUT), trust erosion (HAND) — each authored to the failure-mode **5-field template** + category tag + observable signature + numeric threshold + current-status read + surface (so G7 structural conformance holds). Thresholds are **lifted from corpus precedent, not invented**: SPOF `>=3 contexts` from pmo-technical-analyst; push-to-resolve `>=80%` from this skill's Mode C; echo-chamber `round-over-round divergence` from eval-writer F-30; RACI single-Accountable from delivery-engine gate-definitions; hallucination zero-tolerance from the platform guardrail. The 2 operator-specific absolutes (D6 concurrency ceiling, D8 walk-back rate floor) are **config-referenced** (`platform-config.toml [failure_mode_detectors]`), not canonicalized inline. SKILL.md edits (Mode A G9 gate category + gate-table row; Mode E battery step emitting a `## Failure-Mode Detector Battery` section in `findings-register.md` + the SUMMARY echo detector-status line; Mode E Scope note; Reference-Docs row). The auditor's own `## Domain-Specific Failure Modes` section is **untouched** (the 8 detectors are detection infrastructure, not the auditor's own authoring surface — G7 self-conformance preserved: 5 entries unchanged). Additive; no existing gate (G1–G8) or mode (A–E) semantics altered; mode count stays 5. `version:` bumped v1.21 → **v2.00**. Built via pmo-skill-editor Mode A; all added SKILL.md + ref-doc prose reference-durable (no bare issue refs / markdown links / raw URLs). Files: `pmo-qa-auditor/SKILL.md`, `references/failure-mode-detectors.md`. | DONE |
| #252 | **artifact-generator** gains **lifecycle state tracking + zombie detection + a documentation-debt register** (P2; EXTEND Generate + Wrapper modes). **RE-SPEC (operator decision):** the build now adopts the **platform-canonical Artifact Workflow vocabulary** — field **`artifact_state:`** (not `status:`) with the canonical **5 states `DRAFT → REVIEWED → APPROVED → PROMOTED → ARCHIVED`** (object-typed `Artifact-<STATE>` in prose) per `core/standards/lifecycle-states-canonical.md` §3.2 — **resolving** the deviation originally flagged for Stage-9 (the prior `status:`/6-Title-case set collided with the forward-binding canonical contract). Every emitted artifact now stamps **`artifact_state: DRAFT`** on emit (`Artifact-DRAFT`); the prior `status: PENDING_REVIEW` → now `artifact_state: DRAFT`. The original 6-state intent maps onto the canonical 5 — Draft→DRAFT, Review→REVIEWED, Approved→APPROVED, **Active→PROMOTED** (in-use = promoted to target folder), Archived→ARCHIVED; **`Superseded` has no canonical state** and is represented via the **documentation-debt register** (a no-longer-current artifact still in a live, non-`ARCHIVED` state is flagged for the `ARCHIVED` transition), not as a frontmatter value. Contained field rename (verified: no skill outside artifact-generator reads the literal value) propagated across both frontmatter blocks (Generate Step 5 + Wrapper Mode), the C7 chained-contract line, the Auto-Archive Policy, the 3 status-citing failure modes, the Reversibility structured-frame bullet, and the 3 routing/wrapper reference docs. The **Artifact Health Check** zombie-detection step now flags artifacts unreferenced **> 30 days** with **`artifact_state` ≠ `ARCHIVED`** (flag-not-transition); the named **documentation-debt register** lists zombies + no-longer-current-but-live artifacts (each row carrying recommended action + reversibility·confidence; `none` when empty). `references/lifecycle-states.md` is re-specced as the **artifact-generator application layer over §3.2** — it **references** the canonical source as the authoritative vocabulary/state-machine home and does **not** restate the state machine (duplicate-source-discipline); it documents the stamping rules, the original-intent→canonical mapping (incl. the Superseded→debt-register decision + rationale), zombie/debt logic, and the `APPROVED → PROMOTED` promotion transition. The old "ticket-versus-architecture gap" section is removed (gap resolved). The 30-day cutoff still aligns with the platform-canonical 30-day source-artifact staleness threshold. `version:` stays **v2.00** (same-release re-spec). Re-specced via pmo-skill-editor Mode A (impact + coherence + regression PASS); all SKILL.md + lifecycle-states.md prose reference-durable (canonical link under `allow-link`; no bare issue refs / raw URLs); canonical ref resolves clean under `deploy.sh --check`. SKILL.md pre-existing >500-line count (out of #252 scope to refactor). Files: `artifact-generator/SKILL.md`, `references/lifecycle-states.md`, `references/wrapper-mode.md`, `references/tech-doc-routing.md`, `references/prd-routing.md`. | DONE |
| #212 | **release-executor** Mode D (Close Release) — **build-out, not rebuild** (RECONCILE; the inversion finding: Mode D + `automated-closeout.sh` already exist). Three documented elements wired onto the existing mode: (1) a new explicit **Step 2.5 issue-closure audit** that narrates the script's Phase 4 open-issue enumeration (`gh issue list --milestone <slug> --state open`) as a **blocking finding** — a non-zero open-issue count blocks a clean Milestone close and routes to either auto-close-anomaly manual close (at apply) or carry-forward (Step 6), never a silent close-over-open-issues; (2) **carry-forward / deferred-item tracking** added as a **named Step-6 close-out output** (previously present in the script's Phase 9 scaffold + chore-PR `Deferred items` line but un-narrated in SKILL.md) — two surfaces (chore PR body compact line + Stage-13 sub-task full enumeration), following the canonical deferred-item procedure (enumerate → status-deferred label → de-milestone → comment trail → summarize); reported every close-out (`none (clean close)` when empty) so nothing scoped-but-unshipped is dropped; (3) new reference doc `references/close-out-checklist.md` enumerating the Stage-13 checklist (audit → milestone → log → branch → evidence → carry-forward) Mode D follows, named from the Mode D section + Reference Files. Milestone closure + RELEASE_LOG finalization reference the canonical `release/releases/RELEASE_LOG.md` path. Additive documentation build-out; no existing Mode A–G step semantics altered; the script is not modified; `version:` stays **v2.00**. Built via pmo-skill-editor Mode A; all added lines reference-durable (no bare issue refs / markdown links / raw URLs in SKILL.md or the new checklist). Files: `release-executor/SKILL.md`, `references/close-out-checklist.md`. | DONE |
| #246 | **release-planner** gains **typed artifact linking** — a generated release plan now labels each in-release edge with one of four **§Category 4 artifact-relationship types** (**GENERATES / DEPENDS_ON / BLOCKS / SUPERSEDES**), so the operator reads *how a release's artifacts relate* instead of inferring the graph by hand (the testable AC: the dependency view labels each edge with one of the 4 types, not a bare link). The vocabulary is **ADOPTED verbatim** from the ADR-018-frozen `core/schemas/frontmatter-schema.md` §Category 4 (a subset of the 7 MVP types) — referenced, not redefined, so planner output and backlog relations cannot diverge (the prior "#409 blocker" was reconciled-stale per D-246). **Critical orthogonality (the load-bearing build decision):** the artifact-relationship type is a **second, independent edge attribute** added alongside the existing `edge_type` (FS/SS/FF/SF PMBOK **scheduling-precedence** axis the CPM forward-pass consumes) — the two compose on one edge (an edge can be both `BLOCKS` and `FS+0d`) and the **CPM longest-path math never reads the artifact-relationship field**, so the regression delta is provably additive. Edges are typed **deterministically** (native `blocks`→BLOCKS; body-dep→DEPENDS_ON default; File-Change-Matrix Create→GENERATES; version-supersession Modify→SUPERSEDES), with default-to-DEPENDS_ON as the weakest-correct-claim safety net and an explicit empty-state positive-signal body when zero edges type. New `## Artifact-Relationship Classification` section in `references/dependency-analysis.md` (derivation rule table + edge-case dispositions + the §Cat-4 SSOT cite); `### Artifact Relationship Graph` H3 + an `Edge Type` column + Mermaid edge-labels added to `references/release-plan-template.md`; the typed-graph block added to the Mode A example in `references/output-templates.md`; SKILL.md gains the 4-type definition list, a Mode A Step 3 + Mode B Step 5d classify-and-emit, the Output-Format row update, and **one new PROC failure mode** ("Artifact-relationship type conflated with FS/SS scheduling type"; 13→14). The `bundle-issues-parser.py` `--json` output is **read-only-extended** with an additive `artifact_relationships` field (91 insertions / 0 deletions; `parse_dependencies`/contention/`emit_tsv` untouched; native `blocks` is unavailable via `gh issue view --json` here, so the derivation runs the spec's degraded-but-complete path); self-test OK. Additive; no existing mode/step/output-contract semantics altered beyond the additive graph; `version:` bumped v1.18 → **v2.00**. Built via pmo-skill-editor Mode A (impact + coherence + regression PASS); all added SKILL.md + reference prose reference-durable (no bare issue refs / markdown links / raw URLs). Pre-existing SKILL.md >500-line count (541 before this card) grows to 596 — flagged, out of #246 scope to refactor (consistent with #252). Files: `release-planner/SKILL.md`, `references/dependency-analysis.md`, `references/release-plan-template.md`, `references/output-templates.md`, `release/tools/bundle-issues-parser.py`. | DONE |

Key decisions: D-246-Blocker resolves to **INCLUDE** the typed-artifact-linking card this release (the "Blocked by #409" edge is stale — the card needs only the already-frozen 7-type relationship vocabulary; rationale and evidence in the D-gate above). D-ReleaseClass resolves to **novel** (≥1 new reference doc + ≥1 D-class decision; cross-cutting count-gates not met). Scope-lock note for #1127: the Stage-5 locked spec recommended surfacing the capability *within Mode A + Mode B* (Option B); the operator's scope-lock decision (carried in the Stage-6 sub-task) directs a dedicated **Mode G (Adoption Tracking)** instead — implemented here as a 7th mode, with the "6 modes → 7" drift fixed.

Reversibility: MODERATE / Confidence HIGH — the release as a whole is skill-file + reference-doc edits on one release branch (no schema migration, no data, no external system); whole-release rollback = `git revert` on the merge SHA, per-card rollback = revert the card's commit. The #1127 increment specifically is additive (a new Mode G block + a new reference doc + inline path-reference corrections + a version bump) and reverts cleanly.

Downstream impact: the change-management runtime skill gains one mode (Mode G) and one reference doc (`adkar-assessment.md`); the deployed mirror + `.skill` package refresh on the post-merge deploy. The serialized change-management run continues with #1128/#1129 (same SKILL.md, adoption-science framing must stay consistent) then #1130→#1131 (Mode-D hypercare; #1130 first is a hard edge). The four stale `pmo-platform/reference/specs` → `core/specs` path references in change-management/SKILL.md are corrected, removing a dead-path reference surface. Carry-forward: the remaining 10 cards of this milestone.

Cross-references: this release plan (top of file); the Stage-4 planning sub-task and Stage-5 locked-spec sub-task; the user-facing release note authored at Stage 13 will live at `release/releases/notes/v2.00_RELEASE_NOTES.md`. See the Source references block below for the specific issue and sub-task numbers.

---

### Source references

- Milestone: `01-FNH-qa-change-release-hardening` (parent epic: skill-function-hardening).
- This PR's build card: #1127 — change-management ADKAR barrier-assessment + training-timing validation.
- Stage-4 release plan: #1139 (reproduced above as Engineering Commit 0).
- Stage-5 locked spec (Collective-Review-locked): #1140.
- Stage-6 sub-task: #1170.
- Remaining milestone cards (own Stage-6 commits): #1128, #1129, #1130, #1131, #212, #244, #245, #246, #248, #252.
