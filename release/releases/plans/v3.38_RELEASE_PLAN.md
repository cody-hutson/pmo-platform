# v3.38 Release Plan — 66-release-identity-and-spec-hardening

> Durable release-plan record for milestone `66-release-identity-and-spec-hardening`. Committed at Engineering (Stage 6 Commit 0) on the release branch under single-branch topology. Transcribes the approved Stage-4 plan. Provisional v3.37 re-verified free at Commit 0 (major-max anchor; latest published = v3.36); **re-versioned forward to v3.38 at Stage 12 pre-merge** when a sibling release (milestone `21-shared-entity-storage-layout`) claimed the v3.37 slot first — the R1 contingency firing as planned (forward-only, per Phase A.5.6c).

## Summary (30 seconds)

A coherent **3-card spec-hardening slice** that shores up release-identity and frozen-spec precision across Stages 4 / 5 / 6 without late Stage-12 reconciliation. All three are doc/protocol edits to the pipeline reference corpus; **no application code, no new file class, no schema**. Cards are **independent (shared-parent epic only — zero real inter-card dep edges)** and touch **disjoint primary files**, so they parallelize cleanly within the release.

- **Sequence (layer-ordered, all `layer:foundation`):** placement-forward-check card → commit-group-decision card → frozen-spec-precision card (tie-break priority-desc then issue#-asc; the doc-only-S prose rule lands last as the lowest-weight change).
- **Sizing:** raw **10 pts** (M+M+S). Class **`novel`** (trigger (b): the commit-group card carries a genuine D-class enforce/audit/advisory decision). Under `novel` weight 1.15, **effective_pts = round_half_up(10 × 1.15) = 12** → **Acceptable band (10–15)**, lower edge.
- **Genuine Stage-5 design decision** lives in the commit-group card (enforce / audit / advisory on the now-Required `Commit group` field). Rendered **ADVISORY** at Stage 5.
- **One stale-input correction surfaced at Stage 4:** the milestone description names the frozen-spec card's target home as `release/references/templates/solutioning-output-template.md` — that path does not exist; the file lives at `release/references/standards/solutioning-output-template.md`. The corrected path is carried into this plan's change matrix.
- **Parallel-release reality:** multiple releases shipped this session. **D-Version** is a RECORDED DETERMINATION (next-free per `anchor()`). Re-verified at Commit 0 = **v3.37** (latest published v3.36; v3.37 free then). At Stage 12 pre-merge the slot had been claimed by a sibling → **re-versioned forward to v3.38** (the next free slot; forward-only atomic claim at tag).

---

## Release Class + Sizing

- **Class:** `novel` (ratified at Stage 4). Trigger (b) fires — the commit-group card carries a genuine D-class enforce/audit/advisory decision.
- **Differentiation posture (`novel`):** Engagement density Standard · Stage 9 review depth **Deep** · Stage 5 activation bias ALL · Stage 13 outcome-window 30-day.
- **Sizing:** raw 10 pts (M=4 + M=4 + S=2); class_weight(`novel`) = 1.15; **effective_pts = round_half_up(10 × 1.15) = 12** → **Acceptable band (10–15)**, lower edge. Clears the 10-pt floor as effective_pts.
- **Disposition:** SHIP AS-IS — a lean-but-coherent slice (release-identity + spec-precision across Stages 4–6 is a clear capability boundary).
- **Domain classification:** `domain: governance` — entire File Change Matrix is `release/` governance + pipeline reference corpus. `domain_practice: { source: N/A — pipeline-internal release, date: 2026-06-30, domain: governance }`. Stage 4 Phase A1.5 external-sourcing is EXEMPT (pipeline-internal deliverable).

---

## Dependency Graph

All three cards cite the same PIPE-Release-Identity epic as shared parent. Parent-membership is **not** a dependency edge. Each pair was tested for a real directional edge (hard verb: blocks / depends-on / requires / after) — none found.

```
epic (shared parent, NOT a dep edge)
   ├── placement-forward-check card   (Stage-4/6)   [independent]
   ├── commit-group-decision card     (Stage-6)     [independent]
   └── frozen-spec-precision card      (Stage-5)     [independent]

Inter-card edges: NONE (hard).  Shared-parent only.
```

**Soft (informational) edge, cross-milestone, not in-bundle:** the commit-group card's *if-enforce/audit* verifier would compose with the verification-execution-surface milestone. Not a blocker — recorded in the Risk Register as a coordination note, not a sequence edge. (The decision rendered ADVISORY at Stage 5, so no verifier is built and this edge does not fire.)

---

## Implementation Sequence

Per bundle-composition-doctrine §9 (foundation → infra → skill-core → eval) + within-layer tie-break priority-desc → issue#-asc. All three are `layer:foundation`, `protocol`, no eval-class member, so the layer rule is satisfied trivially and the tie-break drives order.

| Order | Card | Size | Rationale for slot |
|---|---|---|---|
| First | **placement-forward-check** | M | The most structural change (touches both `release-process.md` governance + `stage-04-planning.md`). Land the structural Stage-4 edit first so the plan-file placement convention is in force. |
| Second | **commit-group-decision** | M | Carries the genuine D-class decision (rendered ADVISORY). Edits `stage-06-engineering.md` + `sub-task-methodology.md`. |
| Last | **frozen-spec-precision** | S | Doc-only, single prose/checklist rule, lowest weight. Lands last as the cheapest additive edit. |

**Note:** the cards are independent and file-disjoint — this is a *recommended* serial order for review legibility under single-branch topology, **not** a hard dependency chain.

---

## File Change Matrix

All edits **additive** (new sub-phase / new advisory note / new required-when-triggered rule). No file moves — place per current-main topology. Reversibility CHEAP for every row.

| Card | Path | Intent | Change |
|---|---|---|---|
| placement-forward-check | `release/references/pipeline/stage-04-planning.md` | MODIFY (add) | Insert **Phase A0.7 — Placement forward-check (G-PL3)** paragraph after the A0.6/G-PL2 paragraph, before the Parallelization-Map currency check. Trigger uses the unified `git log <base>..origin/main --name-status --find-renames` predicate (dir-crossing R-rows PLUS D-rows of any in-scope path). |
| placement-forward-check | `release/governance/release-process.md` | MODIFY (add) | Insert **Placement Forward-Check (Phase A0.7 / G-PL3)** governance summary + cross-ref after the Parallelization-Map Currency Check paragraph, before D-Concurrency Posture, inside `### Stage 4: Planning`. |
| commit-group-decision | `release/references/pipeline/stage-06-engineering.md` | MODIFY (add) | Insert the "Commit-group traceability (documented simplification — advisory, not enforced)" paragraph after Phase B1, before B2. Includes the monitored-residual note (rollup-vs-commit-history divergence per §7 closure-comment-SHA discipline). |
| commit-group-decision | `release/references/standards/sub-task-methodology.md` | MODIFY (add) | Append the "Commit-group enforcement posture" sentence after the §5 required-four sentence. Posture cross-ref after §5. |
| frozen-spec-precision | `release/references/standards/solutioning-output-template.md` | MODIFY (add) | Add the frozen-spec prose-artifact precision rule as a **required-when-triggered** block (matching the BLOCK-DESTRUCTIVE-022 conditional-row pattern) in `## Blast Radius`, plus one anti-pattern bullet. |
| frozen-spec-precision | `release/references/pipeline/stage-05-solutioning.md` | MODIFY (add) | Append one pointer sentence to the § 6 Outputs paragraph routing a spoke to the rule's canonical home. |

**Mirror sync (Stage 12, not a matrix row):** `release/governance/release-process.md` is byte-mirrored to `.claude/rules/release-process.md` via deploy.sh. Engineering edits the SOURCE only; `./deploy.sh --deploy` re-syncs the mirror at Stage 12.

---

## Delivery Strategy

- **Branch topology:** SINGLE-branch (default). Plan file committed as Engineering Commit 0. No per-issue PRs. One PR, one merge — issues are delivery slices on one branch (milestone-one-PR discipline).
- **Concurrency posture:** P0 fully-serial (default when undeclared). Cards land in the recommended sequence order; the next commit waits until the prior lands on the release branch.
- **Commit plan:** Commit 0 = plan file. Commit 1 = placement-forward-check (both files). Commit 2 = commit-group-decision (both files). Commit 3 = frozen-spec-precision (both files). Commit messages reference issue numbers per B1.
- **Container:** doc-only, additive, ≤5 sub-tasks per card → **PR-body checklist container** (the three always-generated special sub-tasks — sync / plan-update / verification — render as checklist rows).

---

## Risk Register

| ID | Risk | Class | Likelihood | Reversibility | Owner | Mitigation |
|---|---|---|---|---|---|---|
| **R1** | **Version-claim contention at Stage 12.** Multiple releases shipped this session; sibling pipeline milestones mid-flight. The next-free version may be claimed by a sibling between Commit 0 and merge. | contention | HIGH | CHEAP (re-version-up is mechanical) | hub / Stage-12 spoke | **FIRED (resolved).** D-Version is a RECORDED DETERMINATION. Commit-0 re-verify (passed = v3.37) + Stage-12 pre-merge freeness check flagged the sibling claim + atomic claim at tag. A sibling (milestone `21-shared-entity-storage-layout`) claimed v3.37 first → re-versioned forward to **v3.38** (the next free slot). Mitigation worked as designed; forward-only, no data loss. |
| **R2** | **Commit-group decision (rendered ADVISORY at Stage 5).** | scope / dependency | resolved | CHEAP (advisory → enforce later is additive) | operator | Rendered ADVISORY at Stage 5 — doc-only note, no verifier built. Ratified. |
| **R3** | **Stale target-path in milestone description (frozen-spec card).** Milestone names `templates/`; the file is at `standards/`. | scope / staleness | resolved at Stage 4 | CHEAP | Stage-6 spoke | Corrected path `release/references/standards/solutioning-output-template.md` carried into this change matrix. |
| **R4** | **Cross-milestone soft file-contention on Stage-5 spec.** A sibling gate-criteria milestone edits `stage-05-solutioning.md` regions; a same-window merge could trigger a Stage-9 divergence re-check. | contention | LOW–MEDIUM | MODERATE (rebase + re-validate) | hub | Baseline-pinned at Stage 4; Stage 9 G-PR8/G-PR9 catch a sibling merge pre-GO. The frozen-spec card edits a *different* file (`solutioning-output-template.md`) — actual collision risk is low. |
| **R5** | **Structural-blast-radius (Tier-S) — self-case.** This release adds *new* sub-phase content to Stage-4/12 specs; no reorg is in-flight that would relocate these files. | dependency | LOW | CHEAP | Stage-4 A4 | A4 structural sub-audit: this release's mover-set is **empty** (all edits are in-place additions). SURFACE(R) = ∅ on the mover axis; only the version-slot token is live (= R1). |
| **R6** | **Rollback complexity.** Three independent doc edits on one branch (single PR). | rollback | LOW | CHEAP (additive doc edits; revert the merge or drop a commit) | operator | All edits additive. Revert is clean — no data migration, no deployed-mechanism change. Per-card revert possible since edits are file-disjoint. |

---

## Verification Plan

Stage 7 Dev Testing verifies each card's ACs via grep (run S7 + S8 as real stages per rigor-invariance):

- **placement-forward-check:** `grep -nE 'Phase A0\.7|Placement forward-check|G-PL3'` non-empty in both target files; trigger command present with the unified `--name-status --find-renames` predicate covering dir-crossing R-rows and D-rows; compose-with notes to the A4 sub-audit + Stage 12 Phase A.5 present.
- **commit-group-decision:** `grep -n 'documented simplification — advisory, not enforced' release/references/pipeline/stage-06-engineering.md` = 1 hit; `grep -n 'Commit-group enforcement posture' release/references/standards/sub-task-methodology.md` = 1 hit; monitored-residual note (rollup-vs-commit-history divergence) present; `sub-task-methodology.md` `Commit group … Required` line intact.
- **frozen-spec-precision:** `grep -nE 'Frozen-spec prose.artifact precision' release/references/standards/solutioning-output-template.md` ≥1 hit in `## Blast Radius`; the `[DEFERRED — <rationale>]` escape present; the re-adjudication cost named; shard pointer present in `stage-05-solutioning.md` § 6.

---

## Rollback Strategy

All edits are additive doc/protocol changes on one branch shipping as one PR. Rollback = `git revert` the merge commit (or drop an individual commit pre-merge). No data migration, no deployed-mechanism change, no schema change. Per-card revert is possible because the cards edit disjoint files. Reversibility: **CHEAP** for the whole release and per-card.

---

## Operator Decisions (D-Gate Block)

### D-ReleaseClass: `novel` (ratified at Stage 4)
Ratified `novel` — the commit-group card carries a genuine D-class decision. Routing: Stage 9 Deep + Stage 5 bias ALL + effective_pts 12.

### D-CommitGroup-Enforcement: ADVISORY (rendered at Stage 5)
Rendered **ADVISORY** — under single-agent-on-a-branch execution the commit-to-sub-task lineage is already reconstructable from B1 issue-referencing commit messages plus the §7 SHA-naming closure comment. No verifier built. Doc-only note lands in `stage-06-engineering.md` B1 + `sub-task-methodology.md` §5.

### D-C Branch Topology: SINGLE (default, RECORDED)
SINGLE-branch topology. Plan file commits as Engineering Commit 0. No per-issue PRs.

### D-Concurrency Posture: P0 fully-serial (default, RECORDED)
P0 fully-serial. A4 contention map = no within-release contention; the cards *could* run parallel (file-disjoint) but P0 serial is the safe default given the modest size.

### D-Version: v3.38 (RECORDED DETERMINATION — re-versioned forward at Stage 12)
Provisional **v3.37** = next-free per `anchor()` at Commit 0 (major-max anchor; latest published = v3.36; v3.37 free then). Re-verification rungs ran as designed: Commit-0 re-verify (passed) → Stage-12 pre-merge freeness check → atomic claim at tag. At Stage 12 pre-merge the freeness check found the v3.37 slot **claimed by a sibling release** (milestone `21-shared-entity-storage-layout`, its signed tag on origin), so the release **re-versioned forward to v3.38** (the next free slot; forward-only per Phase A.5.6c). The plan file, tag, and RELEASE_LOG row carry the realized **v3.38** label; the four on-branch commit subjects retain their `docs(v3.37):` provisional prefix (historical, immutable-in-practice — the authoritative label is the tag + corpus). Re-version ledger row appended at Stage 13 (`v3.37 → v3.38`; collided with `21-shared-entity-storage-layout@v3.37`).

---

## Stage-13 Close Notes

- **frozen-spec card secondary AC:** mark the decision-discipline observation-log entry (the placement-collision observation this release codifies) as **absorbed**. This is a **Stage-13 operator action on the Layer-2 auto-memory observation log** (operator-write-only per decision-discipline § 4.1 D-5) — NOT an Engineering corpus write.
- **Milestone-description hygiene (out of release scope):** the milestone body still names the frozen-spec card's home as `templates/…`; the file is at `standards/…`. A one-line milestone-description fix would prevent a future spoke trusting it. Out of this release's corpus scope; surface to the operator as a hygiene fix.

---

*Per-card closure note for PR-body transcription (parser-safe phrasing): mark the placement-forward-check card as done at Stage 13; mark the commit-group-decision card as done at Stage 13; mark the frozen-spec-precision card as done at Stage 13. Closure happens at Stage 13 close, not before.*
