# v3.77 Release Plan — skill-hardening

> Version **v3.77** (re-anchored from provisional v3.75 — v3.75 and v3.76 were claimed by concurrent releases while #267 ran Stages 6–9; the version binds atomically at the Stage-12 tag-claim, so this label re-anchored forward per the version-freeness protocol). Plan **APPROVED by operator 2026-07-12 (Sunday)** at the Stage-4 plan-approval gate (parent #267, Stage-4 sub-task #3447); re-anchor v3.75→v3.77 operator-approved 2026-07-16 (Thursday).

## Header

| Field | Value |
|-------|-------|
| **Version** | v3.77 (re-anchored from v3.75; next-free minor after v3.76 — v3.75/v3.76 claimed by concurrent releases) |
| **Date Created** | 2026-07-12 (Sunday) · re-anchored 2026-07-16 (Thursday) |
| **Release Manager** | Agent-assisted (release-hub; operator gates at Stage 9 / Stage 12) |
| **Status** | Executing (Stage 12 — Execute; re-anchored onto current main) |
| **Branch** | release/v3.75-skill-hardening (v3.75-named branch, re-anchored to v3.77; cut from origin/main) |
| **PR** | one PR / one merge — opened by the first Engineering spoke (#2699) after Commit-0; three slices land on the single branch |
| **Milestone** | skill-hardening (#267) |
| **Release Class** | MIXED (1 capability story + 1 code-build task + 1 hygiene task) |
| **Topology** | D-C SINGLE (one branch, one PR) · P0 fully-serial |
| **Build order** | #2699 → #3114 → #155 |
| **Size** | ~15–24 pts (in band; three size:M slices) |

## Summary

Milestone **skill-hardening** (#267) bundles **three mutually-independent skill-suite hardening slices** — no shared files, no cross-edges. The three issues are a **MIXED** class bundle: one capability **story** (#155 — new health-check rollup modes), one code-build **task** (#2699 — semantic routing-conflict detection harness), and one hygiene **task** (#3114 — SKILL.md size/author-cap debt). Delivery = **one release branch, one PR, three slices** (milestone-one-PR discipline). All 13 stages apply; Stages 10 (Dry Run) + 11 (Snapshot) run **compressed** under the Claude Code path (the PR diff IS the dry-run; git history IS the snapshot) — no separate sub-tasks.

**Build order #2699 → #3114 → #155.** #2699 goes first because it owns the release's only internal gate — the Stage-5 #17-reuse design-readiness check (AC-4). The three issues are otherwise independent (disjoint file sets), so the order is a sequencing convenience, not a hard dependency chain.

## Scope

| # | Issue | Title | Type | Size | Build order | Scope-lock decision |
|---|-------|-------|------|------|-------------|---------------------|
| 1 | **#2699** | Implement semantic routing-conflict detection (deferred code half of #704) | task | M | 1 | **BUILD-OWN** semantic layer + optional lazy embedding backend + stub-for-CI; **one ADR** (first optional-heavy-dep in the zero-dep eval corpus) |
| 2 | **#3114** | SKILL.md size / author-cap debt reduction (consolidated) | task | M | 2 | **RE-BASELINE the guideline** — record in `core/standards/canonical-skill-structure.md §5` that multi-mode skills carrying a non-empty `references/` may exceed the ~500-line authoring guideline; no file splits |
| 3 | **#155** | Extend health-check skill with rollup invocation modes (up-to-portfolio, down-through-project) | story | M | 3 | **COMPOSE** — extend `health-check/SKILL.md` with rollup invocation modes; regression scope = **7 modes** (full/timeline/attribution [v1] + comms/plan/raid/sources [v2]) |

## Dependency Graph

Single-branch release, so these are **within-branch build-order edges**, not cross-PR merge dependencies. The three issues have **disjoint file sets** — every edge below is a soft sequencing convenience, not a hard logical blocker.

```
#2699 (harness+ADR) ──seq──▶ #3114 (guideline re-baseline) ──seq──▶ #155 (health-check modes)
   │                             │                                      │
   └─ scripts/ (new files)       └─ core/standards/ (1 edit)            └─ operations/skills/health-check/ (COMPOSE)
```

| Edge | Type | Evidence |
|---|---|---|
| **#2699 → #3114** | SEQ (convenience) | Disjoint files (`release/skills/pmo-skill-refiner/scripts/*` + `core/ADRs/` vs. `core/standards/canonical-skill-structure.md`). #2699 first because it owns the sole internal design-readiness gate (AC-4). |
| **#3114 → #155** | SEQ (convenience) | Disjoint files (`core/standards/*` vs. `operations/skills/health-check/SKILL.md`). No shared surface. |

## Cross-Milestone Coordination

**#155 ↔ #158 share `health-check/SKILL.md`.** #158 is **OPEN in a different milestone** (not in v3.77). This is a **coordination flag, not a blocker**: whichever of #155 / #158 ships first sets the shared 5-section contract on `health-check/SKILL.md`; the later one composes onto it. #155 proceeds in v3.77 as planned; no hold.

## Implementation Sequence

**Stage-6 write-order (single release branch, dep-ordered):**

1. **#2699 (harness + ADR)** — FIRST. Owns the release's only internal gate (Stage-5 #17-reuse design-readiness check). Builds the new `run_routing_audit.py` semantic routing-conflict harness (three-tier: Jaccard pre-filter → embed+cosine on candidates → optional agent-runtime judge), the pluggable/lazy optional embedding backend with a stub fallback (zero-dep offline CI contract preserved), a new ADR recording the optional-heavy-dep precedent, and offline stub-backend tests. Opens the release PR (draft) after Commit-0.
2. **#3114 (guideline re-baseline)** — SECOND. Records in `core/standards/canonical-skill-structure.md §5` that multi-mode skills carrying a non-empty `references/` may exceed the ~500-line authoring guideline (all cited files already pass the hard Check-6 gate). Re-baseline only — no file splits.
3. **#155 (health-check modes)** — LAST. Extends `operations/skills/health-check/SKILL.md` with rollup invocation modes (up-to-portfolio, down-through-project). Regression scope = 7 existing modes preserved.

## Risk Register

| ID | Class | Risk | Owner | Mitigation | Reversibility |
|---|---|---|---|---|---|
| **R-1** | dependency | #2699 — #17's judge/embedding machinery not reusable in-place (cross-release; #17 shipped v3.69) | Solutioning (S5) → Engineering (S6) | Stage-5 spike CONFIRMED no importable embedding substrate exists in-repo → **BUILD-OWN** the semantic layer (AC-4 permits). | MODERATE · HIGH |
| **R-2** | scope (self-decided) | #3114 — direction ambiguous (split files vs. re-baseline the guideline) | Operator (S4) | RESOLVED at plan-approval: operator chose **RE-BASELINE** — record rationale in `core/standards/canonical-skill-structure.md §5`; no splits. | CHEAP · HIGH |
| **R-3** | governance (bridge-file) | #155 — a rollup mode could write to `PORTFOLIO.md` (a Layer-3 bridge file Cowork owns) | Engineering (S6) | AC-3 staging gate: rollup output stages to the project structure, never mutates the bridge file; health-check stays read-only on PORTFOLIO.md. | CHEAP · HIGH |
| **R-4** | contention (net-new-file guard) | #2699 — the harness touches `run_eval_audit.py` (must stay byte-identical for its zero-dep CI contract) | Engineering (S6) | Import-don't-edit: `run_routing_audit.py` imports `run_eval_audit`'s lexical primitives read-only; AC-1 `git diff` shows zero change; `test_eval_scripts.py` is the CI tripwire. | CHEAP · HIGH |

## Release Class

**MIXED** (per `release-class-taxonomy.md`). One capability story (#155), one code-build task (#2699), one hygiene task (#3114). #2699 carries one D-class decision (the optional-heavy-dependency posture → ADR at Stage 6); #3114 carries one governance re-baseline (guideline rationale). Stage-9 review depth: **Standard** (one ADR + one guideline re-baseline to verify; no charter-tier amendment).

### Domain Practice Provenance

Pipeline-internal release — the entire File Change Matrix is pmo-platform artifacts (a skill extension, an eval-tooling harness + ADR, and a governance-standard edit), so the release is **exempt from external Domain-Best-Practice sourcing** per Stage-4 §5.7 (the `governance` domain-best-practice guide is the encoding of the platform's own internal-deliverable practice). Label authored at Stage-6/7 per the introducing-release-shape convention (Stage-4 Planning predated this per-release emission for v3.77):

domain_practice: { source: N/A — pipeline-internal release, date: 2026-07-12, domain: governance }

## Closure

All per-issue closure is deferred to Stage 13: **mark #2699 / #3114 / #155 closed at Stage 13** on merge (Refs, not close-keywords, in the PR body). No issue is closed at planning or engineering. The `pmo-skill-refiner.skill` package rebuilds at release-cut (Stage 12; Check-7 package-drift) — NOT in this Stage-6 PR.
