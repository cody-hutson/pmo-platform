<!-- reference-durability: allow-link -->
# v3.34 Release Plan — 21-shared-entity-storage-layout

## Stage 4 Release Planning — 21-shared-entity-storage-layout (v3.34)

**Release Class:** novel (Deep Stage 9 · Stage 5 ALL · 30-day outcome window) · **Topology:** SINGLE-branch / P0 · **D-Version:** v3.34 (provisional, binds at Stage 12) · **Readiness:** GO (Mode R) · **Size:** 20 pts.

### Summary
Stands up the `_pmo/` shared-entity layout (#362), redesigns PROJECT.md as a thin composed wiki-link index with a gated live-project migration (#363), and resolves the deferred `plan_type` G5 enum membership while dropping `plan_subtype` (#159). Build order: **#159 ∥ #362 → #363** on one branch / one PR / one merge. Schema + structure + skill + one live-data migration (functional, not docs-only).

### Dependency Graph
- G1 (`project-entity-model.md`) + G2 (`entity-field-schemas.md`) — shipped prereqs → unblock all three.
- **#362 → #363** (hard): the composed index wiki-links INTO #362's entity pages.
- **#159 ↔ #363** (soft pairs-with): #363's `## Plans` section points at #159's typed plan entities; resolved within the same PR.
- **#159 independent of #362** for build (edits §3.4 + plan-templates/ + 2 SKILL.md; no `_pmo/` dependency).

### Implementation Sequence (one branch)
1. Commit 0 — plan file `release/releases/plans/v3.34_RELEASE_PLAN.md`.
2. **#159** (lowest-risk, reversible schema edit; do early): fill `plan_type` membership in §3.4 {comms,training,hypercare,cutover,change-management,raid} + extension slot; close `[ASSUMPTION–CONFIRM @ G5]`; drop `plan_subtype`; per-subtype lifecycle as V-PLN-05 extensions reconciled with `lifecycle-states-canonical.md`; relationship matrix using the 7 MVP types; add `operations/templates/plan-templates/` (6); edit `artifact-generator` + `change-management` SKILL.md.
3. **#362** (parallel-eligible; before #363): create `projects/_pmo/{people,systems,vendors,workstreams,decisions,dependencies}/`; author 6 entity-page templates matching frozen §3.10-3.16/§3.3/§3.5; alias-tracking convention aligned with ADR-040 person_id + people-roster (compose, don't fork); edit `project-initiator` Mode A `_pmo/` bootstrap; populate ≥3 entity types.
4. **#363** (last; carries the EXPENSIVE step): CHEAP first — ≤50-line composed-index template; `project-initiator` Mode A Step-3 template-ref swap; 4-step migration protocol; backwards-compat vs live `project-schema.md` consumer table. EXPENSIVE/gated/last — migrate ≥1 live project (snapshot-before / verify-links-after).

### File-Change Matrix (machine-readable path block — tracked diff)
```
release/releases/plans/v3.34_RELEASE_PLAN.md
core/schemas/entity-field-schemas.md
core/standards/lifecycle-states-canonical.md
operations/templates/plan-templates/comms.md
operations/templates/plan-templates/training.md
operations/templates/plan-templates/hypercare.md
operations/templates/plan-templates/cutover.md
operations/templates/plan-templates/change-management.md
operations/templates/plan-templates/raid.md
operations/templates/person-entity-template.md
operations/templates/system-entity-template.md
operations/templates/vendor-entity-template.md
operations/templates/workstream-entity-template.md
operations/templates/decision-entity-template.md
operations/templates/dependency-entity-template.md
operations/templates/project-md-composed-index-template.md
operations/skills/project-initiator/SKILL.md
operations/skills/artifact-generator/SKILL.md
operations/skills/change-management/SKILL.md
```
Git-ignored / out-of-diff (executor-tracked, not in PR): `projects/_pmo/*` (dirs + ≥3 pages); `<one active project>/PROJECT.md` (EXPENSIVE migration — snapshot before).

### Contention Map
- ⚠️ `project-initiator/SKILL.md` Mode A — **#362 (bootstrap) + #363 (Step-3 ref)** both edit; single branch sequences (362 then 363); per-issue branches would conflict → argues for SINGLE.
- `operations/templates/` — all 3 add files, distinct names, no collision.
- `entity-field-schemas.md` — no edit collision (#159 edits §3.4; #362 reads §3.10-3.16).

### Stage Applicability — all stages apply to all 3 (novel); no skips.

### Risk Register (top)
- **R1** live PROJECT.md migration (#363 AC-5) — EXPENSIVE / no git rollback on the git-ignored ops tree. Mitigation: CHEAP-first, gate per-project, snapshot-before/verify-after, POC=1 project, last.
- **R2** Mode A double-edit order (#362 before #363) — MODERATE; single branch + Stage-8 QA.
- **R3** `plan_type` fill vs ADR-018 / orphan lifecycle — MODERATE; Stage-5 confirms state set; Stage-8 greps orphans.
- **R4** entity-template frontmatter drift vs §3.10-3.16 — CHEAP; §6.2 worked `_pmo/` Person example is the conformance reference; Stage-7 parses every template.
- **R5** PROJECT.md consumer back-compat — MODERATE; keep Methodology/Status inline; verify vs live consumer table; Stage-9 blast-radius.
- **R7** `.skill` rebuild after 3 SKILL.md edits — Stage 11.
- **R8** cross-PR contention on shared files — re-check at Stage 9 entry; pin baseline SHA.
Rollback: the single PR is the revert unit for all tracked changes; only R1 (live migration) is non-git-reversible (snapshot + re-inline).

### Quota Budget: PASS — worst wave = 3 spokes (St5/7/8 all-apply); Checkpoint B re-validates each wave.

### Branch Topology: SINGLE / P0 (recommended + approved) — Engineering Commit 0 carries the plan file.

### Release Outcome Statement
(approved; PATCHed into milestone #192 description) — `_pmo/` shared-entity layout + composed-index PROJECT.md + resolved `plan_type` membership; landed when `_pmo/` has 6 subfolders w/ ≥3 conformant pages, ≥1 live project migrated with resolving links + parsing consumers, and §3.4 V-PLN-02 membership no longer deferred.

### Out of scope (do NOT bundle)
- `projects/` → `operations/` operator-tree rename (standalone ticket).
- Bulk migration beyond the AC-5 POC (gated follow-up).
- `_`-prefix #232 allowlist note for `_pmo/` (one-liner, non-blocking).
