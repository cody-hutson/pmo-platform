---
version: v1.09
date: 2026-06-11
type: plan
issues: ["#187"]
pr: null
links:
  note: null
  log_archive: "logs/v1.09.md"
reversibility-tier: CHEAP
themes: ["epic:pipeline-engine-automation", "cluster:automation", "cluster:process-protocol"]
---

# v1.09 Release Plan — agent-to-script-promotion

**Milestone:** `agent-to-script-promotion`
**Release identifier:** v1.09 (D-Version A, operator-rendered 2026-06-09; Milestone title kept per v1.07/v1.08 practice). Plan file `v1.09_RELEASE_PLAN.md` · branch `release/v1.09-agent-to-script-promotion` · tag v1.09 at Stage 12.
**Release Class:** `novel` (D-ReleaseClass, operator-rendered 2026-06-09). Basis: a new tracked reference standard (the agent-to-script promotion framework) + 7 D-class decisions — novel triggers (a) and (b); trigger (c) confirmed at Stage 5 (ADR-020).
**Branch Topology:** `D-C SINGLE` — one release branch `release/v1.09-agent-to-script-promotion`; this plan committed as Engineering Commit 0; one release PR.
**Differentiation posture (per `novel`):** Engagement density Standard; Stage 9 review depth Deep; Stage 5 activation bias ALL; Stage 13 outcome-window 30-day.

**Stage 4 plan content of record:** the Stage 4 Release Planning comment on sub-task #579 (operator-reviewed at the Stage 4 Decision Briefing; plan approved 2026-06-09). This file transcribes that comment verbatim as Engineering Commit 0, followed by the Decision Log (Stage 4 gate verdicts + the Stage 5 scope-lock) and the Engineering Commit 0 Amendments (Tier-A declaration, refined File Change Matrix, Deviation Log).

**Stage 5 spec of record:** the two-part Stage 5 Solutioning comment on sub-task #582, ratified RATIFY + FOLD ALL 8 at the operator's 2026-06-10 scope-lock (Decision Log below). The ratified spec — as amended by the 8 folded adversarial dispositions — is the implementation spec Engineering executes.

---

## Stage 4 Release Planning — agent-to-script-promotion

**Plan baseline (pinned):** `main` @ `3f91d0520d4fa0449c95028cfb2b22e638dccf45` (merge of PR #576, v1.08 close-out), 2026-06-09. Open-PR population at baseline: **0** (empty). Recent-merge window (last 5): #576, #570, #543, #539, #528. Baseline policy per the release-ops pattern `audit-baseline-when-target-population-is-empty` (promoted 2026-06-04): SHA pin + last-5-merged window, re-check at Engineering Commit 0.

---

### Phase A0 — Triage→Design Re-Review (per triage-design-rereview.md § 1)

**Header**

| Field | Value |
|---|---|
| issue_number | #187 |
| release_milestone | agent-to-script-promotion (version-less at planning; see D-Version) |
| stage | 4 |
| spoke_author | Stage 4 Release Planning spoke (Release Manager persona, Skills-Map §13 Mode 1) |
| re_review_date | 2026-06-09 |
| issue_body_revision | 2026-06-08T23:47:27Z |
| triage_decision_date | 2026-05-13 (Stage 2 batch B2) |
| effort_tier | complex (size:XL; Solutioning activates; blast radius spans script estate + skill roster) |

**Per-requirement table** (D1 best-practices / D2 platform-localized / D3 learnings; C1 survives / C2 refine / C3 challenge)

| Requirement | D1 finding | D2 finding | D3 finding | Cls | Delta / PT |
|---|---|---|---|---|---|
| Premise: no governed agent↔script boundary exists; framework is the missing layer | discovery-discipline § 3.1 supports premise interrogation at stage entry — performed here, premise re-confirmed | [VERIFIED 2026-06-09: grep -ril "script governance\|promotion ladder\|agent-to-script" core/ release/ docs/ → 0 files; ls core/standards/ → no script/promotion/automation standard] gate-criteria-spec.md (live at `core/schemas/`) still splits structural-auto vs judgment; release-process.md § Stage Compression still encodes "git mechanisms replace agent process" | v1.08 #2 (skill↔pipeline alignment standard) is the nearest sibling: audit + tracked-standard + operator-local registers shipped cleanly — same deliverable shape works | **C1** | (verbatim — premise survives) |
| AC1: audit artifact at `pmo-platform/analysis/<name>-YYYY-MM-DD/` | intermediate-artifact discipline + prefer-durable-structures: read-once analysis is not durable corpus | `pmo-platform/analysis/` does not exist post-restructure; `public-repo-vs-operator-instance-taxonomy.md` § 2 apply-test → read-once analysis has no cross-operator readership → OPERATOR-INSTANCE; pipeline already uses `<OPERATOR_INSTANCE_ANALYSIS_PATH>/<audit>-<date>/` (stage-04 § A4) | v1.08 #2 split "tracked standard vs operator-local audit/recommendations registers" — operator-approved as correct | **C2** | AC1 → audit lands at `<OPERATOR_INSTANCE_ANALYSIS_PATH>/script-promotion-audit-YYYY-MM-DD/` with SUMMARY.md; public-repo-trackable portion = the framework standard (worked examples inline the audit substance). See D-AuditPlacement. |
| AC2: audit catalogs every `*.sh`/`*.py` under `pmo-platform/`, `.claude/hooks/`, `projects/` | review-discipline Rule 1: scope must be concretely enumerable | [VERIFIED 2026-06-09: git ls-files \| grep -E "\.(sh\|py)$" \| wc -l → 71] live scopes: `core/hooks/` (14 + 9 tests), `release/tools/` (11 + 1), `release/skills/pmo-skill-refiner/scripts/` (11), `core/deploy/` (5 + 7 tools + 8 tests), `docs/scripts/` (2), `install.sh`, `update.sh`. Runtime `.claude/hooks/` is the deploy mirror of `core/hooks/`. `projects/` is Layer 2 operator-instance (operations-bridge read-only boundary) | A prior reorg taught silent-truncation lessons (git-workflow § Batch CLI Query Limits): pin glob counts at audit time | **C2** | AC2 → enumerate tracked repo scripts (71 at baseline) + note runtime-mirror identity + Layer 2 scope per D-Layer2AuditScope. Row-count method: audit rows ≥ live `git ls-files` glob count at audit SHA. |
| AC3: mechanical-vs-judgment row per "the 19 custom skills" | parameterize-over-hardcode (CLAUDE.md): derive counts from source of truth | [VERIFIED 2026-06-09: core/deploy/deploy.sh arrays → OPERATIONS_SKILLS=13, RELEASE_SKILLS=6, CORE_SKILLS=3 → 22 deployed; +1 source-only canary = 23 dirs] skill-deployment.md count convention: derive from arrays, never hardcode | Cascade-omission-at-count-update is a named failure mode (failure-mode-standard § PROC); v1.08 audit sized "~22 skills" the same way | **C2** | AC3 → "every skill in the deployed roster derived from the deploy.sh per-module arrays (22 at baseline; +1 canary)"; method: row count = live array totals at audit time. |
| AC4/AC5/AC8: inventory ≥5 ranked candidates, 5 fields each; framework has 6 named sections | durable decision-table form per prefer-durable-structures — AC already specifies schema-validation methods | section/field enumerations are restructure-independent | none contradicting | **C1** | (verbatim) |
| AC6: inventory maps #287, #286, #234 or documents exclusion | verify-before-recommend: existence-not-digit-count staleness test applied | [VERIFIED 2026-06-09: gh — #287 OPEN "Implement automated DoR validation for GitHub Issues"; #286 OPEN "Define triage analysis mode for release pipeline"; #234 OPEN "Define dual-format document model", epic:project-data-architecture] | issue-body renumber-rot lesson: keep existing refs unless verified-mismatch | **C1** | #287 KEEP (DoR content validation = promotion candidate); #286 KEEP (triage Phase A automation = skill-mode-vs-script boundary case); #234 KEEP-AS-WEAK-FIT — primary frame is data architecture; its translation-map executor + drift detection are promotion-shaped; the AC's own OR-clause permits documented exclusion on audit evidence. No body edit needed. |
| AC7: framework doc at location approved in Planning; "linked from OPERATIONS.md index" | pre-creation governance check performed: no existing governed home (see Premise row) → new file justified | live registry of record = `core/specs/framework-catalog.md` (11-col schema; deploy.sh Check 18); OPERATIONS.md no longer carries a doc index — KM registry delegation per OPERATIONS.md § KM Governance Ownership | v1.08 #2 registered its standard via cross-links + catalog-pattern surfaces | **C2** | AC7 → "file exists at the D-FrameworkHome location; registered as an INTERNAL row in `core/specs/framework-catalog.md` (Check 18); OPERATIONS.md cross-ref optional per Stage 5 (mirror-pair discipline if done)". |
| AC9: cross-references `pmo-platform/reference/failure-mode-standard.md` + `reversibility-protocol.md` | both disciplines remain binding (CLAUDE.md Universal Preferences) | [VERIFIED 2026-06-09: ls → `core/specs/failure-mode-standard.md`, `core/specs/reversibility-protocol.md` both exist] | path-drift Pattern B per doc-link-maintenance.md | **C2** | AC9 → live `core/specs/` paths. |
| AC10: ≥2 worked examples with "cross-link from framework worked examples → inventory rows" | reference-durability standard: durable corpus summarizes referenced content inline; a tracked doc must not live-link an operator-instance path (dead-file-ref gate would also reject it) | inventory is operator-instance per AC1 reconciliation | repo-integrity gates (v3.18) block dead links from tracked files | **C2** | AC10 → "worked examples name the inventory candidate IDs and inline their substance; no live link from the tracked framework to the operator-instance inventory". |
| Evidence/Notes: "#10 — prerequisite for the broader autonomy model" (+ #10 in the scattered-issues list) | verify-before-recommend | [VERIFIED 2026-06-09: gh — #10 OPEN "Parameterize repo-platform adapter — GitHub abstraction…" → **MISMATCH** with "autonomy umbrella" framing; #573 OPEN "[Epic]: Pipeline Engine & Automation" is the live umbrella; #187 already carries label `epic:pipeline-engine-automation`] | renumber-rot reconciliation: replace on verified-mismatch | **C2** | Replace #10 → #573 in Evidence + Notes (reconcile, don't annotate). |
| Dependencies: "[ASSUMPTION – CONFIRM] non-blocking relationships" | dependency analysis is mandatory (persona anti-pattern: no "independent without evidence") | [VERIFIED 2026-06-09: gh api …/issues/187/dependencies/blocked_by → []; …/sub_issues → []] case-study issues are read-only inputs; their states do not gate this release | — | **C2** | Confirm non-blocking (assumption resolved); replace #10 → #573 in the list. |

**Classification tally:** C1 ×3, C2 ×8, **C3 ×0** → no Tier 0 Premise Rejection. All C2 deltas are Tier 1 [ADJUST]-class body refinements — consolidated in the Reconciliation Register below for the hub to apply via `gh issue edit 187 --body` after plan approval (this spoke is planning-only and does not mutate the issue body). One instrumentation row (13-field schema, § 8) is included at the end of this comment for the hub to append to the operator-instance instrumentation log.

**G-PL1 (AC/substrate currency):** FAIL-as-found → reconciled in-plan (8 currency mismatches, all path/count/reference class, none premise-invalidating). Routes Tier 1 [ADJUST]: hub applies the Reconciliation Register to the issue body before Stage 5 reads it.
**G-PL2 (pre-plan crisping):** PASS — G1-02 description actionable (problem + 3 deliverables); G1-04 proposed change names deliverables and defers locations to Stage 5 by explicit design ("intentionally stays at WHAT level"); G1-05 all 10 ACs carry verification methods.
**Parallelization-Map currency:** Milestone #157 predates the standing-map convention (no `## Parallelization Map` H2 required) — check suppressed per stage-04 § Standing applicability. Single-issue bundle has no intra-bundle parallelization surface.

---

### Summary (30 seconds)

Single-issue release: #187 ships the agent-to-script promotion framework as three dependency-chained deliverables — (1) current-state audit of the script estate (operator-instance artifact), (2) ranked scripting-opportunity inventory (operator-instance artifact), (3) the promotion governance framework (tracked standard, the public deliverable) plus its `framework-catalog.md` registration. The issue body dates from 2026-04-24 (pre-restructure): 8 currency mismatches are reconciled in this plan; none invalidate the premise — the framework is still missing and the deliverables stand. Proposed: Release Class **novel**, version **v1.09**, topology **D-C SINGLE**, Stage 5 **ACTIVATE** (ADR-011 analysis-class variant for the audit), Stage 7 DT **skip** (no runtime impact), rollback **additive-only / LOW**. Seven D-gates for the operator, then scaffold.

### Reconciliation Register (parent body → live state; hub applies via `gh issue edit 187 --body` post-approval)

| # | Body location | Stale text | Live replacement |
|---|---|---|---|
| R-1 | Description + Evidence | `deploy.sh` | `core/deploy/deploy.sh` |
| R-2 | Description + Evidence + AC2 | `.claude/hooks/*.sh` | `core/hooks/*.sh` (source; `.claude/hooks/` is the deployed runtime mirror) |
| R-3 | Evidence | `pmo-platform/skills/pmo-skill-refiner/scripts/*.py` | `release/skills/pmo-skill-refiner/scripts/*.py` |
| R-4 | Description + Evidence + AC2 | `projects/Deep Research PMO/` …`projects/` | Layer 2 operator-instance scripts (outside the public repo); audit scope per D-Layer2AuditScope |
| R-5 | Evidence | `.claude/rules/release-process.md § Stage Compression` | `release/governance/release-process.md § Stage Compression (Git-Native)` |
| R-6 | Evidence | `pmo-platform/reference/schemas/gate-criteria-spec.md` | `core/schemas/gate-criteria-spec.md` |
| R-7 | Affected Files | `[ASSUMPTION – CONFIRM] TBD` | resolved by this plan's File Change Matrix (framework home per D-FrameworkHome; audit/inventory per D-AuditPlacement) |
| R-8 | AC1 | `pmo-platform/analysis/<audit-name>-YYYY-MM-DD/` | `<OPERATOR_INSTANCE_ANALYSIS_PATH>/script-promotion-audit-YYYY-MM-DD/` (operator-instance; SUMMARY.md required) |
| R-9 | AC3 | "the 19 custom skills" (count ≥ 19) | "every skill in the deployed roster derived from the `deploy.sh` per-module arrays" (22 at baseline: 13+6+3; +1 source-only canary; method: count = live array totals at audit time) |
| R-10 | AC7 | "linked from OPERATIONS.md index" | "registered as an INTERNAL row in `core/specs/framework-catalog.md` (deploy.sh Check 18); OPERATIONS.md cross-ref optional per Stage 5, mirror-pair discipline if added" |
| R-11 | AC9 | `pmo-platform/reference/failure-mode-standard.md`, `pmo-platform/reference/reversibility-protocol.md` | `core/specs/failure-mode-standard.md`, `core/specs/reversibility-protocol.md` |
| R-12 | AC10 | "cross-link from framework worked examples → inventory rows" | "worked examples name inventory candidate IDs and inline their substance (no live link from tracked doc to operator-instance artifact)" |
| R-13 | Evidence + Notes + Dependencies | "#10 (autonomy umbrella)" / "#10 — prerequisite building block for the broader autonomy model" | #573 ([Epic]: Pipeline Engine & Automation — the live umbrella; #187 already carries the epic label). #287/#286/#234 retained per A0 dispositions. Dependencies confirmed non-blocking. |

### Dependency Graph

**In-bundle:** #187 only. [VERIFIED 2026-06-09: native blocked_by = [] ; sub_issues = [] ] — no blockers, no sub-issues. No intra-bundle edges (single issue).

**Internal deliverable DAG (hard, within #187):**
```
D1 audit  →  D2 opportunity inventory  →  D3 framework doc (+ catalog row)
```
D2 ranks candidates D1 discovers; D3's ≥2 worked examples (AC10) ground in D2 rows. This chain is why merge/split keeps the issue whole.

**External soft edges (non-blocking, read-only inputs):** #573 (parent epic — context); #287, #286, #234 (case-study inputs to D2 — their issue states do not gate this release); #415 (version-identity codification, bundled in a different milestone — informs D-Version but does not block); #375 (agent-definition carryover — Model Provenance context only).

### Implementation Sequence

Dependency-ordered; sequence, not schedule (single-operator PMO — no dates).

| Step | Work | Output surface |
|---|---|---|
| 0 | Engineering Commit 0: transcribe this approved plan to `release/releases/plans/v1.09_RELEASE_PLAN.md` on `release/v1.09-agent-to-script-promotion` (per D-C SINGLE; carry v1.08 plan-file conventions: frontmatter, override markers as needed, Issue References block) | release branch |
| 1 | Stage 5 Solutioning (ACTIVATE): framework skeleton (6 sections as decision tables), promotion-ladder design, audit methodology per ADR-011 analysis-class research-methodology-design variant (sampling frame over 71 tracked scripts + 22-skill roster, evidence grading, validity threats), placements per D-gates, worked-example selection criteria | Stage 5 spec on sub-task; adversarial-review pairing per stage-05 Phase A6.5 (Collective Review itself does not fire — single issue) |
| 2 | D1 audit execution → `<OPERATOR_INSTANCE_ANALYSIS_PATH>/script-promotion-audit-<date>/SUMMARY.md`: catalog 71 tracked scripts (+ runtime-mirror note + Layer 2 per D-Layer2AuditScope) with location / purpose / governance pattern / test coverage / agent invocation path; mechanical-vs-judgment segmentation across the 22-skill roster | operator-instance artifact |
| 3 | D2 inventory: ≥5 ranked candidates × 5 fields (pattern, stability, frequency evidence, target promotion level, readiness); map #287 + #286 as case studies; #234 mapped-or-documented-excluded | operator-instance artifact |
| 4 | D3 framework doc at `core/standards/agent-script-promotion-framework.md` (per D-FrameworkHome): 6 sections — triggers / authoring+review path / testing standards / drift monitoring / agent-script interface / versioning+deprecation; cross-refs to `core/specs/failure-mode-standard.md` + `core/specs/reversibility-protocol.md`; ≥2 worked examples inlining D2 candidates | release branch |
| 5 | Registration: INTERNAL row in `core/specs/framework-catalog.md`; optional OPERATIONS.md cross-ref (both mirrors, same commit, Check 9) per Stage 5 determination | release branch |
| 6 | Stage 8 QA (document conformance; Stage 7 DT skipped per matrix) → Stage 9 Deep review → Stage 12 (merge release PR; signed-annotated tag v1.09; `gh release create`; `deploy.sh --check` incl. Check 18 on the new catalog row) → Stage 13 close-out corpus; mark #187 as closed at Stage 13; 30-day outcome window | main + close-out chore PRs |

### Stage Applicability Matrix

Per `core/standards/planning-solutioning-handoff.md` § 4 (all-or-nothing release-level activation, logical OR over T1–T6):

| Issue | T1 | T2 | T3 | T4 | T5 | T6 | Verdict | Rationale |
|---|---|---|---|---|---|---|---|---|
| #187 | ✓ new tracked standard (framework doc) | ✗ no skill-logic change planned (see note) | ✓ body literally defers HOW "to Stage 5 Solutioning" (auto-flag) | ✓ multiple valid approaches: framework home, audit placement, ladder design, discovery mechanism | ✗ ≤2 governance files touched | ✓ Affected Files "[ASSUMPTION – CONFIRM] TBD"; audit finding-count unknown | **ACTIVATE** | T1+T3+T4+T6 fire |

**Release-level rollup: ACTIVATE.** ADR-011 analysis-class research-methodology-design persona variant applies to the audit deliverable (v1.08 #2 precedent). **Collective Review does not fire** (single issue with Solutioning activated; skip condition is 0–1); the stage-05 Phase A6.5 adversarial-review output still pairs with the Stage 5 spoke and routes directly into the operator's Stage 5 Decision Briefing.

| Stage | Applies? | Basis |
|---|---|---|
| 5 Solutioning | **YES** | T1/T3/T4/T6 above; novel-class bias ALL |
| 6 Engineering | YES | authoring + registration commits |
| 7 Dev Testing | **SKIP** | governance + analysis artifacts; no executable/runtime impact (v1.08 #406/#2 precedent). **Conditional re-activation:** if Stage 5 design adds executable tooling (e.g., a script-inventory generator or governance check), DT re-activates via plan deviation — note in Stage 5 output. |
| 8 QA Testing | YES | document conformance: 6 sections present, 10 reconciled ACs verified, catalog row valid, cross-refs resolve |
| 9 Plan Review | YES — **Deep** | novel class; G-PR7 goal-conformance vs the Outcome Statement |
| 10 Dry Run / 11 Snapshot | Compressed | git-native; no non-git deployment, no destructive ops, no skill deploys |
| 12 Execute | YES | merge, signed-annotated tag v1.09, GitHub Release, `deploy.sh --check` |
| 13 Close | YES | close-out corpus; mark #187 as closed; 30-day outcome window |

### Contention Map

**Within-release:** single issue — no cross-issue file contention. One internal coupling: if Stage 5 confirms the OPERATIONS.md cross-ref, `core/governance/OPERATIONS.md` ↔ `operations/OPERATIONS.md` must change byte-identically in the same commit (Check 9 mirror pair — v1.08 R4 precedent).

**Cross-PR (A4 extension, baseline-pinned):** open PRs at baseline = **0** [VERIFIED 2026-06-09: `gh pr list --state open` → []]. Empty-population policy applied per the promoted release-ops pattern: baseline pinned to `3f91d05` + last-5-merged window (#576, #570, #543, #539, #528 — all merged, all already in baseline; #570/v1.08 added `core/standards/skill-pipeline-alignment.md` and edited both OPERATIONS.md mirrors, no collision with this release's filenames [VERIFIED: no `agent-script-promotion*` file exists]). **Re-check the open-PR population at Engineering Commit 0**; mid-pipeline divergence is caught at Stage 9 Phase A6.5 (PRIMARY) per the baseline-pin awareness clause. `check-line-range-overlap.py` not invoked (≤3-PR audit, population empty).

### File Change Matrix

Engineering scope (release PR; one path per line, machine-readable per the baseline-pin clause). Entries 4–5 are conditional on Stage 5 confirming the OPERATIONS.md cross-ref:

```
release/releases/plans/v1.09_RELEASE_PLAN.md
core/standards/agent-script-promotion-framework.md
core/specs/framework-catalog.md
core/governance/OPERATIONS.md
operations/OPERATIONS.md
```

Intent: line 1 ADD (Commit 0) · line 2 ADD (the framework standard; filename per D-FrameworkHome) · line 3 EDIT (append INTERNAL catalog row) · lines 4–5 EDIT (conditional cross-ref; byte-identical mirror pair).

*(Commit-0 note: this transcribed matrix is superseded by the refined matrix in § Engineering Commit 0 Amendments below — lines 4–5 dropped per the ratified D-4; three pre-authorized additions.)*

**Operator-instance artifacts (outside the repo, not in the machine-readable block):** `<OPERATOR_INSTANCE_ANALYSIS_PATH>/script-promotion-audit-<date>/SUMMARY.md` + the opportunity-inventory artifact (ADD; per D-AuditPlacement).
**Stage 12/13 close-out corpus (post-GO chore commits, standard for every release):** `release/releases/RELEASE_INDEX.md`, `CHANGELOG.md`, `release/releases/notes/v1.09_RELEASE_NOTES.md`, operator-instance RELEASE_LOG row.

### Risk Register

| ID | Class | Risk | Owner-action + Mitigation |
|---|---|---|---|
| R1 | Scope (XL, audit cardinality) | Audit spans 71 scripts + 22 skills; finding count unknown until it runs (T6). Inventory could explode. | **Stage 5 spoke** defines sampling frame + evidence-grading + a cap discipline per ADR-011; inventory is the synthesis (ranked candidates, ≥5 floor). Candidate *executions* (actually promoting anything to a script) are explicitly OUT of this release — follow-ups per candidate. D-AuditSynthesisGranularity pre-decides downstream issue granularity. |
| R2 | Substrate staleness | Stage 5/6 spokes re-derive stale paths if the 2026-04-24 body is consumed unreconciled. | **Hub** applies the Reconciliation Register via `gh issue edit 187 --body` (Tier 1 [ADJUST] + deviation-log entry) after plan approval, BEFORE Stage 5 scaffolding. |
| R3 | Boundary (Layer 2 audit) | `projects/` scripts are operator-instance; operations-bridge enumerates only 3 read-allowed `_config` files — a full Layer 2 script sweep exceeds the enumerated read surface, and spoke worktrees may not see operator paths. | **Operator** renders D-Layer2AuditScope. Default: repo-tracked + runtime-mirror scope with Layer 2 documented as an enumerated gap; option B authorizes a read-only operator-session sweep (reads only — operations-bridge write boundary is never crossed). |
| R4 | Public-leak | Audit/inventory carry operator-instance paths and operational details; framework worked examples inline their substance. | **Stage 6 spoke** keeps audit/inventory operator-local; inlines only depersonalized substance in the tracked framework. Repo-integrity depersonalization gate backstops at PR time. |
| R5 | Over-engineering | A 6-section governance framework can balloon into ceremony (the issue itself warns: under-scripting via governance friction). | **Stage 5 spoke** runs the build-principle assessment; sections authored as decision tables per prefer-durable-structures; enforcement tooling (hooks/deploy-checks for script governance) deferred to follow-ups — this release is additive paper governance + registration only. |
| R6 | Contention (mirror pair) | One-sided OPERATIONS.md edit fails Check 9. | **Stage 6 spoke** edits both mirrors in one commit; runs the Check-9 diff before PR (only if Stage 5 confirms the cross-ref). |
| R7 | Cross-PR divergence | Baseline population empty → a PR opening mid-pipeline silently invalidates the zero-contention finding. | **Hub** re-checks open PRs at Engineering Commit 0; Stage 9 A6.5 divergence checkpoint is the PRIMARY catch; baseline SHA documented here for reproducibility. |
| R8 | Rollback | New standard + catalog row + cross-refs + plan file. | **LOW complexity, additive-only:** `git revert` of the release PR removes all tracked changes; no data migration, no enum/stage-count change, no skill deploys. Operator-instance artifacts revert by `trash` of the analysis folder (CHEAP). |
| R9 | Upstream-compat (forward flag) | Framework section (e) "agent-script interface" may introduce SKILL.md conventions (how skills discover/invoke scripts) — that WOULD touch skill-authoring surface. | **Stage 5 spoke** must render that design choice as a D-decision WITH the full Upstream-compatibility subsection (consult `core/standards/upstream-reference-catalog.md`; `anthropic-skills:skill-creator` frontmatter is `name:`+`description:` only — any new field must register as a PMO extension). Flagged now so Stage 5 cannot omit it. |

**Aggregate rollback strategy:** single `git revert` of the release PR; tag/Release removal per RELEASE_PROTOCOL only if reverting post-Stage-12. Nothing deploys to runtime surfaces (no skills changed), so there is no deploy rollback leg.

### Release Class & Domain

**Proposed Release Class: `novel`** (operator renders at Phase B3 via D-ReleaseClass).
- **Trigger evidence:** novel (a) ≥1 new reference doc — the framework standard; ✓ novel (b) ≥1 D-class decision — 7 D-gates below; ✓ novel (c) Stage 5 ADR plausible (promotion-ladder canonicalization may warrant one). ✓
- **Why not `routine`:** new tracked file + size:XL + 7 D-decisions disqualify (routine requires zero new files, zero D-class).
- **Why not `cross-cutting`:** 0 `pipeline/stage-*.md` edits; ≤2 named governance surfaces (OPERATIONS.md mirror pair, conditional); 0 in-bundle compositional edges (single issue).
- **Why not `hotfix`:** no P1/P2 defect; not corrective.
- **Differentiation posture (novel):** Engagement density **Standard** · Stage 9 review depth **Deep** · Stage 5 activation bias **ALL** · Stage 13 outcome-window **30-day**.

**Milestone-description `## Release Class` block (for the hub's Phase B3 PATCH):**
```
## Release Class

Class: novel
Rationale (max 2 sentences): The release introduces a new tracked reference
standard (the agent-to-script promotion framework) and renders 7 D-class
decisions — novel triggers (a) and (b) fire. Cross-cutting thresholds
(≥3 stage files / ≥3 governance surfaces / ≥3 bundle edges) are not met.
Differentiation posture:
  - Engagement density: Standard
  - Stage 9 review depth: Deep
  - Stage 5 activation bias: ALL
  - Stage 13 outcome-window: 30-day
```

**Domain classification (A1.5/A3):** `domain: governance`, **sourcing-exempt** — the File Change Matrix consists entirely of internal pmo-platform artifacts (a `core/standards/` doc, a catalog row, governance cross-refs, the plan file); the platform encodes its own internal-deliverable practice at `core/standards/domain-best-practices/governance.md` [VERIFIED: file exists]. Provenance label:
`domain_practice: { source: N/A — pipeline-internal release, date: 2026-06-09, domain: governance }`
Stage 5 note: if design refinement exposes citable external automation-maturity practice (e.g., for the promotion-trigger ladder), upgrade toward a cited source per the Stage 5 Domain-Practice Sourcing Refinement — optional, not required for an internal-governance deliverable.

### Release Outcome Statement (draft — for the Phase B1 briefing; hub PATCHes the Milestone description top-of-description on approval)

```
### Release Outcome Statement

**AFTER** — The platform carries a governed agent-to-script promotion
framework as a tracked standard: promotion triggers, authoring/review path,
testing standards, drift monitoring, agent-script discovery, and
versioning/deprecation are defined once, registered in the framework
catalog, and grounded in a completed audit of the script estate plus a
ranked opportunity inventory (≥5 candidates) with at least two worked
examples inlined in the standard. Scripting decisions cite the framework
instead of being re-litigated ad hoc.

**BEFORE** — Scripts across the platform follow at least four divergent
governance patterns (deploy tooling without rule IDs; hooks with rule IDs,
allowlists, and warn→enforce shakedown; skill-internal eval scripts;
ungoverned operator-instance scripts), there is no inventory of what is
scripted versus agent-executed, and no rule says when a repeated pattern
earns scripting.

**Actor(s):** Stage 5/6 spokes author audit, inventory, and framework; hub +
operator cite the framework at future promotion decisions.

**Success Indicator:** The framework standard exists on main with all 6
governed section headings and a registered row in
core/specs/framework-catalog.md, and `deploy.sh --check` Check 18 passes
against the new row at the post-merge verification.
```

(The Milestone's existing **Value/Today** paragraphs are the grandfathered pre-schema form; this block supersedes them at top-of-description per release-outcome-statement-template.md § 2. The existing initiative metadata bullets stay below it.)

### Operator Decisions (D-gates)

Rendered per the hub-spoke-bridge D-Gate Template. Operator renders before scaffolding (Procedure 0 Step 7 → Procedure 1).

#### D-ReleaseClass: What Release Class does this release carry?
**Gate input:** Spoke-proposed class + trigger evidence per `release-class-taxonomy.md` (§ Release Class & Domain above).
**Gate decision:** (A) routine, (B) **novel** [RECOMMENDED], (C) cross-cutting, (D) hotfix.
**Blocks:** Phase B3 milestone-description authoring; downstream per-class posture (Stage 9 depth, engagement density).
**Upstream compatibility:** N/A — Release Class is PMO platform internal taxonomy; no Anthropic upstream surface. Upstream compatibility check does not apply.
**Reversibility / Confidence:** CHEAP / HIGH (re-classifiable per Re-Classification Protocol).

#### D-Version: What release identifier does this release carry?
**Gate input:** Milestone title is version-less. RELEASE_INDEX lineage: v1.08 is the most recent versioned release (2026-06-08); the two freshest releases (v1.07, v1.08) used slug-titled milestones + assigned vX.Y; three version-less precedents exist (public-flip-install-blockers, intake-elicitation-skill, domain-aware-stage5-design), each an ad-hoc Stage 4 exception; the codification of version-vs-versionless identity is OPEN at #415 (bundled in a different milestone — not yet a standing rule). [VERIFIED 2026-06-09: RELEASE_INDEX.md rows; #415 OPEN, milestone stage3-bundling-composer-and-identity]
**Gate decision:** (A) **v1.09** — next sequential in the active v1.x line; milestone title stays `agent-to-script-promotion` (v1.07/v1.08 practice) [RECOMMENDED]; (B) v1.09 + rename milestone to `v1.09-agent-to-script-promotion` (bundle-composition-doctrine §5 letter); (C) version-less (slug identity; repeats the ad-hoc exception #415 exists to end — gets no tag and no GitHub Release).
**Blocks:** plan filename (`v1.09_RELEASE_PLAN.md`), branch name (`release/v1.09-agent-to-script-promotion`), tag, GitHub Release, INDEX/CHANGELOG rows.
**Upstream compatibility:** N/A — versioning is PMO-internal; does not touch skill-authoring surface. Upstream compatibility check does not apply.
**Reversibility / Confidence:** CHEAP / HIGH — a label until the tag is cut at Stage 12.

#### D-C Branch Topology: single release branch or per-issue branches?
**Gate input:** Single-issue bundle → exactly one Engineering stream; no parallel-PR benefit to Option-A; the concurrent-spoke contention pattern (promoted release-ops feedback, 2026-06-04) is moot at N=1 spokes.
**Gate decision:** (A) **D-C SINGLE** — one branch `release/v1.09-agent-to-script-promotion`, plan committed as Engineering Commit 0, one release PR [RECOMMENDED — default]; (B) D-C OPTION-A — per-issue branch + Stage 4 release-plan chore PR (adds ceremony with zero parallelism gain at N=1).
**Blocks:** Commit-0 mechanics; where Stage 5/6 spokes read the plan from.
**Upstream compatibility:** N/A — branch topology does not touch skill-authoring surface. Upstream compatibility check does not apply.
**Reversibility / Confidence:** CHEAP / HIGH.

#### D-FrameworkHome: where does the framework standard live?
**Gate input:** Framework governs scripts + agent work across core (hooks, deploy), release (tools, skill scripts), and operations (skill mechanical work) — a cross-module structural standard. Pre-creation governance check found no existing home [VERIFIED: grep → 0 files]. Precedents: `core/standards/skill-pipeline-alignment.md` (v1.08), canonical-skill-structure, reference-durability all live at `core/standards/`.
**Gate decision:** (A) **`core/standards/agent-script-promotion-framework.md`** [RECOMMENDED — cross-module scope; Check 18 registry pattern]; (B) `release/references/standards/` (understates the operations/core scope); (C) other operator-chosen home. Filename refinable at Stage 5 within the chosen directory.
**Blocks:** File Change Matrix line 2; AC7 reconciliation text; catalog-row `canonical_doc` value.
**Upstream compatibility:** N/A — document placement does not modify skill-authoring surface. (Forward flag: the framework's agent-script-interface SECTION may touch skill-authoring surface at Stage 5 — that Stage 5 D-decision carries the REQUIRED upstream-compat subsection per R9.)
**Reversibility / Confidence:** CHEAP / HIGH (git mv + catalog-row edit if relocated).

#### D-AuditPlacement: where do the audit + inventory artifacts land?
**Gate input:** AC1's `pmo-platform/analysis/` path predates the restructure. Taxonomy § 2 apply-test: read-once analysis with no cross-operator readership → OPERATOR-INSTANCE; pipeline precedent uses `<OPERATOR_INSTANCE_ANALYSIS_PATH>/<audit>-<date>/`; v1.08 #2 shipped the same tracked-standard/operator-local-registers split.
**Gate decision:** (A) **operator-instance analysis path** `<OPERATOR_INSTANCE_ANALYSIS_PATH>/script-promotion-audit-<date>/` (SUMMARY.md + inventory); public repo carries only the framework with inlined worked examples [RECOMMENDED]; (B) track the audit in-repo (forces depersonalization of operator-instance paths and makes a point-in-time snapshot durable corpus — contra intermediate-artifact discipline).
**Blocks:** AC1/AC10 reconciliation text; Stage 6 write targets; R4 mitigation.
**Upstream compatibility:** N/A — artifact placement does not touch skill-authoring surface. Upstream compatibility check does not apply.
**Reversibility / Confidence:** CHEAP / HIGH.

#### D-Layer2AuditScope: does the audit enumerate operator-instance (`projects/`) scripts?
**Gate input:** AC2 names `projects/` in scope; operations-bridge.md grants Claude Code read access to 3 enumerated `_config` files and forbids writes; a full `projects/` script sweep is read-only but exceeds the enumerated read surface; `projects/` is not enumerable from the public repo at all.
**Gate decision:** (A) repo-tracked + runtime-mirror scope only; Layer 2 documented as an explicit enumerated gap in the audit SUMMARY (smallest boundary footprint); (B) **operator-authorized read-only sweep** of `projects/**/*.{sh,py}` executed from the operator session (hub-side, not a spoke worktree), feeding the audit — satisfies AC2's original intent; reads only, no writes [RECOMMENDED — with the authorization recorded in the decision log]; (C) operator self-enumerates and hands the listing to the audit.
**Blocks:** AC2 reconciliation text; audit SUMMARY scope section; R3 mitigation.
**Upstream compatibility:** N/A — audit scope does not touch skill-authoring surface. Upstream compatibility check does not apply.
**Reversibility / Confidence:** CHEAP / HIGH (scope is re-expandable in a follow-up audit).

#### D-AuditSynthesisGranularity: how do inventory candidates become downstream issues?
**Gate input:** hub-spoke-bridge G-Hybrid rule — default 1 issue per audit-SUMMARY downstream-handoff item when count ≤ 10 and single-axis; high-cardinality audits need a pre-decided override. Candidate count unknown until D1/D2 run (T6).
**Gate decision:** Pre-decide the default now: (A) **G-Hybrid default** — 1:1 issue per promotion-ready candidate when ≤ 10, single-axis [RECOMMENDED]; (B) per-category bundles (≤ 5 buckets) if the inventory exceeds 10 promotion-ready candidates; (C) sample-then-stage (file top-N, defer rest). Candidate issues are filed via the standard intake templates AFTER this release ships — none are created in-release.
**Blocks:** Stage 13 follow-up routing; nothing pre-Engineering.
**Upstream compatibility:** N/A — issue granularity does not touch skill-authoring surface. Upstream compatibility check does not apply.
**Reversibility / Confidence:** CHEAP / HIGH.

### Recommendations

1. **Approve the plan with the 13-row Reconciliation Register applied to #187's body first** (hub, Tier 1 [ADJUST], `gh issue edit 187 --body` + deviation-log entry) — Stage 5 must read a reconciled body, not the 2026-04-24 substrate (R2).
2. **Merge/split: keep #187 whole.** The audit → inventory → framework chain is dependency-coupled (AC10 grounds the framework's worked examples in inventory rows); splitting deliverables into separate issues would serialize across releases for no risk reduction. Guard the other direction instead: candidate promotions, enforcement tooling, and any remediation are OUT of this release — follow-ups per D-AuditSynthesisGranularity (v1.08 D-2-Scope precedent).
3. **Render the 7 D-gates** (D-ReleaseClass novel · D-Version v1.09 · D-C SINGLE · D-FrameworkHome core/standards/ · D-AuditPlacement operator-instance · D-Layer2AuditScope read-only sweep · D-AuditSynthesisGranularity G-Hybrid default), then PATCH the Milestone description with the `### Release Outcome Statement` block (top) + `## Release Class` block per Phase B1/B3, then scaffold per Procedure 1.
4. **Forward flag for Stage 5 (binding):** the agent-script-interface section is the one place this release can touch skill-authoring surface; any SKILL.md field/convention it proposes carries the REQUIRED Upstream-compatibility subsection (consult `upstream-reference-catalog.md`; note its `last_verified` dates are aging — re-verify and bump as a side effect, v1.08 recommendation 4 precedent).
5. **Out-of-scope discoveries for the hub to route (none filed by this spoke — issue creation is out of spoke scope):**
   - `core/rules/skill-deployment.md` § Tracked Skills hardcodes "OPERATIONS_SKILLS (12)" while the live array carries 13 (intake-desk added) — exactly the count-drift class Check 5(c) exists for; worth a one-line surgical fix via intake.
   - The Milestone description tagline reads "~16 pts across 1 issues" — minor grammar/count check at the Phase B3 PATCH (it counts work items correctly; "1 issues" is the typo).
   - The hub-verified orientation note said "no `.claude/agents/pmo-planning.md` exists (tracked at #375)" — confirmed by this spoke's runtime (no agent-definition default in effect); no action this release.
6. **Stage 13 instrumentation row** (hub appends to the operator-instance `triage-design-rereview-instrumentation.md`; this spoke does not write operator-instance files): `release_version: v1.09 (pending D-Version) · issue_number: 187 · stage: 4 · re_review_date: 2026-06-09 · effort_tier: complex · requirements_count: 11 · c1_count: 3 · c2_count: 8 · c3_count: 0 · c2_deltas_logged: 0 (register pending hub application) · c3_pt_distribution: — · c3_outcomes: — · false_positive_flagged / downstream_rework_avoided: fill at Stage 13`.

### Model Provenance

- Invocation model parameter: `fable` (passed explicitly by hub)
- Agent-definition default: N/A — no `.claude/agents/pmo-planning.md` exists in this install (tracked at #375)
- Parent-session model: claude-fable-5[1m] (Fable 5, 1M context) as reported by this runtime
- Designated-model match: **YES** — highest-tier model at largest context per the operator's standing (deliberately not version-pinned) preference

### Issue References

References #187 (parent — the framework work item; mark #187 as closed at Stage 13). References #579 (this Stage 4 sub-task; mark #579 as closed on completion of this planning pass). References #573 (live automation epic — replaces the body's stale umbrella pointer), #287 + #286 + #234 (case-study inputs, all verified OPEN), #415 (version-identity codification, different milestone), #375 (agent-definition carryover), #10 (verified MISMATCH for the umbrella framing — now the repo-platform adapter issue), #576 + #570 + #543 + #539 + #528 (baseline merge window), #157 (the Milestone, by number).


---

## Decision Log

### Stage 4 Release Planning Gate (Phase B1/B3) — rendered 2026-06-09, main-thread chat (operator)

| Decision | Verdict | Notes |
|---|---|---|
| Release plan (Phase B1) | **APPROVED — full package** | Authorizes: Reconciliation Register application to #187's body, Milestone description PATCH (Outcome Statement + Release Class + count typo), Procedure 1 scaffolding |
| D-ReleaseClass | **novel** (as recommended) | Stage 9 depth Deep · engagement Standard · Stage 5 bias ALL · 30-day outcome window |
| D-Version | **A — v1.09, title kept** (as recommended) | Plan file `v1.09_RELEASE_PLAN.md` · branch `release/v1.09-agent-to-script-promotion` · tag v1.09 |
| D-C Branch Topology | **SINGLE** (as recommended) | Plan committed as Engineering Commit 0 on the release branch |
| D-FrameworkHome | **`core/standards/agent-script-promotion-framework.md`** (as recommended) | Filename refinable at Stage 5 within the chosen directory (latitude not exercised — name conforms to the surveyed scheme) |
| D-AuditPlacement | **Operator-instance analysis path** (as recommended) | `script-promotion-audit-<date>/` + SUMMARY.md; public repo carries only the framework with inlined worked examples |
| D-Layer2AuditScope | **A — repo scope only** (operator's choice; DIVERGES from spoke recommendation B) | Audit covers tracked repo + runtime mirror only; Layer 2 (`projects/`) documented as an explicit enumerated gap in the audit SUMMARY |
| D-AuditSynthesisGranularity | **G-Hybrid default** (as recommended) | 1:1 issue per promotion-ready candidate when ≤10 and single-axis; candidates filed post-release via standard intake |

Out-of-scope routing at that briefing: #580 filed (skill-deployment.md roster-count drift); #375 enriched (no `.claude/agents/pmo-*.md` definitions in this install).

### Stage 5 Scope-Lock (Solutioning + Adversarial Review) — rendered 2026-06-10, main-thread chat (operator)

**Verdict: RATIFY + FOLD ALL 8.** The two-part Stage 5 spec on sub-task #582 is the ratified implementation spec for v1.09.

| Item | Disposition |
|---|---|
| D-1 Promotion ladder AS0–AS4 + ADR-020 | **RATIFIED** — ADR-020 promotes Proposed → **Accepted** at this gate (single-issue-release equivalent of Collective Review per the ADR status enum) |
| D-2 Discovery = point-of-use citation convention; NO new SKILL.md frontmatter field | **RATIFIED** — upstream verdict `aligned` (R9 binding satisfied) |
| D-4 DROP the conditional OPERATIONS.md cross-ref | **RATIFIED** — catalog row is the governed registration surface; plan risk R6 retires; File Change Matrix lines 4–5 removed |
| D-5 Readiness-gated lexicographic ranking | **RATIFIED** |
| Adversarial corrections ×5 (PRF-1/FMF-1 · PRF-2 · FMF-2 · FMF-4 · FMF-5) | **FOLD into Stage 6** via Commit-0 deviation-log lines + direct spec-amendment incorporation |
| Adversarial counter-designs ×3 (CDF-1 · CDF-2 · CDF-3) | **ADOPTED** — incorporated at Stage 6 authoring |
| Catalog `canonical_doc` drift (10 of 13 INTERNAL rows) + km-governance count narrative | **Post-release intake item** — hub files after close; NOT v1.09 scope |
| Stage 7 DT | Remains **SKIP** (no executable tooling added by the spec) |

Path resolution recorded at the scope-lock: `<OPERATOR_INSTANCE_ANALYSIS_PATH>` resolves on this install to the operator-instance `analysis/` folder; D1/D2 artifacts land at `script-promotion-audit-${AUDIT_DATE_UTC}/` under it via absolute-path writes (lazy directory creation on first emit).

---

## Engineering Commit 0 Amendments

Recorded by the Stage 6 Engineering spoke at plan transcription (Commit 0). The transcribed Stage 4 sections above are the historical record of the approved plan; this section is the live state after the Stage 5 scope-lock.

### Tier-A Activated Design Artifacts

Carried per the ratified Stage 5 spec (G-CL6 obligation; Stage 13 reads this declaration):

| Artifact path | Flow class | Trigger | Activation tier | G-CL6 obligation |
|---|---|---|---|---|
| `core/standards/agent-script-promotion-framework.md` § promotion-lifecycle flow-block | Process-flow (agent/decision) | ≥1 gate (trigger evaluation; AS4 shakedown), ≥2 actors (agent proposes / operator ratifies), cited-as-canonical | A — NEW (ASCII flow-block in the framework doc) | Stage 13 reads this declaration; refresh-gate binds future releases touching the lifecycle |

No architecture/data-flow/concept-model class fires: no `explanation/` doc, no tracked schema file (the D2 inventory schema is specified in the ratified Stage 5 spec and instantiated operator-instance), no structural diagram outside the flow-block.

### File Change Matrix — refined (supersedes the transcribed Stage 4 matrix)

Per the ratified Stage 5 spec's Blast Radius matrix, as amended by the scope-lock:

| # | Path | Intent | Reversibility | Note |
|---|---|---|---|---|
| 1 | `release/releases/plans/v1.09_RELEASE_PLAN.md` | ADD (Commit 0) | CHEAP | this file |
| 2 | `core/standards/agent-script-promotion-framework.md` | ADD | CHEAP (git revert) | the public deliverable |
| 3 | `core/specs/framework-catalog.md` | EDIT (append 1 INTERNAL row) | CHEAP | Check 18-validated |
| 4 | both OPERATIONS.md mirrors | **DROPPED** per D-4 (scope-lock RATIFIED) | — | risk R6 retired |
| 5 | `core/standards/upstream-reference-catalog.md` | EDIT (bump 4 `last_verified_*` pairs) | CHEAP | matrix addition pre-authorized by approved-plan recommendation 4 |
| 6 | `core/ADRs/ADR-020-agent-script-promotion-ladder.md` | ADD | CHEAP | matrix addition pre-authorized by the plan's novel-class trigger (c) |
| 7 | `core/ADRs/README.md` | EDIT (ADR-020 index entry) | CHEAP | registry hygiene for change 6 |

Machine-readable block (one path per line; supersedes the transcribed block above):

```
release/releases/plans/v1.09_RELEASE_PLAN.md
core/standards/agent-script-promotion-framework.md
core/specs/framework-catalog.md
core/standards/upstream-reference-catalog.md
core/ADRs/ADR-020-agent-script-promotion-ladder.md
core/ADRs/README.md
```

Operator-instance artifacts (outside the repo, not in the machine-readable block): `script-promotion-audit-${AUDIT_DATE_UTC}/SUMMARY.md` + `opportunity-inventory.md` at the operator-instance analysis path (ADD; revert = `trash` the folder, CHEAP).

### Deviation Log

All entries Tier 1 [ADJUST] (minor adjustment, committed with rationale per the inter-stage feedback protocol) unless marked otherwise.

**(a) D-4 DROP — OPERATIONS.md cross-ref removed.** File Change Matrix lines 4–5 (both OPERATIONS.md mirrors) removed per the Stage 5 D-4 determination, ratified at the 2026-06-10 scope-lock. Risk R6 (mirror-pair contention) retired. Framing correction carried into this plan of record: the OPERATIONS.md pair contract is **content-identical modulo depth-adjusted link prefixes** (Check 9 normalizes link targets for that pair in `core/deploy/deploy.sh`), not byte-identical as the transcribed Contention Map / R6 text states — live raw diff 92 lines, 0 lines after link normalization.

**(b) Matrix additions ×3.** `core/ADRs/ADR-020-agent-script-promotion-ladder.md` (ADD) + `core/ADRs/README.md` index entry (EDIT) — pre-authorized by the approved plan's novel-class trigger (c) ("Stage 5 ADR plausible — promotion-ladder canonicalization"); `core/standards/upstream-reference-catalog.md` 4-entry `last_verified` bump (EDIT) — pre-authorized by approved-plan recommendation 4 ("re-verify and bump as a side effect"). No re-approval gate; this log preserves traceability.

**(c) The 8 folded adversarial dispositions** (scope-lock: 5 corrections FOLD + 3 counter-designs ADOPT; one line each):

| # | Item | Fold applied at Stage 6 |
|---|---|---|
| 1 | PRF-1/FMF-1 — population-per-claim rule | Deployed-roster claims use /22; directory claims use /23; corrected figures: 5 of 22 deployed skills cite scripts → 17 of 22 cite zero; the audit §3 segmentation table carries exactly the 22 array-roster skills, canary noted in §0 scope as source-only out-of-roster; every cited command carries the same filters as the number it backs |
| 2 | PRF-2 — D-1 reversibility-cell language | ADR-020's framing is canonical: CHEAP at ship, trending MODERATE as downstream artifacts accumulate rung citations (day-one enum spread is ≥3 tracked surfaces) |
| 3 | FMF-2 — mirror-diff excludes | Step-3 runtime-mirror diff runs with the extended exclude set (`*.template`, `tests`, `testdata`, `*.log`, `deploy-check.mode` added to `.mode`/`*.jsonl`/`*allowlist*`) AND SUMMARY §2 carries a declared expected-asymmetry table; identity = zero lines after declared exclusions |
| 4 | FMF-4 — SP-4 durable substance | Worked example SP-4 authored around the mechanism (catalog path-resolution check, AS0→AS3); the 10-row catalog drift cited as date-stamped historical motivation ("as of `3f91d05`"), not current state; worked-example selection criterion (d) durability added |
| 5 | FMF-5 — ranking key 3 comparator | Frequency-evidence strength = `[SOURCE]` occurrence count; tie → most recent occurrence date; final tie → SP-id order |
| 6 | CDF-1 (ADOPTED) — caller-type AS2/AS3 boundary test | A script is AS3 iff its invoker is another governed executable; AS2 iff agent-invoked; at AS3/AS4 `invocation_path` is coded from the caller registration (caller script / hook wiring / workflow YAML), not citation topology |
| 7 | CDF-2 (ADOPTED) — readiness-class-exempt cap | The cap-12 binds the NEEDS-STABILIZATION/WATCH tail only; READY rows always carry the full 8-field schema; if READY alone exceeds 12 the table grows and the pre-decided G-Hybrid >10 bundle override governs filing |
| 8 | CDF-3 (ADOPTED) — PR-bound discovery anchor | Framework §2 gains a REQUIRED row: the discovery anchor lands in the same PR as the script; Stage 9 review verifies it via the §1 promotion decision line |

**(d) R7 open-PR re-check at Engineering Commit 0.** `gh pr list --state open` → **0 open PRs** (2026-06-11 UTC) — matches the empty baseline population. Baseline note: `main` advanced from the plan baseline `3f91d05` to `eecbb5e` via the version-less `memory-to-corpus-codification` release (merged 2026-06-10); its diff touches release-corpus files only (CHANGELOG, RELEASE_INDEX/DIGEST/LOG, notes, its plan file) — zero overlap with this release's File Change Matrix. All spec baseline figures are re-derived at the audit SHA per validity threat V6.

**(e) `${AUDIT_DATE_UTC}` resolved = `2026-06-11`.** `date -u +%Y-%m-%d` at Engineering first commit per the ratified spec's Date Variable block. The operator-local calendar date at the scope-lock was 2026-06-10; the UTC convention governs, so all date-bearing artifacts in this release (audit folder, inventory header, catalog `last_reviewed`, upstream-catalog `last_verified_date`, ADR date) carry 2026-06-11.

**(f) Branch mechanics — pre-existing release branch (emergent).** `release/v1.09-agent-to-script-promotion` already existed local + remote at the branch point (`eecbb5e`, zero commits ahead of main) and was checked out by an orphaned sibling worktree (clean tree, no live session — liveness-checked). Engineering therefore operates detached-HEAD in its own session worktree and lands commits via refspec push (`git push origin HEAD:release/v1.09-agent-to-script-promotion`) per the documented concurrent-spoke contention recovery; no nested worktree created, the sibling worktree left untouched for the Stage 13 orphan-state sweep.

---

## Change Description

### Outcome

v1.09 gives the platform a governed answer to "when does repeated agent work earn a script, and what does a script owe once it exists." The tracked deliverable is `core/standards/agent-script-promotion-framework.md` — the AS0–AS4 promotion ladder (agent procedure → documented command → tracked tool → checkpoint-wired → autonomous guard; decision record ADR-020) plus six decision-table sections covering triggers, authoring/review, testing, drift monitoring, the agent-script interface, and versioning/deprecation. It is grounded in an executed census of the full script estate (71 tracked scripts coded across 9 columns; 22-skill mechanical-vs-judgment segmentation; runtime-mirror identity confirmed) and a ranked 8-candidate opportunity inventory, both landed operator-instance per D-AuditPlacement. Future scripting decisions cite the framework instead of being re-litigated ad hoc.

### Issues resolved

| # | One-line outcome | Status |
|---|---|---|
| #187 | Audit (71-row census + 22-row segmentation + mirror identity), ranked inventory (8 candidates; #287 + #286 mapped, #234 documented-excluded per its pre-decided rule), framework standard + catalog registration + ADR-020 — all 10 reconciled ACs verified at Engineering | DONE (#187 transitions to closed at Stage 13 per the block-close protocol) |

### Key decisions

- **D-1 / ADR-020 (Accepted at the 2026-06-10 scope-lock):** form-anchored five-rung enum AS0–AS4; split-promotion rule for judgment-class steps; the `AS` prefix keeps the autonomy-tiers disambiguation table clean.
- **D-2:** agent-script discovery = point-of-use citation convention on the agent's existing read path; **no new SKILL.md frontmatter field** (upstream verdict `aligned`; the four skill-creator-sourced upstream-catalog entries re-verified + bumped as the recommendation-4 side effect).
- **D-4 DROP:** the conditional OPERATIONS.md cross-ref is not confirmed — the framework-catalog row is the governed registration surface; plan risk R6 retired; both OPERATIONS.md mirrors untouched.
- **D-5:** readiness-gated lexicographic inventory ranking with the FMF-5 comparator (`[SOURCE]` occurrence count → recency → SP-id).
- **8 adversarial dispositions folded** (5 corrections + 3 adopted counter-designs) per the scope-lock — full echo in the Deviation Log above; highlights: caller-type AS2/AS3 boundary test (CDF-1), readiness-class-exempt inventory cap (CDF-2), discovery anchor lands in the promoting PR (CDF-3).

### Reversibility

CHEAP / HIGH confidence — single `git revert` of the release PR removes all tracked changes (additive standard + ADR + registry rows; no skill deploys, no data migration); the operator-instance audit folder reverts by `trash`.

### Downstream impact

- Promotion decisions now carry a citable trigger/counter-signal table and a per-PR promotion-decision-line + discovery-anchor requirement (Stage 9-verifiable).
- 4 READY inventory candidates queue for post-release 1:1 intake filing per D-AuditSynthesisGranularity (G-Hybrid default); the framework §6 growth path names the enforcement-tooling family (anchor-conformance check, script-registry spec, SP-4 path-resolution check) as future intake candidates.
- Stage 12 verification exercises `deploy.sh --check` Check 18 over the new catalog row (pre-verified exit 0 at Engineering).
- Catalog `canonical_doc` reconciliation (10 stale INTERNAL rows as of `3f91d05`) + the km-governance count narrative are routed as ONE post-release intake item per the scope-lock — NOT in this release.

### Cross-references

- Release plan: top of this file (`release/releases/plans/v1.09_RELEASE_PLAN.md`).
- Milestone: `agent-to-script-promotion` (v1.09) on the repository milestone surface.
- User-facing release notes: `release/releases/notes/v1.09_RELEASE_NOTES.md` (authored at Stage 13 Close; does not exist yet at Engineering time).

---

### Issue References

References #187 (parent — the framework work item; the 10 reconciled ACs; mark #187 as closed at Stage 13). References #579 (Stage 4 planning sub-task — the transcribed plan comment + first Decision Record + the Stage 5 scope-lock record live there). References #582 (Stage 5 Solutioning sub-task — the two-part ratified spec). References #583 (Stage 5 adversarial-review sub-task — the 8 folded dispositions). References #584 (Stage 6 Engineering sub-task — this Commit 0). References #287, #286, #234 (D2 case-study inputs; read-only). References #573 (parent epic — Pipeline Engine & Automation). References #580 (skill-deployment.md roster-count drift, filed at the Stage 4 briefing). References #375 (agent-definition carryover — Model Provenance context).
