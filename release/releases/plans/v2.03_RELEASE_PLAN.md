# v2.03 Release Plan — 03-ROLE-factory-and-pilots

| Field | Value |
|---|---|
| **Version** | v2.03 (re-versioned from v2.02 at Stage 12 — another release shipped v2.02 to `main` first, so this release moves up to v2.03 per the re-versioning discipline. Base HEAD was `v2.01`; `git describe` → `v2.01-2-g629c783` at branch-cut. The v3.x tag lineage is NOT reachable from HEAD — orphan, not next.) |
| **Milestone** | 03-ROLE-factory-and-pilots |
| **Epic** | skill-role-build (#284, parent epic — OPEN; inherits design) |
| **Release Class** | `novel` — Standard engagement / Deep Stage-9 review / Stage-5 activation bias ALL / 30-day outcome-window. Operator-rendered at the Stage-4 gate. |
| **Branch topology** | D-C SINGLE — one `release/v2.02-role-factory-and-pilots` branch, write-serialized, one release PR at Stage 12. |
| **Baseline** | `main` at `629c783` (0 open PRs at baseline for the in-scope paths; re-check the PR population at Stage 9 Phase A6.5 per audit-baseline discipline). |
| **Scope (LOCKED at Collective Review)** | **#186** (Phase 0 — factory foundation, task/L) + **#185** (Phase 1 — TPM + Program Coordinator pilots + format lock, story/M). |
| **Scope change** | **#450 (`pmo-architect`) DEFERRED to `04-ROLE-delivery-coverage`** — its compose-the-mode seam converges with a stub until `pmo-principal-engineer` (#1116) ships in milestone 04. Decision recorded on #450. |
| **Rollback** | Single `git revert -m 1` of the merged release PR (additive-only; `_shared/` files have no prior consumer, the deploy.sh guard reverts cleanly). |

This file is **Engineering Commit 0** on `release/v2.02-role-factory-and-pilots`. Each issue's implementation-ready Stage-5 design spec lives on its Stage-5 sub-task comment (cited below) — engineer each issue from its sub-task comment, not from this summary.

## Summary

This release builds the **role-Specialist factory and its first products**:

- **#186 — Phase 0 factory foundation** stands up the plumbing every role-skill build depends on: wires a **feeding-document consumption** path into `pmo-skill-refiner` (a pre-Interview input branch of Mode 2 / Create New), authors the **5 `operations/skills/_shared/` reference files** all role skills consume, and authors the **14-section `pmo-feeding-document-v02.md`** format reference. It also adds a fail-CLOSED `deploy.sh` Check 5(a) skip so the new `_shared/` sibling directory does not trip skill-roster-drift.
- **#185 — Phase 1 pilots + format lock** builds the 2 pilot skills (TPM + Program Coordinator) from the v0.2 format and **locks the feeding-document format** (post-Phase-1 changes require a governance update). Gated behind #186's foundation.

The hard edge is the **strict serialization #186 → #185** (format-lock gate — #185 cannot start until #186's foundation exists). Under D-C SINGLE the hub routes **one Engineering chip at a time**; #185 Stage 6 is gated behind #186 Stage 6.

## Collective Review scope-lock (authoritative)

Locked at Collective Review (adversarial review #1279). The three Stage-5 issues' verdicts: #186 PASS (adversarial Minor) · #185 PASS (adversarial Major) · #450 DEFERRED.

**Approved refinements folded into this plan:**

- **(a) #186 — fail-CLOSED `deploy.sh` Check 5(a) skip.** The skip is an **exact-match `== _shared`** guard (NOT a fail-open `_*` wildcard), plus a comment **codifying the convention** (an `_`-prefixed directory is a non-skill shared-resource dir; extend the allowlist explicitly if more are added). This preserves the gate's fail-closed property — a future stray `_`-prefixed directory still FAILs roster-drift unless explicitly allowlisted.
- **(b) #185 — author the format-lock note AFTER CS-15 sign-off.** The lock is enforced by the repo note + the No-ungoverned-changes governance rule (the operator-local Skills-Map surface is unverifiable by a repo gate).
- **(c) Both pilots carry the `skill_discipline_migrated_v10_2` migration flag** so future edits are gated through `pmo-skill-editor`; initial authoring uses the NEW-skill path (not hook-gated).

## Implementation Sequence (D-C SINGLE — one Engineering chip at a time)

The #186 → #185 chain is strictly serial. There is no independent parallel work this release (#450 deferred).

1. **#186 — Phase 0 factory foundation** (FIRST; unblocks #185) — Stage-5 spec #1263 · Stage-6 sub-task #1264.
   - EDIT `release/skills/pmo-skill-refiner/SKILL.md` → add the **feeding-document consumption** workflow path as the pre-Interview input branch of Mode 2 (Create New), via **`pmo-skill-editor` discipline** (impact + coherence + regression — migrated skill). Bump `version:`.
   - CREATE `release/skills/pmo-skill-refiner/references/pmo-feeding-document-v02.md` (14 sections) — new reference under a migrated skill ⇒ routes through the same editor discipline.
   - CREATE 5 `operations/skills/_shared/*.md` files (`behavioral-markers`, `anti-pattern-catalog`, `five-model-variations`, `deployment-strategies`, `lifecycle-gates`) — durable shared dependency surface (every later role skill consumes them). Direct Write (not skills → not hook-gated).
   - EDIT `core/deploy/deploy.sh` Check 5(a) → add the fail-CLOSED `== _shared` exact-match skip + convention comment (refinement a).
   - REBUILD `packages/pmo-skill-refiner.skill` (Check 7 freshness) + run `pmo-skill-editor` regression (existing Interview/Create/Refine flows intact).
   - **Operator-local hand-off (Layer-2):** 2 pilot feeding docs (TPM + Program Coordinator) + L1 Pre-Build Validation report. A repo-bound spoke cannot produce these — see R5. Designed in #1263 as format + validation criteria; flagged operator-attached.

2. **#185 — Phase 1 pilots + format lock** (AFTER #186 — hard gate) — Stage-6 gated behind #186 Stage-6 build.
   - Build `operations/skills/pmo-technical-program-manager/SKILL.md` via `pmo-skill-refiner` consuming the TPM feeding doc (composes `pmo-technical-analyst` + `delivery-engine`). NEW skill — Create-New path (not hook-gated). Carries the migration flag (refinement c).
   - Build `operations/skills/pmo-program-coordinator/SKILL.md` via `pmo-skill-refiner` consuming the Coordinator feeding doc (composes `tracker-manager` + `daily-status`). Carries the migration flag (refinement c).
   - 11/11 verification each; trigger-deconfliction between the 2 pilots; ≥3 failure modes + reversibility labels each.
   - **L2 Full-tier eval (5+5 per skill, ≥90%)** — operator-local eval run (Layer-2 hand-off; R5).
   - **FEEDING-DOCUMENT FORMAT LOCK:** after CS-15 sign-off (refinement b), add the "format locked — post-Phase-1 changes require a governance update" note to `release/skills/pmo-skill-refiner/references/pmo-feeding-document-v02.md` (routes through `pmo-skill-editor`); capture calibration decisions operator-local (Skills-Map CARRY-FORWARD).
   - Register pilots in `core/deploy/deploy.sh` `OPERATIONS_SKILLS`; `version:` frontmatter; rebuild both `.skill` packages.

## File Change Matrix (machine-readable — one path per line)

```
EDIT   release/skills/pmo-skill-refiner/SKILL.md                                    #186 (consumption path) + #185 (format-lock reference-table touch)
ADD    release/skills/pmo-skill-refiner/references/pmo-feeding-document-v02.md       #186 create (14 sections) — #185 appends the lock note
ADD    operations/skills/_shared/behavioral-markers.md                              #186
ADD    operations/skills/_shared/anti-pattern-catalog.md                            #186
ADD    operations/skills/_shared/five-model-variations.md                           #186
ADD    operations/skills/_shared/deployment-strategies.md                           #186
ADD    operations/skills/_shared/lifecycle-gates.md                                 #186
ADD    operations/skills/pmo-technical-program-manager/SKILL.md                     #185 (NEW skill via refiner)
ADD    operations/skills/pmo-program-coordinator/SKILL.md                           #185 (NEW skill via refiner)
EDIT   core/deploy/deploy.sh                                                        #186 (Check 5(a) == _shared skip) + #185 (OPERATIONS_SKILLS +2 pilots)
EDIT   packages/pmo-skill-refiner.skill                                             #186 (rebuild — Check 7)
ADD    packages/pmo-technical-program-manager.skill                                 #185 (build)
ADD    packages/pmo-program-coordinator.skill                                       #185 (build)
```

**No deletions.** **Layer-2 artifacts NOT in the repo matrix** (operator-local; out of repo scope): 2 pilot feeding docs (TPM + Coordinator), L1 Pre-Build Validation report, L2 Full-tier eval reports, Skills-Map CARRY-FORWARD calibration capture. These are tracked as **operator hand-off obligations** (R5), not repo files.

**Registration tax (every new skill):** each #185 pilot requires (1) `version:` frontmatter (Check 6), (2) `.skill` package rebuild (Check 7), (3) roster registration in `OPERATIONS_SKILLS` (Check 5 roster-drift), and (4) skill-registry entry per `skill-deployment.md`. `pmo-skill-refiner` (#186) is already rostered — it needs only the package rebuild + version bump on material edit.

## Contention Map

| Shared surface | #186 | #185 | Contention class | Resolution |
|---|---|---|---|---|
| `release/skills/pmo-skill-refiner/SKILL.md` | EDIT (consumption path) | EDIT (reference-table row for the lock note may touch) | Sequential (same file) | #186 edits first; #185's touch lands after. The hard dep #186→#185 already serializes this. |
| `release/skills/pmo-skill-refiner/references/pmo-feeding-document-v02.md` | CREATE | EDIT (format-lock note) | Sequential (create-then-edit) | #186 creates; #185 appends the lock note after CS-15 sign-off. |
| `operations/skills/_shared/*.md` (5 files) | CREATE | consumes (read-only, runtime) | No write contention | #186 sole writer; #185 is a downstream consumer. |
| `core/deploy/deploy.sh` | EDIT (Check 5(a) skip) | EDIT (`OPERATIONS_SKILLS` +2 pilots) | EDIT-EDIT, disjoint regions | Git-serialized under SINGLE. #186's Check-5 guard and #185's array entries are disjoint; the second chip rebases. |
| `packages/*.skill` | refiner rebuild | 2 pilot packages | No contention | Each skill owns its own `.skill` artifact. |

**Contention summary:** Everything is intra-chain (#186→#185, serialized by the hard dep). **No within-release file collision requires a scope split.** The deploy.sh edit is the reason Stage 6 routes ONE chip at a time.

**Cross-PR overlap audit — baseline pin:** Audit-start baseline SHA = **`629c783`** (HEAD; `git describe` → `v2.01-2-g629c783`), recent-window = last 10 merged PRs + all open PRs at this SHA. **At this baseline the open-PR population is observably empty for the in-scope paths** (`release/skills/pmo-skill-refiner/`, `operations/skills/_shared/`, `core/deploy/deploy.sh`, `operations/skills/pmo-*`). Per audit-baseline discipline, this default-to-no-cross-PR-contention is **pinned to SHA `629c783` + the last-10-merged-PR window** and MUST be re-checked at Stage 9 Phase A6.5 (PRIMARY, HALT-eligible) and at Stage 7/8 entry (warn-only).

## Risk Register

| ID | Risk | Severity | Owner | Mitigation | Reversibility |
|---|---|---|---|---|---|
| **R2** | **Hard-dependency stall #186 → #185.** If #186 slips or its `_shared/` surface / feeding-doc format is wrong, #185 cannot start (format-lock gate) and the pilots block. | MEDIUM | Hub (routing) + #186 Engineering spoke | Strict sequencing already encoded (Implementation Sequence step 1 before step 2). #186 `[ASSUMPTION – CONFIRM]` on v0.2 drift resolved at Stage 5 (#1263 R7): format is current and lockable; the only correction is the CS range-label (R3). | CHEAP pre-build; the chain is re-runnable. |
| **R3** | **Skill-edit-discipline bypass on #186.** #186 EDITS `pmo-skill-refiner` (migrated skill — `skill_discipline_migrated_v10_2: true`). A direct Write/Edit to its SKILL.md or `references/*.md` is the editor-gated path; a spoke that hand-edits bypasses impact/coherence/regression discipline. | MEDIUM | #186 Engineering spoke | Route ALL #186 edits to `pmo-skill-refiner` through **`pmo-skill-editor` discipline** (impact + coherence + regression — documented in the Stage-6 output). The NEW skills (#185 pilots) are NOT hook-gated — Create-New / direct authoring path. | CHEAP (process control). |
| **R4** | **deploy.sh registration drift (#185).** A NEW skill directory without its `OPERATIONS_SKILLS` entry fails `deploy.sh --check` Check 5; a missing `version:` fails Check 6; a stale package fails Check 7. | MEDIUM | #185 Engineering spoke | Each new-skill Engineering spoke does its OWN full registration (array entry + `version:` + package rebuild) in the same commit, then `deploy.sh --check` before close. | CHEAP (re-runnable; check is the safety net). |
| **R5** | **Layer-2 verification boundary.** #186 completion-condition 4 (2 pilot feeding docs + L1 report) and #185 L1/L2 eval ACs resolve to operator-local Layer-2 artifacts (git-ignored Skills-Map staging). A repo-bound spoke CANNOT produce or attach them. | MEDIUM | Operator (hand-off) + Hub (surfaces the gap) | The plan explicitly carves out the Layer-2 obligations. Repo-bound spokes produce repo files + repo-observable verification (`deploy.sh --check`, package rebuild, `pmo-skill-editor` regression) and **flag feeding-doc completion / L1 / L2 eval as operator-attached evidence**. DT/QA spokes verify the *presence* of the operator-attached evidence, not its production. | N/A (boundary discipline). |
| **R6** | **Trigger collision across new skills.** 2 new pilot skills (TPM, Program Coordinator). False-positive cross-triggers degrade routing. | LOW–MEDIUM | Engineering/DT spokes | Each pilot's AC mandates trigger-deconfliction / cross-skill false-positive detection (`pmo-skill-refiner` harness). The autonomy/complexity-extreme pairing (TPM vs Coordinator) is deliberately divergent to minimize collision. Run at DT (Stage 7). | CHEAP (description/trigger tuning is editable). |
| **R7** | **Rollback complexity (release-level).** 2 skills + a shared `_shared/` surface + deploy.sh edits land together. | LOW | Operator (Stage 12/13) | Under D-C SINGLE the entire release is one PR — rollback = `git revert -m 1` of the merge commit (CHEAP; git-native, no data migration, no schema change). The `_shared/` surface is additive (new files, no prior consumer) — removing them reverts cleanly. | CHEAP release-level. |

**Rollback strategy (consolidated):** D-C SINGLE ⇒ one release branch, one PR. Rollback = `git revert` of the merge commit. No data migration, no schema change, no Document-Tier-1 governance-file mutation (deploy.sh is infra, not governance). Per-skill partial rollback: remove the offending skill's `deploy.sh` array entry + directory + `.skill` package, re-run `deploy.sh --check`. **Reversibility tier: CHEAP** (undo in hours; git-native) / Confidence HIGH.

## Operator Decisions (D-gates) — rendered

| D-Gate | Decision | Reversibility / Confidence |
|---|---|---|
| **D-ReleaseClass** | `novel` (new reference docs + new skills; ≥1 D-class decision). Differentiation: Standard engagement / Deep Stage-9 / Stage-5 bias ALL / 30-day window. | CHEAP / HIGH |
| **D-C (topology)** | **D-C SINGLE** — one `release/v2.02-*` branch, sequential Engineering commits, one release PR. The plan file commits as Engineering Commit 0. | CHEAP / HIGH |
| **D-186-SharedHome** (Stage 5) | Keep `operations/skills/_shared/`; add a fail-CLOSED `== _shared` exact-match skip to `deploy.sh` Check 5(a) (refinement a — NOT the `_*` wildcard the Stage-5 spec first proposed). | CHEAP / HIGH |
| **D-186-ConsumptionShape** (Stage 5) | Consumption path as a pre-Interview input branch of Mode 2 (Create New), rejoining the existing pipeline at the eval step; route the SKILL.md edit through `pmo-skill-editor`. | MODERATE / HIGH |
| **D-186-FieldSet** (Stage 5) | Consumption path implements the **12 build-time CS changes {CS-01..CS-11, CS-13}**; CS-15 deferred to #185 Phase-1 calibration; cite the precise set (not the imprecise contiguous "CS-01…CS-13" range label). | CHEAP / HIGH |
| **D-450-Composition** | **DEFERRED with #450 to milestone 04** (`04-ROLE-delivery-coverage`) — compose-the-mode seam converges with a stub until `pmo-principal-engineer` (#1116) ships. | MODERATE / HIGH |

## Discoveries outside scope (noted, NOT added to the Milestone)

1. **`operations/skills/_shared/` becomes a platform-wide shared dependency surface** with no governing doc yet. Once #186 creates it, every future role skill consumes it, and a later change ripples to all consumers. Recommend (follow-up): a `_shared/`-surface governance note (placement + change-discipline + which skills consume which file), analogous to the `TEMPLATE_SYNC_MAP` discipline for templates. Out of scope for this release — flagged.
2. **`pmo-principal-engineer` is a phantom composition target across the role-skill suite.** Any future Specialist whose skill-matrix row says "composes `pmo-principal-engineer`" faces the same gap (#297 built a *mode*, not a deployable skill). The durable fix (build `pmo-principal-engineer` as a deployable composable Specialist, or formally rule it stays a Stage-5 mode) is tracked at #1116 in milestone 04. Out of scope here — flagged.

---

## Change Description

### Outcome

v2.03 stands up the **role-Specialist factory** and its first pilots. Phase 0 (#186) wires a feeding-document consumption path into `pmo-skill-refiner` so a filled v0.2 feeding document can drive skill generation without the interactive Interview, authors the 5 durable `operations/skills/_shared/` reference files every role skill consumes, and adds a fail-closed `deploy.sh` guard so the new shared directory does not trip roster-drift. Phase 1 (#185) builds the two pilot skills (TPM + Program Coordinator) from that format and locks the feeding-document format.

### Issues resolved

| # | Outcome | Status |
|---|---|---|
| #186 | Phase 0 factory foundation — feeding-document consumption path in `pmo-skill-refiner`, 14-section `pmo-feeding-document-v02.md`, 5 `_shared/` reference files, fail-closed Check 5(a) `_shared` skip; package rebuilt. | DONE (this Engineering spoke) |
| #185 | Phase 1 pilots (TPM + Program Coordinator) + feeding-document format lock. | PENDING — Stage 6 gated behind #186's foundation (separate Engineering chip on this branch). |

### Key decisions

- **D-186-SharedHome:** keep `operations/skills/_shared/`, add a fail-CLOSED `== _shared` exact-match skip to `deploy.sh` Check 5(a) (Collective-Review refinement a — exact match, not a `_*` wildcard, so a stray `_`-prefixed dir still fails roster-drift unless explicitly allowlisted). Rationale: honors the #186/#284-specified literal path while preserving the gate's fail-closed property.
- **D-186-ConsumptionShape:** the consumption path is a pre-Interview input branch of Mode 2 (Create New), reusing the proven Create-New spine downstream of the seed. Rationale: matches #284 design (bypass Capture Intent + Interview) and composes-not-absorbs per skill-pipeline-alignment DT-3.
- **D-186-FieldSet:** the consumption path implements the precise build-time CS set {CS-01..CS-11, CS-13} (12 changes); CS-15 is Phase-1-deferred. Rationale: corrects the ticket's imprecise contiguous "CS-01…CS-13" label against the canonical epic table before #185 locks the format.

### Reversibility

CHEAP / HIGH — under D-C SINGLE the whole release is one PR; rollback is a single `git revert -m 1` of the merge commit. The 5 `_shared/` files and the feeding-doc reference are purely additive (no prior consumer); the SKILL.md section and the deploy.sh `_shared` skip revert cleanly. No data migration, no schema change.

### Downstream impact

- Unblocks **#185** (pilots) on this branch — the format-lock gate that protects the feeding-document format before scale-up.
- The 5 `operations/skills/_shared/` files become a **platform-wide dependency surface** every future role skill (Wave-2/3/4 builds, #181 capstone audit) consumes — authored for longevity (structural contracts, not point-in-time counts).
- Carry-forward (next-release issues, flagged not added): a `_shared/`-surface governance note; building `pmo-principal-engineer` as a deployable skill (#1116, milestone 04, which also unblocks the deferred #450 `pmo-architect`).

### Cross-references

- Release plan top: this file (`release/releases/plans/v2.03_RELEASE_PLAN.md`).
- Milestone: `03-ROLE-factory-and-pilots`.
- Stage-5 spec (implementation blueprint): #1263. Stage-4 plan + Collective Review scope-lock: #1262.
- User-facing release notes: authored at Stage 13 (`release/releases/notes/v2.03_RELEASE_NOTES.md`).
