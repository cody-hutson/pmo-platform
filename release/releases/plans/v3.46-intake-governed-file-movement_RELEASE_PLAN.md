# v3.46 Release Plan — 08-INTAKE-governed-file-movement

> Durable release-plan record for milestone `08-INTAKE-governed-file-movement` (milestone #211, Release Class `routine`). Transcribes the operator-approved Stage-4 plan (sub-task #2838) as the canonical on-branch record, written as Engineering Commit 0 by the first Engineering spoke. **Provisional version re-versioned `v3.45 → v3.46`** per the recorded D-Version determination on #2838: a concurrent release (`42-pipeline-skill-doc-reconciliation`) claimed and shipped v3.45 while this run was in Stage 5, so this milestone advanced to the next free slot per bundle-composition-doctrine §5.3 / §9.1. Binds atomically at Stage 12 (anchor = published v3.45; next-free = v3.46 — verified free: no v3.46 tag, no v3.46 GitHub release, frontier max = v3.45).

---

## Stage 4 Release Planning — 08-INTAKE-governed-file-movement

> Provisional version **v3.46** (re-versioned from v3.45 after a sibling release claimed v3.45; recorded determination per D-Version rule, binds at Stage 12 — not gated here). Working reference: single-branch / D-C SINGLE topology.

### §1 Release Plan

- **Provisional version:** `v3.46` [RECOMMENDED] — anchor = mainline tag frontier `v3.45`; next-free = `v3.46` (re-versioned up from the original v3.45 provisional after the concurrent `42-pipeline-skill-doc-reconciliation` release published v3.45 during this run's Stage 5). Binds atomically at Stage 12; re-versions forward on collision. Slug: `v3.46-intake-governed-file-movement`.
- **Release Class:** `routine` (two EXTEND-mode additions to existing operations-module skills via pmo-skill-editor; no new skill, no schema/governance change). Composition shape: capability-slice.
- **Size:** raw 12 pts (#240 S=2, #565 S=2, #289 M=4 after re-size) → effective 12 (routine weight = 1). Acceptable band (clear capability boundary).
- **Branch topology:** SINGLE release branch, one PR, one merge. Concurrency posture P0 fully-serial. Force-push prohibited on the shared branch.

### Dependency graph + sequence

- **Internal hard edges: ZERO.** #565↔#289 is a SOFT edge ("relates to") + a file-contention point, not a scheduling dependency. #240 fully independent (`layer:infrastructure`).
- **Critical path:** empty (no hard edges). The binding constraint is **file-contention**, not dependency.
- **Emitted order: `#240 → #565 → #289`** — #240 infrastructure-first/independent; #565 before #289 because both edit `intake-desk/SKILL.md` (smaller change first; #289 builds on #565's reconciled FM #7). Within each issue: Stage 5→6→7→8.

### File-contention map (the load-bearing sequencing fact)

| File | Issues | Contention | Rule |
|---|---|---|---|
| `operations/skills/intake-desk/SKILL.md` | #565 (FM #7 reconcile), #289 (new Mode C + ≥3 FMs) | BINARY | **#565 before #289** |
| `operations/skills/intake-desk/references/elicitation-loop.md` | #565 (Phase-2 scan); #289 (possible) | BINARY (potential) | #565 first; #289 confirms at S5 |
| `operations/skills/file-router/*` | #240 only | none | — |
| `CLAUDE.md` (`core/CLAUDE.md.template`) | #289 (auto-log rule → invoke Mode C); #240 (protocol prose ref) | light | different sections; serialize on branch |
| `.github/ISSUE_TEMPLATE/*.yml` | none (all author against existing forms) | none | Check 22 stays green |

### Per-issue stage plan (all three = skill-EXTEND via pmo-skill-editor: impact + coherence + regression)

- **#240** — S5 (4-direction rule design + promotion approval-gate alignment) · S6 (edit `file-router/SKILL.md` + `references/routing-patterns.md`) · S7 (regression: inbound 3-layer/confidence/queue) · S8 (pmo-qa-auditor). No skips.
- **#565** — S5 (hard-gate-vs-advisory + container-altitude scoping) · S6 (Phase-2 scan step in `elicitation-loop.md` + reconcile FM #7 to name initiative/container-duplication; cross-ref CLAUDE.md duplicate-discipline) · S7 (regression: 4-phase loop + type-landing + other 8 FMs) · S8 (G7: no redundant 10th FM). No skips.
- **#289** — S5 **MANDATORY + heaviest: crisping FIRST** (re-author the stale "new skill" body → the Mode-C scope below) then the residual design · S6 (EXTEND intake-desk w/ Mode C; update CLAUDE.md auto-log rule; ≥3 ambient FMs) · S7 (regression: existing 2 modes + 9 FMs + output-contract emit) · S8 (G7 + G4). No skips.

### §2 #289 Residual scope (Stage-5 crisping target)

**Re-frame:** "Build dedicated intake-logger skill" → **"EXTEND `intake-desk` with an ambient auto-log mode (non-interactive detect→create) governed by `automation_level`."**

- Covered (close as subsumed): `author`/`validate`/`create` (intake-desk front door) + `classify` (#286).
- **Residual (build):** ONE new **Mode C — Ambient Auto-Log** = the non-interactive entry point (invoked by #322's ambient path) + confirm-gate-bypass under `automation_level` + non-interactive self-repair. Reuses Mode-A author/create machinery.
- Proposed ACs: (1) SKILL.md gains Mode C, non-interactive; (2) reads `automation_level` as a ceiling (off/recommend/bounded_auto); (3) reuses the output-contract create path (no parallel schema); (4) non-interactive self-repair → re-author once then downgrade to observation; (5) Tier-0 set (governance/financial/security/RAID-close) NEVER auto-created regardless of level — reconciles FM line 239; (6) CLAUDE.md auto-logging rule invokes Mode C + ≥3 ambient FMs.
- Affected: `intake-desk/SKILL.md`, `intake-desk/references/output-contract.md` (likely), `CLAUDE.md`. Does NOT create `operations/skills/intake-logger/`.

### §3 Risks

1. #289 residual thin / ahead-of-demand (MODERATE·MED) — no named ambient consumer yet.
2. intake-desk file-contention (CHEAP·HIGH) — mitigated by #565→#289 order.
3. #289 body stale (CHEAP·HIGH) — mandatory S5 crisping before S6.
4. Bundle undersize (CHEAP·MED) — acceptable per doctrine.

**Plan reversibility:** MODERATE (branches/sub-tasks, git-revertable through Stage 9). No irreversible action before the Stage-12 tag.

### Deferred scaffolding

Release-level tail sub-tasks (Stage 9 Plan Review / 10 Dry Run / 11 Snapshot / 12 Execute / 13 Close) are created as single release-level sub-tasks when the release reaches Stage 9 (blocked until all Stage 5-8 close; "never chip an unmet dependency"). Stages 10/11 are PLATFORM-SATISFIED (PR diff = dry run; git history = snapshot).

---

## §240 Stage-5 disposition carried into build (CDF-240-1)

The #240 Stage-5 spec (sub-task #2839) selected the **unified 4-direction routing model** (D-1 Option A) and **govern-and-delegate** promotion scope (D-2 Option B). The Collective Review (2026-07-01) returned **PROCEED-WITH-CONDITIONS** with **CDF-240-1**:

- file-router **resolves the promotion target** and **CITES** `operations/skills/artifact-generator/SKILL.md`'s existing PROMOTE / REVISE / REJECT gate (Actions-available block + Promotion Workflow, ~lines 380-385) — it does **not** restate or duplicate it, and does **not** edit `artifact-generator/SKILL.md`.
- file-router owns a **NEW approval gate ONLY for cross-project-out** (the one greenfield resolver).
- The gate is expressed as **two gate-types**: a **confidence-threshold** gate for the inbound-family (#1 inbound + #4 cross-project — both Layer-2 scored) and a **flat-approval** gate for the Domain-C family (#2 generated-staging + #3 promotion — target pre-stamped, no confidence variable).
- **Citation discipline:** the `promotion_state` field is **live** (cited from `core/schemas/frontmatter-schema.md § Domain C`); `core/artifact-workflow-protocol.md §4` is treated as **Stage-6-current** (its own `Status: Stage 6 Engineering`), not shipped governance.

This disposition is the binding build contract for the #240 Engineering spoke; the SKILL.md / routing-patterns edits implement it exactly (no re-design).
