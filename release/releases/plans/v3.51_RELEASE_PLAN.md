# v3.51 Release Plan — 100-adr-and-frontmatter-schema (routine)

> Release-scoped Stage 4 plan, committed as Engineering Commit 0 on the release branch `release/v3.51-100-adr-and-frontmatter-schema` (D-C SINGLE topology). Version **v3.51** (rule-computed next-free off v3.50; recorded D-Version determination — binds at Stage 12; re-verified free at Commit 0). Base: `1b5bd70` (origin/main HEAD, post-v3.50). All Mode R premises re-verified; no divergence found. Source: Stage 4 planning comment on #2930 (version corrected v3.49→v3.51 and base re-anchored 1fa6293→1b5bd70 at Commit 0).

## Summary (30 seconds)

Five open issues, one coherent capability: **canonicalize the ADR frontmatter+body-section schema and align every consumer to it**. Build order is a dependency chain, not a flat set — **#2156 authors the canonical ADR schema doc** (foundation, has a *waiting consumer*: `platform-doc-frontmatter-standard.md` §9 defers the ADR class to it by number), then **#2155** (README repoint + category miscount) and **#2157** (schema value refinements) align to it, then **#2068** (durability regex hardening — the only functional/tested change besides #1487), then **#1487** (a scoped depersonalization-gate carve-out — a **security-control change**, sequenced last). Topology **single-branch / single PR / single merge** confirmed appropriate (all files internal, tight capability coherence). Release Class **routine** — confirmed. Top risks: (1) the #2156→#2155-item-2 internal dependency (README repoint needs a real target first); (2) **cross-milestone README contention with #431** (`31-immutable-adr-system`, OPEN) — both milestones edit `core/ADRs/README.md` **and** `release/ADRs/README.md`; (3) #1487's PII-protection blast radius (a too-broad carve-out weakens the depersonalization gate). Quota Budget **PASS** (worst parallel wave = 2 spokes). Two structural design questions (#2156 doc home; #1487 field-scope-vs-file-scope) plus #2157's migration approach made **Stage 5 Solutioning mandatory for #2156, #2157, #1487** and recommended-light for #2068; #2155 is doc-only and skipped Stage 5.

## Dependency Graph

No native GitHub `blocked-by`/`blocks` or sub-issue (`trackedIn`/`tracks`) edges exist on any of the five — verified via GraphQL, all empty. Every edge below is **soft (aligns-to / consumes-definition)**, inferred from file+content coupling, not a hard blocker.

```
#2156 (canonical ADR schema doc — FOUNDATION)
  ├──(soft: repoint-needs-target)──▶ #2155  (item 2 repoints release/ADRs/README.md → #2156's new doc)
  └──(soft: parity/adjacent surface)─▶ #2157 (frontmatter-schema refinements; adjacent schema surface)

#2155 (frontmatter-schema category count + ADR-README dead link)
   (item 1 = frontmatter-schema.md category count — INDEPENDENT of #2156)
   (item 2 = release/ADRs/README.md dead pointer — soft-depends on #2156's doc existing)

#2157 (generated_by keep-decision + human-readable domain values)
   (soft-adjacent to #2156; independent build; touches frontmatter-schema.md like #2155 item 1)

#2068 (durability ISSUEREF_RE hex-color hardening)
   (INDEPENDENT — no edge to the schema docs; hook + fixtures + allowlist only)

#1487 (ADR deciders depersonalization carve-out — SECURITY CONTROL)
   (INDEPENDENT of the schema chain; enabler for a later normalization pass in #1488, NOT in this milestone)
```

**Edge classification (hard/soft):** every edge is **SOFT**. The single load-bearing soft edge is **#2156 ▶ #2155(item 2)**: #2155's dead-pointer fix has two valid resolutions — (a) repoint at #2156's *new* canonical doc, or (b) repoint at the existing `## Format` heading in `core/ADRs/README.md`. Building #2156 first makes (a) available and is the milestone's stated intent (Outcome Statement: "a single schema doc replaces the duplicated prose"). On one branch in the recommended wave order this edge is satisfied by construction — no cross-branch coordination needed.

## Implementation Sequence (waves)

Single release branch (`release/v3.51-100-adr-and-frontmatter-schema`), D-C SINGLE topology (default). Issues are delivery slices on **one branch, one PR, one merge**. Waves are *build order on that branch*, not parallel branches.

| Wave | Issue(s) | Why here | Build note |
|---|---|---|---|
| **W1 (foundation)** | **#2156** | Authors the canonical ADR schema doc that W2 aligns to; has a waiting consumer (`platform-doc-frontmatter-standard.md` §9). | Create the canonical doc at `core/schemas/adr-schema.md` (Stage-5 decided); repoint both ADR READMEs to cite it instead of restating fields; this also resolves #2155 item 2's target. |
| **W2 (align-to-canonical)** | **#2155**, **#2157** | Both align to / sit adjacent to the canonical surface from W1. #2155 item 2's repoint target now exists. | #2155: fix `frontmatter-schema.md:44` count + repoint `release/ADRs/README.md`. #2157: `frontmatter-schema.md` domain vocabulary + `generated_by` keep-decision. Both touch `frontmatter-schema.md` — sequence to avoid intra-branch churn (disjoint regions). |
| **W3 (functional/tested)** | **#2068** | Independent; hook regex hardening with a fixture harness. Land after the doc work to keep the hex-color allowlist removal isolated for CI verification. | Harden `ISSUEREF_RE` (trailing word-boundary excluding `[0-9A-Fa-f]`); then remove the `process-flow-diagram-standards.md` allowlist stopgap; re-run `run-fragile-ref-fixtures.sh`. |
| **W4 (security control)** | **#1487** | Sequenced last: modifies a required CI gate (depersonalization). Isolating it as the final slice keeps the security-control diff reviewable on its own and lets Stage 9 review it with full attention. | Add scoped carve-out to `depersonalization-spec.md` + `git-workflow.md`; field-scope the scanner in `.github/workflows/repo-integrity.yml`. |

Rationale: canonical schema first (W1) → consumers align (W2) → independent functional fix (W3) → security-control change isolated last (W4). W3/W4 order is by *review-isolation* preference, not a hard dependency.

## Stage Applicability Matrix

Default = all stages 5–13 apply. Justify every skip. Stages 1–3 already complete (all issues `status: bundled`). Stages 10/11/12/13 are **release-level** (run once for the whole PR), not per-issue — marked ✓(release) below.

| Issue | S5 Solutioning | S6 Eng | S7 DevTest | S8 QA | S9 Review | S10–13 | Notes |
|---|---|---|---|---|---|---|---|
| **#2156** | ✓ **REQUIRED** | ✓ | ✓ | ✓ | ✓ | ✓(release) | S5 done (#2984 closed): doc home resolved to `core/schemas/adr-schema.md`; #431 seam ratified; register surface = `core/schemas/README.md § Index`. S7/S8: doc-only but grep-verifiable ACs (grep each field + section name). |
| **#2155** | ⃠ **SKIP** | ✓ | ✓(light) | ✓(light) | ✓ | ✓(release) | Pure documentation-drift fix, two deterministic edits, no structural choice → skip S5. S7/S8 reduce to grep assertions (count == 7 headings; pointer resolves). |
| **#2157** | ✓ **REQUIRED** | ✓ | ✓(light) | ✓(light) | ✓ | ✓(release) | S5 done (#2992 closed): migration/alias design for `domain: A/B/C` → human-readable; `generated_by` narrowed to record the keep-decision citing the shipped dual-semantics note. |
| **#2068** | ⃠ **SKIP (light)** | ✓ | ✓ **REQUIRED** | ✓ | ✓ | ✓(release) | Regex fix has one correct approach (trailing word-boundary) → light design note (#2996 closed). **S7 load-bearing**: functional hook change with a fixture harness — add a hex-color fixture (`#28A745` must NOT match) + confirm real issue-refs still match. |
| **#1487** | ✓ **REQUIRED** | ✓ | ✓ **REQUIRED** | ✓ | ✓ **deep** | ✓(release) | S5 done (#3000 closed): field-scope vs file-scope of the scanner bounded. **S7 load-bearing**: gate passes on `deciders:` line w/ literal name; gate still blocks the name outside `deciders:`. **S9 deep**: security-control change to a required CI gate. |

**S5 activation summary:** 3 of 5 needed Solutioning (#2156, #2157, #1487); 2 skipped (#2155 trivial; #2068 light design note). Stage-5 activation bias **moderate**.

**S7/S8 functional load:** #2068 and #1487 are the true functional/tested slices (hook + CI-gate behavior, both with fixtures). #2156/#2155/#2157 are doc changes whose ACs are grep-verifiable.

## Contention Map

**Within-release file contention** (multiple issues touch the same file — sequence to resolve):

| File | Issues | Overlap class | Resolution |
|---|---|---|---|
| `core/ADRs/README.md` | #2156 (repoint to canonical doc) | single-writer in-release | No in-release contention; collides cross-milestone with #431 (below). |
| `release/ADRs/README.md` | #2156 (repoint), #2155 (item 2 repoint dead pointer) | line-range-overlap (`## Format` region + footer xref) | **Sequence W1→W2 on one branch** — #2156 repoints first, #2155 item 2 verifies the pointer resolves. Same-branch serial = no merge conflict. |
| `core/schemas/frontmatter-schema.md` | #2155 (item 1, count line), #2157 (domain, generated_by) | line-range-overlap, **disjoint regions** | Non-overlapping line ranges; land in W2 as one logical pass. |

**Cross-milestone contention — #431 in `31-immutable-adr-system` (OPEN):** #431 edits the *when-to-write* region of both ADR READMEs; this milestone (#2156/#2155) edits the *## Format* region of both. **Disjoint semantic regions** → same-file textual conflict possible-but-small. **Serialization:** whichever milestone reaches Stage 12 first merges; the second re-baselines its README hunk. The real coordination need — *don't let both define supersession* — is resolved by the Stage-5 #431 seam ruling (guide owns policy; schema owns the data contract). Recorded in the Risk Register (R2) and the Parallelization Map.

## File Change Matrix

Per-file add/edit/delete intent across the five issues. Paths verified present at base `1b5bd70` unless marked NEW.

| File | Issue | Intent | What |
|---|---|---|---|
| `core/schemas/adr-schema.md` | #2156 | **ADD** (NEW) | Canonical ADR frontmatter (7 fields) + body-section contract (6) + supersession-representation convention. Home = `core/schemas/` (Stage-5 decided). Registered in `core/schemas/README.md § Index`. |
| `core/schemas/README.md` | #2156 | EDIT | Add one Index-table row for `adr-schema.md` (sorts first, above `agent-processing-contracts.md`). |
| `core/ADRs/README.md` | #2156 | EDIT | Replace restated §Format field list with a citation of the canonical doc. |
| `release/ADRs/README.md` | #2156, #2155 | EDIT | #2156: cite canonical doc in §Format + footer xref. #2155 item 2: repoint the dead "§ Canonical ADR Format" pointer to the canonical doc. |
| `core/schemas/frontmatter-schema.md` | #2155, #2157 | EDIT | #2155 item 1: fix "Six categories." count to 7. #2157: human-readable `domain` values + recorded `generated_by` keep-decision. |
| `core/hooks/block-fragile-refs.sh` | #2068 | EDIT | Harden `ISSUEREF_RE` with trailing word-boundary excluding `[0-9A-Fa-f]`. |
| `core/hooks/lib/positional-issueref.awk` | #2068 | EDIT (verify) | Confirm regex parity; edit only if the awk carries its own copy. |
| `core/hooks/reference-durability-allowlist.txt` | #2068 | **DELETE (line)** | Remove the `process-flow-diagram-standards.md` stopgap entry once the regex fix lands. |
| `core/hooks/run-fragile-ref-fixtures.sh` | #2068 | EDIT (add fixture) | Add hex-color fixture (`#28A745`→ must NOT flag) + regression fixture (real `#NNNN` still flags). |
| `core/standards/depersonalization-spec.md` | #1487 | EDIT | Add scoped carve-out: operator literal name permitted only in ADR `deciders:`; all other fields/emails/paths/handles stay blocked. |
| `core/rules/git-workflow.md` | #1487 | EDIT | Document the carve-out in § Repository-Integrity Gates. |
| `.github/workflows/repo-integrity.yml` (+ its scanner) | #1487 | EDIT | Field-scope the depersonalization block to exempt the ADR `deciders:` line. |

## Risk Register

Passive-voice-free; each names risk, owner, mitigation, reversibility.

| ID | Risk | Sev | Owner | Mitigation | Reversibility |
|---|---|---|---|---|---|
| R1 | **#2156→#2155(item2) internal dependency**: #2155's README repoint needs a real target. If #2155 lands before #2156, the "repoint at canonical doc" resolution is unavailable and #2155 falls back to a weaker fix. | Med | Stage 6 (build order) | Build **W1 (#2156) before W2 (#2155)** on the one branch. Satisfied by construction on a single branch. | CHEAP (re-edit one pointer line) |
| R2 | **Cross-milestone #431 README contention**: #431 (`31-immutable-adr-system`, OPEN) edits both ADR READMEs; this milestone edits both too. Whichever merges second must re-baseline; risk of both defining supersession. | Med | Stage 5 (seam) + Stage 12 (merge order) | Stage-5 #431 seam ruling: #431 owns when/how/lifecycle-policy; #2156 owns the field/body-section data contract; #431's template *references* #2156's schema. At Stage 12, second-to-merge re-baselines its README hunk (disjoint regions keep conflict small). | MODERATE (re-baseline README edits) |
| R3 | **#1487 security-gate blast radius**: a too-broad depersonalization carve-out weakens PII protection on a required CI gate. Over-scoping (file-scope instead of field-scope) would exempt whole files. | **High** | Stage 5 (design) + Stage 9 (deep review) | Scope to the **single field, single name** (ADR `deciders:` only). Stage 7 verifies BOTH directions. Stage 9 reviews the security-control diff with heightened depth. | MODERATE→EXPENSIVE (a merged over-broad carve-out is a PII-protection regression on a public repo) |
| R4 | **#2157 domain-value migration**: changing `domain: A/B/C` to human-readable values is a data migration; existing artifacts carry A/B/C and multiple consumers read the field. A rename without an alias/forward-only path breaks readers. | Med | Stage 5 (blast-radius map) | Stage 5 mapped the consumer blast radius + specified forward-only + alias (parallel the shipped `synthesis_scope→source_inputs` alias). | MODERATE (alias makes it forward-compatible) |
| R5 | **Intra-file churn on `frontmatter-schema.md`** (#2155 item 1 + #2157 both edit it). | Low | Stage 6 | Disjoint line regions; land as one logical W2 pass. | CHEAP |
| R6 | **`generated_by` re-scope churn**: #2157(a) touches a field the dual-semantics note *just* clarified. Re-opening it risks re-litigating a settled rationale. | Low | Stage 5 | Narrowed to *record the keep-decision citing the shipped note* — not a fresh redundancy analysis. Hold to that scope. | CHEAP |
| R7 | **Scanner-surface uncertainty (#1487)**: exact file(s) implementing the depersonalization scan + whether it can field-scope. | Med | Stage 5 | Stage 5 confirmed the scanner surface + field-scoping capability before Engineering; fallback = an allow-comment sentinel on the `deciders:` line. | MODERATE |

No dependency **cycles** (verified). No **hard** cross-milestone leaks (only the soft #431 README contention). No rollback-ordering hazard within the single PR (one merge).

## Quota Budget

**Verdict:** PASS (per `quota-budget-protocol.md` Checkpoint A). Single-branch release, so per-stage spokes are the parallel unit. Worst realistic parallel wave = **2 concurrent spokes**. Even at the upper bound (Stage 7 with 5 spokes), only 2 carry functional weight (#2068, #1487). Per-spoke cost band — all five issues are milestone-sized `M`. Estimated cumulative draw % (worst parallel batch) ≈ **<50%** → PASS. Checkpoint B (hub per-wave, runtime) is the load-bearing gate.

## Rollback Strategy

Single PR / single merge (D-C SINGLE) → the whole release reverts as **one `git revert` of the merge commit** (CHEAP, standing operator-authorized per RELEASE_PROTOCOL.md § Rollback). Per-change reversibility:

| Change | Revert mechanism | Tier |
|---|---|---|
| #2156 new doc + README citations | Delete the NEW doc; restore the restated §Format field lists in both READMEs (git history is the snapshot). | CHEAP |
| #2155 count + pointer edits | Re-edit 2 lines. | CHEAP |
| #2157 domain vocabulary + generated_by note | Revert the value vocabulary; the alias makes rollback forward-compatible. | CHEAP→MODERATE |
| #2068 regex + allowlist removal | Restore the prior `ISSUEREF_RE` + re-add the allowlist stopgap line; re-run fixtures. | CHEAP |
| **#1487 security-gate carve-out** | Remove the carve-out clause + un-field-scope the scanner. Text/config revert is CHEAP; the risk is the exposure window while a too-broad carve-out is live. Prefer catching at Stage 9. | MODERATE (decision) / CHEAP (diff) |

## Recommendations

**Topology:** **Single-branch / single PR / single merge.** All target files are internal pmo-platform artifacts; the five issues form one capability slice. Agrees with the "milestone ships as one PR" invariant.

**Merge/split:** **No merges, no splits.** #2156 and #2155-item-2 are tightly coupled but correctly separate issues (distinct ACs) — sequence them (W1→W2), do NOT merge. #1487 is correctly a standalone slice (isolate for review).

**Release Class — `routine`.** Bounded to the ADR corpus + `frontmatter-schema.md` + the durability hook + one CI-gate carve-out. Nuance: #1487 is a security-control change → heightened Stage 9 depth on that slice; Stage-5 activation bias moderate.

**#431 cross-milestone boundary (record + coordinate, do NOT relocate):** the seam (guide owns when/how/policy; schema owns the field/body-section contract; guide references schema) belongs in the Stage 5 design of #2156, and the serialization edge belongs in the Parallelization Map. **Do not merge #431 into this milestone** — different capability, different milestone.

**Divergence from Mode R cache:** **None** at Stage 4. All premises re-verified. Native dep edges: **zero**. #2067 correctly trimmed (CLOSED, shipped v1.04 #540).

---

## Approved Modifications & Stage-5 Outcomes

Recorded at Engineering Commit 0 (v3.51). Captures the deltas from the Stage-4 plan established during Stage-5 Solutioning + hub review.

- **Version + base re-anchor.** Provisional version corrected v3.49→**v3.51** (rule-computed next-free off v3.50; re-verified `gh release view v3.51` = 404 at Commit 0). Base re-anchored from the Stage-4 `1fa6293` (v3.48 HEAD) to **origin/main `1b5bd70`** (post-v3.50 HEAD); release branch cut off fresh `origin/main`.
- **#2068 Stage-5 = LIGHT.** Confirmed the single-approach regex hardening; a hub adversarial-review modification was adopted (LIGHT design note, no ADR). Carried into Stage 6.
- **#2157 `generated_by` = minimal inline keep-decision (NO ADR-067).** The `generated_by`/`created_by` reconciliation is recorded inline as a keep-decision citing the shipped dual-semantics note in `frontmatter-schema.md`; **no ADR opened** (below ADR threshold; already-made-in-kind).
- **#2156 register surface = `core/schemas/README.md § Index`** (NOT framework-catalog). Stage-5 corrected the Stage-4 "framework-catalog/xref" assumption: the four sibling schema *contracts* are all absent from `framework-catalog.md`; the register surface schema contracts use is the `core/schemas/README.md` per-folder Index table. `adr-schema.md` sorts first (above `agent-processing-contracts.md`).
- **#2156 doc home = `core/schemas/adr-schema.md`** (Stage-5 D-1, CHEAP/HIGH). Chosen over `core/standards/adr-frontmatter-schema.md` for parity with the `type: schema` sibling contracts; the `type` axis, not the "frontmatter" topic, governs the directory. **No ADR** (below threshold, already-made-in-kind per `platform-doc-frontmatter-standard.md` §3 precedent).
- **#2156 #431 seam (Stage-5 D-2, S2).** This doc owns the DATA CONTRACT (fields + body sections + supersession's *frontmatter representation*); #431 (`31-immutable-adr-system`) owns the POLICY (when-to-write rubric, template, supersede-not-edit/immutability *rule*). #431's template references this schema. Residual [ASSUMPTION – CONFIRM] at Collective Review: #431 adopts the reference direction rather than re-inlining the fields.
- **#2156 field-value convention (Stage-5 D-4).** `status`/`deciders` typed as **leading-enum-token + optional prose tail**, NOT strict closed enums — grounded in the 63-value ADR corpus (mirrors `platform-doc-frontmatter-standard.md` §10 reversibility treatment).

**Stage-5 design sub-tasks (closed):** #2984 (#2156) · #2992 (#2157) · #2996 (#2068) · #3000 (#1487).
