<!-- repo-integrity: allow-issue-ref -->
<!-- reference-durability: allow-link -->
# v3.54 Release Plan — 86-methodology-pack-foundation

> Durable release-plan record for milestone `86-methodology-pack-foundation` (#228, Release Class **`cross-cutting`** — Deep Stage 9). Transcribes the operator-approved Stage-4 plan (sub-task #3010) UPDATED for the Stage-5 Collective-Review **SCOPE-LOCK** (#3010 CONCLUDED comment): the type-pack meta-schema widen is **absorbed into #1968**, the milestone stays 3 issues, and the build order re-sequences **#1967 → #1968 (widen + scaffold) → #1970 (labels)**. Version **v3.54** provisional (recorded determination; binds atomically at Stage 12 per the repo versioning lineage — anchor = published `v3.53`). Wave-0 foundation of the methodology-packs track, shipped as **one branch / one PR / one merge** (D-C SINGLE).

---

## Version determination (recorded — not gated)

- **Provisional version: v3.54.** Anchor = published `v3.53` (latest GitHub Release / highest tag). The four next slots below the anchor (`v3.50`–`v3.53`) are all claimed by concurrent releases that shipped during this release's design phase; the rule-determined next-free (anchor + 1) is **v3.54**. Re-versioned up from the original provisional `v3.50` per `bundle-composition-doctrine` §5.3 / §9.1 (forward-only; version gaps are permanent + benign) — a rule-determined call, not an operator gate. Re-verified next-free at Engineering Commit 0 (this file): `git fetch --tags origin` → `anchor()` = `v3.53`, `claimed_set()` = {v3.48…v3.53}, recomputed next-free = **v3.54**, not in the claimed set → PROCEED. Binds atomically at the Stage-12 tag; the Stage-12 pre-merge freeness check re-confirms.
- **⚠️ Version-slot contention (surfaced to hub/operator — NOT a HALT).** A concurrent in-flight release branch `release/v3.54-frontier-system-business-specialists` (milestone `09-FRONTIER-system-business-specialists`, #407) also provisionally targets **v3.54** (Engineering Commit 0 + plan file present; last commit 2026-07-02; **not merged, no `v3.54` tag**). Both releases are provisional at the branch level; the version binds only at the Stage-12 tag (ADR-036 version-claim-determinism). Whichever release reaches Stage 12 first claims `v3.54`; the other forward-only bumps. This is the designed contention-resolution mechanism (handle-at-Stage-12), flagged early so the hub/operator can adjudicate ordering before either reaches the tag. No authoritative (tag/main) claim on `v3.54` exists at Commit-0 time, so the Commit-0 re-verify PROCEED condition holds on the authoritative anchor+claimed-set rule.
- **ADR numbers (this release authors TWO):**
  - **#1967 → ADR-069** (`core/ADRs/ADR-069-methodology-pack-composing-unit.md`) — the founding methodology-pack ADR (Option 1: a methodology pack is the composing unit at `core/packs/<archetype>/`).
  - **Composition-grammar sibling → ADR-070** (`core/ADRs/ADR-070-methodology-pack-composition-grammar.md`) — extends ADR-018 for the pack-composition layer (the `role` / composition meta-schema decision).
  - **Numbering re-verified at Commit 0** via `check-adr-numbers.py` (reports contiguous `001..067` on mainline — max on disk **067**) + a scan of both ADR dirs and all in-flight release branches: **ADR-069 and ADR-070 are absent everywhere** (no in-flight collision). Note the Stage-5 CR recommendation named **ADR-067** for the composition sibling; that recommendation is now **STALE** — `ADR-067-stage-gate-eval-set-home.md` was claimed by a concurrent release (83-eval-framework-completion) after the CR was written. Per the gap-free global-sequence invariant (`check-adr-numbers.py`), the next-free contiguous slots are **068** (founding) and **069** (composition grammar). The number space spans BOTH `core/ADRs/` and `release/ADRs/`; both new ADRs are core-scope (cross-module platform-architecture decisions rooted in core).
  - Both ADRs carry a prose `deciders` field per the recent-ADR convention (`operator + Stage 5 Solutioning + Collective Review scope-lock`), matching ADR-062/063/064. The literal-name carve-out (#1487) is now closed, but recent-ADR practice still uses the prose form; the prose form clears the Depersonalization gate unambiguously.
- **Branch topology: D-C SINGLE** — one branch `release/v3.54-methodology-pack-foundation`, one PR, one merge for all three issues. **D-Concurrency Posture P0** (fully serial; single PR / single branch; strict linear dep chain #1967 → #1968 → #1970 — no isolable parallel slice). No force-push (incl. `--force-with-lease`) — serial single-writer.

---

## Stage 4 Release Planning — 86-methodology-pack-foundation (transcribed + scope-lock update)

### Summary (30 seconds)

Three issues, one branch, one PR, one merge — the Wave-0 foundation of the methodology-packs track. Founding ADR + type-pack meta-schema widen + pack scaffold + label-grammar restructure. Clean acyclic DAG **#1967 → #1968 → #1970** after the scope-lock re-sequence. Size **16 raw / 21 effective** (cross-cutting ×1.3, round-half-up) — inside the 15–25 band. All three issues are real, verified work: `core/packs/` is confirmed absent `[SOURCE: ls core/packs → absent at 9c973c3]`, and no methodology-pack ADR exists in either ADR directory `[SOURCE: grep both ADR dirs → the pack contract is unrecorded]`. Release Class **cross-cutting confirmed** — the milestone Rationale reads as a multi-*surface* justification (type-pack meta-schema + pack abstraction + label-taxonomy grammar + ADR corpus); the class holds on the compositional-scope basis. All three are governance/schema/doc changes: **Stage 7/8 verification is grep/ls/file-state assertion + ADR well-formedness, not runtime test** (retained, not skipped).

### Dependency Graph

Directional DAG (acyclic, single connected component) — re-sequenced at the scope-lock:

```
#1967 ──▶ #1968 ──▶ #1970
   │                  ▲
   └──────────────────┘
```

- **#1967 → #1968** `[SOURCE: #1968 body "Requires #1967 first"; scope-lock re-sequence]` — the founding ADR fixes the pack contract (the widened pack model); #1968 widens the type-pack meta-schema to carry that model and scaffolds the manifests that instantiate it.
- **#1968 → #1970** `[SOURCE: scope-lock — #1970's label rows land in the widened grammar #1968 provides]` — the label restructure moves per-pack label contributions into the `[[labels]]` facet that #1968's meta-schema widen enables; #1970's grammar-only file references packs #1968 scaffolds.
- **#1967 → #1970** `[SOURCE: #1970 body "Requires #1967 first"]` — the label-grammar restructure demotes instance labels into `core/packs/<archetype>/`, a structure the ADR must first establish.

No cross-milestone hard edge INTO 86: milestone 87 (`87-methodology-pack-catalog`) depends on 86 (catalog consumes the foundation), not the reverse `[SOURCE: #228 Parallelization Map]`. #749/#765/#432 are CLOSED `[SOURCE: injected cache + #1970 body strike-throughs]`.

### Implementation Sequence (one branch, one PR, one merge)

Dependency-ordered slices under the milestone-one-PR discipline (a milestone ships as a SINGLE PR + single merge; issues are delivery slices on one branch, built in dep order):

1. **Commit 0** — this plan file (Engineering Commit 0, first Engineering spoke).
2. **#1967 [ADR] — Methodology packs as plug-and-play units** (foundation, THIS SPOKE). The Wave-0 founding ADR authored INSIDE this run. **ADR-069** fixes the pack contract (`core/packs/<archetype>/` bundling kinds + work-status + fields + labels + body-schema + gates; shared grammar in `_common`; per-pack deltas; best-practice defaults selected via `operator.toml`, overridable per project at K4). The composition-grammar sibling **ADR-070** extends ADR-018 for the pack-composition layer (the `role` / composition meta-schema decision — a grammar-altitude decision warranting its own ADR per the CR recommendation + PA-1). Everything downstream cites them.
3. **#1968 — Widen the type-pack meta-schema + scaffold `core/packs/{_common,scrum,kanban}` manifests** (foundation). Widen `work-item-type-schema.md` (shared `_common` with optional `kinds`, a `[[labels]]` facet, work-status, composition/`role`); then scaffold the three pack directories + `pack.toml` manifest stubs conforming to the widened grammar. `_common` (shared Axis-1 work-status, priority, universal labels), `scrum` (epic/story/task, story points, sprint lifecycle), `kanban` (card kind, class-of-service, WIP set-aggregate gate, continuous lifecycle). Best-practice defaults only (K1/Layer-3); no operator-specific values committed. Carries the Stage-6 should-fixes from #3025 (FM-B/FM-C WIP-gate `checks`-stub; CD-A `applies_to` conditioned on `role`; PR-A/FM-A/PA ground the `kinds` requiredness-relaxation separately from ADR-039).
4. **#1970 — Restructure `label-taxonomy.md` into grammar + per-pack deltas** (skill-core). Extract methodology-agnostic grammar (groups + rules); demote the repo-specific instance list; document the live `type:*` family as grammar; move per-pack label contributions into the `[[labels]]` facets #1968's widen enables. Carries the Stage-6 fixes from #3021 (FM-1 parity-gate green-capability vs row-removal contradiction — gate S6 GO on this; PR-1 correct the false "no anchor into moved rows" blast-radius claim — 2 live anchors at `stage-02-triage.md` and `stage-03-bundle.md`; FM-2 add `"type:"` to the parity gate's `REGISTERED_NAMESPACES` or record the carried ORPHAN gap). The meta-schema widen (#1968) resolves #1970's FM-3 (`_common` needs a schema).

Order is forced by content (linear dep chain); single merge gate at Stage 12.

### File Change Matrix

```
# --- Commit 0 : plan file (THIS SPOKE) ---
add  | release/releases/plans/v3.54_RELEASE_PLAN.md
# --- #1967 : founding methodology-pack ADR + composition-grammar sibling (THIS SPOKE) ---
add  | core/ADRs/ADR-069-methodology-pack-composing-unit.md
add  | core/ADRs/ADR-070-methodology-pack-composition-grammar.md
# --- #1968 : type-pack meta-schema widen + pack scaffold (own spoke) ---
edit | core/schemas/work-item-type-schema.md                # widen: _common, [[labels]] facet, work-status, composition/role
add  | core/packs/_common/pack.toml                          # shared root pack (Axis-1 work-status, priority, universal labels)
add  | core/packs/scrum/pack.toml                            # extends="_common"; epic/story/task, story points, sprint lifecycle
add  | core/packs/kanban/pack.toml                           # extends="_common"; card kind, class-of-service, WIP gate, continuous
add  | core/packs/README.md                                  # one-screen orientation (recommended per #3025 R3)
# --- #1970 : label-taxonomy restructure (own spoke) ---
edit | core/specs/label-taxonomy.md                          # grammar-only (groups + rules); type:* documented as grammar
# (per-pack [[labels]] contributions populate the pack.toml files #1968 scaffolds)
```

> **Scope boundary for THIS spoke (#1967 + Commit-0 only):** this spoke authors ONLY the plan file (Commit 0) and the two ADRs (ADR-069 founding + ADR-070 composition grammar). It does **not** touch `work-item-type-schema.md` (#1968's meta-schema widen), does not create the `core/packs/*` manifests (#1968's scaffold), and does not edit `label-taxonomy.md` (#1970's restructure). Those are separate Stage-6 spokes on this same branch, built after this one lands (P0 serial). The ADR filenames/numbers resolve at authoring time; placement is current-main topology.

---

## SCOPE-LOCK (Collective Review) — transcribed authoritative decisions

Per the #3010 **Collective Review — CONCLUDED** comment (2026-07-01, release-hub Mode O) and the #228 Amendment Log:

### ① Meta-schema widen ABSORBED into #1968 (operator scope-lock)
- The Stage-5 adversarial reviews found both pack designs assume a wider type-pack meta-schema than exists (no pack-level `[[labels]]` / work-status / optional-`kinds`; the grammar-widen — closed #1090 — was never scoped).
- Per operator directive: the pack layer is where best-practice-baseline + plug-and-play + user-config/override converge, so the grammar is *designed to accommodate* that convergence — the type-pack meta-schema widen is **ABSORBED into #1968** (re-titled; scope + ACs expanded), keeping 86 at 3 issues. #1968 re-sized M→L (~8).
- Sequence re-ordered **#1967 → #1968 (widen + scaffold) → #1970 (labels into the widened grammar)**.
- #1968's Stage-5 (#3025) re-run for the widened scope; #1970's Stage-5 (#3021) stands — the widen resolves its FM-3 cross-ticket schema contract; FM-1/PR-1/FM-2 carry as Stage-6 fixes.

### ② Founding ADR (#1967) authored at S6 — plus the ADR-070 composition-grammar sibling
- **ADR-069** — the founding methodology-pack ADR (widened model): a methodology pack is the composing unit at `core/packs/<archetype>/`.
- **ADR-070** — the composition-grammar sibling extending ADR-018 (the `role` / composition decision is a grammar-altitude decision warranting its own ADR, per the CR recommendation + PA-1). The CR named ADR-067 for this sibling; that number is now taken by a concurrent release, so the next-free contiguous number **069** is used (068 = founding). Engineering judgment kept these as **two ADRs** (not one): the founding ADR records the *placement + composing-unit* decision (adopt-Option-1); the composition-grammar ADR records the *meta-schema `role` / composition-altitude* decision extending ADR-018 — distinct decision kernels at distinct altitudes.

### ③ #1968 carried Stage-6 should-fixes (detail on #3025)
- **FM-B/FM-C** — demote the Kanban `set-aggregate` WIP-gate `checks` to a stub (the live `condition` as drafted is grammar-invalid — missing §1.2.1 Arm-2 `scope_ref` + `on_unresolved: BLOCK-TRANSITION` — and a live `condition` pushes reversibility toward EXPENSIVE).
- **CD-A** — conditionalize `applies_to` on `role` (or pin `role=base ⇒ applies_to="*"`) — a `role=base` pack can't satisfy the Layer-2 join-key semantics.
- **PR-A / FM-A / PA** — ground the `kinds` requiredness-RELAXATION separately from ADR-039 (ADR-039's precedent is an optional-ADD, symmetric on both compat axes; a relaxation isn't forward-compatible for a pre-existing validator — currently theoretical, no validator yet).

### ④ #1970 carried Stage-6 fixes (detail on #3021)
- **FM-1 (gate S6 GO on this)** — resolve the parity-gate green-capability vs row-removal contradiction (retain illustrative rows / repoint `--source` / accept a warn-mode ORPHAN spike).
- **PR-1** — correct the false "no anchor into moved rows" blast-radius claim (2 live anchors: `stage-02-triage.md`, `stage-03-bundle.md`).
- **FM-2** — add `"type:"` to the parity gate's `REGISTERED_NAMESPACES` or record the carried ORPHAN gap.
- The meta-schema widen (in #1968) resolves #1970's FM-3 (`_common` needs a schema).

### Confirmed (ratified)
- Milestone stays **3 issues** (16 raw / 21 effective — in band). Reversible: revert #1968's scope + the re-sequence.
- Topology: ONE branch `release/v3.54-methodology-pack-foundation`, ONE PR, sequential commits #1967 → #1968 → #1970 (D-C SINGLE; milestone-one-PR). The three Stage-6 sub-tasks (#3018 / #3026 / #3022) are delivery slices ON that one branch, NOT separate PRs.
- **ADR immutability:** a merged ADR is not edited — a wrong ADR is superseded by a new ADR, not reverted-and-rewritten (unless the whole release is pulled pre-lock).

### Engineering/QA directives
- **PR body:** SAFE closure phrasing throughout (`mark #N as closed at Stage 13`); **NO** auto-close keywords for #1967/#1968/#1970 — they close deliberately at Stage 13.
- **ADR numbering:** re-verify next-free across BOTH `core/ADRs/` + `release/ADRs/` + in-flight at authoring time; `check-adr-numbers.py` gate at Stage 7/9.
- **#1968 AC "parses against the methodology-pack grammar (method: validator once available)"** is a declared-but-deferred verification method — record it; verify by grammar-conformance review + TOML parse at S7/S8 until an executor exists. Do not drop or rewrite the AC.

---

## Risk Register (carried from Stage 4, adjusted for scope-lock)

No dates (single-operator PMO — scope + sequence only). Reversibility tier + confidence per item.

| ID | Class | Risk | Reversibility | Mitigation / Owner |
|---|---|---|---|---|
| **R1** | Dependency | #1970 built before #1968's widened grammar lands → label rows parse against a stale/absent grammar; rework. | CHEAP / HIGH | Honor the #1968 → #1970 edge (sequenced). Trigger: any attempt to move label rows before the grammar widen commits. Action: block #1970's pack-label write until #1968's widen is on the branch. |
| **R2** | Contention (cross-milestone) | #1970's `label-taxonomy.md` edit collides with 87's #1777/#1971 on the same file. | CHEAP / HIGH | 87 depends on 86; 86 merges first by construction. Trigger: 87 starts before 86 merges. |
| **R3** | Namespace | ADR next-free collides (numbering spans core/ADRs + release/ADRs + in-flight); the CR-recommended ADR-067 is already taken. | CHEAP / MEDIUM | Allocate **ADR-069 (founding) + ADR-070 (composition)**; re-verified free at Commit 0 (max-on-disk 067, both absent everywhere). `check-adr-numbers.py` gate at Stage 7/9. |
| **R4** | Version | Provisional v3.54 contended by a concurrent branch (`release/v3.54-frontier-system-business-specialists`, #407). | CHEAP / HIGH | NOT blocking now — v3.54 binds at Stage-12 tag; ADR-036 version-claim-determinism resolves (first-to-Stage-12 claims; other forward-only bumps). Surfaced to hub/operator for ordering adjudication. Trigger: Stage-12 freeness check finds v3.54 tagged. Action: re-run authoritative next-free + atomic claim at Stage 12. |
| **R5** | Scope (public-safety) | A manifest (#1968) leaks an operator-specific value into public corpus. | CHEAP / HIGH | #1968 AC3 = grep for operator tokens returns none; best-practice defaults only (K1/Layer-3), operator selection stays K4/operator-local. Trigger: grep finds an `[OPERATOR_*]` or literal value in a manifest. |
| **R6** | Rollback | ADR immutability breach — post-merge edit to a merged ADR. | MODERATE / HIGH | ADR "test" is well-formedness + immutability. Trigger: any PR editing a merged ADR body. Action: treat corrections as superseding ADRs, not edits. |

No HIGH/CRITICAL risks. Aggregate rollback complexity is LOW (net-new ADRs + net-new packs; one edited governed file — `label-taxonomy.md`).

---

## Stage Applicability

Default = all Stages 5–13 apply. Verification method is **grep/ls/file-state assertion + ADR well-formedness**, never runtime test — Stages 7/8 DO apply; do NOT skip them (cross-cutting biases Stage 9 to Deep + Stage 5 to ALL-activation).

- **#1967 (ADR):** S5 **N/A** (the ADR *is* the Stage-5 design act — authored in-run) · S6 APPLY (author ADR-069 + ADR-070) · S7/S8 APPLY (ADR well-formedness = schema/heading conformance + immutability = no post-merge edit) · S9 Deep.
- **#1968 (widen + scaffold):** S5 APPLY (re-run for widened scope, #3025) · S6 APPLY · S7/S8 APPLY (ls + TOML parse + grammar-conformance review + operator-token grep) · S9 Deep.
- **#1970 (label restructure):** S5 APPLY (#3021 stands) · S6 APPLY · S7/S8 APPLY (grep-based AC checks: grammar-only file, `type:*` documented, per-pack contributions under `core/packs/*`) · S9 Deep.

---

## Rollback

- **Pre-merge:** abandon the release branch (no main impact). CHEAP.
- **Post-merge, whole release:** `git revert` the merge commit — removes the ADRs + meta-schema widen + pack scaffold + label restructure atomically. CHEAP/MODERATE (`label-taxonomy.md` + `work-item-type-schema.md` are the only *edits*; ADRs + packs are net-new adds that revert cleanly).
- **Per-slice (if a single issue fails S7/8/9):** because history is linear and issues are dep-ordered, drop the failing slice's commit(s) before the merge gate; downstream slices that depend on it drop with it. #1967 failing = whole release halts (it is the foundation). #1970 failing alone = revert only its commits; #1967/#1968 can still ship (independent value: the ADRs fix the contract, the meta-schema widen + scaffold stand alone).
- Rollback is operator-authorized per RELEASE_PROTOCOL.md § Rollback protocol; self-repair within the PR follows `autonomous-execution-model.md` (Retry/Escalate/Rollback).

---

*Audit baseline: branch cut from `origin/main` @ `ccc92ff`; Commit-0 version re-verify at the same anchor (`anchor()` = published `v3.53`; recomputed next-free = `v3.54`). domain_practice: { source: N/A — pipeline-internal release, date: 2026-07-02, domain: governance }. Closure discipline: the three delivery issues (#1967/#1968/#1970) are marked closed at Stage 13, not by this PR.*
