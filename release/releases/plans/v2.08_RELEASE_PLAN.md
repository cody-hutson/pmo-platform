<!-- reference-durability: allow-issue-ref -->
<!-- repo-integrity: allow-memory-ref -->
# v2.08 Release Plan — 11-artifact-lineage-and-generated-surface-governance

> **Provisional version v2.08** — latest tag `v2.05`; v2.06/v2.07 in-flight untagged. Provisional, finalize at Stage 12 per the re-versioning discipline.
> **Branch topology: single-branch (D-C SINGLE)** — `release/v2.08-artifact-lineage-and-generated-surface-governance`; two strictly-serial cards (#334 → #1165), near-zero file contention, one release PR.
> **Scope refinement (Stage 5 design #1449):** the Stage 5 Solutioning pass refined #334's File-Change-Matrix from 2 → 3 files — it adds `core/standards/template-protocol.md` (rename `topic` → `sibling_topic` + bidirectional schema pointer) alongside `frontmatter-schema.md` (add fields) and `lifecycle-states-canonical.md §3.2` (cross-ref line).

This file is **Engineering Commit 0** on `release/v2.08-artifact-lineage-and-generated-surface-governance`. The body below is the approved Stage 4 Release Planning output (verbatim). Each issue's implementation-ready Stage-5 design spec lives on its Stage-5 sub-task comment — engineer each issue from its sub-task comment, not from this summary.

---

## Stage 4 Release Planning — 11-artifact-lineage-and-generated-surface-governance

> **Persona:** Release Manager — Release Planning (release-scoped; one pass over all Milestone issues).
> **Milestone:** `11-artifact-lineage-and-generated-surface-governance` (GitHub milestone **#195**) — OPEN; 2 open + 2 closed = 4 issues.
> **Build scope (milestone "Cards (2)"):** #334 (lineage frontmatter fields), #1165 (`artifact-lint` NEW skill). **Context-only (closed):** #117, #370.
> **Treatment:** Repo READ-ONLY; planning only (no edits/commits/branches/scaffolding). Provisional version **v2.08** (latest tag `v2.05`; v2.06/v2.07 in-flight untagged) — **provisional, finalize at Stage 12** per re-versioning discipline.
> **Platform-config baseline (hub-resolved, single resolution):** `bundle_doctrine_frame = F1` · `release_size_target_pts = 15–25` · `default_release_class = novel`.

### Summary (30 seconds)

This release ships the **two open cards** of the artifact-lineage-graph split as a **strict serial pair**: #334 wires the horizontal-lineage frontmatter vocabulary into `core/schemas/frontmatter-schema.md` (RECONCILE — the vocabulary already exists in `lifecycle-states-canonical.md §3.2`; this lands it in the schema layer and reconciles two naming-drift pairs), then #1165 builds the NEW `operations/skills/artifact-lint/` skill that **reads** those fields. **#334 → #1165 is a hard serial edge** — the lint reads lineage fields the schema edit ships; no parallelism.

**The pivotal decision is D-Dependency:** #334 declares "Blocked by #205" and #1165 needs #201; both #205 and #201 are OPEN in **milestone 15** (a different milestone). I verified their *substance* is already present in the corpus (the provenance/Connections container in `frontmatter-schema.md`; the 5-state workflow in `§3.2`). My verdict, with disconfirming-evidence test below: **#205/#201 are satisfied-in-substance — proceed now**, because #334-a's fields are explicitly defined as *distinct from* #205's `source_inputs`, and the lint reads only lineage fields + the already-canonical 5-state machine — neither needs the #205/#201 *named additions* to exist.

**Release Class: `novel`** (NEW skill + schema change with downstream consumers + ≥3 D-class decisions). **Topology: single branch (D-C SINGLE)** — two strictly-serial cards, one new skill dir, near-zero file contention.

**All 6 hub-verified findings CONFIRMED** against canonical sources (file:line cited per finding below). Two are **sharpened**: #117 is **not a hollow close** (merged into OPEN #369, which owns `artifact-naming-standard.md` in milestone 20); #370 is **not abandoned** (merged into OPEN #201, milestone 15). Neither is build scope here.

---

### Hub-Finding Verification (CONFIRM / REFUTE — file:line checked)

| # | Hub finding | Verdict | Evidence (file:line I checked) |
|---|---|---|---|
| 1 | #334 is RECONCILE: §3.2 already defines the lineage fields; schema MISSING `parent_artifact`+`sibling_topic`/`topic`, HAS `superseded_by`+`promoted_from`, LACKS backward `supersedes`; 7 MVP types present | **CONFIRMED** | `core/standards/lifecycle-states-canonical.md:135-138` (`parent_artifact`, `supersedes`, `sibling_topic`, `origin_transcript`). `core/schemas/frontmatter-schema.md`: `superseded_by` present `:181`, `promoted_from` present `:198`; `parent_artifact`/`sibling_topic`/`topic`/backward-`supersedes` = **0 grep hits**. 7 MVP types `:106-112` (GENERATES/DEPENDS_ON/BLOCKS/SUPERSEDES/BELONGS_TO/RELATES_TO/ASSIGNED_TO) |
| 2 | WITL: the 7 MVP relationship vocab is already built; #334 must COMPOSE/ADOPT, not redefine verbs; #507 is the consolidation ref | **CONFIRMED** | 7 types present at `frontmatter-schema.md:102-112` ("MVP Relationship Types (7 of 12 from C17)"). #334's 2026-06-07 comment ("adopt / compose against that vocabulary rather than redefine its own relationship verbs; #507 … consolidation reference") read verbatim from issue body. Composition posture adopted in plan (the lineage **fields** are NOT relationship-graph **verbs** — see Risk R-7) |
| 3 | Naming drift: `topic`(#334) vs `sibling_topic`(§3.2); `supersedes`(backward) vs `superseded_by`(forward). Exactly one name per concept; both docs aligned. Required D | **CONFIRMED** → **D-NameReconcile** | `lifecycle-states-canonical.md:136-137` (`supersedes`, `sibling_topic`); `frontmatter-schema.md:181` (`superseded_by` forward). The two docs currently disagree on naming surface. Rendered as D-NameReconcile |
| 4 | Cross-milestone blocker (PIVOTAL): #334 blocked-by #205, #1165 needs #201; BOTH OPEN in ms 15; substance appears present | **CONFIRMED (state) + substance verified** → **D-Dependency** | #205 OPEN, milestone **15-generated-vs-source-provenance**, size:M; #201 OPEN, milestone **15-generated-vs-source-provenance**, size:L (both via `gh issue view`). Substance: Category 4 Connections container `frontmatter-schema.md:86`, Domains A/B/C `:62`; 5-state workflow `DRAFT→REVIEWED→APPROVED→PROMOTED→ARCHIVED` `lifecycle-states-canonical.md:107`. **Nuance found:** #205's *named field* `source_inputs` does **NOT** yet exist (0 grep hits in either doc) — but #334-a is explicitly distinct from it, so this does not gate #334. Full reasoning in D-Dependency |
| 5 | #117 CLOSED COMPLETED but `core/standards/artifact-naming-standard.md` ABSENT — hollow close or shipped-elsewhere? | **CONFIRMED absent; REFUTES "hollow"** | `core/standards/artifact-naming-standard.md` = **No such file** (and `find` across core/operations/release = 0 matches). BUT #117's close comment (2026-06-15): "Closing — merged into **#369**." #369 ("Adopt NARA-grade PM file naming") is **OPEN** in milestone **20-records-management-naming-and-cleanup**. So: dedup, not abandonment; the file is legitimately not-yet-shipped because #369 hasn't shipped. Impact on #334/#1165 below |
| 6 | #370 is NOT_PLANNED; `08-Generated/_archived/` archive convention not built; #1165's "version chains move to `_archived/`" references an unformalized convention → Stage 5 design note | **CONFIRMED unformalized; REFINES "NOT_PLANNED"** | #370 CLOSED; close comment (2026-06-15): "Merged into **#201**" (not "won't do"). #201 OPEN, milestone 15. The `_archived/` convention is therefore *deferred to #201*, not formally shipped. Net effect identical to the finding: #1165's `_archived/` reference is to an unformalized convention → flagged as Stage 5 design note for #1165 |

**Localization note (against generic capacity/scope heuristics):** the generic heuristic "a cross-milestone blocker = hard blocker, defer the dependent release" does NOT apply here — platform-specific context (the §3.2 vocabulary is already canonical and #334-a is scoped *distinct from* #205's fields) localizes the decision to satisfied-in-substance. Reconciliation rendered as D-Dependency.

---

### Dependency Graph

**Internal (within this release) — STRICT SERIAL:**

```
#334 (lineage frontmatter fields → frontmatter-schema.md + §3.2 sync)
  │  (HARD edge: artifact-lint reads the lineage fields the schema edit ships)
  ▼
#1165 (artifact-lint NEW skill — consumer/enforcement surface)
```

**External (cross-milestone) — directional, satisfied-in-substance:**

```
#205 (provenance header / Category 4 container) ──blocks──▶ #334
        [OPEN · ms 15 · container present in schema · source_inputs field NOT yet added]
        ⇒ SATISFIED-IN-SUBSTANCE for #334 (fields are distinct from source_inputs)

#201 (DRAFT→…→ARCHIVED workflow) ──needed-by──▶ #1165
        [OPEN · ms 15 · 5-state machine already canonical in §3.2:107]
        ⇒ SATISFIED-IN-SUBSTANCE for #1165 (stale-draft / version-chain checks key off the present §3.2 states)

#334 / #334-a ──blocks──▶ #277 (cleanup automation; OPEN · ms 20) [DOWNSTREAM — out of this release]
#1165 ──blocks──▶ #277 (#277 consumes lint output) [DOWNSTREAM — out of this release]
#370 (→ merged into #201) ──relates-to──▶ #1165 (_archived/ convention; unformalized) [CONTEXT]
#369 (owns artifact-naming-standard.md; OPEN · ms 20) ──composes-with──▶ both [CONTEXT — naming surface not yet shipped]
```

**Note on #277:** #277 ("Automate 08-Generated/ cleanup") is OPEN in **milestone 20** and is the *downstream consumer* of both cards — it is NOT in this release. Per the #1165 body's explicit `#277 conflation` flag: `cleanup-orphan-state.sh` is a DIFFERENT tool (state-file removal) and must NOT be wired into artifact-lint or #277. Carried as Risk R-6.

---

### Implementation Sequence

Dependency-ordered. No parallel batches (the two cards are strictly serial; finding "Hard sequence").

| Seq | Issue | Type | Work | Gating predecessor |
|---|---|---|---|---|
| **1** | **#334** | Schema/governance RECONCILE edit | Add `parent_artifact`, the chosen sibling-grouping field, and backward `supersedes` to `frontmatter-schema.md` (Category 4 / Domain-A / Domain-C tables `:86`/`:175`/`:191`); add the lineage-vs-`source_inputs` distinction table (AC-4); list lineage fields in the Sidecar Specification (`:202`, AC-5); **sync `lifecycle-states-canonical.md §3.2` in the SAME change** so the two docs never disagree (AC-2/AC-3); re-run the schema Validation Checklist (AC-6, regression). | #205 (satisfied-in-substance) |
| **2** | **#1165** | NEW skill via skill-refiner pipeline | Author `operations/skills/artifact-lint/` (SKILL.md + `references/work-plan-taxonomy.md` 17 rows + evals); 5 checks (orphan / sibling duplicate / stale draft / displaced content / version chain); strict-match dedup heuristic reading the #334 lineage fields; Autonomy Tier 1 (read-and-recommend, no auto moves/deletes); hard-exclude `09-Prototype/`,`_templates/`; ≥3 domain-specific failure modes; eval harness (variance + trigger-optimization + cross-skill false-positive). | **#334 (HARD)** + #201 (satisfied-in-substance) |

**Why serial, not parallel:** #1165's AC-4 explicitly requires the dedup heuristic to "read the lineage fields from `core/schemas/frontmatter-schema.md` (#334-a)". If #1165 is authored before #334 lands, it cites fields that do not yet exist in the schema layer → it would harvest from `§3.2` only and the schema/skill contract would be unverifiable. Serialize.

---

### Stage Applicability Matrix

Stages 5–13 (per `stage-04-planning.md` handoff + `planning-solutioning-handoff.md` activation criteria; both cards are governance/skill-domain, so Stages 10–11 compress for git-native releases per `harness-deployment.md` §Release process).

| Stage | #334 (schema RECONCILE) | #1165 (NEW skill) | Rationale |
|---|---|---|---|
| **5 Solutioning** | **APPLY** | **APPLY** | `novel` class biases Stage 5 activation to ALL. #334: the naming-reconcile choice + the lineage-vs-`source_inputs` semantics need a design pass. #1165: carries the Stage-5 deferrals from #334 (conflict-resolution UX; non-markdown lineage-carrier mechanism; the 7 edge-case register) + the `_archived/` unformalized-convention design note (Finding 6). |
| **6 Engineering** | **APPLY** | **APPLY** | Both produce file changes. |
| **7 Dev Testing** | **APPLY** (light) | **APPLY** (full) | #334: re-run schema Validation Checklist (AC-6) + grep-based AC verification. #1165: eval harness is the DT surface — the `_v1.._v4` supersede-chain fixture (last AC) + the 5-check fixtures + cross-skill false-positive run. |
| **8 Peer/QA Review** | **APPLY** | **APPLY** | #1165 routes through `pmo-qa-auditor` (G7 ≥3 failure-mode structural gate) + `pmo-skill-refiner`/`pmo-skill-editor` discipline. #334 governance-edit review. |
| **9 Plan Review (GATE)** | **APPLY — Deep** | **APPLY — Deep** | `novel` ⇒ Deep Stage 9 depth (Collective Review N-way consistency + cross-D upstream-compat scan + design-spec conformance). Human GO/NO-GO. |
| **10 Pkg / 11 Deploy prep** | **COMPRESS** | **APPLY (skill deploy + .skill build)** | #334 is docs-only (no skill/package artifact) → compress per git-native. #1165 is a NEW skill → must deploy (`./deploy.sh --deploy artifact-lint`) + build its `.skill` package (Check 7 package-freshness) + register in `deploy.sh` per-module arrays (`OPERATIONS_SKILLS`) so Check 5 roster-drift stays green. |
| **12 Execute (GATE)** | **APPLY** | **APPLY** | Merge, tag (finalize version here — provisional v2.08), operator authorization. |
| **13 Close-out** | **APPLY** | **APPLY** | RELEASE_LOG + INDEX + DIGEST + CHANGELOG; **mark #334 and #1165 as closed at Stage 13** (close-family verb avoided per parser-clean discipline). Orphan-state cleanup via `cleanup-orphan-state.sh --release-close`. |

**Skips / compressions enumerated:** Stage 10/11 **compress for #334** (no package artifact — governance docs only). No full skip of any 5–13 stage for either card (a `novel` release with a new skill exercises the full pipeline). Stage 5 is APPLIED, not skipped, because the `novel` class's ALL activation bias + the carried #334 Stage-5 deferrals make design-uncertainty real.

**Per-issue closure mechanics note (parser-clean):** both cards are fully resolved by this release → in the eventual PR they belong in the dedicated Issue References block with a close-family verb; everywhere else use `mark #334 as closed at Stage 13` / `mark #1165 as closed at Stage 13`.

---

### Contention Map

**Shared affected files between #334 and #1165:** assessed — **none.**

| File | #334 | #1165 | Collision? |
|---|---|---|---|
| `core/schemas/frontmatter-schema.md` | **EDIT** (add 3 fields + distinction table + sidecar list) | READ-only (cites it as the field source) | **No** — #1165 does not write it |
| `core/standards/lifecycle-states-canonical.md` | **EDIT** (`§3.2` naming sync) | READ-only (cites `§3.2` for the 5-state machine) | **No** — #1165 does not write it |
| `operations/skills/artifact-lint/**` | — | **NEW dir (create)** | **No** — #334 does not touch it |
| `deploy.sh` (`OPERATIONS_SKILLS` array) | — | **EDIT** (register new skill — Stage 11) | **No** — #334 does not touch it |
| `packages/artifact-lint.skill` | — | **NEW (build)** | **No** |

**Verdict: file contention is near-zero**, exactly as the brief anticipated (#1165 is a new skill dir). The only *logical* coupling is the field-name contract: #1165 must read the names #334 chooses in D-NameReconcile — which the strict serial sequence guarantees. **Cross-PR / cross-release contention:** none detected — no open PRs touch these files (baseline-pinned at this read; population observably empty — see Risk R-5). Sibling milestones 15 (#205/#201) and 20 (#277/#369) edit *adjacent* surfaces but their work is NOT in this release; the structural-blast-radius check finds no rename/relocate/delete by this release (additive field edits only), so no Tier-S serialization edge against any sibling milestone.

---

### Risk Register

| ID | Risk | Class | Likelihood | Impact | Mitigation | Owner-action |
|---|---|---|---|---|---|---|
| **R-1** | **Dependency mis-call** — #205/#201 are *actually* hard blockers (the in-substance match is superficial) and #334/#1165 ship against a moving target | Dependency | Low | High | D-Dependency disconfirming-evidence test (below) is explicit; #334-a fields scoped *distinct from* #205's `source_inputs` (which doesn't exist yet); 5-state machine is already canonical, not borrowed from unshipped #201. If #205/#201 later add a *conflicting* field, reconcile at that release (forward-binding §3.2 already anticipates this). | Operator ratifies D-Dependency at Stage 9 |
| **R-2** | **Naming-reconcile picks the losing name** — choosing `topic` over `sibling_topic` (or vice-versa) strands the other as an orphan alias across the corpus | Contention / drift | Medium | Medium | D-NameReconcile makes the choice explicit + BOTH docs aligned in one PR (#334 AC-2/AC-3); grep both files post-edit for zero orphaned alias. Recommend `sibling_topic` (canonical §3.2 already uses it; fewer edits, no schema-side churn). | Operator confirms name at D-NameReconcile |
| **R-3** | **#117 hollow-close illusion** — building #334/#1165 assuming a filename standard exists when `artifact-naming-standard.md` is absent | Scope / stale-evidence | Low | Low–Med | Verified: file ABSENT; #117 merged into OPEN #369 (ms 20). **Neither #334 nor #1165 depends on the filename standard** (#334 = frontmatter; #1165 = graph lint keyed on frontmatter + lifecycle state, NOT on filename). The naming surface is a *future compose-with*, not a blocker. Do not assume `artifact-naming-standard.md` at build. | None for this release; note #369 as the owner |
| **R-4** | **#370 unformalized `_archived/` convention** — #1165's "version chains move to `_archived/`" references a convention not yet shipped (merged into OPEN #201) | Scope | Medium | Low | **Stage 5 design note for #1165** (per Finding 6): artifact-lint's version-chain check must treat `_archived/` as a *directional* target, not a hard dependency; the lint *recommends* (Tier 1), it does not *move* files — so an unshipped destination convention does not block the recommend-only behavior. | Stage 5 spoke records the design note |
| **R-5** | **Audit-baseline empty population** — the cross-PR contention "no open PRs" finding rests on a transiently-empty population | Audit baseline | Low | Low | Pin baseline: read at the worktree HEAD (latest tag `v2.05` + the v2.06/v2.07 in-flight branches), bounded recent-window. **Re-check open-PR population at Stage 9 entry** before relying on the no-contention verdict (per audit-baseline-discipline). | Stage 9 spoke re-checks PR population |
| **R-6** | **#277 tool conflation** — wiring `cleanup-orphan-state.sh` into artifact-lint/#277 | Scope | Low | Medium | Explicit in #1165 body: `cleanup-orphan-state.sh` = state-file removal, a DIFFERENT tool; artifact-lint = markdown/artifact-graph lint. Keep them disjoint. #277 is OUT of this release (ms 20). | Stage 5/6 honor the disjointness |
| **R-7** | **Relationship-verb redefinition** — #334 accidentally redefines the 7 MVP relationship verbs instead of composing against them (WITL finding) | Architecture / duplication | Low | Medium | The lineage **fields** (`parent_artifact`/sibling-grouping/`supersedes`) are *frontmatter scalar fields*, NOT entries in the `relationships[]` MVP-type enum (`frontmatter-schema.md:102-112`). Plan keeps them as distinct Category-4 / Domain-A/C *fields* and does NOT add new `type:` enum values. `SUPERSEDES` (the verb) already exists `:109` and is untouched; backward `supersedes` (the field) is the new scalar. #507 is the consolidation reference. | Stage 5/6 keep fields ≠ verbs |
| **R-8** | **block-skill-direct-edit hook on first SKILL.md Write** for #1165 | Process | Medium | Low | Per `skill-deployment.md` §Mandatory Tooling: **NEW-skill authoring path is NOT hook-gated** ("PMO tooling does NOT gate the NEW-skill authoring path"). Author via `pmo-skill-refiner` Mode 2 (Create New). Do NOT self-bypass / self-create a `.editor-session` sentinel. If a warn-mode hook fires on the first Write, proceed (NEW-skill path); only an EXISTING migrated skill is enforce-gated. | Stage 6 uses skill-refiner Create-New |
| **R-9** | **Rollback complexity** | Rollback | Low | Low–Med | See Delivery & Rollback strategy below — both cards are additive (new fields, new skill dir) → rollback is CHEAP (revert the PR; no data migration; no destructive schema change; the new skill is self-contained and de-registers from `deploy.sh` arrays cleanly). | Operator-authorized rollback per RELEASE_PROTOCOL |

---

### Operator Decisions (D-Gates)

#### D-Dependency: Are #205 / #201 hard blockers, or satisfied-in-substance (proceed now)?
**Gate input:** #334 provenance header declares "Blocked by #205"; #1165 body lists "#201 (lifecycle states — the stale-draft check needs the DRAFT/PROMOTED distinction)". Hub-verified that #205 + #201 are OPEN in milestone 15; substance verification by this spoke (file:line below).
**Gate decision:** Choose between **(A) satisfied-in-substance → proceed with this release now** vs **(B) hard blockers → defer this release until milestone 15 ships #205/#201**.
**Spoke recommendation:** **(A) satisfied-in-substance — PROCEED NOW.** Reasoning, with disconfirming-evidence test:
- *Substance present:* the 5-state machine `DRAFT→REVIEWED→APPROVED→PROMOTED→ARCHIVED` is already canonical in `lifecycle-states-canonical.md:107` (not pending #201); the provenance/Connections container (Category 4 `frontmatter-schema.md:86`; Domains A/B/C `:62`) already exists (not pending #205).
- *Scope disjointness:* #334-a defines its lineage fields **explicitly distinct from** #205's `source_inputs` (the issue body's semantics table: `source_inputs` = upstream human evidence, outward; `parent_artifact`/`supersedes`/sibling-grouping = horizontal within AI outputs). I verified `source_inputs` does **not yet exist** in either doc (0 grep hits) — and that is fine, because #334-a does not extend or require it.
- *Disconfirming evidence sought (and not found):* I tested the opposite hypothesis — "the lint or the schema edit needs a #205/#201 named addition." (a) Does any #334 AC reference a #205 field? No — all 6 ACs reference only the lineage fields + the existing `superseded_by`/`SUPERSEDES`/`promoted_from`. (b) Does any #1165 check need a lifecycle *state* that #201 has not yet shipped? No — the stale-draft and version-chain checks key off `DRAFT`/`PROMOTED`/`ARCHIVED`, all present at `§3.2:107`. (c) Would #205's *eventual* `source_inputs` collide with #334-a? No — distinct field, distinct direction, distinct Category placement. The only residual is forward-binding: if a *future* #205/#201 release adds a conflicting name, reconcile then (§3.2 already carries forward-binding contract language at `:107`/`:140`). **No disconfirming evidence overturns (A).**
**Blocks:** Implementation Sequence Step 1 (cannot start #334 until this is rendered). Everything downstream inherits.
**Upstream compatibility:** **N/A — this D-decision does not modify skill-authoring surface.** It concerns PMO operations-domain artifact-frontmatter dependency posture (document schema + cross-milestone sequencing), not Anthropic skill-creator SKILL.md structure. Upstream compatibility check does not apply.
**Reversibility / Confidence:** **MODERATE / HIGH.** Reversible — if #205/#201 later prove to add conflicting fields, the schema add is additive and re-reconcilable at that release without data migration. HIGH confidence given the explicit scope disjointness + present 5-state machine.

#### D-NameReconcile: `topic` vs `sibling_topic`; and the `supersedes` / `superseded_by` inverse pair
**Gate input:** Naming drift confirmed: `lifecycle-states-canonical.md:136-137` uses `supersedes` (backward) + `sibling_topic`; #334 body proposes `topic`; `frontmatter-schema.md:181` already has `superseded_by` (forward). Exactly one name per concept must be chosen and BOTH docs aligned (a required, explicit one-word reconcile — #334 does not pre-decide).
**Gate decision:** (1) sibling-grouping field name: **(A) `sibling_topic`** vs **(B) `topic`**. (2) supersede pair: confirm **`superseded_by` (forward, existing) + `supersedes` (backward, new)** as a documented inverse pair (vs collapsing to one direction).
**Spoke recommendation:** **(1) `sibling_topic`** — the canonical `§3.2` already uses it; choosing it means the schema *adopts* the canonical name (one doc changes minimally, zero orphan aliases) rather than forcing `§3.2` to rename. **(2) Keep both** — `superseded_by` (forward, already in schema `:181`) + add `supersedes` (backward); document them as an inverse pair in the semantics table (#334 AC-3). Note: this is the *frontmatter scalar field* `supersedes`, distinct from the `SUPERSEDES` *relationship-graph verb* at `:109` (which stays untouched — see Risk R-7).
**Blocks:** #334 Step 1 field-naming (the schema edit cannot be authored until the name is chosen); transitively #1165's strict-match heuristic (it reads the chosen field name).
**Upstream compatibility:** **N/A — does not modify skill-authoring surface.** These are PMO operations-domain ARTIFACT frontmatter fields (document schema for generated artifacts), NOT Anthropic skill-creator SKILL.md frontmatter (which is `name` + `description` only per `upstream-reference-catalog.md` `skill-md-frontmatter` entry). Upstream compatibility check does not apply.
**Reversibility / Confidence:** **MODERATE / HIGH.** A field rename after adoption is a corpus-wide find-replace (MODERATE — touches both docs + any future consumer), so deciding once now is the cheap path. HIGH confidence: `sibling_topic` is the lower-churn choice because it is already canonical.

#### D-ReleaseClass: What Release Class does this release carry?
**Gate input:** Spoke-proposed class + trigger-condition evidence per `release-class-taxonomy.md` § Class Enum.
**Pre-decided:** Hub-injected `default_release_class = novel` (template default) — proposed here as the merits-based class, not a hedge.
**Gate decision:** Choose between **(A) routine**, **(B) novel**, **(C) cross-cutting**, **(D) hotfix**.
**Spoke recommendation:** **(B) novel.** Trigger evidence (any one fires; multiple fire here): `novel` (a) "≥1 issue introduces a new reference doc, schema, or skill" → #1165 is a NEW skill **and** #334 adds new schema fields; (b) "≥1 D-class decision in release plan" → 4 D-decisions here. Disqualifies `routine` (new files added + new D-class decisions present → fails routine triggers (c)/(d)). Not `cross-cutting` (does NOT touch ≥3 `pipeline/stage-*.md` files nor ≥3 of the named governance surfaces {CLAUDE.md, OPERATIONS.md, …}; it touches 2 schema/standard docs + 1 new skill dir + deploy.sh registration — below the ≥3 threshold). Not `hotfix` (no P1/P2 deployed-release defect; this is net-new capability). Multi-trigger resolution: only `novel` triggers fire → `novel` is unambiguous.
**Differentiation posture:** Engagement density **Standard** · Stage 9 review depth **Deep** · Stage 5 activation bias **ALL** · Stage 13 outcome-window **30-day**.
**Blocks:** Milestone-description `## Release Class` authoring (currently ABSENT — milestone #195 description carries no Release Class section, so this is a first declaration, not a re-classification); downstream per-class differentiation posture (Stage 9 depth, Stage 5 activation bias).
**Upstream compatibility:** N/A — Release Class is PMO platform internal taxonomy; no Anthropic upstream surface. Upstream compatibility check does not apply.
**Reversibility / Confidence:** CHEAP / HIGH (re-classifiable later with operator approval per `release-class-taxonomy.md` Re-Classification Protocol).

#### D-ArtifactLintSourcing: What Anthropic-coupling posture does the NEW `artifact-lint` skill take? (own / guarded-wrap / pass-through)
**Gate input:** #1165 is a NEW skill built via the skill-refiner pipeline. ADR-023 requires every new/changed skill to declare its sourcing-coupling posture at the Stage-4 D-Gate, chosen against the blast-radius × commodity-stability test.
**Gate decision:** Choose between **(A) own (independent)**, **(B) guarded-wrap (extends, drift-canary-guarded)**, **(C) pass-through**.
**Spoke recommendation:** **(A) own (independent) — own-with-harvested-learnings, the ADR-023 default.** Justification per ADR-023's 3-condition exception test (a runtime wrap is permitted ONLY if all three hold):
- *Commodity-stable upstream contract?* **No** — there is no Anthropic commodity engine for "artifact-graph lint / lineage dedup." This is a PMO-judgment surface (it interprets lineage fields + lifecycle state and *recommends* dispositions to the operator), not a stable I/O contract like a document-format engine. Fails condition (1).
- *Low blast radius of silent upstream drift?* **No** — artifact-lint emits operator-facing recommendations that gate file moves/deletes (Tier 1 recommend). A silent upstream change to lint logic would change what the operator is told to do with their generated surface. Fails condition (2).
- *Therefore:* the guarded-wrap exception does NOT apply on two of three conditions; and ADR-023's bright line is explicit — "any PMO-judgment or governance-binding skill never takes a runtime Anthropic dependency." artifact-lint is PMO-judgment → **own**.
- *Harvest at design time:* the skill harvests *structure* (SKILL.md anatomy, `references/` plural convention, failure-mode discipline) from `skill-creator` at design time via the upstream-reference catalog — a recorded divergence with a drift-check cadence, NOT a runtime call.
**Blocks:** #1165 Stage 5 design (the posture sets whether the skill embeds a runtime call) and Stage 6 authoring path.
**Upstream Compatibility:** **REQUIRED (touches skill-authoring surface).**
- *Anthropic skill-creator convention (cited):* `skill-creator` produces frontmatter `name` + `description` only — `upstream-reference-catalog.md` `skill-md-frontmatter` entry (`upstream_required: name, description`; `upstream_citation: skill-creator/SKILL.md:1-4`; `last_verified_date: 2026-06-11`, commit `353ad8b`). Body anatomy = H1 title + body sections (`skill-md-body-anatomy` entry). References dir = `references/` **plural** (`skill-references-directory` entry, `upstream_required: references/ (plural)`).
- *Verdict:* **aligned + diverged-with-rationale (the PMO extensions are catalog-registered, not conflicts).** `artifact-lint`'s SKILL.md will carry the upstream `name`+`description` baseline AND the PMO extensions: `version:` (governing `version-field-semantics.md`), the `## Domain-Specific Failure Modes` section ≥3 entries (governing `failure-mode-standard.md`, enforced at G7), and `references/work-plan-taxonomy.md` (plural dir — upstream-aligned). No `**CONFLICT.**` — the PMO additions are catalog-registered extensions over a stable upstream baseline, not redefinitions of it.
- *Catalog staleness check:* the cited catalog entries were `last_verified` 2026-06-11 (8 days before this plan, today 2026-06-19) — **within the 90-day window**, no staleness flag needed.
- *Sourcing posture (per ADR-023):* **own.** Blast-radius × commodity-stability justification: high blast radius (operator-facing recommend gate) × non-commodity (PMO-judgment surface) ⇒ own-only. ADR-023 holds the rule; this line cites it.
**Reversibility / Confidence:** **MODERATE / HIGH.** Reversal (own → wrap) would be a superseding ADR + re-point of the registry/D-Gate citation + skill rework; no data migration. HIGH confidence — ADR-023's bright line for judgment/operator-facing skills is unambiguous.

**Decision-discipline note (Mechanisms applied):** all four D-decisions are schema-change / architectural / D-class per the `decision-discipline.md §3` triage table → M1 (Localization) + M2 (Opposing View) + M3 (Pattern Cache Scan) all apply. **M1** rendered inline per decision (the localization that overturns the generic "cross-milestone blocker = defer" heuristic is the load-bearing D-Dependency content; the localization that picks the lower-churn name is D-NameReconcile). **M2 (strongest opposing view):** the highest-stakes opposing view is on D-Dependency — "the substance match is superficial and #205/#201 are real blockers"; it is dismissed with cited disconfirming evidence (scope disjointness + present 5-state machine + `source_inputs` absence being irrelevant), and the residual is escalated to the operator as the explicit (A)/(B) gate — i.e., the opposing view is *surfaced for operator ratification*, not silently buried (honest-uncertainty per G4). **M3 (Pattern Cache Scan):** as a spawned spoke I cannot read the operator-instance auto-memory observation log / confirmed-pattern store (operator-instance Layer 2, not in this worktree; `decision-discipline.md §4` also forbids engineering pre-population and requires per-write operator approval). Checkable patterns I *can* scan — the persona's own anti-pattern set and the hub-verified findings — surface two directly applicable cautions: the *audit-snapshot reconciliation* sub-mechanism (§2.1.1) — applied: I verified all 6 findings against live `git`/`grep`/`gh` rather than trusting the brief's snapshot, with file:line evidence; and the *localization-failure* confirmed-pattern theme — applied: the generic capacity/dependency heuristics were localized against platform context before each decision. Per G7 (load-bearing trumps completeness) and G4, the honest report is: **no operator-confirmed cache entry is readable from this spoke context; the two checkable patterns above were applied.** Operator/hub should run the full M3 scan against the auto-memory store at the Decision Briefing.

---

### Release Class

**Proposed class: `novel`** (see D-ReleaseClass for trigger evidence + differentiation posture).

```
## Release Class

Class: novel
Rationale (max 2 sentences): #1165 introduces a NEW skill (operations/skills/artifact-lint/) and #334 adds new schema fields to frontmatter-schema.md — novel trigger (a) "introduces a new reference doc, schema, or skill"; the release also carries 4 D-class decisions — novel trigger (b). No cross-cutting trigger fires (touches 2 standard/schema docs + 1 new skill dir + deploy.sh registration, below the ≥3-governance-surface / ≥3-pipeline-stage threshold).
Differentiation posture:
  - Engagement density: Standard
  - Stage 9 review depth: Deep
  - Stage 5 activation bias: ALL          (OPTIONAL)
  - Stage 13 outcome-window: 30-day        (OPTIONAL)
```

**Differentiation consequences to carry forward:** Deep Stage 9 (Collective Review N-way consistency + cross-D upstream-compat scan — all 4 D-decisions get the explicit cross-D check; design-spec conformance; blast-radius assessment). Stage 5 ALL activation bias ⇒ activate Stage 5 for both cards even where triggers are borderline (the carried #334 Stage-5 deferrals + the `_archived/` design note make design questions real). This class section is **absent from milestone #195's description today** — Stage 3 never declared it; recommend the hub author the `## Release Class` H2 into the milestone description at Phase B1 acceptance (a first declaration, not a re-classification).

---

### Recommendations

**Delivery & rollback strategy (required — no plan without it):**
- **Topology: single branch (D-C SINGLE).** Both cards are strictly serial children of one split, file contention is near-zero, and #1165 hard-depends on #334. A single release branch (`release/v2.08-artifact-lineage-and-generated-surface-governance`, name provisional) carrying both cards in sequence is correct; Option-A multi-branch topology buys nothing here (no parallelism to isolate) and would add merge-ordering overhead against the same two docs. PR-review IS the dry-run governance gate (Claude Code path per CLAUDE.md "No ungoverned changes").
- **Merge/split:** do NOT split into two releases — #1165 would ship against schema fields that don't exist on main, breaking its AC-4 source-citation. Keep as one release, two sequenced commits/PRs (or one PR with ordered commits #334-then-#1165). Both children stay open until Stage 13 — **mark #334 and #1165 as closed at Stage 13** (parser-clean phrasing; in the PR these go in the Issue References block).
- **Rollback:** CHEAP. Both changes are additive — new optional frontmatter fields (no existing field changed; `superseded_by`/`SUPERSEDES`/`promoted_from` untouched, AC-6 regression-clean) and a new self-contained skill dir. Rollback = revert the PR; de-register `artifact-lint` from `deploy.sh` arrays; rebuild affected packages. No data migration, no destructive schema change, no consumer breakage (the fields are `Required: NO`). Operator-authorized per RELEASE_PROTOCOL § Rollback.

**Verification plan (release-level):**
- #334: re-run the `frontmatter-schema.md` Validation Checklist (AC-6); grep both docs for the chosen field names (zero orphan aliases — AC-2/AC-3); grep the lineage-vs-`source_inputs` distinction table (AC-4); grep the Sidecar section for the lineage fields (AC-5).
- #1165: run the eval harness (variance + trigger-optimization + cross-skill false-positive); the `_v1.._v4` supersede-chain fixture must yield a supersede-chain proposal + **zero** duplicate flags (last AC); grep SKILL.md for the 5 check names + the autonomy-tier citation + the exclusion section + `## Domain-Specific Failure Modes` (≥3); confirm `./deploy.sh --check` Check 5 (roster), Check 6 (canonical structure), Check 7 (package freshness) green after registration.
- Re-check the open-PR population at Stage 9 entry (R-5 audit-baseline re-check).

**Version (provisional, do NOT pin):** latest tag = `v2.05`; v2.06 (ms 04) + v2.07 (ms 10) in-flight (branches exist, untagged). Provisional version for THIS release = **v2.08**, to be **finalized at Stage 12** per re-versioning discipline (re-verify + re-version-up if intervening tags land).

**Out-of-scope discoveries (noted, not actioned — planning only):**
1. **#117 / #369 naming-standard gap is real but unscheduled-here.** `artifact-naming-standard.md` is absent; #369 (its owner) sits in milestone 20, not this milestone. Neither #334 nor #1165 needs it. No action — flag only so a future planner doesn't assume the file exists.
2. **#370 / #201 `_archived/` convention is unformalized** (merged into OPEN #201, ms 15). artifact-lint's version-chain check should treat `_archived/` as directional (recommend-only), not a hard target — carried as R-4 / Stage 5 design note.
3. **#205's `source_inputs` field does not yet exist** in `frontmatter-schema.md` or `lifecycle-states-canonical.md` (0 grep hits) despite #205 being the declared blocker. Not a defect for *this* release (scope-disjoint), but the milestone-15 planner should know the provenance *field* is still unbuilt even though the *container* exists.
4. **Milestone #195 description carries neither a `## Release Class` nor a `## Parallelization Map` section** — it predates those conventions, so the Phase A0 Parallelization-Map currency check is suppressed (correctly) for it. With only 2 strictly-serial cards, a Parallelization Map is moot. Recommend the hub add the `## Release Class` H2 at Phase B1.

**Bottom line for the operator:** approve the strict serial sequence (#334 → #1165), render the 4 D-gates (D-Dependency is the load-bearing one — recommend **proceed / satisfied-in-substance**), accept Release Class **novel**, and authorize single-branch topology. No blockers prevent starting #334 today.
