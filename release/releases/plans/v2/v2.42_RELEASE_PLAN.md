---
version: v2.42
date: 2026-06-29
type: plan
issues: ["#68", "#128", "#432"]
pr: null
links:
  note: null
  log_anchor: "#v2-42-terminology-and-controlled-vocabulary"
reversibility-tier: MODERATE
themes: ["cluster:documentation", "project:knowledge-corpus"]
---

# v2.42-terminology-and-controlled-vocabulary Release Plan

**Milestone:** 34-terminology-and-controlled-vocabulary
**Release Class:** `novel` (operator-rendered at the Stage 4 D-ReleaseClass gate, 2026-06-29). All three `novel` triggers fire — a new ADR (ADR-049), a D-class design decision (D-432-Wording), and a Stage-5-class design record. Differentiation posture: Standard engagement density; Deep Stage 9 review (Collective Review N-way consistency + cross-D upstream scan + blast-radius); Stage 5 activation bias ALL; 30-day outcome window.
**Branch Topology:** `D-C SINGLE` (operator-rendered, 2026-06-29) — one release branch `release/v2.42-terminology-and-controlled-vocabulary`; commits land in dependency order on a shared corpus surface; one release PR carrying each issue's reference in the dedicated Issue References block only (each marked closed at Stage 13). This plan is committed as the first Engineering commit (Commit 0).
**Reversibility:** MODERATE / Confidence MEDIUM — every edit is `git revert`-able governance + reference-doc content (CHEAP per edit; #68 and #128 are purely additive). MODERATE at the release level because #432 ripples canonical vocabulary into the framework + label-taxonomy + a new ADR; runtime blast radius is none (no code consumes the terms). The PR diff IS the dry-run governance gate; git history IS the snapshot (Claude Code path).

> Provisional version **v2.42**. Repo `cody-hutson/pmo-platform`. Milestone 34-terminology-and-controlled-vocabulary. Scope: #68 (glossary refresh — keystone), #128 (tier-disambiguation extension), #432 (initiative/roadmap canonicalization + ADR-049). Concrete version atomic-claimed at Stage 12.

> **Commit-0 version re-verify (2026-06-29):** recomputed next-free against `gh release list` (latest = v2.41) and `release/releases/RELEASE_LOG.md` (latest entry = v2.41). **v2.42 is next-free — no collision.** ADR re-verify: highest existing = ADR-048; **ADR-049 is the next gap-free number** (`check-adr-numbers.py` will pass). Proceeding.

---

## Stage 4 Release Plan — 34-terminology-and-controlled-vocabulary (v2.42)

> Transcribed from the operator-approved Stage 4 plan-of-record. Three same-branch delivery slices canonicalize the platform's terminology contract.

### Determinations (operator-rendered / recorded)
- **D-ReleaseClass:** `novel` — all three `novel` triggers fire (new ADR-049 + D-432-Wording design decision + Stage-5-class ADR); zero `cross-cutting` triggers (0 `pipeline/stage-*.md` files; 0 named governance-surface files — `core/CLAUDE.md.template` is a template mirror, not the CLAUDE.md governance surface).
- **D-Version:** **v2.42** — rule-computed next-free after v2.41 (minor bump: additive capability, no breaking change); re-verified FREE at Commit-0. Binds atomically at the Stage-12 merge.
- **D-C Branch Topology:** **SINGLE** — one branch `release/v2.42-terminology-and-controlled-vocabulary`, one PR, one merge gate (aligns with `milestone = one PR`). FILE-level Stage-6 serialization mechanically enforces #68-before-#432 on the co-edited glossary.
- **D-432-Wording:** Canonical wording rendered at the Stage 5 Collective Review (2026-06-29) — **I-1** (Initiative = multi-milestone grouping theme; not a hierarchy level — a cross-milestone grouping label, never a container tier or `parent_ref` target) + **R-1** (Roadmap = architected path across milestones; may span one or more initiatives — one-per-initiative is the default, not a definitional limit). Routing: the new ADR-049 IS the governance record (satisfies the glossary's Adding-new-terms requirement). Tracker mapping: Initiative maps to the live `epic:*` / `project:*` grouping-label namespaces, NOT a separate Projects field.

### Dependency Graph (acyclic DAG)
- Hard edge: `#68 → #432` (file + design — both edit `core/specs/terminology-glossary.md`; #68 establishes the frontmatter + `{#term-*}` anchor convention + Appendix-A schema that #432's canonical term rows + Appendix-B fix build on).
- Soft edge: `#68 → #128` (sequence-only — #128 edits `core/specs/autonomy-tiers.md` + `core/CLAUDE.md.template`, disjoint from the glossary; recorded build order, not a content block).
- `#128 ⟂ #432` — INDEPENDENT (disjoint files).
- Dropped: phantom edge `#14 → #432` (validated dropped 2026-06-27 — #14 is external-user onboarding, zero terminology coupling).

### Implementation Sequence (dependency order)
1. **#68** (glossary refresh — keystone) → 2. **#128** (tier-disambiguation extension — disjoint no-contention slice) → 3. **#432** (initiative/roadmap canonicalization + ADR-049 — builds last on #68's restructured glossary).

Commit plan (D-C SINGLE, dep order):
- **Commit 0** — this release plan (`chore(v2.42): Stage 4 — release plan (Commit 0)`).
- **Commit 1** — #68: glossary frontmatter + Role tier-anchoring + 4 actor terms (Hub/Spoke/Skill/Sub-agent) + Appendix-A "agent" overload row + Total-terms → 23.
- **Commit 2** — #128: Hierarchy Tier row + collision note + OOS prose + count cascade in `autonomy-tiers.md`; `core/CLAUDE.md.template` L310 cross-ref (2nd "four"→five).
- **Commit 3** — #432: canonical `{#term-initiative}` + `{#term-roadmap}` + Appendix-B rewrite + Total-terms → 25; framework canonical-scope note; label-taxonomy dead-row reconciliation; ADR-049 authored + registered.

### File Change Matrix
```
core/specs/terminology-glossary.md             edit   #68    (frontmatter + Role tier-anchor + 4 actor terms Cat 2 + Appendix-A agent-overload row + Total-terms 19→23)
core/specs/terminology-glossary.md             edit   #432   (canonical term: Initiative + term: Roadmap in Cat 6 + Appendix-B L249 rewrite + Total-terms 23→25 + categories framing reword)
core/specs/autonomy-tiers.md                   edit   #128   (Hierarchy Tier row + namespace-overlap collision note + OOS prose for 4 named conventions + count FOUR→FIVE L114/L125 + frontmatter disambiguates_from)
core/CLAUDE.md.template                         edit   #128   (L310 2nd "four"→five + Hierarchy Tier parenthetical; PRESERVE the 1st "four" = Autonomy's own 0-3)
core/standards/initiative-roadmap-framework.md  edit   #432   (additive canonical-scope note after the §2.1 diagram-fence + inline clause at the "one initiative" line; surgical — one-initiative narrative preserved)
core/specs/label-taxonomy.md                   edit   #432   (§Initiative Labels: cite canonical term on the grouping-mechanism line; replace 3 dead initiative:* rows with live epic:*/project:* grouping labels; delete the unregistered-sibling note)
core/ADRs/ADR-049-canonical-initiative-roadmap-vocabulary.md  add  #432  (NEW ADR — canonical vocabulary + initiative:→epic:/project: label mapping)
core/ADRs/README.md                            edit   #432   (register ADR-049)
```

**Conditional / verified no-op:** `core/governance/OPERATIONS.md` — #128 AC names "cross-refs updated in CLAUDE.md and OPERATIONS.md" for any convention *renamed or re-clarified*. This release renames nothing (Hierarchy Tier is *added*); OPERATIONS.md carries no Tier Disambiguation Table and no convention-count line → **no edit** (stated per AC, not silent).

### Contention Map
| Contended file | Issues | Class | Resolution |
|---|---|---|---|
| `core/specs/terminology-glossary.md` | #68, #432 | within-release, complementary line-ranges | **Sequence**: #68 commits frontmatter + actor-terms + Appendix-A FIRST (Commit 1); #432 commits canonical Initiative/Roadmap terms + Appendix-B rewrite SECOND (Commit 3) against the already-restructured file. Single branch + dep-order commits enforce #68-before-#432. Distinct sections → low merge risk. The Total-terms count line is the one shared edit — #432 reconciles it to the post-#68 base (23 → 25), read live, never assumed. |

**Cross-PR contention (A4 baseline-pin):** Baseline anchored at `main` @ v2.41 (2026-06-29), clean working tree. No open PR touches the 8 matrix files at baseline. Per audit-baseline discipline: a sibling release touching `terminology-glossary.md`/`autonomy-tiers.md`/`label-taxonomy.md`, or claiming ADR-049 / the v2.42 slot after this baseline, silently invalidates this finding — re-checked at Commit 0 (FREE) and re-checked again at the Stage 12 atomic claim.

### Risk Register
1. **Glossary co-edit collision (#68 ∥ #432).** *Owner:* Engineering. *Mitigation:* dep-order commits (#68 first) on the single branch; distinct sections; the count line is computed from the post-#68 live base, not hardcoded.
2. **#432 inherits a sharpened premise the body predates (`initiative:*` retired).** *Owner:* #432 author. *Mitigation:* authored from live label state (0 `initiative:*`, 8 `epic:*`, 9 `project:*`, 0 `area:*`) — NOT the stale body Evidence; ADR-049 states the `initiative:`→`epic:`/`project:` mapping by namespace role.
3. **AC `area:*` imprecision.** *Owner:* #432 author. *Mitigation:* `area:*` has 0 labels and never existed; the live grouping namespaces are `epic:*` + `project:*`. The reconciliation + ADR use the correct namespaces; flagged.
4. **CLAUDE.md AC targets an un-PR-able surface (#128).** *Owner:* #128 author. *Mitigation:* the git-editable surface is `core/CLAUDE.md.template` (L310); the workspace-root `CLAUDE.md` is operator-instance, not PR-editable. Edit the template; operator re-syncs the root file.
5. **ADR-049 / v2.42 slot contention.** *Owner:* Commit-0 re-verify + Stage 12 atomic claim. *Mitigation:* both re-confirmed FREE at Commit 0; CI `check-adr-numbers.py` HALTs on a duplicate/gap; v2.42 re-verified via `gh release list` + RELEASE_LOG.
6. **Rollback complexity — MODERATE (#432).** *Owner:* operator (Stage 12). *Mitigation:* single-PR `git revert` un-does all three slices atomically; runtime blast radius none.
7. **Regression — silent content loss.** *Owner:* Stage 7/8 doc-conformance. *Mitigation:* assert preservation — #68 keeps the 19 existing terms + Appendix-A's 5 resolutions; #128 keeps the 4-row baseline + critical-inversion warning; #432 keeps Appendix-B's other rows. Grep-verify counts before/after.

### Verification Plan (AC greps, run on the release branch)
- #68: `grep -cE '\{#term-(hub|spoke|skill|sub-agent)' core/specs/terminology-glossary.md` ≥ 4; `head -20` shows the `---` frontmatter block with the 5 keys; `awk '/^### term: Role/,/^### term: Persona/' … | grep -c 'autonomy-tiers.md\|Autonomy Tier'` ≥ 2; Appendix-A data rows = 6.
- #128: `grep -ci 'hierarchy tier' core/specs/autonomy-tiers.md` ≥ 1; `grep -niE '\b(four|FOUR)\b'` shows L114/L125 → FIVE/five and L20 preserved; `core/CLAUDE.md.template` L310 reads "five" (2nd) + "four" (1st preserved).
- #432: `grep -c '{#term-initiative}' core/specs/terminology-glossary.md` ≥ 1; `grep -c '{#term-roadmap}'` ≥ 1; Appendix-B Initiative row no longer asserts "not modeled"; `ls core/ADRs/ADR-049*` exists + README registration present; `check-adr-numbers.py` gap-free; no `initiative:(project-data|template|knowledge|skills-mini)` dead rows remain in label-taxonomy.
- Robust term count after Commit 3: `grep -cE '^### term:.*\{#term-' core/specs/terminology-glossary.md` = 25.

### Rollback Strategy
Single-PR git-revert posture (D-C SINGLE → one merge commit). Revert the single merge commit on `main` to atomically un-do all three slices including ADR-049 + its README registration (`check-adr-numbers.py` stays gap-free — 049 was highest). If rollback occurs after the Stage 12 atomic v2.42 claim, the tag is immutable lineage; the next release computes next-free above v2.42. Runtime blast radius: none (no code consumes the terms). Partial rollback under SINGLE topology = a forward-fix PR, not history rewrite.
