# Release Plan — v2.24 · 26-corpus-conventions-and-standards-hygiene

**Milestone:** 26-corpus-conventions-and-standards-hygiene (#140)
**Release Class:** novel (operator-rendered — Deep Stage 9 · Standard density · Stage 5 ALL bias · 30-day outcome window)
**Version:** v2.24 (minor bump off the v2.x mainline; tagged) — provisional-display; **claimed at merge** via the Stage-12 atomic ref-CAS claim. Re-verified free at Engineering Commit 0 (2026-06-25): anchor v2.22, v2.23 unclaimed at that time. Re-versioned v2.23→v2.24 at Stage 12 — v2.23 claimed by concurrent release v2.23-functional-people-graph (PR #2020); next-free minor = v2.24 per the defer-to-merge allocation rule.
**Topology:** D-C SINGLE — one release branch `release/v2.23-corpus-conventions-and-standards-hygiene`, one PR; Engineering commits land in implementation-sequence order (#77 first).
**Collective Review scope-lock:** GO 2026-06-25 (Thursday).
**CLAUDE governance edits target:** `core/CLAUDE.md.template` in-repo (root `CLAUDE.md` is operator-instance / out of the in-repo PR for the issues that touch CLAUDE-class governance).
**Status:** This file is Engineering Commit 0 (D-C SINGLE topology); the working reference until it landed was the Stage-4 planning sub-task comment (#1961). The body below transcribes that planning-spoke output verbatim. Per-issue closure is phrased SAFE throughout (this text is transcribed into the PR body downstream; GitHub's auto-close parser is lexical).

---

## Stage 4 Release Planning — 26-corpus-conventions-and-standards-hygiene

> Spoke output for sub-task #1961. Authoritative baseline: `origin/main` @ `e9fc8d3` (`v2.22-1`), re-verified 2026-06-25 (Thursday). Planning only — no implementation, no branches, no repo-file edits. Per-issue closure phrased SAFE throughout (this text is later transcribed into PR bodies; GitHub's auto-close parser is lexical). Platform-config injected by hub (single resolution): `bundle_doctrine_frame=F1`, `release_size_target_pts=15-25`, `default_release_class=novel` (fallback), release-class capacity weights `{routine=1.0, novel=1.15, cross-cutting=1.3, hotfix=0.9}`.

### Summary (30 seconds)
10 open issues, all `project:knowledge-corpus`, near-pure documentation/governance — **no executable surface**. The release has one **blast-radius hub (#77)**: a mechanical `specs/`→`standards/` relocation of two `*-standard*.md` files with a **live-verified 90-file inbound sweep** `[SOURCE: grep 2026-06-25 → 90]`. #77 MUST run first — it rewrites a ref inside `triage-design-rereview.md` (the file #81 edits) and relocates `process-flow-diagram-standards.md` (the file #113 audits). The other 7 issues are mutually independent (4 create net-new docs, 3 are isolated drift fixes). **Recommended Release Class: `cross-cutting`** (the named-governance-surface trigger fires by letter via #77's sweep) with a documented "mechanical-sweep" posture note. **Recommended version: bump-class `major`-floor (new-governance-file rule) → concrete next-free `v2.23` on the v2.x mainline** (v3.x is the excluded orphan). **Recommended branch topology: single-branch** (Engineering Commit 0). **2 stage-skips proposed** (both Stage 5 SKIP on the 3 mechanical drift fixes that already carry reconciled ACs — see matrix). No merge/split recommended; keep all 10 as-is.

---

### Dependency Graph

Directional edges, each grounded in card-body evidence or a live-tree grep (not assertion):

```
#77 ──(must-precede: file-relocation + ref-rewrite)──▶ #81   [evidence: #77 sweep rewrites triage-design-rereview.md:59; #81 edits :218 — same file]
#77 ──(must-precede: relocates audited file)─────────▶ #113  [evidence: #77 moves process-flow-diagram-standards.md specs/→standards/; #113 audits that exact file]

#751 ──(couples-with: umbrella↔facet xref)──────────── #751 self-contained; edits duplicate-source-discipline.md (1-line xref up to the new discipline)  [evidence: #751 AC-4]

#1094 ··(informs, NOT blocks)·· downstream #1090 K1 pack authoring  [evidence: #1094 "Precursor (P0) to #1090" — #1090 is OUT of this milestone]
#171  ··(informs, NOT blocks)·· operator-local analysis/ backfill (out of repo)  [evidence: #171 scope note]

#663  — NO edge to #77  [evidence: km-governance-framework.md NOT in #77 sweep — file-disjoint, live grep 2026-06-25]
#580, #764, #163, #1094 — NO inbound/outbound hard edges (fully independent)
```

**Hard edges (serialize):** `#77 → #81`, `#77 → #113`. Two edges, one source. Everything else is parallel-eligible.
**Soft edges (informational, do not serialize):** `#663 ↔ #113` (both touch `km-governance-framework.md` but in disjoint sections — #113 reads its diagram for the inventory, #663 edits its count narrative at lines 117/147/349; an audit-read does not move the file). `[INFERRED]` from live section analysis.

---

### Implementation Sequence

Dependency-ordered. The hard edges force exactly one ordering constraint (#77 before #81/#113); all else is free.

| Order | Issue | Why here |
|---|---|---|
| **1** | **#77** | Blast-radius hub. Establish final `core/standards/` paths + sweep all 90 inbound refs FIRST, so #81 and #113 rebase against final paths. `[SOURCE: #77 card "Hub sequencing: run #77 first"; live sweep=90]` |
| 2 | #81 | Edits `triage-design-rereview.md` :218 — must follow #77's rewrite of :59 in the same file to avoid a stale-path edit landing on a moved ref. `[SOURCE: #77 coupling note + live line check]` |
| 2 | #113 | Audits `process-flow-diagram-standards.md` at its FINAL `standards/` path — must follow #77 so the inventory cites final paths. `[SOURCE: #113 coupling note]` |
| 2 | #663 | Independent (km-governance count narrative, AC2 only — AC1 resolved upstream). `[SOURCE: #663 prework reconciliation]` |
| 2 | #580 | Independent (skill-deployment.md roster de-hardcode; OPERATIONS_SKILLS=27 live). `[SOURCE: deploy.sh array, 27 entries]` |
| 2 | #764 | Independent (NEW `domain-best-practices/process.md`). |
| 2 | #751 | Independent (NEW `minimal-addition-discipline.md` + 1-line xref); design pre-resolved in issue comment. `[SOURCE: #751 Design-alignment comment 2026-06-25]` |
| 2 | #171 | Independent (NEW `audit-recommendation-status-badges.md` + CLAUDE.md + Stage 13 doc hook). |
| 2 | #163 | Independent (NEW `claude-code-runtime-state.md` + Governance File Map registration). |
| 2 | #1094 | Independent `type:spike` — produces an analysis findings register under `pmo-platform/analysis/`, no corpus mutation. Can run anytime. `[SOURCE: #1094 Documentation Impact: None]` |

**Wave model (for parallel execution):** Wave 1 = {#77} (solo, must complete + merge ref-sweep before dependents). Wave 2 = {#81, #113, #663, #580, #764, #751, #171, #163, #1094} (9-way parallel-eligible after #77 lands). Under single-branch topology these are sequential Engineering spokes on one branch; the wave model governs Stage-5/7/8 spoke parallelism, not Engineering-commit order.

---

### Stage Applicability Matrix

Default = all-apply. This is a documentation/governance release with **no executable surface**, so Stages 7 (Dev Testing) and 8 (QA) REDUCE to doc-conformance grep passes (`check-doc-links.py`, `deploy.sh --check` Checks 14/9/5, ref-resolution greps) rather than code test suites — applied release-wide. Stage 5 (Solutioning) is per-issue: SKIP for mechanical drift fixes whose ACs are already reconciled to live state; APPLY for net-new convention/reference docs and the spike. Stages 6/9/12/13 always apply.

| Issue | S5 Solutioning | S6 Eng | S7 DevTest | S8 QA | S9 Review | S12 Execute | S13 Close | Rationale |
|---|---|---|---|---|---|---|---|---|
| #77 | **APPLY** | APPLY | REDUCE→link-check | REDUCE→`deploy.sh --check` | APPLY | APPLY | APPLY | 90-file mover; needs A3.2 ref-sweep design + structural-blast-radius check; conformance = link-resolution + Check 14/15, not code tests. |
| #81 | **SKIP** | APPLY | REDUCE→grep AC | REDUCE→grep AC | APPLY | APPLY | APPLY | Mechanical drift fix; AC reconciled to live state (line 218 prose) in card. No design uncertainty. `[SOURCE: #81 AC-reconciled note]` |
| #113 | **APPLY** | APPLY | REDUCE→register check | REDUCE→register check | APPLY | APPLY | APPLY | Audit + exemption-register design (classification rules, Adoption-section wiring) is genuine Stage-5 work; inventory live-derived. |
| #663 | **SKIP** | APPLY | REDUCE→grep | REDUCE→grep | APPLY | APPLY | APPLY | AC2-only de-hardcode of 3 count strings; AC1 resolved upstream. Pure reconciliation, no design. `[SOURCE: #663 prework]` |
| #580 | **SKIP** | APPLY | REDUCE→array-vs-doc diff | REDUCE→array-vs-doc diff | APPLY | APPLY | APPLY | De-hardcode roster counts; "derive from arrays" is the card's own prescribed remediation. No design. |
| #764 | **APPLY** | APPLY | REDUCE→section-header diff | REDUCE→cite-not-dup grep | APPLY | APPLY | APPLY | NEW domain doc; cite-not-reinvent structure decision + which surfaces to index = Stage-5 design. |
| #751 | **APPLY** (REDUCED) | APPLY | REDUCE→grep ACs | REDUCE→grep ACs | APPLY | APPLY | APPLY | NEW discipline doc, but design `[ASSUMPTION-CONFIRM]`s already RESOLVED in the issue's Design-alignment comment (home + floor/ceiling). Stage 5 reduces to encoding the resolved design + authoring the binary test. |
| #171 | **APPLY** | APPLY | REDUCE→grep badges | REDUCE→grep hooks | APPLY | APPLY | APPLY | NEW convention doc + protocol-hook placement (which Stage 13 doc to wire) = design. |
| #163 | **APPLY** | APPLY | REDUCE→entry-schema check | REDUCE→xref check | APPLY | APPLY | APPLY | NEW reference doc; placement + catalog-entry schema is a Stage-5 call (card says "exact placement is a Stage-5 call"). |
| #1094 | **APPLY (spike variant)** | APPLY | REDUCE→register-schema check | REDUCE→classification audit | APPLY | n/a-output-is-analysis | APPLY | `type:spike` → **Stage 5 fires the Research-Methodology Design variant** (audit method + register schema are explicitly Stage-5 work per card). Output lands under `pmo-platform/analysis/`, not corpus; Stage 12 deploy is N/A (no skill/governance change to deploy) but Stage 13 closure applies. |

**Stage-skips proposed: 2** (Stage 5 SKIP on #81 and #663). #580 is also a strong SKIP candidate (mechanical de-hardcode) — flagged as **operator-confirm** rather than auto-skipped, because it is the third mechanical fix and the operator may prefer a single Stage-5 "drift-fix cluster" pass; default recommendation is SKIP. Counting only the two unambiguous skips → **2**.

**Stage 7/8 REDUCE (release-wide):** no executable surface → DevTest/QA collapse to the doc-conformance grep battery: `python3 core/deploy/tools/check-doc-links.py` (Pattern A/B link resolution), `core/deploy/deploy.sh --check` (Check 14 governance-link scope, Check 9 rules-mirror, Check 5 roster-drift for #580, Check 18/25 where touched), and per-AC `grep` verifications. This is REDUCE, not SKIP — the gates still run, they just have no code-test dimension.

---

### Contention Map

Which issues share affected files (the resolution surface for A4 file-contention).

| File | Issues touching | Class | Resolution |
|---|---|---|---|
| `release/references/standards/triage-design-rereview.md` | **#77** (rewrites `specs/failure-mode-standard.md` ref at **:59**) + **#81** (edits size-enum prose at **:218**) | **HARD — same file, different lines** | Sequence #77 → #81. Single-branch makes this a clean sequential-commit ordering (no merge conflict if #77 lands first). `[SOURCE: live line check — :59 vs :218]` |
| `core/specs/process-flow-diagram-standards.md` → `core/standards/...` | **#77** (relocates it) + **#113** (audits it) | **HARD — relocation under an auditor** | Sequence #77 → #113; #113 cites the final `standards/` path. `[SOURCE: #113 coupling note]` |
| `core/specs/framework-catalog.md` | **#77** (repoints the `failure-mode-standard` INTERNAL row — AC bullet 3) | single-PR | #77 owns it entirely; no other issue touches it. (#663's AC1 — which DID touch this file's paths — is already resolved upstream, so #663 no longer edits it.) `[SOURCE: #663 prework: framework-catalog dropped from scope]` |
| `core/standards/km-governance-framework.md` | **#663** (count narrative :117/:147/:349) + **#113** (reads its mermaid diagram for the inventory) | **SOFT — disjoint sections, read-vs-write** | No serialization needed. #113 inventories the diagram (read); #663 edits prose counts (write) in non-overlapping regions. Note in Risk Register as a watch-item only. `[SOURCE: live section analysis]` |
| `core/governance/OPERATIONS.md`, `operations/OPERATIONS.md`, `release/governance/release-process.md`, `release/references/how-to/hub-spoke-bridge.md`, `release/releases/RELEASE_LOG.md`, `core/CLAUDE.md.template` | **#77 only** (mechanical path-string rewrites in the 90-file sweep) | single-PR (within #77) | All inbound-ref rewrites belong to #77's atomic sweep. No sibling edits these in this milestone. These are the named-governance surfaces that trip the `cross-cutting` trigger — see D-ReleaseClass. |
| 4 net-new docs (`process.md`, `minimal-addition-discipline.md`, `audit-recommendation-status-badges.md`, `claude-code-runtime-state.md`) | #764 / #751 / #171 / #163 respectively (creates) | none | Net-new files, none in #77's sweep set (live-verified). Zero contention. Fully parallel. |

**Net contention assessment:** exactly **2 HARD edges, both rooted at #77, both resolved by the #77-first sequence**. Once #77 lands, the remaining 9 issues are file-disjoint (1 soft watch-item: #663/#113 on km-governance, non-blocking).

---

### File Change Matrix

Per-issue add/edit/delete intent. (Intent is directional; exact line-level specs are Stage-6 work.)

**#77** — relocate + sweep:
- `git mv` `core/specs/failure-mode-standard.md` → `core/standards/failure-mode-standard.md` (DELETE old path / ADD new)
- `git mv` `core/specs/process-flow-diagram-standards.md` → `core/standards/process-flow-diagram-standards.md` (DELETE old / ADD new)
- EDIT ~88 inbound files repointing `specs/failure-mode-standard` + `specs/process-flow-diagram-standards` → `standards/...` (live count 90 files total incl. the 2 movers; read each file's context before the path edit — regex is not context-aware)
- EDIT `core/specs/framework-catalog.md` — repoint the `failure-mode-standard` INTERNAL row `canonical_doc` cell
- EDIT relative-path links INSIDE the two moved files so they re-resolve from the new `standards/` location

**#81** — EDIT `release/references/standards/triage-design-rereview.md` (§7 prose ~:218 → add `size:XS` to `size:XS/S/M/L/XL`; Trivial-row of tier table admits XS); EDIT `core/specs/label-taxonomy.md` if `size:XS` missing there too (AC-3 conditional)

**#113** — EDIT `core/standards/process-flow-diagram-standards.md` (post-#77 path) Adoption section to record the diagram inventory + exemption register (live inventory = 5 diagram-bearing files: the standard itself + `km-governance-framework.md`, `planning-solutioning-handoff.md`, `universal-vs-localized-context.md`, `release/skills/release-planner/references/release-plan-template.md`); ADD a follow-up retrofit issue ONLY IF a non-conformant diagram is found (reactive)

**#663** — EDIT `core/standards/km-governance-framework.md` (:117/:147/:349 — replace hardcoded "All 21 (17 seed + 4 new)" / "17 seed" totals with a source-of-truth pointer to `framework-catalog.md`)

**#580** — EDIT `core/rules/skill-deployment.md` (§ Tracked Skills count bullet → drop literal per-array counts, point at arrays; § Initial bootstrap roster example → derive from arrays). NOTE: `.claude/rules/skill-deployment.md` mirror is operator-local (untracked) → flagged for operator-side Check-9 sync, NOT in the PR.

**#764** — CREATE `core/standards/domain-best-practices/process.md` (parallel structure to `software.md`/`governance.md`; cite pipeline + discovery/decision/review disciplines by relative path, no rule-body duplication)

**#751** — CREATE `core/disciplines/minimal-addition-discipline.md` (the discipline + named "simplicity" value + binary minimal-addition test + durability floor/ceiling boundary); EDIT `core/standards/duplicate-source-discipline.md` (1-line xref up to the new umbrella). CLAUDE.md pointer is operator-instance → out of in-repo PR scope.

**#171** — CREATE `core/standards/audit-recommendation-status-badges.md` (5 badges + merge-time maintenance protocol); EDIT `CLAUDE.md` "Analysis folder convention" (reference the badge requirement) — NOTE CLAUDE.md is workspace-root Layer-1, governed; EDIT the Stage 13 Close doc (`core/rules/release-process.md` or pipeline Stage-13 doc — verify live path) to wire the badge-update step. Backfill of `analysis/<audit>/recommendations.md` is operator-local, out of scope.

**#163** — CREATE `core/references/claude-code-runtime-state.md` (suggested path; exact placement Stage-5 call) cataloging runtime state surfaces (`~/.claude/backups/` snapshot mechanism as one entry, cadence marked UNKNOWN — do not fabricate); EDIT `CLAUDE.md` § Governance File Map to register the new file

**#1094** — CREATE `pmo-platform/analysis/<audit-name>-YYYY-MM-DD/` findings register (NOT corpus — analysis artifact). NO corpus file edits. Audit targets (read-only): `core/disciplines/`, `core/standards/`, `core/governance/OPERATIONS.md`, the 7 methodology-consuming skills, `core/schemas/`.

**Consolidated machine-readable path list** (repo-relative; one path per line; for downstream Stage 7/8/9 chip extraction — baseline-pin awareness per Procedure 0: this list is the union of declared mover-targets + edits + creates at baseline `e9fc8d3`; the #77 90-file sweep set is enumerated separately below since it is too large to inline accurately without re-derivation at Engineering time):

```
core/standards/failure-mode-standard.md
core/standards/process-flow-diagram-standards.md
core/specs/framework-catalog.md
release/references/standards/triage-design-rereview.md
core/specs/label-taxonomy.md
core/standards/km-governance-framework.md
core/rules/skill-deployment.md
core/standards/domain-best-practices/process.md
core/disciplines/minimal-addition-discipline.md
core/standards/duplicate-source-discipline.md
core/standards/audit-recommendation-status-badges.md
core/references/claude-code-runtime-state.md
CLAUDE.md
core/rules/release-process.md
```

> **#77 sweep set (~88 inbound editing targets + 2 mover destinations) is NOT inlined** — it is too large and MUST be re-derived live at Engineering time via `grep -rlE 'specs/failure-mode-standard|specs/process-flow-diagram-standards' core release operations` (baseline 90 files @ `e9fc8d3`). Inlining a frozen 90-path list would be a staleness target. The Engineering spoke re-derives, edits, and re-runs the grep to 0.

---

### Risk Register

Named risk, owner, mitigation — no passive voice.

| ID | Risk | Owner | Mitigation | Reversibility |
|---|---|---|---|---|
| R1 | **#77 sweep is mid-size and growing** (60→90 files over 11 days). A partial sweep leaves dangling `specs/` refs that `check-doc-links.py` flags as broken-after-move. | Stage-6 #77 spoke | Re-derive the live count at Engineering start; edit ALL inbound; re-run grep to **0** before claiming done; run `check-doc-links.py` + `deploy.sh --check` Check 14 as the exit gate. The card's completion-condition already encodes the grep-to-0 check. | CHEAP (git revert of a doc-only PR) |
| R2 | **#81 lands a stale-path edit** if it runs before #77 — editing `triage-design-rereview.md:218` while :59 still points at `specs/`, then #77's later sweep collides or misses the file. | Hub (sequencing) | Enforce the #77→#81 hard edge in the implementation sequence; single-branch topology makes this a clean sequential commit. | CHEAP |
| R3 | **#113 inventory cites pre-move paths** if it audits before #77 relocates `process-flow-diagram-standards.md`. | Hub (sequencing) | Enforce #77→#113; OR (fallback) have #113 reference the standard by ROLE not path. The card already offers this fallback. | CHEAP |
| R4 | **#663/#113 soft collision** on `km-governance-framework.md` — a diagram retrofit (if #113 finds one non-conformant) could touch the same file #663 edits. | Stage-6 spokes | Sections are disjoint (counts :117/:147/:349 vs the mermaid block); #113's retrofit is reactive-only. If both fire, land #663 first (smaller, deterministic), then #113's retrofit rebases. | CHEAP |
| R5 | **Scope/capacity** — 10 issues, mixed S/M. Effective points (S≈3, M≈8): 5×S + 5×M ≈ 55 raw pts × cross-cutting weight 1.3 ≈ **71 weighted** — FAR above the 15-25 band. | Operator | This is a **documentation release where the band does not map cleanly** (the band is calibrated for skill/code work; 8 of 10 issues are ≤30-line doc edits or single-file creates). The real cost driver is #77's 90-file sweep (1 issue ≈ 60% of the effort), not issue count. Recommend NOT splitting on the band — the issues are capability-coherent (corpus-conventions hygiene) per bundle-composition-doctrine §4 (merge by dep-edges, not points). Flag for operator awareness; see Quota Budget. | n/a (assessment) |
| R6 | **#171 + #163 both edit `CLAUDE.md`** (workspace-root governed file) — #171 adds the analysis-folder badge ref, #163 registers the new file in the Governance File Map. | Stage-6 spokes | Different sections of CLAUDE.md (Analysis-folder convention vs Governance File Map); land sequentially, re-read before each edit per the surgical-edits discipline. Low collision risk but real — track it. | CHEAP |
| R7 | **#1094 spike scope balloon** — a corpus-wide methodology-assumption audit could expand past the 7-skill bound. | Stage-5/6 #1094 spoke | Bound to the methodology-consuming surfaces (7 skills + work-org framework + OPERATIONS.md protocol) per the card's own `[ASSUMPTION-CONFIRM]` scope note; output is a findings register, not remediation (remediation issues carry their own scope). | CHEAP (analysis artifact, no corpus mutation) |
| R8 | **Rollback complexity: LOW across the board.** Entire release is doc/governance — no migrations, no deploy-state, no data. | Operator | Standard rollback = `git revert` the PR(s). #77's mover is the only structurally-interesting revert (re-`git mv` back + re-sweep), but still CHEAP. No skill `.skill` package rebuilds are triggered (no SKILL.md edits except #77's mechanical inbound-ref rewrites — those DO touch many SKILL.md files, so a `.skill` rebuild at release-cut IS required; flag for Stage 12). | CHEAP-MODERATE |

**Rollback strategy (release-level):** doc-only release → `git revert <merge-sha>` restores prior state byte-for-byte. The one nuance: #77 mechanically edits ~30 `SKILL.md` files (inbound-ref path rewrites), so **`.skill` package rebuild at release-cut is mandatory** per skill-deployment.md (Check 7 package-freshness). This is a Stage-12 action item, not a Stage-4 blocker — flagged here so it is not missed.

---

### Quota Budget

**Verdict:** PASS (with WARN watch on the #77 wave)
**Parallel-eligible spokes per parallel stage (from Stage Applicability Matrix):** Stage 5: 7 (the APPLY set: #77, #113, #764, #751, #171, #163, #1094) · Stage 7: 10 (all, but REDUCED to grep passes) · Stage 8: 10 (all, REDUCED)
**Per-spoke cost estimate:** size-bucket band (`[CALIBRATE-AFTER-3]`, no telemetry medians yet): S-issue spoke ≈ 0.3 unit, M-issue spoke ≈ 0.6 unit; #77's Stage-6 spoke is the outlier at ≈ 1.5 units (90-file sweep). Stage 7/8 REDUCED spokes ≈ 0.15 unit each (grep-only).
**Assumed/stated remaining usage-window envelope:** conservative default (operator did not state quota state at hub start) — assume a fresh 5-hour window.
**Estimated cumulative draw % (worst parallel batch):** The worst batch is **not** a wide parallel fan-out (Stage 7/8 REDUCED spokes are cheap); it is the **#77 Stage-6 solo spoke** (the 90-file sweep) which runs SERIAL by the dep graph, not parallel. Worst *parallel* batch = the 7-way Stage-5 wave ≈ 7 × 0.5 avg ≈ 3.5 units. Against a conservative 5-hour envelope, 3.5 units ≈ **<50%** → PASS. The #77 sweep, while heavy, draws serially and does not stack against siblings.
**Routing:** PASS → proceed parallel for the Stage-5/7/8 waves; **WARN-watch the #77 Stage-6 spoke** — it is the single largest draw and should run when the window has headroom (window-aware timing). No batch-split required.
**Note:** Checkpoint B re-validates at every parallel wave at runtime (load-bearing). This is the plan-time Checkpoint-A estimate only. Bands are `[CALIBRATE-AFTER-3]` MEDIUM confidence. STAGGER is a rate-limit-only defense, not a usage-window mitigation.

---

### Operator Decisions (D-Gate)

#### D-ReleaseClass: What Release Class does this release carry?
**Gate input:** Spoke-proposed class + trigger-condition evidence per `release/references/specs/release-class-taxonomy.md`. Live triggers evaluated against the 10-issue scope.
**Gate decision:** Choose between (A) routine, (B) novel, (C) cross-cutting, (D) hotfix.
**Blocks:** Stage 3 Phase B3 milestone-description `## Release Class` authoring; downstream per-class differentiation posture (engagement density, Stage 9 review depth).
**Upstream compatibility:** N/A — Release Class is PMO platform internal taxonomy; no Anthropic upstream surface. Upstream compatibility check does not apply.
**Reversibility / Confidence:** CHEAP / HIGH (re-classifiable later with operator approval per release-class-taxonomy.md Re-Classification Protocol; cheaper-to-stricter is CHEAP).
**Recommendation:** **(C) `cross-cutting`.** Trigger evidence (multiple fire; highest-ceremony wins per multi-trigger resolution `cross-cutting > novel > routine`):
- **`cross-cutting` trigger (b) FIRES** — #77's 90-file sweep touches **≥3 of the named-governance set**: `core/governance/OPERATIONS.md`, `release/governance/release-process.md`, `release/references/how-to/hub-spoke-bridge.md`, and `release/releases/RELEASE_LOG.md` (4 of the 7-name set). `[SOURCE: live grep 2026-06-25]`
- **`novel` trigger (a) ALSO fires** — 4 net-new corpus docs (#764, #751, #171, #163). `[SOURCE: card File Change Matrices]`
- **`novel` trigger (b) fires** — D-class decisions in this plan.
- **Differentiation posture (recommended):** Engagement density **Tight** (cross-cutting default) — BUT with an explicit **mechanical-sweep caveat**: the ≥3 governance-surface touches are *path-string rewrites inside #77's atomic relocation*, not semantic governance edits. This is the `cross-cutting` anti-pattern's neighbor ("a single edit that happens to be in a governance file"), except here it is genuinely ≥3 files. The letter of the trigger fires; the spirit is lighter. **Operator may legitimately override to `novel`** (Standard density) on the rationale that the governance touches are mechanical and the real design surface is the 4 net-new docs (a `novel` signature). I recommend `cross-cutting` to respect the closed-enum trigger as written, and flag the override as reasonable. Stage 9 review depth **Deep** either way (both `novel` and `cross-cutting` map to Deep). Stage 5 activation bias **ALL**; Stage 13 outcome-window **30-day**.

> **OPERATOR-RENDERED (2026-06-25):** Release Class = **novel** (the reasonable override the spoke flagged — Standard density, Stage 9 Deep, Stage 5 ALL bias, 30-day outcome window). The governance touches are mechanical path-string rewrites inside #77's atomic relocation; the real design surface is the 4 net-new docs.

#### D-Version: What version does this release claim?
**Gate input:** Spoke-recommended next-free, computed at recommendation time against AUTHORITATIVE host state (re-ran `git fetch --tags origin` + `git tag --merged origin/main --sort=-v:refname | grep '^v2'` this session). The concrete number is provisional-display; the durable declaration at plan time is the **bump-class**.
**Gate decision:** Operator renders identity — (A) accept spoke-recommended next-free, (B) version-less theme-named milestone (no tag at Stage 12), or (C) operator-specified override.
**Blocks:** release branch name (`release/<slug>`), plan-file path, Stage 12 atomic version claim, any `version:` frontmatter the release writes (the #77 SKILL.md inbound-ref edits do NOT bump those skills' `version:` fields — mechanical path rewrites are not material behavioral edits per version-field-semantics; flag for the Engineering spokes).
**Upstream compatibility:** N/A — version identity is PMO platform internal; no Anthropic upstream surface. Upstream compatibility check does not apply.
**Reversibility / Confidence:** CHEAP pre-Engineering (recommendation only) / MODERATE after Engineering Commit 0 (identity propagates into branch name, plan path) / HIGH confidence on the number itself.
**Recommendation:** **bump-class `major`-floor → concrete next-free `v2.23`**, OR (operator's call) **(B) keep the theme-named `26-...` milestone version-less**. The trade-off, stated explicitly:
- **Authoritative anchor:** `anchor()` on the v2.x mainline = **v2.22** (`git tag --merged origin/main | grep '^v2'` → v2.22 highest; `git describe origin/main` = `v2.22-1-ge9fc8d3`). `v3.20` exists but is the documented **ORPHAN lineage — EXCLUDED** (not reachable from `origin/main`); next-free is computed on the v2.x mainline, never max-tag. `[SOURCE: live tag query 2026-06-25]`
- **Bump-class per the Selection Guide:** "New governance file created → **major**" fires (4 new files). "Reference/Protocol addition → minor" also fires. Highest floor = **major**. BUT the platform's mainline *practice* numbers every release as a `.Y` increment on the v2 line (v2.21→v2.22 were themselves new-file releases). A literal `major` → v3.x would re-enter the orphan-lineage digit. **Resolution:** compute next-free ≥ floor on the **v2.x mainline lineage** → the lowest free slot is **v2.23** (v2.23 is NOT claimed — hub-confirmed + live-verified). The bump-class signal is "major-significance" (new structural files), expressed as the next-free v2.x number per the host-agnostic allocation rule.
- **Trade-off (A v2.23 vs B version-less):** This milestone is **theme-named** (`26-corpus-conventions-and-standards-hygiene`), which is the sanctioned shape for a version-less close-out. Version-less is defensible IF the operator treats this as a hygiene-batch with no public release note. BUT this release ships **4 net-new governed corpus docs + a 90-file structural relocation** — that is durable, user-relevant capability that warrants a tagged release + public notes per the "a real fix gets a tag + public notes" discipline. **I recommend (A) v2.23** (tag + RELEASE_LOG row + public notes), reserving version-less only if the operator explicitly de-scopes to a silent hygiene batch. The number binds atomically at Stage 12 (re-verify next-free at Engineering Commit 0 per the founding defer-to-merge architecture); a sibling release could claim v2.23 in the interim, in which case the Commit-0 re-verify HALTs and the operator re-renders.

> **OPERATOR-RENDERED (2026-06-25):** Version = **v2.23 (minor, tagged)**. Minor bump off the v2.x mainline anchor v2.22; tagged with a RELEASE_LOG row + public notes. Re-verified free at Engineering Commit 0 (this session): anchor v2.22, `git tag -l 'v2.23'` empty → PROCEED.

#### D-C: Branch Topology — single-branch vs Option-A?
**Gate input:** The #77 90-file sweep + the two hard sibling edges (#77→#81, #77→#113) + 9 independent issues.
**Gate decision:** (A) **single-branch** (default; plan committed as Engineering Commit 0; all issues land as sequential commits on one `release/<slug>` branch) vs (B) **Option-A** (per-issue branches + per-issue chore PRs + a dedicated Stage-4 release-plan chore PR before scaffolding).
**Blocks:** Procedure 1 scaffolding shape; plan-file commit mechanism; per-issue PR vs single-PR review surface.
**Upstream compatibility:** N/A — this D-decision does not modify skill-authoring surface. Upstream compatibility check does not apply.
**Design-Principle Conformance:** N/A — no design-principle `scope_predicate` matches a pure branch-topology decision.
**Reversibility / Confidence:** CHEAP / HIGH.
**Recommendation:** **(A) single-branch.** Rationale: (1) the #77 sweep is one atomic mover that MUST land before #81/#113 — single-branch sequential commits enforce that ordering naturally without inter-PR merge-race coordination; (2) the release is doc-only with low rollback complexity (one revert of one merge SHA restores everything); (3) the two hard edges are intra-release sequencing, which single-branch handles as commit order — Option-A's per-issue PRs would force explicit merge-gating between #77's PR and #81/#113's PRs (more coordination, no benefit here); (4) Option-A's value is early-merge of independent issues to reduce a long-lived branch — but the dominant cost here (#77) is serial regardless of topology, so early-merge buys little. The 9 independent issues land as clean sequential commits after #77. **Single-branch is the lower-ceremony, lower-risk choice for this release's shape.**

> **OPERATOR-RENDERED (2026-06-25):** Topology = **single-branch**. One `release/v2.23-corpus-conventions-and-standards-hygiene` branch; this plan file is Engineering Commit 0; all issues land as sequential commits (#77 first).

#### Stage-skip decisions needing operator awareness (not separate gates — surfaced per the Stage Applicability Matrix)
- **Stage 5 SKIP on #81 and #663** (mechanical drift fixes, ACs reconciled to live state). RECOMMENDED — no design uncertainty. Operator may veto if a Stage-5 "drift-fix cluster" pass is preferred.
- **#580 Stage 5: SKIP recommended but operator-confirm** (third mechanical fix; bundling it into a drift-fix Stage-5 pass with #81/#663 is an alternative).

---

### Recommendations

1. **Sequence: #77 first, then everything else.** This is the single load-bearing decision. The 90-file sweep + the two hard edges (#77→#81, #77→#113) make #77 the gating issue. Verified live, not asserted. `[SOURCE: grep 2026-06-25 → 90 files; line-level :59 vs :218 check]`
2. **Release Class `cross-cutting`** (trigger fires by letter via the governance-surface sweep) with a documented mechanical-sweep caveat; **operator override to `novel` is reasonable** if the governance touches are judged mechanical-only.
3. **Version `v2.23`** (bump-class major-floor, next-free on the v2.x mainline; orphan v3.x excluded), tagged + public notes — OR version-less only if de-scoped to a silent hygiene batch. Recommend tagged.
4. **Single-branch topology**, plan as Engineering Commit 0.
5. **No merge/split.** All 10 issues are capability-coherent (`project:knowledge-corpus` hygiene). The bundle is correct as-is per bundle-composition-doctrine §4 (dep-edge coherence, not point-count). The size-band (15-25) does not map to this doc-heavy release — do NOT split on the band (R5).
6. **2 Stage-5 skips** (#81, #663); #580 a third candidate pending operator confirm. Stages 7/8 REDUCE release-wide to the doc-conformance grep battery (no executable surface).
7. **Stage-12 action item (do not miss):** #77 mechanically edits ~30 `SKILL.md` files (inbound-ref path rewrites) → **`.skill` package rebuild at release-cut is mandatory** (Check 7 package-freshness). These are path-only edits, NOT material behavioral changes, so the affected skills' `version:` frontmatter does NOT bump.
8. **Out-of-scope discoveries (noted, not acted on):**
   - The `.claude/rules/skill-deployment.md` mirror (#580) and the CLAUDE.md operator-instance pointer (#751) are operator-local/untracked — they are correctly out of the in-repo PR per each card; flagged for operator-side Check-9 mirror sync post-merge.
   - #1094's output is an analysis findings register (NOT corpus); its follow-on remediation (#1090 K1 pack authoring, methodology-leak fixes) is explicitly downstream and OUT of this milestone.
   - #663 AC1 (the original P2 headline — 10/13 stale `canonical_doc` paths) is **already resolved on live main**; only AC2 (count narrative) remains. The card is correctly downgraded P2→P3. No action needed beyond AC2.

**Verification battery for Stages 7/8 (release-wide, REDUCED):** `python3 core/deploy/tools/check-doc-links.py` (link resolution touching all moved/edited files → 0 broken) · `core/deploy/deploy.sh --check` (Check 14 governance-link scope, Check 9 rules-mirror, Check 5 roster-drift for #580) · `grep -rlE 'specs/failure-mode-standard|specs/process-flow-diagram-standards' core release operations` → **0** (the #77 exit gate) · per-AC `grep` assertions from each card's completion-condition block.
