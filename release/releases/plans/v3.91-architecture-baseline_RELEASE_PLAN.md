<!-- repo-integrity: allow-issue-ref -->
# v3.91 Release Plan — architecture-baseline

Milestone: architecture-baseline (milestone #285) · Release **v3.91** (recorded determination — rule-computed next-free minor; binds atomically at the Stage-12 tag-claim)
Release class: **novel** · Reversibility: **CHEAP–MODERATE**
Topology: **D-C SINGLE** — one release branch, one PR, one merge (plan lands as Engineering Commit 0)
Identity: versioned
Scope: **#877 → #912 → #1097** (Phase-0 spike #3617 landed — homes + affected-files resolved) · linear dependency chain + shared-surface contention → **P0 fully-serial**
Branch base: cut from current `origin/main` `1f9af719` (Stage-4 plan recorded base `62dae553`; origin/main advanced since — v3.89 template release merged at `5fff643a`, and the v3.89 tag + v3.90 branch were claimed in the interim). Prepared 2026-07-24 (Fri); Commit-0 re-verify 2026-07-25 (Sat).

> **Engineering Commit-0 version re-verify (2026-07-25, Saturday).** Recomputed next-free minor against
> **origin tags + branches** (not the RELEASE_LOG tail — the ledger lags because a shipped version's row
> lands in a separate chore PR). `git fetch --tags origin && git fetch origin main` at
> `origin/main = 1f9af719`:
> - **v3.89** — TAGGED (`git tag --list` shows `v3.89`; shipped as *template-system-governance-wave-1*,
>   release PR #3913, merge `5fff643a`, RELEASE_LOG DEPLOYED 2026-07-25). CLAIMED.
> - **v3.90** — BRANCH-CLAIMED (`release/v3.90-blast-radius-scan-correctness`; no tag yet). CLAIMED.
> - **v3.91** — no tag (`git tag --list | grep 3.91` → none) AND no branch (`git branch -a | grep 3.91`
>   → none). **UNCLAIMED = next-free ⇒ PROCEED with v3.91.** `[SOURCE]`
>
> **Version lineage.** The Stage-4 plan comment recorded provisional **v3.89**; that version was claimed by
> the template-system-governance-wave-1 release (tagged + DEPLOYED 2026-07-25). v3.90 was then branch-claimed
> by blast-radius-scan-correctness. The hub-provisional for this release advanced **v3.89 → v3.91** (rule-computed
> next-free over the two interim claims). The binding claim remains the Stage-12 atomic ref-CAS; re-version-up
> is expected under parallel release work. See § Deviation Log D-Version.

## Summary (30 seconds)

- **Serialized build confirmed.** The three build items form a linear dependency chain **#877 → #912 → #1097** *and* share the same edit surface (`architecture-overview.md` touched by all three; `disciplines/README.md` by two). Both axes independently force a **fully-serial** sequence — this is not a parallel wave. Hub hypothesis CONFIRMED.
- **Delivery = one release branch, one PR, one merge gate** (milestone-one-PR model), built in dependency order under **D-Concurrency Posture P0 (fully-serial)** + **D-C SINGLE** topology.
- **All Stages 5–13 apply** to all three issues. Stages 7/8 **apply** (do not skip) — ACs are grep/link/fixture-verifiable, which is exactly the Stage-7/8 surface for corpus releases; #1097 additionally carries a behavioral fixture AC (a real functional test).
- **Release Class — governed check yields `novel`; ratified `novel` at the plan-approval gate** (milestone Class PATCHed routine→novel).
- **Quota Budget: PASS** (serial chain → worst parallel batch effectively 1 spoke).
- **Top risk:** shared-surface contention on `core/disciplines/architecture-overview.md` (all three issues) — mitigated by serial merge-ordering, not parallelism.

## Phase A0 Pre-Plan Gate Results

Cutover-subject (enters Stage 4 on 2026-07-24, after the 2026-04-25 re-review cutover). Results:

| Gate | Result | Note |
|---|---|---|
| **Triage→Design Re-Review** | PASS (no C3) | No premise rejections; the sole D2-column currency finding is #1097's stale paths (Tier 1 [ADJUST], not Tier 0). |
| **G-PL1 — AC/substrate currency** | **1 FINDING → Tier 1 [ADJUST]** | #1097 Evidence + AC quote pre-restructure paths `pmo-platform/releases/notes/` and `pmo-platform/governance/RELEASE_LOG.md`. Verified: those paths are **absent**; live paths `release/releases/notes/` + `release/releases/RELEASE_LOG.md` **present**. #1097's *Affected Files* block already carries the corrected paths; the AC/Evidence prose does not. Build MUST use corrected paths; operator may `gh issue edit --body` #1097 to reconcile the AC prose. Recorded in Risk Register (R-2). |
| **G-PL2 — Pre-plan crisping** | PASS | All three bodies are plan-ready — spike #3617 baked artifact homes + affected-files into each. G1-02/04/05 substantive checks pass at Stage-4 entry. |
| **G-PL3 — Placement forward-check** | **SKIP (correct non-ceremony)** | `git log 62dae553..origin/main --name-status --find-renames` was empty at plan time — no structural reorg merged after base. Nothing to re-home. (Re-checked at Commit-0 against `1f9af719`: interim merges are the v3.89 template release + v3.90 fixes — neither re-homes a #877/#912/#1097 target artifact.) |
| **G-PL4 — Bundle-entry freshness re-verify** | PASS (all admit-still-valid) | Each headline defect still reproduces on pinned `main`: the three target artifacts (`actor-model-and-governance-as-contract.md`, `cross-chain-architecture-map.md`, the conformance audit mode) are all **absent** on `main` — verified. No card admits as already-resolved-on-main. None is drift/reconciliation-class. |
| **Parallelization-Map currency** | PASS | Map present + dated `2026-07-17`, within the bundle→Stage-4 window; no A7 refresh-trigger event since; Tier-A self-unblocking, no in-flight collision. Tier-S re-derivation: no sibling milestone declared a mover intersecting this release's surface (window empty). |
| **Domain classification (A1.5 / A3-time)** | `domain: governance` | Entire File Change Matrix is internal pmo-platform artifacts → **pipeline-internal release, external-sourcing-EXEMPT**. Label: `domain_practice: { source: N/A — pipeline-internal release, date: 2026-07-24, domain: governance }`. Secondary surface: skill-definition (#1097 SKILL.md/mode) — still governance-class (markdown skill def; no application source/tests). |

## Dependency Graph

```
#3617 (spike — Phase 0 landed; homes+affected-files resolved)
   │  unblocks (satisfied)
   ▼
#877  Consolidate actor-model + governance-as-contract  (new discipline doc)
   │  SOFT edge: #912 indexes #877's consolidated baseline "as one entry" (AC4)
   │  HARD edge: #912 + #877 both edit architecture-overview.md AND disciplines/README.md (file contention)
   ▼
#912  Cross-chain architecture index  (new sibling doc; provides the baseline #1097 audits against)
   │  HARD edge: #1097 DEPENDS_ON #912 (audits delivered work against the #912 index baseline)
   ▼
#1097 As-built architecture-conformance audit  (new pmo-qa-auditor Mode I + cadence protocol)
```

Directional edges (evidence-cited):
- **#3617 → {#877, #912, #1097}:** the spike settled each item's artifact home + affected-files (state: landed; Completion condition met). Dependency **satisfied** — build items unblocked.
- **#877 → #912:** SOFT (reference-coherence). #912 AC4 references #877's consolidated doc "without restating"; the index cites #877's output as one indexed entry. Independently reinforced by **file contention** on `architecture-overview.md` + `disciplines/README.md` → serialize regardless.
- **#912 → #1097:** HARD (explicit `DEPENDS_ON`). #1097 audits delivered work against "the architecture baseline (ADRs + architecture-overview + **the #912 index**)". Interim mitigation documented in #1097 (audit against ADRs+overview only) applies *only* if #912 were descoped — it is not; #912 is sequenced ahead in-milestone, so the full baseline exists at #1097 build time.
- **External:** #912 AC5 (gate-wiring) `DEPENDS_ON #779` (**OPEN** — verified). Correctly **DEFERRED** out of scope (cannot wire to a gate that does not yet exist). #1097's health-check feed integrates with #161 (integration point, not a build blocker).

## Implementation Sequence

Strict serial, dependency-ordered, on one release branch:

1. **#877** — author `actor-model-and-governance-as-contract.md`; edit `architecture-overview.md` (cross-ref), `operating-model.md` (link), `disciplines/README.md` (register), `terminology-glossary.md` (actor terms). *First — establishes the baseline the index references.* **← THIS SPOKE (Stage 6, first engineering spoke).**
2. **#912** — author `cross-chain-architecture-map.md`; edit `architecture-overview.md` (link — **second writer**, rebase on #877's edit), `disciplines/README.md` (register — **second writer**), `five-function-spine-and-process-flows.md` (reciprocal pointer). *Second — indexes #877's baseline as one entry.*
3. **#1097** — extend `pmo-qa-auditor` with new **Mode I** (architecture-conformance): SKILL.md + 2 new `references/`; create `architecture-conformance-cadence.md`; extend `per-skill-output-contracts.md`; edit `health-check/SKILL.md` (conformance-dimension feed); edit `architecture-overview.md` (**third writer**, reciprocal pointer). *Last — audits against the #912 index baseline.* Repackage `pmo-qa-auditor.skill` + `health-check.skill`.

**Merge-ordering discipline for the shared files:** `architecture-overview.md` is written in the order #877 → #912 → #1097; each later writer rebases on the prior. `disciplines/README.md` in the order #877 → #912. Because this is one branch built in sequence, contention resolves by build-order, not by merge conflict.

## Stage Applicability Matrix

Per-issue for Stages 5–8 (the per-issue stages); Stages 9–13 are **release-scoped** (run once for the whole PR).

| Issue | S5 Solutioning | S6 Engineering | S7 Dev Testing | S8 QA Testing |
|---|---|---|---|---|
| **#877** | **APPLY** — open design Q's (orchestration-as-skill-vs-agent boundary; ADR-090 deferred at scope-lock; doc structure) | APPLY | **APPLY** (lightweight) — grep-AC verification (all-3-actor terms, per-row citation, mixed-determinism cite, enforcement-shift, seed-doc links) + link-resolution | **APPLY** — per-AC acceptance (7 ACs) |
| **#912** | **APPLY** — index columns/rows design; freshness-check design; gate-wiring boundary (deferred) | APPLY | **APPLY** (lightweight) — grep every chain row present; 3-column population; reference-not-restate; overview reachability | **APPLY** — per-AC acceptance (5 ACs) |
| **#1097** | **APPLY** (design-heaviest) — Mode I spec, dimension rubric, cadence, baseline-set, confidence/severity model, fragmentation-detection method | APPLY | **APPLY (substantive)** — behavioral fixture AC ("two divergent deliveries → exactly one fragmentation finding"); zero-issues-created check; register field-set; release-record enumeration | **APPLY** — per-AC acceptance (8 ACs) |

**Stages 9–13 (release-scoped, all APPLY):** S9 Plan Review (Deep per `novel` Release Class — see § Release Class), S10 Dry Run, S11 Snapshot, S12 Execute, S13 Close (RELEASE_LOG row, CHANGELOG, RELEASE_NOTES, INDEX/DIGEST, **+ `.skill` repackage** for pmo-qa-auditor + health-check).

**No stage skipped.** Rationale for **not** skipping 7/8 on the pure-doc issues (#877/#912): their ACs are grep/link-integrity predicates that *are* the DT/QA surface for corpus releases; a missing cross-link or unregistered doc is a real (navigational) functional defect. Collapsing 7/8 into CI + Stage-9 is the exact anti-pattern the run-the-verification-stages discipline forbids (actually execute the scaffolded Stage 7 Dev Testing + Stage 8 QA; do not self-skip them into CI + the Stage-9 review) — **run the scaffolded stages**. #1097 has unambiguous functional impact (an executable audit mode).

## Contention Map

Shared write-surfaces (the reason the build serializes on file axis, independent of the logical chain):

| File | #877 | #912 | #1097 | Resolution |
|---|---|---|---|---|
| `core/disciplines/architecture-overview.md` | edit (cross-ref) | edit (link) | edit (reciprocal ptr) | **3-way — highest contention.** Serial build-order #877→#912→#1097; each writer rebases on prior. Every #877 edit is **append-only** (no anchor/heading/count mutation) to protect the 31 inbound refs. |
| `core/disciplines/README.md` | edit (register) | edit (register) | — | 2-way. Serial #877→#912; both register into the same foundational-architecture group (watch for duplicate/orphan entries → CIAC-2). #877 edit is **append-only, minimal** to avoid churn ahead of #912's second-writer edit. |
| `core/specs/terminology-glossary.md` | edit (light-touch) | — | — | Single-writer. Light-touch inline disambiguation + Appendix-A(iii) cross-ref; **no `### term:` registration → no Total-terms cascade.** |
| `core/disciplines/five-function-spine-and-process-flows.md` | — | edit | — | Single-writer. |
| `core/skills/pmo-qa-auditor/SKILL.md` (+`references/`) | — | — | edit/create | Single-writer. |
| `core/schemas/per-skill-output-contracts.md` | — | — | edit | Single-writer (existing pmo-qa-auditor entry). |
| `operations/skills/health-check/SKILL.md` | — | — | edit | Single-writer. |

- **Cross-PR / cross-release contention (A4):** window `62dae553..origin/main` was empty at plan time → **zero** external collisions; no open sibling milestone declares a mover intersecting this surface. Commit-0 re-check against `1f9af719`: interim merges (v3.89 template release, v3.90 fixes) do not touch the #877/#912/#1097 target surfaces. Structural-blast-radius sub-audit: clean.
- **Within-release contention:** the two shared files above, resolved by serial build-order (not a blocker under P0/SINGLE).

## Risk Register

| ID | Class | Risk | Sev | Reversibility | Mitigation |
|---|---|---|---|---|---|
| **R-1** | Contention | 3-way edits to `architecture-overview.md` collide if built out of order or in parallel | Med | CHEAP | P0 serial + build-order rebasing; single branch/PR means build-order resolves it, no merge conflict. #877 edits append-only. |
| **R-2** | Scope/currency | #1097 AC+Evidence prose cite stale `pmo-platform/...` paths (G-PL1 finding) | Low | CHEAP | Build uses corrected `release/releases/...` paths (already in #1097 Affected Files). Tier 1 [ADJUST]: operator may `gh issue edit --body` #1097 to reconcile AC prose; deviation-log entry in plan. |
| **R-3** | Dependency | #1097 needs #912's index as baseline; if #912 slips/descopes, #1097 audits against a partial baseline | Med | CHEAP | Both in-milestone, #912 sequenced ahead; #1097 carries a documented interim mitigation (ADRs+overview-only). No action unless #912 descopes. |
| **R-4** | Scope (deferred) | #912 AC5 gate-wiring `DEPENDS_ON #779` (OPEN) | Low | CHEAP | AC5 explicitly **out of scope** this release; index authoring is not blocked. Do not attempt gate-wiring — #779 not shipped. |
| **R-5** | Rollback complexity | 5 new files + 8 edits + 2 `.skill` repackages across core/ + operations/ + release/ in one PR | Low | MODERATE | Single-PR revert is atomic (one merge → one revert). `.skill` packages rebuild deterministically via `build-skill-packages.sh`. No data migration, no runtime state. |
| **R-6** | Scope (novelty) | #1097 ships an executable new skill mode (not just docs) — genuine design + test surface | Med | MODERATE | Stage 5 ALL-activation + substantive Stage 7 (fixture AC). Reinforces the `novel` class ratified below. |

**Rollback strategy:** single release branch, single squash-merge → rollback = one `git revert` of the merge (or delete-branch pre-merge). `.skill` artifacts are regenerated, not hand-edited. No irreversible or stakeholder-facing surface. Overall rollback: **CHEAP–MODERATE**.

## Release Class Declaration

**Governed classification procedure (per `release-class-taxonomy.md` § Class Enum + Classification Procedure) yields `novel`:**

- **`novel` trigger (a) — "≥1 issue introduces a new reference doc, schema, or skill" — FIRES on all three:** #877 creates `actor-model-and-governance-as-contract.md`; #912 creates `cross-chain-architecture-map.md`; #1097 creates a new `pmo-qa-auditor` **Mode I** + 2 new `references/` + a new `architecture-conformance-cadence.md`. (5 net-new files total.)
- **`routine` trigger (a) FAILS** — requires all issues `size:S/M`; #877 and #1097 are `size:L`.
- **`routine` trigger (c) FAILS** — requires "zero new files added"; this release adds 5.
- **Multi-trigger resolution:** `novel` > `routine`.

**Ratified `novel` at the plan-approval gate** (Procedure 0, 2026-07-24): the milestone's prior operator-documented override to `routine` (rationale: *"documentation/index/audit cluster on the architecture-overview surface; no novel capability class"*) was reconciled — the governed trigger is literally *file-introduction*, which fires three times, and the operator PATCHed the milestone Class routine→novel. Reversibility of the reclassification: cheaper-to-stricter = **CHEAP / HIGH** per taxonomy Re-Classification Protocol.

**Posture delta (routine → novel):**

| Dimension | routine (prior) | novel (ratified) | Practical impact |
|---|---|---|---|
| Stage 9 review depth | Standard | **Deep** (blast-radius + design-spec conformance + Empirical Verification) | **Material** — release authors 2 net-new foundational discipline docs + a new executable audit mode; Deep is the safer posture. |
| Stage 5 activation bias | SKIP-where-trivial | ALL | **Moot** — all three have genuine open design questions; Stage 5 activates regardless. |
| Engagement density | Light | Standard | Minor. |

## Operator Decisions (D-Gate Block)

| D-decision | Kind | Rendering | Value / Outcome |
|---|---|---|---|
| **D-Version** | rule-determined | **RECORDED DETERMINATION** (not a click-gate) | **v3.91** — next-free minor over claimed v3.89 (tag) + v3.90 (branch). Provisional advanced v3.89 → v3.91 at Commit-0 re-verify (2026-07-25). Binds atomically at the Stage-12 tag-claim. |
| **D-ReleaseClass** | judgment | **Operator gate — RENDERED** | **`novel`** (triggers fire ×3; milestone Class PATCHed routine→novel). Reversibility CHEAP / HIGH. See § Release Class Declaration. |
| **D-Concurrency Posture** | judgment (recurring) | **Operator ratifies — RENDERED** | **P0 (fully-serial)** + **D-C SINGLE** topology. Driver: hard dependency chain + 3-way `architecture-overview.md` contention. Reversibility CHEAP / HIGH. Force-push on the shared branch prohibited under any non-serial posture (moot at P0). |

*Upstream-compatibility (D-Gate Template):* D-ReleaseClass + D-Concurrency + D-Version — **N/A**, none modifies skill-authoring surface (frontmatter/layout/naming). #1097's Mode I extension *does* touch skill-authoring surface but is decided at Stage 5 (mode spec), not a Stage-4 D-gate; flag for Stage-5 upstream check against `anthropic-skills:skill-creator` (per ADR-023, pmo-qa-auditor is a judgment/stakeholder-facing skill → own posture).

## Quota Budget

**Verdict:** PASS (per `quota-budget-protocol.md` Checkpoint A)
**Parallel-eligible spokes per parallel stage (from Stage Applicability Matrix):** Stage 5: 3 (nominal) · Stage 7: 3 (nominal) · Stage 8: 3 (nominal) — **but realizable concurrency = 1** (the hard chain #877→#912→#1097 + P0 serial posture prevent any two from running the same parallel-safe stage simultaneously: #912's design needs #877's baseline; #1097's needs #912's index).
**Per-spoke cost estimate:** `size:L` → moderate–high band; `size:M` → low–moderate band (heuristic — no telemetry medians yet; `[CALIBRATE-AFTER-3]` MEDIUM).
**Estimated cumulative draw % (worst parallel batch):** worst batch = 1 × `size:L` (moderate–high) → **well under 50%** of any reasonable envelope.
**Routing:** PASS — proceed serial; no window-aware timing needed. Even the *nominal* 3-parallel interpretation is not realizable here (dependency chain caps concurrency at 1), so the parallel-batch draw surface is effectively absent.
**Note:** Checkpoint B re-validates at every parallel wave at runtime (load-bearing) with PROCEED/SERIALIZE/DEFER/REDUCE-scope. Bands + cumulative-draw budget are `[CALIBRATE-AFTER-3]` MEDIUM.

## Cross-Issue Acceptance Criteria

Release-scoped cohesion constraints spanning ≥2 issues (graded at Stage 9 QC3.5 / Phase A3.6 on the merged PR; distinct from per-issue ACs and Stage-5 INT-N):

- [ ] **CIAC-1 (#877 × #912 × #1097 on `core/disciplines/architecture-overview.md`):** the integrated overview links to all three new surfaces (actor-model doc, cross-chain index, pmo-qa-auditor conformance mode) with resolvable links and no duplicate registration. *Method:* `grep -E 'actor-model-and-governance-as-contract|cross-chain-architecture-map|architecture-conformance' core/disciplines/architecture-overview.md` returns ≥1 match per surface, and each linked path resolves. *Graded at Stage 9 QC3.5 on the merged PR.*
- [ ] **CIAC-2 (#877 × #912 on `core/disciplines/README.md`):** both new discipline docs are registered in the foundational-architecture group with exactly one entry each (no duplicate, no orphan). *Method:* `grep -c 'actor-model-and-governance-as-contract' core/disciplines/README.md` = 1 AND `grep -c 'cross-chain-architecture-map' core/disciplines/README.md` = 1. *Graded at Stage 9 QC3.5 on the merged PR.*
- [ ] **CIAC-3 (#877 × #912 × #1097 — baseline→index→audit throughline):** the release's core cohesion constraint holds — the #912 index references #877's consolidated doc as an indexed entry, AND #1097's Mode I spec names the #912 cross-chain index among its architecture-baseline sources. *Method:* `grep 'actor-model-and-governance-as-contract' core/disciplines/cross-chain-architecture-map.md` ≥1 AND `grep 'cross-chain-architecture-map' core/skills/pmo-qa-auditor/references/architecture-conformance-mode-spec.md` ≥1. *Graded at Stage 9 QC3.5 on the merged PR.*

## File Change Matrix

Structurally machine-readable — one path per line; first whitespace-delimited token after the intent marker is the path. Comment lines start `#`. Owning issue(s) in trailing parens. Read-only inputs are listed separately (NOT part of the write-set).

```
# ADD (create — 5)
core/disciplines/actor-model-and-governance-as-contract.md (#877)
core/disciplines/cross-chain-architecture-map.md (#912)
release/references/protocols/architecture-conformance-cadence.md (#1097)
core/skills/pmo-qa-auditor/references/architecture-conformance-mode-spec.md (#1097)
core/skills/pmo-qa-auditor/references/architecture-conformance-dimension-rubric.md (#1097)
# EDIT (8)
core/disciplines/architecture-overview.md (#877,#912,#1097)
core/disciplines/README.md (#877,#912)
core/disciplines/operating-model.md (#877)
core/specs/terminology-glossary.md (#877)
core/disciplines/five-function-spine-and-process-flows.md (#912)
core/skills/pmo-qa-auditor/SKILL.md (#1097)
core/schemas/per-skill-output-contracts.md (#1097)
operations/skills/health-check/SKILL.md (#1097)
# BUILD/CLOSE-OUT ARTIFACTS (release-scoped — Stage 6/13)
release/releases/plans/v3.91-architecture-baseline_RELEASE_PLAN.md (plan)
packages/pmo-qa-auditor.skill (#1097 — repackage)
packages/pmo-qa-auditor.skill.sha256 (#1097 — repackage)
packages/health-check.skill (#1097 — repackage)
packages/health-check.skill.sha256 (#1097 — repackage)
release/releases/RELEASE_LOG.md (Stage 12/13)
CHANGELOG.md (Stage 13)
# DELETE (none)
# READ-ONLY INPUTS (no write — #877 seed docs cited by the new doc; #1097 audit inputs)
core/schemas/gate-evaluation-spec.md (#877 read)
core/schemas/gate-criteria-spec.md (#877 read)
core/standards/agent-script-promotion-framework.md (#877 read)
core/specs/autonomy-tiers.md (#877 read)
core/standards/subagent-security-posture.md (#877 read)
core/ADRs/ADR-019-specialists-compose-not-absorb.md (#877 read — AC3 dual-ground)
release/releases/notes/ (#1097 read — delivered-work record)
release/releases/RELEASE_LOG.md (#1097 read — engineering audit trail)
```

Note: the #877 seed docs are **outbound references from** the new doc (read-only), not edits — listing them as edits would over-scope the write-set. Only `terminology-glossary.md` (light-touch actor-term disambiguation) is an actual #877 edit. #1097's release-record paths are the **corrected** live paths (G-PL1); the stale `pmo-platform/...` forms in #1097's AC prose must not be used.

## Recommendations

1. **Release Class `novel`** ratified at the D-ReleaseClass gate (governed triggers fire `novel` ×3; milestone Class PATCHed routine→novel). Deep Stage 9 over the 2 net-new discipline docs + new audit mode.
2. **D-Concurrency Posture P0 (fully-serial) + D-C SINGLE topology** ratified. Build as **one PR**, delivery slices in dep order #877 → #912 → #1097, one merge gate (the milestone-one-PR discipline: a milestone ships as a single PR and a single merge; issues are delivery slices on one branch, built in dependency order).
3. **No merge, no split.** Each issue is a distinct, cohesive artifact (consolidation doc / index doc / audit mode). #877≠#912 (actor-model scope vs all-chains scope). #1097's mode + spec + rubric + cadence + output-contract + health-check feed are one cohesive capability — splitting would fragment the mode.
4. **Run Stages 5, 7, 8 for real** — do not collapse into CI + Stage 9 (the run-the-verification-stages discipline: execute the scaffolded DevTest/QA stages, do not self-skip them). Stage 5 activates for all three (open design questions); Stage 7/8 verify the grep/link ACs (#877/#912) and the behavioral fixture AC (#1097).
5. **G-PL1 currency fix (Tier 1 [ADJUST]):** build #1097 against corrected `release/releases/...` paths; optionally reconcile #1097's AC prose via `gh issue edit --body`. Log as a plan deviation.
6. **Close-out must repackage** `pmo-qa-auditor.skill` + `health-check.skill` (Check-7 package-drift discipline) and record the new **Mode I** in `per-skill-output-contracts.md`. Stage 13 ships RELEASE_LOG + CHANGELOG + RELEASE_NOTES + INDEX/DIGEST.

**Discoveries outside scope** (surface to hub; do not action here):
- **(a) Stale-path drift in CLAUDE.md governance map + `.github/ISSUE_TEMPLATE/improvement.yml`** — the pre-modular-monolith `pmo-platform/...` paths persist (per #877 Notes (b) and confirmed by the #1097 AC drift). Candidate for a separate governance-hygiene intake; NOT this release's scope.
- **(b) `pipeline/stage-NN.md` "Automation Level — Today/Target" notes lag the live skill roster** (per #877 Notes (a) — e.g., Stage 3/4 cite release-planner as future though Modes A/B ship). Separate intake candidate.
- **(c) #912 AC5 gate-wiring is parked on #779 (OPEN).** When #779 (architectural-fit acceptance gate) ships, a fast-follow can wire the #912 index into the gate spec. Track as a #912 → #779 follow-up edge, not a blocker here.

## Deviation Log

Departures from the Stage-4 plan comment (#3845), captured at Engineering Commit 0 per the write-first-speak-second discipline. Each is a plan-transcription deviation, not a build defect.

**(a) D-Version — provisional advanced v3.89 → v3.91.**
The Stage-4 plan recorded provisional **v3.89**. At Commit-0 re-verify (2026-07-25, Sat), v3.89 is **tag-claimed** (shipped as *template-system-governance-wave-1*, release PR #3913, RELEASE_LOG DEPLOYED 2026-07-25) and v3.90 is **branch-claimed** (`release/v3.90-blast-radius-scan-correctness`). Rule-computed next-free minor over both interim claims = **v3.91**. All v3.89 references in this plan are updated to v3.91; the plan file itself is named `v3.91-architecture-baseline_RELEASE_PLAN.md`.
> *Lineage-note namespace clarification:* the hub/milestone lineage note cites **#275** as the vehicle that tagged v3.89. This resolves in the **milestone** namespace — milestone **#275 = template-system-governance-wave-1** (closed), which shipped v3.89 (release PR #3913, parents #3380/#3381/#2855; RELEASE_LOG DEPLOYED 2026-07-25). The hub attribution is therefore **accurate**; the milestone and issue namespaces are distinct (issue #275 is an unrelated item, so a first-pass issue-namespace check reads as a mismatch — it is not). Reversibility CHEAP / Confidence HIGH `[SOURCE]`.

**(b) Collective-Review scope-lock decisions (amend the Stage-5 design as-built for #877).**
- **ADR-090 — NOT authored (deferred).** The Stage-5 design D-1 recommended a thin ADR-090; the adversarial review (PR-2 / CD-1 Option B) challenged it as altitude-inflation over open decision D1 (OD-11 "no big-bang keystone"). **Scope-lock decision: NO ADR-090** — nothing is ratified to anchor it. The actor-model adoption is carried as reversible target-state prose inside the discipline doc only.
- **Target-state framing (not current-state).** The new doc is framed **forward-looking / target-state**; it carries a `## Status & Scope — target-state` section and a frontmatter `status:` that does **not** assert the model is current/ratified (addresses adversarial FM-2). Forward-looking normative claims are made self-marking inline so framing travels with grep-addressable sentences.
- **AC3 dual-grounding (adversarial PR-1, HIGH — applied).** "Judgment stays with free agents, permanently" is grounded on **BOTH** the mixed-determinism rule (`agent-script-promotion-framework.md` — the script-promotion axis) **AND** ADR-019 "specialists compose, not absorb" (the actor-allocation axis: judgment must not be absorbed into a bounded specialist skill). The falsification condition carries both clauses.
- **Terminology — light-touch (edit D primary).** No `### term:` registration → the "Total terms" figure is unchanged → **no Cascade-Sweep**. `orchestrator` is disambiguated **inline** in the new doc (3-way overloaded: human orchestrators / Hub session / ADR-019 Organizer-Orchestrator function-skill) rather than minting a 4th canonical sense; a 1-line glossary Appendix-A(iii) cross-ref points to the new doc.
- **Entity-count untouched.** The #877 edits do **not** touch the disciplines/README entity-count (that reconcile is #912's on README:39). README + architecture-overview + operating-model edits are **append-only + minimal** to protect the 31/37 inbound refs and avoid churn ahead of #912's second-writer edits.

**(c) CHEAP build directives (why the deliverable stays CHEAP / git-revertible).**
Markdown-only across all #877 targets; every shared-file edit append-only (no anchor/heading/count mutation); no ADR decision-of-record minted; light-touch glossary (no term-count cascade). The whole #877 deliverable is **CHEAP / Confidence HIGH** — single `git revert` of the merge undoes it, no runtime state, no data migration.

**(d) Substrate currency (Stage-5 M1.1, carried forward).** All 6 seed-doc line citations drifted upstream (content intact — `currency-reconciled-upstream`); relates-to epics #573/#284/#365 are CLOSED (design-neutral — a closed epic strengthens the consolidation premise). #877 is built against **current state**; seed-doc citations use section anchors / re-verified lines, not the stale line numbers in the issue body (ADR-062: issue body stays historical record).

**(e) #1097 scope-lock decisions (Collective Review — applied as-built).** CD-A baseline inversion (ADR corpus primary, cross-chain index secondary); fragmentation kept full-scope as a candidate-grade honest heuristic (no deterministic-detection claim); confidence/severity made two-factor orthogonal (severity never diluted by low baseline confidence; `no-governing-baseline` cap bounds severity, a separate volume control bounds count); F2 fix (health-check consumes a committed in-repo conformance-summary surface, not the git-ignored folder). Markdown + additive Mode I only; `.skill` repackage is a mechanical build step. CHEAP–MODERATE / Confidence HIGH.

**(f) Pre-existing package-drift (inherited — NOT #1097 scope, flagged not fixed).** `deploy.sh --check-package-freshness` reports 5 unrelated skills stale (delivery-engine, pmo-process-designer, pmo-skill-refiner, release-planner, eval-writer) — the known recurring Check-7 baseline-drift cluster, inherited from the branch base, WARN-mode (non-blocking). This release rebuilt ONLY its two in-scope packages (pmo-qa-auditor, health-check), which are fresh. The 5 stale packages are out of the #1097 file-change matrix and are left to their own tracked hygiene owner.

## Change Description

### Outcome

This release ships the platform's **architecture-baseline** capability: a consolidated cross-chain architecture index, a target-state actor-model + governance-as-contract statement, and — the operator-facing headline — a standing **as-built architecture-conformance audit** (`pmo-qa-auditor` Mode I) that reads the release record and audits *delivered* work against the platform's own architecture, catching conformance-drift and the cross-release fragmentation the forward per-ticket architecture-fit gate is structurally blind to. The audit is observational (a dated audit folder plus a committed conformance-summary surface that health-check consumes) and auto-files nothing.

### Issues resolved

| # | Outcome | Status |
|---|---|---|
| #877 | Actor-model + governance-as-contract target-state discipline doc (forward-looking; no ADR minted; append-only shared-file edits) | DONE |
| #912 | Cross-chain architecture index — every management chain mapped to its governing data model / flow / holding gate (references, does not restate) | DONE |
| #1097 | `pmo-qa-auditor` Mode I as-built architecture-conformance audit — 2 new references (mode-spec + dimension-rubric), a 4th audit-cadence axis, the health-check conformance feed, the output-contract reconcile, and the seeded behavioral fixture | DONE |

### Key decisions

- **D-ReleaseClass = novel** — the governed trigger (an issue introduces a new reference doc / schema / skill) fires on all three slices; milestone Class PATCHed routine→novel at the plan-approval gate.
- **D-Version = v3.89 → v3.91** — provisional v3.89 was tag-claimed and v3.90 branch-claimed in the interim; rule-computed next-free minor = v3.91.
- **ADR-090 deferred (not authored)** — the actor-model adoption is carried as reversible target-state prose in the discipline doc; nothing is ratified to anchor an ADR (adversarial CD-1).
- **#1097 CD-A** — the ADR corpus is the PRIMARY conformance baseline (per-decision, unit-matched to per-delivery conformance); the cross-chain index is a SECONDARY routing aid. Addresses the altitude mismatch and de-risks the index dependency.
- **#1097 honest-heuristic fragmentation (kept full-scope)** — cross-release fragmentation ships as a candidate-grade, ADR-citation-bounded heuristic with a stated confidence bound; the behavioral fixture is seeded so counting is testable in isolation. No deterministic-detection claim.
- **#1097 confidence/severity + F2** — severity and baseline-confidence kept orthogonal (a HIGH-severity divergence under a LOW-confidence baseline is never diluted; the `no-governing-baseline` cap bounds severity while a distinct aggregate + load-bearing-surface gate bounds volume); health-check consumes a committed in-repo conformance surface so the AC-7 feed is not satisfied vacuously off-instance.

### Reversibility

**CHEAP–MODERATE / Confidence HIGH** — content-only across 13 files (markdown + an additive Mode I + two rebuilt `.skill` packages); a single `git revert -m 1` of the release merge restores prior behavior (Mode I is additive, no mode renamed or removed, the contract reconcile is a superset edit). #1097's executable audit-mode surface is the MODERATE element; no data migration.

### Downstream impact

- The audit-cadence family is now **four axes** — architecture-conformance joins process-fitness / structural / Anthropic base-vs-build; future cadence work cross-references all four.
- health-check `full` now surfaces a platform-context conformance flag from the committed summary surface — the durable producer→consumer seam (Mode I produces; health-check composes, never re-runs).
- The forward architecture-fit gate (Stage-2, deferred pending #779) gains its retrospective complement; when that gate lands, the two compose.
- Two `.skill` packages were rebuilt in-PR (pmo-qa-auditor, health-check) with refreshed `.sha256` baselines; they redeploy via `deploy.sh` at release-cut so the deployed skill copies carry Mode I + the conformance feed.
- The pre-existing 5-skill package-drift cluster (Deviation Log (f)) is inherited and WARN-mode; it is out of this release's scope and left to its own hygiene owner.

### Cross-references

- Release plan (this file): top of `release/releases/plans/v3.91-architecture-baseline_RELEASE_PLAN.md`.
- Milestone: architecture-baseline (#285) — https://github.com/cody-hutson/pmo-platform/milestone/285.
- User-facing release notes (authored at Stage 13): `release/releases/notes/v3.91_RELEASE_NOTES.md`.
