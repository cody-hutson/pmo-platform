<!-- repo-integrity: allow-issue-ref -->
# v2.04 Release Plan — Close-out Registers (ADR-032 design + combined register template)

> **Release:** v2.04 (FINAL — reclaimed the original abandoned v2.04 slot at Stage 12; verified free: no v2.04 tag exists) · **Milestone:** `62-close-out-registers` (milestone 154) · **Release Class:** `cross-cutting` (Tight engagement / Deep Stage 9 / Stage 5 ALL / 30-day outcome window).
> **Topology:** D-C SINGLE (one consolidated release branch, write-serialized; all cards land on `release/v2.10-close-out-registers`, one release PR, merged at Stage 12).
> **Survey baseline:** branched from `origin/main` at the current HEAD (post `#1493` merge). · **Authored:** 2026-06-20 (Engineering Commit 0).
> **Reversibility:** CHEAP/MODERATE — this release ships a NEW ADR (design-only) + a NEW combined register template + a `stage-13-close.md` §6 edit; `git revert` of the merge restores prior state. The #1412 corpus *migration execution* is DEFERRED to a follow-up issue (filed at Stage 13).

> **Version note (final).** This plan was scoped as `v2.07` at Stage 4 (when v2.03 was the latest tag), then re-versioned **up** to a provisional `v2.10` at Engineering Commit 0 (v2.07/v2.08 had shipped, `v2.07_RELEASE_PLAN.md` was taken by a *different* release — Ambient Intake Automation, `10-ambient-intake-automation` — and two `release/v2.09-*` branches were in flight). At the **Stage 12 Execute gate (2026-06-20)** the operator decided to **reclaim this milestone's original abandoned `v2.04` slot** — v2.04 was the held v2.04-close-out-registers attempt (PR #1407 never merged) and `git ls-remote --tags origin v2.04` confirmed **no v2.04 tag exists** (slot free). This is a deliberate backwards-version reclamation (numerically below the shipped v2.05–v2.08); the operator accepted the `git describe` non-monotonicity per the no-versionless-as-hedge + decide-on-merits discipline. The release branch name `release/v2.10-close-out-registers` is retained for traceability only (deleted at Stage 13 cleanup); the final tag, plan filename, PR title, and commit scope all carry **v2.04**.

## 1. Release Outcome (goal-conformance target for Stage 9)

**AFTER** — The release corpus has a settled, ratified architecture decision (ADR-032) classifying every release-corpus surface PUBLIC-capability (S1) vs OPERATOR-INSTANCE-content (S3) per ADR-017, resolving #1412's 5 open questions and stating the register-placement rule. A single combined close-out **register template** ships PUBLIC under `release/references/templates/`, with `stage-13-close.md` §6 naming the register and pointing its *filled* per-release output at the operator-instance path. The corpus *migration execution* is captured as a complete-by-construction follow-up issue, ready for its own dedicated release.

**BEFORE** — The release corpus is half-migrated: `deploy.sh` Check 4 already declares `RELEASE_LOG.md` operator-instance and 28 references use the `<OPERATOR_INSTANCE_RELEASE_LOG_PATH>` token, yet the files (RELEASE_LOG + INDEX + DIGEST + 38 notes + 35 plans) are still tracked and the toolkit hardcodes the in-repo `release/releases/` paths. There is no architecture-of-record for the split, and the two close-out register templates (#360/#361) have no decided home.

**Success Indicator (Stage 8 + Stage 9 gate):** `core/ADRs/ADR-032-release-corpus-public-vs-instance-split.md` exists, status Accepted, CD-3-corrected (`${CLAUDE_WORKSPACE_ROOT:-$HOME/Claude}` canonical, not the orphan `PMO_INSTANCE_PATH`); `core/ADRs/README.md` carries the ADR-032 catalog entry; `python3 release/tools/check-adr-numbers.py` → PASS; the combined register template exists PUBLIC and `stage-13-close.md` §6 names it; PR body is parser-clean.

## 2. Scope — 3 issues, two work-classes, one decided dependency

| Issue | Capability | Verdict | Persona |
|---|---|---|---|
| **#1412** | Release-corpus public-vs-instance split — **DESIGN ONLY** this release (ADR-032 + deferred-migration plan) | NEW ADR + README append | Engineering — ADR authoring (this sub-task) |
| **#360** | PMBOK 7 lessons-learned register | NEW template (combined) | Engineering — template + `stage-13-close.md` §6 |
| **#361** | Kerth retro register | NEW template (combined; same file as #360) | Engineering — template + `stage-13-close.md` §6 |

**Operator decisions (rendered at the Stage 4 plan-approval gate, 2026-06-19):**

| D-Gate | Decision | Note |
|---|---|---|
| **D-1412-Scope** | **Design #1412 here, defer migration** | #1412's full public-vs-instance DESIGN (ADR) is produced this release (resolve its 5 open questions + corpus classification + register-placement rule); the corpus migration execution (`.gitignore` + toolkit re-point + history) is deferred to a follow-up issue/milestone. #360/#361 build onto the decided layout. |
| **D-RegisterShape** | **One combined template** | Techniques achieving the same outcome are grouped. Single `release-learnings-register-template.md` with a PMBOK 7 lessons block + a Kerth retro block. |
| **D-Right-Sizing** | **Full ceremony** | Full Kerth (Prime Directive + 3-question + ritual-of-closure) + full PMBOK 7 (Situation / Outcome / Lessons / Next-cycle). |
| **D-ReleaseClass** | **cross-cutting** | #1412's architecture decision binds downstream toolkit/migration; warrants Deep Stage 9 + Tight engagement. Re-classifiable per `release-class-taxonomy.md`. |

**Deferred (follow-up issue, filed at Stage 13):** the #1412 corpus migration execution (`.gitignore` + `git rm --cached` + move + toolkit re-point + bootstrap). The full migration blast radius is enumerated in ADR-032's deferred-migration plan and in the Stage 5 Solutioning output, so the follow-up issue is complete-by-construction.

## 3. Dependency Graph

Directional dependencies (`A → B` = B depends on A):

```
#1412 (register-placement rule only, = ADR-032 Decision 5)
   │
   ├──→ #360  (lessons-learned template — where does template + filled content land?)
   └──→ #361  (retro template       — where does template + filled content land?)

#360 ⇄ #361  (sibling co-author edge — DEFAULT-COMBINED; both edit stage-13-close.md §6)

(full #1412 migration: .gitignore + toolkit re-point)
   └──→ NO blocking edge to #360/#361 — they need the *rule*, not the *migration*
```

- **#1412 → #360 / #1412 → #361 (hard, but narrow):** #360/#361 cannot finalize their template home until #1412's register-placement rule is decided. **Decided** ≠ **migrated** — the rule ("register template ships public; filled per-release content writes to the operator-instance path") is a one-line classification (ADR-032 Decision 5), rendered without executing the migration.
- **#360 ⇄ #361 (bidirectional co-author):** Both edit `stage-13-close.md` §6 and reconcile the same learnings-triple; they Solution and Engineer together as ONE combined template.
- **Full #1412 migration → (nothing in this milestone):** heavyweight, EXPENSIVE, deferred.

## 4. Implementation Sequence (D-C SINGLE, write-serialized)

```
ADR (#1412 design)  →  combined register template (#360 + #361)
```

1. **Operator rendered D-gates at the Stage 4 plan-approval gate** (2026-06-19) — design #1412 here, defer migration; one combined template; full ceremony; cross-cutting.
2. **Engineering Commit 0** — create the release branch + commit this release plan file.
3. **Commit 1 — #1412 ADR (this sub-task #1415).** Author the ADR (rebased to the next free number after a sibling claimed 029) + append the README catalog entry. NO migration execution; NO `.gitignore`/`deploy.sh` edit.
4. **Commit 2 — #360 + #361 combined register template (next Engineering spoke, #360/#361 sub-task).** Author one `release-learnings-register-template.md` PUBLIC under `release/references/templates/`; edit `stage-13-close.md` §6 to name the register + state its relationship to the `#### Release Learnings v<X.Y>` H4 block; filled register writes to the operator-instance path per ADR-032 Decision 5.
5. **Stages 7/8/9/12/13** per the matrix below. Per-issue closure happens at Stage 13.

## 5. Stage Applicability Matrix

Stage 5 is **all-or-nothing per release** — any in-bundle issue firing any trigger routes the WHOLE release through Solutioning.

| Issue | T1 new-file | T3 structural-design | S5 Solutioning | S6 Eng | S7 DevTest | S8 QA | S9 Review | S12 Execute | S13 Close | Rationale |
|---|---|---|---|---|---|---|---|---|---|---|
| **#1412** | ✓ | ✓ | **ACTIVATE** | ✓ | compress | compress | ✓ (Deep) | ✓ | ✓ | New ADR file; the public-vs-instance classification IS a structural design decision (5 open questions). Design-only this release → S7/S8 compress to presence + `check-adr-numbers.py` + parser-clean verification (no runtime surface; the migration that carries regression risk is deferred). |
| **#360** | ✓ | ✓ | **ACTIVATE** | ✓ | compress | compress | ✓ (Std) | ✓ | ✓ | New template file; the learnings-triple reconciliation (extend/replace/coexist) is real design. S7/S8 compress to presence + section-grep + one dogfood filled register. |
| **#361** | ✓ | ✓ | **ACTIVATE** | ✓ | compress | compress | ✓ (Std) | ✓ | ✓ | Co-authored with #360; same compression. |

**Release-level Stage 5 verdict: ACTIVATE** (completed — Stage 5 Solutioning produced ADR-032 + the register-placement rule + the deferred-migration plan).

## 6. Risk Register

| ID | Risk | Severity | Mitigation | Reversibility |
|---|---|---|---|---|
| **R1** | **#1412 blast radius** if the migration were folded in (touches `.gitignore` + toolkit + corpus + possibly history). | HIGH | **Split #1412 into design-only (this release) + a deferred execution issue** (the operator's D-1412-Scope decision). This release commits only the ADR + README append + the template. | CHEAP (design); MODERATE (deferred migration) |
| **R2** | **ADR-number collision.** A sibling spoke may claim the next ADR number during this release's engineering window. | MEDIUM | The executing spoke runs `check-adr-numbers.py`, takes the next free gap-free number, and updates title/filename/README. (Materialized: 029 was claimed by the Memory-SSOT ADR; this release uses ADR-032.) | CHEAP |
| **R3** | **Version collision.** The provisional version may collide with shipped tags / committed plans / in-flight branches. | MEDIUM | Re-decide at Engineering Commit 0 / Stage 12 against live tags. (Materialized: v2.07/v2.08 shipped + v2.07 plan taken + two v2.09 branches → re-versioned to a provisional v2.10; then at the Stage 12 gate the operator reclaimed the original abandoned **v2.04** slot — re-verified free via `git ls-remote --tags origin v2.04`.) | CHEAP |
| **R4** | **Learnings-triple duplication.** A register authored without reconciling the live `#### Release Learnings v<X.Y>` H4 block creates a parallel close-out surface. | MEDIUM | Stage 5 reconciliation mandatory; the §6 schema edit names the extend/replace/coexist relationship. | CHEAP |
| **R5** | **Cross-PR contention.** `release/v2.06-*` / `release/v2.09-*` branches exist; a sibling touching `stage-13-close.md` would collide with the #360/#361 §6 edit. | LOW (split) | Re-run the cross-PR overlap audit at Stage 9 + Stage 12 against live branch diffs over `stage-13-close.md`. Baseline pinned at this Commit-0 SHA. | CHEAP |

## 7. Recommendations

1. **SPLIT disposition adopted** (operator D-1412-Scope): ADR-032 design this release; migration deferred to a complete-by-construction follow-up issue filed at Stage 13.
2. **Release Class: `cross-cutting`** (operator-rendered) — Deep Stage 9, Tight engagement.
3. **Version finalized at Stage 12** against live tags — operator reclaimed the original abandoned `v2.04` slot (verified free; no v2.04 tag).
4. **Stage 5 ACTIVATED** for this release (completed — ADR-032 + register-placement rule).
5. **S7/S8 compress** for the design-only ADR + the template-only register work; the regression-bearing migration is deferred.
6. **Re-run the cross-PR overlap check at Stage 9 and Stage 12** against live `release/v2.06-*` / `release/v2.09-*` branch diffs over `stage-13-close.md`.

---

### Issue References

- `#1412` — Release-corpus public-vs-instance split (design-only this release; closes at Stage 13).
- `#360` — PMBOK 7 lessons-learned register (combined template).
- `#361` — Kerth retro register (combined template).
- `#1413` — Stage 4 release plan + decision record (this plan's source).
- `#1414` — Stage 5 Solutioning (ADR-032 design source).
- `#1415` — Stage 6 Engineering sub-task (this ADR commit).
- `#322` — Ambient Intake Automation (the *different* release holding the `v2.07` plan filename — the version-collision evidence).

*Plan author: Stage 6 Engineering spoke (Engineering Commit 0). All per-issue closure happens at Stage 13. Version FINAL — `v2.04` reclaimed at Stage 12 Execute (original abandoned slot; verified free against live tags).*
