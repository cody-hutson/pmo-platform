# v3.47 Release Plan — 23-tracker-comms-session-config (milestone #191)

> Durable release-plan record for milestone `23-tracker-comms-session-config` (#191, Epic #1154 PDA-Interface, Release Class `novel`). Transcribes the operator-approved Stage-4 plan (sub-task #2812, approved 2026-07-01). Binds at Stage 12 per the repo versioning lineage.
>
> **Version re-provisioning (2026-07-01, Stage-6 version gate):** originally planned as **v3.45**; v3.45 shipped to another milestone (42-pipeline-skill-doc-reconciliation — published release + signed tag) and v3.46 is claimed by an in-flight release (`release/v3.46-intake-governed-file-movement`). Per the authoritative-version-selection rule (next-free over tags ∪ in-flight claims), this release re-provisions to **v3.47** — a rule-determined recorded determination, not a gate. It binds atomically at Stage 12 (re-version-up on any further collision; never version-less as a hedge). All prior `v3.45` references in this milestone's pipeline artifacts supersede to **v3.47**.

<!-- reference-durability: allow-version-ref -->
<!-- reference-durability: allow-link -->

---

## Stage 4 Release Planning — 23-tracker-comms-session-config

**Operator-approved 2026-07-01** (SINGLE topology · sequence #234->#247->#241->#239->#233 · Release Class novel · provisional v3.47).

> **Stage 4 (Release Planning) output.** Release-scoped across all 5 issues. READ-ONLY planning artifact — no code changes, commits, or GitHub mutations were made producing this.
> **Repo:** cody-hutson/pmo-platform · **Milestone:** #191 · **Epic:** #1154 (PDA-Interface) · **Project:** project-data-architecture
> **Provisional version:** v3.47 (recorded determination — see § Operator Decisions). **Release Class:** novel (operator-rendered at readiness). **Bundle frame:** F1. **Raw size:** 12 pts (S+S+S+S+M).

---

## Summary (30 seconds)

Five bundled cards ship one capability: **operational-tracker + comms + session-config interface hardening** on the PDA-Interface epic. The scope is a governance/skill-logic release — no application code — that (a) defines a net-new dual-format document model spec (#234), (b) reformats the RAID Log container CSV→agent-native Markdown against that model (#233), (c) adds 2-day/5-day SIOR-conformant escalation behavior on the existing Communications Tracker fields (#239), (d) moves CORRECTIONS.md to project scope + adds a cross-project pattern detector (#241), and (e) adds a per-project rotation-focus field to SESSION_STATE.md (#247).

**Sequence:** one hard internal edge (#234→#233); the other three cards are independent. Critical chain length = 2 (#234→#233). **Topology:** SINGLE release branch, one PR, one merge (milestone = one PR). **Stage 5 Solutioning: ACTIVATE release-wide** (novel class + net-new spec + open design fork + skill-logic edits). **Quota:** PASS (worst wave 5 parallel size-S/M spokes, well under 50% of the usage-window envelope). **Two governance touches** (#241 + #247 both edit CLAUDE.md Session Management) each require their own `improvement.yml` + plan-approval per "No ungoverned changes" and raise Stage-9 review depth. **Two file-contention pairs** to serialize within the branch. Rollback: MODERATE for the release as a whole (one governance-touching PR revert); #247 alone is CHEAP.

**Decisions needing operator judgment at the plan gate:** (1) D-C Branch Topology (recommend SINGLE); (2) the two `improvement.yml` governance intakes for #241/#247 (CLAUDE.md touch) — confirm they are filed before Engineering; (3) D-ReleaseClass differentiation posture (confirm `novel`). D-Version is a recorded determination, not a gate.

---

## Dependency Graph

Directional edges (source → target means "target must land before source"; i.e., arrow points to the blocker's dependent):

```
#234 (dual-format model spec)  --blocks-->  #233 (RAID CSV->MD container)
#239 (comms escalation)         -- independent --
#241 (CORRECTIONS.md project-scope + detector)  -- independent --
#247 (SESSION_STATE rotation field)  -- independent --
```

- **Internal edges (within this release):** exactly ONE hard edge — `#234 → #233`. #234 defines the dual-format document model; #233 *instances* it (the RAID source ↔ CSV/Confluence stakeholder rendering uses #234's translation-map seam, not a bespoke path). Confirmed from #233 AC#5 + #234 AC#3 and the #1154 sequencing note (#234 → #233). This is a HARD build-order edge, not a soft coupling.
- **#239 / #241 / #247 are independent** — no blockers, no internal edges to each other or to #234/#233. Their contention is a merge-serialization concern (handled in § Contention Map), not a dependency.
- **External live deps:** ZERO. No card in this release is blocked by an open issue outside milestone #191.
- **DAG property:** acyclic. Single edge #234→#233; three isolated nodes.

---

## Implementation Sequence

Layer order per bundle-composition-doctrine §9 (foundation → infrastructure → skill-core → eval) + §3 Step 6 internal sequence, with the CPM critical chain called out.

**Layer classification (from live issue labels):**
- `layer:foundation` → **#247** (SESSION_STATE field)
- `layer:infrastructure` → **#241** (CORRECTIONS.md relocation + detector)
- `layer:skill-implementation` → **#234, #233, #239**

**Build sequence (dependency-ordered commits on the single release branch):**

| Order | Card | Layer | Rationale for position |
|---|---|---|---|
| 1 | **#234** | skill-implementation | Head of the critical chain. Net-new governing spec (`dual-format-document-model.md`) that #233 instances — MUST precede #233. Its design outcome informs #233's Stage-5 fork. |
| 2 | **#247** | foundation | Foundation-layer, independent. Smallest/CHEAPEST card (single additive field). Sequenced early to land the foundation touch before the CLAUDE.md-contending #241. Governance-touch (CLAUDE.md) — improvement.yml gate. |
| 3 | **#241** | infrastructure | Infrastructure-layer, independent. Sequenced after #247 to resolve the CLAUDE.md Session Management contention deterministically (247 lands its field, 241 rebases onto it). Governance-touch — improvement.yml gate. |
| 4 | **#239** | skill-implementation | Skill-core, independent. Behavior-only on existing Tracker 2 fields; composes with the (existing) SIOR protocol. No contention with the other four. |
| 5 | **#233** | skill-implementation | Tail of the critical chain. Instances #234's model; must land after #234. Also touches tracker-schemas.md (contends with #234 — resolved by this ordering, since #234 lands first). |

**Critical chain (CPM longest-path over the DAG):** `#234 → #233` — length **2**. The other three cards (#247, #241, #239) are off the critical path; their sequencing is driven by (a) layer order and (b) contention-pair de-confliction. Under SINGLE-branch topology every card is a serial commit regardless.

---

## File-Change Matrix

One path per line, add/edit/delete intent. `[CONTENTION]` marks a path touched by >1 card. **CLAUDE.md path note:** the tracked, committable realization of "EDIT CLAUDE.md" in this repo is `core/CLAUDE.md.template` — the depersonalized source that resolves to the operator-instance workspace-root `CLAUDE.md` at install via `setup-workspace.sh`. Workspace-root `CLAUDE.md` is git-ignored / operator-instance (parallel to SESSION_STATE.md). The Session-Management edits below land in `core/CLAUDE.md.template`.

```
# ===== #234 — Define dual-format document model =====
core/standards/dual-format-document-model.md                     ADD     net-new governing spec (source def + per-target translation-map schema + version/drift rule)
core/ADRs/ADR-064-dual-format-document-model.md                  ADD     ADR: dual-format governing model + artifact-generator executor selection (rejects comms-writer / both)
core/schemas/tracker-schemas.md                                  EDIT    [CONTENTION #233] cite existing RAID-Confluence instance as PoC (reference, not duplicate)
core/disciplines/project-entity-model.md                         EDIT    honor Artifact entity §4/9 reconciliation seam (domain + content_lifecycle_pattern); bind model to Artifact entity
operations/skills/artifact-generator/SKILL.md                    EDIT    translation-map executor (dual-format render mode + orphan-guard failure mode)

# ===== #247 — SESSION_STATE.md per-project rotation field =====
core/CLAUDE.md.template                                          EDIT    [CONTENTION #241][GOVERNANCE] Session Management: rotation-focus field + derived weeks-in-focus (focus_since anchor)

# ===== #241 — CORRECTIONS.md project-scope + cross-project detector =====
core/CLAUDE.md.template                                          EDIT    [CONTENTION #247][GOVERNANCE] Session Management + Context File Hierarchy + Governance File Map: project-scope CORRECTIONS read at session start (RETAIN-NARROWED)
operations/skills/weekly-status-rollup/SKILL.md                  EDIT    add §3.x cross-project correction-recurrence detector (N>=2 projects -> improvement candidate via EXISTING intake path)

# ===== #239 — Comms escalation (2-day auto-draft / 5-day SIOR) =====
operations/skills/comms-writer/SKILL.md                          EDIT    2-day rule: PENDING RESPONSE + Response due elapsed >=2 bd -> auto-draft follow-up
operations/skills/daily-status/SKILL.md                          EDIT    5-day rule: same item >=5 bd -> SIOR-block escalation flag in daily status output

# ===== #233 — RAID Log agent-native surface (OPTION B — retain CSV) =====
core/schemas/tracker-schemas.md                                  EDIT    [CONTENTION #234] Tracker 5: keep Format CSV, add agent-native-surface annotation + #234-model cross-ref; 14-col table byte-unchanged
operations/skills/tracker-manager/SKILL.md                       EDIT    document native read/write (no csv module on agent path) + export-via-map
core/schemas/raid-log.schema.json                                NO-CHANGE  container unchanged -> csv.DictReader bridge correct -> schema NOT re-authored
```

**Contention pairs (2):**
1. `core/CLAUDE.md.template` (Session Management) — **#241 + #247**. Both edit the same governance section. Resolution: sequence #247 (order 2) before #241 (order 3); #241 rebases onto #247's additive field.
2. `core/schemas/tracker-schemas.md` — **#233 + #234**. #234 cites RAID-Confluence (reference); #233 annotates Tracker 5 §Structure (write). Resolution: #234 (order 1) before #233 (order 5); aligns with the hard dependency edge; different regions of the file.

**Soft contention (resolved):** `operations/skills/comms-writer/SKILL.md` — #234 + #239. #234's Stage-5 executor decision routed to **artifact-generator** (ADR-064), NOT comms-writer, so the soft overlap is **eliminated** — #239 is the sole editor of comms-writer.

---

## Stage Applicability Matrix

Per-issue applicability for Stages 5–13. Default = ALL apply. Novel class biases Stage 5 = ALL.

| Issue | S5 Solutioning | S6 Eng | S7 DevTest | S8 QA | S9 Review | S10 Pre-Deploy | S11 Deploy | S12 Execute | S13 Close |
|---|---|---|---|---|---|---|---|---|---|
| #234 | ✓ | ✓ | ✓ | ✓ | ✓ (raised) | ✓ | ✓ | ✓ | ✓ |
| #233 | ✓ | ✓ | ✓ | ✓ | ✓ (raised) | ✓ | ✓ | ✓ | ✓ |
| #239 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| #241 | ✓ | ✓ | ✓ | ✓ | ✓ (raised — CLAUDE.md gov) | ✓ | ✓ | ✓ | ✓ |
| #247 | ✓ | ✓ | ✓ | ✓ | ✓ (raised — CLAUDE.md gov) | ✓ | ✓ | ✓ | ✓ |

- **No stage skipped for any card.** Novel class + governance touches + a live design fork mean the full pipeline applies.
- **Stages that must actually RUN (not collapse into CI):** S7 DevTest and S8 QA per card.
- **Raised Stage-9 depth** on #234/#233 (spec + design fork) and on #241/#247 (Tier-1 CLAUDE.md governance touch).

---

## Contention Map

| # | Path | Cards | Class | Resolution |
|---|---|---|---|---|
| 1 | `core/CLAUDE.md.template` (Session Management + Context File Hierarchy + Governance File Map) | #241, #247 | Governance write-write | Sequence #247 (order 2) → #241 (order 3). #247 lands the additive rotation-field semantics; #241 rebases onto it for the session-start-read + corrections-scope update. Both are governance touches → each files its own improvement.yml. |
| 2 | `core/schemas/tracker-schemas.md` | #233, #234 | Read (234 cite) + Write (233 §Structure annotate) | Sequence #234 (order 1) → #233 (order 5). Aligns with the hard dependency edge; #234's RAID-Confluence citation lands first, #233's Tracker-5 §Structure annotation rebases onto it (different region). |
| 3 (soft) | `operations/skills/comms-writer/SKILL.md` | #234 (routed away), #239 | Resolved | #234's executor is artifact-generator (ADR-064) — the overlap is eliminated; #239 is the sole editor. |

**Under SINGLE-branch topology all contention resolves by commit order** — each commit builds on the branch with prior slices applied.

**Cross-PR (external) contention:** re-check at Stage 9 Phase A6.5 and Stage 12 Phase A.5. Baseline pinned at plan time; a later open PR touching these paths silently invalidates the no-collision finding (audit-baseline discipline).

---

## Risk Register

| ID | Risk | Reversibility | Conf | Mitigation |
|---|---|---|---|---|
| **R1** | Dependency risk — #234 outcome under-specifies #233's design fork. | MODERATE | HIGH | Build #234 first (order 1); Stage 5 solutioned #234's translation-map seam concretely (`source.container` fork variable) BEFORE #233's fork was decided. Critical-chain integrity is the control. |
| **R2** | Contention pair — CLAUDE.md template (#241 + #247). | MODERATE | HIGH | Serialize #247→#241 (orders 2,3). SINGLE-branch commit order resolves at build time. Both additive to distinct sub-parts (rotation field vs. corrections-scope read). |
| **R3** | Contention pair — tracker-schemas.md (#233 + #234). | MODERATE | HIGH | Serialize #234→#233 (orders 1,5). #234 appends a RAID-Confluence citation; #233 annotates Tracker-5 §Structure; different regions. |
| **R4** | Governance touch — #241 edits CLAUDE.md template (Tier-1). | MODERATE | HIGH | improvement.yml + plan-approval gate satisfied by this release's plan approval + the Stage-9 PR review (Claude Code path: PR-review IS the dry-run governance gate). Raise Stage-9 review depth. |
| **R5** | Governance touch — #247 edits CLAUDE.md template (Tier-1). | MODERATE / CHEAP (field) | HIGH | Same gate as R4. The rotation field is CHEAP (derivable, additive). |
| **R6** | #233 open design fork (AC#1) — RESOLVED at Stage 5 (Option B — retain CSV; JSON schema is the agent-native surface). | MODERATE | HIGH | Fork closed; Engineering has a determinate documentation-only target. |
| **R7** | #239 SIOR-conformance regression. | MODERATE | HIGH | #239 AC#3 pins SIOR conformance against `sior-escalation-protocol.md`. Stage 7/8 verify the SIOR block structure + warn-and-route-to-PgM fallback. |
| **R8** | #234 orphan-rendering failure-mode AC. | MODERATE | HIGH | #234 AC#6 requires the do-NOT-orphan case (`orphan_guard: reject`). Stage 5 design + Stage 7 verify the orphan-rejection path. |
| **R9** | Cross-PR external collision appears mid-pipeline. | MODERATE | MEDIUM | Stage 9 Phase A6.5 (PRIMARY, HALT-eligible) + Stage 12 Phase A.5 (ultima-ratio). Baseline pinned + documented for reproducibility. |
| **R10** | Version collision at Stage 12. | MODERATE | HIGH | D-Version rule-determined (anchor()+minor-floor→next-free). Re-verified at Engineering Commit 0 (v3.47 free over tags ∪ in-flight). Claim atomically at Stage 12; re-version UP on collision (forward-only, never version-less as a hedge). |

**Rollback complexity per card:** #247 = **CHEAP**. #233, #234, #239, #241 = **MODERATE**.

---

## Operator Decisions (D-Gate Block)

### D-C: Branch Topology — SINGLE vs OPTION-A?
**Decision: (A) SINGLE-branch** — one release branch `release/v3.47-tracker-comms-session-config`, all 5 issues as dependency-ordered commits, one merge gate. Both hard contention pairs resolve automatically as commit order on one branch; the one hard dependency edge (#234→#233) is naturally serialized. **Consequent:** SINGLE ⇒ P0 fully-serial; force-push prohibited on the shared release branch. **Reversibility / Confidence:** CHEAP / HIGH.

### D-Version: What version does this release claim?
**Determination:** **v3.47** — re-provisioned from v3.45 at the Stage-6 version gate (v3.45 shipped to milestone #42; v3.46 in-flight). Next-free over tags ∪ in-flight branches = v3.47. Bump-class = minor (capability release, backward-compatible governance/skill additions). Binds atomically at Stage 12; re-version UP forward-only on any further collision. **Reversibility / Confidence:** CHEAP pre-Engineering; MODERATE after Commit 0 / HIGH.

### D-ReleaseClass: Confirm `novel` + differentiation posture
**Decision:** confirm `novel`. Differentiation: higher engagement density (surface the two governance-intake gates + the #233 fork at the plan gate); RAISED Stage-9 depth for #234/#233 + #241/#247; Stage-5 activation ALL; Stage-13 outcome-window on the dual-format model's first non-RAID instance. **Reversibility / Confidence:** CHEAP / HIGH.

### D-Gov: Confirm the two CLAUDE.md governance intakes are filed
**Decision:** BEFORE Engineering commits the CLAUDE.md template edits, an `improvement.yml` intake exists for (a) #241's Session Management + corrections-management change and (b) #247's Session Management rotation-field semantics — plan approved. **Authorization-scope note:** approving THIS release plan authorizes the 5-card Engineering sequence under SINGLE topology; it does NOT by itself discharge the two CLAUDE.md improvement.yml intakes.

---

## Recommendations

1. **Approve SINGLE-branch topology** (`release/v3.47-tracker-comms-session-config`) and build the 5 cards as dependency-ordered commits in the sequence #234 → #247 → #241 → #239 → #233, one merge gate.
2. **File the two CLAUDE.md improvement.yml intakes (#241, #247) before Engineering** and get plan-approval.
3. **Stage 5 owns #233's design fork (AC#1)** — RESOLVED as Option B (retain CSV; JSON schema is the agent-native surface) — and the #234 executor choice (artifact-generator).
4. **Run Stage 7 DevTest + Stage 8 QA per card — actually run them**, do not collapse into CI + Stage-9 review.
5. **No cross-milestone relocations.** All 5 cards are correctly homed in #191.
6. **Re-check external contention at Stage 9 Phase A6.5 and Stage 12 Phase A.5.**
7. **Confirm `novel` differentiation posture.**

---

## Rollback Strategy

**Release-level posture: MODERATE.** Under SINGLE-branch topology the release is ONE PR + ONE merge, so rollback is a single revert of the milestone merge commit — atomic, clean, un-lands all 5 cards together.

**Rollback triggers:** a Stage 9 NO-GO, a post-merge regression surfaced in hypercare, or a governance-review objection to the CLAUDE.md changes.

**Per-layer rollback notes:** #247 (CHEAP — additive derivable field); #234 (MODERATE — net-new spec + opt-in maps; cannot revert in isolation while #233 is live); #233 (MODERATE→CHEAP under Option B — documentation annotations only; lossless); #239 (MODERATE — additive behavior on existing fields); #241 (MODERATE — governance edit + additive detector). CLAUDE.md governance edits revert with the single milestone-merge revert; record the revert per "No ungoverned changes."

**Rollback reversibility declaration:** MODERATE for the release. No IRREVERSIBLE or EXPENSIVE action; no data loss. Confidence: HIGH.
