<!-- repo-integrity: allow-issue-ref -->
<!-- reference-durability: allow-link -->
---
version: v1.07
date: 2026-06-07
type: plan
issues: ["#506", "#507"]
pr: null
links:
  note: null
  log_anchor: "#v1-07-declarative-workitem-type-model"
reversibility-tier: EXPENSIVE
themes: ["initiative:project-data-architecture", "cluster:work-item-type-layer"]
---

# v1.07-declarative-workitem-type-model Release Plan

**Milestone:** `declarative-workitem-type-model`
**Release Class:** `novel` (Stage 4 spoke-proposed; operator renders at the Phase B3 scope-commit). Basis: two new files (the establishing ADR-017 + a new work-item type meta-schema), ≥3 D-class decisions, and a scoped Tier-2 governance change to the FROZEN entity roster.
**Branch Topology:** `D-C SINGLE` — one release branch `release/v1.07-declarative-workitem-type-model`; sequential Engineering commits; one release PR (shared-release-PR append model). This plan is committed as the first Engineering commit (Commit 0).
**Architecture input:** the Work-Item Type Layer (WITL) ADR ratified on the #409 umbrella (D1 hybrid / D2 methodology-projected / D4 now-scope) — built ON, not re-litigated.
**Differentiation posture (per `novel`):** Engagement density Standard; Stage 9 review depth Deep (Collective Review N-way consistency + cross-D upstream-compatibility scan + blast-radius); Stage 5 activation bias ALL (both issues activate Stage 5); Stage 13 outcome-window 30-day.

**Stage 4 plan content of record:** the Stage 4 Release Planning comment on sub-task #508 (operator-reviewed at the Stage 4 Decision Briefing, 2026-06-07). This file transcribes that comment as Engineering Commit 0.

---

## Summary (30 seconds)

This release is **two issues, hard-sequenced**: #506 (C1 — establish a thin generic `Work Item` entity + the establishing ADR) is the hard predecessor of #507 (C2 — the declarative type-pack meta-schema). Both are **governance/architecture-class**, so **Stage 5 Solutioning is REQUIRED for both** (no skips). The release is **`novel`** (new ADR, new schema file, ≥3 D-class decisions, a scoped Tier-2 governance change). The headline operator gate is the **scoped Tier-2 SCOPE CHANGE** authorizing one new entity on the FROZEN 17-entity roster — surfaced at #506's Stage 9 GO. One shared-file contention edge exists (`project-entity-model.md`, written by C1 then read/overlaid by C2) and is fully resolved by the hard sequence; no parallel Engineering. **Recommended topology: D-C SINGLE.** Capacity is in-scope and appropriately sized — but C2 is the heaviest single design issue in recent releases and is a **likely Stage-6 sub-task-decomposition candidate** (NOT a Stage-4 split). The Architecture Stress-Test surfaces **four real risks** the ratified ADR under-specifies — one of them (the queryable-rollup-vs-EAV-sparseness question, F3) is load-bearing for #506's AC and was closed at Stage 5, not deferred — but **no blocking gap** prevents this release from proceeding.

> **Greenfield verification (2026-06-07):** `ls core/schemas/work-item-type-schema.md` → absent; `ls core/ADRs/ADR-017*` → absent; the generic `Work Item` entity is not present in `project-entity-model.md §4`; no `release/...declarative*` plan file; no release branch. No WITL work has landed; this plan is not stale-input-derived.

---

## Dependency Graph

Directional (`A → B` = "A blocks B / B depends on A"):

```
#506 (C1: generic Work Item entity + ADR-017)
   │  establishes: the entity that C2 parameterizes; the discriminator field
   │               work_item_type; the parent_ref BELONGS_TO edge; the
   │               establishing ADR that records D1/D2/D4
   ▼
#507 (C2: declarative type-pack meta-schema)
      consumes: the generic entity (resolves work_item_type into the type registry);
                references frontmatter-schema §Category 4 (built 7 MVP vocab) — does NOT redefine it;
                composes methodology-parameterization-v1 + ticket-information-architecture
```

**Edge inventory:**

| Edge | Type | Direction | Hardness |
|---|---|---|---|
| #506 → #507 | Hard (entity-existence) | C1 blocks C2 | **HARD** — C2's `work_item_type` discriminator resolves against an entity C1 must have placed in `project-entity-model.md §4` first |
| #506 → ADR-017 | Hard (creation) | #506 creates `core/ADRs/ADR-017-work-item-type-layer.md` | **HARD** (intra-issue) |
| #507 → frontmatter-schema §Category 4 | Soft (reference-only) | C2 references the built 7 MVP types | **SOFT** — read-only dependency on an already-frozen, already-merged surface; no edit, no blocking |
| #507 → methodology-parameterization-v1 + ticket-information-architecture | Soft (compose, then edit) | C2 composes the custom-methodology pattern and unifies the projection table | **SOFT→edit** — C2 reads these as the proven pattern, then edits ticket-IA to unify the projection table (an edit, not a blocking dep) |

**No cross-issue edges beyond #506 → #507.** This is a clean linear chain, not a DAG with branches. The release graph is a 2-node path.

---

## Implementation Sequence

Canonical Engineering order (authoritative for Stage 6 chip routing):

1. **#506 (C1) — FIRST, in full.**
   - 1a. Author `core/ADRs/ADR-017-work-item-type-layer.md` (establishing ADR; records D1 hybrid / D2 projected / D4). Lowest-risk first commit (a new file, zero contention); it pins the architecture the rest of the release transcribes.
   - 1b. Add the generic `Work Item` entity to `core/disciplines/project-entity-model.md §4` (Entity Core 7 + `work_item_type` discriminator + `parent_ref` + `relationships`-by-reference) and the work-item→Milestone rollup edge in §5; **re-freeze** the roster with a dated SCOPE-CHANGE amendment line mirroring the 2026-05-16 RAID-Item precedent.
   - 1c. Add the generic entity's field schema to `core/schemas/entity-field-schemas.md §3` (Core 7 + `work_item_type` + `parent_ref`) and the bounded X-rule(s) in §4. This is the G2 cascade.
   - **Gate inside the sequence:** #506's Stage 9 GO carries the **Tier-2 SCOPE CHANGE authorization** — the entity-model edit (1b) must not be treated as authorized until that GO is rendered. (Engineering may stage the commit on the release branch; the authorization is the Stage 9 gate.)

2. **#507 (C2) — SECOND, only after #506's entity exists on the release branch.**
   - 2a. Create `core/schemas/work-item-type-schema.md` (the type-pack meta-schema — the heart of the release).
   - 2b. Wire `work_item_type` resolution + per-kind field overlay into `project-entity-model.md` / `entity-field-schemas.md` (small, additive edits to the surfaces C1 just created).
   - 2c. Unify the `ticket-information-architecture.md §Methodology Variation` projection table with the entity-domain type layer (one model, two views).
   - 2d. Compose (do not restate) `methodology-parameterization-v1.md` + `methodology-archetype-matrix.md` per-archetype kind projection.
   - 2e. Generalize the `operations/skills/intake-desk/references/type-map.md` seam.

**Within-#507 ordering note:** 2a (the meta-schema) is the spine; 2b–2e are overlays/refs onto it and onto C1's surfaces. The Stage-6 spoke for #507 will likely decompose 2a–2e into ordered sub-task commits.

**Skill-propagation refit (the delivery-engine / ppm-agent / tracker-manager / weekly-status-rollup "read the registry at invocation" work) is OUT of this release** — #507's body routes it to C5 (deferred, tracked under #409). Do NOT pull it forward; doing so would convert a `novel` 2-issue release into a `cross-cutting` multi-skill release.

---

## Stage Applicability Matrix

Default = all stages apply (5–13). No stage is skipped for either issue — the governance + architecture + new-file character of this release means even the normally-skippable stages (5, 7, 8) carry real work; that is the correct posture for a frozen-roster amendment.

| Stage | #506 (C1 — entity + ADR) | #507 (C2 — type meta-schema) | Justification |
|---|---|---|---|
| **5 Solutioning** | REQUIRED | REQUIRED | Both carry open design questions; the release is `novel` → Stage 5 activation bias ALL. NEITHER may skip Stage 5. |
| **6 Engineering** | REQUIRED | REQUIRED | File-modifying work on both. |
| **7 Dev Testing** | REQUIRED | REQUIRED | Both have verifiable AC; DT verifies AC against the committed diff. |
| **8 QA Testing** | REQUIRED | REQUIRED | Cross-output coherence: #507's schema must be coherent with #506's entity (the discriminator must resolve). |
| **9 Plan Review** | REQUIRED (release-scoped gate) | REQUIRED (release-scoped gate) | **The Tier-2 SCOPE CHANGE operator authorization is rendered here for #506.** `novel` → Stage 9 depth Deep. |
| **10–11** | REQUIRED | REQUIRED | Standard. |
| **12 Execute** | REQUIRED (release-scoped) | REQUIRED (release-scoped) | One deploy for the bundle. Includes the Stage 12 chore PR (RELEASE_LOG row + Deployment Log). |
| **13 Close** | REQUIRED (release-scoped) | REQUIRED (release-scoped) | One Close chip; INDEX + DIGEST + RELEASE_NOTES + RELEASE_LOG VERIFIED transition. Both parent issues are marked closed at Stage 13 (NOT auto-closed by PR — multi-issue governance initiative; PR bodies use `References #N`). |

**Collective Review fires:** ≥2 issues with Stage 5 activated → after BOTH Stage 5 sub-tasks complete, the hub runs Collective Review before routing Engineering (the `novel` Deep posture).

---

## Contention Map

| File | #506 (C1) intent | #507 (C2) intent | Resolution |
|---|---|---|---|
| `core/disciplines/project-entity-model.md` | EDIT — add generic entity §4 + rollup edge §5; re-freeze | EDIT — `work_item_type` resolution + per-kind field overlay | **Hard sequence resolves it.** C1 lands the entity first; C2 overlays resolution semantics. C1 declares the entity, C2 parameterizes it — additive post-sequence. The one file requiring deliberate within-file section discipline. |
| `core/schemas/entity-field-schemas.md` | EDIT — generic-entity field schema §3 + X-rule §4 (G2 cascade) | EDIT — per-kind field overlay | Same hard-sequence resolution. C1 adds the base field schema; C2 adds the per-kind overlay rule. Additive post-sequence. |
| `core/ADRs/ADR-017-work-item-type-layer.md` | CREATE | (reads, no edit) | No contention — C1 creates, C2 references. |
| `core/schemas/work-item-type-schema.md` | (none) | CREATE | No contention — C2-only new file. |
| `core/schemas/frontmatter-schema.md` | (none) | READ-ONLY (reference §Category 4) | No edit. Already-frozen, already-merged surface. |
| `core/specs/terminology-glossary.md` | no change (Work Item already canonical; Appendix B bans Story/Epic) | no change (kinds are projections — no Appendix-B amendment) | Confirmed no edit either side. |
| `release/references/specs/ticket-information-architecture.md` | (none) | EDIT — unify §Methodology Variation table | C2-only. No cross-issue contention. |
| `release/references/specs/methodology-parameterization-v1.md` | (none) | EDIT — per-archetype kind projection (compose) | C2-only. |
| `release/references/specs/methodology-archetype-matrix.md` | (none) | EDIT (compose) | C2-only. |
| `operations/skills/intake-desk/references/type-map.md` | (none) | EDIT — generalize seam | C2-only. |

**File Change Matrix (machine-readable path list):**
```
core/disciplines/project-entity-model.md
core/schemas/entity-field-schemas.md
core/ADRs/ADR-017-work-item-type-layer.md
core/schemas/work-item-type-schema.md
release/references/specs/ticket-information-architecture.md
release/references/specs/methodology-parameterization-v1.md
release/references/specs/methodology-archetype-matrix.md
operations/skills/intake-desk/references/type-map.md
```

**Cross-PR overlap audit (baseline-pinned):** Baseline SHA = current `origin/main` HEAD at the Stage-4 spoke run (2026-06-07). No open release branch or in-flight PR touches any of the 8 files above. **Re-check this population at Stage 9 Phase A6.5** — the baseline-pin only surveys this SHA; a concurrent release merging to main during this release's Engineering/DT/QA window is not caught by the Stage-4 snapshot. Per audit-baseline discipline, this empty-population finding is pinned to the 2026-06-07 baseline + the bounded "no open release branch" window, and must be re-confirmed before Stage 12.

**Two-edge contention only** (`project-entity-model.md`, `entity-field-schemas.md`), both within the C1→C2 hard sequence, both resolved by sequencing. No parallel Engineering is safe or needed — the entire release is a 2-node linear chain. Engineering serializes naturally.

---

## Risk Register

| ID | Risk | Reversibility | Mitigation | Owner-stage |
|---|---|---|---|---|
| R1 | Frozen-roster Tier-2 authorization not rendered before the entity-model edit lands | **EXPENSIVE / HIGH** (once C2 + downstream consume the entity, it is a contract) | #506 AC-1 makes the Stage 9 GO the authorization gate. Engineering may stage the commit; the entity-model §4 edit is not authorized until Stage 9 GO. The hub surfaces the Tier-2 SCOPE CHANGE explicitly at the Deep Stage 9 review. Re-freeze line dated + precedent-cited (RAID 2026-05-16). | Stage 9 (#506) |
| R2 | Bounded cascade to G2 (`entity-field-schemas.md`) widens beyond one field schema | MODERATE | The cascade is intended and bounded: one new field schema (Core 7 + 2 fields) in §3 + ≥1 X-rule in §4 + one D4-generated per-entity V-rule set. Stage 5 confirmed the entity does NOT force changes to the §3.0 Core V-rule set (which all 17 entities inherit). A Stage-5 verification item, not a deferral. | Stage 5 (#506) |
| R3 | `project-entity-model.md` shared-file edit conflict between C1 and C2 | CHEAP | Hard sequence + D-C SINGLE + Worktree Discipline. C2's edits are additive overlays onto C1's just-landed entity, touching different concerns. Hub routes ONE Engineering chip at a time. | Stage 6 routing |
| R4 | C2 oversized → Engineering stalls / partial commit | MODERATE | C2 = 1 new meta-schema + edits across 5 files + the EAD materialization mechanism. Recommend Stage-6 sub-task decomposition (NOT Stage-4 split). Each sub-commit is independently revertible. | Stage 6 (#507) |
| R5 | Concurrent-release interaction with #346 `domain-aware-stage5-design` | MODERATE | No file contention (#346 touches stage-05/stage-07 specs; this release touches entity/schema files). The interaction is sequencing/method, not file-collision. This release does not depend on #346. Flag at Stage 9 Phase A6.5 divergence check. | Stage 5 / Stage 9 |
| R6 | Glossary Appendix-B adjacency — a future kind demands first-class Story/Epic | MODERATE | D2 ratified: kinds are methodology projections; Appendix B bans Story/Epic as canonical. Verified no Appendix-B edit is needed for this release. The risk is future; #507's custom-kind escape hatch + N=2 operator-gated promotion is the pressure-release valve. | Stage 5 (#507) design |
| R7 | F7 — Test-as-work-item-kind vs Test-as-artifact collision (#334 plan taxonomy) | MODERATE | #409 ADR names F7 as an explicit Stage-5 reconciliation item for C2. Stage 5 #507 produces an explicit reconciliation (a Test kind is a tracked work item; a test plan is an artifact the kind GENERATES). | Stage 5 (#507) |
| R8 | EAD-materialization mechanism unproven at platform scale (sparseness) | MODERATE→EXPENSIVE | The thin-generic-entity + per-type EAD schema is the central architectural bet. If per-kind schemas materialize sparsely, queryable rollup degrades toward EAV. Stage 5 #507 must produce a worked example deriving ≥2 kinds' concrete schemas AND demonstrate a rollup query across kinds (#507 AC-6 — make the rollup dimension explicit). | Stage 5 (#507) |
| R9 | Rollback complexity — entity becomes a contract mid-release | EXPENSIVE (per #506) / MODERATE (per #507 at spec stage) | The C1→C2 sequence means if the release is abandoned BEFORE C2's Engineering, only the ADR + entity declaration revert (cheap). The expensive window opens only once C2's `work_item_type` resolution lands. Stage 9 GO is the commit point; before it, reversibility is CHEAP. | Stage 9 / Stage 12 |

**Risk posture summary:** No risk is rated blocking. The two highest-impact items (R1 frozen-roster authorization, R8 EAD-sparseness) are both gated: R1 by the Stage 9 Tier-2 gate, R8 by a sharpened Stage-5 AC. R2 and R8 are the two items where Stage 5 must verify, not defer.

---

## Operator decisions rendered (Stage 4 gate)

- **D-ReleaseClass → `novel`** — two new files (ADR-017 + work-item-type-schema.md); ≥3 D-class decisions; Stage-5 ADR required for both issues. `cross-cutting` was evaluated and rejected (zero governance-surface files, zero pipeline-stage files, one compositional edge — below all three cross-cutting thresholds). Reversibility CHEAP / HIGH.
- **D-C Branch Topology → SINGLE** — one release branch, Engineering chips serialized at file HEAD, release plan committed as Engineering Commit 0. The bundle is a 2-node linear chain that serializes naturally; the only two shared files are resolved by the hard sequence. Reversibility CHEAP / HIGH.
- **D-Stage5Activation → YES for both** — both issue bodies defer mechanism to Stage 5, so SKIP is not available without contradicting the ratified scope. Collective Review fires after both Stage 5 complete. Reversibility MODERATE / HIGH.

## Merge / Split disposition

- **Keep #506 and #507 SEPARATE.** They are correctly fissioned: #506 is a governance act (a scoped Tier-2 amendment + an establishing ADR) with a distinct authorization gate; #507 is a design act (a declarative meta-schema). The hard #506→#507 sequence gives the coupling benefit without the entanglement cost.
- **#507 (C2) is a Stage-6 sub-task-decomposition candidate — NOT a Stage-4 split.** Recommended Stage-6 commit decomposition: (i) the meta-schema spine; (ii) entity-model/field-schema resolution overlay; (iii) ticket-IA projection-table unification; (iv) methodology-param composition; (v) intake-desk seam generalization. Each is an independently revertible commit on the single release branch.
- **Do NOT pull C5/C6/C7/C8/C9 forward.** #409 ADR D4 scopes Now = ADR + C2 + C1 authorization; the propagation refit, rollup/translation-fidelity, storage/projection, glossary, and migration are deferred and tracked under the #409 umbrella.

---

## Reversibility

**EXPENSIVE / HIGH confidence at the release level.** The entity addition becomes a contract once #507 consumes it; undo means unwinding the type layer + every consumer. The Tier-2 + ADR-017 + Stage 9 GO are the sign-off gate. Pre-consumption (before #507's Engineering lands) reversibility is CHEAP — only the ADR + an additive §4 block revert.

## Downstream impact

- Establishes the generic `Work Item` entity that #507 (C2) parameterizes — #507 becomes a hard consumer (the entity is a contract).
- Closes the F3 container↔work-item rollup edge gap (`Work Item → Milestone BELONGS_TO many:1`).
- The thin-entity dividend: future work-item kinds parameterize the entity via the declarative type layer and never amend the roster.

## Cross-references

- Release plan top: this file (`release/releases/plans/v1.07_RELEASE_PLAN.md`).
- Milestone: `declarative-workitem-type-model` (GitHub Milestone on cody-hutson/pmo-platform).
- User-facing release notes: `release/releases/notes/v1.07_RELEASE_NOTES.md` (authored at Stage 13 Close).

## Source references

The issue and sub-task numbers cited above, for provenance (this block is the designated reference home; the prose leads with self-describing roles so the meaning survives renumbering):

- **#506** — C1: establish the generic `Work Item` entity + the establishing ADR (the governance act; the Tier-2 SCOPE CHANGE).
- **#507** — C2: the declarative type-pack meta-schema (the design act; the hard consumer of #506).
- **#508** — the Stage 4 Release Planning sub-task whose comment is the plan content of record transcribed here.
- **#409** — the Work-Item Type Layer (WITL) tracking umbrella; carries the ratified D1/D2/D4 architecture decisions and the deferred C3/C5/C6/C7/C8/C9 scope.
- **#346 / #334 / #351 / #54** — adjacent milestones/issues named in the Risk Register as soft adjacencies (no hard dependency on this release).
