# v3.44 Release Plan — 78-pipeline-triage-automation

> **Milestone:** `78-pipeline-triage-automation` · **Cards:** #286 (define the Stage-2 triage-analysis skill) + #282 (bind auto-execute as the operative default) · **Release Class:** novel · **Shape:** one branch / one PR / one merge (milestone-one-PR convention).
> **D-Version = v3.44** — recorded determination. Rule: `anchor()` = latest published GitHub release (v3.43) → next-free minor = **v3.44**. Re-verified free at Engineering Commit 0 (no v3.44 tag/branch/plan). Stated, not an operator gate (rule-determined recurring-D).
> **ADR:** ADR-063 (next-free — global ADR max across `core/ADRs/` + `release/ADRs/` = 062; re-verified at Commit 0).

## Re-slot note (v3.42 → v3.44)

The Stage-4 plan and the Stage-5 design were authored under D-Version **v3.42** with **ADR-062**. A prior Stage-6 attempt HALTed at Commit-0 on a v3.42 + ADR-062 collision. Those concurrent releases (v3.42 + v3.43) have since **SHIPPED** (both published; anchor is now v3.43; ADR-062 is on main). The operator adjudicated: proceed on the true next-free. This release therefore carries **v3.44** and **ADR-063**; all version/ADR references below are updated accordingly. The design substance (a new standalone triage skill in `release/skills/`) is unchanged — see the Stage-5 resolution on #2752.

## Summary (30 seconds)

Two OPEN cards, one capability slice: **#286** defines the Stage-2 triage-analysis **skill** (executes A1–A6, produces one consolidated triage summary); **#282** binds **auto-execute as the operative default** on that skill + wires the Close/Reject confirmation gate. The A1–A6 phase sequence and the auto-execute posture are **already fully specified** in `release/references/pipeline/stage-02-triage.md` (§5 Phase A enumerates A1/A2/A2.5/A3/A3.5/A4/A5/A5.5/A6/A6.5; §8 declares the Tier-2 auto-execute posture) — so neither card authors new *policy*; both build the **execution surface** the spec already assumes.

**Host decision (Stage-5-resolved, operator-confirmed 2026-07-01 on #2752): a NEW standalone triage skill in `release/skills/` — NOT an extension of `delivery-engine` or `release-planner`.** All three reuse candidates were ruled out (per `stage-to-skill-mode-mapping.md` G2 + `operating-model.md` two-backlog distinction): `delivery-engine` reads the Jira/project backlog (`grep status:proposed` = 0), project-ops module; `release-planner` Mode A consumes `status: approved` issues (downstream of triage, does not produce triage decisions); `ppm-agent` produces decision frames, does not run the Workflow Readiness gate. ADR-019's net-new bar (*necessary, not plausible*) is therefore MET — no existing skill fits. Skill name (evidence-grounded): **`pipeline-triage`**.

**Merge/split call: ship as ONE slice, one PR, one merge** with #286 built first (the skill) and #282 layered on top (auto-execute default + Close/Reject gate) in the same branch.

## Dependency Graph

Directional edges (`A → B` = A must land before / enables B):

```
#286 (define pipeline-triage skill) ──enables──> #282 (bind auto-execute default + confirmation gate)
```

- **#286 → #282 (HARD, in-release).** #282's AC1 ("the triage skill runs A1–A6 without per-action approval") names #286's skill as its binding target. #282 has no execution surface to bind a default onto until #286's skill exists. Both card bodies state this: #282 "Binding target = #286"; #286 "#282 (auto-execute default consumes this).".
- **No inbound external blockers.** All historical blocked-by edges are met/void:
  - #286 blocked-by **#8** (map stages→skill modes) and **#23** (Stage-2 definition) — both CLOSED/met.
  - #282 body-cited **#42** (three-gate model) — renumber-rot, resolved to a completed deploy task; not a live blocker.
  - **#34** (pmo-triage agent) and **#33** (pmo-intake agent) — CLOSED NOT_PLANNED (agent premise superseded → skill-based). Removed from scope; not re-added.
- **Outbound (informational, NOT in-release):** #286's A2.5 scope-delta (emit undeclared-dependency candidates, inherited from void #34) and the Stage-2→1 routing capability route to **#779** (OPEN — Stage-2 architectural-fit acceptance gate) and to epic **#1188** (OPEN). Downstream homes, not dependencies of this release.

## Implementation Sequence

Dependency-ordered, single branch (`release/v3.44-pipeline-triage-skill`), one wave (P0 fully-serial):

**Commit 0 — plan + version claim.** This file. Re-verify v3.44 + ADR-063 free (done).

1. **#286 — define the `pipeline-triage` skill** (foundation; #282 has nothing to bind without it).
   - Author `release/skills/pipeline-triage/SKILL.md` to the pmo-skill-refiner standard (7 PMO injection fields) executing A1–A6 by **citing** `stage-02-triage.md` §5 phases (do not re-author the phase definitions — reference-durability + no-duplicate-source).
   - Bind the concrete skill name into `stage-02-triage.md` §3 Persona / §8 (AC5) — replaces the bare "skill mode runs A1-A6" stub with the named skill; update §9 Gap Summary "no triage skill mode (P2)" → resolved.
   - Update `stage-to-skill-mapping` G2 row: GAP → coverage, naming `pipeline-triage`.
   - Register: append the CI row to `core/skills/registry.md`; append the output-contract to `core/schemas/per-skill-output-contracts.md`; add `pipeline-triage` to the `RELEASE_SKILLS` array in `core/deploy/deploy.sh`.
2. **#282 — bind auto-execute as operative default** (layered on #286's skill, same branch).
   - Add the auto-execute default declaration + no-per-action-approval statement to the skill body (AC1/AC2), citing `stage-02-triage.md` §8 (definition STANDS, referenced not rewritten — R1).
   - Wire the Close/Reject human-confirmation gate (AC5) — the one carve-out from auto-execute.
   - Add an `auto-execute` reference to `release/governance/release-process.md` § Full-Phase-Scope Discipline (AC6).
3. **Reflexive cutover clause** (both cards' AC7) — author the "applies to releases entering Stage 2 after this release's merge SHA; this release exempt" clause once, in the skill body, referenced from `release-process.md`.
4. **ADR-063** — author `core/ADRs/ADR-063-*.md` recording the standalone-skill decision (delivery-engine / release-planner / ppm-agent rejected with cited evidence).
5. **`.skill` package** — create/rebuild `packages/pipeline-triage.skill` (deterministic `build-skill-packages.sh pipeline-triage`); at latest, at Stage 12. Never hand-edit.

## Stage Applicability Matrix

Default = all stages apply; justify skips. Both cards are skill/governance-doc updates with functional behavior.

| Stage | #286 (define skill) | #282 (bind default) | Rationale |
|---|---|---|---|
| 5 Solutioning | **DONE** | DONE (shared) | Host-selection decision resolved on #2752 (standalone `pipeline-triage`; operator-confirmed). ADR-063 records it. |
| 6 Engineering | APPLY | APPLY | Both mutate skill + doc files + registration surfaces. Single branch, serial (P0). |
| 7 Dev Testing | **APPLY** | **APPLY** | Functional behavior — the skill must run A1–A6 and produce the consolidated summary (#286 AC2/AC3); auto-execute must skip per-action approval AND the Close/Reject gate must block (#282 AC5). Grep-verifiable ACs + behavioral ACs both need DT. Do NOT skip — rigor-invariance governs stage EXECUTION. |
| 8 QA | APPLY | APPLY | pmo-qa-auditor against principal-contributor standard + failure-mode discipline (≥3 domain failure modes); skill-edit coherence check. |
| 9 Plan Review | APPLY | APPLY | Deep review (novel class). Single human GO gate for the slice. |
| 10–11 | APPLY | APPLY | Standard pipeline (git-native compression). |
| 12 Execute | APPLY | APPLY | Single merge (one PR). `.skill` build for `pipeline-triage`. Deploy via `deploy.sh`. |
| 13 Close | APPLY | APPLY | Mark #286 and #282 as closed at Stage 13; RELEASE_LOG row; INDEX/DIGEST/RELEASE_NOTES. |

No stage is skipped. Stages 7/8 explicitly APPLY (functional behavior present).

## File Change Matrix

Machine-readable (one path per line per issue). Add/edit intent prefixed. Paths verified against current-main topology at base `6af3a8e` (no post-base structural reorg — SKIP).

**#286 — define the `pipeline-triage` skill:**
```
ADD    release/skills/pipeline-triage/SKILL.md
EDIT   release/references/pipeline/stage-02-triage.md
EDIT   release/references/specs/stage-to-skill-mode-mapping.md
EDIT   core/skills/registry.md
EDIT   core/schemas/per-skill-output-contracts.md
EDIT   core/deploy/deploy.sh
```

**#282 — bind auto-execute as the operative default (layered on #286, same branch):**
```
EDIT   release/skills/pipeline-triage/SKILL.md
EDIT   release/governance/release-process.md
```

**Slice-level (shared, authored once):**
```
ADD     core/ADRs/ADR-063-standalone-triage-skill.md
ADD     packages/pipeline-triage.skill              (deterministic build — build-skill-packages.sh pipeline-triage; Stage 12 at latest, not hand-edited)
```

Domain classification: **`domain: governance`** — the matrix targets `release/` pipeline + governance docs and a `release/skills/` skill (platform-internal deliverable). Pipeline-internal → sourcing-exempt (`source: N/A — pipeline-internal release`).

## Contention Map

Files touched by BOTH cards (in-release contention):

| File | #286 | #282 | Contention class | Resolution |
|---|---|---|---|---|
| `release/skills/pipeline-triage/SKILL.md` | ADD (create skill) | EDIT (add auto-execute default + Close/Reject gate to that skill) | **HARD sequential** — #282 edits the skill body #286 creates | Strict order #286→#282 on one branch; P0 serial. Not parallelizable (same construct, dependency edge). |

No cross-release / cross-milestone contention: **open-PR population = 0** at Engineering Commit 0 (re-checked at base `6af3a8e`; audit-baseline discipline satisfied). No sibling milestone is in-flight against these files.

## Risk Register

| # | Risk | Type | Reversibility | Mitigation |
|---|---|---|---|---|
| R1 | **#282 re-authors the already-shipped §8 auto-execute definition** (mis-scope). #282 body flags this explicitly (MEDIUM confidence for exactly this reason). | Scope | CHEAP | Scope-lock: #282 = execution-binding + Close/Reject gate ONLY; §8 definition STANDS, referenced not rewritten. DT AC6 verifies "no re-authored §8 policy block." |
| R2 | **A1–A6 phase logic duplicated** into the skill body instead of cited → drift target vs `stage-02-triage.md`. | Maintainability | CHEAP | Skill CITES `stage-02-triage.md` §5 phase IDs; reference-durability hook + no-duplicate-source. DT AC2 checks phase-ID *references* present, not re-stated definitions. |
| R3 | **HARD #286→#282 edge on one file** (`pipeline-triage/SKILL.md`) — parallel Engineering chips would race. | Contention | CHEAP | P0 fully-serial posture (default); #286 lands before #282. |
| R4 | **`.skill` package drift** — runtime `pipeline-triage` diverges from source if package not built at cut. Recurring Check-7 cluster (milestone 94). | Deploy | CHEAP | Build `packages/pipeline-triage.skill` deterministically at Stage 12 (`build-skill-packages.sh pipeline-triage`); do NOT hand-edit. `deploy.sh --check` Check 7 verifies parity. |
| R5 | **New-skill registration incomplete** — a net-new skill needs registry row + output-contract + `deploy.sh` array member + package, or `deploy.sh --check` FAILs (Check 5 roster / Check 7 package / registry currency). | Deploy / Governance | CHEAP | All four registration surfaces authored in the #286 commit (registry.md, per-skill-output-contracts.md, deploy.sh, packages/). Run `deploy.sh --check` before PR review-ready; distinguish real `  FAIL:` from mirror-drift. |
| R6 | **Concurrent release claims v3.44 / ADR-063** between now and merge. | Dependency (version) | CHEAP | Commit-0 re-verify rung (done — free). Atomic tag-claim at Stage 12; ADR contiguity enforced by `check-adr-numbers.py`. |
| R7 | **Rollback complexity: LOW overall.** Both cards CHEAP-reversible (#286 CHEAP/HIGH; #282 CHEAP/MEDIUM). Net-new skill + doc edits; no corpus-semantics change. | Rollback | CHEAP | `git revert` the single merge + `deploy.sh` rebuild. No data migration, no downstream artifact invalidation. |

Highest single risk: **R1** (mis-scope re-authoring §8) — controlled by the scope-lock in both card bodies + a DT assertion. Runner-up: **R5** (new-skill registration completeness) — controlled by authoring all four registration surfaces in the #286 commit + a `deploy.sh --check` pass.

## Release Class

**Class: `novel`** (operator renders at the D-ReleaseClass gate; recommendation confirmed at Stage 4).

- **novel trigger (a) — ≥1 issue introduces a new … skill:** #286 introduces a net-new **`pipeline-triage` skill** (a new invokable execution surface + new registry CI + new `.skill` package). First-of-kind Stage-2 triage skill for the platform.
- **novel trigger (b) — ≥1 D-class decision in the release plan:** YES — D-Design (host selection: standalone-vs-extend) was a genuine Stage-5 design decision, resolved on #2752 and recorded in ADR-063.

**Differentiation posture (novel):** Engagement density **Standard** · Stage 9 review depth **Deep** · Stage 13 outcome-window **30-day**.

## Recommendations

1. **Ship #286 + #282 as ONE slice: one `release/v3.44-pipeline-triage-skill` branch, one PR, one merge.** Build #286 first (the skill), layer #282 (auto-execute default + Close/Reject gate) on top in the same branch. Do NOT split — the hard dependency edge + single shared file make split pure overhead.
2. **Cite, do not duplicate, `stage-02-triage.md` §5 phases.** The skill body references phase IDs (A1…A6.5); the phase *definitions* stay in the pipeline spec (R2).
3. **Scope-lock #282 to the execution-binding + Close/Reject gate.** Do not re-author §8's auto-execute definition — it already stands (R1).
4. **Author the skill to the pmo-skill-refiner standard** (#286 AC6): 7 PMO injection fields; ≥3 domain-specific failure modes (failure-mode-standard.md 5-field template + category tags); output-contract + dependency-graph node registered. Build `packages/pipeline-triage.skill` deterministically at Stage 12 (R4).
5. **Author the reflexive cutover clause once** (both AC7): "applies to releases entering Stage 2 after this release's merge SHA; this release exempt."
6. **Out-of-scope discoveries (note only, do NOT action here):** #286's A2.5 undeclared-dependency-candidate emission + Stage-2→1 routing (inherited from void #34) route to **#779** (OPEN) / epic **#1188** (OPEN) — NOT this release's scope.

---
*Engineering Commit 0. D-Version recorded (v3.44), re-verified free. ADR-063 re-verified next-free. Per-issue closure phrased safely ("mark #N as closed at Stage 13") for clean transcription into the PR body.*
