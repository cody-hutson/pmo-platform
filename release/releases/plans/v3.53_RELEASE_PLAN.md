# v3.53 Release Plan — 80-solutioning-and-engineering-skill-modes

> Release-scoped plan for Milestone **80-solutioning-and-engineering-skill-modes**. Authored at Stage 6 (Engineering Commit 0) from the Stage-4 spoke output (sub-task #2931) + the operator Plan-Approval and Collective-Review decision records. D-C **SINGLE** (one release branch → one PR → one merge). Reversibility **MODERATE**. **Built as v3.53** — the provisional slots **v3.50** then **v3.51** were claimed by concurrent releases during the heavy concurrent-release window; this milestone re-claimed forward to the next-free minor **v3.53** at Engineering Commit 0 re-verify (forward-only per bundle-composition-doctrine §5.3; the durable declaration is the bump-class, minor). The § D-Version section records the build-time re-verify determination. Scope covers only the two OPEN issues **#674** + **#505** (the milestone's 3 closed stories #278/#297/#298 shipped 2026-06-15 and are out of this plan).

## Release Class declaration

- **Release Class:** `novel` (operator-ratified at the Plan-Approval gate; written to milestone #165 description). Trigger (a) #505 introduces a new executable tool/capability under `release/tools/`; trigger (b) ≥1 D-class decision in the plan (topology + #674 scope-expansion + #505 sibling-CLI-vs-extend design choice); trigger (c) a Stage-6 ADR (ADR-067) for the scanner-plug architecture. Only `novel` triggers fire cleanly (1 stage-file → not cross-cutting).
- **Domain:** governance (governance/schema/doc + tooling release). `source: N/A — pipeline-internal release` (sourcing-exempt, not classification-exempt).
- **Domain Practice Provenance:** `domain: governance | date: 2026-07-01 | source: N/A — pipeline-internal release`
- **Differentiation posture (per-class mapping):** Engagement density Standard · Stage 9 Plan Review depth Deep · Stage 5 Activation bias ALL (#505 full Stage 5; #674 light path) · Stage 13 Outcome-window 30-day.

## Delivery Strategy

- **Branch topology (D-C):** SINGLE — one release branch `release/v3.53` (operator standing convention, rendered at the Plan-Approval gate). Both issues committed as delivery slices in Implementation-Sequence order (#674 → #505), **one PR → one merge**. Posture **P0 fully-serial** (only 2 disjoint issues; parallelism buys nothing; undeclared → default P0). Force-push prohibited on the shared branch under multi-chip activity.
- **Merge approach:** single squash/merge PR into `main` at Stage 12; the merge SHA is the release SHA. Stage 9/12 gates own the merge — Engineering does NOT merge (this spoke bootstraps the branch and lands #674 only; #505's Engineering follows on the SAME branch; the PR opens later).
- **Rollback strategy:** git-native — `git revert -m 1 <merge-sha>` reverts the whole release atomically (single PR/single merge). No DB/external state; Stages 10/11 PLATFORM-SATISFIED (git-native).

## D-Version (RECORDED DETERMINATION — not a gate)

- **anchor()** = highest claimed minor in the v3.x mainline = **v3.51** (`claim-version.sh` anchor(), read-only, CLAIM_REPO=cody-hutson/pmo-platform, @ Engineering Commit 0 re-verify).
- **Bump-class** = minor (capability release — #505 adds a new tool) — the durable declaration.
- **claimed_set** (read-only via `claim-version.sh claimed_set()`) tops out at **v3.51**; **v3.52** and **v3.53** are both free of the claimed set. Assigned slot = **v3.53** (hub-verified free at re-dispatch). No `v3.53` tag and no `release/v3.53*` in-flight branch exist on origin. `[SOURCE: git fetch --tags origin; git fetch origin main; git ls-remote --heads/--tags origin 'refs/{heads/release,tags}/v3.53*']`
- **Provisional lineage:** Stage-4 spoke computed provisional v3.49 (collision flagged R1); operator Plan-Approval re-rendered to v3.50; the release then HALTed twice as v3.50 then v3.51 were claimed by concurrent releases mid-window. At re-dispatch v3.53 was verified FREE → bound as the build slot. Re-claims atomically at Stage 12 (forward-only) if a sibling takes v3.53 first. Detection rungs: Engineering Commit 0 re-verify (done — this file) → Stage 9 G-PR9 baseline-currency → Stage 12 Phase A.5 pre-merge freeness → atomic tag claim.

## Implementation Sequence

Risk-shaped (both issues are independent roots — no dependency edge, zero file contention). One Engineering commit per issue on ONE release branch (write-serialized Stage 6).

| # | Issue | Size | Layer | Why here |
|---|---|---|---|---|
| 1 | **#674** — release-executor Dry-Run-Record halt made lineage-aware | XS(1) | skill-doc | Tiny single-file edit; land first to de-risk the branch early and get the additive-only-departure (governed modification of an existing failure-mode entry) reviewed on its own small diff. `release-executor/SKILL.md` line 166 + PROC entry (604–611). v2.14→v2.15 + package rebuild. |
| 2 | **#505** — domain-fan-out impact-analysis instrument (sibling CLI) | L | tooling | The larger build (new `domain-blast-radius.sh` + `lib/schema-v1-emit.sh` shared lib + `blast-radius.sh` refactor to source the lib + 2 doc reconciliations + regression fixture + **ADR-067**). Lands second so the bulk of review attention hits an unobstructed diff. Follows on the SAME branch (separate spoke). |

**Load-bearing ordering:** #674 before #505 is a risk-shaping preference (smaller/lower-risk first), NOT a commit-serialization constraint — the two change surfaces are fully disjoint (Contention Map = NONE). Under SINGLE they serialize on the shared branch in this order.

## File Change Matrix (machine-readable)

One path per line; `intent` ∈ {add, edit}. No deletes.

```
# path                                                          intent   issues   note
release/skills/release-executor/SKILL.md                        edit     #674     Line 166 Step-1 input-validation halt → lineage-aware pair (Cowork halt+Mode-C verbatim; git-native = lineage, route pipeline/Modes B/D/E/F); PROC-entry Mitigation (604–607) + Principal/junior field (608–611) branch identically; cross-ref PROC+TRIG; NO TRIG change; version v2.14→v2.15
packages/release-executor.skill                                 edit     #674     Rebuild via build-skill-packages.sh release-executor (Check 7); not hand-edited
packages/release-executor.skill.sha256                          edit     #674     Content-baseline sidecar regen (Check 7)
release/tools/domain-blast-radius.sh                            add      #505     NEW sibling CLI — domain-keyed scanner selected by domain: class field; emits schema-v1 JSON; ≥1 non-doc domain (code import-graph for domain: software)
release/tools/lib/schema-v1-emit.sh                             add      #505     NEW shared emitter lib (schema-v1 surface: stats.first_order_count, first_order[].path/reference_count/matches)
release/tools/blast-radius.sh                                   edit     #505     Refactor default emitter to source schema-v1-emit.sh (D3 — removes two-emitter drift); markdown-tree default path unchanged; regression-guarded
release/references/pipeline/stage-05-solutioning.md             edit     #505     Phase A3.1 rows: implemented domain "tool deferred" → cite shipped instrument
release/references/protocols/blast-radius-protocol.md           edit     #505     §12: "no such tool exists in release/tools/ today" → cite shipped instrument for implemented domain(s)
release/tools/tests/test_domain_blast_radius.sh                 add      #505     NEW regression guard: (a) doc-corpus default output unchanged; (b) code-fixture import-graph fan-out non-empty schema-v1 result
core/ADRs/067-*.md OR release/ADRs/067-*.md                     add      #505     ADR-067 — scanner-plug architecture (sibling CLI + shared lib); next-free 067 across core/ADRs + release/ADRs + in-flight
release/releases/plans/v3.53_RELEASE_PLAN.md                    add      (C0)     This plan (Engineering Commit 0)
```
> Test filename and ADR path/number are indicative; #505's Engineering spoke sets the final names (ADR-067 confirmed next-free at Collective Review). The `blast-radius.sh` edit (D3) is regression-guarded by #505's default-path test.

## Risk Register

| ID | Risk | Type | Sev | Mitigation | Reversibility |
|---|---|---|---|---|---|
| R-1 | Version collision — provisional slot claimed before merge (v3.50/v3.51 already lost mid-window). | contention (version) | HIGH | v3.53 verified free at Engineering Commit 0 (this file); durable = bump-class (minor). Re-claims atomically forward at Stage 12; re-check claimed set at Stage-12 Phase A.5. | CHEAP pre-Eng / MODERATE post-Commit-0 |
| R-2 | #505 default-path regression — refactoring `blast-radius.sh` to source the shared lib risks degrading the markdown-tree default the pipeline's own reflexive blast-radius run depends on. | rollback / blast-radius | HIGH | #505 AC mandates a doc-corpus regression run matching current output; the new test fixture hard-guards it. Sibling-CLI (not in-place extension) isolates the new capability. Revert the tool commit. | CHEAP (additive; revert commit) |
| R-3 | #505 schema drift — the new scanner emits a surface diverging from the schema-v1 contract `design-review-checklist.md` §1 consumes. | contention (contract) | MED | D3 sources BOTH default + new CLI through the single `schema-v1-emit.sh` lib (one emitter, no drift); #505 AC diffs output shape vs the checklist schema-v1 surface. | MODERATE |
| R-4 | #674 additive-only departure — modifying an existing failure-mode entry departs from the additions-only regression posture; an additions-only AC would wrongly reject the change. | scope / process | MED | Operator AUTHORIZED the modification at the Plan-Approval gate (Decision Record: "#674 regression AC: authorized to modify an existing failure-mode entry"). Flag at Stage 9. | CHEAP |
| R-5 | #674 incomplete-fix / twin-halt miss — the identical unconditioned halt lives at BOTH the Step-1 instruction (166) AND the PROC entry (604–611). | scope (completeness) | HIGH (if scoped to one) | Scope LOCKED to BOTH surfaces + the Principal/junior field, each lineage-aware and consistent with the v1.10 TRIG entry. Exactly two Mode-C dead-ends confirmed by grep; no third. | CHEAP (git revert) |
| R-6 | ADR-067 numbering collision — ADR numbering spans core/ADRs + release/ADRs + in-flight. | process | LOW | Collective Review hub-verified 067 next-free across both dirs + in-flight. Re-check at #505 Engineering. | CHEAP |

**Audit-baseline note:** the concurrent-release population driving R-1 was assessed against origin/main @ fetch 2026-07-01, anchor v3.51. Re-check the claimed set at Stage 12 before the atomic claim.

## Verification Plan (AC → method; Stage 7/8 runs these)

| Issue | Verification Method | Expected Result |
|---|---|---|
| #674 | Section read of amended `release-executor/SKILL.md` shows the git-native branch routed to the pipeline (never release-planner Mode C) at BOTH the Step-1 instruction (166) AND the PROC entry (604–611), consistent with the v1.10 TRIG entry; Cowork-lineage branch preserves the verbatim "Run release-planner Mode C first." halt. `grep -c "Run release-planner Mode C first" SKILL.md` reflects the lineage-scoped occurrences; version block v2.15; pmo-qa-auditor Mode A G7 structural + LLM-graded PASS; Check 7 package freshness (sha256 sidecar regenerated). | Both halts lineage-aware; TRIG unchanged and now internally consistent; G7 PASS; package rebuilt. |
| #505 | Code-import-graph fan-out runs and produces a non-empty schema-v1 result on a code fixture (`domain: software`); the doc-corpus default run still matches current `blast-radius.sh` output (regression); emitted JSON shape diffs clean against the checklist schema-v1 surface; ADR-067 present + next-free; new test passes both cases. | New instrument ships; default path unregressed; schema conformant; ADR authored. |

## Deferred Items

None. Scope LOCKED at 2 open issues (#674 + #505). The 3 closed stories (#278/#297/#298) are out of scope (shipped 2026-06-15).

## Out-of-scope discoveries (Recommendations, not this release)

- **#674 sibling general mentions.** Three additional "no Dry-Run Record" mentions (SKILL.md ~:24–25 role prose, ~:43 invocation checklist, ~:518 anti-patterns) carry the same lineage-agnostic framing but do NOT name Mode C and are NOT dead-end mis-routes — out of #674's scope. Candidate follow-up hygiene pass; not this release.
- **Stale/abandoned tag hygiene.** Prior planning surfaced a v3.49 tag/release-ledger mismatch (tag without a published GitHub Release). Separate hygiene item; not this release's work.
- **No blast-radius.sh test today.** #505's regression fixture is the first blast-radius test. A broader blast-radius test-coverage backfill is a latent gap beyond #505's minimum.

---
_Stage 6 Engineering Commit 0. Plan authored from Stage-4 spoke (#2931) + operator Plan-Approval + Collective-Review decision records. Version re-verified FREE at Commit 0 = v3.53. Per-issue closure phrased SAFE (mark #N as closed at Stage 13) for downstream PR-body transcription._
