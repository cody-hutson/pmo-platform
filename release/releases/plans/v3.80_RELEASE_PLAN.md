# Release Plan — v3.80

**Milestone:** `92-version-identity-and-parser-ssot` (GH #235)
**Version:** v3.80 (minor · next-free from `.version`=v3.79 · claimed at Stage 12)
**Branch:** `release/v3.80-version-identity-and-parser-ssot`
**Status:** Stage 4 approved · in build (hub-inline-serial)
**Planner:** release-hub Mode O (composing release-planner Mode B) · 2026-07-19

## Summary

Cleanup-debt slice closing out the version-determinism capability line. **Composition shape:** cleanup-debt. **Capability outcome:** *the version-grammar SSOT is sourced everywhere (zero duplicate parsers), the version-claim safety nets are complete, and theme-named close-outs emit the full Stage-13 output set.* **Size:** ~14 pts — below the 15–25 band; operator-approved proceed-as-is (padding would violate composition discipline). **Frame:** F1 (SAFe Feature-Slicing + Vertical Slice). Prior context: this milestone passed release-hub Mode R readiness + a coordinated re-triage (2026-07-19) — #2022 and #2493 closed OBE, #2075 trimmed to residual.

## Dependency Graph

**Topologically-sorted sequence:** `#2075 → #1800 → #1801 → #2048 → #1286` (coherence-ordered — no hard edges).

### Critical Path
`[DEGRADED-MODE: typed-dep substrate absent]` — (none — bundle has no dependency edges). All five are independent changes to distinct files.

### Artifact Relationship Graph
No typed artifact relationships — flat cleanup slice. Soft coupling only: #1800 + #1801(pt 2) + #2075 share the SSOT-sourcing pattern (source `version-grammar.sh` / `version_canonical`; do not re-encode a regex).

## File Contention Map

| File | Issues | Severity | Handling |
|---|---|---|---|
| `release/tools/automated-closeout.sh` | #1801 (validate_version) + #2048 (output-set emit) | BINARY | serial commit order #1801 → #2048 (single branch serializes) |
| `release/tools/claim-version.sh` | #2075 | NONE | isolated |
| `core/deploy/deploy.sh` | #1800 | NONE | isolated (Check 39) |
| _(v7.5-ref set)_ | #1286 | NONE | pending Stage-5 discovery scan |

## Cross-Milestone Dependency Validation

Suppressed — bundle has zero dependency edges (cross-milestone edges died when #2022/#2493 closed OBE). No sequence violations possible.

## Implementation Sequence

### Commit Plan (one branch, serial)
1. **#2075** — `claim-version.sh` `_host_release_log_deployed` `st=$7`→`$8` + `:113` comment correction + self-test (DEPLOYED row counted). *(XS)*
2. **#1800** — `deploy.sh` Check 39 → source `version-grammar.sh`; retire duplicate `sed` parser; `set -e`-safe sourcing. *(S)*
3. **#1801** — recovery-runbook forward-pointers (`stage-12-execute.md` / `stage-13-close.md`); `automated-closeout.sh` `validate_version` → source `version_canonical` + invert self-tests; `git-workflow.md` signed-tag pointer. *(S)*
4. **#2048** — `automated-closeout.sh` emit full Stage-13 output set (`.version` + CHANGELOG + INDEX + DIGEST) for theme-named/version-less milestones. *(M — after #1801, same file)*
5. **#1286** — reconcile stale v7.5 orphan-lineage version anchors. *(S–M — pending discovery)*

### Stage Applicability Matrix
All 5 → Stages 5–13. Light-design: #2075, #1800, #1801. Real Stage-5 design: #2048 (output-emit logic), #1286 (ref discovery).

## Cross-PR Overlap Audit

Baseline: `origin/main` @ f935185. Open PRs in scope: none. No structural movers (no rename/relocate/delete) → no Tier-S blast-radius edge.

## Risk Register

| # | Risk | Sev | Mitigation |
|---|---|---|---|
| R1 | Classifier-down build phase (spawn-stall) | HIGH (env) | hub-inline-serial build (operator-approved) — no spawned spokes |
| R2 | Reflexivity — edits `claim-version.sh`, the Stage-12 claim tool | MED | #2075 touches only the dead category-3 net (no live-path change); v3.80 claimed by the tool as-is at Stage 12, fix ships after |
| R3 | Dogfooding — #2048 fixes theme-named close-out; this milestone is theme-named | LOW (good) | verify full output set at Stage 13 |
| R4 | #1801/#2048 same file | LOW | serial commit order |
| R5 | #1286 scope unknown | MED | Stage-5 `grep -rn "v7.5"` discovery before build |

## Operator Decisions (D-Gate Block)

### D-Version: v3.80
- **Gate decision:** v3.80 (rule-determined, next-free minor). Claimed at Stage 12. `[IRREVERSIBLE once shipped · HIGH]`

### D-Size-band: proceed-as-is
- **Gate decision:** below-band (~14 pts), proceed-as-is per operator approval. `[MODERATE · HIGH]`

### D-Concurrency Posture: P0 fully-serial (hub-inline)
- **Gate decision:** P0 serial, executed hub-inline (not spawned spokes) — environment-adaptive to R1. Force-push prohibited on the shared branch. `[MODERATE · MEDIUM]`

## Recommendations

1. Build in the §Implementation Sequence order; #1801 before #2048 (same file).
2. Front-load the #1286 v7.5-discovery scan at Stage 5.
3. Verify #2048's fix reflexively at this release's own Stage-13 close.

## Verification Evidence

_(placeholder — populated at Stage 13 close)_
