# v3.80 Release Plan — pipeline-telemetry-tail

> Version **v3.80** is PROVISIONAL. Re-verified at Stage-6 entry (2026-07-19) as the
> next-free minor in the v3 major, concordant across all three sources: highest tag
> `v3.79`, `.version` = `v3.79`, highest `RELEASE_LOG.md` token = `v3.79`. No `v3.80`
> tag exists and no `v3.80` entry is present in the release log.
>
> **Concurrency notice — the v3.80 slot is contended.** Four other in-flight release
> branches carry a `v3.80-` label at Stage-6 entry (`close-out-reliability-hardening`,
> which has an open PR; `governance-doc-reconciliation`; `version-identity-and-corpus-ledgers`;
> `version-identity-and-parser-ssot`). The version binds atomically at the Stage-12
> tag-claim, not at branch-creation, so whichever release merges first takes v3.80 and
> the rest re-anchor forward (forward-only renumber, per the version-freeness protocol
> and the precedent recorded in the v3.79 plan). Re-run the freeness check at the
> Stage-12 A.5.6c pre-merge gate and correct the label on the branch BEFORE the merge.

Milestone: pipeline-telemetry-tail (#261) · epic-#1189 tail
Version: v3.80 (provisional; next-free minor above the v3.79 anchor — binds at Stage 12)
Release class: cross-cutting · Reversibility: Stage 6 MODERATE (branch + commits, git-revertable)
Topology: single release branch, one PR, one merge — issues are delivery slices
Status: operator-APPROVED 2026-07-19 · Collective Review SCOPE LOCK 2026-07-19

## Release Outcome Statement

**AFTER:** Pipeline health is observable at phase-distinctive granularity — the 5 remaining
phases (Demand / Definition / Solution-design / Verify / Authorize) emit quality-attribute
telemetry (#2645); Close-class Indicators 5 & 6 read true rates via a canonical
roll-up-worthy-decisions denominator (#2646) and a pipeline-wide phase-completion evidence
ledger (#2647); per-session self-retrospection feeds the learning substrate at session grain
(#2423); the event-log schema accepts every emission its stage shards codify (#3301); and the
v10.1 intake-two-tier behavioral ACs are re-verified and closed (#166).

**BEFORE:** Phase-distinctive observability exists only for the Build+Deploy and Close layers
(v3.34); Close-class Indicators 5 & 6 report presence/booleans with no computable rate (no
denominator, no evidence store); learning is captured only at decision-moment and release grain
(no session grain); a codified cascade-sweep emission is rejected by the event-log schema; and
the v10.1 intake review is measured-but-not-closed.

**Actor(s):** platform operator + pipeline agents (telemetry consumers).

**Success Indicator:** Close-class Indicators 5 & 6 emit rates (not booleans); each of the 5
remaining phases has >=1 phase-distinctive telemetry read-model; a seeded session yields a
captured self-retro learning; `append-pipeline-event.sh` accepts the `cascade-sweep-block`
emission; #166 re-run recorded and closed.

## Summary

6 independent issues (acyclic; one satisfied cross-milestone edge #2423→#46). One PR / one
branch / one merge; issues are delivery slices. This plan file lands as Engineering Commit 0.
Critical path = 12-pt floor (#2646→#2647 serial on the Close-class telemetry home) or 16 pts
if #2645 is not fissioned.

## Dependency Graph (hard edges)

```
#3301 → ∅        #2646 → ∅ (soft ⇄ #2647)      #2647 → ∅ (soft ⇄ #2646)
#2645 → ∅        #2423 → #46 [CLOSED/shipped → SATISFIED]      #166 → ∅
```

Acyclic. No unsatisfied deps, no cycles, no intra-milestone hard edges. All 6 immediately
buildable; substrate (#143 / #7 / #46) shipped.

## Implementation Sequence

Layer-ordered foundation → infra → skill-core → eval; tie-break issue-ascending.

1. #2645 (foundation · XL/16 · fission at Stage 5)
2. #2646 (foundation · M/4)
3. #2647 (foundation · L/8)
4. #3301 (infra substrate · S/2 · front-loaded for build-safety)
5. #2423 (skill-core · L/8)
6. #166 (eval · S/2 · verification-only, no build)

## Critical Path (CPM)

No dependency-forced chain. Schedule is set by the points-heaviest serial constraint:
**#2646 → #2647 = 12-pt irreducible floor** (both write the Close-class telemetry home —
cannot parallelize). #2645 (16) is the makespan driver unless fissioned; once fissioned the
12-pt pair governs. Fixed serial tail: Stage-13 close-out corpus (single milestone write).

## File-Contention Map (Stage-6 serialization)

- `pipeline-event-log-schema.md` — **write-pair: #3301 + #2423** (per D1); **#2645 reads**.
  Stage 5 resolved the #3301 fork to (A) schema-side, so its §3 `decision` enum write stands;
  #2423 adds a net-new top-level `event_type=session-retro` row. Disjoint regions (enum member
  vs. new row) → no overwrite, but **serialize #3301 → #2423** (Wave A before Wave C) so the
  precedent lands first. Both writes are recorded in ADR-086.
- Close-class telemetry home — **#2646 AND #2647 write** → **SERIALIZE #2646 → #2647**
  (true write-write); confirmed at Stage 5 as `close-class-telemetry.md`.
- calibration-data surface — #2645 read-only. pipeline-event-log data — #2423 append-only.
  Low contention.
- Stage-4 OPEN question ("if #2423's retro signal needs a new subtype it co-writes the schema
  alongside #3301") — the #3301 Stage-5 design provisionally resolved this to "no write-pair
  expected" on the `[ASSUMPTION – CONFIRM]` that #2423 reuses the #46 substrate with no new
  schema. **Collective Review D1 supersedes that assumption:** the ADR is scoped to cover a
  net-new top-level `event_type=session-retro` from #2423, so
  `pipeline-event-log-schema.md` **IS a co-owned write-pair** (#3301 §3 `decision` enum ·
  #2423 new `event_type` row). The two writes are structurally disjoint (an enum member
  inside an existing row vs. a new table row), so they do not overwrite each other, but the
  Wave-A sequencing is now load-bearing rather than precautionary: **#3301 lands first** so
  #2423 extends a §3 that already carries the precedent. Graded under CIAC-1.

## Stage Applicability Matrix

| Issue | 5 | 6 | 7 | 8 | 9 | 12 | 13 | Skip rationale |
|---|---|---|---|---|---|---|---|---|
| #3301 | LIGHT | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | Stage 5 = small fork-pick, not full solutioning |
| #2646 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | Stage 5 may conclude "not-computable" → early close |
| #2647 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | Stage 5 reuse-first may bound ledger scope |
| #2645 | ✓heavy | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | fission + anti-overfit at Stage 5 |
| #2423 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ hook=sep | ✓ | hook governance seam at Stage 12 |
| #166 | N/A | N/A | N/A | N/A | ✓ | ✓ | ✓ | verification-only, no code: skip 5/6/7/8 |

## Decomposition Flags

- **#2645 (XL/16) — FISSION before build** (front/middle cluster per its AC; drops
  critical-path contribution 16 → ≤6). CHEAP re-split.
- **#2647 (L/8) — carry whole**; run the reuse-first check at Stage 5 first (may bound scope
  down).
- Milestone-level oversize already resolved: operator SIZE-BAND OVERRIDE (Bundle Amendment
  Log, 2026-07-19) — ship whole as an epic-#1189 drain. Stage 4 does not re-litigate.

## Collective Review — SCOPE LOCK (2026-07-19)

Stage 5 Solutioning wave complete (5 spokes, all posted + consumed). Operator LOCKED release
scope with the following decisions:

- **D1 (ADR): YES** — author an ADR for the shared event-log-schema change (adds
  `decision`/`cascade-sweep-block` (#3301) + new top-level `event_type=session-retro` (#2423)).
  Scaffold via adr-helper at Stage 6.
- **D2 (#2423 scope): (a) KEEP FULL** — build skill + emission in the release PR; the `Stop`-hook
  *activation* is a SEPARATE operator-instance step (`.claude/settings.json`, ship
  warn/`.mode`-gated), approved separately at Stage 9 / 12.
- **D3 (#2646):** early-close accepted — rate **not computable** (4 candidate denominators
  structurally fail), presence-only retained.
- **D4 (#2645):** new-standards home ratified (front/middle-cluster standards, NOT
  `calibration-data.md`); the **7 DEFER** indicators documented as out-of-scope-this-release.
- **D5:** release scope LOCKED as the 5 Stage-5 designs.

### Locked build scope (per-issue)

| Issue | Build | Files | Serialize |
|---|---|---|---|
| #3301 | fork A: add `cascade-sweep-block` to `pipeline-event-log-schema.md` §3 + `append-pipeline-event.sh` validation | 2 | before #2423 (CIAC-1) |
| #2646 | strengthen Indicator-5 deferral → reached-determination → **early-close** | 1 (`close-class-telemetry.md`) | before #2647 (CIAC-2) |
| #2647 | bounded read-model (no net-new store; scaffold = evidence trail); Indicator-6 rate | 3 | after #2646 |
| #2645 | 2 sub-slices (front/middle-cluster standards + compute tools); 13 read-models (4 pointer / 2 narrowed) | 4 new + xref | — (schema reader) |
| #2423 | new `event_type=session-retro` + retro skill + signal-only emission (hook activation = separate approval) | ~9 | after #3301 (CIAC-1) |
| #166 | verification-only — no build; re-run + record + close at Stage 12 | — | — |

## Build Waves

Milestone = one PR; waves are commit-ordering groups (write-serialized).

- **Wave A — substrate:** #3301 — Engineering Commit 0 (release branch + this plan file +
  version re-verify), then the schema fix.
- **Wave B — foundation:** #2645 ∥ (#2646 → #2647 serial on `close-class-telemetry.md`).
- **Wave C — skill-core:** #2423 (after #3301, shared schema; hook = separate approval).
- **Verification track:** #166 (orthogonal; no build; Stage-9 → 13 close on `∈[20%,50%]`).

Stage 5 / 7 / 8 are parallel-safe; Stage 6 / 13 are write-serialized. The ADR (D1) is authored
during Wave A / C on the co-owned schema surface.

## Cross-Issue Acceptance Criteria

- **CIAC-1** — Issues #3301, #2645, #2423 · shared surface `pipeline-event-log-schema.md`:
  after the release, every emission a stage shard codifies validates against the schema AND no
  telemetry read-model requires a schema subtype absent from §3. Verify: run the codified
  emissions + read-model reads against the merged schema. *[graded at Stage 9]*
- **CIAC-2** — Issues #2646, #2647 · shared surface the Close-class telemetry home:
  Indicators 5 & 6 both read as rates (or a recorded not-computable determination) with no
  mutual overwrite. Verify: both indicator rows present + computed on the merged file.

## Risk Register

No passive voice — each risk carries an owner + mitigation.

| # | Risk | Owner | Mitigation | Rev. |
|---|---|---|---|---|
| R1 | #3301 open design-fork (schema vs re-point) changes the contention map | #3301 Stage 5 | RESOLVED at Stage 5 → fork (A) schema-side, rationale recorded (D-3301-A) and ratified by D1/D5 | CHEAP |
| R2 | #2423 SessionEnd hook not-in-git ("No ungoverned changes") | operator + #2423 | skill + emit ship in the PR; **hook deploy = SEPARATE approval artifact** at Stage 9 / 12 | MODERATE |
| R3 | #2645 XL scope / overfit | #2645 Stage 5 | fission + reuse-first + anti-overfit; CHEAP re-split | CHEAP |
| R4 | "no new schema" constraint (#2645 / #2646 / #2647) — uncomputable-from-existing | foundation spokes | Stage 5 reuse-first computability check; #2646 has the "declare not-computable + close" escape (exercised → D3) | CHEAP |
| R5 | #2646 / #2647 merge-conflict on the Close-class home | build sequencer | single-branch sequential commits; serialize #2646 → #2647 | CHEAP |
| R6 | v3.80 slot contended by 4 concurrent in-flight release branches | Hub (Stage 12) | version binds at the Stage-12 tag-claim, not at branch creation; re-run the A.5.6c freeness check pre-merge and forward-renumber the label on the branch if taken | CHEAP |

## Reversibility Posture

Stage 6 = **MODERATE** (branch + commits, git-revertable). Point-of-no-return remains Stage 12
(Execute / merge) — never auto-crossed.
