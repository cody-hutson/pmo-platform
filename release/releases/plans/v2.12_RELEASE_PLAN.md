<!-- repo-integrity: allow-issue-ref -->
<!-- reference-durability: allow-link -->
<!-- reference-durability: allow-version-ref -->
## Stage 4 Release Plan — v2.12 field-first-intake-enforcement

> **Re-version lineage:** v2.12 → v2.14 → v2.12 (collision avoidance). Operator-directed final version at Stage 12 = **v2.12** — it fills the sub-ceiling gap (v2.12 untagged with no plan slot on `origin/main` at execute; tag set was v2.10/v2.11/v2.13/v2.14). The branch name stays `release/v2.14-field-first-intake-enforcement` (cosmetic working name); the authoritative version is this plan file + the v2.12 tag.

> **Final version:** v2.12 (operator-directed at Stage 12; re-verified free — no v2.12 tag and no `v2.12_RELEASE_PLAN.md` on `origin/main`). Planning baseline was `20d168b`; the version was re-derived twice for collision avoidance (v2.12→v2.14 at branch creation when v2.12 was contended below the then-v2.13 ceiling, then v2.14→v2.12 at Stage 12 once v2.14 was claimed by a concurrent release and v2.12 stood free as the sub-ceiling gap). · **Milestone:** #197 (`12-field-first-intake-enforcement`; roadmap ordinal 12 · Project Data Architecture slice · 3 build cards) · **Base commit:** fresh `origin/main` at branch-creation · **Plan date:** 2026-06-20
> **Persona:** Release Manager — Release Planning (Tier 2; sequencing + change-spec + risk, no calendar dates)
> **Release Class:** novel (≥3 D-class decisions) · Engagement density Tight · Stage 9 review depth Deep · Stage 5 activation bias ALL
> **Topology:** single-branch (D-C SINGLE) — commits land sequentially on one release branch; the release→main PR is created later, before Stage 9.
> **Canonical source:** the approved Stage 4 spoke output + Phase B1 operator-decisions/hub-reconciliation + Collective Review scope-lock + the v2.12→v2.14→v2.12 re-version records on planning sub-task #1641.

---

### Summary (30 seconds)

Three buildable cards, all in the intake / Definition-of-Ready enforcement family, all CHEAP-to-MODERATE reversible, clustered on two surfaces (`improvement.yml` + `gate-criteria-spec.md`). **#27 is the anchor** (form-level + content-level G1 enforcement; extends the already-running `deploy.sh` Check 22 structural validator rather than building from scratch). **#344 is a small additive template change** (represent work domain) that contends with #27 on `improvement.yml`. **#54 is a `type:observation` reconciled to a HYBRID determination at Stage 5** — its field-first priority idea is accepted but is *largely already built* (a queryable GitHub Projects Priority field, body-derived, already exists); its genuine net-new contribution is a body→Projects-Priority sync gate criterion (new **G2-12**), modeled on the already-canonical G2-06 Decision-Date precedent. **#54's "G2-04 = priority validation" framing was stale** — live G2-04 is dependency validation; priority is G2-01 — corrected in the Stage 5 determination and carried into Engineering.

Build sequence: **#54 → #27 → #344**, write-serialized at Stage 6 (the hub serializes Stage 6 under D-C SINGLE). #54 lands first to settle the Gate-2 wording (#27's Gate-1 text references G2-12); #27 claims the larger `improvement.yml` change before #344 rebases its additive field. Two closed predecessors (#287, #235) are closure-record only — no gap review, no rebuild.

---

### Dependency Graph

Directional (`A → B` = A must land before B):

```
#54 (G2-12 body→Projects-Priority sync criterion + Gate-2 wording reconcile)
   └─→ #27  [SOFT: #27's Gate-1 enforcement-layer-split text references G2-12 in the SAME
             gate-criteria-spec.md; settling #54's Gate-2 wording first keeps the spec
             internally coherent when #27 lands its Gate-1 policy]
#27 (form + content-level G1 enforcement; extends Check 22; decouples to g1-enforcement.mode WARN)
   └─→ #344 [HARD-ish FILE: both edit .github/ISSUE_TEMPLATE/improvement.yml — write-serialize, #27 first]
#344 (optional Domain dropdown in improvement.yml)
   ··→ #351 [CROSS-MILESTONE, NOT in this release — complement only, see below]
```

**Cross-milestone dependency — #351 (verified live):** #351 is OPEN in milestone `18-pmbok-coverage-and-project-schema` (NOT this milestone), `type:story`, "Add a first-class domain / deliverable-type axis to PROJECT.md". #344's body names #351 as a *complement*, not a blocker ("independently shippable; #344 is the template layer, #351 is the schema layer"). #351 is one ordinal-18 milestone away and must not gate this ordinal-12 release. Resolution in D-344-mechanism-timing below.

**No intra-release hard blockers.** All three cards carry `Blocked by: —` in their bodies. The only ordering constraints are (a) the #27↔#344 file-contention on `improvement.yml` and (b) the soft #54→#27 gate-spec-wording sequencing.

---

### Implementation Sequence (#54 → #27 → #344)

| Order | Issue | Why here | Reversibility |
|---|---|---|---|
| 1 | **#54** | Adds criterion **G2-12** (body→Projects-Priority sync) to `gate-criteria-spec.md § Gate 2` + Triage wiring, and corrects the stale G2-04 reference. Settling the Gate-2 priority wording FIRST means #27's Gate-1 enforcement-layer-split documentation edits the spec once, coherently. Smallest, lowest-risk; unblocks clean #27 edit. | CHEAP/HIGH |
| 2 | **#27** | Anchor card. Documents the G1-01..G1-09 HARD-STOP vs SOFT-WARN split into `gate-criteria-spec.md § Gate 1`, aligns template flags, extends the runnable structural validator (Check 22), and decouples it to a new `g1-enforcement.mode` shipping in WARN mode. Claims the larger `improvement.yml` change before #344's additive field. | MODERATE/HIGH |
| 3 | **#344** | Additive optional `Domain` dropdown in `improvement.yml`. Sequenced last so it rebases onto #27's template state (avoids the known `improvement.yml` edit collision). Field-vs-enum mechanism settled at Stage 5. | CHEAP/HIGH |

Rationale for #54-first over #27-first: #54 is a Gate-2 wording-and-criterion change in `gate-criteria-spec.md § Gate 2`; #27 edits `§ Gate 1` of the same file. Different sections (disjoint line blocks), so not a hard line-overlap, but settling #54's clarification first keeps the spec internally consistent when #27 lands its layer-split policy. #27's Gate-1 text references G2-12, reinforcing #54-first.

---

### Stage Applicability Matrix (reconciled at Phase B1)

| Issue | S5 Solutioning | S6 Eng | S7 DevTest | S8 QA | S9 PlanReview | S12 Execute | S13 Close | Note |
|---|---|---|---|---|---|---|---|---|
| #54 | Y (#1644) | Y | S | S | Y | Y | Y | Stage 5 = reconcile field-first priority expansion → HYBRID / G2-12 |
| #27 | Y | Y | Y | Y | Y | Y | Y | anchor card; only functional-code deliverable |
| #344 | Y | Y | S | S | Y | Y | Y | skip-biased; revisit if Stage 5 reveals functional impact |

Per-issue justification:
- **#54** — Stage 5 APPLIED (operator changed the Stage 4 disposition from clarification-only to RECONCILE; Stage 5 rendered the HYBRID determination on #1644). SKIP Stage 7/8 — the deliverables are a gate-spec criterion + Triage-doc wiring; a prose/spec change verifies via grep that the wording exists (the Stage-6 AC), not a unit/QA run.
- **#27** — ALL stages apply. Stage 5 REQUIRED (the enforcement-layer split + Check-22 extension + the warn→enforce decouple are genuine design decisions; D-class). Stage 7/8 REQUIRED (a runnable validator extension is executable code with functional impact; must be tested against known-good + known-bad bodies per its own AC).
- **#344** — Stage 5 REQUIRED (field-vs-enum mechanism is a Stage 5 decision). Stage 7/8 skip-biased: an optional YAML dropdown add to `improvement.yml` has no runnable logic to unit-test; verification is structural (template renders; G1-01 still passes for both ticket shapes — a Stage-6 AC dry-run). Operator confirmed skip at scaffolding.

Note: This milestone (#197) carries no `## Parallelization Map` section (predates that Stage-3 convention) → the Stage 4 Parallelization-Map currency check is correctly suppressed for this release.

---

### Contention Map

| File | #344 | #27 | #54 | Contention class | Resolution |
|---|---|---|---|---|---|
| `.github/ISSUE_TEMPLATE/improvement.yml` | edit (add optional `Domain` dropdown) | edit (align flags + 13 D2 stale-path fixes across 3 templates) | — | **DIRECT — same file** | Write-serialize: #27 lands first, #344 rebases its additive field onto #27's state. Distinct concerns (flag tuning + path fixes vs. new field) → low merge-conflict risk if commit order preserved. |
| `core/schemas/gate-criteria-spec.md` | read-only (G1-01 dry-run) | edit (`§ Gate 1` — HARD-STOP/SOFT-WARN doc) | edit (`§ Gate 2` — add G2-12, reconcile G2-01 wording, correct stale G2-04 ref) | **SAME FILE, DIFFERENT SECTIONS** | Gate 1 and Gate 2 are disjoint blocks → not a line-overlap. Sequence #54 (Gate 2) before #27 (Gate 1) for spec coherence; git merge is clean regardless since sections do not share lines. #344 touches this file read-only — no write contention. |
| `core/specs/label-taxonomy.md` | conditional edit (only IF a `domain` label/enum is introduced — verified zero `domain` matches today) | — | — | none (single writer) | No action unless Stage 5 introduces a label. |
| `release/references/pipeline/stage-02-triage.md` | — | edit (content-check entry-point ref, per #27 AC) | edit (annotate the A5/B2a priority-field write as G2-12's enforcement point; add G2-12 to Workflow-Readiness exit criteria) | LOW — same file, both additive references | #54 lands first; #27 adds the content-check entry-point ref after. Append-class, low risk. |
| `.github/workflows/*` | — | (none — Check-22 extension stays in `deploy.sh`; spoke's "new workflow file" trigger retracted at Phase B1) | — | none | No new workflow file in this release. |

The two known signals from the planning prompt are confirmed: (1) #27 and #344 both edit `improvement.yml` (DIRECT contention → serialize #27→#344); (2) #54 and #27 both touch `gate-criteria-spec.md` but in different gate sections (#54 = Gate 2; #27 = Gate 1) → no line-overlap.

---

### File Change Matrix

One path per line, `intent` ∈ {add, edit, edit?, edit-conditional, read} — downstream Stage 7/8/9 chips extract deterministically. `edit?` = applicability pending operator; `edit-conditional` = depends on a Stage 5 mechanism choice.

```
release/releases/plans/v2.12_RELEASE_PLAN.md | add | Stage 4 plan committed at Engineering Commit 0 (single-branch topology default; re-versioned v2.14→v2.12 at Stage 12)
core/schemas/gate-criteria-spec.md | edit | #54 (§ Gate 2 — add G2-12 body→Projects-Priority sync criterion + self-repair row; state body-canonical/field-mirror/label-not-a-surface near G1-06/G2-01; reconcile G2-01 wording; correct stale "G2-04=priority" framing — G2-04 stays dependency validation) ; #27 (§ Gate 1 — document HARD-STOP vs SOFT-WARN enforcement-layer split across G1-01..G1-09)
.github/ISSUE_TEMPLATE/improvement.yml | edit | #27 (align flags; fix D2 stale paths) ; #344 (add optional Domain dropdown) — SERIALIZE #27 then #344
.github/ISSUE_TEMPLATE/bug.yml | edit | #27 (D2 stale-path fixes; audit required-field set against G1 via bug adapters)
.github/ISSUE_TEMPLATE/observation.yml | edit | #27 (D2 stale-path fixes; audit required-field set against G1 observation-branch)
release/references/pipeline/stage-02-triage.md | edit | #54 (annotate A5/B2a priority-field write as G2-12 enforcement point; add G2-12 to Workflow-Readiness exit criteria) ; #27 (reference the runnable content-level check entry point)
core/specs/label-taxonomy.md | edit-conditional | #344 (define a domain label ONLY IF Stage 5 introduces one; else N/A — enum/field-only change)
core/deploy/deploy.sh | edit | #27 ONLY (extend Check 22; decouple to g1-enforcement.mode WARN) — #54 does NOT touch deploy.sh (Check 22 / G1-06 body-priority anchor stays untouched by #54)
```

---

### Risk Register

| ID | Risk | Type | Reversibility | Confidence | Mitigation |
|---|---|---|---|---|---|
| R1 | **#54's premise was stale** — its named gate G2-04 is dependency-reference validity, NOT priority validation; the priority criterion is G2-01. Building #54 as originally written (enforce a priority *label* at G2-04) would (a) wire enforcement to the wrong gate and (b) contradict G1-06 ("label-based priority is NOT expected"). | scope/premise | n/a (avoided) | HIGH | Stage 5 HYBRID determination corrected the reference and rejected the label-enforcement premise. Engineering adds G2-12 (body→Projects-Priority sync) instead; does NOT add a priority-label rule; does NOT modify G1-06. |
| R2 | **#27↔#344 `improvement.yml` edit collision** — both edit the same template file. | contention | CHEAP | HIGH | Serialize Engineering: #27 commits first; #344 rebases. Under D-C SINGLE the hub write-serializes Stage 6 — standard path. |
| R3 | **#344 mechanism ambiguity** — new `Domain` field vs Category-enum expansion vs align-to-#351-axis. The "align to #351" option imports a cross-milestone (ordinal-18) dependency that would stall this release. | dependency | CHEAP | HIGH | D-344 (A): build self-contained (own optional `Domain` dropdown); do NOT adopt the #351 axis as a hard dep. #351 stays a complement. |
| R4 | **#27 content-check is real executable code** — the runnable G1 validator extension is the only functional-impact deliverable; bugs here have downstream triage impact. | scope | MODERATE | MEDIUM | Stage 5 design + Stage 7/8 testing both REQUIRED for #27. Test against ≥1 known-good and ≥1 known-bad issue body per #27 AC. |
| R5 | **Warn→enforce flip blocks this release's own deploy gate** — `deploy.sh` Check 22 in enforce mode FAILs on `status: bundled` G1 violations; this release's own issues are bundled. | execution | CHEAP | HIGH | Collective Review decision: **decouple + warn now, enforce flip later.** #27 ships `g1-enforcement.mode` in WARN mode (honors the ≥3-day shakedown discipline). The enforce flip is deferred to follow-on #1686. |
| R6 | **#54 audit-baseline drift** — the original "9/9 approved issues lack a priority label" finding rests on a transiently-small population (2026-05-22). | scope | CHEAP | HIGH | Baseline re-pinned at planning: at HEAD `20d168b`, 20/20 open `status: approved` issues lack a `priority:` label. Per G1-06 this is conformance, NOT a gap — reinforces R1. |
| R7 | **Rollback complexity** — all changes are doc/template/config edits + one `deploy.sh` Check-22 extension. | rollback | CHEAP (#54, #344) / MODERATE (#27) | HIGH | `git revert` of the release branch restores prior `improvement.yml` + spec + `deploy.sh`. No data migration, no state mutation. |

---

### Operator Decisions (D-gates) — rendered at Phase B1 + Collective Review

#### D-Observation-#54: Promote, clarify, or defer? → **RECONCILE** (operator override of the spoke's clarification-only recommendation)
**Gate input:** #54 body (`type:observation`); live `gate-criteria-spec.md` G2-04 + G2-01 + G1-06; re-pinned baseline (20/20).
**Operator decision:** **#54 = RECONCILE** (not clarification-only): determine whether the field-first priority expansion is an acceptable data-architecture expansion and how to reconcile it vs. body-level G1-06; track on the reconciliation list. → #54 Stage 5 now **applies**.
**Stage 5 determination (HYBRID, #1644):** body-authoritative + already-built Projects-field mirror + a new body→field sync criterion at Gate 2. ADOPT-FIELD-FIRST rejected (would invalidate the 22/22-conformant backlog and break the G1-06/Check-22 chain for zero portability gain the mirror does not already deliver). CLARIFY-AND-KEEP-BODY-LEVEL rejected (under-reconciles). HYBRID selected — body canonical, Projects field the queryable mirror, new **G2-12** sync gate, modeled on G2-06. **Stale-reference correction:** #54's "G2-04" is dependency validation; priority is G2-01. Reversibility MODERATE/HIGH.

#### D-Closed-Issues: Gap review on #287 + #235?
**Gate input:** full bodies + closure comments of #287, #235 (read live).
**Recommendation: closure-record only for BOTH; no gap review, no rebuild.**
- **#287** — closed as "Merged into #27" — an administrative absorption, not a completed build. The residual (the runnable content-level check) is real and carried by the open anchor #27 (its AC). No separate gap review; #27 owns the scope. #287 stays closed as duplicate-absorbed.
- **#235** — closed as superseded; its AC targeted `IMPROVEMENTS.md` + `PMO.md`, both removed when the backlog moved to GitHub Issues. No buildable residual against the current tree. Closure-record only.
**Reversibility:** CHEAP/HIGH (a closure-record disposition changes nothing in-tree).

#### D-344-mechanism-timing: Self-contained vs depend-on-#351?
**Gate input:** #344 body; live #351 state (OPEN, milestone `18-pmbok-coverage-and-project-schema`, `type:story`).
**Recommendation: (A) self-contained.** #351 lives in a different, later milestone (ordinal 18 vs this release's ordinal 12) and is a `type:story` (larger scope). #344's own body states it is independently shippable in the template alone. Adopting the depend-on-#351 option would stall an ordinal-12 release behind ordinal-18 work — clear over-serialization. Build #344 with its own optional `Domain` dropdown now; keep #351 as the named complement (the schema layer). The field-vs-enum choice was settled at Stage 5 (optional `Domain` dropdown). Reversibility CHEAP/HIGH.
**Upstream compatibility:** N/A — an `improvement.yml` form-field add is not skill-authoring surface.

#### D-Version: which version?
**Operator decision (Phase B1):** v2.12 (not the spoke's candidate v2.13). **Re-version at branch creation:** re-derived against fresh `origin/main` (planning baseline was 15 commits stale) — `git describe` ceiling = v2.13; v2.12 contended by two in-flight release branches; **v2.14 verified free** → re-versioned up to v2.14 per the versioning-lineage discipline (decide by `git describe`, NOT semver-max; re-version up on collision). **Stage 12 re-version (final):** at execute, a concurrent release had claimed v2.14 (tag set v2.10/v2.11/v2.13/v2.14) and v2.12 stood free below the ceiling with no plan slot on `origin/main` — operator directed the final version **back to v2.12** to fill the sub-ceiling gap. Final lineage: **v2.12 → v2.14 → v2.12**.

#### D-ReleaseClass: which class? → **novel**
Three D-class decisions (D-Observation-#54, D-Closed-Issues, D-344-mechanism-timing) satisfy the `novel` trigger "≥1 D-class decision in release plan". The spoke's second novel trigger ("new `.github/workflows/*` file class") was **retracted at Phase B1** (#27 extends the existing `deploy.sh` Check 22 rather than adding a workflow file); `novel` stands on the ≥3-D-decisions trigger alone. routine/cross-cutting/hotfix all fail their thresholds.

---

### Release Class Declaration

**Class: `novel`** (operator-rendered at the D-ReleaseClass gate).

**Trigger-condition evidence:**
- **novel trigger (b)** — "≥1 D-class decision in release plan." This plan carries **three** D-decisions (D-Observation-#54, D-Closed-Issues, D-344-mechanism-timing). FIRES.
- **novel trigger (a)** — RETRACTED at Phase B1. #27 extends `deploy.sh` Check 22 (an existing structural validator) rather than adding a new `.github/workflows/*` file class. Does NOT fire.
- **routine ruled out** — `improvement.yml` has only 1 prior commit (routine trigger (b) requires ≥3 prior release touches); #27 also adds runnable-validation surface (routine trigger (c) requires zero new files/logic). Both routine triggers fail.
- **cross-cutting ruled out** — touches at most 1 `pipeline/stage-*.md` (`stage-02-triage.md`) and 1 named governance surface (`gate-criteria-spec.md`); neither reaches the ≥3 threshold. Does NOT fire.
- **hotfix ruled out** — no P1/P2 defect against a deployed release; forward intake-enforcement work.

**Multi-trigger resolution:** only `novel` fires → `novel` stands.

**Differentiation posture for `novel`:**
- **Engagement density: Tight** — structured handoff / Decision Briefing at every transition.
- **Stage 9 review depth: Deep** — PR diff review includes design-spec conformance + blast-radius (the Check-22 extension is executable; the gate-spec edits are governance).
- **Stage 5 activation bias: ALL** — #27 and #344 both legitimately carry Stage 5 design questions (#54's Stage 5 applies after the operator's RECONCILE decision).
- **Stage 13 outcome-window: standard** (no hotfix).

---

### Collective Review — N-way consistency scope-lock (RENDERED)

**N-way consistency: PASS.** All 3 Stage 5 designs cohere — all three keep G1-06 body-canonical:
- **#54** = Gate-2 / **G2-12** body→Projects-Priority field-sync (new criterion).
- **#27** = Gate-1 enforcement-layer split (HARD-STOP / SOFT-WARN documentation) + Check-22 extension.
- **#344** = optional `Domain` field in `improvement.yml`.

D2 stale paths → #27's three-template audit. Upstream-compat N/A (no skill-authoring surface). All 3 R1-grounded.

**Locked Stage 6 order: #54 → #27 → #344** (D-C SINGLE). #27's Gate-1 text references G2-12 → #54 lands first; #27 claims `improvement.yml` before #344 rebases.

**Per-card scope (locked):**
- **#54** — add criterion **G2-12** (body→Projects-Priority sync) + Triage wiring; correct the stale G2-04 reference. (CHEAP)
- **#27** — document HARD-STOP/SOFT-WARN split (Gate 1); extend Check 22 (add G1-05a + recommend-flags → evaluate-9 / enforce-5 / flag-4); **decouple Check 22 to a new `g1-enforcement.mode` shipping in WARN mode**; fix 13 D2 stale paths across 3 templates; zero template `required:` additions; intake-scope ratified bundled-only.
- **#344** — add optional `Domain` dropdown to `improvement.yml`; rebases onto #27. (CHEAP)

**Warn-flip-deferred decision:** operator chose **decouple + warn now, enforce flip later** — honors the ≥3-day shakedown discipline (`bypass-mode-readiness.md`) and avoids blocking v2.12's own deploy gate (enforce mode FAILs on `status: bundled` G1 violations; this release's own issues are bundled). The enforce flip is deferred to follow-on #1686.

**Follow-ons filed (unmilestoned, for operator triage):**
- **#1684** — Projects-Priority field reconcile / backfill across the approved issues (data migration; distinct reversibility; deliberately NOT folded into #54 / G2-12).
- **#1685** — triage-doc warn-log path drift.
- **#1686** — enforce flip after shakedown.

---

### Recommendations

1. **Build order #54 → #27 → #344**, write-serialized at Stage 6 (the hub serializes Stage 6 under D-C SINGLE). #54 and #344 are doc/template-only; #27 is the one functional card.
2. **#54 builds the HYBRID determination** (add G2-12 + Triage wiring + stale-ref correction); does NOT add a priority label and does NOT modify G1-06 / Check 22. (R1, D-Observation-#54.)
3. **#344 self-contained**, decoupled from #351 (different/later milestone). Optional `Domain` dropdown. (D-344.)
4. **Issues #287 and #235 (both already closed): no rebuild.** #287's residual is owned by open card #27; #235 has no residual. Closure-record dispositions only. (D-Closed-Issues.)
5. **Mark all three open cards as closed at Stage 13** via the PR's Issue References block — never use close-family verbs + `#N` anywhere else in PR bodies (the parser is lexical).
6. **Version:** final **v2.12** (operator-directed at Stage 12; re-verified free — fills the sub-ceiling gap after v2.14 was claimed by a concurrent release). Lineage v2.12→v2.14→v2.12.

**Discoveries outside scope (noted, not actioned):**
- **D1 — #54 body carries a factually-wrong gate reference** ("G2-04 priority validation"). The issue body itself should be corrected so the backlog does not preserve the false anchor. The Engineering spoke does NOT edit the #54 issue body (a hub GitHub-write per observation #1342) — the correction is surfaced to the hub for application.
- **D2 — `improvement.yml` internal path references are stale** (pre-`core/`-restructure `pmo-platform/reference/...` / `pmo-platform/governance/...` paths across the three templates). Folded into #27's template audit (13 stale paths across 3 templates).

---
*Stage 4 Release Plan, consolidated for Engineering Commit 0 from the approved Stage 4 spoke output + Phase B1 reconciliation + Collective Review scope-lock + the v2.12→v2.14→v2.12 re-version records on planning sub-task #1641.*

### References
- #197 — milestone `12-field-first-intake-enforcement` (this release's milestone).
- #1641 — Stage 4 planning sub-task (canonical source: Stage 4 plan + Phase B1 + Collective Review scope-lock + re-version record).
- #1644 — #54 Stage 5 Solutioning sub-task (HYBRID determination; G2-12 spec; stale-ref correction).
- #1645 — #54 Stage 6 Engineering sub-task (this commit's authorizing instruction).
- #54 — field-first priority (RECONCILE); #27 — form + content-level G1 enforcement (anchor); #344 — domain representation in `improvement.yml`.
- #287, #235 — closed predecessors (closure-record only).
- #351 — cross-milestone complement (milestone 18); not in this release.
- #1684, #1685, #1686 — Collective-Review follow-ons (unmilestoned).
