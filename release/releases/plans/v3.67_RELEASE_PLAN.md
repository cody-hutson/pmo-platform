# v3.67 Release Plan — 79-qa-devtest-modes-and-automated-eval-execution

> Version **v3.67** (provisional at Stage 4 as v3.66; **COLLIDED** — the concurrent release `v3.66-software-domain-templates` (PR #3308) merged during the Stage-5→6 pause and claimed v3.66; hub re-determination 2026-07-09 recomputed next-free minor = v3.67. **Commit-0 re-verify PASSED 2026-07-09**: tag-set max v3.66 (v3.65.1 is a `.Z` patch, does not consume the minor slot); plan-file set max v3.66; latest published GitHub Release v3.65; zero v3.67 tags / plan files / ledger rows → recomputed next-free = v3.67). Claimed on the branch axis at Commit 0 (`release/v3.67-qa-devtest-modes-and-automated-eval-execution`); bound atomically at Stage-12 tag-claim. This file consolidates the approved Stage-4 plan (#3219 first comment), the Phase B Operator Decision Record (2026-07-03), the Collective Review scope-lock amendments (#3302, 2026-07-04), the mid-pipeline divergence disposition (2026-07-09), and the D-Version re-determination (2026-07-09), carried into Stage 6.

## Header

| Field | Value |
|-------|-------|
| **Version** | v3.67 (provisional v3.66 collided; re-determined 2026-07-09; Commit-0 re-verify PASSED 2026-07-09) |
| **Date Created** | 2026-07-09 (Thursday) |
| **Release Manager** | Agent-assisted (release-hub Mode O; operator gates per Phase B / Collective Review scope-lock) |
| **Status** | Executing (Stage 6 complete — Seq 4 of 4 landed; awaiting Stage 7 Dev Testing) |
| **Branch** | release/v3.67-qa-devtest-modes-and-automated-eval-execution (cut from origin/main 671e0bf — contains v3.65.1 + v3.66) |
| **PR** | #3314 (draft — ready-for-review at the Stage-9 gate) |
| **Milestone** | 79-qa-devtest-modes-and-automated-eval-execution |

## Scope

### Issues Included

| # | Issue | Title | Size | Mode letter | Labels |
|---|-------|-------|------|-------------|--------|
| 1 | #222 | Add dedicated Dev Testing mode to QA Auditor skill | L | **G** (D-ModeLetters) | cluster: skill-modes, layer:skill-implementation, size:L, skill-update, status: bundled, project:pipeline, type:story |
| 2 | #219 | Add acceptance-review mode to QA Auditor skill (Stage 8) | M | **H** (D-ModeLetters) | cluster: skill-modes, size:M |
| 3 | #2226 | Mode F release-process fitness audit | L | **F** (LOCKED at prior Collective Review) | cluster: skill-modes, size:L |
| 4 | #172 | Automated eval execution at ≥2 pipeline stages | M | (no new mode — wiring) | cluster: skill-modes, size:M |

Epic #1195 (ENG-Skill-Modes-Eval) under #573. Capacity: 24 pts (L8+M4+L8+M4) — inside the 15–25 band.

### Dependency Graph

| Edge | Type | Evidence |
|---|---|---|
| #222 → #172 | HARD intra-milestone | #172 registers automated invocation against stage skill modes (the Stage-7 trigger registers against Mode G by its identifier per the D-2 seam statement) |
| #219 → #172 | HARD intra-milestone | Same registration seam on the Stage-8 side; #172 AC1 requires ≥2 named stages |
| #218 → #219 | Cross-milestone, SATISFIED | Acceptance-assertion contract merged in v3.64 (`core/skills/eval-writer/references/acceptance-assertion-type.md`) — #219 CONSUMES, does not re-define |
| #2226 | Independent | Design inputs LOCKED at prior Collective Review; runner-dispatch seam is data-emission only |
| #219 ∥ #222 | No dependency, shared surface | Contention edge on SKILL.md, not dependency edge |

Provenance (NOT live edges — pre-renumber rot): #222's "#45 eval runner" / "Blocks #211"; #219's "Blocked by #197". Parent epic #1195 OPEN.

### Implementation Sequence (P0 fully-serial, one release branch, one release PR)

| Seq | Issue | Size | Rationale |
|---|---|---|---|
| 1 | #222 Mode G — Dev Testing | L | Largest surface; the Stage-7 seam #172 depends on; lands the release-wide count-free shared-surface conventions (D-4) + Engineering Commit 0 |
| 2 | #219 Mode H — Acceptance Review | M | Consumes the live #218 assertion contract; PR-1 rider re-anchors its four contended SKILL.md rows post-Seq-1; mode-section anchor "insert `### Mode H` immediately after `### Mode G`" |
| 3 | #2226 Mode F — Fitness Audit | L | Independent producer mode; inserts `### Mode F` BETWEEN Mode E and Mode G; new references/ + evals/ + scripts/ classes |
| 4 | #172 Automated eval execution | M | Integration seam — registers stage triggers against Modes G/H shipped at Seq 1–2; ZERO SKILL.md edit points (D-6 refinement; SKILL.md contention 4/4 → 3/4 per scope-lock) |

### File Change Matrix

Intent per file (scope-locked at Collective Review 2026-07-04; updated for v3.67):

| Path | Intent | Issue(s) |
|---|---|---|
| `core/skills/pmo-qa-auditor/SKILL.md` | EDIT (Modes G/H/F sections, selection surfaces, output variations, failure modes, producer note, D-4 count-free restructure, Change-5 staleness remediation, version bump) | #222, #219, #2226 (NOT #172 per D-6) |
| `core/skills/pmo-qa-auditor/references/dev-testing-mode-spec.md` | ADD | #222 |
| `core/skills/pmo-qa-auditor/references/acceptance-review-mode-spec.md` | ADD | #219 |
| `core/skills/pmo-qa-auditor/references/fitness-audit-mode-spec.md` | ADD (D-5 reference-file 1→2 split, CR-approved) | #2226 |
| `core/skills/pmo-qa-auditor/references/fitness-audit-dimension-rubric.md` | ADD | #2226 |
| `core/skills/pmo-qa-auditor/evals/fitness-audit-characterization-fixtures.md` | ADD (new dir) | #2226 |
| `core/skills/pmo-qa-auditor/scripts/fitness-audit-search-primitives.sh` | ADD (new dir) | #2226 |
| `core/config/allowlists/script-execution-allowlist.txt` | EDIT (D-7 append, 4-form block) | #2226 |
| `core/hooks/reference-durability-allowlist.txt` | EDIT (allowlist the fitness-audit mode-spec for the durability-delta gate — CF-3's `#[0-9]+` validation-regex literal; hub-direct CI fix, commit `c6490a3`) | #2226 |
| `release/references/pipeline/stage-07-dev-testing.md` | EDIT (#222: §3 persona row CD-3 replace + §9 mode-gap clause; #172: protocol H2 + runner clause + trajectory row + §7 metrics) | #222, #172 |
| `release/references/pipeline/stage-08-qa-testing.md` | EDIT (#219: §3 persona annotate + §9 gap line; #172: EI-S8 block + §7 metrics) | #219, #172 |
| `release/references/specs/stage-to-skill-mode-mapping.md` | EDIT (CR scope addition: row 7 forward-ref resolution = #222; row 8 = #219) | #222, #219 |
| `release/references/protocols/process-fitness-cadence.md` | EDIT (CR scope addition: §6 one-liner — Mode F executor + cadence runner as `dispatched`-column writer) | #2226 |
| `core/specs/autonomy-tiers.md` | EDIT (one Tier-2 example bullet) | #172 |
| `packages/pmo-qa-auditor.skill` + `.sha256` | REBUILD once at release-cut (release-scoped, shared) | all four |
| `release/releases/plans/v3.67_RELEASE_PLAN.md` | ADD (Engineering Commit 0) | release |

Machine-readable path list (one path per line, for Stage 7/8/9 chip extraction):

```
core/skills/pmo-qa-auditor/SKILL.md
core/skills/pmo-qa-auditor/references/dev-testing-mode-spec.md
core/skills/pmo-qa-auditor/references/acceptance-review-mode-spec.md
core/skills/pmo-qa-auditor/references/fitness-audit-mode-spec.md
core/skills/pmo-qa-auditor/references/fitness-audit-dimension-rubric.md
core/skills/pmo-qa-auditor/evals/fitness-audit-characterization-fixtures.md
core/skills/pmo-qa-auditor/scripts/fitness-audit-search-primitives.sh
core/config/allowlists/script-execution-allowlist.txt
core/hooks/reference-durability-allowlist.txt
release/references/pipeline/stage-07-dev-testing.md
release/references/pipeline/stage-08-qa-testing.md
release/references/specs/stage-to-skill-mode-mapping.md
release/references/protocols/process-fitness-cadence.md
core/specs/autonomy-tiers.md
packages/pmo-qa-auditor.skill
packages/pmo-qa-auditor.skill.sha256
release/releases/plans/v3.67_RELEASE_PLAN.md
```

### Contention Map

| Shared surface | Issues | Resolution |
|---|---|---|
| `core/skills/pmo-qa-auditor/SKILL.md` | #222 + #219 + #2226 (3/4 — #172 dropped to zero edit points per D-6, scope-lock recorded determination) | Serialize (P0); D-4 count-free restructure lands ONCE at Seq 1; siblings append rows/sections only — no count edits |
| Producer-asymmetry note | #222 authors (E + G bullets, FM-5-safe terminal sentence); #2226 inserts F bullet between E and G (final order E, F, G per CIAC-3); #219 = consumer (no bullet) | Seq-1 text is the appendable base |
| Mode-section ordering | #222 inserts `### Mode G` after Mode E; #2226 inserts `### Mode F` between E and G; #219 inserts `### Mode H` after G | Final order E/F/G/H — CIAC-1 verifies at Stage 9 |
| Line-440 report header | CD-2 scoped-pointer form landed at Seq 1 (gate-table modes A–D only; E/F producer-exempt; G/H excluded — own stage report formats; Mode H is NOT a member of that report frame) | No sibling edits |
| `stage-07-dev-testing.md` §9 gap census | #222 removes the mode-gap clause (6→5); #172 removes the runner clause (5→4) + flips the Automation-trajectory row | Serialized 1 → 4 |
| `stage-to-skill-mode-mapping.md` | #222 row 7; #219 row 8 (line-disjoint) | Serialized 1 → 2 |
| `packages/pmo-qa-auditor.skill` (+ `.sha256`) | all four converge | Rebuild ONCE at release-cut |

### Release Class declaration

**Class: novel** (operator-rendered at the plan gate, 2026-07-03). Trigger evidence: new reference/eval/script file classes (#2226, #222, #219); ≥1 D-class decision (D-C, D-Concurrency, D-ModeLetters). Engagement density: Standard · Stage 9 review depth: Deep · Stage 13 outcome-window: 30-day.

domain_practice: { source: N/A — pipeline-internal release, date: 2026-07-03, domain: software }

Classification rationale: dominant surface is skill/tooling code (SKILL.md modes + bash search primitives + eval fixtures) → `software`; secondary `governance` (pipeline-shard trigger registrations). Entire matrix is internal platform artifacts → external-sourcing exempt.

### Release Outcome Statement

**AFTER** — pmo-qa-auditor carries three new modes: a Stage-7 dev-testing mode (Mode G) that ingests a PR + release plan, runs the structural→contract→content-quality→integration eval ladder, and posts a quality report as a PR comment; a Stage-8 acceptance-review mode (Mode H) that ingests GitHub Issue AC and renders per-criterion verdicts under the Stage-8 enum by consuming the acceptance-assertion contract; and a Mode F release-process fitness audit emitting a dated audit artifact with UNTRACKED/PARTIAL/ALREADY-TRACKED classification. At least two pipeline stages invoke evals automatically, with operator escalation only on FAIL/EXCEPTION.

**BEFORE** — pmo-qa-auditor has five modes (A–E), all Cowork-skill-output reviewers; the Stage 7/8 shards name dev-testing and acceptance modes the skill does not have; DT/QA runs conversation-based with no automated eval invocation; the release-process fitness audit has no skill home.

**Actor(s):** pmo-qa-auditor (the three modes); Stage 7/8 spokes + hub (automated eval invocation); the shipped audit-cadence runner (Mode F deep-dive dispatch).

**Success Indicator:** the SKILL.md mode-selection table lists the three new modes with distinct triggers, AND ≥1 existing eval invocation runs automatically at a named pipeline stage with its FAIL/EXCEPTION escalation path recorded (pilot conformance evidence in this plan's Verification Evidence section).

## Operator Decisions (Phase B Decision Record + Collective Review scope-lock, rendered)

| Decision | Rendered | When |
|---|---|---|
| Plan + Release Outcome Statement | **APPROVED** as posted (sequence #222 → #219 → #2226 → #172; capacity 24 pts) | 2026-07-03 plan gate |
| D-ReleaseClass | **novel** | 2026-07-03 plan gate |
| D-C Branch Topology | **SINGLE** — one release branch, one release PR; plan file = Engineering Commit 0 | 2026-07-03 plan gate |
| D-Concurrency Posture | **P0 fully-serial engineering** (4-way SKILL.md contention at plan time; 3/4 after D-6) | 2026-07-03 plan gate |
| Stage-5 wave sizing | **2+2 split waves** | 2026-07-03 plan gate |
| D-ModeLetters | **F = #2226 (locked), G = #222, H = #219** | 2026-07-03 plan gate (recorded determination) |
| D-Version | Provisional v3.66 → **COLLIDED** → re-determined **v3.67** (2026-07-09); Commit-0 re-verify PASSED 2026-07-09; Stage-12 atomic tag-claim binds final | 2026-07-03 / 2026-07-09 (recorded determinations) |
| CR: N-way conventions | **RATIFIED**: D-4 count-free phrasing (release convention); #2226 R-6 "no PARTIAL" rider wording; producer list = E, F, G (CIAC-3 order) with FM-5 terminal-sentence fix (no "in-chat only" claim — Mode H's sub-task report home acknowledged); CD-3 per-shard persona-row treatment (replace@stage-07, annotate@stage-08) | 2026-07-04 scope-lock |
| CR: Line-440 | Gate-table-only enum + **CD-2 scoped-pointer header**. E/F producer-exempt; G/H excluded (own stage report formats) | 2026-07-04 scope-lock |
| CR: Premise discharges + riders | **ADOPTED**: PR-1 re-anchor rider on Seq-2; **PR-2 + FM-6 riders on Seq-1** (re-verify stage-07 v3.65 deltas at build; align Mode G rung-1 execution scope with EI-S7-01); PR-3 CIAC-1 restated (canonical-section placement is conformant); FM-3 stage-08 "8 gaps" token → #219's sweep; FM-7 banding interval-closure rider on Seq-3; FM-1/Change-6 script self-test = deployed hook layout | 2026-07-04 scope-lock |
| CR: Scope additions + LOCK | **APPROVED**: stage-to-skill-mode-mapping rows 7 AND 8 · cadence §6 one-liner · D-5 reference-file 1→2 split · Change-5 (9 dead `../../reference/` paths + Role-text remediation, per plan Rec 5). **SCOPE LOCKED. Stage 6 serial engineering authorized.** | 2026-07-04 scope-lock |
| Mid-pipeline divergence (v3.65.1) | **REBASE + RE-VALIDATE, INFORMATIONAL ONLY.** Branch cut from current origin/main (contains v3.65.1). Author-association trust boundary inherited by reference — no Mode G/H design change. Engineering re-anchors edits to current shard text. Warn-only Tier-1 safety net: a mode's comment-ingest flow found to bypass the protocol gate is flagged on its sub-task, not shipped silently | 2026-07-09 disposition |

**Action items (tracked to the 7a close gate):** AI-1 — verify SKILL.md Role-text ("6 operational skills") + Mode A schema-path staleness at Seq-1 Engineering (CONFIRMED at Stage 5; remediated via Change-5 in Seq-1). AI-2 — sibling-merge re-check at Stage 9 entry.

## Risk Register

| ID | Risk | Likelihood / Impact | Mitigation | Reversibility |
|---|---|---|---|---|
| R1 | 4-way write contention on SKILL.md corrupts mode tables if chips parallelize | HIGH if parallel / HIGH | P0 fully-serial + fixed sequence; D-4 restructure lands once at Seq 1; CIAC-1 at Stage 9 | CHEAP (pre-merge) |
| R2 | Mode-letter collision | MEDIUM / MEDIUM | D-ModeLetters ratified before Engineering (F=#2226, G=#222, H=#219) | CHEAP |
| R3 | Producer-asymmetry note invalidated ("lone producer") | HIGH without action / LOW-MEDIUM | Seq-1 rewrites to appendable producer list; CIAC-3 grades at Stage 9 | CHEAP |
| R4 | #172 scope creep at the integration seam | MEDIUM / HIGH | D-2 seam statement binding (#172 registers, adds no execution logic); ADR-019 compose-not-absorb | MODERATE |
| R5 | Mid-pipeline divergence (siblings merging) | HIGH / LOW | Materialized twice pre-Stage-6 (v3.65.1 + v3.66) — dispositioned; branch re-cut from 671e0bf; Stage 9 A6.5 re-check registered | CHEAP |
| R6 | `.skill` package drift (Check-7 class) | MEDIUM / LOW | Single rebuild at release-cut via `build-skill-packages.sh pmo-qa-auditor`; Check 7 in CI | CHEAP |
| R7 | Capacity 24 pts top-half of band | LOW-MEDIUM / MEDIUM | Named split-line: defer #172 if Stage findings push past 25 | CHEAP |
| R8 | #219 re-defines the #218 rubric instead of consuming it | LOW / MEDIUM | Binding authoring constraint; Stage 8 grades against `acceptance-assertion-type.md` | CHEAP |

## Delivery Strategy

One release branch `release/v3.67-qa-devtest-modes-and-automated-eval-execution` (D-C SINGLE), plan file as Engineering Commit 0, four serialized issue-slices in sequence order, ONE release PR (created draft at Stage 6, ready-for-review at Stage 9), single merge gate at Stage 12. Every SKILL.md / references edit routes through pmo-skill-editor discipline (impact + coherence + regression; `Skill-Editor-Audit-Trail:` commit trailer per Check 10). The `.skill` package rebuilds ONCE at release-cut via `build-skill-packages.sh pmo-qa-auditor` — not per-slice.

## Verification Plan

### Per-Issue Verification

| Issue | Method class | Expected result |
|---|---|---|
| #222 | file-content assertion (grep SKILL.md mode row + input contract + ladder rungs + stage-07 §6 cross-ref + `gh pr comment` write surface + 5-bucket enum) + behavioral **declared**: this release's Stage 7 DT runs Mode G against this very PR (self-hosting pilot) | Mode G section present; DT spoke exercises the ladder on this release |
| #219 | file-content assertion (mode row, Stage-8 enum verbatim, 3-lane routing + Override Record cross-refs) + behavioral **declared**: Stage 8 acceptance run against this release's AC | Per-criterion verdicts under the extended enum; no rubric re-definition |
| #2226 | characterization-fixture assertion (classifier matches ground-truth on ≥4/5 fixtures); pmo-skill-editor regression PASS; script self-test against deployed hook layout | Fixture + regression evidence recorded in Verification Evidence |
| #172 | file-content assertion (≥2 named stage triggers + criteria; result→gate schema + worked example; autonomy-tier cross-ref) + pilot conformance (≥1 existing eval invocation migrated + run before Stage 13) | Pilot run recorded; FAIL/EXCEPTION path cites release-process.md § Inter-Stage Feedback Protocol |

### Cross-Issue Acceptance Criteria (graded at Stage 9 QC3.5 on the merged PR)

- [ ] **CIAC-1 (#219 × #222 × #2226 on the SKILL.md mode-selection table):** the table carries exactly one row per new mode with mutually-distinct trigger phrases, and each new mode carries ≥3 mode-scoped entries in the canonical `## Domain-Specific Failure Modes` section (wording per the CR PR-3 restatement). Mode sections in E/F/G/H order. *Method:* grep the Mode Selection table rows + failure-mode blocks.
- [ ] **CIAC-2 (#172 × #222 × #219 on stage-trigger spec):** every stage trigger #172 registers names a mode identifier that exists in SKILL.md at merge — no dangling mode references in `stage-07-dev-testing.md` / `stage-08-qa-testing.md`. *Method:* grep cross-reference between shard trigger spec and the SKILL.md mode table.
- [ ] **CIAC-3 (#2226 × #222 on the producer-vs-consumer asymmetry note):** the asymmetry callout no longer states Mode E is "the lone producer mode"; it enumerates all producer/write-surface modes post-merge in E, F, G order. *Method:* `grep -c "lone producer" core/skills/pmo-qa-auditor/SKILL.md` → 0, updated enumeration present.

### Release-Level Verification

- `deploy.sh --check` Check 7 (package freshness) green after the release-cut rebuild.
- Doc-link integrity (Check 14 primitive) on all modified `.md` files.
- Parser-clean PR body (close-family verbs confined to the Issue References block).
- CIAC methods run solely by the verification-execution executor (`verify-release-plan.sh`) per stage-07 § Plan-verification re-execution; Stage-9 reads emitted verdicts read-only.

## Rollback Strategy

### Per-Issue Rollback

Mode-adds reverse by removing the mode section + table row + AUQ option + output variation + failure-mode entries + reference doc(s); #172 wiring reverses by removing trigger registrations from the stage shards. No schema migration anywhere in the bundle.

### Whole-Release Rollback

Pre-merge = branch abandonment (CHEAP). Post-merge = single-PR revert, unambiguous linear history (MODERATE). Rollup: **MODERATE / Confidence HIGH**.

## Operational Deployment Manifest

| Step | Target | When |
|---|---|---|
| `bash core/deploy/tools/build-skill-packages.sh pmo-qa-auditor` | `packages/pmo-qa-auditor.skill` + `.sha256` | Once at release-cut (Stage 12 window) |
| `./deploy.sh --deploy pmo-qa-auditor` | Cowork install path + user-local mirror (SKILL.md + references/ + evals/ + scripts/) | Stage 12 |
| `./deploy.sh --check` | Checks 1/2/7/13 green | Stage 12 verification |

### Schema Migrations

None.

## Deviation Log

Seeded at Engineering Commit 0. CR scope-lock amendments (2026-07-04) are BINDING and override the Stage-5 designs where they conflict.

| # | Source | Deviation / Amendment | Severity | Disposition |
|---|--------|----------------------|----------|-------------|
| 1 | D-Version re-determination (2026-07-09) | Provisional v3.66 collided with the merged `v3.66-software-domain-templates` release; version re-computed to **v3.67**; plan file renamed accordingly; branch cut from 671e0bf (not the stale 99d5a9f floor) | minor (version-only; zero file-surface overlap with the colliding release) | Applied at Commit 0; Stage-12 atomic tag-claim binds final |
| 2 | Divergence disposition (2026-07-09) | v3.65.1 shipped the author-association trust boundary (ADR-076) editing stage-07/stage-08/release-process within this release's matrix — **inherited by reference**; Engineering re-anchors to current shard text; warn-only Tier-1 comment-ingest bypass check at each mode's build | minor (informational) | Seq-1 check executed — see Verification Evidence |
| 3 | CR scope-lock (2026-07-04) | Scope additions folded into the File Change Matrix: `stage-to-skill-mode-mapping.md` rows 7 (#222) + 8 (#219); `process-fitness-cadence.md` §6 one-liner (#2226); D-5 reference-file 1→2 split (#2226); Change-5 staleness remediation inside #222's SKILL.md edit | minor (binding) | Matrix above reflects the locked scope |
| 4 | Stage-6 Seq-1 spoke (this commit) | **`## Change Description` deferred to the final Engineering spoke (Seq 4)** — authored at PR-assembly completion before Stage 9 ready-for-review, not at Commit 0 (v3.66 precedent) | minor | Seq-4 Engineering appends the section per RELEASE_PROTOCOL.md § Change Description Protocol |
| 5 | Stage-5 #3226 F-2 finding | SKILL.md finding-template `**Gate**: [G1–G6]` predates gates G7–G11 — R4 (one-line widen) was OPTIONAL and NOT ratified in the CR scope-lock approved-additions list | minor | NOT executed at Seq 1 (scope discipline); route to next release or operator call at Stage 9 |
| 6 | Stage-6 Seq-4 spoke | **R-2 tier-vocabulary fix executed as a one-line SKILL.md edit** — Mode H's tier line relabeled `**Automation Tier 2 (Recommend)**` (was `**Autonomy: Tier 2 (Recommend)**`, the named Autonomy/Automation inversion collision). A bounded, sub-task-sanctioned exception to D-6's zero-edit-points recorded determination; design R-2 + INT-4 own the fix | minor | Applied at Seq 4 (kept to that one line); INT-4 grades coexistence at Stage 8; old form 0 hits repo-wide post-edit |
| 7 | Stage-5 #3238 F-2 finding | stage-07:501 "DT scope enforcement" Automation-trajectory row's target state is satisfied by Mode G's QA-return full-re-review input contract, but the 1-cell Current-State flip was NOT in the CR approved-additions list | minor | NOT executed at Seq 4 (scope discipline); row noted for next release or operator call at Stage 9 |
| 8 | Stage-7 DT F-03 finding | File Change Matrix omitted `core/hooks/reference-durability-allowlist.txt` (added by the hub-direct Seq-3 CI fix `c6490a3`) — the Deviation Log narrated it but the matrix + path list did not carry the row | minor (bookkeeping) | Self-corrected: matrix + machine-readable path list now carry the row (this commit) |
| 9 | Stage-7 DT F-01 Blocker | Release branch cut from `671e0bf` before v3.66 Stage-13 close-out merged to `main`; 4 base commits (`210269a`/`cff6976`/`f3976ef`/`5b38f98`) unreachable — a branch-freshness FAIL (S7-I04). Zero path overlap; PR MERGEABLE/CLEAN; CI green | Blocker (Tier-1 [ADJUST]; environment/sequencing, not a content defect) | Resolved hub-direct: `git merge origin/main` (merge commit `c73445b`, zero-conflict — 18 v3.66 close-out files); 4 base commits now reachable; DT Pass-2 freshness re-check PASS |

## Verification Evidence

(Populated per-slice during Stage 6 self-verification and after Stage 12 execution.)

### Seq-1 (#222) self-verification — 2026-07-09

- Commit-0 version re-verify: **PASSED** (v3.67 free — see header note).
- Warn-only trust-boundary check (Deviation row 2): **PASS — no bypass.** Mode G defines no comment-ingest path of its own; iteration-context and QA-return ingest routes cite the stage shard's DT↔Engineering Iteration Loop / DT↔QA Handoff protocols, whose intro now carries the author-association gate (stage-07-dev-testing.md § DT↔Engineering Iteration Loop Protocol). No Mode G text reads external comments ahead of that gate.
- Remaining Seq-1 evidence: posted on sub-task #3227 (Stage 6 Engineering output comment).

### Seq-2 (#219) self-verification — 2026-07-09

- Warn-only trust-boundary check (Deviation row 2): **PASS — no bypass.** Mode H defines no comment-ingest path of its own; the SKILL.md Mode H input block and the mode-spec consumption map route iteration context and any PR-review / issue-thread comment through stage-08 §5 Phase D, whose author-association trust boundary gates external comment ingest before tier classification (release-process.md § Inter-Stage Feedback Protocol).
- FM-3 count-token sweep (CR-assigned to #219): stage-08 §9 gap census `8 gaps` → `6 gaps` with both closed Key clauses (acceptance-review mode; acceptance assertion framework, v3.64-stale) dropped in the same line edit; sentence structure preserved per the CR-recorded target line.
- #218 contract consumption (R8 negative assertion): formula-shaped definition grep (`acceptance_score(issue)` / `= count(MET)`) → **2 matches** at `SKILL.md:1318–1319`, both the PROC failure-mode's Mitigation **quoting the forbidden pattern to forbid it** (self-quotation in an anti-pattern definition), **not** a formula re-definition (DT F-02 / QA-219-2 corrected the Seq-2 "0 matches" self-verification claim, which was factually stale; substance holds). The six-value enum appears only as consumed from stage-08 §5 / the §3 projection table; the acceptance-score formula is consumed by-name from the #218 contract, never re-derived; matrix columns cited from contract §5, none added. (Optional CHEAP follow-up: reword the self-defeating Verifiable-boundary sentence in a future edit.)
- R-6 scoping sentence (CR-ratified wording, covering E/F/G/H) landed in Operating principles + the parallel rider on the `PARTIAL verdict emitted — PROC` entry's Conditional. Mode F clause present for Seq-3 verification.
- Remaining Seq-2 evidence: posted on sub-task #3231 (Stage 6 Engineering output comment).

### Seq-3 (#2226) self-verification — 2026-07-09 (hub-direct completion)

- Completed hub-direct after a session-boundary spoke exit (Retry self-repair; equal-rigor clause). Full verification battery + evidence: posted on sub-task #3235 (Stage 6 Engineering output comment). Commits `2ac523f` (Mode F slice: SKILL.md section + 2 reference docs per the D-5 split + fixtures + search primitives + allowlist registration + cadence §6 dispatched-writer) and `c6490a3` (reference-durability allowlist for the mode-spec's `#[0-9]+` validation-regex literal). PR #3314 CI 26/26 green after the Seq-3 push.
- Forward-flags carried on #3235: concurrent v3.67 claimant branch (resolved at the Stage-12 atomic claim; AI-2 re-check at Stage 9); `block-skill-direct-edit` warn-log observation candidate; F-1/R-8 precedent-script allowlist observation deferred to Stage-13 synthesis per CR.

### Seq-4 (#172) self-verification — 2026-07-10

- Warn-only trust-boundary check (Deviation row 2): **PASS — no bypass.** #172 adds no comment-ingest path: EI rows fire on stage events (spoke entry, re-entry, Phase-B entry validation), never on comment arrival; escalation routes through the Inter-Stage Feedback Protocol, whose author-association gate precedes tier-mapping on every comment-shaped channel.
- AC1 (≥2 named stages, stated criteria): `EI-S7-01`/`EI-S7-02` registered in stage-07 § Automated Eval Invocation Protocol; `EI-S8-01` in stage-08 §5 Phase B — each row carries a "fires when ALL hold" criterion column; executors named by their merged mode identifiers (Mode G — Dev Testing / Mode H — Acceptance Review).
- AC2 (escalation wiring): the FAIL/EXCEPTION escalation table cites release-process.md § Inter-Stage Feedback Protocol Tier 1/2/3 (`[ADJUST]` / `[SCOPE CHANGE]` / `[PLAN REJECTION]`); the engagement charter is cited for communication FORM only, never routing (DR-2 discharge).
- AC3 (result→gate schema as a contract): § Eval-invocation record (7-column schema; Result enum `PASS / FAIL / SKIP / EXCEPTION` derived from the A8 Test-results vocabulary) + § Result → gate projection + one worked example (FAIL → Tier 1 end-to-end, with the EXCEPTION variant).
- AC4 (autonomy-tier registration): one Tier-2 Bounded-Auto example bullet in `core/specs/autonomy-tiers.md` § Tier 2 Examples with declared scope; the protocol's § Autonomy declaration carries scope + FAIL/EXCEPTION descent to Autonomy Tier 1 under prefixed terminology; stage-level Automation Tier labels untouched.
- AC5 (pilot conformance) — **EXECUTED + MIRRORED** (evidence-producing, NOT gate-binding per the introducing-release exemption). The `stage-07-dev-testing-gate` invocation was migrated from conversation-based to registered `EI-S7-01` (stage-07:286 trajectory row flipped; § Cutover + pilot :648–660). The pilot record rows (record home = the sub-task report bodies per stage-07:555; mirrored here per § Cutover + pilot):

  | Gate | EI row | Result | Assertion tally | Executor | Timestamp (UTC) |
  |---|---|---|---|---|---|
  | `stage-07-dev-testing-gate` | EI-S7-01 | **FAIL** | branch-freshness assertion detected 4 unreachable base commits (F-01) | Mode G lens / DT release-scoped spoke | 2026-07-10T17:56Z |
  | `stage-08-qa-testing-gate` | EI-S8-01 | PASS | structural 1/0 · acceptance 3/0 · judgment 1/0 | Mode H lens / QA release-scoped spoke | 2026-07-10T18:20Z |

  The EI-S7-01 **FAIL was the strongest possible pilot outcome** — it exercised the ENTIRE projection chain live: assertion FAIL → Blocker F-01 → Phase D FAIL → Tier 1 [ADJUST] routing → fix (merge `c73445b`) → EI-S7-02 re-fire → Pass-2 freshness re-check PASS. Full records: DT #3240 / #3228 Pass-2 (EI-S7-01) + QA #3241 (EI-S8-01). QA-172-1 discharged.
- INT-1..INT-4: mode identifiers exact; zero novel verdict tokens in changed blocks; composed stage-07 §9 line = `4 gaps. Key: fresh-context separation not enforceable (P3).`; the EI-S8 block defines no formula, verdict value, or matrix column; every Tier-2 mention in the changed blocks carries an Autonomy/Automation prefix or the feedback-protocol signal tag.
- FM-6 seam verified CONSISTENT (no flag): Mode G rung 1 ("execute the Stage 7 stage-gate eval set as written — its judgment-typed assertions grade within this rung's gate input"), dev-testing-mode-spec §1/§3 ("as written — the full set"), and EI-S7-01 ("executed as written") agree.
- CD-4 rider applied: EI-S8-01's registered eval surface = the Stage-8 stage-gate eval set (`stage-08-qa-testing-gate`); Phase-B grading cited as Mode H's §5 constitutive process that the set's `acceptance`-type assertions exercise — the row registers automated invocation + record, not new execution semantics.
- R-2 rider applied (Deviation row 6): Mode H tier line relabeled `**Automation Tier 2 (Recommend)**`; old form 0 hits repo-wide post-edit; no other SKILL.md line touched.
- Cascade sweep re-run: `no eval runner` / `Conversation-based, operator triggers DT` / `Eval runner auto-executes` → 0 outside plans/archive; Tier-2 example roster single-homed; stage-07:501 row REMAINS by design (Deviation row 7).
- Doc-link integrity (Check 14 primitive, `--require-targets`): 0 broken links across the 4 modified markdown files.
- Runtime suite: selection-map row 6 no-match (doc/governance-only change set) → `test-run/suite-skip` event emitted (stage=6, actor=spoke:#172).
- Layer boundary: zero Layer-2 (`projects/`) files in the diff; commit message parser-clean and depersonalization-clean.

## Deployment Execution Log

(Populated during Stage 12.)

| Step | Timestamp | Result | Notes |
|---|---|---|---|

## Change Description

### Outcome

pmo-qa-auditor now carries the full Stage-7/Stage-8 executor surface: Mode G (Dev Testing) runs the structural→contract→content-quality→integration eval-assertion ladder over a PR + release plan and posts the quality report as a PR comment; Mode H (Acceptance Review) grades GitHub-Issue AC per criterion under the Stage-8 enum by consuming the acceptance-assertion contract; Mode F (Release-Process Fitness Audit) emits a dated audit artifact classifying pipeline-change coverage UNTRACKED / PARTIAL / ALREADY-TRACKED. On top of those executors, #172 registers automated eval invocation at both stages — the new stage-07 § Automated Eval Invocation Protocol defines the EI trigger rows (`EI-S7-01`/`EI-S7-02`; `EI-S8-01` in the stage-08 shard), a result→gate projection contract with the `PASS / FAIL / SKIP / EXCEPTION` record schema, FAIL/EXCEPTION escalation wired to the Inter-Stage Feedback Protocol tiers, and an Autonomy Tier 2 — Bounded Auto declaration with Tier-1 descent.

Register-don't-build: no runner skill, no new verdict values, no parallel gate — eval outcomes reach the gate evaluator through the shard §7 Metrics lines gate-evaluation-spec.md already parses (its declared modification-free extension path). Eval authoring stays with eval-writer; execution stays with Modes G/H; #172 shipped only the invocation registration + contract.

### Issues resolved

| # | Outcome (one line) | Status |
|---|---|---|
| #222 | Mode G — Dev Testing (Stage-7 skill-invocable executor) + release-wide D-4 count-free conventions + CD-2 scoped-pointer report header + Change-5 staleness remediation | DONE |
| #219 | Mode H — Acceptance Review (Stage-8 executor) consuming the acceptance-assertion contract; FM-3 gap-census count fix | DONE |
| #2226 | Mode F — Release-Process Fitness Audit (producer mode): dimension rubric + mode spec (D-5 split), characterization fixtures, search primitives, allowlist registration, cadence §6 dispatched-writer | DONE |
| #172 | Automated Eval Invocation Protocol (stage-07 extended-protocol H2) + EI-S8 registration block (stage-08 §5 Phase B) + result→gate projection contract + autonomy-tiers Tier-2 example + the R-2 tier-vocabulary fix | DONE |

### Key decisions

- **D-C SINGLE + D-Concurrency P0:** one release branch, one PR, four fully-serial slices — the 4-way (3/4 after D-6) SKILL.md contention made parallel execution valueless. See § Operator Decisions.
- **D-ModeLetters:** F = #2226 (locked), G = #222, H = #219 — ratified before Engineering so four specs cite one letter map.
- **D-6 (scope-lock recorded determination):** #172 ships ZERO planned SKILL.md edit points — registration is stage-side; the modes already declare invocability. One sanctioned exception executed: the CR R-2 one-line tier-vocabulary fix on Mode H's line (Deviation row 6).
- **CD-4 (A6.5 rider):** EI-S8-01's registered surface narrowed to the Stage-8 stage-gate eval set; Phase-B grading cited as Mode H's constitutive process — the registry claims invocation + record, not pre-existing execution semantics.
- **CR scope-lock riders — all discharged in-sequence:** PR-1 re-anchor (Seq 2) · PR-2 + FM-6 (Seq 1) · FM-3 (Seq 2) · FM-5 terminal-sentence fix (Seq 2) · FM-7 + FM-1/Change-6 (Seq 3) · FM-4 dispatched-writer (Seq 3) · CD-3 per-shard persona treatment (Seq 1/2) · R-2 + CD-4 (Seq 4).
- **D-Version:** provisional v3.66 collided mid-pipeline → re-determined **v3.67** (Commit-0 re-verify PASSED 2026-07-09); the Stage-12 atomic tag-claim binds final.

### Reversibility

**MODERATE / HIGH confidence.** Pre-merge: branch abandonment (CHEAP). Post-merge: a single-PR revert removes the three mode sections + reference/eval/script files, the invocation protocol H2 + EI-S8 block + Tier-2 bullet, and the shard line edits — no schema migration anywhere; the posture is MODERATE because from the NEXT release the stage gates read the §7 eval-invocation clause (cutover), and the `.skill`/deploy sync must re-run after any revert.

### Downstream impact

- **The next release entering Stage 7/8 runs under the EI rows** — the §7 Metrics clause + trigger registration bind post-cutover; this release itself is exempt except the AC5 pilot (`EI-S7-01`, evidence-producing only, recorded at its own Stage-7 DT pass).
- **Release-cut/Stage-12 obligations:** `.skill` rebuild ONCE via `build-skill-packages.sh pmo-qa-auditor` (R6 mitigation) + `deploy.sh --deploy` post-merge.
- **Carry-forwards (tracked, not shipped):** stage-07:501 DT-scope-enforcement row flip (Deviation row 7); SKILL.md `[G1–G6]` finding-template widen (Deviation row 5); pmo-qa-lead G+H seam follow-up (CR R-4, Stage-13 synthesis); precedent-script allowlist observation (F-1/R-8, Stage-13 synthesis); `eval-run` event subtype + plan-verification-executor EI registration (design R-6/R-5 observations).
- **Concurrent v3.67 claimant** (`release/v3.67-methodology-pack-catalog` branch on origin) — resolved at the Stage-12 atomic claim; Stage-9 divergence re-check registered (AI-2).

### Cross-references

- Release plan: this file, top section (Header + Scope)
- Milestone: 79-qa-devtest-modes-and-automated-eval-execution — https://github.com/cody-hutson/pmo-platform/milestone/161
- User-facing release notes: `release/releases/notes/v3.67_RELEASE_NOTES.md` (authored at Stage 13 Close per `release/references/standards/release-notes-standard.md`)
