<!-- repo-integrity: allow-memory-ref — this plan deliberately names the operator-local memory pointer that #1067 AC-4 retires at Stage-13 close (the pointer's rule is codified into the corpus by this release); naming it keeps the close-out action precise. -->
# v3.67 Release Plan — 87-methodology-pack-catalog

> Version **v3.67** (re-versioned from the Stage-4 provisional v3.66; **Commit-0 re-verify run 2026-07-09**: the v3.66 slot was claimed by the sibling `software-domain-templates` release during a pipeline pause — tag `v3.66` + `plans/v3.66_RELEASE_PLAN.md` on main; recomputed next-free across tags / plan files / remote release branches = **v3.67** = the operator-recorded value → PROCEED, the plan-R1 contingency materialized exactly as designed). Claimed on the branch axis at Commit 0 (`release/v3.67-methodology-pack-catalog`); bound atomically at Stage-12 merge. This file consolidates the approved Stage-4 plan (#3259 first comment) with the Stage-5 scope-lock matrix refinements (v2) carried into Stage 6; version references updated v3.66→v3.67 surgically (contention rows carry their resolution annotations rather than a mechanical substitution, preserving the historical record of the predicted collision).

## Header

| Field | Value |
|-------|-------|
| **Version** | v3.67 (Commit-0 re-verify PASSED 2026-07-09) |
| **Date Created** | 2026-07-09 (Thursday) |
| **Release Manager** | Agent-assisted (release-hub Mode O; operator gates per Stage 9 / Stage 12) |
| **Status** | Executing (Stage 6 — single Engineering spoke, P0 fully-serial) |
| **Branch** | release/v3.67-methodology-pack-catalog |
| **PR** | (populated at Stage 6 draft-PR creation) |
| **Milestone** | 87-methodology-pack-catalog |

## Stage 4 Release Planning — 87-methodology-pack-catalog

**Phase A0 re-review:** CONSUMED the hub's Mode R premise cache (run 2026-07-03, this milestone) per hub injection — PT-1..4 not re-run. Delta re-verification (blast-radius reads only, all [SOURCE], this spoke, 2026-07-03): (1) `elicitation-loop.md` has 0 `delivery_approach` reads (#1085 premise current); (2) `intake-style-guide.md` carries 5-test routing + the evidence-gated template-addition policy (~L192) but no type→template fork (#1067 premise current); (3) `git grep operational_methodology|release_methodology` over `core/` → 0 hits (#1788 premise current); (4) `work-item-type-schema.md:162` — Arm-3 `control-field` discriminator slot RESERVED, body unspecified (#1803 premise current); (5) `.github/ISSUE_TEMPLATE/` = {adr, bug, improvement, observation}.yml only (#1973 premise current). All five in-scope bodies pass **G-PL2** crisping (Description/Proposed Change/AC with named files — readiness-fix additions per the milestone Amendment Log). **G-PL1** AC-currency: PASS — all AC-cited paths/sections exist on the worktree at origin/main parity; project-schema V-series live tail = **V15** [SOURCE: `core/schemas/project-schema.md` validation traces V1–V15]. **G-PL3** placement forward-check: SKIP (release branch not yet cut; base ≈ current `origin/main` `99d5a9f` → empty mover window; non-ceremony skip per decision-discipline G2). **Parallelization-Map currency:** PASS — map recorded 2026-06-27, amended 2026-07-03 (re-bundle date = Stage-4-entry date; fresh). Trimmed members #1777/#1971 confirmed NOT in scope — not re-added.

### Summary (30 seconds)

Five issues, 18 raw / 23 effective pts (cross-cutting × 1.3), in the 15–25 band — no over-scope flag. Dependency analysis **VALIDATES** the milestone's declared sequence #1085 → #1067 → (#1788 ∥ #1803) → #1973 as a correct linearization (only the two edges into #1973 are hard). Stage 5 **ACTIVATES release-wide** (all-or-nothing; T1/T2/T3/T4 fire across 4 of 5 issues; class bias ALL). Intra-release contention: #1085 and #1973 share `intake-desk/SKILL.md` + `references/type-map.md` — resolved by serial P0 sequencing on one branch. Cross-release: **zero file overlap** with sibling `software-domain-templates` (disjoint trees), but both contended for the **v3.66 version slot** (Tier-S edge; the sibling claimed v3.66 first and this release re-versioned to v3.67 at the Commit-0 re-verify — expected behavior, not an error). Plan-changing delta vs hub context: **v3.65 close-out has already landed on main** (`99d5a9f`) and milestone 70 is CLOSED — that contention row is RESOLVED. Release Class **cross-cutting CONFIRMED**. Quota: **WARN** (shared 5-hour window with an active sibling hub). 3 CIACs authored. No merge/split recommended.

### Dependency Graph

**In-bundle hard edges (declared DEPENDS_ON):**
- #1085 → #1973 (templates consume the config-read: type selection drives template + body lens)
- #1067 → #1973 (templates consume the type→template routing rule)

**In-bundle soft/coordination edges (compositional, no DEPENDS_ON):**
- #1788 ⇢ #1973 — per-space methodology fields refine which pack lens intake renders per space [SOURCE: milestone Outcome Statement — "operator can select a methodology per space and intake renders the matching work-item vocabulary"]
- #1788 ⇢ #1085 — resolution-order seam: intake-desk's resolver read must be defined against the per-space fields shipping in the same release (see CIAC-3)
- #1085 ∥ #1067 — no edge in either direction (different modules, disjoint files); their relative order is a linearization choice

**External dependencies — all satisfied or non-blocking [SOURCE: issue bodies + gh state]:**
| Dependency | State | Bearing |
|---|---|---|
| #1967 (pack lenses, → #1973) | Shipped v3.60 (state: closed) | Satisfied |
| #409 (declarative type system) | Closed | #1085 is the interim config-read; coordinate wording, no block |
| #351 + #262 (project-schema V-rule keystones) | Both closed | V-series tail unblocked for #1788 |
| #497/#501 (architecture/DoR gate) | Closed (gate shipped milestone 68) | Consumer exists for #1803's coupling clause |
| #384 (tracker carriage) | Open | Coordination only — #1803 must not block on it |
| #160 (PDA epic, BELONGS_TO parent of #1803) | [ASSUMPTION – CONFIRM] owner: triage | Non-blocking for planning |
| #1151 / #1964 (epics) | Builds-on / parent links | Non-blocking |

### Implementation Sequence

**Declared sequence VALIDATED:** `#1085 → #1067 → (#1788 ∥ #1803) → #1973`.

Evidence note: only {#1085, #1067} → #1973 are hard edges. #1085-before-#1067 and the schema pair's mid-position are linearization choices, and good ones — (a) the config-read (#1085) and routing rule (#1067) precede the templates that consume them; (b) #1788's fields land before #1973 renders per-space lenses (soft edge honored); (c) under P0 serial execution the ∥ pair occupies adjacent slots as `#1788 → #1803` (disjoint files, order between them immaterial — keep declared order for determinism). No correction proposed.

**Delivery strategy:** SINGLE topology (D-C default). One release branch `release/v3.67-methodology-pack-catalog` (v3.67 confirmed at the Commit-0 re-verify — see D-Version), plan file committed as Engineering Commit 0 after the Commit-0 version re-verify, five serial Stage-6 slices in the order above, **one PR, one merge** (milestone-one-PR discipline). Stage 6 dispatch: P0 fully-serial (see D-Concurrency Posture).

**Rollback strategy:** single-PR revert (`git revert` of the merge commit) restores all five slices atomically — no data migration, no runtime state. The #1803 first-instance control set is K4 operator-local (not git-tracked) — zero repo rollback surface. Reversibility: **CHEAP** pre-tag, **MODERATE** post-tag (tag + ledger row + corpus appends to unwind per RELEASE_PROTOCOL rollback). Regression guard: #1788 AC-3 asserts existing single-value / SPM-Hybrid / Hybrid-Two PROJECT.md still validate.

### Stage Applicability Matrix

**Stage 5 release verdict: ACTIVATE (all-or-nothing per planning-solutioning-handoff §2).** Fired triggers by issue:

| Issue | Size | Stage 5 (triggers) | Stage 6 | Stage 7 Dev Test | Stage 8 QA | Verification method class (AC→method) |
|---|---|---|---|---|---|---|
| #1085 intake-desk resolves delivery_approach | M | YES — T2 (skill logic), T3 (mechanism deferred to Stage 5 — literal), T6 (precise edit set TBD); §3.1 + §3.2 riders fire | YES (slice 1) | APPLY — behavioral ACs (Scrum project yields Story/Task/Bug typing) | APPLY | behavioral/domain (declared method: archetype-typing check) + file-content grep (resolution step present) |
| #1067 type→template rule in intake-style-guide | S | YES — no issue-level trigger fires; activated by release rollup + class bias ALL | YES (slice 2) | REDUCE — doc-conformance grep pass (all ACs grep/read assertions, no executable surface) | APPLY | file-content grep (4 template names in one routing section; tool-blind clause; single-source check) |
| #1788 per-space methodology fields | M | YES — T3 (V-rule numbering, owner: solutioning — literal); §3.2 rider (extends V-series) | YES (slice 3) | REDUCE — doc-conformance + V1–V15 regression validation traces re-run against worked examples | APPLY | file-content grep (fields + V-rules) + regression predicate (existing PROJECT.md examples validate) |
| #1803 control-field layer in type-pack meta-schema | M | YES — T3 (mechanism deferred to Stage 5 — owner: architecture — literal), T4 (3 candidate mechanisms); §3.1 + §3.2 riders fire | YES (slice 4) | APPLY — pack-validation surface flips (reserved→specified); executor run where built, else declared-deferred | APPLY | file-content grep (cross-kind section; coupling clause naming the gate consumer; rationale note) |
| #1973 Epic/Story templates / type selector | M | YES — T1 (new template files), T2 (SKILL.md update), T4 (template-per-type vs selector — literal "decided at Solutioning") | YES (slice 5) | APPLY — YAML template lint + field/label inspection; the "create test issue from template" method executes only post-merge → **declared, verification deferred** (honesty note per stage-04 § Verification-Plan) | APPLY | behavioral (post-merge template instantiation — declared/deferred) + file-content grep (type-map binding) |

**Release-scoped stages (apply once, all issues):** Stage 9 Plan Review — **Deep** (cross-cutting posture; CIACs graded at QC3.5/A3.6). Stage 10 Dry-Run — satisfied by platform mechanism (PR diff IS the dry run, git-native compression). Stage 11 Snapshot — satisfied by platform mechanism (git history IS the snapshot). Stage 12 Execute — applies (merge + atomic version claim + tag + ledger row). Stage 13 Close — applies (corpus close-out; mark #1085, #1067, #1788, #1803, #1973 as closed at Stage 13 via the PR's Issue References block on merge).

### Contention Map

**Baseline pin:** `origin/main` @ `99d5a9f` (v3.65 Stage-13 corpus merge), audited 2026-07-03 by the Stage-4 spoke. **Commit-0 refresh (2026-07-09):** origin/main advanced to `7de12b8` via the sibling v3.66 merge + its Stage-12 chore PR; a matrix-file divergence check (`git diff 99d5a9f origin/main -- <all matrix paths>`) returned **empty** — zero in-scope files moved; all Stage-5 anchors hold. Stage 9 Phase A6.5 remains the mid-pipeline re-check for anything merging after this refresh.

**Intra-release (within-release A4 contention):**
| File | Issues | Resolution |
|---|---|---|
| `operations/skills/intake-desk/SKILL.md` | #1085 + #1973 | Serial same-branch sequencing (#1085 slice 1, #1973 slice 5) — no split needed |
| `operations/skills/intake-desk/references/type-map.md` | #1085 + #1973 | Same — #1085 lands the Layer-2 consumption; #1973 appends the template↔type binding |
| All other matrix files | single-issue each | No contention |

**Cross-release (sibling surfaces):**
| Sibling | Their surface | Ours | Verdict |
|---|---|---|---|
| `software-domain-templates` (milestone 268; front pipeline) | `operations/templates/<7 family>-template.md` (create) + `core/standards/template-taxonomy.md` (edit) [SOURCE: milestone 268 description + `git ls-files`] | No path in common with our matrix | **Disjoint files — parallel-safe on the file axis.** Confirmed at Commit-0: the merged v3.66 diff touches no path in our matrix |
| Same sibling — version slot | `Δversion/v3.66` virtual-path token | Same token | **Tier-S serialization edge — RESOLVED at Commit-0 (2026-07-09):** the sibling claimed v3.66 first (tag + plan file on main); this release re-versioned to v3.67 per the D-Version rungs (expected behavior, not an error) |
| Same sibling — Stage 12/13 corpus files | `RELEASE_LOG.md`, `INDEX`, `DIGEST`, `CHANGELOG.md`, `releases/notes/` | Same at our Stage 12/13 | `overlap_class = append-pattern` → informational (structurally HIGH, operationally LOW per ADR-005) |
| v3.65 close-out (milestone 70) | Stage-13 corpus writes | — | **RESOLVED — landed.** Corpus merge is `99d5a9f` on main; milestone 70 CLOSED, 0 open issues [SOURCE: gh milestone query, 2026-07-03]. No remaining write surface |

### File Change Matrix

`domain:` classification (A3): **governance** — the matrix is entirely internal platform artifacts (skill references, how-to protocol, disciplines, schemas, intake templates); secondary: none. Sourcing-exempt, domain-classified (label under D-ReleaseClass below).

**Scope-lock File Change Matrix v2 (operator-ratified; supersedes the Stage-4 v1 rows — Stage-5 conditional-row resolutions + single-source additions consolidated):**

| Path | Action | Issue(s) | Notes |
|---|---|---|---|
| `operations/skills/intake-desk/references/elicitation-loop.md` | edit | #1085 | Methodology-resolution step at Phase-2 entry + gate item (f) + place-and-relate + Phase-3 kind-field source + 5-test registry-conformance rider |
| `operations/skills/intake-desk/references/type-map.md` | edit | #1085, #1973 | Two-tier registry + kind-derivation contract + binding table (#1085); Emission-template cell rewrite + worked-example emission sentence (#1973) |
| `operations/skills/intake-desk/SKILL.md` | edit | #1085, #1973 | Operating principle + Mode-A touch + type-registry rewrite + methodology-mismatch failure mode + v2.28 (#1085); emission-vehicle sentence + v2.29 (#1973) |
| `release/references/how-to/intake-style-guide.md` | edit | #1067, #1973 | New §1b type→template routing table ahead of the 5-test + §5b scope-of-trigger + §6 row (#1067); §1b Epic/Story rows + residual rule + §5b enumeration update + §6 rows (#1973) |
| `core/disciplines/decision-discipline.md` | edit | #1067 | Determinate-classification exemption — §1.1 NOT-list clause + §3 Exempt row + decided-tool-blind guardrail paragraph (3 additive edits) |
| `.github/ISSUE_TEMPLATE/bug.yml` | edit | #1067 | Description → discriminator-only; §1b pointer comment (single-source, plan rider d) |
| `core/CLAUDE.md.template` | edit | #1067 | One-line §1b pointer appended to the Continuous Improvement template sentence (RECOMMENDED — operator KEPT) |
| `core/schemas/project-schema.md` | edit | #1788 | Optional `operational_methodology` + `release_methodology`; V16 + V17 appended to the live tail; §6.7 worked example; V1–V15 byte-unchanged |
| `release/references/specs/methodology-parameterization-v1.md` | edit | #1788 | §5 Step 0 space-scoped resolution + per-space consumer audit; CASE text byte-stable |
| `core/schemas/work-item-type-schema.md` | edit | #1803 | §1.1.2 `[[controls]]` facet; Arm-3 `control-field` body specified; `x-pmo-control` third annotation class; §6.2b; value→gate/filter coupling naming the shipped Structure-Review gate |
| `core/ADRs/ADR-077-cross-cutting-control-field-layer.md` | add | #1803 | Grammar-altitude decision record (spec's provisional ADR-076 re-allocated to ADR-077 — 076 claimed by the v3.65.1 sibling; next-free re-verified at Stage 6) |
| `.github/ISSUE_TEMPLATE/epic.yml` | add | #1973 | Template-per-type arm (Stage-5 D1) — pack-projected Epic kind form; stamps `type:epic` + `status: proposed` |
| `.github/ISSUE_TEMPLATE/story.yml` | add | #1973 | Pack-projected Story kind form; stamps `type:story` + `status: proposed` |
| `core/specs/label-taxonomy.md` | edit | #1973 | Rule-1 pattern-level stamping sentence for pack-projected kind forms (RECOMMENDED — operator KEPT) |
| `docs/FIRST_STEPS.md` | edit | #1973 | Count-free template phrasing + epic/story table rows (RECOMMENDED — operator KEPT) |

**Conditional-row resolutions (Stage-5 outcomes):** `core/schemas/gate-criteria-spec.md` — **NOT edited** (pruned; the shipped Structure-Review gate rows live in the Stage-5 pipeline shard, not there — #1803 D-1803-4). `.github/ISSUE_TEMPLATE/improvement.yml` — **NOT edited** (the selector arm was rejected; template-per-type shipped — #1973 D1). `core/disciplines/work-organization-mapping-framework.md` — **read-only, no edit** (consumer declaration pre-exists — #1085 Phase A1.5).

Pipeline-standard release artifacts (not issue scope): `release/releases/plans/v3.67_RELEASE_PLAN.md` (Commit 0 + Change Description at PR assembly), RELEASE_LOG row + notes + INDEX/DIGEST/CHANGELOG at Stages 12/13.

Machine-readable path list (Stage 7/8/9 chip extraction — scope-lock v2):

```
operations/skills/intake-desk/references/elicitation-loop.md
operations/skills/intake-desk/references/type-map.md
operations/skills/intake-desk/SKILL.md
release/references/how-to/intake-style-guide.md
core/disciplines/decision-discipline.md
.github/ISSUE_TEMPLATE/bug.yml
core/CLAUDE.md.template
core/schemas/project-schema.md
release/references/specs/methodology-parameterization-v1.md
core/schemas/work-item-type-schema.md
core/ADRs/ADR-077-cross-cutting-control-field-layer.md
.github/ISSUE_TEMPLATE/epic.yml
.github/ISSUE_TEMPLATE/story.yml
core/specs/label-taxonomy.md
docs/FIRST_STEPS.md
```

### Risk Register

| ID | Risk | Class | Likelihood / Severity | Mitigation | Reversibility |
|---|---|---|---|---|---|
| R1 | v3.66 slot claimed first by `software-domain-templates` (its Stage 4 completed 2026-07-04T03:06Z — ahead of us) | Version contention (Tier-S) | HIGH / LOW | Commit-0 re-verify (detect-and-HALT) + Stage-12 atomic claim; re-version to next-free (v3.67) — expected behavior. **MATERIALIZED as designed 2026-07-09:** sibling shipped v3.66; this release re-versioned to v3.67 (operator-recorded) | CHEAP |
| R2 | Sibling Stage-12/13 corpus appends land mid-pipeline (RELEASE_LOG/INDEX/DIGEST/CHANGELOG) | Cross-PR contention | MEDIUM / LOW | `overlap_class = append-pattern` (informational per ADR-005); Stage 9 A6.5 re-check | CHEAP |
| R3 | Intra-release collision on `intake-desk/SKILL.md` + `type-map.md` (#1085 × #1973) | Within-release contention | Eliminated by design / — | Serial P0, single branch, #1085 before #1973 | CHEAP |
| R4 | #1803 over-modeling: duplicating per-kind `criteria.readiness`, or re-opening the criterion grammar, or growing a second architecture gate | Design/scope | MEDIUM / MEDIUM | Stage 5 closed it: specify the RESERVED Arm-3 slot only, feed the existing gate (shipped, milestone 68), record the one-line rationale (AC-4); no gate-criteria-spec edit | MODERATE (schema concept, once consumed) |
| R5 | #1788 V-rule numbering collides with a concurrent V-series edit | Schema-coordination | LOW / LOW | No sibling touches `project-schema.md` [SOURCE: contention map]; tail re-confirmed at Stage 5 (V15) and re-grepped at Stage 6 build time; number V16+ | CHEAP |
| R6 | #1973 arm decision contradicts intake-style-guide's evidence-gated template-addition policy (~L192: no speculative per-type splits) | Design-coherence | MEDIUM / MEDIUM | Stage-5 reconciliation shipped: §5b scope-of-trigger note (#1067) + §1b registration + §5b enumeration update (#1973); CIAC-2 grades on the merged PR | CHEAP |
| R7 | #1085 hardcodes an archetype into the public skill body (methodology-neutrality breach) | Guardrail | LOW / HIGH | AC-4 explicit; methodology-framed-views discipline cited in issue; Stage 8 QA checks config-read-not-literal | CHEAP |
| R8 | Per-space fields (#1788) ship dead-on-arrival — intake-desk resolver (#1085) never reads them | Integration cohesion | MEDIUM / MEDIUM | CIAC-3 (below) makes the resolution order a graded release predicate | CHEAP |
| R9 | Shared 5-hour usage window with the sibling hub exhausts mid-wave | Capacity | MEDIUM / MEDIUM | Quota Budget WARN routing below; Checkpoint B per-wave (load-bearing); split-batch option pre-declared | CHEAP |

### Quota Budget

**Verdict:** WARN (per `quota-budget-protocol.md` Checkpoint A)
**Parallel-eligible spokes per parallel stage (from A2 Stage Applicability Matrix):** Stage 5: 5 · Stage 7: 5 (2 of them REDUCEd doc-conformance passes — cheaper) · Stage 8: 5
**Per-spoke cost estimate:** size-bucket ordinal band (source: heuristic — no telemetry medians yet): worst batch = 4 × `size:M` (low–moderate) + 1 × `size:S` (lowest)
**Assumed/stated remaining usage-window envelope:** conservative default — operator quota state unstated at spoke time, AND the envelope is shared with the active `software-domain-templates` hub drawing from the same per-account window
**Estimated cumulative draw % (worst parallel batch):** ~50–65% of a conservative shared envelope (5-spoke Stage-5 wave ≈ 4M+1S, plus concurrent sibling-hub draw) — lands in the WARN band; the same wave on a fresh, uncontended window would read PASS
**Routing:** WARN — window-aware launch timing + quota-budgeting: recommend splitting the Stage-5 wave 3+2 (slices 1–3, then 4–5) unless the operator states a fresh window at wave time; do not route this to STAGGER (rate-limit-only, changes nothing cumulative)
**Note:** Checkpoint B re-validates at every parallel wave (runtime, load-bearing) with PROCEED/SERIALIZE/DEFER/REDUCE-scope; STAGGER is a secondary rate-limit-only defense, not a usage-window mitigation. Bands + cumulative-draw budget are `[CALIBRATE-AFTER-3]` MEDIUM. Cumulative-draw framing: the number that binds is total tokens within the sliding window across BOTH hubs, not batch size. **Stage-6 note (2026-07-09):** Stage 6 executed as ONE serial spoke (all five slices), not a parallel wave — Checkpoint B parallel-wave routing did not fire for Engineering.

### Cross-Issue Acceptance Criteria

**Cross-Issue Acceptance Criteria**
- [ ] **CIAC-1 (#1085 × #1973 on `operations/skills/intake-desk/references/type-map.md`):** The archetype kind set intake-desk types items with (#1085) and the `type:*` labels the new intake path stamps (#1973) agree — one binding table covers both, and every template-stamped label is a member of the documented `type:*` namespace. *Method:* `grep -n "type:" operations/skills/intake-desk/references/type-map.md` — the template↔type binding and the archetype kind set resolve to the same label vocabulary; cross-check each new/changed template's `labels:` field against it. *Graded at Stage 9 QC3.5 on the merged PR.*
- [ ] **CIAC-2 (#1067 × #1973 on `release/references/how-to/intake-style-guide.md` routing section):** Every template the type→template routing rule names exists on the merged tree, the shipped Epic/Story intake arm appears in the routing rule, and the guide's evidence-gated template-addition policy paragraph is reconciled (no contradiction between "no speculative per-type splits" and the shipped arm). *Method:* `grep -nE "bug\.yml|improvement\.yml|observation\.yml|adr\.yml|epic|story|selector" release/references/how-to/intake-style-guide.md` + `ls .github/ISSUE_TEMPLATE/` — every named template resolves; read the policy paragraph for the reconciliation sentence. *Graded at Stage 9 QC3.5 on the merged PR.*
- [ ] **CIAC-3 (#1788 × #1085 on the methodology-resolution seam — `elicitation-loop.md` × `project-schema.md`):** intake-desk's resolution step defines its read order over the per-space fields shipping in this release — it resolves `operational_methodology` (when present) before falling back to `delivery_approach` — so the new fields are live at the intake seam, not dead-on-arrival [SOURCE: milestone Outcome Statement — per-space selection renders the matching intake vocabulary]. *Method:* `grep -n "operational_methodology" operations/skills/intake-desk/references/elicitation-loop.md` (≥1 hit inside the resolution step) + `grep -n "operational_methodology" core/schemas/project-schema.md` (field + V-rule present). *Graded at Stage 9 QC3.5 on the merged PR.*

### D-Decisions (D-ReleaseClass confirmation · D-Version recorded determination)

**D-ReleaseClass (judgment recurring-D — confirmation): CONFIRM `cross-cutting`.**
- **Gate input:** milestone-declared class (Stage 3, re-confirmed at the 2026-07-03 re-bundle) vs this plan's trigger evidence.
- **Trigger evidence:** cross-cutting trigger (c) fires — ≥3 in-bundle compositional edges per the A2 DAG (2 hard: #1085→#1973, #1067→#1973; 2 soft: #1788⇢#1973, #1788⇢#1085 = 4 edges). Surface span corroborates: 1 skill + 1 protocol + 2 schemas + intake templates (5 distinct governance-surface classes) [SOURCE: File Change Matrix]. Novel triggers also fire ((a) new template files / new schema concept; (b) ≥1 D-class decision in this plan) — multi-trigger resolution: `cross-cutting` > `novel` → cross-cutting dominates. Sizing already recorded at the 1.3 cross-cutting weight (18 raw / 23 effective, in band).
- **Differentiation posture:** Engagement density: **Tight** · Stage 9 review depth: **Deep** · Stage 5 activation bias: **ALL** (consistent with the all-or-nothing ACTIVATE above) · Stage 13 outcome-window: **30-day**.
- `domain_practice: { source: N/A — pipeline-internal release, date: 2026-07-03, domain: governance }`
- Reversibility: CHEAP (re-class cheaper-to-stricter n/a — already strictest of the three non-hotfix classes) / Confidence HIGH.

**D-Version (rule-determined recurring-D — recorded determination, no operator gate):**
- **Stage-4 determination:** bump-class **minor**; anchor **v3.65**; provisional **v3.66**.
- **Stage-4 verification (2026-07-03):** highest tag v3.65; ledger last row v3.65 VERIFIED; v3.66 free at verification time.
- **Commit-0 re-verify (rung 1 — this spoke, 2026-07-09):** `git fetch --tags origin && git fetch origin main`; claimed set recomputed — tags max **v3.66** (sibling `software-domain-templates` shipped during the pause; v3.65.1 also present), `plans/v3.66_RELEASE_PLAN.md` on main, zero remote release branches. Planned v3.66 **IS in the claimed set** → the detect-and-HALT contingency resolved operator-side during spawn: the operator re-rendered D-Version to **v3.67** (recorded in the Stage-6 spawn instructions); this spoke re-ran the re-verify against v3.67 → NOT in the claimed set AND equals recomputed next-free → **PROCEED**.
- **Remaining rung:** Stage-12 atomic tag claim — bounded-retry rung at the tag.

**D-Concurrency Posture (judgment recurring-D — operator ratifies at the D-Gate): `P0 fully-serial` (the undeclared default) — CONFIRMED; Stage 6 executed as one serial spoke.**
- **Inputs (all in hand per stage-04 §5):** A4 contention map (2 intra-release shared files), D-C topology SINGLE (default; one PR / one merge), 5-slice chain with only 2 hard edges, `overlap_class` distribution benign (append-pattern only cross-release). Parallelizing Stage 6 buys little (shared files force serialization of slices 1 and 5 anyway) and spends shared-window quota; P0 preserves the safe-by-construction serial guarantee. Non-serial postures would prohibit force-push on the shared branch — moot under P0.

### Tier-A Activated Design Artifacts

| Artifact | Flow class | Trigger fired | Tier |
|---|---|---|---|
| The §1.1.2 value→gate/filter coupling-contract table (3 consumer classes) in `core/schemas/work-item-type-schema.md` (#1803, slice 4) | Data flow | "creates/modifies a schema … with cross-component data flow (≥2 producer/consumer entities)" — producer = pack `[[controls]]` declaration; consumers = tracker-manager gate eval, DoR checks, Structure-Review/grooming filters | Tier-A (rendering tool: markdown tables, per the standard's Data-flow row) |

(G-CL6 reads this declaration at Stage 13. The other four slices: no Tier-A activation — negative declarations recorded in their Stage-5 specs.)

### Recommendations

1. **Merge/split: NONE.** Five issues are each a distinct capability seam (config-read / routing rule / schema fields / control layer / template bridge); sizing in band; the two file-sharing issues (#1085/#1973) are distinct capabilities correctly kept separate and sequenced, not merged.
2. **Stage-5 rider carry-list (bound into the Solutioning specs — all discharged at Stage 5):** (a) #1973 — template-per-type vs single-selector decided (template-per-type; selector rejected) AND reconciled with the evidence-gated template-addition policy; (b) #1803 — reserved Arm-3 body specified only (no criterion-grammar re-open), shared-vs-per-kind decided with rationale, feeds the existing architecture/DoR gate (no second gate), meta-schema stays v1 + ADR authored; (c) #1788 — V-rules numbered against the live tail (V16/V17 after V15, re-confirmed at spec + build time); (d) #1085 — the 5-rung resolver cited as the named seam; public corpus methodology-neutral (config-read, no archetype literals); CIAC-3's resolution-order clause included.
3. **Out-of-scope discoveries (route, do not action here):** (a) #1803's BELONGS_TO #160 parent remains [ASSUMPTION – CONFIRM] — owner: triage; (b) #1067 AC-4 (retire the `feedback_intake_template_by_type` memory pointer once shipped) is an operator-local action at close — flag it in the Stage-13 checklist, not in repo scope; (c) #384 (tracker carriage) open — coordination note only for #1803's spec.
4. **Hub-context staleness note:** the injected sibling row for v3.65 ("milestone still open, 7 issues; close-out will touch corpus files") was stale — corpus writes landed (`99d5a9f`) and milestone 70 is CLOSED with 0 open issues [SOURCE: gh, 2026-07-03]. No action; recorded so the Risk Register's downgrade (R2 scope now sibling-268-only) is traceable.
5. **Quota:** honor the WARN routing — capture operator quota state before the Stage-5 wave; default to the 3+2 split if partial; Checkpoint B decides per wave.

*Closure phrasing note: all five in-scope issues auto-transition via the single PR's Issue References block at merge; prose above deliberately uses mark-as-closed phrasing per the parser-clean discipline.*

## Change Description

### Outcome

An operator can now select a methodology **per space** — `operational_methodology` for PMO/project-delivery work, `release_methodology` for pipeline/SDLC work — and the intake front door renders the matching work-item vocabulary: intake-desk resolves the space-effective methodology through the platform's 5-rung config resolver, derives its kind registry from the shipped methodology packs, and types/places/relates items per the mapping framework, while dedicated pack-projected `epic.yml`/`story.yml` forms give the operator a direct filing path that stamps `type:*` labels structurally at submission. The deterministic type→template routing rule is single-sourced at intake-style-guide §1b (decided tool-blind, never escalated as an operator question), and the type-pack grammar gains the cross-cutting control-field layer (pack-level `[[controls]]` + the specified Arm-3 gate body, ADR-077).

### Issues resolved

| # | Outcome (one line) | Status |
|---|---|---|
| #1085 | intake-desk resolves `operational_methodology` ?? `delivery_approach` at Phase-2 entry, derives the kind registry via the 4-rung contract (type-pack > packs > Layer-2 map > invariant floor), and place-and-relates per framework §4.2; methodology-mismatch failure mode added; SKILL v2.28 | DONE |
| #1067 | Type→template routing table codified as guide §1b ahead of the 5-test, with the decided-tool-blind clause + decision-discipline determinate-classification exemption; bug.yml + CLAUDE.md.template reduced to discriminator + pointer (single-source) | DONE |
| #1788 | Optional per-space `operational_methodology` + `release_methodology` fields with V16 + V17 (V1–V15 byte-unchanged, absent ⇒ today's behavior); spec §5 Step-0 space-scoped resolution + per-space consumer audit; worked example §6.7 | DONE |
| #1803 | RESERVED Arm-3 `control-field` body specified + pack-level `[[controls]]` facet (§1.1.2) with the 3-consumer value→gate/filter coupling naming the shipped Structure-Review gate; `x-pmo-control` third annotation class; meta-schema stays v1; ADR-077 authored | DONE |
| #1973 | Template-per-type arm shipped: pack-projected `epic.yml` + `story.yml` (structural `type:*` + `status: proposed` stamping, per-type DoR fields); selector arm rejected/pruned (improvement.yml untouched); §1b/§6 registration + binding-table emission rules; SKILL v2.29 | DONE |

### Key decisions

- **D-Version → v3.67.** Commit-0 re-verify (2026-07-09) found the provisional v3.66 claimed by the sibling `software-domain-templates` (the plan-R1 contingency, materialized as designed); operator re-rendered to v3.67 = recomputed next-free; binds atomically at Stage-12 merge. See § D-Decisions.
- **D-Concurrency → P0 fully-serial.** One Engineering spoke built all five slices serially on one branch (shared intake-desk files forced slice-1-before-slice-5 anyway). See § D-Decisions.
- **#1973 arm → template-per-type (Stage-5 D1).** GitHub issue-forms mechanics force it: only a form's static `labels:` stamps structurally at submission, and forms have no conditional fields for per-type DoR — the selector arm fails both. §5b scope-of-trigger note reconciles the template-addition policy.
- **#1803 mechanism → pack-level `[[controls]]` facet (Stage-5 forced-single-survivor).** Declare once / project per kind; `gate-criteria-spec.md` deliberately NOT edited (the shipped gate rows live in the Stage-5 pipeline shard); `scope: self` v1 with container scopes registered-RESERVED fail-loud.
- **ADR numbered 077** (spec's provisional 076 was claimed by the v3.65.1 sibling during the pause; next-free re-verified across both ADR dirs at authoring) and recorded **Accepted-at-authoring** with the ratification in § Status — the flip gate (Collective Review scope-lock) pre-fired at spawn; deferring the flip recreates the ADR-039 ratified-but-never-flipped drift this release's Stage-5 routed.

### Reversibility

**CHEAP pre-tag / Confidence HIGH** — a single `git revert` of the release PR's merge commit restores all five slices atomically (content-only markdown/YAML; zero live PROJECT.md carries the new fields; zero packs declare `[[controls]]`; the K4 control-set instance is operator-local with no repo surface); MODERATE post-tag (tag + ledger row + corpus appends to unwind per RELEASE_PROTOCOL rollback).

### Downstream impact

- **Enables** per-space pack-lens rendering at every future intake touchpoint: the Step-0 resolution seam is live for the §8 consumer-table skill refits, and G3-18's release-space refit has a defined rung to adopt (routed follow-up).
- **Enables** control-set adoption: a deployment can now declare a design & architecture control set (K4) whose values feed the Structure-Review gate and grooming filters with zero consumer code changes; a reserved-scope widening release is owed when container-level value carriage ships.
- **Affected surfaces:** intake-desk (v2.29 — `.skill` package rebuild + mirror re-sync at Stage 12/13 per the standard R-7 step), the GitHub new-issue chooser (renders 6 forms post-merge), decision-discipline consumers (monotonic exemption-set extension).
- **Carry-forward items (route at close, not repo scope):** OPERATIONS.md Methodology-Awareness refresh bundle + G3-18 refit + per-space-conflation FM promotion (slice-3 Findings 2–4); gate-scope reconciliation for kind forms (Check-22 scan set / G-CL8 — slice-5 REC-4); ADR-039 status ratification-flip (slice-4 Finding 5); operator-local: `feedback_intake_template_by_type` memory-pointer retirement + the K4 control-set instantiation; post-merge live template-instantiation check (AC-1 honesty note).

### Cross-references

- Release plan: this file, top section (Header + Scope).
- Milestone: 87-methodology-pack-catalog — https://github.com/cody-hutson/pmo-platform/milestone/229
- User-facing release notes: `release/releases/notes/v3.67_RELEASE_NOTES.md` (authored at Stage 13 Close per `release/references/standards/release-notes-standard.md`).
