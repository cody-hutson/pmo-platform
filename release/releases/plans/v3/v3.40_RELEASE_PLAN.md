# v3.40 Release Plan — 30-doc-link-drift-drainage

> Lean implementation plan distilled from the Stage-4 planning-spoke output (release-planning issue #2705). Base `origin/main` @ 2d6ce1d. **Version = v3.40** (anchor v3.38; v3.39 claimed by the concurrent in-flight draft #2704 — see Deviation Log). Release Class = **routine**.

## Scope (4 slices)

Four documentation-drainage / corpus-accuracy chores, all doc-only — no code-behavior change, no operator-facing change, no new files. The 4 cards touch **disjoint file sets** (intersection = ∅) → clean any-order build on one branch, one PR, one merge.

| Slice | Card | Concept drained |
|---|---|---|
| 1 | #108 | Doc-link protocol §8 names a never-implemented dedicated `.claude/hooks/doc-link-warn-log.jsonl`; reconcile to the shared `deploy-check-warn-log.jsonl` Check 14 actually writes via `flag_warn_or_issue`. |
| 2 | #131 | Capital-P `Projects/` prose drift in the OPERATIONS.md pair → lowercase `projects/` (genuine prose refs to the Layer-2 root only). |
| 3 | #2684 | `routing-rules.md` Self-Update Protocol step 4 still routes intake to the dead `IMPROVEMENTS.md` / `IMP-###` mechanism → route to a GitHub Issue (`improvement.yml` / `observation.yml`). |
| 4 | #2700 | The project-path placeholder appears in inconsistent tokens (`Projects/[Project]/`, `Projects/[Project Name]/`) across 5 schema/spec/skill docs → unify to canonical `projects/[Project]/`. |

**CRUX resolution (inherited from #2705, BROAD reading):** #2700 owns ALL placeholder-token work (`[Project]` AND `[Project Name]`); #131 = genuine prose refs only (OPERATIONS.md pair). This preserves the disjoint-file property.

## Implementation Sequence

Cheapest-verification-first (a readability ordering, not a dependency ordering — all four are mutually independent):

1. **#108** — 2 lines, 1 file. `grep -c 'doc-link-warn-log'` → 0.
2. **#131** — genuine-prose casing refs across the OPERATIONS.md pair. Read-classify-per-line; never mechanical `sed`.
3. **#2684** — 1 line, 1 file. `grep -nE 'IMPROVEMENTS\.md|IMP-###'` → 0.
4. **#2700** — placeholder-token unification across 5 files (LAST — carries the pmo-skill-editor edit-discipline on the migrated `project-initiator` skill; `.skill` rebuild deferred to Stage 12).

## File Change Matrix

| # | Card | File | Change |
|---|---|---|---|
| 1 | #108 | `core/standards/doc-link-maintenance-protocol.md` | §8 mechanism line + flip-to-enforce row: `.claude/hooks/doc-link-warn-log.jsonl` → shared `deploy-check-warn-log.jsonl` (written by `flag_warn_or_issue`). No `deploy.sh` change. Mirror `core/rules/doc-link-maintenance.md` = 0 hits (no touch). |
| 2 | #131 | `core/governance/OPERATIONS.md` | Genuine prose `Projects/` → `projects/` naming the Layer-2 root. Read-classify each occurrence. |
| 3 | #131 | `operations/OPERATIONS.md` | Same — genuine prose refs only. (NOT byte-identical to core/ pair — 1271 vs 1079 lines; no mirror-reconciliation obligation.) |
| 4 | #2684 | `core/schemas/routing-rules.md` | Self-Update Protocol step 4: route proposal to a GitHub Issue, not `IMPROVEMENTS.md`/`IMP-###`. Other steps unchanged. |
| 5 | #2700 | `core/schemas/frontmatter-schema.md` | `Projects/[Project]/` → `projects/[Project]/`. |
| 6 | #2700 | `core/schemas/navigation-layer-schema.md` | `Projects/[Project]/` → `projects/[Project]/`. |
| 7 | #2700 | `core/specs/health-check-specification.md` | `Projects/[Project]/` → `projects/[Project]/`. |
| 8 | #2700 | `core/schemas/sqlite-index-schema.md` | Lowercase the bare `Projects/` casing refs (no `[Project]` token present) → `projects/`. |
| 9 | #2700 | `operations/skills/project-initiator/SKILL.md` | `Projects/[Project Name]/` → `projects/[Project]/`; lowercase genuine prose `Projects/`. pmo-skill-editor Mode A discipline; `.skill` rebuild DEFERRED to Stage 12. Frontmatter + cross-skill contracts intact. |

**Must-keep (never touched):** BLOCK-DESTRUCTIVE-007 guard + its test; `deploy.sh` uppercase-duplicate detectors; `settings.json.template`; dated historical/changelog records (e.g. the 2026-03-19 OPERATIONS entries); frozen fixtures; `[PROJECT_KEY]` refs; bare `[Project Name]` name-placeholders in prose/closure-summary templates that are not path tokens.

## Release Class

**routine** — all four `routine` triggers fire (all P3/P4 + size:S/M; zero new files; zero new D-class decisions; long-lived corpus targets). No higher class fires (not novel / cross-cutting / hotfix). The #131↔#2700 boundary is a settled scope-confirmation from the 2026-06-30 split, not a new D-class decision.

## Release Outcome Statement

**AFTER:** The active corpus names one consistent surface for each of four drifted concepts — the doc-link warn-log is described as the shared `deploy-check-warn-log.jsonl` the code actually writes (#108); the last live Self-Update instruction routes intake to a GitHub Issue instead of the retired `IMPROVEMENTS.md`/`IMP-###` mechanism (#2684); the Layer-2 project root reads in lowercase `projects/` across the OPERATIONS.md prose (#131); and the project-path placeholder is the single canonical `projects/[Project]/` token across the named schema/spec docs and the `project-initiator` skill (#2700). All must-keep guards (BLOCK-DESTRUCTIVE-007, deploy.sh uppercase detectors, `settings.json.template`), dated historical records, frozen fixtures, and `[PROJECT_KEY]` refs are preserved unchanged.

**BEFORE:** The doc-link protocol §8 names a never-implemented dedicated `.claude/hooks/doc-link-warn-log.jsonl`; `routing-rules.md` step 4 still instructs authoring into the dead `IMPROVEMENTS.md`/`IMP-###` mechanism; the OPERATIONS.md pair carries capital-`P` `Projects/` prose drift; and the same project-path concept appears in inconsistent placeholder tokens across schema and skill docs.

## Deviation Log

- **Concurrent-release contention (version):** The Stage-4 plan (#2705) computed the next-free minor as v3.39 against anchor v3.38. Since planning, a concurrent in-flight draft PR (#2704, milestone `07-INFRA-hygiene-measurement`) claimed the v3.39 slot on branch `release/v3.39-infra-hygiene-measurement`. This release therefore binds **v3.40** (anchor v3.38; v3.39 taken). Re-verified at Commit-0: `git tag -l 'v3.40'` empty, `gh release view v3.40` = not found, no branch/PR named `release/v3.40-*` other than this one.
- **Shared-file overlap with #2704 (pre-Stage-12 re-verify MANDATORY):** #2704 and this PR both touch `core/governance/OPERATIONS.md` and `operations/skills/project-initiator/SKILL.md` — same file, different lines, auto-mergeable at time of writing. Before Stage 12 (execute/merge), re-verify these two files for merge cleanliness against whichever of the two PRs merges first; rebase this branch onto the updated `origin/main` if #2704 lands first.

## Rollback

`git revert` of the single squash/merge PR reverts all four cards atomically (one PR = one merge = one revert). Reversibility **CHEAP / Confidence HIGH** — doc-only, no data migration, no schema change, no deploy-state beyond the `project-initiator` `.skill` rebuild (re-derivable from source at Stage 12).
