# v3.58 Release Plan — 44-repo-structure-and-filesystem-hygiene

> Durable release-plan record for milestone `44-repo-structure-and-filesystem-hygiene` (#117, Release Class `novel`). Transcribes the operator-approved Stage-4 plan (sub-task #2871) verbatim, plus the Collective Review scope-lock addendum. Topology **D-C SINGLE** (one branch / one PR). D-Version **v3.58** — re-versioned forward at Stage-12 execute (original provisional v3.46 was claimed by a sibling that shipped it as v3.49; v3.47 through v3.53 all shipped since while this branch was in flight — next-free over the tag frontier is now v3.58). Binds at Stage 12 per the repo versioning lineage; forward-only re-version-up on collision.

---

## Stage 4 Release Planning — v3.58 · 44-repo-structure-and-filesystem-hygiene (milestone #117)

**Status:** APPROVED at Procedure-0 gate 2026-07-01. Topology **D-C SINGLE** (one branch / one PR). Working reference until Engineering Commit 0. Produced by the Stage-4 planning spoke; hub-verified (R1 adversarial pass).

### Summary
3 open issues — **#73** (git-tracking policy + audit, M/4), **#230** (release-plan/notes major-version foldering, S/2), **#1079** (v2 vocabulary reconciliation umbrella, XL/16). Raw = **22 pts**. **Class `novel`** → `effective_pts = round_half_up(22 × 1.15) = 25` (at band ceiling) → **ship as one**. Sequence **#73 → #230 → #1079**. Dependencies clean; no contention (no file written by >1 issue).

### Dependency Graph
- `#230 → #73` — soft `relates-to` only (advisory; not a hard edge).
- `#1079 → #73` — none (taxonomy doc has 0 flat-path hits; absent from #1079 scope).
- `#1079 → #230` — none (mutually-exclusive scopes: #1079 freezes `release/releases/plans|notes/*`; #230 relocates via `git mv`).
- No hard edges, no cycles, no cross-milestone leaks. External-dep count = 0 (≤2 target).

### Implementation Sequence (foundation → infra → skill-core/eval)
1. **#73** (foundation: policy is the decision criteria the others rely on) — authoring only.
2. **#230** (infra: mechanical `git mv` reorg) — after #73 so version-less-file disposition cites the policy.
3. **#1079** (skill-core + eval: ~30-file sweep + version-bumped canary + 2 schema syncs) — sweeps a settled tree; internal order: (a) doc-path reconciliation, (b) canary skill-edit via pmo-skill-editor + version bump, (c) 2 schema syncs, (d) `deploy.sh --check` 9/13/14.

Single-PR discipline: all three are delivery slices on **one release branch, one merge gate**.

### Stage Applicability Matrix
| Stage | #73 | #230 | #1079 |
|---|---|---|---|
| 5 Solutioning | APPLY (policy-home design) | SKIP-trivial (documented-rule disposition) | APPLY (re-scope + canary approach) |
| 6 Engineering | APPLY | APPLY | APPLY |
| 7 Dev-Test | APPLY light (AC greps) | APPLY light (path-integrity) | APPLY functional (skill selftest + deploy.sh --check) |
| 8 QA | APPLY | APPLY | APPLY functional (skill-edit regression) |
| 9 Plan Review | Release-level — **Deep** (novel) | | |
| 12 Execute / 13 Close | Release-level — single merge + close | | |

### Contention Map — NONE (serialization not required)
No file is written by more than one issue (verified live). #73 sole-writes the taxonomy doc; #230 sole-writes RELEASE_LOG/DIGEST/INDEX + moves plans/notes; #1079 sole-writes the ~30 corpus files. The #230↔#73 `relates-to` is a coordination note, not a collision.

### Risk Register
- **R1 (scope, HIGH):** #1079 = 73% of raw pts. Mitigate: build internal order mechanical-first, skill-edit-second; contingent split boundary available if operator later overrides to cross-cutting.
- **R2 (rollback, MED):** #1079 canary fix is a version-bumped skill edit (MODERATE reversibility) — sequence as a discrete reviewable commit; doc edits revert CHEAP, canary reverts + version-bump-down + `deploy.sh --deploy` re-sync.
- **R3 (contention-latent, MED) [hub-verified — larger than spoke found]:** #230 `git mv` invalidates **151 path-refs** — RELEASE_LOG.md **46** + RELEASE_DIGEST.md **3** + RELEASE_INDEX.md **102** — plus ADR narrative refs. AC4 reconcile must be grep-exhaustive across `release/` + `core/`.
- **R4 (scope-boundary, MED):** #73 "100% coverage" AC is against **911** tracked files (not the 498 in its June body). Audit by file-class pattern, not per-file.
- **R5 (dependency-soft, LOW):** #230↔#73 soft edge — sequencing #73 first converts it to natural build order.
- **R6 (data-currency, LOW, mitigated):** counts re-verified live (88 plans / 102 notes / 911 tracked / 18 canary SKILL_LIST refs).
- **F2 (scope-staleness, MED) [hub-verified]:** #1079's body targets `CLAUDE.md.template` + `OPERATIONS.md`, both **already reconciled** (0 hits). Stage 5 re-scopes #1079 to its real remaining surface (canary + 2 schema docs + residual flat-path standards); XL may be inflated.

### D-ReleaseClass — `novel` (ratified)
`cross-cutting` evidence-refuted: (a) 0 `pipeline/stage-*.md` files touched; (b) ≤1 governance surface (RELEASE_LOG path-reconcile), not ≥3 (CLAUDE.md.template + OPERATIONS.md already clean); (c) 1 DAG edge, not ≥3. Posture: engagement Standard · Stage-9 Deep · Stage-5 activation ALL · 30-day outcome window.

### D-Version — recorded determination `v3.58`
**Re-versioned forward at Stage-12 execute (2026-07-02).** The original provisional `v3.46` (anchor v3.45, minor bump) was claimed by a concurrent sibling release (`release/v3.46-intake-governed-file-movement`), which shipped it forward as **v3.49**; **v3.47 through v3.53** all published (signed tags + GitHub Releases) while this branch was in flight. The plan was re-versioned to v3.50, then v3.51, as successive tags were claimed ahead of this branch reaching execute; each of those was in turn taken by concurrent releases (v3.51 by milestone-106, v3.52/v3.53 by subsequent cuts). Per the repo versioning lineage (next-free = highest-numeric-minor + 1 over the tag frontier), next-free is now **v3.58** (v3.46 gap is permanent/benign). Bump-class = minor (backward-compatible corpus/governance restructure). Claimed atomically at the Stage-12 merge tag; forward-only re-version-up on any further collision (never version-less as a hedge).

### Quota Budget — PASS
Worst parallel wave (Stage 7/8, 3 spokes incl. XL #1079) < 50% of the 5-hour envelope. Checkpoint B re-validates at every wave.

### File Change Matrix (machine-readable — refined per-issue at Stage 5/6)
```
core/standards/public-repo-vs-operator-instance-taxonomy.md
release/releases/RELEASE_LOG.md
release/releases/RELEASE_DIGEST.md
release/releases/RELEASE_INDEX.md
release/releases/plans/**
release/releases/notes/**
release/skills/pmo-skill-refiner-selftest-canary/SKILL.md
core/schemas/per-skill-output-contracts.md
core/specs/anthropic-base-vs-build-registry.md
```

### Out-of-scope discoveries (follow-up triage, not this release)
- #42 (fossil `v15.12` framing) — operator should re-scope or mark #42 as closed at Stage 13 by #1079 later.
- #328 (architecture-overview SKILL_LIST) — a #1079 child if pulled in before cut; else separate follow-up.

---

## Collective Review — scope-lock (2026-07-01)

- Both Stage-5 designs APPROVED (#73 EXTEND taxonomy §7; #1079 re-scope 30->8).
- #1079 re-sized **XL -> M** (raw 22->10 pts; effective@novel=12, ship-as-one unaffected).
- **Two-skill release**: #1079 Bucket B edits pmo-skill-refiner reference files -> pmo-skill-editor Mode A + version bump on BOTH canary AND pmo-skill-refiner.
- Recorded: #73 no-ADR (single forced approach); hub adversarial review run post-lock.
- Next: Stage 6 Engineering, write-serialized, sequence #73 -> #230 -> #1079.
