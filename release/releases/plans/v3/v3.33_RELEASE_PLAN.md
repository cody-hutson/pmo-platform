<!-- reference-durability: allow-link -->
# v3.33 Release Plan — 89-skill-anchoring-governance

| Field | Value |
|---|---|
| **Milestone** | `89-skill-anchoring-governance` (#232) |
| **Provisional version** | v3.33 (next-free; claimed atomically at Stage 12) |
| **Release Class** | routine |
| **Branch topology** | D-C SINGLE — one release branch, one PR, one merge |
| **Plan authored** | 2026-06-30 |
| **Orchestrator** | release-hub Mode O |

> **Branch-slug note:** the release branch is named `release/v3.32-skill-anchoring-governance` (cosmetic anchor only — `v3.32` avoids clashing the in-flight `release/v3.33-records-management` branch). The version is NOT bound by the branch name; it binds at the Stage-12 tag via `release/tools/claim-version.sh`. The provisional `v3.33` recorded here is re-verified and claimed atomically at Stage 12.

## Release Outcome Statement

**AFTER** — The six governance-domain specialist skills (pmo-project-manager, pmo-program-manager, pmo-portfolio-manager, pmo-knowledge-manager, pmo-program-coordinator, release-planner) each cite `domain-best-practices/governance.md` as their authoritative practice anchor.

**BEFORE** — The governance-domain specialists assert PMO/governance practices with no shared best-practice anchor; guidance is duplicated and drifts across the six skills.

**Success Indicator** — Each of the 6 SKILL.md files references `domain-best-practices/governance.md`; `governance.md` `consumers:` names all six skills.

## Summary (30 seconds)

Routine, mechanically-identical reference-anchoring across 6 sibling governance specialist skills — the direct sequel to v3.30 (software.md), same mechanical pattern against governance.md. 12 pts / 6 issues. Compose-by-reference only (ADR-019) — pointers, no content absorption. Single shared write-surface (`governance.md` `consumers:`) forces serial execution; built inline on one branch.

## Dependency Graph

```
#2172 ┐
#2173 ┤
#2174 ┼─ (6 anchors; all append governance.md consumers — shared write → serial)
#2175 ┤
#2176 ┤
#2181 ┘ (release-planner — reflexive: edits the planning skill itself; additive only, safe)
```

Acyclic. Six independent skill-core edits; no internal dependency edges.

## Implementation Sequence

1. #2172 pmo-project-manager → governance.md (create `## Reference docs`)
2. #2173 pmo-program-manager → governance.md (create `## Reference docs`)
3. #2174 pmo-portfolio-manager → governance.md (create `## Reference docs`)
4. #2175 pmo-knowledge-manager → governance.md (create `## Reference docs`)
5. #2176 pmo-program-coordinator → governance.md (create `## Reference docs`)
6. #2181 release-planner → governance.md (add a SEPARATE `## Reference docs` — its existing `## Reference Files` section is left intact)

## Stage Applicability Matrix

| Issue | St5 | St6 | St7 | St8 | St9 | St12 | St13 |
|---|---|---|---|---|---|---|---|
| #2172–#2181 (6 anchors) | skip | done | run | run | gate | gate | pending |

Stage 5 SKIPPED — routine + trivial, mechanically-identical reference edits with no design surface. Stage 7 (link-resolution) + Stage 8 (structural-AC) RUN, not collapsed into CI. Gate at Stage 9. Stage 12 claims the version, repackages the `.skill` set, and adds the DEPLOYED-row chore. Stage 13 is the close-out chore.

## File Contention Map

| File | Touched by | Class |
|---|---|---|
| `core/standards/domain-best-practices/governance.md` `consumers:` | all 6 anchors | shared → **serialize** |
| each `*/SKILL.md` | its own issue | no intra-milestone contention |

## File Change Matrix

```
operations/skills/pmo-project-manager/SKILL.md
operations/skills/pmo-program-manager/SKILL.md
operations/skills/pmo-portfolio-manager/SKILL.md
operations/skills/pmo-knowledge-manager/SKILL.md
operations/skills/pmo-program-coordinator/SKILL.md
release/skills/release-planner/SKILL.md
core/standards/domain-best-practices/governance.md
release/releases/plans/v3.33_RELEASE_PLAN.md
packages/pmo-project-manager.skill
packages/pmo-project-manager.skill.sha256
packages/pmo-program-manager.skill
packages/pmo-program-manager.skill.sha256
packages/pmo-portfolio-manager.skill
packages/pmo-portfolio-manager.skill.sha256
packages/pmo-knowledge-manager.skill
packages/pmo-knowledge-manager.skill.sha256
packages/pmo-program-coordinator.skill
packages/pmo-program-coordinator.skill.sha256
packages/release-planner.skill
packages/release-planner.skill.sha256
```

## Risk Register

| # | Risk | Reversibility | Mitigation |
|---|---|---|---|
| R1 | `governance.md` consumers serial-write contention (intra-milestone) | CHEAP | serial inline execution; extend the existing prose string (no restructure) |
| R2 | `consumers:` is a prose string, not a list | CHEAP | extend prose (surgical least-invasive); no restructure to a YAML list |
| R3 | `.skill` packages drift until rebuilt at release cut | CHEAP | rebuild the 6 packages at Stage 12 (`build-skill-packages.sh`); `deploy.sh --check` Check-7 flags until then (expected) |
| R4 | reflexivity — #2181 edits the `release-planner` skill that plans releases | CHEAP | edit is additive (a new `## Reference docs` pointer only); no behavioral/mode change; safe |
| R5 | provisional-version contention with in-flight `release/v3.33-records-management` | CHEAP | branch slug deliberately `v3.32` (cosmetic); version binds atomically at Stage 12 via `claim-version.sh`; re-verify next-free at the tag |
| — | rollback | CHEAP | all edits additive; revert = `git revert` the PR |

## Operator Decisions

**D-Version:** v3.33 — provisional next-free recorded determination, not a gate; re-verified and claimed atomically at Stage 12 via `release/tools/claim-version.sh`. Contention with the in-flight `release/v3.33-records-management` branch is resolved at the atomic claim — the branch slug (`v3.32`) is cosmetic and does not bind the version.

**D-C Branch Topology:** SINGLE — rule-determined (default + milestone-one-PR). Recorded.

**D-ReleaseClass:** routine — pre-declared at bundle time; mechanically-identical reference edits, low ceremony.

## Recommendations

1. **R5 version-claim:** confirm `release/v3.33-records-management` has not yet claimed v3.33 at Stage 12; if it has, re-version up per `bundle-composition-doctrine` §9.1 (re-version-up is expected runtime behavior under parallel work, not an error).
2. **CI coverage:** if only CodeQL + Analyze produce check-runs and the PR-time governance gates do not fire, equivalent assurance is provided by local Stage 7/8 + `deploy.sh --check` (same observation as v3.30).
