# v3.45 Release Plan — 42-pipeline-skill-doc-reconciliation

> Durable release-plan record for milestone `42-pipeline-skill-doc-reconciliation` (#121, Release Class `routine`). Transcribes the operator-approved Stage-4 plan (sub-task #2806) + the Procedure 0 gate decisions: plan APPROVED, Release Class `routine` (cross-cutting trigger (a) fired by letter on #499's 5 stage-file touches, operator-overridden), D-Version **v3.45** provisional. Binds at Stage 12 per the repo versioning lineage (anchor = published v3.44).

---

## Stage 4 Release Planning — 42-pipeline-skill-doc-reconciliation

> Provisional version **v3.45** (anchor = published v3.44; recorded determination per D-Version rule, binds at Stage 12 — not gated here). Release-scoped analysis; READ-ONLY. Live scope = 2 OPEN cards (**#499** `size:M`, **#887** `size:S`); **#306** `size:L` is CLOSED / shipped (excluded from build sequence, carried as shipped-context).

### Summary (30 seconds)
- **Sequence:** `#887 → #499` (contention-clean; either order is safe — #887 first only because it is smaller and touches a single file, a natural warm-up). No hard dependency forces the order. `[INFERRED]`
- **Dependency graph:** zero hard edges between #499 and #887 (both bodies declare "Blocked by: —" / "No hard prerequisite"). `[SOURCE]`
- **Contention:** file sets are **disjoint** — #499 edits `release/references/pipeline/*.md` + README; #887 edits `core/schemas/gate-criteria-spec.md` only. No shared file. `[SOURCE]`
- **Merge/split:** ship as **ONE PR / one merge** (milestone = one PR model). Both are doc/registry edits under one Release Outcome Statement; disjoint files mean zero merge risk. `[RECOMMENDED]`
- **Release Class:** **`routine`** — triggers (c) zero new files + (d) zero new D-class decisions both fire; not `cross-cutting` (touches only 1 `pipeline/stage-*.md` file class edited by #499 across several stage docs, but the `cross-cutting` trigger (a) requires ≥3 *distinct* `pipeline/stage-*.md` files — see note). `[RECOMMENDED]`
- **Size/band:** effective **6 pts** (M=4 + S=2, `routine` weight ×1.0) — **UNDER** the 15–25 band. **Accepted** per bundle-composition-doctrine (capability boundary is clear + this is the residual live slice of an epic whose other members already shipped) — not a re-bundle signal. `[INFERRED]`
- **Quota (Checkpoint A):** **PASS** — worst parallel wave = 2 spokes (cost bands "lowest" + "low–moderate"); nowhere near the 50% envelope threshold. Load-bearing gate is Checkpoint B per wave (hub Step 5.5). `[INFERRED]`
- **Rollback:** unit = the single release PR; pre-merge = close PR (no trace), post-merge = `git revert` the squash-merge commit. Reversibility **CHEAP / HIGH**. `[INFERRED]`
- **Operator decisions to render:** (1) confirm Release Class `routine`; (2) confirm under-band ship-as-is (vs. hold for more scope); (3) D-Version recorded (v3.45) — no gate.

---

### Dependency Graph

```
#306 [CLOSED/SHIPPED] ····(shipped-context; G-PR8 prose already at stage-09-plan-review.md §A6.5 on main)
        ┊ coordinate-with (soft, now satisfied by #306 being live)
        ▼
#887 (size:S) ── register G-PR8 row in gate-criteria-spec.md Gate-9 table + Self-Repair row
                 no hard edge
#499 (size:M) ── make stage-purpose contract explicit (README + 6 stage docs §1 Purpose)
                 no hard edge; companion (not blocked-by) to fission siblings #500/#501/#502 in OTHER milestones
```

- **Hard edges among live cards:** **NONE.** `[SOURCE]` — #499 body: "Blocked by: — · Blocks: —"; #887 body: "No hard prerequisite." GitHub native dependency check: no `blocks`/`blocked by`/`depends on` link present between #499 and #887 (timeline cross-references are to #497/#500/#779/#1007/#427/#546, none a blocking edge).
- **#887 ↔ #306 (soft coordinate-with):** #887 body asks to "coordinate with #306 (prose relocation) so the registry row and relocated prose stay consistent." #306 is **already shipped on main** (its G-PR8 prose lives at `stage-09-plan-review.md § Phase A6.5`, verified). The coordination constraint is therefore **satisfiable by reading the live prose** — #887's registry row must describe the same G-PR8 (Stage 9 Phase A6.5 mid-pipeline-divergence check) that #306 already relocated. This is a *consistency* obligation, not a build-order blocker. `[SOURCE]`
- **#499 companions in other milestones:** #500 (shipped, M69), #501 (open, M68), #502 (shipped, M35) are fission siblings b/c/d of epic #497. They are **companions, not dependencies** — #499 inherits parent AC#1 and does not block on them. No cross-milestone hard edge. `[CONTEXT]` (per injected Mode R cache + #499 body).

### Implementation Sequence

Recommended build order on the single release branch:

| Order | Card | Size | Files | Why here |
|---|---|---|---|---|
| 1 | **#887** | S | `core/schemas/gate-criteria-spec.md` | Smallest, single-file, self-contained additive registry row. Zero contention. Establishes the "complete Gate-9 registry" half of the Outcome Statement. `[INFERRED]` |
| 2 | **#499** | M | `release/references/pipeline/README.md` + `stage-01/04/05/06/08` §1 Purpose (~6 files) | Larger surface (README contract + multiple stage-doc §1 lines). Disjoint from #887's file, so order is a convenience not a constraint. Establishes the "explicit map/design/build/validate contract" half. `[INFERRED]` |

**Order is contention-driven convenience, not dependency.** Under D-C SINGLE topology (default P0 fully-serial), Stage 6 Engineering routes ONE chip at a time regardless; the disjoint file sets mean the reverse order (#499→#887) is equally safe. Sequencing rationale: land the single-file additive change first, then the multi-file contract edit. `[INFERRED]`

### Stage Applicability Matrix

Both cards are internal documentation/registry edits (`domain: governance`, sourcing-exempt pipeline-internal per stage-04 §5). Stage posture per card:

| Stage | #887 (registry row) | #499 (stage-purpose contract) |
|---|---|---|
| **5 Solutioning** | **APPLY (light).** WHAT is specified in the body; exact G-PR8 row *wording* is a Stage-5 determination (body says "exact wording is a Stage 5 determination if bundled"). Under `routine` class the Stage-5 activation bias is SKIP-where-trivial — but a genuine wording-design choice exists (mirror the #306 prose), so activate lightly. `[SOURCE]` | **APPLY (light).** Body verdict = "EXTEND existing §1 Purpose statements"; the four-part contract wording for README is a design choice. `routine` SKIP-where-trivial bias applies, but the cross-stage contract phrasing warrants a light Stage-5 pass. `[SOURCE]` |
| **6 Engineering** | **APPLY.** The edit itself (add G-PR8 criteria row + Self-Repair row). Write-serialized. `[SOURCE]` | **APPLY.** The edits (README contract + ~5 stage-doc §1 lines). Write-serialized. `[SOURCE]` |
| **7 Dev Testing** | **APPLY (doc-verify posture).** Not code — no unit tests. DT = grep-based AC verification (`grep 'G-PR8' gate-criteria-spec.md` ≥1; Self-Repair row present). Run the scaffolded stage, do not collapse into CI. `[INFERRED]` | **APPLY (doc-verify posture).** DT = grep the four-part contract in README + each edited §1 Purpose line retains its stage-specific detail (regression AC). `[INFERRED]` |
| **8 QA Testing** | **APPLY (acceptance posture).** Confirm the registry row semantics match the live G-PR8 (Phase A6.5) + the #306-relocated prose (consistency AC). `[INFERRED]` | **APPLY (acceptance posture).** Confirm contract is consistent across README + all edited stage docs; no stage silently absorbs another's role (the Outcome Statement's success condition). `[INFERRED]` |
| **9 Plan Review** | **APPLY (release-scoped, Standard depth).** Single gate for the whole release. `routine` → Stage 9 **Standard** depth (PR-diff conformance to File Change Matrix + G-PR7 outcome-conformance). `[SOURCE]` | (same single release gate) |
| **10 (n/a in 13-stage numbering)** | — | — |
| **11 (n/a)** | — | — |
| **12 Execute** | **APPLY (release-scoped).** Single per-release Execute: merge PR, claim/tag v3.45, DEPLOYED row. `[SOURCE]` | (same single Execute) |
| **13 Close** | **APPLY (release-scoped).** Single Close chip: RELEASE_LOG row + visible-H4 Deployment Log + INDEX + DIGEST + RELEASE_NOTES. `routine` → 30-day outcome-window. `[SOURCE]` | (same single Close) |

**SKIP reasons (explicit):** No stage is fully skipped for either card. The *only* soft-skip signal is the `routine` **Stage 5 SKIP-where-trivial** bias — but both cards carry a genuine wording/design determination (deferred from intake per the intake-vs-solutioning discipline), so Stage 5 activates lightly rather than skipping. **Stage 7/8 posture for doc-only changes:** they APPLY but as *verification* stages (grep-AC + semantic-consistency acceptance), not code-test stages — do NOT collapse them into CI + Stage-9, and do NOT skip them (per the "run the scaffolded verification stages" discipline). `[INFERRED]`

**Stages numbered 10/11:** the platform's "13-stage pipeline" numbering does not expose standalone operator-facing spokes at 10/11 in the same sense as 5–9/12/13; the release-scoped stages 4/9/12 + Close(13) plus per-issue 5/6/7/8 are the operative set. Marked n/a rather than SKIP to avoid implying a spoke exists to skip. `[ASSUMPTION – CONFIRM]` — expected answer: 10/11 carry no per-issue parallelism axis for this doc-only release and require no separate applicability call.

### Contention Map

| File / surface | #887 | #499 | Contention |
|---|---|---|---|
| `core/schemas/gate-criteria-spec.md` | EDIT (add G-PR8 criteria row + G-PR8 Self-Repair row) | — | none |
| `release/references/pipeline/README.md` | — | EDIT (add four-part map/design/build/validate contract) | none |
| `release/references/pipeline/stage-01-intake.md` §1 | — | EDIT (reflect "map current state / surface gaps") | none |
| `release/references/pipeline/stage-04-planning.md` §1 | — | EDIT (reflect "map current state / surface gaps") | none |
| `release/references/pipeline/stage-05-solutioning.md` §1 | — | EDIT (reflect "design") | none |
| `release/references/pipeline/stage-06-engineering.md` §1 | — | EDIT (reflect "build/tweak") | none |
| `release/references/pipeline/stage-08-qa-testing.md` §1 | — | EDIT (reflect "validate") | none |

- **Within-release contention:** **NONE** — the two cards' file sets are fully **disjoint** (`core/schemas/*` vs `release/references/pipeline/*`). No file is claimed by both change-specs. `[SOURCE]`
- **Cross-PR / cross-release contention (A4 baseline-pinned):** `gate-criteria-spec.md` is a high-traffic governance file (it just received G-PR9 from #87 and G-PR8-adjacent edits) — flag as a **watch item** for the Stage-6 Commit-0 re-verify + the Stage-9 G-PR9 sibling-merge predicate, NOT a within-release blocker. #499's stage-doc edits are §1-Purpose-line-local (append/clarify pattern), low cross-PR collision risk. Baseline to be pinned at release-branch base at Engineering Commit 0; re-checked at Stage 9 per G-PR9. `[INFERRED]`
- **Structural-blast-radius:** neither card is a file-mover (no rename/relocate/delete; both are in-place edits). `SURFACE(R)` structural axis does not fire. `[INFERRED]`

### Risk Register

| # | Risk | Class | Severity | Mitigation |
|---|---|---|---|---|
| R1 | **#887 registry row diverges from #306's live A6.5 prose** (two descriptions of the same G-PR8 drift apart). | Consistency / dependency | **MEDIUM** | Stage 5/6 authors the G-PR8 row by reading the live `stage-09-plan-review.md § Phase A6.5` prose (on main) as the source of truth; Stage 8 acceptance AC explicitly checks row↔prose consistency. Record the coordination note (#887 AC#3). CHEAP to fix. `[SOURCE]` |
| R2 | **`gate-criteria-spec.md` concurrent-edit collision** — a sibling release edits the same high-traffic file between this release's baseline pin and Stage 12. | Contention (cross-PR) | **LOW–MEDIUM** | Stage-6 Commit-0 version/file re-verify + Stage-9 G-PR9 sibling-merge predicate (`git log baseline..origin/main --name-status`) intersected against `SURFACE(R)`; STALE-REVALIDATE → re-run divergence check. Additive row = low semantic-conflict risk even on textual overlap. `[SOURCE]` |
| R3 | **#499 regression — a §1 Purpose edit *replaces* stage-specific detail** instead of clarifying it (violates #499 AC#4). | Scope / correctness | **LOW–MEDIUM** | #499 build rule is "wording clarification, not replacement"; Stage 7 DT regression AC verifies each edited §1 line preserves its existing stage-specific detail. Per-line diff review at Stage 9. `[SOURCE]` |
| R4 | **Under-band bundle (6 pts)** read as an incomplete/mis-scoped release. | Scope / capacity | **LOW** | Documented acceptance: this is the residual live slice of a governance-hygiene epic whose sibling members (#306, and #499's fission-siblings) already shipped; capability boundary (the Release Outcome Statement's two grep-verifiable conditions) is clear. Not a re-bundle signal. `[INFERRED]` |
| R5 | **Rollback complexity** | Rollback | **LOW (CHEAP)** | Single-PR unit; pre-merge close = zero trace; post-merge `git revert` of the squash commit cleanly removes both additive doc changes (no schema/data migration, no downstream state). `[INFERRED]` |

No HIGH/CRITICAL risks. All entries CHEAP-to-MODERATE reversibility.

### Recommendations

**1. Release Class — `routine`** `[RECOMMENDED]`
- **Trigger evidence (Class Enum, any-one-fires; two fire):**
  - (c) **zero new files added** — both cards EDIT existing files (registry row into an existing table; §1 Purpose clarifications into existing docs). `[SOURCE]`
  - (d) **zero new D-class decisions in the release plan** — no design-uncertainty D-decision; the only Ds are the recurring D-ReleaseClass (judgment, this decision) + D-Version (rule-determined, recorded). Neither is a *new* D-class decision introduced by the scope. `[INFERRED]`
- **Differentiation from `novel`:** trigger (a) "introduces a new reference doc, schema, or skill" does NOT fire — #887 adds a *row to an existing schema*, not a new schema; #499 edits existing docs. Ticket-newness ≠ protocol-newness (the `novel` anti-pattern). `[SOURCE]`
- **Differentiation from `cross-cutting`:** trigger (a) requires the File Change Matrix to touch **≥3 `pipeline/stage-*.md` files**. #499 *does* edit stage-01/04/05/06/08 (5 stage-doc §1 lines) — **so cross-cutting trigger (a) technically fires on ≥3 stage files.** This is the one genuine classification tension. **Resolution:** the `cross-cutting` anti-pattern warns against classifying a release `cross-cutting` because edits "happen to be in `pipeline/stage-*.md` files" when the change is a **single coherent wording-clarification** rippled across sibling stage docs (not 3 independent stage-behavior changes). The edits are one contract expressed in N places — aggregate *coordination* scope is low (append/clarify §1 lines, no gate-behavior change per #499 "no gate-behavior change (low risk)"). **Operator call needed** (see below). If the operator reads the ≥3-stage-file letter as binding → `cross-cutting` (higher ceremony: Tight engagement + Deep Stage 9). Planner recommendation: **`routine`**, because the substance is a low-blast-radius wording clarification, and multi-trigger resolution's ceremony escalation is meant for genuine 3-way structural change. `[RECOMMENDED]`
- **Proposed differentiation posture (if `routine`):**
  - Engagement density: **Light** (REQUIRED)
  - Stage 9 review depth: **Standard** (REQUIRED)
  - Stage 5 activation bias: **SKIP-where-trivial** (OPTIONAL) — but activate lightly for the two deferred wording determinations
  - Stage 13 outcome-window: **30-day** (OPTIONAL)
- **Milestone-description `## Release Class` section is NOT yet authored** (only the `### Release Outcome Statement` is present). Per the taxonomy, class is declared at Stage 3 Phase B3 — **[ASSUMPTION – CONFIRM]:** this milestone predates the Release-Class section convention or it was deferred; recommend the hub author the `## Release Class` H2 (class `routine` + rationale + posture) at the D-ReleaseClass gate before Stage 6. `[INFERRED]`

**2. Merge/split — ONE PR / one merge** `[RECOMMENDED]`
- **Evidence:** milestone = one-PR model (per feedback discipline: "a milestone ships as a SINGLE PR + single merge; issues are delivery slices on one branch"). Both cards are delivery slices of one Release Outcome Statement (the two grep-verifiable AFTER conditions). File sets are **disjoint** → zero merge-conflict risk shipping together. Splitting into two PRs would add release ceremony (two Stage 9s, two Stage 12s, two version claims) for no risk-reduction benefit. `[SOURCE]`
- Build both on one release branch in the sequence above; one PR; one Stage 9 GO; one merge; one v3.45 tag.

**3. Size/band — 6 effective pts, UNDER-band, ACCEPTED (not a re-bundle signal)** `[INFERRED]`
- Raw = M(4) + S(2) = **6 pts**. `routine` class weight ×1.0 → `effective_pts = 6`. Band = 15–25 (doctrine default). 6 pts sits **below the <10 "merge or keep as gating-only" floor**.
- **Disposition = ACCEPTED as-is**, not merge/re-bundle, because: (a) capability boundary is **clear and grep-verifiable** (the Release Outcome Statement's two conditions); (b) this is the **residual live remainder** of a governance-hygiene epic whose other members already shipped (#306 shipped; #499's fission-siblings #500/#502 shipped) — the natural adjacent slices to merge with are *already released*, leaving nothing to merge into; (c) doctrine §3 Step 5 explicitly permits a <10-pt slice to "ship if capability boundary is clear." Holding these two for a larger future bundle would strand two verified-ready, live registry/doc gaps. **Recommend ship.** `[INFERRED]`
- **Operator confirm:** accept under-band ship (recommended) vs. hold for more scope.

**4. Rollback strategy** `[INFERRED]`
- **Unit of rollback:** the single release PR (both cards).
- **Pre-merge:** close the PR — zero trace, nothing landed on main. Reversibility **CHEAP / HIGH**.
- **Post-merge:** `git revert <squash-merge-SHA>` on main — cleanly removes the G-PR8 registry row + Self-Repair row and the README/stage-doc §1 clarifications in one revert commit. No schema migration, no data, no downstream-consumer state to unwind (additive registry row + wording edits only). Reversibility **CHEAP / HIGH**.
- **Partial rollback (if only one card regresses):** because files are disjoint, a targeted `git revert -- <path>` or a follow-up corrective commit can back out just #499's stage-doc edits **or** just #887's registry row without touching the other. **CHEAP / HIGH.**

**5. Quota-Budget estimate (Checkpoint A) — PASS** `[INFERRED]`
- **Parallel-eligible spokes per parallel stage (from the A2 matrix, stages 5/7/8 parallel-safe):** Stage 5: up to **2** (#887, #499) · Stage 7: up to **2** · Stage 8: up to **2**. Stage 6 is write-serialized (1 at a time). Worst parallel wave = **2 spokes**.
- **Per-spoke cost (size-bucket ordinal band, quota-budget-protocol §5):** #887 `size:S` → **lowest**; #499 `size:M` → **low–moderate**. Worst-batch cumulative = lowest + low–moderate.
- **Envelope:** operator-stated quota state unstated → conservative default assumed (fresh/near-full 5-hour window). `[ASSUMPTION – CONFIRM]` — expected answer: window is not near-exhausted at hub start.
- **Estimated cumulative draw (worst wave):** 2 low-cost spokes against a ~full envelope → well **< 50%**. **Verdict: PASS** — proceed parallel, no warning in plan.
- **Note:** advisory only. The **load-bearing gate is Checkpoint B (hub Procedure 2 Step 5.5)**, re-validated before *every* parallel wave against the *remaining* envelope. Do not treat this plan-time PASS as clearing the whole release.

**Plan-conformance to the Release Outcome Statement:** the sequence realizes both AFTER conditions — #887 delivers "the `gate-criteria-spec.md` Gate-9 registry lists every live criterion including G-PR8"; #499 delivers "every stage doc states an explicit map/design/build/validate purpose contract." The Success Indicator (both grep-verifiable post-merge: README four-part contract present + `gate-criteria-spec.md` carries a registered G-PR8 row) maps 1:1 to the two cards' completion conditions. **Conformant.** `[SOURCE]`

---

*Stage 4 (Release Planning) — Release Manager persona (Skills-Map §13 Mode 1). READ-ONLY analysis; no repo files edited, no branch created. Scaffolding is the hub's Procedure 1 job. D-Version is a recorded determination, not a gate.*

