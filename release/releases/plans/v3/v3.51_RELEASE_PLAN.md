# Release Plan — v3.51 · Milestone #255 `106-skill-suite-tail-closeout`

**Release Class:** `routine` · **D-Version:** v3.51 (provisional; binds at Stage 12)
**Re-version provenance:** initial provisional v3.49 collided (v3.49 published as a GitHub Release, v3.50 reached Stage 12 on main during orchestration). Forward-only re-anchored to next-free **v3.51** (max claim v3.50 + 1). Stage 12 re-verifies next-free and may bump again.
**Domain:** governance (pipeline-internal; `source: N/A — pipeline-internal release`)
**Overall reversibility:** CHEAP · confidence HIGH — additive documentation cross-references + a deterministic `.skill` repackage; no EXPENSIVE/IRREVERSIBLE action.

## Release Outcome Statement
**AFTER** raid-templates §9 references the canonical type-split age-escalation thresholds, and `pmo-tier-2-support` cites the now-shipped `support.md` guide — closing the last tail of skill-function-hardening (#1105) and skill-bp-anchoring (#2164).
**BEFORE** raid-templates §9 carries a generic `>30d` escalation diverging from the type-split; `pmo-tier-2-support` cites `root-cause-analysis.md` but not `support.md`.

## 1. Implementation Sequence
One branch (`release/v3.51-skill-suite-tail-closeout`), one PR, one merge. Two slices, independent (no cycle, no cross-milestone leak). Order: **#931 first** (smaller, isolated file), then **#2208** (SKILL.md edit + `.skill` repackage adjacent to the cut).

## 2. File Change Matrix
| # | Issue | File | Change | Precise edit |
|---|---|---|---|---|
| 1 | #931 | `operations/skills/delivery-engine/references/raid-templates.md` **§9 Anti-Patterns** (Stale-RAID row, line 292) | EDIT | Replace generic "age-based escalation (>30 days = auto-escalate)" with the type-split summary (issues >14d warn/>30d escalate; risks >30d warn/>60d escalate) + cite canonical home `ppm-agent/references/escalation-thresholds.md` §3. |
| 2 | #2208 | `operations/skills/pmo-tier-2-support/SKILL.md` | EDIT | Add one `## Reference docs` section citing `core/disciplines/root-cause-analysis.md` (RCA method) + `core/standards/domain-best-practices/support.md` (support best-practice anchor). Placement: after `## Dependency Graph Node`. |
| 3 | #2208 | `packages/pmo-tier-2-support.skill` (+ `.sha256`) | REBUILD at cut | `bash core/deploy/tools/build-skill-packages.sh pmo-tier-2-support` |

**Scope note (verified):** `support.md` `consumers:` **already** lists `pmo-tier-2-support` → AC2(ii) pre-satisfied; **support.md is NOT edited.** Net durable edit = 2 files.
**Section correction (verified):** target text is in **§9 Anti-Patterns** (not §8 as the issue body states). §8 = "Methodology Variation."
**Durability note (verified):** raid-templates.md carries `<!-- reference-durability: allow-link -->` (line 1) — bare links permitted; summarize-then-cite still preferred (applied).

## 3. Integration Points
- `raid-templates.md` → `escalation-thresholds.md` §3 (doc-of-record, type-split table) — target verified present.
- `pmo-tier-2-support/SKILL.md` → `root-cause-analysis.md` (relative depth `../../../core/…`) — present.
- `pmo-tier-2-support/SKILL.md` → `support.md` — present (committed f35e569, 2026-06-30).
- support.md `consumers:` back-reference — ALREADY satisfied (no edit).

## 4. Stage Applicability Matrix
| Stage | Disposition | Rationale |
|---|---|---|
| 5 Solutioning | SKIP | Routine; two additive cross-refs, zero design uncertainty/new files/D-class. |
| 6 Engineering | APPLIES | The edits + repackage. |
| 7 Dev Testing | APPLIES (real) | grep-assert cross-refs resolve; assert link targets exist on disk. |
| 8 QA Testing | APPLIES (real) | reference-durability form; SKILL.md structural integrity; `.skill` + sha256 rebuilt. |
| 9 Plan Review | APPLIES (Standard) | PR-diff conformance vs this matrix; human GO/NO-GO. |
| 10 Dry Run | SKIP | Claude Code path — PR diff IS the dry-run. |
| 11 Snapshot | SKIP | Claude Code path — git history IS the snapshot. |
| 12 Execute | APPLIES | Merge single PR; bind v3.51 (re-verify next-free, tag-claim); RELEASE_LOG row + visible-H4 Deployment Log; `.skill` verified at cut. |
| 13 Close | APPLIES | Milestone close; INDEX+DIGEST+RELEASE_NOTES; RELEASE_LOG VERIFIED; published GitHub Release. |

## 5. Risk Register
| Risk | Sev | Owner | Mitigation |
|---|---|---|---|
| `.skill` package drift (Check-7 cluster) | MED | Eng (St 6/12) | Rebuild via build-skill-packages.sh; `.skill`+sha256 committed on-branch; `deploy.sh --check` Check 7 green pre-merge. AC3 gates. |
| Cross-ref link-rot (relative-path depth) | MED | Eng / St 7 | DT asserts each target resolves from the editing file's dir (tier-2 SKILL → `../../../core/…`; raid → `../../ppm-agent/…`). |
| **Version re-collision** (rapid concurrent releases) | MED | Stage 12 | v3.51 provisional; Stage 12 re-verifies next-free and forward-only bumps if taken. Never version-less, never down. |
| Wrong-section edit (#931 body says §8) | LOW | Planning (pinned) | Edit §9 Anti-Patterns Stale-RAID row (done). |

## 6. Delivery Strategy
Branch `release/v3.51-skill-suite-tail-closeout`; ONE PR / ONE merge; two commits (#931, then #2208 incl. repackage). Close-keywords in the PR Issue-References block only. Green CI + Stage-9 GO required. Never merge red CI. P0 fully-serial.

## 7. Verification Plan (AC → method)
- #931: `grep escalation-thresholds raid-templates.md` ≥1 AND generic-only ">30 days = auto-escalate" phrasing gone from the Stale-RAID row.
- #2208 AC1: `grep root-cause-analysis.md SKILL.md` present under a `## Reference docs` section.
- #2208 AC2: (a) `grep domain-best-practices/support.md SKILL.md` ≥1; (b) support.md `consumers:` lists tier-2 (already ✓).
- #2208 AC3: build-skill-packages.sh exits 0; `deploy.sh --check` Check 7 green; sha256 matches.
- Links: `test -f` each resolved target.

## 8. Rollback Strategy
CHEAP. Pre-merge: delete branch. Post-merge: `git revert` merge commit + rebuild `.skill`. Operator-authorized per RELEASE_PROTOCOL.md § Rollback.

## 9. Quota Budget (A6)
PASS. Worst parallel batch = 1 spoke (St 7 or 8). Trivial draw vs 5-hour envelope.

## 10. Gate Results
- **A0 Triage→Design Re-Review:** CONSUMED from Mode R (not re-run). R1 C1 · R2 C2/PT-2-partial · R3 C1 · R4 C1; deps independent.
- **A0.5 AC currency (G-PL1):** #931 PASS; #2208 PASS (AC2(ii) pre-satisfied — narrowing, not mismatch).
- **A0.6 crisping (G-PL2):** both PASS (#931 acceptance implicit in "what good looks like").
- **A0.7 placement forward-check (G-PL3):** SKIP — no cross-directory reorg in the base→main window; both targets exist; no new files.
