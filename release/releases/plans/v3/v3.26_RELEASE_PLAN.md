# v3.26 Release Plan — 39-governance-drift-canonicalization-ci

> Stage 4 (Release Planning) output. Captured as Engineering Commit 0 at release branch bootstrap; **re-versioned at Stage-9 re-baseline** (merge of `origin/main`). **Version PROVISIONAL** (next-free over latest published v3.24; v3.20–v3.25 already claimed — v3.20–v3.24 via tags, v3.25 via an in-flight release branch + committed plan). Active parallel-release work today may shift the number further — binds atomically at Stage 12, never reserved early.

## Summary (30 seconds)

Four issues form **one** governance-drift-canonicalization-CI capability slice: **#135** (Tier-1 governance reconcile of the 6 genuine `Projects/_governance/` → `projects/_config/` drift hits, then a net-new capital-`P` `Projects/` casing gate), **#133** (a governance-file-map cross-check job that regex-parses the `## Governance File Map` table and asserts each claimed path resolves on disk), **#228** (a platform-convention linter — `deploy.sh` Check 49 + a new `check-convention.sh` engine covering 4 residual dims), **#757** (pre-flip hardening of the cross-reference-integrity gates). The one same-file contention is **`repo-integrity.yml`** (#133 writes job A first, #135 commit-2 appends job B after it — resolved by sequencing). Ships as **one PR / one merge**. Release Class **routine**. The 3 NEW gates all ship **warn-mode-initial** (per `core/rules/bypass-mode-readiness.md`); #757 leaves the existing gates warn-mode (it hardens, it does not flip). Rollback = `git revert -m 1` of the single merge.

## Release Outcome Statement

**AFTER:** the platform CI / `deploy.sh` gate family catches governance-drift recurrence at PR-time — governance-file-map drift (#133), `Projects/`→`projects/` casing (#135), platform-convention violations (#228) — and the cross-reference-integrity gates are hardened for a clean flip-to-enforce (#757).

**BEFORE:** three canonicalization rules shipped (#134 / #136 / #137); the map / casing / convention-linter / pre-flip-hardening gaps remained open.

## Dependency Graph

Directional edges derived from each issue's prework comment (2026-06-27) and the Stage-4 record (#2499):

```
#135 ──commit-1 (reconcile)──▶ #135 commit-2 (casing gate)   (clean-first precondition)
#133 ──writes repo-integrity.yml job A FIRST──▶ #135 commit-2 (appends job B after)   (same-file sequencing)
#228            (no edges — extends deploy.sh + new tool)
#757            (no edges — hardening edits to existing gate surfaces)
```

- **#135 internal: reconcile → casing gate.** The casing gate forbids net-new capital-`P` `Projects/`; the corpus must be reconciled clean FIRST (the 6 genuine hits) so the gate activates against a clean baseline. The reconcile is its own reviewed commit (it edits Tier-1 governance).
- **#133 → #135 commit-2.** Both append a job to `repo-integrity.yml`. #133 is the FIRST writer (governance-file-map job); #135 commit-2 appends the casing job AFTER it. Sequencing on one branch resolves the same-file contention — no textual conflict.
- **#228, #757** — independent (different files: `deploy.sh` + new `check-convention.sh` for #228; `check-skill-count-imp.py` / `reference-durability.yml` / `run-fragile-ref-fixtures.sh` for #757).

No cycles. Single ordering spine: **#135 commit-1 → #133 → #135 commit-2 → #228 → #757**.

## Implementation Sequence (write-serialized on one branch — commit order)

| Order | Issue | Container | Why here |
|---|---|---|---|
| 0 | (plan) | — | Release plan committed as Commit 0 (this file). |
| 1 | **#135 commit-1** | Tier-1 reconcile | Reconcile the 6 genuine `Projects/_governance/` → `projects/_config/` hits in its OWN reviewed commit. Clean-first precondition for the casing gate. Historical 2026-03-19 changelog entries left untouched (editing falsifies the dated record). |
| 2 | **#133** | repo-integrity.yml job A | FIRST writer to `repo-integrity.yml`. Governance-file-map cross-check job + self-test fixture. Warn-mode-initial. |
| 3 | **#135 commit-2** | repo-integrity.yml job B | Net-new capital-`P` `Projects/` casing gate, appended AFTER #133's job. `allow-*` markers for the `block-destructive.sh` guard + tests + `Projects/[Project]/` schema. Warn-mode-initial. |
| 4 | **#228** | deploy.sh Check 49 + new tool | Check 49 + new `core/deploy/tools/check-convention.sh` covering 4 residual dims (naming / Layer-2 path / evidence-label / placeholder). Delegated dims documented in-tool. Warn-mode-initial. NOT a pre-commit hook. |
| 5 | **#757** | hardening | (a) document the skill-count recall bound as accepted-residual; (b) source `minwords` from shared `$MIN_SELFDESCRIBE_WORDS`; (c) numeric-validate the `ISSUEREF-POS` fixture coords. Gates stay warn-mode. |

## Ratified design forks (operator-approved 2026-06-29, per #2499)

| Card | Fork | Reversibility |
|---|---|---|
| **#133** | regex table-parse · asymmetric (FAIL unresolvable claimed path / WARN unmapped on-disk file) · new job on `repo-integrity.yml` · warn-mode + self-test fixture | MODERATE · MEDIUM |
| **#135** | `allow-*` markers (block-destructive.sh guard+tests, `Projects/[Project]/` schema) · clean-first-then-gate (6 genuine hits reconciled; 2 historical changelog entries untouched) | MODERATE · HIGH |
| **#228** | 4 residual dims only (naming / layer-2 / evidence-label / placeholder) · extend `deploy.sh` check family (Check 49) + new `check-convention.sh` · NOT a pre-commit hook | MODERATE · MEDIUM |
| **#757** | accept-as-residual recall bound · source `minwords` from `$MIN_SELFDESCRIBE_WORDS` · numeric-validate `ISSUEREF-POS` fixture | CHEAP · HIGH |

## Contention Map

| Shared file | Issues | Class | Resolution |
|---|---|---|---|
| **`.github/workflows/repo-integrity.yml`** | **#133 + #135 commit-2** | SOFT (appended job blocks) | #133 appends job A (governance-file-map) FIRST; #135 commit-2 appends job B (casing) AFTER. Distinct job blocks at the end of the `jobs:` map → no textual conflict. The build sequence is the resolution. |

**Non-contentions confirmed:** #228 touches `deploy.sh` + a new tool only; #757 touches `check-skill-count-imp.py` / `reference-durability.yml` / `run-fragile-ref-fixtures.sh` only. No cross-card file overlap beyond the one `repo-integrity.yml` sequencing point.

## Risk Register

| ID | Class | Risk | Reversibility | Owner | Mitigation |
|---|---|---|---|---|---|
| **R1** | Self-trip | The #135 reconcile commit could itself trip the new casing gate. | CHEAP | Build seq | The casing gate scans **net-new added lines** only; the reconcile REMOVES capital-`P` `Projects/_governance/` (it adds `projects/_config/`). No net-new capital-`P` line is introduced → no self-trip. Verified by running the gate logic against the reconcile diff before commit. |
| **R2** | Historical-record | Editing the 2026-03-19 changelog entries would falsify the dated record. | n/a | Build seq | Those 2 lines (`core/governance/OPERATIONS.md` + `operations/OPERATIONS.md` changelog) are **left untouched**. The gate does not flag them (pre-existing, not net-new). |
| **R3** | False-positive | A new warn-mode gate annotates legitimate content (e.g. the `Projects/[Project]/` operator-instance schema, or the `rm -rf Projects/` legacy guard). | CHEAP | Build seq | `<!-- repo-integrity: allow-* -->` markers on the legitimate lines (per #135 prework). All 3 NEW gates ship warn-mode-initial → no PR break during shakedown. |
| **R4** | Same-file | #133 and #135 both append to `repo-integrity.yml`. | CHEAP | Build seq | Sequenced (job A then job B). Single branch, no cross-PR window. |
| **R5** | Detector-correctness | A warn-mode gate that silently mis-parses reads green while catching nothing. | CHEAP | Build seq | #133 ships a `--self-test` fixture with a labeled expected-match that **hard-fails independent of warn-mode** (mirrors `skill-count-imp-check.yml`). The parser's own correctness is gated even while findings are warn-only. |

**Rollback strategy.** One PR / one merge → rollback is a single `git revert -m 1 <merge-sha>`. Every change is additive (new CI jobs, a new tool, a new check, hardening edits, a governance-path reconcile) — no destructive edits, no renames. Reversibility of the merge: **MODERATE** (CI + governance surfaces touched; undo in hours via revert).

## File Change Matrix

`[NEW]` = file created; `[EDIT]` = existing file modified.

- `core/schemas/frontmatter-schema.md` — **#135 [EDIT]** (reconcile `Projects/_governance/` → `projects/_config/`)
- `core/governance/OPERATIONS.md` — **#135 [EDIT]** (reconcile the genuine drift hit; changelog entry left untouched)
- `operations/OPERATIONS.md` — **#135 [EDIT]** (reconcile the genuine drift hit; changelog entry left untouched)
- `release/governance/RELEASE_PROTOCOL.md` — **#135 [EDIT]** (reconcile the File Structure block)
- `operations/skills/project-initiator/SKILL.md` — **#135 [EDIT]** (×2 reconcile)
- `.github/workflows/repo-integrity.yml` — **#133 [EDIT]** (job A: governance-file-map) **then #135 [EDIT]** (job B: capital-`P` casing gate) — sequenced in this order
- `core/hooks/block-destructive.sh` — **#135 [EDIT]** (allow-* marker on the `rm -rf Projects/` guard)
- `core/hooks/tests/block-destructive.test.sh` — **#135 [EDIT]** (allow-* marker on the test)
- `core/deploy/deploy.sh` — **#228 [EDIT]** (Check 49 — platform-convention linter)
- `core/deploy/tools/check-convention.sh` — **#228 [NEW]** (the convention-linter engine)
- `core/deploy/tools/check-skill-count-imp.py` — **#757 [EDIT]** (document recall bound as accepted-residual)
- `.github/workflows/reference-durability.yml` — **#757 [EDIT]** (source `minwords` from shared `$MIN_SELFDESCRIBE_WORDS`)
- `core/hooks/run-fragile-ref-fixtures.sh` — **#757 [EDIT]** (numeric-validate ISSUEREF-POS fixture coords)

```
core/schemas/frontmatter-schema.md
core/governance/OPERATIONS.md
operations/OPERATIONS.md
release/governance/RELEASE_PROTOCOL.md
operations/skills/project-initiator/SKILL.md
.github/workflows/repo-integrity.yml
core/hooks/block-destructive.sh
core/hooks/tests/block-destructive.test.sh
core/deploy/deploy.sh
core/deploy/tools/check-convention.sh
core/deploy/tools/check-skill-count-imp.py
.github/workflows/reference-durability.yml
core/hooks/run-fragile-ref-fixtures.sh
```

## Recommendations

1. **Ship as ONE slice / ONE PR.** The four cards form one governance-drift-canonicalization-CI capability with a real same-file (`repo-integrity.yml`) sequencing edge between #133 and #135. ~12 pts, acceptable band — ship as-is.
2. **All 3 NEW gates ship warn-mode-initial.** Per `core/rules/bypass-mode-readiness.md` Shakedown → Enforce ladder. Flip-to-enforce is a later Stage-13 assessment line item, not in this slice.
3. **#757 hardens, does not flip.** The cross-reference-integrity gates (#112 family) stay warn-mode; #757 only removes the last hand-copied scalar, documents the recall bound, and makes a malformed fixture row fail loud.
4. **D-Version note.** The next-free version is rule-computed (`anchor()` = latest published GitHub Release, next-free claimed at merge / Stage 12). v3.26 is **provisional-display only** — re-versioned up from the original v3.23 at the Stage-9 re-baseline because v3.20–v3.25 are all claimed (v3.20–v3.24 tags + published Releases; v3.25 an in-flight release branch the governed `claim-version.sh` does not yet see, since its `claimed_set()` reads only tags/published/DEPLOYED-rows, not in-flight branch plan files). Active parallel-release work today may shift it further; binding is forward-only at Stage 12. Do not ship version-less to dodge a collision.
5. **Cross-milestone (outbound).** #135 blocks #232 (ms 20-records-management) — sequence-legal, G3-07 PASS per #2499.

## Change Description

> Authored at Engineering (Stage 6) per RELEASE_PROTOCOL.md § Change Description Protocol — operator-facing voice, committed before the PR is marked ready-for-review so it is visible at Stage 9 Plan Review.

**Outcome.** Adds three PR-time / deploy-time governance-drift gates and hardens the existing cross-reference-integrity gate family. After this release, the CI + `deploy.sh` gate family catches governance-file-map drift, capital-`P` `Projects/` casing recurrence, and four residual platform-convention violations at PR-time; the cross-reference-integrity gates are clean for a later flip-to-enforce.

**Issues resolved.** #133 (governance-file-map cross-check), #135 (Projects/ casing reconcile + gate), #228 (platform-convention linter), #757 (pre-flip hardening).

**Key decisions.** (1) #133 parses the existing map table rather than a curated machine-readable block (one source per fact); asymmetric strictness — FAIL on an unresolvable claimed path, WARN on an unmapped on-disk file. (2) #135 reconciles 6 genuine drift hits in their own commit and leaves 2 historical changelog entries untouched. (3) #228 extends the `deploy.sh` check family (Check 49) + a new tool rather than adding a pre-commit hook (CLAUDE.md forbids normalizing `--no-verify`). (4) #757 accepts the skill-count detector recall bound as a documented residual rather than broadening recall (which risks false positives).

**Reversibility.** MODERATE — one PR / one merge; rollback = `git revert -m 1 <merge-sha>`. All changes additive.

**Downstream impact.** #135 unblocks #232 (ms 20-records-management). The 3 NEW gates are warn-mode → no downstream PR will break during the shakedown window.

**Cross-references.** Stage-4 plan: #2499. Per-card prework: #133 / #135 / #228 / #757 (2026-06-27 comments).
