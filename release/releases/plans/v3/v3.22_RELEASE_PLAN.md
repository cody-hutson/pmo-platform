# v3.22 Release Plan — 18-pmbok-coverage-and-project-schema

> Stage 4 (Release Planning) output. Captured as Engineering Commit 0 at release branch bootstrap. Version provisional (next-free over latest v3.21); binds at Stage 12.

## Summary (30 seconds)

Four issues form **one** PMBOK-coverage capability slice for the *Project Data Architecture* project: **#351** (keystone — `deliverable_type` axis on PROJECT.md + founding ADR), **#262** (org-structure + team-roster schema fields), **#206** (five PMBOK/stakeholder templates + Tracker 8/9 schemas + deploy-sync), **#371** (lifecycle-tailoring discipline doc). One real hard-dependency edge governs the whole sequence: **#351 → #262** (both edit `core/schemas/project-schema.md`; #262's V-rules derive off #351's post-V13 tail). Layered build order: **#371, #206 (foundation) → #351 (keystone) → #262 (infrastructure, after #351)**. Ships as **one PR / one merge** (operator size-override ratified; raw 22 pts / effective 29 pts cross-cutting, compensated by Deep Stage-9 review). Release Class **cross-cutting** (confirmed). Rollback = `git revert -m 1` of the single merge. No merge/split change recommended.

## Dependency Graph

Directional edges derived from each issue's `## Dependencies` block, verified against the V-reservation note at `core/schemas/project-schema.md:184` (V13→keystone deliverable-type axis; V14→org/team card):

```
#351 ──blocks──▶ #262        (HARD: same-file schema dep; #262 V-rules append to post-V13 tail)
#371            (no edges — "Dependencies: None")
#206            (no upstream code edge — "Blocked by: —"; absorbs CLOSED #203)
```

- **#351 → #262** — the one load-bearing edge. #351 lands V13 (`deliverable_type`); #262 appends V14 (`org_structure_type`) + V15 (`team_roster`) off the post-V13 live tail. Both issues state this explicitly; the schema:184 reservation note independently corroborates the V13/V14 ordering. **Verified, not assumed.**
- **#371** — zero dependencies (confirmed: body `## Dependencies` = "None"; affected files are a new discipline doc + 3 one-line pointers, none shared with the schema chain).
- **#206** — no upstream *code* dependency. Its only `Blocked by` relation is the now-CLOSED #203 (which it *absorbs*, not waits on). It "reconciles-with" #262 (both join on the one `person_id` anchor — `team_roster` and the Stakeholder Register are distinct projections of one identity) but that is a **consistency constraint, not a build-order edge** — neither writes the other's files.

No cycles. Single critical path: **#351 → #262** (the only chain with >1 node).

## Implementation Sequence

Dependency-ordered + layered per bundle-composition-doctrine §9 (foundation → infrastructure → skill-core → eval), within-layer tie-break priority-desc → issue-number-asc. Labels read live from GitHub.

| Order | Issue | Layer (label) | Size | Why here |
|---|---|---|---|---|
| 1 | **#371** | `layer:foundation` | S (2) | No deps; pure doc + 3 pointers. Foundation-layer, lowest issue-number among the two foundation tickets *with no inbound edge*. Lands first to keep the schema-edit window clean. |
| 2 | **#206** | `layer:foundation` | L (8) | No upstream code dep. Foundation-layer template/registration authoring. Touches OPERATIONS.md (different section than #371) + tracker-schemas + deploy.sh — none of which the schema chain touches. |
| 3 | **#351** | `cluster: architecture` (keystone; effectively foundation/keystone) | L (8) | **Keystone.** Must land V13 on `project-schema.md` + author the founding ADR **before** #262 can derive V14/V15. Gates the one hard edge. |
| 4 | **#262** | `layer:infrastructure` | M (4) | Builds on #351's post-V13 schema tail (V14 `org_structure_type`, V15 `team_roster`). Infrastructure-layer, sequenced strictly **after** #351. |

**Sequencing rationale (decision-discipline Localization Check applied — see Risk Register R1).** The strict bundle-doctrine §9 layering would put both `layer:foundation` tickets (#371, #206) ahead of the `layer:infrastructure` ticket (#262), with the keystone #351 (no explicit layer label, but functionally the schema keystone) ahead of its #262 dependent. The dependency edge #351→#262 is the binding constraint and is honored. #371 and #206 are independent of the schema chain and may, on a single branch, be authored in any order relative to it — but emitting them **first** keeps the `project-schema.md` edit window (#351 then #262, serialized) contiguous and un-interleaved, which is the lower-risk commit ordering. There is **no parallelism within this slice** — it is one branch, one PR (see Recommendations); the sequence is commit order, not concurrent-spoke order.

## Stage Applicability Matrix

Cross-cutting **Stage-5 activation bias = ALL** is honored (recorded in the milestone description). Every issue runs Stages 6–13 as part of the single PR. Stage 5 (Solutioning) is activated for all four; #351 specifically requires a **founding ADR authored in-slice** (Stage-5 design work, per the ADR-sliced-into-hub-spoke discipline). Stages 7–8 apply to all (each issue has verifiable ACs / `deploy.sh --check` / regression checks). The milestone ships as ONE PR, so Stages 9/12/13 are **release-scoped** (run once for the whole slice), not per-issue.

| Issue | S5 Solutioning | S6 Eng | S7 DevTest | S8 QA | S9 Review | S10 Sec | S11 Prep | S12 Execute | S13 Close | Skips + rationale |
|---|---|---|---|---|---|---|---|---|---|---|
| **#351** | Yes (founding ADR in-slice) | Yes | Yes | Yes | Yes (Deep) | Yes | Yes | Yes | Yes | none — keystone, deepest design |
| **#262** | Yes | Yes | Yes | Yes | Yes (Deep) | Yes | Yes | Yes | Yes | none — schema + V-rule + worked example |
| **#206** | Yes | Yes | Yes | Yes | Yes (Deep) | Yes | Yes | Yes | Yes | none — Stage-5 bias=ALL overrides the "mechanical authoring" temptation to skip; templates carry deploy-sync (Check 13) + PII-scan obligations that warrant DevTest/QA + Sec |
| **#371** | Yes (light — tailoring rationale design) | Yes | Yes | Yes | Yes (Deep) | Yes | Yes | Yes | Yes | S5 is light (1-page doc) but **not skipped** — cross-cutting bias=ALL is explicit; CLAUDE.md is a governance surface so the change carries review weight disproportionate to its 2-pt size |

**Justification for zero skips.** Absent the cross-cutting posture, #371 (CHEAP, 1-page doc) and #206's mechanical authoring would be candidate Stage-5 skips. The recorded **Stage-5 activation bias = ALL** (cross-cutting class) overrides that default — every issue gets a Solutioning pass. Stage 10 Security applies because #206 explicitly carries a PII-scan completion condition (templates must contain zero operator-local identifiers) and #371/#351 touch CLAUDE.md (a governance/depersonalization surface). No issue is no-functional-impact, so no Stage 7/8 skip is warranted.

## Contention Map

Files claimed by more than one issue (within-release; single branch so these are **commit-ordering** serialization points, not cross-PR conflicts):

| Shared file | Issues | Class | Resolution |
|---|---|---|---|
| **`core/schemas/project-schema.md`** | **#351 + #262** | **HARD serialization** | #351 lands V13 (`deliverable_type`) **first**; #262 appends V14/V15 off the post-V13 tail. This is the milestone's central write-serialization point. Build #351 → #262, never interleaved. Corroborated by the schema:184 reservation note. |
| **`core/governance/OPERATIONS.md`** | **#206 + #371** | SOFT (different sections) | #206 edits **§ Operational Artifacts** (table at ~`:910`); #371 adds a **new one-line lifecycle pointer** in a different section (~`:970`). Disjoint line ranges → no textual conflict, but order the two edits (any order) so the second edit rebases cleanly on the first's committed state. Single-branch append-pattern; LOW operational risk. |

**Non-contentions explicitly confirmed (so downstream stages don't re-derive):**
- `core/schemas/gate-criteria-spec.md` is touched **only by #351** (conditions G3-05 on `deliverable_type`) — NOT by #206. The milestone-description File Change Matrix correctly attributes it to #351 alone.
- `core/schemas/tracker-schemas.md` (Tracker 8 Stakeholder Register + Tracker 9 RACI) and `core/deploy/deploy.sh` (`TEMPLATE_SYNC_MAP`) are touched **only by #206**.
- #262 touches **only** `project-schema.md` — no other file — so its sole contention is the HARD edge with #351.

**project-schema.md:184 gloss correction (belongs to #262's build, flagged here per task):** the existing V-reservation note glosses V14 as the "org-structure / **delivery-model** / team-roster card." #262 explicitly **drops `delivery_model`** (verified a legacy alias resolving to `delivery_approach`, per `operations/skills/delivery-engine/references/lifecycle-stages.md:371`). When #262 lands V14/V15, it must **correct this gloss** (remove "delivery-model" from the :184 note) so the schema's own coordination note doesn't reference a field the milestone deliberately did not add. This is a #262 in-build edit, recorded so it is not lost.

## Risk Register

| ID | Class | Risk | Reversibility | Owner | Mitigation |
|---|---|---|---|---|---|
| **R1** | Dependency | #262 built/committed before #351 lands V13 → V-rule numbering collision (V14/V15 derived off a stale V12 tail). | MODERATE | Build seq | Enforce sequence #351 → #262. Do not hardcode V-numbers in #262; derive off live post-V13 tail. **Localization Check (decision-discipline M1):** *Platform context* — schema:184 reservation note + both issue bodies pin V13→#351, V14→#262. *Generic heuristic* — "independent schema fields can be authored in parallel." *What invalidates it* — both edit the **same file's V-series** and #262's rule numbers are **defined relative to** #351's tail. *Reconciliation* — heuristic does NOT apply; hard-serialize #351→#262. |
| **R2** | Contention | Concurrent edits to `project-schema.md` (R1's file) or to `OPERATIONS.md` (#206/#371) cause merge friction. | CHEAP | Build seq | Single branch / single PR (no cross-PR window). Order the project-schema edits (#351 then #262) and the OPERATIONS.md edits contiguously. The append-pattern OPERATIONS.md edits are disjoint sections → near-zero conflict. |
| **R3** | Scope | Effective size 29 pts = 16% over the 25-pt ceiling (G3-15 effective-size breach). | n/a (sizing) | Operator | **Accepted + recorded** in the milestone Size Disposition (operator-override 2026-06-29). Compensating control already in posture: Deep Stage-9 Plan Review + Tight engagement density. Confirmed below — not re-litigated. |
| **R4** | Scope/Drift | #206 authors templates against a *stale* live state (e.g., a template already exists, or `tracker-schemas.md` already has Tracker 8/9). | CHEAP | #206 spoke | **Verified live this stage (`HEAD` of worktree):** all five templates ABSENT from `operations/templates/`; `tracker-schemas.md` ends at **Tracker 7** (8/9 absent); OPERATIONS.md § Operational Artifacts registers none of the five. Baseline is clean → author-fresh is correct. Stage-4 A0.5/A0.6 currency gates re-affirm at engineering entry. |
| **R5** | Rollback-complexity | A defect in the merged slice (e.g., a V-rule that breaks existing PROJECT.md validation) requires backing out four issues' worth of additive changes at once. | MODERATE | Operator | All four issues are **additive** (new files + new optional frontmatter fields + new table rows + new V-rules with documented defaults / optional-on-legacy). Each issue's AC includes a regression check against an existing PROJECT.md (V1–V12 + new rules must still pass). Rollback = single `git revert -m 1 <merge-sha>` (one PR / one merge). Low blast radius because nothing is destructive or a rename. |
| **R6** | Dependency (downstream, out-of-slice) | #351 is a keystone that other milestones' R1–R5 downstream-coupling items depend on; a late or reshaped #351 design ripples outward. | MODERATE | Operator | Out of THIS slice's scope (those siblings are other milestones). Noted so Stage-9 review weighs #351's ADR design for downstream durability, not just in-slice correctness. The founding ADR (authored in-slice) is the durable contract those siblings will consume. |

**Rollback strategy (explicit).** The milestone ships as **one PR / one merge** (single-branch topology, per the one-PR-per-milestone discipline + the operator size-override). Rollback is therefore a single `git revert -m 1 <merge-commit-sha>` on `main`, which atomically removes all four issues' additive changes. No partial-rollback path is needed or offered — the slice is the rollback unit. Because every change is additive (no deletions, no renames, no destructive schema edits), revert restores the prior state cleanly; existing PROJECT.md files (which validated under V1–V12) are unaffected by revert since the new V13–V15 rules are optional/defaulted. Reversibility of the merge: **MODERATE** (schema + governance surfaces touched; undo in hours via revert, but consumer-skill expectations may have begun reading the new fields — re-validate consumers post-revert).

## File Change Matrix

Aggregated across the four issues; add/edit/delete intent noted. One path per line below in the fenced block for deterministic downstream extraction. `[NEW]` = file created; `[EDIT]` = existing file modified; `(new-N)` = N-th new file.

**Intent annotations (human-readable):**
- `operations/templates/project-charter-template.md` — **#206 [NEW]**
- `operations/templates/lessons-learned-template.md` — **#206 [NEW]**
- `operations/templates/change-log-template.md` — **#206 [NEW]**
- `operations/templates/stakeholder-register-template.csv` — **#206 [NEW]** (header-only CSV; `decision_owner` + `authority` columns)
- `operations/templates/raci-template.md` — **#206 [NEW]**
- `operations/templates/README.md` — **#206 [EDIT]** (registry rows for all five)
- `core/governance/OPERATIONS.md` — **#206 [EDIT]** (§ Operational Artifacts: five rows) **+ #371 [EDIT]** (new lifecycle-tailoring pointer, different section)
- `core/schemas/tracker-schemas.md` — **#206 [EDIT]** (Tracker 8 Stakeholder Register, Tracker 9 RACI, before § Extensibility)
- `core/deploy/deploy.sh` — **#206 [EDIT]** (`TEMPLATE_SYNC_MAP` entries; Check 13 must pass)
- `core/standards/template-storage.md` — **#206 [EDIT]** (§7)
- `core/schemas/project-schema.md` — **#351 [EDIT]** (`deliverable_type` + V13; orthogonality note) **then #262 [EDIT]** (`org_structure_type` V14, `team_roster` V15; correct the :184 delivery-model gloss; worked example in §6) — **HARD-serialized in this order**
- `release/references/specs/methodology-parameterization-v1.md` — **#351 [EDIT]** (new §5A domain-axis consumption pattern)
- `core/schemas/gate-criteria-spec.md` — **#351 [EDIT]** (condition G3-05 on `deliverable_type`)
- `core/ADRs/ADR-NNN-deliverable-domain-axis.md` — **#351 [NEW]** (founding ADR, authored in-slice, immutable path)
- `<one existing PROJECT.md>` — **#351 [EDIT]** (reflexive cutover: add a `deliverable_type` value as worked migration) — operator-instance/git-ignored; not a repo-tracked path
- `core/disciplines/lifecycle-tailoring.md` — **#371 [NEW]** (1-page discipline doc)
- `CLAUDE.md` — **#371 [EDIT]** (§ Project Lifecycle one-line cross-reference)
- `core/disciplines/README.md` — **#371 [EDIT]** (catalog row, "Lifecycle, concurrency, & safety" table)

```
operations/templates/project-charter-template.md
operations/templates/lessons-learned-template.md
operations/templates/change-log-template.md
operations/templates/stakeholder-register-template.csv
operations/templates/raci-template.md
operations/templates/README.md
core/governance/OPERATIONS.md
core/schemas/tracker-schemas.md
core/deploy/deploy.sh
core/standards/template-storage.md
core/schemas/project-schema.md
release/references/specs/methodology-parameterization-v1.md
core/schemas/gate-criteria-spec.md
core/ADRs/ADR-NNN-deliverable-domain-axis.md
core/disciplines/lifecycle-tailoring.md
CLAUDE.md
core/disciplines/README.md
```

> Note: the single existing PROJECT.md edited by #351 (reflexive-cutover worked migration) lives in the git-ignored operations domain (`projects/…`), is NOT a repo-tracked path, and is deliberately excluded from the machine-readable block above so the path list contains only release-tracked files. Stage 7/8/9 file-divergence checks operate on the 17 tracked paths.

## Recommendations

1. **Confirm: ship as ONE slice / ONE PR — do NOT split.** The operator size-override (raw 22 / effective 29 pts, cross-cutting ×1.3) is ratified and recorded in the milestone Size Disposition. The four issues form one coherent PMBOK-coverage capability with a real #351→#262 schema dependency; capability coherence outweighs the 25-pt target (bundle-composition-doctrine §3 Step 5 operator-judgment override; composition is judgment, not bin-packing). The G3-15 effective-size breach is **accepted**, compensated by the cross-cutting posture's Deep Stage-9 review + Tight engagement density. **No merge/split change recommended — I found no reason to revisit the override.**

2. **Confirm Release Class = cross-cutting (restated, not re-derived).** Trigger **(b)** fires: the File Change Matrix touches **3 named governance surfaces** — `CLAUDE.md` (#371), `core/governance/OPERATIONS.md` (#206 + #371), `core/schemas/gate-criteria-spec.md` (#351). `novel` also fires (#351 founding ADR + new schema field; #371 new discipline doc; #206 new templates + Tracker 8/9 schemas), but **cross-cutting wins** per multi-trigger resolution (cross-cutting > novel). Posture confirmed: engagement density **Tight** · Stage 9 review depth **Deep** · Stage 5 activation bias **ALL** · Stage 13 outcome-window **30-day**. All four are honored in this plan.

3. **D-Version note (rule-computed determination, not an operator gate).** The next-free version for this release is a **rule-computed determination** per the authoritative-version-selection procedure — `anchor()` = the latest published GitHub Release, next-free allocated by the host-adapter's atomic claim, **claimed at merge (Stage 12)**, never reserved early. It is NOT an operator click-gate at Stage 4. A provisional number may be cited in the plan for reference, but binding is forward-only at merge; if a concurrent sibling claims the slot first, this release re-versions up at Stage 12 (routine, not an error). Do not ship version-less to dodge a collision — claim the next free slot (per the no-versionless-as-hedge discipline).

4. **#262 must correct the `project-schema.md:184` delivery-model gloss in-build** (flagged in Contention Map). The reservation note currently names V14 as the "org-structure / **delivery-model** / team-roster card"; #262 deliberately drops `delivery_model` (a confirmed alias for `delivery_approach`). The in-build edit removes "delivery-model" from the :184 note. Recorded so it is not lost at Engineering.

5. **#351 founding ADR is authored in-slice, not pre-written** (per the ADR-sliced-into-hub-spoke discipline). The new `core/ADRs/ADR-NNN-deliverable-domain-axis.md` is the **first** design artifact of #351's build; this plan supplies the spec, not the ADR text. Stage-5 Solutioning for #351 owns its authorship. The ADR is the durable contract the out-of-slice R1–R5 downstream siblings (R6) will consume — Stage-9 Deep review should weigh its design for downstream durability, not only in-slice correctness.

6. **Out-of-scope discoveries (noted, not acted on):**
   - **Naming-debt (pre-existing, OUT of this milestone):** the bare word `domain` is overloaded 4 ways (deliverable-class, artifact-provenance `domain: A|B|C`, content-area `delivery/{domain}`, behavioral-predicate adjective). #351 sidesteps it by using the non-colliding name `deliverable_type` and explicitly flags renaming the shipped A/B/C-provenance + content-area `domain` fields as **separate naming-debt**. Recommend a follow-up intake ticket if not already filed — do not absorb into this slice.
   - **"How delivery is *sourced*" axis** (in-house / vendor-led / managed-service): #262 notes this has no schema home today and is **explicitly NOT in this milestone's scope**. A candidate future field; do not add here.
   - **SIOR escalation upgrade:** #206's `decision_owner` + `authority` columns *graduate* the SIOR Decision-Owner Mapping (`core/standards/sior-escalation-protocol.md:110-114`) from free-text-heuristic to structured resolution. That upgrade is enabled by this slice but is itself downstream consumer work — note for a follow-up, do not pull into #206's build.
