# v3.63 Release Plan — 74-controlled-deployment-environment-and-modes

> Durable release-plan record for milestone `74-controlled-deployment-environment-and-modes` (#164, Release Class `routine`). Transcribes the operator-reviewed Stage-4 plan (sub-task #3132) plus the Stage-4/5-boundary D-Version re-determination. Topology **D-C SINGLE** (one branch / one PR), **D-Concurrency Posture P0 fully-serial**. D-Version **v3.63** — the Stage-4 provisional was **v3.62**, re-versioned **forward** to **v3.63** at the Stage-4/5 boundary when the concurrent release #431 (milestone `31-immutable-adr-system`) claimed `release/v3.62` on origin. Binds atomically at Stage 12 per the repo versioning lineage; forward-only re-version-up on collision. `release/v3.62` is left untouched — it is #431's active work.

---

## Stage 4 Release Planning — 74-controlled-deployment-environment-and-modes

### Summary (30 seconds)
3-survivor routine release (#213 · #214 · #379; ~12 pts, S+S+L; inside the 10–15 acceptable band). **No survivor blocks another** — all three blocked-by edges (#225→#213, #210→#214, #379 none) point to already-CLOSED Stage-definition issues (#210, #225 both CLOSED, verified). Recommended sequence **#213 → #214 → #379** (all skill-core; within-layer tie-break = issue-number-asc; #379 last as the largest and the one that reads the most shared files). **SINGLE-branch topology** + **P0 fully-serial** Stage-6 posture recommended (3 low-contention doc/skill cards). **One true contention hotspot** — `stage-09-plan-review.md` (edited by both #214 and #379-F3); two secondary append-overlaps. **D-Version provisional = v3.63** (re-versioned forward from v3.62 at the Stage-4/5 boundary — see D-Version below). **Quota verdict PASS.** Two operator decisions at the plan gate: (1) **#379-F1 slice recommendation** (slice F1 as its own Stage-6 commit-group / candidate standalone deliverable), (2) **#214 host is Solutioning-determined and the designated host (`pmo-release-manager`) is not yet built** — the host-SKILL.md edit target is undetermined at plan time (scope-fork risk R-4).

### Dependency Graph
Directional edges (native `blocked-by` + release-plan graph). **Verified: no survivor→survivor edge.**

```
#225 (Stage-12 defn) ──blocks──▶ #213      [#225 CLOSED — edge satisfied, clears]
#210 (Stage-9 defn)  ──blocks──▶ #214      [#210 CLOSED — edge satisfied, clears]
#379                  (no blocked-by edge — "None blocking" per card, confirmed)

Survivor-to-survivor edges: NONE.
  #213 ⟂ #214 ⟂ #379  (mutually independent for correctness ordering)
```
Evidence: `gh issue view 210/225 --json state` → both CLOSED; #379 body "Dependencies: None blocking" + empty blocked-by JSON. The three survivors are therefore orderable purely by convention (skill-core layer → issue-number-asc), not by a hard dependency chain.

### Implementation Sequence
All three are skill-core (SKILL.md + pipeline-doc edits); no intra-slice dependency forces an order, so tie-break = issue-number-asc, with the L-card placed last to minimize rebase churn against the shared `stage-09` / `stage-12` files it reads:

1. **#213** (size:S) — reconcile `release-executor` Mode A ↔ `stage-12-execute.md` (Delivery Strategy naming + current merge/tag/S-2/chore-PR mechanism; retire stale gap note). Isolated blast radius (executor SKILL + stage-12 + mapping row 12).
2. **#214** (size:S) — add Stage-9 evidence-compilation / Go-No-Go prep skill MODE. Touches `stage-09-plan-review.md` (line-80 gap note + Phase A) + mapping row 9 + host SKILL.md (host TBD at Solutioning).
3. **#379** (size:L) — GitHub-native PR-review surfaces (F1 draft→ready / F2 review-comments→edits / F3 review-decision→advisory Stage-9). Broadest surface; reads/edits `stage-06`, `stage-09`, `stage-12`, `release-process.md`. Sequenced last so #214's `stage-09` edits land first and #379-F3 layers its advisory-input block onto a settled file.

Serialization implication: **#214 before #379** is the load-bearing ordering constraint (shared `stage-09-plan-review.md`); see Contention Map.

### Stage Applicability Matrix
Default = all of stages 5–13 apply. Stages 10/11 are git-native/compressed (N/A as spokes — PR diff IS the dry-run, git history IS the snapshot, per stage-to-skill-mode-mapping rows 10/11). No card qualifies for a Stage 7/8 skip: all three are skill/governance-doc edits with **functional impact** (a skill MODE addition in #214, a skill-body mechanism swap in #213, pipeline-behavior doc changes in #379) — none is a pure typo/cosmetic no-op, so DevTest (7) + QA (8) both apply.

| Issue | S5 Solutioning | S6 Eng | S7 DevTest | S8 QA | S9 PlanRev | S10 DryRun | S11 Snapshot | S12 Execute | S13 Close |
|---|---|---|---|---|---|---|---|---|---|
| #213 | ✅ (host/mechanism reconcile design is trivial-tier) | ✅ | ✅ | ✅ | ✅ | N/A (git-native) | N/A (git-native) | ✅ | ✅ |
| #214 | ✅ (**host-skill selection** — release-planner vs pmo-release-manager — is a genuine S5 design decision) | ✅ | ✅ | ✅ | ✅ | N/A | N/A | ✅ | ✅ |
| #379 | ✅ (**slice-vs-bundle F1** design decision + single-operator no-op degradation design) | ✅ | ✅ | ✅ | ✅ | N/A | N/A | ✅ | ✅ |

Skip justifications (evidence): S10/S11 N/A for all — release is 100% in-repo `.md`/SKILL.md edits, zero non-git deploy target, so git-native compression applies (stage-to-skill-mode-mapping rows 10/11; release-process § Stage Compression). No S7/S8 skip granted — grep confirms each card changes load-bearing content (e.g., #214 adds a new skill mode; #213 swaps the legacy `_governance/Releases/*_RELEASE_PLAN.md` flow for the gh-pr-merge mechanism), so functional-impact verification is required.

### Contention Map
File-level overlaps across the 3 change-specs (drives Stage-6 serialization). Hotspot = a file **two cards both EDIT**.

| File | #213 | #214 | #379 | Verdict |
|---|---|---|---|---|
| `release/skills/release-executor/SKILL.md` | EDIT (Mode A body) | — | — | Single-owner |
| `release/references/pipeline/stage-12-execute.md` | EDIT (retire gap note @ line **301**, not :286 — see R-5) | — | EDIT (F3 cross-facet: confirm draft-PR-cannot-merge note / read-only verify) | **HOTSPOT (soft)** — #213 edits § 9 Gap Summary (line 301); #379 touches the Phase-A merge-flow area. Distinct regions; #213 first, #379 verifies against settled file. |
| `release/references/specs/stage-to-skill-mode-mapping.md` | EDIT (row 12, line 31) | EDIT (row 9, line 28) | — | **APPEND-OVERLAP (LOW)** — distinct table rows 3 lines apart; `overlap_class = append-pattern` (structurally HIGH, operationally LOW per ADR-005). Serialize #213-then-#214 by sequence; no scope split needed. |
| `release/references/pipeline/stage-09-plan-review.md` | — | EDIT (line-80 gap note + Phase A evidence-assembly mode ref) | EDIT (F3 — review-decision states as **advisory** Phase-A/A6.5 input, NOT displacing Tier-3 operator) | **HOTSPOT (true)** — the one file two cards genuinely both edit in substance. **Serialize #214 → #379.** #214 lands the evidence-assembly mode + closes line-80 gap; #379-F3 then adds the advisory review-decision block onto the updated Phase A. |
| `release/references/pipeline/stage-06-engineering.md` | — | — | EDIT (F1 — Phase C draft-state PR creation) | Single-owner (#379). grep confirms zero current `draft` language → net-new addition. |
| `release/governance/release-process.md` | — | — | EDIT (F2 Inter-Stage Feedback Protocol review-comment channel; F1/F3 narrative consistency; preserve single-operator reviewer convention) | Single-owner (#379). |
| host SKILL.md (release-planner *or* pmo-release-manager) | — | EDIT (host TBD — Solutioning) | — | **UNDETERMINED (R-4)** — target file not knowable until S5 picks host; if `pmo-release-manager` (not yet built) is chosen, #214 forks into a new-skill build, not an edit. |

**Serialization order (Stage-6 commit sequence):** #213 → #214 → #379. The binding constraint is the `stage-09-plan-review.md` true hotspot (#214 before #379). The `stage-to-skill-mode-mapping.md` append-overlap and the `stage-12-execute.md` soft overlap are both satisfied by the same sequence.

### File Change Matrix
Machine-readable (one path/line; `EDIT`/`ADD`; owning issue). Host-SKILL path for #214 is parameterized pending S5.

```
EDIT  release/skills/release-executor/SKILL.md                                   #213
EDIT  release/references/pipeline/stage-12-execute.md                            #213   # retire stale gap note @ line 301 (§9 Gap Summary)
EDIT  release/references/specs/stage-to-skill-mode-mapping.md                    #213   # row 12 (line 31) — reaffirm Coverage + Delivery-Strategy consume
EDIT  release/references/pipeline/stage-09-plan-review.md                        #214   # line-80 gap note → closed; Phase A evidence-assembly mode ref
EDIT  release/references/specs/stage-to-skill-mode-mapping.md                    #214   # row 9 (line 28) — name evidence-assembly mode (distinct from GAP-BY-DESIGN decision)
EDIT  <host-skill>/SKILL.md                                                      #214   # host = release-planner OR pmo-release-manager (S5-determined; R-4)
EDIT  release/references/pipeline/stage-06-engineering.md                        #379   # F1 — Phase C draft-state PR creation
EDIT  release/references/pipeline/stage-09-plan-review.md                        #379   # F3 — review-decision states as advisory Stage-9 input
EDIT  release/references/pipeline/stage-12-execute.md                            #379   # F3 cross-facet — draft-PR-cannot-merge constraint (verify/annotate)
EDIT  release/governance/release-process.md                                      #379   # F2 review-comment feedback channel; F1/F3 narrative consistency
```
Shared-file rows (Stage 7/8/9 extraction flags): `stage-to-skill-mode-mapping.md` = {#213,#214} append-overlap; `stage-09-plan-review.md` = {#214,#379} true hotspot; `stage-12-execute.md` = {#213,#379} soft overlap.

### Risk Register
| ID | Class | Risk | Sev | Owner | Mitigation | Reversibility |
|---|---|---|---|---|---|---|
| R-1 | contention | `stage-09-plan-review.md` edited by both #214 (Phase A evidence-assembly + line-80 gap) and #379-F3 (advisory review-decision input) — merge collision if built in parallel | MED | Eng (S6) | Serialize #214 → #379 on the single branch; P0-serial posture makes this automatic (next chip waits for prior commit to land) | CHEAP |
| R-2 | scope | #214 F3 must land as **advisory** input only — a build that lets review-decision state bind/gate the Stage-9 GO would violate the Tier-3 human-only decision (stage-09 § 8) | HIGH | S5 design + S8 QA | S5 spec states non-binding; S8 AC "operator binding preserved" (#379 cross-facet AC already greppable); QA verifies operator-Tier-3 language intact | MODERATE |
| R-3 | scope | #379 is size:L with 3 facets (F1/F2/F3) each with its own AC cluster — bundling all three in one commit-group risks an over-large, hard-to-review change | MED | S5 (slice call) + operator | **Slice F1 as its own Stage-6 commit-group** (candidate standalone deliverable per card); F2/F3 follow. See Recommendations. | CHEAP |
| R-4 | dependency | #214 host skill undetermined at plan time; live architecture (`pmo-devops-sre/SKILL.md:39`) designates a **future `pmo-release-manager`** (NOT yet built) as the go/no-go/evidence-compilation host — if S5 picks it, #214 forks from a SKILL.md-edit into a new-skill build (size + Stage-5/6 effort change) | HIGH | S5 + operator | S5 renders host decision explicitly; if `pmo-release-manager` chosen, re-tier #214 and re-run quota. Card AC offers "release-planner OR release-executor" as fallback hosts that keep it an edit. Flag at plan-approval gate. | MODERATE |
| R-5 | scope (currency) | #213 AC5 cites the stale gap note at `stage-12-execute.md:286`; actual location is **line 301** (§ 9 Gap Summary). AC line-reference is stale (`[CURRENCY-MISMATCH]`) | LOW | S4→S6 | Plan records corrected line (301); Eng edits by content-match ("no Release Manager Mode 2 skill (P3)") not by line number. Non-blocking. | CHEAP |
| R-6 | rollback | All changes are in-repo `.md`/SKILL.md on one release branch; rollback = revert the merge commit | LOW | S12 | Git revert; no data/state migration, no external deploy. `.skill` packages rebuild at release-cut, not per-commit. | CHEAP |
| R-7 | audit-baseline | "Zero open PRs touching the 8 target files" is a point-in-time read; a PR appearing before Stage 12 silently invalidates the no-cross-PR-contention finding | LOW | S9/S12 | Baseline pinned: `origin/main` @ **99a9476** (2026-07-03T09:10 CDT). Re-check open-PR population at Stage 9 A6.5 + Stage 12 A.5 (both already in the pipeline). | CHEAP |

No dependency-class blocking risk (survivors mutually independent). No IRREVERSIBLE-tier item.

### D-Decisions
**D-Version (recorded determination — NOT an operator gate):**
- Stage-4 provisional (anchor `gh api repos/.../releases/latest --jq .tag_name` = v3.61; active major = v3; next-free minor) = **v3.62**.
- **Re-versioned forward to v3.63 at the Stage-4/5 boundary (2026-07-03):** the v3.62 provisional collided with a concurrent release — issue **#431** (milestone `31-immutable-adr-system`) claimed `release/v3.62` (pushed to origin with a Stage-4 plan + a #431 feature commit, active locked worktree). Per `bundle-composition-doctrine` §9.1 parallel-release semantics + §5.3 forward-only renumbering, #164 re-versions **up** to the verified next-free **v3.63** (no origin `release/v3.63` branch, no v3.63 tag at determination time). `release/v3.62` is left untouched — it is #431's active work.
- **Cross-release contention:** NONE. #431 (v3.62) edits `stage-05-solutioning.md` + ADR-governance files; #164 (v3.63) edits stage-06/08/09/12 + the release-manager skills. Disjoint edit sets → the two runs proceed in parallel cleanly.
- **PROVISIONAL version = v3.63** — binds **atomically at Stage 12** (defer-to-claim per repo-host-adapter-versioning), re-checked advisory at Stage 9 A6.5. Re-verified free at Engineering Commit 0. Not claimed until the atomic tag claim.

**D-C Branch Topology:**
- **Recommendation: SINGLE-branch** (one release branch `release/v3.63`, sequential Engineering commits in the #213→#214→#379 order, one PR / one merge).
- Rationale: 3 low-contention doc/skill cards; only one true file hotspot (`stage-09`), fully resolved by serial commit ordering on a single branch — per-issue branches (OPTION-A) would add merge-queue overhead with no contention benefit and forfeit the "milestone = one PR / one merge" invariant. Pairs with **D-Concurrency Posture = P0 fully-serial** (default; opt-in parallelism not warranted).

### Quota Budget
**Checkpoint A (plan-time estimate).**
- **Verdict: PASS.**
- Worst parallel batch: Stages 5 / 7 / 8 each fan out to **3 concurrent spokes** (one per survivor).
- Per-spoke cost (size-bucket heuristic, `[CALIBRATE-AFTER-3]` MEDIUM): #213 S · #214 S · #379 L. Worst batch = 3 spokes with one L → est. well under 50% of a fresh per-account 5-hour usage-window envelope for doc/skill-scoped read-analyze-write spokes (no heavy compute, no large-corpus sweep; each spoke edits ≤4 small `.md` files).
- Estimated cumulative draw (worst batch, conservative fresh-window envelope): **< 50%** → PASS band (proceed parallel; no split needed).
- **Load-bearing gate is NOT this estimate** — the hub's Procedure 2 Step 5.5 (Checkpoint B) re-validates at every wave with PROCEED/SERIALIZE/DEFER/REDUCE-scope; if the operator opens the run mid-window with depleted quota, Checkpoint B may SERIALIZE regardless of this PASS. STAGGER is rate-limit-only, not a window mitigation.

### Recommendations
1. **Slice #379-F1 as its own Stage-6 commit-group / candidate standalone deliverable** (card explicitly flags F1 as the foundation the other facets build on). F1 (draft→ready lifecycle in `stage-06` + `stage-09` + `release-process` narrative) is self-contained and independently valuable; F2/F3 layer on top. Recommend build order within #379: F1 → F3 → F2. This is a Stage-6 commit-group split, **not** a card/milestone split (keeps milestone = one PR).
2. **Do NOT split the milestone.** SINGLE-branch, one PR, sequence #213 → #214 → #379. The bundle is coherent (release-manager Stage-9/Stage-12 modes + the PR-review surfaces feeding the Stage-9 gate) and inside the size band.
3. **Operator decision required at plan-approval gate — #214 host (R-4):** confirm whether Solutioning may target the not-yet-built `pmo-release-manager` (forks #214 into a new-skill build) or must keep #214 an edit to an existing host (`release-planner`, the card's primary candidate — already owns Stage 3/4 plan synthesis and reads the same upstream reports). This changes #214's size and Stage-5/6 effort.
4. **Operator awareness — #379-F3 advisory-only guardrail (R-2, HIGH):** the review-decision-state input must be advisory; it must not gate the Tier-3 GO. Flag to S5/S8 as a hard AC.
5. Correct #213's stale AC line-ref (`stage-12-execute.md:286` → **:301**) in the plan; Eng edits by content-match (R-5).

### Canonical-checklist attestation
Bound to `release/references/pipeline/stage-04-planning.md` Phase checklist. Attestation of each codified Phase step (Rigor-Invariance):
- **Phase A0 (Triage→Design re-review):** No C3 premise rejection surfaced — the 3 survivors are post-Mode-R and re-verified against live state; #213 re-scope already reconciled. No Tier-0.
- **Phase A0 ticket-architecture reconciliation:** Ran per-card vs governing surfaces — found 1 CURRENCY-MISMATCH (#213 AC cites `stage-12-execute.md:286`; actual line 301) → Tier-1 [ADJUST] recorded as R-5; no stale-path/version cites beyond it.
- **Phase A0.5 (AC/substrate currency, G-PL1):** Reconciled — #214 card's "autonomy-tiers.md (incoming v11.02)" is stale (LIVE at `core/specs/autonomy-tiers.md`, noted in card); #214 host-framing stale (pmo-release-manager designation, R-4); #213 line-ref stale (R-5). All routed [ADJUST], none invalidates a premise.
- **Phase A0.6 (pre-plan crisping, G-PL2):** All 3 bodies carry actionable descriptions + files-or-protocols + verifiable ACs (each has a greppable AC block) — PASS, no crisping pre-gate needed.
- **Phase A0.7 (placement forward-check, G-PL3):** SKIP — `git log <base>..origin/main --name-status --find-renames` over this release's window shows no merged structural reorg (no cross-directory R rows, no scope-path D rows); all target files exist at current-main paths. Correct non-ceremony signal.
- **Parallelization-Map currency check:** Milestone #164 predates the standing `## Parallelization Map` convention (carries none by construction) → check suppressed per Standing-applicability clause. This plan's Contention Map supplies the equivalent for Stage 6.
- **Phase A0 currency-decision confidence gate:** Refresh outcome = **no-op** (Mode R already amended the bundle 2026-07-03; corroborated by A0.5/A0.6 agreement) → grounded signal, proceed at existing authorization.
- **Phase A1 (milestone validation):** PASS — milestone has Release Outcome Statement (AFTER/BEFORE present), Release Class routine, 3 issues assigned.
- **Phase A1.5 (domain-best-practice sourcing):** `domain: governance` — File Change Matrix is 100% internal pmo-platform pipeline/skill artifacts → **sourcing-exempt** (`source: N/A — pipeline-internal release`); domain still classified per the mandatory-field rule. Matrix evidence: all 10 rows are `release/**` `.md`/SKILL.md.
- **Phase A2 (dependency-ordered sequencing):** Done — #213→#214→#379 (evidence: deps clear, tie-break issue-asc, L-last).
- **Phase A3 (per-issue change spec):** Done — File Change Matrix above (add/edit per file per issue).
- **Phase A4 (file contention resolution):** Done — Contention Map; within-release hotspot (`stage-09`) resolved by serialization; Cross-PR Overlap Audit = zero open PRs at baseline 99a9476 (append-pattern classification applied to `stage-to-skill-mode-mapping.md` per ADR-005); structural-blast-radius sub-audit = no mover-set (no rename/relocate/delete in scope) → no cross-release Tier-S edge.
- **Phase A5 (release plan assembly):** This record IS the assembled plan (Implementation Sequence · File Change Matrix · Risk Register · Delivery Strategy [SINGLE-branch/P0-serial] · Verification implied by Stage Applicability · Rollback [R-6 git-revert]).
- **Phase A6 (quota-budget pre-check, terminal):** Done — PASS; reads parallel-eligible count (3) from A2 Stage Applicability Matrix + contention from A4.
- **D-Gate set:** D-Version (v3.63 provisional, recorded — re-versioned forward from v3.62) · D-C Topology (SINGLE) · D-Concurrency Posture (P0 serial) — recorded determinations, operator ratifies at plan-approval.
- **Phase B (human):** Deferred to operator at the plan-approval gate (approve/modify/split/hold) — this spoke does not self-approve.

---

## Delivery Strategy (Engineering-consumed)

- **Branch:** `release/v3.63` (single branch, cut from `origin/main` @ `99a9476`).
- **Topology:** D-C SINGLE — one branch, one PR, one merge (milestone = one PR invariant).
- **Concurrency posture:** P0 fully-serial — one Engineering commit-group at a time in the #213 → #214 → #379 sequence; the next chip waits until the prior commit lands on the release branch.
- **Commit sequence:** Engineering Commit 0 (this plan file) → #213 → #214 → #379. Reference issue numbers in commit messages; commit per issue/facet.
- **Merge approach:** `merge` (merge-commit) — consistent with the milestone-one-PR invariant; the release PR merges to `main` at Stage 12 (`gh pr merge --admin` per branch protection).
- **Tag convention:** `v3.63` signed-annotated tag at the merge SHA, claimed atomically at Stage 12 (`claim-version.sh`).
- **S-2 deploy targets:** deployed skill copies / `core/rules/` mirrors for any SKILL.md-body changes (`.skill` packages rebuild at release-cut, not per-commit).
- **Rollback strategy:** `git revert` the merge commit (R-6) — no data/state migration, no external deploy target.
