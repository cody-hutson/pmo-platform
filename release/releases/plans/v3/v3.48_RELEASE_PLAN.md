<!-- repo-integrity: allow-issue-ref -->
<!-- reference-durability: allow-link -->
# v3.48 Release Plan — 105-knowledge-corpus-tail-closeout

> Durable release-plan record for milestone `105-knowledge-corpus-tail-closeout` (#254, Release Class **`novel`** — Deep Stage 9). Transcribes the operator-approved Stage-4 plan (sub-task #2858) + the Stage-5 Collective-Review **SCOPE-LOCK** (#2862). Version **v3.48** provisional (recorded determination; binds atomically at Stage 12 per the repo versioning lineage — anchor = published v3.45). Three near-independent knowledge-corpus tail-closeout residuals shipped as **one branch / one PR / one merge** (D-C SINGLE).

---

## Version determination (recorded — not gated)

- **Provisional version: v3.48.** Anchor = published `v3.45` (highest GitHub release / tag). `v3.46` and `v3.47` are claimed by concurrent in-flight PRs (#2861 `release/v3.46-*`, #2860 `release/v3.47-*`) — the two next-free slots above the anchor are taken, so this release takes **v3.48**. Binds atomically at Stage 12; re-verified next-free at Engineering Commit 0 (this file).
- **ADR numbers:** #930 → **ADR-065** (`core/ADRs/ADR-065-health-rag-band-canonical-home.md`); #1087 → **ADR-066** (`release/ADRs/ADR-066-design-gate-delivery-approach-conditioning.md`). ADR-064 is taken by concurrent PR #2860 (`ADR-064-dual-format-document-model.md`). Re-verified free at Commit 0 via `check-adr-numbers.py` (001..063 contiguous on mainline; 064 on the sibling branch) + a scan of `origin/release/v3.46-*` / `origin/release/v3.47-*` for ADR additions (only v3.47 adds ADR-064). Both ADRs authored **without** a `deciders` field pending #1487/#1488 — a deliberate template exception recorded in each ADR.
- **Branch topology: D-C SINGLE** — one branch `release/v3.48-knowledge-corpus-tail-closeout`, one PR, one merge for all three issues. D-Concurrency Posture P0 (fully serial; parallelism unneeded for three small independent leaves).

---

## Stage 4 Release Planning — 105-knowledge-corpus-tail-closeout (transcribed)

### Summary (30 seconds)

Routine-shaped 3-item knowledge-corpus tail-closeout, re-classified to **`novel`** (Deep Stage 9) at the plan-approval gate (2 new ADR files + 2 D-class design decisions fire all `novel` triggers). The three issues are **near-independent** — their content file-sets are disjoint, so "any order" is VALIDATED for content. The one coupling is a namespace edge: #930 and #1087 each author a new ADR and must claim distinct numbers (resolved by allocation, not sequencing). All three are governance/doc changes: **Stage 7/8 verification is grep/file-state assertion, not runtime test** (retained, not skipped — `domain: governance`).

### Dependency Graph

```
#930  (health-RAG canonical home + watermelon single-source; ADR-065)   [no inbound / no outbound content edges]
#1087 (design-before-slicing gate conditioned on delivery_approach; ADR-066 + G3-18)   [no inbound / no outbound content edges]
#1093 (notes-non-authoritative rule + destructive-action guardrail)     [no inbound / no outbound content edges]

Shared-resource edge (NOT a content dependency):
  #930 ⟺ #1087 : ADR-number allocation namespace (core/ADRs + release/ADRs are one shared number space).
                 Resolution = allocate distinct numbers (ADR-065, ADR-066). No sequencing forced.
```

Content-independent, any content order — CONFIRMED. No file appears in two issues' change-sets.

### Implementation Sequence (one branch, one PR, one merge)

1. **Commit 0** — this plan file.
2. **#930** — ADR-065 records the health-RAG band canonical home (Option A′) + single-source the watermelon *concept* to `watermelon-detection.md` across the anti-pattern-prose files.
3. **#1087** — ADR-066 records the design-before-slicing gate parameterization + add criterion **G3-18** to `gate-criteria-spec.md` conditioning on `delivery_approach`.
4. **#1093** — add a single Universal Preferences bullet to `core/CLAUDE.md.template` (notes-non-authoritative + destructive-action guardrail).

Order is not forced by content (any permutation is safe); built in dep-clean logical order. Single merge gate at Stage 12.

### File Change Matrix (as-built under Option A′)

```
# --- Commit 0 : plan file ---
add  | release/releases/plans/v3.48_RELEASE_PLAN.md
# --- #930 : health-RAG canonical home (Option A′) + watermelon single-source ---
add  | core/ADRs/ADR-065-health-rag-band-canonical-home.md
edit | operations/skills/comms-writer/references/channel-formats.md          # see-also pointer to canonical watermelon home
edit | operations/skills/ppm-agent/references/evidence-quality.md            # see-also pointer to canonical watermelon home
# --- #1087 : design-before-slicing gate parameterized on delivery_approach ---
add  | release/ADRs/ADR-066-design-gate-delivery-approach-conditioning.md
edit | core/schemas/gate-criteria-spec.md                                    # add G3-18 criterion + self-repair row + v1.21 version entry
# --- #1093 : notes non-authoritative + destructive-action guardrail ---
edit | core/CLAUDE.md.template                                               # one Universal Preferences bullet
```

> **Note (Option A′ scope):** the Stage-4 matrix listed a wider `#930` band-ownership cascade (~11 files, an ownership MOVE to `metric-registry.md`). The Stage-5 Collective-Review SCOPE-LOCK selected **Option A′ (lighter)** instead — `channel-formats.md` STAYS the canonical band owner (no ownership move), so `metric-registry.md`'s references + `estimation-standards.md` §7 stay valid and are NOT edited. The cascade collapses to (i) ADR-065 recording the retained-owner decision and (ii) watermelon-concept see-also pointers at the two prose sites that come closest to a second definition.

---

## SCOPE-LOCK (Collective Review) — transcribed authoritative decisions

### ① ADR-065 (#930) scope: **Option A′ (lighter — relax AC-2)**
- `channel-formats.md` **STAYS the canonical health-RAG band owner** (no ownership move).
- **ADR-065 RECORDS** the decision: `channel-formats.md` is canonical owner; `metric-registry.md` remains the INDEX referencing it; AC-2 relaxed from "channel-formats references the home" → "canonical ownership documented + watermelon single-sourced" (per distill-intent — the single-canonical-home intent is served).
- Rationale = **placement-correctness + AC-2** — NOT "break the R1 cycle." A6.5 PR-1 proved no live cycle exists (`channel-formats → metric-registry` = 0 edges; the edge is already one-directional `metric-registry → channel-formats`).
- Watermelon *concept* single-sourced to `watermelon-detection.md` across the anti-pattern-prose files (grep-verifiable: one definition + references, no fork).
- **FM-1** (`estimation-standards.md` §7 SPI band): assessed at Stage 6 — if a genuine FORK, single-source to `channel-formats.md`; if outside the lighter scope, file a follow-on `improvement.yml` (do NOT expand #930 scope).

### ② #1087 → ADR-066 / criterion G3-18
- ADR-066 records the design-before-slicing gate `delivery_approach` conditioning (per the G3-05 domain-axis precedent + §5 Skill Consumption Pattern). Correct altitude (Stage 3→4 Gate 3), G3-05 shape.
- **G3-18** conditions the design-invocation expectation on `delivery_approach`; absent-axis / `Custom` → base behavior UNCHANGED via an **explicit no-op default** (FM-3 — a Stage-8 regression gate `G3-18-absent-axis-noop`).
- **CD-2:** reconcile the criterion's Type-column against G3-05 at Engineering.

### ③ #1093 home: **Keep together in `core/CLAUDE.md.template` Universal Preferences**
- A single Universal Preferences bullet carrying BOTH the non-authoritative-notes principle AND the destructive-action guardrail. Cross-ref #500.

### Confirmed (ratified)
- ADR-065 + ADR-066 authored **WITHOUT** a literal-name `deciders` field (pending #1487/#1488) — deliberate template exception; flagged in each ADR so the omission is not read as a defect.

### Engineering/QA directives
- **FM-3:** the G3-18 absent-axis/`Custom` no-op is a **Stage-8 regression GATE** (`G3-18-absent-axis-noop`), not a recommendation.
- **CD-2:** reconcile the G3-18 Type-column against the G3-05 precedent (`field` vs `validation`).
- **PR body:** SAFE closure phrasing throughout (`mark #N as closed at Stage 13`); **NO** auto-close keywords for #930/#1087/#1093 — they close deliberately at Stage 13.

---

## Risk Register (carried from Stage 4, adjusted for Option A′)

| ID | Class | Risk | Reversibility | Mitigation / Owner |
|---|---|---|---|---|
| **R1** | Design | (Stage-4) band-ownership direction / reference-cycle. | CHEAP | **Resolved at Stage 5:** Option A′ keeps `channel-formats.md` as owner; PR-1 proved no cycle exists. No ownership move. |
| **R2** | Namespace | Two ADRs into one shared number space. | CHEAP | Allocate ADR-065/066 (done); re-verify free at Commit 0; `check-adr-numbers.py` gate at Stage 7/9. |
| **R3** | Regression | #1087 conditions a live Gate-3 criterion on `delivery_approach`; a wiring that changes the absent-axis path regresses the un-declared installed base. | MODERATE | AC-2 makes base-behavior preservation the pass condition; Stage-8 gate `G3-18-absent-axis-noop`; follow the G3-05 precedent (conditioning ADDS a lens, never removes the base check). |
| **R4** | Rollback | Governance/reference edits across ~5 files on one PR. | CHEAP | Single-PR revert: one merge commit; `git revert <merge>` restores all three issues atomically. PR-diff IS the dry-run + snapshot. Owner: Stage 12 / operator-authorized. |
| **R6** | Deploy | #930 edits `references/` under 2 deployed skills (comms-writer, ppm-agent). References-only changes are NOT no-ops. | CHEAP | Stage 12: `./deploy.sh --deploy <touched skill>` + `build-skill-packages.sh <touched skill>` (Check 1 + Check 7 backstop). Enumerate touched skills at Stage 12 from the final matrix. |

---

## Stage Applicability

Default = all Stages 5–13 apply. Verification method for all three is **grep/file-state assertion** (`domain: governance`) — Stages 7/8 DO apply; their method is grep/file-content assertion, never runtime test. Do NOT skip 7/8.

- **#930:** S5 REQUIRED (Option A′ decision recorded in ADR-065) · S7/S8 grep (one watermelon definition + references, no fork; ADR exists).
- **#1087:** S5 REQUIRED (parameterization recorded in ADR-066 before wiring) · S7/S8 grep (criterion references `delivery_approach` + conditions on it; absent-axis path == pre-change).
- **#1093:** S5 REQUIRED — LIGHT (home-selection) · S7/S8 grep (rule present; names the gate; cross-refs #500).

---

*Audit baseline: HEAD `3dd0e0c`, 0 open PRs at planning time. domain_practice: { source: N/A — pipeline-internal release, date: 2026-07-01, domain: governance }. Closure discipline: the three delivery issues (#930/#1087/#1093) are marked closed at Stage 13, not by this PR.*
