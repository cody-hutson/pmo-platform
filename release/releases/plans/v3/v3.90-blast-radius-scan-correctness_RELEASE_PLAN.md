# v3.90 Release Plan — blast-radius-scan-correctness

> Durable release-plan record for milestone `blast-radius-scan-correctness` (#272, Release Class `novel`). Transcribes the operator-approved Stage-4 plan (sub-task #3846) plus the Stage-4/5-boundary D-Version re-determination. Topology **D-C SINGLE** (one branch / one PR / one merge), **D-Concurrency Posture P0 fully-serial**. D-Version **v3.90** — the Stage-4 provisional was **v3.89**, re-versioned **forward** to **v3.90** at the pre–Stage-6 Commit-0 re-verify when the concurrent release milestone #275 `template-system-governance-wave-1` (open PR #3913, `feat/v3.89-…` branch) claimed `v3.89`. Next-free after yielding v3.89 = **v3.90** (verified: no v3.90 branch/tag/plan/log-row). Binds atomically at Stage 12 per the repo versioning lineage; forward-only re-version-up on collision. `v3.89` is left untouched — it is #275's active work.

---

## Stage 4 Release Planning — blast-radius-scan-correctness

**Release:** v3.90 · **Class:** novel (confirmed) · **Topology:** D-C SINGLE (confirmed) · **Milestone #272** · Planning-only (read-only), no branch/edits created at Stage 4.

### Summary (30 seconds)

Five members, one tool. Four edit `release/tools/blast-radius.sh` on **four disjoint regions** (zero line-level overlap); the fifth (#3197) is an observation with **no build artifact** (verify-at-close only). **Sequence `#3300 → #3291 → #675 → #3120` is validated** and strengthened by live evidence: the primary checkout holds **8 worktree repo copies** [SOURCE], so every basename currently appears ~9× — #3291's uniqueness test and #3120's scan base are both *broken until #3300's worktree-exclusion lands first*. **Topology D-C SINGLE holds** — one branch / one PR / one merge (milestone = one PR; same file). Stage-applicability headline: **REDUCE-to-design-note** for the three localized fixes, **FULL Stage 5** for #3120 (net-new mode + merge-gating wire-in + one genuine D-class decision), **SKIP-build / verify-at-close** for #3197. Native `blocked_by`/`blocking` empty for all five [SOURCE]. Capacity: effective **12 pts** — under the 15–25 target band, correctly (coherent single-tool bundle; padding would bin-pack). Top decisions for the operator: the #3120 *extend-vs-new-script* design gate (Stage 5) and the #3120 merge-gate rollout posture.

---

### Dependency Graph

**Native edges:** NONE. `trackedInIssues` (blocked-by) and `trackedIssues` (children) are empty for #3300 / #3291 / #675 / #3120 / #3197 [SOURCE: `gh api graphql` sub-issue probe, 2026-07-24]. Zero cross-milestone edges; the three prior parallel-contention siblings (#259/#238/#260) are all CLOSED and never touched `blast-radius.sh` [SOURCE: milestone Parallelization Map]. No cycle.

**Soft edges (build-sequence + semantic), all rooted at #3300:**

```
#3300 ──(semantic: worktree-excluded scan base)──▶ #3291   [uniqueness test needs a de-duplicated canonical list]
  │
  ├────(semantic: corrected scan base)───────────▶ #3120   [structural mode must scan the canonical tree only]
  │
  ├────(verify base)─────────────────────────────▶ #3197   [worktree-exclusion may resolve the depth-2 hang]
  │
  └────(same-file serialization)─────────────────▶ #675    [position-flexible; no semantic dep]

#3291 ─(same-file serialization)▶ #675 ─(same-file serialization)▶ #3120
```

- **#3300 → #3291 is load-bearing** [SOURCE]: with 8 worktree copies present, `find_first_order`'s basename token (`t2`) matches ~9 duplicate copies of every file, so a *genuinely unique* basename reads as non-unique. Any uniqueness-detection #3291 implements over `SCAN_LIST_FILE` is meaningless until #3300 excludes worktrees. This is a real semantic dependency, not just serialization.
- **#3300 → #3120** [INFERRED]: #3120's structural mode reuses `build_scan_list` + `DEFAULT_EXCLUSIONS`; running it on the pre-#3300 base would flag worktree-copy consumers as false positives.
- **#3300 → #3197** [INFERRED]: depth-2 transitive on a high-fan-in hub node is ~quadratic in scan-set size; a ~9× smaller set is ~81× less depth-2 work, plausibly resolving the hang — hence verify-at-close *after* #3300.
- **#675** has **no semantic dependency** on any sibling — pure same-file serialization; it may be sequenced anywhere after #3300.

### Implementation Sequence

**`#3300 → #3291 → #675 → #3120`, then #3197 verify-at-close at Stage 13.** The milestone's proposed order is **VALIDATED** with one refinement.

1. **#3300** — add `.claude/worktrees/` to `DEFAULT_EXCLUSIONS` (blast-radius.sh:58-64). MUST be first: it is the corrected scan base for #3291, #3120, and #3197. Shrinks the scanned set ~9–10× [SOURCE: 8 live worktree copies].
2. **#3291** — path-scope the basename match (blast-radius.sh:473-558). Consumes #3300's de-duplicated list so uniqueness classification is correct.
3. **#675** — add `export LC_ALL=C` next to the PATH pin (blast-radius.sh:16). *Refinement:* position-flexible (disjoint region, no semantic dep) — the milestone's #3291-then-#675 order is fine but not load-bearing; it could equally precede #3291.
4. **#3120** — structural/path-move mode (blast-radius.sh arg-parser + new function) on the corrected scan base, **then** the gate wire-in (stage-05-solutioning.md + SR-G5) + protocol doc + regression fixture + ADR.
5. **#3197** — Stage-13 verify-at-close: re-run `--depth=2` on a hub node against the #3300-corrected base; mark #3197 closed at Stage 13 **only if** the hang does not reproduce (promote to a bug if the hang survives).

**File Change Matrix (add/edit/delete intent):**

| File | Intent | Members | Region / note |
|---|---|---|---|
| `release/tools/blast-radius.sh` | EDIT | #3300, #3291, #675, #3120 | 4 **disjoint** regions: L16 (export), L58-64 (exclusions), L473-558 (match tokens), arg-parser+new-fn |
| `release/references/pipeline/stage-05-solutioning.md` | EDIT | #3120 | new Phase A3.3 structural/path-move consumer sweep + SR-G5 row in §7.2 |
| `release/references/protocols/blast-radius-protocol.md` | EDIT | #3120 | document the new structural mode (new §13) |
| `core/ADRs/ADR-090-*.md` + `core/ADRs/README.md` | ADD | #3120 | ADR qualifying ADR-068 (same-scanner→extend; different-scanner→sibling) + index row |
| `release/tools/tests/test_structural_blast_radius.sh` + fixture | ADD | #3120 | reproduces the #230→#3118 miss (flags `automated-closeout.sh` `RELEASE_NOTES_DIR`, deploy.sh Check 47, `check-release-body-drift.sh`) |
| — (no file) | VERIFY | #3197 | verify-at-close; no artifact |

### Stage Applicability Matrix

Legend: **FULL** / **REDUCE** (design-note only) / **APPLY** / **SKIP** / **rel** (release-level, applies once to the bundle).

| Issue | S5 Solutioning | S6 Eng | S7 DevTest | S8 QA/Accept | S9 Review | S10–12 | S13 Close |
|---|---|---|---|---|---|---|---|
| **#3300** | REDUCE — 1-line exclusion; design-note states the added prefix | APPLY | APPLY | APPLY | rel (Deep) | rel | APPLY |
| **#3291** | REDUCE-plus — design-note **states the uniqueness-detection method** (adaptive uniqueness pre-count) | APPLY | APPLY | APPLY | rel (Deep) | rel | APPLY |
| **#675** | REDUCE — 1-line `export`; design-note names covered sorts (:392, :624, redundant :551/:702) | APPLY | APPLY | APPLY | rel (Deep) | rel | APPLY |
| **#3120** | **FULL** — net-new mode, D-class decision, gate merge-block semantics, regression fixture | APPLY | APPLY | APPLY | rel (Deep) | rel | APPLY |
| **#3197** | **SKIP** (no build artifact) | SKIP | SKIP | SKIP | n/a | n/a | **VERIFY-AT-CLOSE** |

- Novel-class **Stage 5 activation bias = ALL** [SOURCE: release-class-taxonomy §Per-Class Mapping] — so even the trivial fixes get a design-note (not a hard skip); the design-note is the REDUCE form, not a full solutioning cycle.
- **(a)** The three localized fixes do NOT need full Stage 5 — REDUCE-to-design-note is correct; #3291 is the richest (a real uniqueness-detection decision) and sits at the REDUCE/FULL boundary → design-note that *names the method*.
- **(b)** #3120 needs **full Stage 5** — merge-gating wire-in + protocol/SR-G5 edits + the extend-vs-new-script D-decision.
- **(c)** #3197 is **verify-at-close only** — no Stage 5/6/7/8 sub-tasks; its sole activity is the Stage-13 re-check against #3300's corrected base.

### Contention Map

**Four edits, one file, four disjoint regions — serialization is process-mandated, not conflict-forced.**

| Region of `blast-radius.sh` | Owner | Overlap with siblings |
|---|---|---|
| L16 — PATH pin (`export LC_ALL=C`) | #675 | none |
| L58-64 — `DEFAULT_EXCLUSIONS` array (add entry) | #3300 | #3120 *reads* the array (no edit) → no write-contention |
| L473-558 — `find_first_order` T1/T2/T3 tokens | #3291 | none (#3120 adds a new function, not a token edit) |
| arg-parser + new mode function | #3120 | none |

- **Zero line-level overlap** among the four [SOURCE: line-region read]. They must serialize only because (i) milestone = one PR / one merge and (ii) git can't cleanly land two independent PRs on the same file — **not** because the edits collide.
- #3120's companion files (stage-05, protocol, ADR) are touched by **no other member** → zero contention there.
- #3197 touches no file → zero contention.

### Risk Register

| ID | Risk | Type | Severity / Reversibility | Mitigation |
|---|---|---|---|---|
| **R1** | 4 edits on one file must serialize | Contention | LOW / CHEAP | Single branch, dep-ordered commits; disjoint regions make merges trivial |
| **R2** | #3291 uniqueness base, #3120 scan base, #3197 verify base all require #3300 first; wrong order → **silently wrong** uniqueness/consumer output | Dependency | MODERATE / CHEAP | Strict #3300-first; CIAC-2 & CIAC-3 assert the corrected base; reorder commits if violated |
| **R3** | #3120 merge-gate false-positive could block a legitimate merge | Reversibility/scope | MODERATE / MODERATE (Confidence HIGH) | **update-or-accept per consumer** (not hard-fail); design gate semantics at FULL Stage 5; Deep Stage 9; **shadow→warn→enforce** rollout for the gate criterion |
| **R4** | #3120 may upgrade **M/4 → L/8** if gate-wiring complexity surfaces | Scope | MODERATE / CHEAP | Re-size at Stage 5 exit; milestone already flags the upgrade path |
| **R5** | An edit breaks schema-v1 JSON emit or first_order counts on unique-basename targets | Regression | MODERATE / CHEAP | CIAC-1 + existing `test_domain_blast_radius.sh` regression guard [SOURCE: blast-radius.sh:590] |
| **R6** | #3300 may **mask** (not fix) the depth-2 hang if it has a non-scan-size cause | Verification | LOW / CHEAP | #3197 verify-at-close explicitly re-runs depth-2; promote to bug if it survives (audit-baseline discipline) |
| **R7** | effective 12 pts is under the 15–25 target band | Capacity | not-a-risk | Coherent single-tool bundle; padding would violate composition-is-judgment — accept under-band |

**Rollback strategy:** single PR → `git revert` of the merge commit reverts the whole release (CHEAP). Structure #3120 as **≥2 commits** — (a) mode code + protocol + fixture + ADR, (b) gate wire-in (stage-05 + SR-G5) — so the MODERATE-reversibility gate can be reverted **independently** of the CHEAP mode and the three fixes. Per-issue rollback within the branch is clean (disjoint regions).

### Cross-Issue Acceptance Criteria

Four CIACs declared (five-field entries; release-scoped; graded at Stage 9 QC3.5 on the merged PR).

- [ ] **CIAC-1 (#3300 × #3291 × #675 × #3120 on `blast-radius.sh` schema-v1 emit):** all four edits preserve the schema-v1 JSON envelope and `first_order_count` on a **unique-basename** governance target. *Shared surface:* `lib/schema-v1-emit.sh` first_order[] roll-up. *Method:* `blast-radius.sh --format=json --depth=1 <unique-basename file>` on the branch base vs the final branch; diff `first_order_count` + `jq` key-set; `test_domain_blast_radius.sh` green.
- [ ] **CIAC-2 (#3120 × #3300 on `build_scan_list`/`DEFAULT_EXCLUSIONS`):** #3120's structural mode runs on the #3300-corrected scan base. *Shared surface:* `DEFAULT_EXCLUSIONS` + scan-list. *Method:* run the structural mode in a checkout containing `.claude/worktrees/`; assert **zero** `.claude/worktrees/` paths in the consumer list.
- [ ] **CIAC-3 (#3291 × #3300 on `SCAN_LIST_FILE`):** basename-uniqueness classification operates on the worktree-excluded canonical list. *Shared surface:* `SCAN_LIST_FILE`. *Method:* with a worktree present, run on a genuinely-unique basename target; consumer set equals the path-true set (no basename over-match, no ~9× worktree inflation).
- [ ] **CIAC-4 (#675 × #3120 on sort primitives):** after #3120 adds its mode, **every** `sort` remains reached under the script-level `LC_ALL=C`. *Shared surface:* all byte-order-sensitive sorts. *Method:* `grep -n "sort" blast-radius.sh` — every hit (incl. :392, :624, and any #3120 adds) is under the exported pin; a representative `--depth=1` run under `LANG=en_US.UTF-8` emits no `Illegal byte sequence`.

### Release Class

**`novel` — CONFIRMED** (challenge posture applied; not a rubber-stamp).

- **Trigger evidence** [SOURCE: release-class-taxonomy §Class Enum]: novel *definition* — "first-of-kind protocol… or **structural pattern**; design uncertainty surfaced at Stage 5" — is satisfied by #3120's net-new structural/path-move mode. Novel *trigger (b)* — "≥1 D-class decision in release plan" — fires: the #3120 **extend-`blast-radius.sh`-vs-new-script + gate merge-block semantics** decision is a genuine D-class design gate at Stage 5.
- **Why not `routine`:** the three fixes alone would be routine (trigger (a): all P3/P4 + size:S). Per **multi-trigger resolution** (novel > routine), the highest-ceremony member governs → novel. Matches the milestone rationale.
- **Why not `cross-cutting` (ruled out):** cross-cutting needs ≥3 `pipeline/stage-*.md` files (#3120 touches **1** — stage-05), OR ≥3 of the governance set (touches **≤2**), OR ≥3 in-bundle compositional edges (the bundle is a serialization chain, **<3** distinct compositional edges). None fires.
- **Differentiation posture** [SOURCE: taxonomy Per-Class Mapping]: Engagement density **Standard** · Stage 9 review depth **Deep** · Stage 5 activation bias **ALL** · Stage 13 outcome-window **30-day**.

### Quota Budget

**Verdict: PASS** (wide margin). This is a **serialized single-file release** — the build (Stage 6) is strictly sequential on one branch, so cross-issue parallelism is structurally capped and never approaches the per-account usage-window envelope. The worst parallel batch (Stage 5/7/8) is ≤4 read-only single-file spokes. The load-bearing gate is the hub's per-wave Checkpoint B, not this one-time Checkpoint A estimate.

### Recommendations

1. **CONFIRM topology D-C SINGLE** — one branch / one PR / one merge. Serialization is process-mandated (milestone = one PR) and conflict-free (four disjoint regions). **Do not split #3120 into a separate release.**
2. **But commit-split #3120** — land its blast-radius.sh mode code and its gate wire-in (stage-05 + SR-G5) as **separate commits**, so the MODERATE-reversibility gate is revertable without losing the CHEAP mode or the three fixes.
3. **Sequence `#3300 → #3291 → #675 → #3120`; #3300 MUST be first** (semantic base for #3291/#3120/#3197). #675 is position-flexible.
4. **Stage 5:** FULL for #3120; REDUCE-to-design-note for #3300/#675; **REDUCE-plus for #3291** — its design-note names the uniqueness-detection method (adaptive uniqueness pre-count).
5. **#3120 gate rollout:** adopt **shadow → warn → enforce** (per progressive-rollout-convention) with **update-or-accept per consumer**, so a false-positive can't hard-block a legitimate merge on day one.
6. **#3197:** keep as verify-at-close; re-run `--depth=2` on a hub node against the #3300-corrected base at Stage 13, and mark #3197 closed at Stage 13 only if the hang does not reproduce (promote to a bug if it survives).

**Operator decisions recorded (2 genuine gates — everything else is rule-determined):**
- **D-1 (Stage 5, #3120):** extend `blast-radius.sh` vs. new `structural-blast-radius.sh` sibling. **Decided: EXTEND** — reuse-first (shares scan-list/exclusions/grep) + lands on the corrected scan base; ADR-068's sibling rationale is scanner-specific and does not reach this same-scanner case.
- **D-2 (Stage 5, #3120):** merge-gate hardness — **Decided: SOFT** update-or-accept per consumer (reconcilable), shadow→warn→enforce rollout, given R3.

**Out-of-scope discoveries (noted, not acted on):**
- **line 624 carries a SECOND bare `sort -u`** (`sort -u "$seen_file" -o "$seen_file"` in `compute_second_order`) that the #675 body does not cite — the script-level `export LC_ALL=C` covers it, but #675's AC verification greps **all** sorts (incl. :624), not only :392. [SOURCE: blast-radius.sh:624]
- **`grep -F` substring over-match** (`plan.md` ⊂ `implementation-plan.md`) — a distinct over-match class outside #3291's same-basename scope; candidate next-release hardening (leading-`/` boundary anchor), not folded into #3291.
- **Points-band reconciliation:** milestone cites a "10–15 acceptable band"; injected config target is **15–25**. Effective 12 pts is under the 15–25 target — acceptable here (single-tool coherence); the two band figures should be reconciled in `bundle-composition-doctrine` at some later cycle.

---

## ✅ Decision Recorded — Gate 1: Plan Approval (Stage 4)

**Operator verdict:** **GO** — plan + Release Outcome Statement approved (release-hub Mode O).

- **Sequence:** `#3300 → #3291 → #675 → #3120` (#3300 first is a *semantic* base for #3291/#3120/#3197, verified: 8 live worktree copies → ~9× basename inflation).
- **Topology:** D-C SINGLE — one branch / one PR / one merge (four disjoint-region edits on `blast-radius.sh`).
- **Stage applicability:** REDUCE-to-design-note (#3300/#675), REDUCE-plus (#3291, names uniqueness-detection method), FULL Stage 5 (#3120), SKIP-build / verify-at-close (#3197).
- **Delivery strategy:** commit-split #3120 (mode code vs. merge-gate wire-in) for independent revertability.
- **4 CIACs** graded at Stage 9; Release Class **novel** (Deep Stage 9, ALL Stage-5 bias, 30-day outcome window).
- **Stage 5 D-gate:** D-1 EXTEND, D-2 SOFT (shadow→warn→enforce), #3291 method (a) adaptive uniqueness pre-count, ADR YES (allocate next-free, qualify ADR-068) — all **Approve as recommended**.

**Re-version v3.89 → v3.90** (recorded determination). At the pre–Stage-6 Commit-0 re-verify, v3.89 was found claimed by a live concurrent release — milestone #275 `template-system-governance-wave-1` (open PR #3913, `feat/v3.89-…` branch). Next-free after yielding v3.89 = **v3.90** (verified: no v3.90 branch/tag/plan/log-row). Release identity is now **v3.90-blast-radius-scan-correctness**; scope/design/plan unchanged.

*Reversibility: CHEAP · Confidence: HIGH · release-hub Mode O.*
