<!-- reference-durability: allow-link -->
# v3.30 Release Plan — 88-skill-anchoring-software

| Field | Value |
|---|---|
| **Milestone** | `88-skill-anchoring-software` (#231) |
| **Provisional version** | v3.30 (next-free; claimed atomically at Stage 12) |
| **Release Class** | routine |
| **Branch topology** | D-C SINGLE — one release branch, one PR, one merge |
| **Plan authored** | 2026-06-30 |
| **Orchestrator** | release-hub Mode O |

> **Re-version note (§5.3 forward-only renumber):** provisionally planned as v3.29; a concurrent release claimed v3.29 mid-build, so this release re-versioned **up** to v3.30 per `bundle-composition-doctrine` §9.1 (re-version-up is the expected runtime behavior under parallel work, not an error). The branch name retains the original `v3.29` slug (cosmetic only — the version binds at the Stage-12 tag, not the branch name).

## Release Outcome Statement

**AFTER** — The five software-domain specialist skills (pmo-architect, pmo-principal-engineer, pmo-software-engineer, pmo-devops-sre, pmo-technical-program-manager) each cite `domain-best-practices/software.md` as their authoritative practice anchor, and the build-philosophy coverage matrix's BP×Skills cells reflect that reality.

**BEFORE** — The software-domain specialists assert practices ad-hoc with no shared best-practice anchor, and the coverage matrix claims anchoring that does not exist in the skill files.

**Success Indicator** — Each of the 5 SKILL.md files references `domain-best-practices/software.md`; the coverage matrix shows no BP×Skills mismatch for the software row.

## Summary (30 seconds)

Routine, mechanically-identical reference-anchoring across 5 sibling specialist skills plus a coverage-matrix reconciliation. 12 pts / 6 issues. Compose-by-reference only (ADR-019) — pointers, no content absorption. Single shared write-surface (`software.md` `consumers:`) forces serial execution; built inline on one branch.

## Dependency Graph

```
#2165 ┐
#2167 ┤
#2169 ┼─ (5 anchors; all append software.md consumers — shared write → serial) ─→ #2209 (matrix, LAST)
#2170 ┤
#2171 ┘ (dual-domain: also edits governance.md consumers)
```

Acyclic. The matrix reconciliation (#2209) depends on all 5 anchors landing first.

## Implementation Sequence

1. #2165 pmo-architect → software.md (append `## Reference docs` bullet)
2. #2167 pmo-principal-engineer → software.md (create `## Reference docs`)
3. #2169 pmo-software-engineer → software.md (create `## Reference docs`)
4. #2170 pmo-devops-sre → software.md (create `## Reference docs`)
5. #2171 pmo-technical-program-manager → software.md **+ governance.md** (dual-domain `## Reference docs`)
6. #2209 reconcile build-philosophy BP×Skills cells (LAST — verifies the anchoring)

## Stage Applicability Matrix

| Issue | St5 | St6 | St7 | St8 | St9 | St12 | St13 |
|---|---|---|---|---|---|---|---|
| #2165–#2171 (5 anchors) | run | done | done | done | gate | gate | pending |
| #2209 (matrix) | run | done | done | done | gate | gate | pending |

Stage 5 run at operator request. Stage 7/8 run, not collapsed into CI.

## File Contention Map

| File | Touched by | Class |
|---|---|---|
| `core/standards/domain-best-practices/software.md` `consumers:` | all 5 anchors | shared → **serialize** |
| `core/standards/domain-best-practices/governance.md` `consumers:` | #2171 + sibling governance-anchoring milestone | **cross-milestone shared** (R2) |
| each `*/SKILL.md` | its own issue | no intra-milestone contention |
| `core/disciplines/build-philosophy.md` | #2209 | none |

## File Change Matrix

```
release/skills/pmo-architect/SKILL.md
release/skills/pmo-principal-engineer/SKILL.md
release/skills/pmo-software-engineer/SKILL.md
release/skills/pmo-devops-sre/SKILL.md
operations/skills/pmo-technical-program-manager/SKILL.md
core/standards/domain-best-practices/software.md
core/standards/domain-best-practices/governance.md
core/disciplines/build-philosophy.md
release/releases/plans/v3.30_RELEASE_PLAN.md
```

## Risk Register

| # | Risk | Reversibility | Mitigation |
|---|---|---|---|
| R1 | `software.md` consumers serial-write contention (intra-milestone) | CHEAP | serial inline execution |
| R2 | `governance.md` consumers shared with sibling governance-anchoring milestone (#2171) | MODERATE | merge-order + Stage-9/12 divergence checks resolve; does not block |
| R3 | `.skill` packages drift until rebuilt at release cut | CHEAP | rebuild packages at Stage 12; `deploy.sh --check` flags until then (expected; pre-existing baseline drift on untouched skills also present) |
| R4 | `consumers:` is a prose string, not a list | CHEAP | extend prose (surgical least-invasive); no restructure |
| R5 | parallel-release version contention (active pipeline) | CHEAP | re-versioned v3.29→v3.30; re-verify next-free at Stage 12 atomic claim |
| — | rollback | CHEAP | all edits additive; revert = revert the PR |

## Operator Decisions

**D-Version:** v3.30 — next-free (v3.29 claimed by a concurrent release; re-versioned up per §5.3). Recorded determination, not a gate; re-verified + claimed atomically at Stage 12.

**D-C Branch Topology:** SINGLE — rule-determined (default + milestone-one-PR). Recorded.

**D-ReleaseClass:** routine — pre-declared at bundle time; mechanically-identical reference edits, low ceremony.

## Recommendations

1. **DD-4 follow-up (auto-log at close):** after the sibling governance/process anchoring lands, no issue yet exists to flip the matrix governance/process Skills cells from `(skill-wiring pending)` → wired. File a follow-up to close that loop.
2. **R2 coordination:** if the sibling governance-anchoring milestone runs concurrently, merge this one first to avoid `governance.md` consumers contention.
3. **CI coverage:** only CodeQL + Analyze produced check-runs on the PR; the PR-time governance gates did not fire. Equivalent assurance provided by local Stage 7/8 + `deploy.sh --check`. Flag the PR-gate-trigger gap as a follow-up observation.
