# v3.32 Release Plan — 64-hub-autonomy-conformance

<!-- repo-integrity: allow-memory-ref -->
<!-- Deliberately-documented exception: this plan names the operator-local memory file
     `feedback_complete_full_phase_scope.md` as the exact file Stage 13 evicts (the
     codification-complete signal per the milestone Success Indicator) — a legitimate
     close-out reference, not a private-store pointer to durable knowledge. -->

> **Version determination (re-base re-verification, 2026-06-30):** `v3.32` — re-verified next-free after four concurrent releases shipped (4th re-target). Anchor = latest GitHub release `v3.31` [SOURCE: `gh release list` → v3.31 Latest]; bump-class = **minor** (additive stage-spec prose + new enforcement surfaces, no breaking change); floor → next-free = **v3.32** (no `v3.32` tag or release exists, incl. draft). Version lineage: the Stage-4 plan recorded a provisional **v3.28**, re-targeted to **v3.29** during the run; a concurrent release (#2585, engineering-parallelism postures) then merged to `main` and **shipped v3.29** first; a second concurrent release (#2596, software-domain skill anchoring) then **shipped v3.30**; two further concurrent releases then **shipped v3.30→v3.31** ahead of this one, so this milestone re-targets again — fourth re-target — to **v3.32**. **v3.28, v3.29, v3.30, and v3.31 are SUPERSEDED for this milestone — v3.32 is authoritative.** Re-verification rungs: this re-base (done) + the Stage 12 atomic ref-claim.
>
> **Release Class (proposed — operator renders at D-ReleaseClass):** `novel`. See § Recommendations.
> **Branch topology:** D-C **SINGLE** (default) — one release branch `release/v3.29` (the PR #2589 head ref, retained across the v3.32 re-target), one PR, one merge gate; the two issues are delivery slices built in dep order. Plan file = Engineering Commit 0. [SOURCE: hub handoff + hub-spoke-bridge.md Procedure 0 § Canonical location]

## Summary (30 seconds)

Two composing slices on one release branch. **#381 (CODIFY, `size:S`)** lands one rule — the *full-phase-scope* discipline — into the `release-process.md` stage-routing surface (a directed phase runs all gates for every in-scope issue to the phase boundary, closing deferred-conditional steps when a transient blocker clears). **#377 (ENFORCE, `size:M`)** adds the R1 pre-gate forcing function, R2 persona hardening, R3 dedup guard, and R4 unauthorized-gate metric so the hub *honors* the autonomy rules at runtime. **Dep edge: #381 → #377** — the forcing function and persona text should reference the codified `release-process.md` prose, so #381's codification commits before #377's enforcement. They **compose, do not merge** (CODIFY surface ≠ ENFORCE surface; confirmed, not challenged). Total **6 pts** — below the 10-pt floor, operator size-floor override already on record in the milestone description; not re-litigated. **No file contention** between the two slices (disjoint file sets). Both are doc/spec changes; **Stage 5 applies** to both (this is the `novel`/conformance-design surface), **Stage 7/8 apply** under the no-functional-impact test (the forcing function + dedup-guard are behavior-changing protocol, and the codified rule is verifiable by grep — both warrant DevTest/QA, justified below). **Quota Budget: PASS** — worst parallel batch is 2 spokes (Stage 5/7/8), well under the 5-hour-window 50% band.

## Scope-Lock — adopted refinements (Collective Review, 2026-06-30)

The operator locked the design scope at Collective Review. Five adversarial-review refinements are adopted into this release (they modify the Stage-5 designs as authored):

- **CDF-1** — literal **Gate 0** eligibility precondition (NO renumber of gates 1–5; strictly dominant — 0 ordinal edits vs 7). Existing "gate 3/4/5" citations stay valid.
- **FMF-1** — disambiguate the eligibility token: **"Autonomy Tier 2/3"** (not bare "Tier 2/3") + one clause distinguishing it from Inter-Stage-Feedback Tier 2 (scope change → escalate), per `core/specs/autonomy-tiers.md`.
- **FMF-2** — reversibility-gate the pre-execution notice for MODERATE+ reversibility actions (the hub emits the notice even in warn-mode for those).
- **FMF-3** — R3 dedup key on `(decision_type, option_set_hash)`, not `source_sub_task`.
- **CDF-3** — single-source the reserved-touchpoint list (#381 cites #377's reserved list; no parallel copy).

**Follow-on (NOT built this release):** CDF-2 — Gate-0 machine-backstop residual → tracked under #172 (eval automation).

## Dependency Graph

```
#381 (CODIFY full-phase-scope → release-process.md)
   │  produces the codified stage-spec prose that …
   ▼
#377 (ENFORCE: R1 forcing-function + R2 persona + R3 dedup-guard + R4 metric)
   │  R1/R2 reference the codified rule text
   ▼
[merge gate — single PR]
```

- **Directional edge:** `#381 → #377` (`BLOCKS` / FS+0d). Basis: #377's R1 pre-gate forcing function "should reference codified stage-spec text" [SOURCE: hub handoff dep-order note + #377 DoR]. The edge is **soft-within-one-branch** — both ship in the same PR; #381's commit(s) land before #377's so the enforcement prose can cite live codified text. [INFERRED]
- **No external blockers.** The OUT-of-scope shipped siblings are CLOSED, not blockers: **#165** (touchpoint phase-out schema) CLOSED, **#164** (shadow→warn→enforce convention) CLOSED, **#581** (Stage-4 failure-mode-coverage) CLOSED. #377 R4 *conforms to* #165's shipped schema and *rides* #164's shipped posture — a consume-not-block relationship. [SOURCE: #377 Mode-R re-review block]
- **Epic parent #1189** OPEN (PIPE-Telemetry-Audit) — the umbrella; not a release blocker.
- **Substrate dependencies for #377 all LIVE (refine-not-build):** R3 → `core/standards/hub-session-continuity.md` § 3.1 Surface A `pending→resolved` queue; R4 → `core/schemas/touchpoint-phaseout-schema.md` + `core/standards/progressive-rollout-convention.md`; R2 → `release/references/specs/release-personas.md` Stage 4 + Stage 9 cards.

## Implementation Sequence

Single release branch `release/v3.29` (PR #2589 head ref; version re-targeted to v3.32), D-C SINGLE. Build order:

1. **Engineering Commit 0** — re-verify version (confirm v3.32 next-free) and write `release/releases/plans/v3.32_RELEASE_PLAN.md` as the first commit. [SOURCE: hub-spoke-bridge.md Procedure 0 § Canonical location]
2. **#381 (CODIFY)** — codify the full-phase-scope rule into `release/governance/release-process.md` (generalizing the Stage-2 Phase-A form at `stage-02-triage.md`); cite `core/specs/autonomy-tiers.md` for self-containment. Lands first so #377 can reference it.
3. **#377 (ENFORCE)** — R1 Gate-0 forcing-function precondition into `release/references/how-to/hub-spoke-bridge.md` (+ hub persona), R2 anti-patterns into `release/references/specs/release-personas.md`, R3 dedup-guard rule against the existing Surface A substrate (`hub-session-continuity.md`), R4 unauthorized-gate metric into `decision-discipline.md § 6` (conforming to the #165 schema + #164 posture). ADR-053 committed.
4. **Memory eviction (#381 AC):** delete `feedback_complete_full_phase_scope.md` from the operator memory store after merge — the codification-complete signal (operator-instance Layer-2 action; not a repo commit).

> Per-issue closure phrasing (SAFE): mark **#381** and **#377** as closed at Stage 13 via the single release PR / chore-PR close-out — NOT via close-family verbs in the plan prose.

## Stage Applicability Matrix

| Stage | #381 (CODIFY full-phase-scope) | #377 (ENFORCE forcing-function/persona/dedup/metric) |
|---|---|---|
| **5 Solutioning** | **APPLY** — placement + generalization design. | **APPLY** — R1 forcing-function shape, R3 dedup semantics, R4 schema-conformance; ADR-053. |
| **6 Engineering** | **APPLY** — write the codified prose. | **APPLY** — write the four R-components + ADR. |
| **7 DevTest** | **APPLY** — grep-verify the rule is present + reads coherently against live `release-process.md`. | **APPLY** — R1–R4 change hub behavior; functional impact is real. |
| **8 QA** | **APPLY** — confirm codified rule meets AC; eviction is the ship-signal. | **APPLY** — audit R1–R4 against testable ACs. |
| **9 Plan Review** | **APPLY** — Deep (novel posture). Single PR review covers both slices. | **APPLY** — Deep. PR-diff conformance against the File Change Matrix. |
| **10–11** | APPLY (mechanical) | APPLY (mechanical) |
| **12 Execute** | **APPLY** — single merge + atomic version claim (v3.32). | **APPLY** — same single merge. |
| **13 Close** | **APPLY** — mark #381 closed; memory eviction is the codification-complete proof. | **APPLY** — mark #377 closed; first post-cutover release reports the R4 unauthorized-gate count (target 0). |

**No-functional-impact test result:** NEITHER issue passes the no-functional-impact skip test for 7/8. #377 is overtly behavior-changing at the hub decision surface. #381, though doc-only, carries a behavioral *claim* whose verification is a grep + coherence read — rigor-invariance keeps Stage 7/8 as real executed stages.

## Contention Map

**Zero within-release file contention between #381 and #377.** Disjoint file sets:

```
#381 edits:
release/governance/release-process.md
release/references/pipeline/stage-02-triage.md   (one back-reference line)

#377 edits:
release/references/how-to/hub-spoke-bridge.md
release/references/specs/release-personas.md
core/standards/hub-session-continuity.md
core/disciplines/decision-discipline.md
core/ADRs/ADR-053-pre-gate-eligibility-forcing-function.md   (new)
```

- The two issues touch **no common file** → no sequencing/scope-split needed for contention reasons. Build order (#381 before #377) is driven by the **dependency edge**, not file contention.
- **No file-mover** (no rename/relocate/delete) in either slice → no structural serialization edge.

## Risk Register

| ID | Risk | Tier | Reversibility | Mitigation |
|---|---|---|---|---|
| R1 | **Dependency-order violation** — #377's R1/R2 prose authored before #381's codified text exists. | Dependency | CHEAP | Build #381 first on the branch (step 2 before step 3). Stage-9 PR review confirms #377's forcing-function/persona prose cites live `release-process.md` text. |
| R2 | **Codification/enforcement boundary blur** — #381 drifts into stating *enforcement* or #377 re-states the *rule*. | Scope | CHEAP | Hard cleave: #381 = the rule's text; #377 = the forcing function that points at it (and, per CDF-3, #381 cites #377's reserved-touchpoint list rather than copying it). |
| R3 | **R4 metric schema divergence** — designed standalone instead of conforming to #165 schema / #164 posture. | Dependency | CHEAP–MODERATE | R4 conforms to `touchpoint-phaseout-schema.md` + rides `progressive-rollout-convention.md`. |
| R4 | **R3 dedup-guard builds NEW state** instead of refining the existing Surface A queue. | Scope | CHEAP | R3 states the no-re-surface rule against the EXISTING `hub-session-continuity.md` § 3.1 substrate. No new file/format. |
| R5 | **Memory-eviction missed at Close** — `feedback_complete_full_phase_scope.md` survives merge. | Rollback/closure | CHEAP | Stage 13 close-out includes the eviction as an explicit close step (operator-instance Layer-2 action). |
| R6 | **Full-phase-scope rule contradicts the shipped Self-Governance section.** | Scope/quality | CHEAP | Sited as an orthogonal adjacent H3 (routing vs execution-disposition); Stage 7 read-through checks for contradiction. The rule *generalizes* the Stage-2 Phase-A form — additive, not contradictory. |
| R7 | **Cross-file ordinal coupling (RETIRED by CDF-1).** | Quality | CHEAP | CDF-1 adopts literal Gate 0 with NO renumber, so the 7-site "gate 3/4/5" coupling never fires. |
| R8 | **Rollback complexity (LOW).** | Rollback | CHEAP | Whole release is additive stage-spec/persona prose + one ADR + one R4 metric field; `git revert` of the single PR restores the corpus. The only destructive step is the memory deletion (intended end-state; re-addable). |

## Quota Budget

**Verdict:** **PASS** (per `quota-budget-protocol.md` Checkpoint A)
**Parallel-eligible spokes per parallel stage:** Stage 5: **2** · Stage 7: **2** · Stage 8: **2** (one spoke per issue).
**Per-spoke cost estimate:** #381 `size:S` = lowest; #377 `size:M` = low–moderate. Worst batch = lowest + low–moderate.
**Estimated cumulative draw % (worst parallel batch):** worst batch is 2 spokes of S+M ordinal cost — **< 50%** against a conservative full-window envelope. Stage 6 / Stage 13 are write-serialized one-at-a-time and excluded from the parallel-draw estimate.
**Routing:** **PASS — proceed parallel; no warning in the plan.** Checkpoint B re-validates at every parallel wave at runtime.

## Recommendations

1. **Merge/split — keep both separate on one branch, build #381 first.** #381 CODIFIES; #377 ENFORCES. They compose but are not duplicates. Reversibility CHEAP, confidence HIGH.
2. **Release Class — propose `novel`; operator renders at D-ReleaseClass.** Trigger: ≥1 D-class decision (#377's R1 forcing-function design + ADR-053). Differentiation: Engagement density Standard; Stage 9 review Deep; Stage 5 activation ALL; Stage 13 outcome-window 30-day (the R4 unauthorized-gate metric is a post-deploy-window measurement).
3. **Stage 5 ADR — authored (ADR-053).** The R1 pre-gate forcing function is a new MUST-pass precondition at the hub decision surface — an architecture-class decision. ADR-053 records the placement decision (Gate 0 at the head of the Information Sufficiency block).
4. **Out-of-scope discovery — memory-eviction is an operator-instance (Layer 2) action, not a repo deliverable.** Surfaced at the Stage 13 Close briefing as an explicit close step.
5. **Rule-4 placement (#381) treated as shipped/out-of-scope.** Only rule 3 is in #381's trimmed scope; rule 4's empty-population framing already landed. Not reopened.

---

*Engineering Commit 0 — v3.32 release plan authored at Stage 6 from the approved Stage-4 plan (issue #2551), version re-verified next-free. Evidence-quality labels per platform discipline.*
