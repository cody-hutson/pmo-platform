# v3.65 Release Plan — 70-verification-execution-surface

> Version **v3.65** (re-provisioned twice from the Stage-4 provisional v3.62: v3.62 held by 31-immutable-adr-system PR #3193, v3.63 held by controlled-deployment-environment-and-modes PR #3199; v3.64 held by executable-acceptance-testing; **v3.65 verified next-free** on tag/branch/PR axes, 2026-07-03). Claimed on the branch axis at Commit 0 (`release/v3.65-verification-execution-surface`); bound atomically to the version number at Stage-12 merge. This file consolidates the approved Stage-4 plan (#3131) + the Collective Review scope-lock corrections carried into Stage 6.

## Stage 4 Release Planning — v3.65 · 70-verification-execution-surface (milestone #70)

### Summary (30 seconds)

Four cards give the pipeline a **verification *execution* surface** — it exercises deliverables rather than only reviewing their source. All four are protocol/tooling changes on a shared, heavily-overlapping file set (stage-08, release-process, gate-criteria-spec, hub-spoke-bridge, stage-04, release/tools). All four verified **GENUINE-OPEN** against live mainline; #103 is re-scoped (Stage-7 A8 runtime-suite gate + selection-map already shipped, so #103 = Stage-8 surface + one behavioral-AC worked example only). Internal dep graph is acyclic and shallow: **#99 → #170 → #103 → #227** (foundation → schema → surface → executor per bundle-composition-doctrine §9). #103's cross-milestone dep #216 is CLOSED/satisfied; no open cross-milestone leaks internally.

**Delivery posture: SINGLE (P0 fully-serial), one PR / one merge.** The contention map shows all four cards write the same four+ shared files (stage-08-qa-testing.md, release-process.md, gate-criteria-spec.md, hub-spoke-bridge.md, and release/tools/), so parallel Stage-6 execution would collide on every high-traffic governance file. Serial is correct-by-construction here. Stages 5/7/8 spoke *analysis* remains parallel-safe (they read, don't co-write the branch).

**Release Class: `cross-cutting` — CONFIRMED** against three independent triggers: (a) File Change Matrix touches ≥3 `pipeline/stage-*.md` files (stage-04, stage-06, stage-07, stage-08, stage-09); (b) ≥3 of the named governance set (release-process.md + gate-criteria-spec.md + hub-spoke-bridge.md); (c) ≥3 in-bundle compositional edges (#99→#170→#103→#227). Highest-ceremony class governs.

**Accepted risk (recorded, not re-opened):** effective_pts **31** (raw 24 × cross-cutting 1.3) exceeds the 15–25 band ceiling. Operator has elected to ship as ONE release (operator-override; 54-pt Shape-1 precedent). No split/merge recommendation — no NEW evidence contradicts the override (see Risk Register R-1).

### Dependency Graph

Directional edges (X → Y means "X should land before Y"):

```
#99 ──▶ #170 ──▶ #103 ──▶ #227
 │                          ▲
 └──────────(soft)──────────┘
```

| Edge | Type | Evidence |
|---|---|---|
| #99 → #170 | Foundation-first (soft-ordering) | #99 makes invariant re-verification structurally sound; #170's cross-issue ACs sit on a sound verification substrate. Doctrine §9 foundation-first. |
| #170 → #103 | Schema-before-surface (soft-ordering) | #170 defines the Cross-Issue AC schema; #103 extends the Stage-8 execution surface that consumes verification evidence. |
| #170 → #227 | HARD (input dependency) | #227's integration-check step consumes #170's Cross-Issue AC schema as its input. |
| #103 → #227 | HARD (dispatch dependency) | #227's check-dispatch invokes/reads the runtime-suite map that #103 keys Stage-8 to. |

**Graph properties:** acyclic; #227 is the unique sink (shared executor); #99 is the unique source (foundation). Cross-milestone dep #216 (blocked #103) is CLOSED — no open external edges. #170→#227 and #103→#227 are HARD input/dispatch edges (the executor literally consumes the two upstream surfaces) — the load-bearing reason #227 builds last.

### Implementation Sequence

Dependency-ordered execution (single serial branch; one commit-group per card, in order):

1. **#99** — QC4-05/AV-1 structural soundness (foundation). Fixes the mechanism every downstream gate's verdict relies on; do it first so the rest of the release builds on a sound verification substrate. *(Stage 5 REQUIRED — mechanism choice.)*
2. **#170** — Cross-Issue AC schema (Stage-4 output + hub-spoke template + persona marker + integration check + QC checkpoint). Establishes the schema #227 consumes. *(Stage 5 REQUIRED — Option A vs B design decision.)*
3. **#103** — Stage-8 execution surface + behavioral-AC worked example (Stage-7 half already shipped). Establishes the runtime-suite dispatch surface #227 reads. *(Stage 5 RECOMMENDED.)*
4. **#227** — verification-automation script under `release/tools/` (executor). Builds last; consumes #170's Cross-Issue ACs and #103's runtime-suite map. *(Stage 5 RECOMMENDED.)*

Because delivery is SINGLE-serial, this sequence is also the literal commit order on the one release branch. #99 (first) creates the release branch + writes this plan file as Commit 0.

### Stage Applicability Matrix

Default: all of Stages 5–13 apply. All four are protocol/tooling changes with functional/behavioral impact.

| Stage | #99 (bug/L) | #170 (story/L) | #103 (story/M) | #227 (task/M) |
|---|---|---|---|---|
| 5 Solutioning | APPLIES (REQUIRED) | APPLIES (REQUIRED) | APPLIES (recommended) | APPLIES (recommended) |
| 6 Engineering | APPLIES | APPLIES | APPLIES | APPLIES |
| 7 Dev Testing | APPLIES | APPLIES | APPLIES | APPLIES |
| 8 QA Testing | APPLIES | APPLIES | APPLIES (self-referential — grade against pre-change Stage-8) | APPLIES |
| 9 Plan Review | APPLIES (Deep) | APPLIES (Deep) | APPLIES (Deep) | APPLIES (Deep) |
| 10–11 | APPLIES | APPLIES | APPLIES | APPLIES |
| 12 Execute | APPLIES | APPLIES | APPLIES | APPLIES (single merge; v3.65 claimed atomically) |
| 13 Close | APPLIES | APPLIES | APPLIES | APPLIES (QC4-05 self-reference — see reflexive flag) |

Stage-7 A8 correctly no-ops (suite-skip) for all four (none touches a runtime-suite-mapped path) — the honest no-op, not a skip.

### File Change Matrix (machine-readable)

One path per line inside the fenced block. Prefix legend: `E:` edit · `A:` add · `X:` cross-reference-only edit. Trailing `[#NN]` = owning card(s).

```
E: release/governance/release-process.md [#99]
E: release/governance/release-process.md [#170]
E: core/schemas/gate-criteria-spec.md [#170]
E: core/schemas/gate-criteria-spec.md [#103]
E: release/references/pipeline/stage-04-planning.md [#170]
E: release/references/how-to/hub-spoke-bridge.md [#170]
E: release/references/specs/release-personas.md [#170]
E: release/references/pipeline/stage-09-plan-review.md [#170]
E: release/references/pipeline/stage-08-qa-testing.md [#103]
E: release/references/pipeline/stage-08-qa-testing.md [#170]
E: release/references/pipeline/stage-13-close.md [#99]
X: release/references/pipeline/stage-06-engineering.md [#227]
X: release/references/pipeline/stage-07-dev-testing.md [#227]
A: release/tools/av-verify.py [#99]
A: release/tools/tests/test_av_verify.py [#99]
A: release/tools/tests/fixtures/av-comment-disclaimer.md [#99]
A: release/tools/tests/fixtures/av-fenced-info-string.md [#99]
A: release/tools/tests/fixtures/av-fenced-indented.md [#99]
A: release/tools/tests/fixtures/av-fenced-nested.md [#99]
A: release/ADRs/ADR-072-region-scoped-av-invariant-verification.md [#99]
A: release/ADRs/ADR-073-cross-issue-integration-check-stage9-extension.md [#170]
A: release/ADRs/ADR-074-stage8-consumes-runtime-evidence-for-behavioral-ac.md [#103]
A: release/tools/verify-release-plan.sh [#227]
```

**Scope-lock corrections applied to the matrix (vs Stage-4 base):**
- **#99 gate-criteria-spec.md edit DROPPED** — AC-3 resolves to **N/A** (QC4-05's home is release-process.md; no Gate-13 G-CL AV-re-verification criterion exists; a new row would duplicate the release-process.md contract). Verified correct at adversarial review.
- **#99 `release/tools/av-verify.py`** is the NEW verdict helper (region-scoped AV assertion); **NOT `verify-release-plan.sh`** (that is #227's executor, added last).
- **B-1 ADR renumber (build order):** #99 → ADR-072 · #170 → ADR-073 · #103 → ADR-074 · #227 → ADR-075. `check-adr-numbers.py` max = 070 at plan authoring; 071 was next-free then, but concurrent release v3.64 (#218) merged `core/ADRs/ADR-071` (acceptance-assertion-type) to main first, so this release's four ADRs reconciled up by one (071–074 → 072–075) at merge-with-main.
- **#170 Option A (stage-09) chosen** over Option B (new stage-07.5) at Stage 5 (matrix lists stage-09-plan-review.md).

### Verification Plan

Per-card AC → verification-method-class mapping. Method classes mirror G1-05a admissible predicate patterns.

**#99 — QC4-05/AV-1 structural soundness**

| AC | Predicate class | Verification method | Expected result |
|---|---|---|---|
| AC-1 Regression case (token only inside disclaiming comment → CORRECT verdict) | behavioral/domain (pattern d) | run the documented `test_av_verify.py` synthetic fixtures against `av-verify.py` | correct verdict (no false-FAIL on comment-only token; symmetric false-PASS guarded) |
| AC-2 release-process.md QC4-05 mechanism ≠ "grep" | file-path+state (pattern b) | `grep -A1 'QC4-05' release/governance/release-process.md` shows mechanism is NOT "AV-1 grep mechanism" | mechanism string changed at line 567 (region-scoped assertion) |
| AC-3 gate-criteria-spec QC4-05 criterion reflects sound mechanism (if present) | file-path+state (pattern b), conditional | grep the QC4-05 criterion in gate-criteria-spec.md | **N/A** — no Gate-13 AV-re-verification criterion exists; QC4-05 lives in release-process.md (documented) |
| AC-4 existing governance AV-1 invariants still evaluate correctly | **synthetic-fixture regression** (M-2 correction) | run `av-verify.py --region any` on the synthetic legacy-style fixtures → byte-equivalent to whole-file grep | **synthetic** regression PASSes — **there are ZERO formal AV-N rows in any historical release plan** (verified); the mechanism is forward-looking, so AC-4 is a synthetic `region: any`-equivalence proof, NOT a replay of prior AV assertions |

> **M-2 correction (scope-lock):** the Stage-5 draft cited `v3.50_RELEASE_PLAN.md` / `v2.42_RELEASE_PLAN.md` as carriers of literal AV grep rows. **Verified: ZERO formal `AV-<n>` assertion rows exist in ANY release plan** — "AV-1" was a naming convention, never an authored corpus. AC-4 is therefore satisfied by a **synthetic-fixture** `region: any`-equivalence regression (proving the `any` path reproduces whole-file grep byte-for-byte), not by replaying non-existent prior assertions. No fabricated v3.50/v2.42 citations appear in the shipped artifacts.

**#170 — Cross-Issue ACs + integration check** — file-path+state greps on stage-04-planning §6, hub-spoke-bridge Procedure 0, release-personas Stage-4 markers, stage-09 integration-check, release-process QA-Checkpoint-Framework; DEFERRED behavioral AC (first ≥2-issue release post-deploy).

**#103 — Stage-8 execution surface** — stage-08 domain-execution path grep, MAPPED-domain worked example (hooks) co-equal with the unmapped web/component one (m-1), selection-map xref, doc-only regression (suite-skip no-op).

**#227 — verification-automation script** — parses a release plan's Verification Plan, executes parsed checks, output matches §5-C3 template, invocable from Stage 6/7, consumes-not-reimplements #170/#103 surfaces.

### Cross-Issue Acceptance Criteria (dog-food of #170's own convention)

Authored in the canonical CIAC scaffold form (per `release/references/pipeline/stage-04-planning.md` § Cross-Issue Acceptance Criteria) so the executor extracts and runs the method string directly — this section is the dog-food that exercises #170's own schema and is itself executed by the #227 executor.

**Cross-Issue Acceptance Criteria**
- [ ] **CIAC-1 (#227 × #170 × #103 on the verification-execution surface):** the #227 executor reads #170's Cross-Issue Acceptance Criteria section AND emits #103's runtime-suite result through the shared `append-pipeline-event.sh` `test-run` writer, rather than re-implementing either surface. *Method:* `grep -cE 'Cross-Issue Acceptance Criteria|append-pipeline-event.sh' release/tools/verify-release-plan.sh` ≥ 2. *Graded at Stage 9 QC3.5 on the merged PR (Stage-9 Phase A3.6 reads the emitted verdict read-only).*

This is the single highest-value cross-issue constraint in the bundle — authored as a real Cross-Issue AC per #170's new convention (dog-fooding), and its method is a single reproducible command the executor runs. Per **M-3**: #227 is the single CIAC runner (executes at Stage 6/7, emits verdicts); Stage-9 reads the emitted verdicts read-only with an evidence-freshness guard (verdict emitted against final PR SHA; stale → re-run #227), mirroring G-PR9.

### Risk Register

| ID | Risk | Class | Reversibility / Confidence | Owner | Mitigation |
|---|---|---|---|---|---|
| **R-1** | Oversize accepted risk: effective_pts **31** exceeds the 15–25 band ceiling. | scope | MODERATE / HIGH | operator | **ACCEPTED (operator-override; 54-pt Shape-1 precedent).** Not re-opened — no NEW evidence contradicts. Serial Stage-6 + Deep Stage-9 are compensating controls; one-PR/one-merge keeps rollback CHEAP. Do NOT re-propose a split. |
| **R-2** | Contention on shared governance files (release-process.md HIGH; gate-criteria-spec.md MEDIUM). | contention | — | Stage-6 spoke | SINGLE/P0 serial Stage-6. Build #99→#170→#103→#227; edits append cleanly. Force-push prohibited. |
| **R-3** | #99 EXPENSIVE mechanism-change risk — the QC4-05 verdict logic every gate relies on. | dependency / correctness | **EXPENSIVE / MEDIUM** | Stage-5 + Stage-9 | Stage 5 chose region-scoped assertion; the synthetic `region: any`-equivalence regression is the falsification test; Deep Stage-9; interim manual-QC4-05 mitigation stays available; **introducing-release runs QC4-05 as-is** at its own Stage 13 (reflexive-pipeline-loop). |
| **R-4** | #99 AV-1 invariant re-authoring regression. | scope / regression | MODERATE / MEDIUM | Stage-5 + Stage-6 | Enumerated affected AV set at Stage 6 = **effectively empty** (no formal AV-N rows exist); the mechanism governs going-forward assertions; the `region: any` default is byte-equivalent so zero forced churn. |
| **R-5** | #170 A-vs-B design bifurcation. | design | MODERATE / MEDIUM | Stage-5 | Resolved at Stage 5 → **Option A (stage-09)**. |
| **R-6** | #103 self-referential Stage-8 change. | dependency / reflexive | CHEAP / HIGH | Stage-8 spoke | Grade #103 against pre-change Stage-8 semantics (introducing release does not run its own new gate on itself). |
| **R-7** | #227 forked-surface risk. | dependency | CHEAP / HIGH | Stage-6 sequencing | Serial sequence puts #227 LAST; the two HARD edges enforce it; #227 requires consume-not-reimplement. |
| **R-8** | Cross-release soft contention (baseline-pinned). Corrected at hub R1: the only genuine live overlap is **#1632** (OPEN, M`93-governance-ci-gates`) editing gate-criteria-spec G3-05. | contention (cross-release) | MODERATE / MEDIUM | Stage-4 + Stage-9 | Monitor #1632 for merge-to-main during the Engineering/DT window; route to Stage-9 Phase A6.5 / G-PR9 baseline-currency re-check. The three originally-cited siblings (#204/#161/#236) are CLOSED/off-surface — non-risks. |

**Audit-baseline discipline note.** R-8 is pinned to this planning SHA and a bounded window (open sibling milestones as of this run). It is transiently valid — a sibling merging later silently invalidates it — which is why R-8 routes to the Stage-9 A6.5/G-PR9 re-check rather than being treated as settled here.

### D-Version — re-provision amendment (v3.62 → v3.63 → v3.65)

Recorded determination, not an operator gate (FM-6: mechanical next-free is not a gate). Stage-4 provisioned **v3.62**; two Commit-0 collisions were detected at Engineering re-runs:
1. **v3.62 → v3.63:** 31-immutable-adr-system (PR #3193, branch release/v3.62) had materially claimed v3.62. Forward-only re-version-up; 70-verification-execution-surface had 0 commits → re-provision to v3.63.
2. **v3.63 → v3.65:** controlled-deployment-environment-and-modes (PR #3199) claimed v3.63 in the interim; v3.64 held by executable-acceptance-testing (branch release/v3.64-executable-acceptance-testing). Census 2026-07-03: v3.62/v3.63/v3.64 all claimed; **v3.65 next-free** (no tag / no branch / no PR). Standing re-version-up resolution applied.

All design artifacts are version-agnostic — only the release number + the plan-file name move. **v3.65 is authoritative**; any v3.62/v3.63 in older #3131 comments/pointers is superseded. The branch axis is claimed at Commit 0 (`release/v3.65-verification-execution-surface`); the version binds atomically at Stage-12 merge.

### Release Class — `cross-cutting` (CONFIRMED)

Three independent triggers fire (over-determined): (a) 5 `pipeline/stage-*.md` files in the matrix; (b) 3 of the named governance set; (c) ≥3 in-bundle compositional edges. Differentiation posture: Engagement density **Tight**; Stage 9 review depth **Deep**; Stage 5 activation bias **ALL**; Stage 13 outcome-window **30-day**.

### Delivery Strategy / Branch Topology

Single release branch off `main` (`release/v3.65-verification-execution-surface`). This plan file is committed as the FIRST file (Commit 0). All four cards are delivery slices on ONE branch, ONE PR, ONE merge. Build order = the Implementation Sequence. PR-review at Stage 9 IS the dry-run governance gate. **Whole-release rollback** = revert the single squash-merge commit (restores every touched file; deletes the new `release/tools/` scripts) — CHEAP at the release grain. The interim manual-QC4-05 mitigation for #99 remains available as the backout path.

**D-Concurrency Posture: SINGLE (P0 — fully serial Stage-6 execution).** Contention-map-driven (genuine multi-card write contention on release-process.md + gate-criteria-spec.md). Force-push (incl. `--force-with-lease`) on the shared release branch is prohibited. Only Stage-6 *authoring* is serial; Stage 5/7/8 analysis waves stay parallel.

### Reflexive-pipeline self-references (flagged for downstream spokes)

1. **Stage-8:** grade #103 against **pre-change** Stage-8 semantics (R-6) — the introducing release does not run its own new gate on itself.
2. **Stage-13:** this release's own QC4-05 runs the **OLD (as-is grep)** mechanism because #99 is the introducing release of the new region-scoped mechanism (R-3). The new mechanism governs releases entering Stage 13 strictly AFTER this release's merge SHA. Both are standard introducing-release-exempt discipline.

---

*Plan consolidated at Stage 6 Commit 0 from the approved Stage-4 plan (#3131) + Collective Review scope-lock (#3131). Per-issue closure language uses safe phrasing throughout (each card is marked closed at Stage 13, not auto-closed by this plan). Evidence-quality labels applied to grounded claims.*

## Change Description

This release gives the pipeline a verification **execution** surface — it exercises deliverables rather than only reviewing their source. Four coupled cards, one PR, dependency order foundation → schema → surface → executor.

- **#99 (foundation) — ADR-072:** `release/tools/av-verify.py` replaces the unsound grep in QC4-05 with region-scoped AV-invariant re-verification (region ∈ code/comment/any × polarity), making comment-vs-code / negation false verdicts structurally unrepresentable — including full `.md` soundness via fenced-code-block isolation. `release-process.md` QC4-05 mechanism + `stage-13-close.md` Phase A4 wired.
- **#170 (schema) — ADR-073:** release-scoped Cross-Issue Acceptance Criteria (`CIAC-N`), a superset of the shipped `INT-N`, authored at Stage 4 and consumed read-only at a new Stage-9 Phase A3.6 (`QC3.5` / `G-PR10`), with an evidence-freshness guard mirroring G-PR9. QA Checkpoint Framework grows to 5 checkpoints.
- **#103 (surface) — ADR-074:** Stage 8 consumes the Stage-7 A8 `test-run` result as acceptance evidence (no re-run) via the DT→QA handoff, keyed to the runtime-suite map, plus a behavioral-AC worked example (mapped + unmapped cases).
- **#227 (executor) — ADR-075:** `release/tools/verify-release-plan.sh` — the **sole runner** of CIAC methods (Stage 9 reads its emitted verdicts), a thin dispatcher over a 5-family check registry, output matching the Stage-6 Verification-Evidence shape.

**Version:** v3.65 (re-provisioned v3.62→v3.63→v3.65 after two parallel-release collisions caught at the Commit-0 re-verify). **Class:** cross-cutting → Deep Stage-9 review. **Effective_pts:** 31 (operator-accepted oversize; Shape-1 precedent). **Reversibility:** MODERATE (branch/PR; single revert at release grain). **Dog-food:** CIAC-1 authored into this plan and executed live (PASS).
