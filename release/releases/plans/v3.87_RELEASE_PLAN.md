# v3.87 Release Plan — design-artifact-backfill

Milestone: design-artifact-backfill (#291)
Version: v3.87 (provisional; binds atomically at the Stage-12 tag-claim)
Release class: novel · Bump class: MINOR · Reversibility: MODERATE / HIGH
Topology: D-C SINGLE — one release branch, one PR, one merge
Identity: versioned
Scope: 8 issues, staged two-wave · Wave 1 = #3725 → #3614-completion (build now) · Wave 2 = six builds (PROVISIONAL, held behind the D-FissionScope gate) · P0 fully-serial

> **Engineering Commit-0 version re-verify (2026-07-24, Friday).** Recomputed next-free against
> **origin tags + open `release/*` branches** (not the RELEASE_LOG tail — the ledger lags because a
> shipped version's row lands in a separate chore PR). `git fetch --tags origin && git fetch origin main`
> at `origin/main = 03f2450`: highest tag `v3.85`; `v3.86` is **claimed** by the concurrent open
> release branch `origin/release/v3.86-deploy-and-tooling-defect-cleanup` (PR #3794) — no tag yet,
> but an open release branch is a live claim. First unclaimed slot after the v3.86 claim = **`v3.87`**:
> no `v3.87` tag, no `release/v3.87*` branch (other than this one), no `v3.87` RELEASE_LOG row →
> **unclaimed and equal to next-free ⇒ PROCEED**. `[SOURCE]`
>
> **v3.86 → v3.87 re-render provenance.** This release was planned at Stage 4 as **v3.86** (see the
> Stage-4 plan + the "Decision Recorded" gate, both naming v3.86). A first Engineering-Commit-0 attempt
> **HALTed correctly** when `v3.86` was found claimed by the concurrent `deploy-and-tooling-defect-cleanup`
> release (PR #3794 — tag-race lost in the selection→Commit-0 window, the originating-incident failure
> mode the re-verify rung exists to catch). The hub re-rendered **D-Version v3.86 → v3.87** (verified
> free: no tag, no branch, not in RELEASE_LOG; no file-overlap with this release) and re-dispatched.
> Every version reference in this file carries **v3.87**; the branch name, the Stage-4 thread, and the
> `deploy-and-tooling` sibling retain their as-authored labels. `[SOURCE]`
>
> **This is designed behaviour, not a defect.** A version selected before Stage 12 is a *lease*, never a
> guarantee: the binding claim is the atomic ref-CAS at Stage-12 Phase B3. `v3.87` above is still
> provisional and may itself be outrun — Stage 12 re-computes next-free against fresh authoritative refs
> and claims whatever is free at that instant.
>
> **Re-version ledger note.** Per the `append_reversions` rule on `main`, a `RELEASE_REVERSIONS.md` row
> is appended only when an abandoned version **left an orphan tag** (`disposition=tag-orphaned`). The
> abandoned `v3.86` was claimed by *another* release and this release never pushed a `v3.86` tag — **no
> orphan tag exists and the ledger is correctly N/A**. Do not append a row at Stage 13 for it.

## Issue References

The eight scope-locked issues, each summarized so the reference survives a renumber (the durable
reference home for this plan per `core/standards/reference-durability-standard.md`; the body discussion
below is self-describing and positionally in-scope of this block):

- **Wave 1 — scope-determining:**
  - Phase 0.5 identification mechanism — ship the §9 `depicts:` frontmatter + embedded-artifact
    declaration + per-flow-type detection criteria so the artifact set is enumerable and the six
    completion conditions become testable; prove it identifies the #64 architecture pilot: **#3725**.
  - Phase 0 enumeration spike — its remaining deliverables (D1 gap table, D3 confirm-or-collapse
    decision) unblock once #3725 makes artifacts identifiable; its corrected-AC instance fixes already
    landed pre-spike: **#3614**.
- **Wave 2 — build (PROVISIONAL; scaffolded only after the D-FissionScope gate):** concept-model
  **#3437** · agent-process **#3438** · decision-tree **#3439** · skill-flow **#3440** · human-process
  **#3441** · data-flow **#3442**. Held at *start* only; issue bodies unchanged.
- **Provenance / relations:** the shipped #64 architecture pilot this mechanism must identify — **#64**;
  epic / knowledge-corpus umbrella — **#1173**.

## Release Outcome Statement

Retained as-is from the milestone description at the operator's Stage-4 gate — it still holds for the
staged shape.

**AFTER:** every existing platform process across the six flow types (concept-model, agent-process,
decision-tree, skill-flow, human-process, data-flow) has a design artifact per
`design-artifact-standard.md`, authored against a **corrected** completion-condition that includes §2's
default "embedded in parent doc" arm — with the true gap quantified and the fission
confirmed-or-collapsed.

**BEFORE:** the six flow-type children carry an inherited AC that contradicts §2 (offers only the
exception location arms); the real gap is unquantified; the 6-way fission is unvalidated.

**Success Indicator:** each flow type's design-artifact set is complete per the corrected standard; no
orphaned diagrams.

## Dependency Graph

Directional edge `A → B` = "A blocks B / B cannot start until A lands." A diamond with a split source
node, not a line.

```
   #3614 (spike, Phase 0) — SPLIT NODE
     ├─ D2 corrected-AC template ....... DONE (instance ACs fixed on #3437–#3442 pre-spike)
     └─ D1 gap table + D3 confirm-or-collapse ... BLOCKED ↓
                    │  (partial run SURFACED the missing prerequisite — provenance edge)
                    ▼
            ┌──────────────────┐
            │      #3725        │  Phase 0.5 — identification mechanism
            │   (mechanism)    │  depicts: frontmatter + embedded-artifact marker
            └───────┬──────────┘  + per-flow-type detection criteria + prove on #64 pilot
                    │  blocks (fan-out)
        ┌───────────┴───────────────────────┐
        ▼                                    ▼
 #3614 completion                    six builds' completion
 (D1 gap table = a QUERY now;        conditions become TESTABLE
  D3 confirm-or-collapse decision)    (mechanism dependency)
        │  D3 = confirm / collapse / trim  (scope decision, operator D-class)
        ▼
  ┌─────┴──────────────────────────────────┐
  ▼      ▼      ▼      ▼      ▼      ▼
#3437  #3438  #3439  #3440  #3441  #3442   ← six builds RE-CONVERGE here,
                                             gated by BOTH #3725 (mechanism)
                                             AND #3614-D3 (scope)
```

**Edge inventory (explicit):**

| From | To | Type | Note |
|---|---|---|---|
| #3614 (partial) | #3725 | provenance / surfaced | #3725 exists *because* #3614 found the gap; not a forward build blocker |
| #3725 | #3614 (D1, D3) | hard (mechanism) | gap table unanswerable, confirm-or-collapse un-decidable, without identification |
| #3725 | #3437–#3442 (×6) | hard (mechanism) | each build's completion condition is untestable pre-#3725 |
| #3614-D3 | #3437–#3442 (×6) | hard (scope) | the fission may collapse/trim; builds' *shape* is unknown until D3 |

**No cycles.** Critical path = **#3725 → #3614-completion → builds**. #3614-D2 is off the critical path
(already done).

## Implementation Sequence

**Concurrency posture: P0 fully-serial** — #3725, #3614, and all six builds share
`design-artifact-standard.md` + `core/diagrams/`, so they serialize regardless.

**Wave 1 — scope-determining (build now, serial):**

| Order | Issue | Rationale |
|---|---|---|
| **1** | **#3725** | FIRST and alone on the critical path. Amends `design-artifact-standard.md` §9 (`depicts:` promoted deferred→required; embedded-artifact HTML-comment marker) + §2 (Human-process current-state cell correction) + new §12 (per-flow-type detection criteria, table-aware for the three table-rendered types) + backfills the #64 pilot. The release's key design risk — front-loaded. Carries **ADR-089** (embedded-artifact declaration marker). |
| **2** | **#3614 completion** | With artifacts now identifiable: produce the D1 gap table as a *query result* (cite #3725's detection criteria as enumeration method), then render the **D3 confirm-or-collapse** decision on real numbers. D2 template codification rides along, off critical path — homed OUTSIDE `design-artifact-standard.md` to avoid a dual-write with #3725. |

**HUB HOLD — D-FissionScope gate** (operator scope decision on the gap table). *The release pivots here.*

**Wave 2 — build (scaffold ONLY after the gate; shape = the gap table, not the prediction):** the
surviving build(s), sequenced by real gap size. Spike-predicted shape (a prediction, **not** a
measurement — bind to the gap table): keep-as-full-build **#3441 human-process** (zero §2 baseline, P2);
likely-partial **#3439 decision-tree**, **#3438 agent-process**; likely-trim-to-verify-and-close
**#3442 data-flow / #3437 concept-model / #3440 skill-flow** (the three table-rendered "under-measured"
types — probably already covered).

**Commit plan:** Wave-1 commit groups on `release/v3.87-design-artifact-backfill`. **Engineering
Commit 0 = this plan file.** Commit 1 = #3725 mechanism (design-artifact-standard.md §9/§2/§12 + pilot
backfill). Commit 2 = ADR-089. Wave-2 issues are added to the branch after D-FissionScope re-compose
(Bundle Mutability amend, not re-bundle, if the milestone stays intact).

## Contention Map + File Change Matrix

**Shared write surfaces (serialize):**

- **`core/standards/design-artifact-standard.md`** — the CENTER. **Single writer = #3725** (§9/§2/§12
  amendments). Read by #3614 + all six builds. **Dual-write hazard:** #3614-D2's corrected-AC template
  MUST be homed *outside* this file (recommend `release/references/` or the issue-template surface) so
  it does not contend with #3725.
- **`core/diagrams/`** — today holds only the #64 pilot. #3725 **edits** the pilot (frontmatter backfill,
  the AC4 proof target). Builds write here only when the §3 centralization test fires (≥3 parents) —
  rare; filenames `<flow-type>-<name>.md`, distinct per type.
- **`docs/` + `docs/README.md`** — the human-facing arm for **#3439** and **#3441** (Wave 2). Serialize
  the index edit if a reader-index lands.
- **Parent docs (embedded artifacts)** — each Wave-2 build embeds into *different* parents; exact paths
  enumerated at Stage 5 post-#3725 (build rule: do not pre-fabricate).

**Cross-release contention (baseline-pinned):** as of Commit-0 (`origin/main = 03f2450`), the only open
release branch is `origin/release/v3.86-deploy-and-tooling-defect-cleanup`, whose scope is deploy/tooling
defects — **no sibling release edits** `core/standards/design-artifact-standard.md`, `core/diagrams/`, or
`docs/`. Milestone description corroborates ("verified 2026-07-24: no other open milestone touches these
surfaces"). **Baseline pinned; re-check at Stage 9 sibling-merge revalidation.**

**File Change Matrix (machine-readable — one path per line; `[intent] owner · note`):**

```
core/standards/design-artifact-standard.md          [EDIT]  #3725 · §9 depicts: spec (deferred→required) + embedded-artifact HTML-comment marker; §2 Human-process current-state cell correction; NEW §12 detection criteria (7 rows, table-aware) + enumeration harness; sole writer; read by #3614 + all six builds
core/diagrams/architecture-platform-structure.md    [EDIT]  #3725 · #64 pilot — add flow_class: architecture + depicts: block to frontmatter (AC4 proof target)
core/ADRs/ADR-089-embedded-design-artifact-declaration-marker.md  [ADD]  #3725 · records the embedded-artifact section-level HTML-comment marker decision (re-run check-adr-numbers.py at commit time; re-number if 089 claimed)
release/releases/plans/v3.87_RELEASE_PLAN.md         [ADD]  hub · Engineering Commit 0 (this file)
<corrected-AC template home — TBD at Stage 5>        [EDIT?] #3614 · D2 template codification; home OTHER THAN design-artifact-standard.md to avoid the dual-write edge with #3725
core/diagrams/                                       [ADD?]  #3437-#3442 · CONDITIONAL (Wave 2) — dedicated file only when §3 centralization test (>=3 parents) fires
docs/ + docs/README.md                               [EDIT?] #3441,#3439 · CONDITIONAL (Wave 2) — human-facing audience arm; serialize the index edit
<parent-doc paths enumerated at Stage 5>             [EDIT]  #3437-#3442 · embedded artifacts; per-flow-type; NOT pre-fabricated (Stage 5 enumeration post-#3725)
```

*(Wave-2 `[ADD?]/[EDIT?]` rows are ⚠ provisional — re-frozen at Stage 5 after D-FissionScope sets the
surviving build set.)*

## Stage Applicability Matrix

Legend: **✓** apply · **~** apply-light (reason stated) · **RL** release-level · **CMP** compressed for
git-native releases (Stages 10 Dry-Run + 11 Snapshot: git history IS the snapshot; PR review IS the
dry-run gate) · **⚠** provisional (row not scaffolded until post-D-FissionScope).

| Issue | S5 | S6 | S7 | S8 | S9 | S10 | S11 | S12 | S13 |
|---|---|---|---|---|---|---|---|---|---|
| **#3725** mechanism | ✓ **(deep — ADR-threshold; embedded-decl is genuine design uncertainty)** | ✓ | ✓ (functional — must identify #64 + reproducible count) | ✓ (5 ACs, per-criterion) | RL | CMP | CMP | RL | RL |
| **#3614** completion | ~ (spike = investigate + decide; D1 gap-table + D3 land as comments) | ~ (post gap table + decision; D2 template codify) | ~ skip-leaning — no functional surface; gap-table query validated under #3725's DT | ~ (coherence/evidence) | RL | CMP | CMP | RL | RL |
| ⚠ **#3437** concept-model | ~ | ✓ | ~ light | ~ light | RL | CMP | CMP | RL | RL |
| ⚠ **#3438** agent-process | ~ | ✓ | ~ light | ~ light | RL | CMP | CMP | RL | RL |
| ⚠ **#3439** decision-tree | ✓ (carries the `docs/` audience-placement decision) | ✓ | ~ light | ~ light | RL | CMP | CMP | RL | RL |
| ⚠ **#3440** skill-flow | ~ | ✓ | ~ light | ~ light | RL | CMP | CMP | RL | RL |
| ⚠ **#3441** human-process | ✓ **(deep — zero §2 baseline + `docs/` audience decision)** | ✓ | ~ light | ~ light | RL | CMP | CMP | RL | RL |
| ⚠ **#3442** data-flow | ~ | ✓ | ~ light | ~ light | RL | CMP | CMP | RL | RL |

**DT/QA-light justification for the six builds (stated, not assumed):** design-artifact authoring is
DT/QA-*light* **only because #3725 ships a testable surface** — DevTest runs the §12 detection criteria
over the authored set + the §9 no-orphaned-diagrams / Check-14 link check; QA renders per-criterion
verdicts against the detection criteria. If #3725's mechanism proves weak (Risk R3), the builds' DT/QA
are **not** safely lightenable. **The six build rows are all ⚠ provisional** — their true stage load (and
whether they exist as full builds) is set by D-FissionScope; several may reduce to a batched
"verify-and-close (already-satisfied)" disposition with no S6 authoring.

**Stage 5 activation (novel class → bias ALL):** Wave-1 activates Stage 5 for #3725 (done — ADR-089
drafted) and #3614. Wave-2 activation re-derived post-D-FissionScope.

## Risk Register

Every entry names risk · owner · mitigation · reversibility.

| ID | Class | Risk | Owner | Mitigation | Reversibility / Confidence |
|---|---|---|---|---|---|
| **R1** | SCOPE — collapse (first-class) | The six builds are **PROVISIONAL**; the confirm-or-collapse decision (post-#3725) may collapse/trim them. Full 13-stage scaffolding for all six now is premature. | hub / operator | Staged release + HUB HOLD @ D-FissionScope; scaffold Wave 2 **only after** the gap table; bind build shape to the query, not the prediction | Not-scaffolding CHEAP; likelihood HIGH |
| **R2** | DEPENDENCY — single point of failure | **#3725 gates everything** downstream. If it stalls, the release stalls. | #3725 spoke | Front-load #3725; ADR the mechanism; treat its Stage-5 design as the pacing item | Critical-path; HIGH |
| **R3** | DESIGN — the hard half | #3725 AC2 (distinguish an *embedded* artifact from prose) is genuinely unsolved. A weak mechanism leaves the six builds' ACs untestable. | #3725 spoke | **RESOLVED at Stage 5** — ADR-089 adopts a section-level HTML-comment marker; proven on ≥2 known parent docs before Wave 1 exits | MODERATE (amends live standard) |
| **R4** | CONTENTION — dual-writer | `design-artifact-standard.md` written by #3725 and possibly #3614-D2. | hub sequencing | Single-writer ordering (#3725 → #3614-D2); home the D2 template *outside* the standard | CHEAP |
| **R5** | CONTENTION / SCOPE-CREEP | `docs/README.md` reader-index + `docs/` arm shared by #3439/#3441; the operator-suggested subject index is an *un-committed* scope addition. | #3439/#3441 spokes | Serialize the index edit; treat the subject-index as an explicit Phase-B scope decision | CHEAP |
| **R6** | ROLLBACK | #3725 amends a live K1 standard six cards cite; promoting a deferred field changes what conformance means. | operator | `git revert` #3725 ("nothing to migrate" holds only if no build artifact exists yet — the staged ordering preserves this) | MODERATE |
| **R7** | SCOPE — verify-before-recommend | Per §2 current-state, embedded artifacts may already exist for several flow types; six full builds against an already-covered corpus is theater. | #3614 D1 | The gap table IS the verification; bind build scope to it (composes with R1) | CHEAP |
| **R8** | VERSION — tag race | v3.87 may be outrun before Stage 12 (v3.86 already was). | Stage 12 | Atomic ref-CAS at Stage-12 Phase B3 claims whatever is free; Commit-0 re-verify already ran (PROCEED) | CHEAP |

## Cross-Issue Acceptance Criteria

Release-scoped cohesion predicates spanning ≥2 issues; graded on the merged PR at Stage 9.

- **CIAC-1 (#3725 × #3437–#3442 on the §12 detection table):** the per-flow-type detection criteria
  #3725 ships **cover every build flow type** — a table-aware rule exists for each of the seven types.
  *Method:* `grep` each flow-type token in §12; assert all present. *(Holds regardless of collapse.)*
- **CIAC-2 (#3725 × #3614 on the gap table):** #3614's gap table (D1) is a **query result of #3725's
  mechanism**, not an estimate — it cites the detection criteria as its enumeration method and yields a
  reproducible per-flow-type count. *Method:* inspect the gap-table artifact for the detection-criteria
  citation + count columns. *Declared; graded at Stage 9.*
- **CIAC-3 (#3441 × #3439 on the `docs/` audience arm) — ⚠ provisional:** both human-audience flow types
  place human-reader artifacts on the human surface, never inside an agent-instruction file; if a
  reader-index lands, both are indexed.
- **CIAC-4 (#3437–#3442 on §9 no-orphaned-diagrams) — ⚠ provisional:** running #3725's detection criteria
  over the merged release yields **zero orphaned artifacts** — every dedicated artifact carries
  `depicts:` + a reciprocal §9 link; every embedded artifact is declared per the marker.

*(CIAC-3/CIAC-4 span the provisional builds and are re-confirmed at the D-FissionScope re-compose gate;
CIAC-1/CIAC-2 are Wave-1-internal and firm.)*

## Operator Decisions

### D-ReleaseClass — DECIDED: `novel`
Operator judgment gate at Stage-4 Plan Review (2026-07-24): reclassified `routine → novel`. `routine`
triggers fail (P1/P2 issues present; ≥2 D-class decisions incl. D-FissionScope; #3725 ADR-threshold).
Differentiation posture: Engagement density **Standard** · Stage 9 review depth **Deep** (Collective
Review N-way + cross-D) · Stage 5 activation bias **ALL** · Stage 13 outcome-window **30-day**.
Reversibility: cheaper-to-stricter = **CHEAP / HIGH**.

### D-Version — v3.87 (recorded determination, re-rendered from v3.86)
Rule-determined next-free, re-rendered v3.86 → v3.87 after v3.86 was claimed concurrently (see the
Commit-0 re-verify block above). Re-verified at Engineering Commit 0 (PROCEED); binds atomically at
Stage 12.

### D-FissionScope — PENDING (HUB HOLD after Wave 1)
The confirm-or-collapse scope decision on #3614's gap table. Operator D-class; the release pivots here.
Rendered only after #3725 (this build) + #3614-completion produce the real per-flow-type gap count.
Wave 2 is scaffolded to the gap table, never to the spike's prediction.

## Bump Class

**MINOR.** Wave 1 is an additive standard amendment (promote a deferred field to required; add a new
detection section §12 + an embedded-artifact marker convention) + a factual current-state correction +
a pilot frontmatter backfill + one new ADR. No capability-class removal, no interface break, no schema
break. **Anchor rule:** next-free anchors on **tags + open release branches**, not the RELEASE_LOG tail.

## Gate Summary

| Gate | Verdict |
|---|---|
| **G-PL1** (AC / substrate currency) | **PASS** — all three #3725 ticket premises LIVE at Commit-0 (§9 deferral present; pilot lacks `depicts:`/`flow_class:`; §2 Human-process row still `Same`) |
| **G-PL4** (bundle-entry freshness) | **PASS** — Wave-1 cards fresh |
| **Cross-PR overlap audit** | **PASS** — sole open sibling (`v3.86-deploy-and-tooling`) does not touch these surfaces; baseline-pinned, re-check at Stage 9 |
| **Commit-0 version re-verify** | **PASS** — v3.87 unclaimed = next-free (PROCEED) |
| **D-FissionScope** | **DEFERRED** — HUB HOLD after Wave 1 |
