<!-- repo-integrity: allow-issue-ref -->
<!-- reference-durability: allow-link -->
<!-- reference-durability: allow-url -->
# RELEASE_INDEX_ARCHIVE-v2

Archive segment of [`RELEASE_INDEX.md`](RELEASE_INDEX.md) — the **v2** release family.

This file is the same record as its parent ledger, relocated. It is a **Vital**
record under `core/governance/RECORDS_POLICY.md`, retained permanently, and it
inherits its parent's class: a segment is a disposition *destination*, never
itself a disposition *source*. Nothing here is a lesser record for having aged
out of the working set.

It lives in the same directory as its parent deliberately, so `grep -r` over
that directory still finds this content exactly as it did before the move. Each
entry below retains its heading in the parent ledger, with a pointer here.

Entries are appended by `release/tools/sweep-release-corpus.py`; the file is
append-only and is never itself swept.

---
#### v2.41

Governance-hygiene — the root-cause-analysis method (#754) is wired by citation to the failure-owning surfaces its own §2 trigger table named but had no recorded binding (`ppm-agent` invoke-or-hand-off, `pmo-devops-sre` Mode-3 regression origination, the failure-owning pipeline stage(s)) (#1883); the hub's D-Version recurring-decision is reframed from an A/B/C click-gate to a recorded determination by default, escalating to an operator gate only on a deliberate version-less/forced-collision close-out (B) or a concurrent slot claim (C), with the (B)/(C) override valves retained (#1918); the extinct `pmo-platform/engineering/tools/` prefix in `practice-efficacy-framework.md` is dropped to the bare `health-check.sh` form (#1562); and a corpus-hygiene sweep fixes the malformed `(DT↔Engineering, )` cell in `handoff-coordinator-spec.md` and enumerates every PR-time durable-corpus gate + override marker in `core/ADRs/README.md` (#2218). `routine` class; slim cherry-picked milestone (release-corpus migration sliced off to #1549) — documentation/governance/skill-text only, no behavior/schema/runtime change. CHEAP / `git revert -m 1`.

#### v2.40

Transcript & meeting processing — the implicit transcript flow is named as one pipeline (Entry → Routing → Core-Tracker → PPM → Stakeholder-Review → Action-Triage) and its outputs are sorted into three automation tiers (auto-write trackers flow, stakeholder-facing/RAID changes stop for approval, action items surface for triage and are never auto-logged); the Open Meetings Tracker gains a governed `lifecycle_state` field (the frozen 3-state `scheduled → held \

#### v2.39

Audit-cadence-and-learning — the platform audits itself on documented event-bound cadences (process-fitness #167 + structural #168 axes) cross-referenced with the Anthropic Base-vs-Build framework as a 3-axis set, and captures recommendation↔choice deltas with a `--window` look-back + Stage-13 retro roll-up (#46), signal-only; `novel` class. Stage 12 completed as a remediation (#2428). CHEAP / `git revert -m 1`.

#### v2.37

Release-notes close-out conformance — the release-notes close-out is hardened so it is conformant on **every** authoring path (script AND hub-direct), and the published GitHub Release page can no longer silently drift from the in-repo source note; additive throughout — standard / pipeline-shard / close-runbook / lint / check edits plus one founding ADR, no data migration, no stored-content move, no project-facing behavior change. **Lint coverage for the missed posting defects (#2120):** `release-notes-standard.md` reconciles the three un-versioned title-format descriptions (§5.2 / §5.3 / §5.5 State-0) and the un-versioned `--title` cell in `stage-12-execute.md` to the single canonical `vX.Y — <headline>` form, fixes the §3.1 template plan link to the absolute `https://github.com/{REPO}/blob/main/...` form, and adds a whole-body link-purity lint (check 13) in `lint_release_corpus.py` flagging any repo-relative target in the frontmatter-stripped body (permitting `https://` / `#anchor` / `mailto:`); the Stage 13 close runbook (`hub-spoke-bridge.md` Procedure 7 + `stage-13-close.md` Phase B5.6) gains two `gh release view`-based LIVE-Release assertions (versioned title; no repo-relative body link); eval rubric synced 12→13. **§3.2 lint bound to the close EVENT (#2082):** the note-content conformance lint is wired into `automated-closeout.sh` (`phase_lint_release_notes`) AND named at the hub-direct Phase-B chore-PR fallback's Step 4 verification — on every path a finding naming the closing version blocks milestone close (a pre-existing finding for an unrelated version does not; audit-baseline discipline); the RCA fix for the v2.26 governance failure. **Deterministic Release-body re-emit + drift gate (#2085):** the published Release body is an enforced deterministic frontmatter-strip transform of the in-repo note (never hand-assembled), applied identically on the script and spoke paths, with §5.5 / §5.6 reconciled; a standing detective gate (`check-release-body-drift.sh`, surfaced by `deploy.sh` Check 47) asserts `published body == strip_frontmatter(note)` (flags, never re-emits). **Absorbed — rigor-invariance + Check 48 (#1290, ADR-048):** a named Rigor-Invariance Principle + canonical-checklist attestation at the Procedure 1 scaffolding seam (hub-direct ≡ spoke clause), plus `deploy.sh --check` **Check 48** + a `--check-close-completeness` CI probe (one shared body) asserting the complete Stage-13 output set on main for every `VERIFIED` `RELEASE_LOG` row at/after its cutover, delegating each sub-assertion to the engine that owns it (composes, never duplicates); reads main's state (scaffold-independent), warn-mode-initial + dormant behind a cutover sentinel. `routine` class; D-C SINGLE topology, one release PR (#2378), 14-commit history preserved; no sibling collision (RELEASE_REVERSIONS N/A). The release PR used safe-phrasing Issue-References (no auto-close keywords), so #2120 / #2082 / #2085 were closed manually in this close-out; the `.version` stamp (v2.36 → v2.37), CHANGELOG append, RELEASE_LOG/INDEX/DIGEST entries, and the release note are the Stage 13 close-out completions the automated tool drops for theme-named milestones. CHEAP / `git revert -m 1`.

#### v2.36

Triage-and-bundling signals — six Stage 1–6 signals the release pipeline used to re-derive each run (and one repeatedly hand-derived step) are converted into codified pipeline outputs and one tracked tool; additive throughout — spec/governance/schema edits plus a new tool, no data migration, no stored-content move, no behavior change to existing surfaces. **Directional-not-authoritative handoff (#500):** `stage-02-triage.md` §4 Inputs states that an approved ticket authorizes the problem/outcome, not its structural premise — downstream stages treat the ticket structure as a proposal to confirm/overturn at Solutioning; `stage-01-intake.md` §6 Outputs carries the forward-pointer and `stage-io-contracts.md` records the Stage 1 → Stage 2 boundary (principle only; enforcing gate is #501, ms `68-stage-gate-criteria-completeness`; compose, don't duplicate). **Priority-model decomposition (#283):** `stage-02-triage.md` splits priority's triple duty (urgency / importance / scope) into urgency vs value with a dependency-count triage signal, `improvement.yml` reconciled to match, public corpus kept methodology-neutral (no hardwired WSJF). **Native-dep mirror → tracked tool (#662):** the Stage 2 A3.5 native-dependency mirror is promoted from re-derived-each-run to a tracked tool (AS2) at `release/tools/native-dep-mirror.py` with a fixture test — line-directional parser, ignores relates-to/sibling edges, comma-safe. **Cluster threshold + tracking selection (#280):** `stage-03-bundle.md` documents a Stage-3 related-issue-cluster detection threshold + tracking-mechanism selection rule over the existing epic/cluster/sub-issue primitives. **A6 per-milestone + gate G3-17 (#292):** `release-process.md` makes the Stage-3 A6 New-Track Placement Rationale fire per-milestone and `gate-criteria-spec.md` adds enforcing gate **G3-17** (G3-13 retired per ADR-012; G3-14/G3-15 shipped via closed #293/#294; G3-16 reserved) — the edit that changed `release-planner`'s source and triggered its release-cut package rebuild. **Sub-task container threshold (#225):** `stage-06-engineering.md` defines a threshold for the Stage-6 sub-task container choice (sub-issues vs PR checklist); forward edge to #213, ships regardless. `novel` class; D-C SINGLE topology, one release PR (#2284), 12-commit history preserved. **Version note:** planned provisional v2.33; siblings claimed v2.33/v2.34/v2.35 mid-run so the atomic claim bound to **v2.36** above the shipped anchor v2.35 (recorded in `RELEASE_REVERSIONS.md`); the `.version` stamp, CHANGELOG append, RELEASE_LOG/INDEX/DIGEST entries, the release note, and the `release-planner` rebuild are the Stage 13 close-out completions the automated tool drops for theme-named milestones. CHEAP / `git revert -m 1`.

#### v2.34

Knowledge and decision confidence — new work gets classified the same way every time, captured learnings record the scope they came from, two more facilitation domains ship, the roadmap gains a curator skill, and a long-standing knowledge-eviction gap closes; additive throughout — new and edited reference docs, a new skill, and one build-check extension, no data migration and no stored-content move. **Architecture evaluative-lens layer (#1102):** a new `core/disciplines/` doc enumerates the design-time lenses — the triple-Venn (right skill ∩ right methodology ∩ right altitude) and the K1-universal vs K2–K5-install-specific plug-and-play classifier — wired as a Stage 2 Triage / Stage 4 Planning design check (graduated from observations #577 / #69 at N=2). **Organizational-altitude axis (#564):** `knowledge-architecture.md` gains a learnings-altitude axis (Unit → Task → Workstream → Project → Program → Portfolio → Organization) declared orthogonal to the K1–K5 universality tiers, with `km-protocols.md §3` recording altitude as a capture-time attribute and an adjacent-altitude rollup relation. **ADR-029 encode-and-evict closure (#2214):** the encode-and-evict lifecycle now re-points or drops downstream links to an evicted note and reconciles partially-absorbed notes at close; `deploy.sh` Check 36 gains two logged-not-blocking finding classes. **Facilitation-technique domains (#1945):** `core/standards/facilitation-techniques/` adds the retrospective and planning domains on the v2.22 estimation foundation, same 9-field schema, surfaced through the existing delivery-engine Mode D/E trigger. **Roadmap-curator skill (#453):** a new skill keeps the living roadmap current — default incremental maintenance against the ratified initiative set plus an operator-triggered re-baseline. `routine` class (`project:knowledge-corpus`); shipped as five separate integration PRs; the `.version` stamp, CHANGELOG append, RELEASE_LOG/INDEX entries, and the release note are the Stage 13 close-out completions the automated tool drops for theme-named milestones (#2048). CHEAP / `git revert` per PR.

#### v2.35

Agent decision-confidence — a written protocol lets an autonomous agent calibrate its own confidence at a non-obvious decision point and decide proceed / pause-to-learn / escalate from observable cross-check agreement (never self-reported confidence), scaled to the action's reversibility × autonomy; additive throughout — a new spec, a new ADR, and four edits wiring the gate into existing disciplines and one pipeline stage, no data migration or stored-content move. **Define — spec (#2286):** `core/specs/decision-confidence-protocol.md` defines the mechanism as a compose-not-redefine layer over five existing docs — (1) a confidence signal collapsed to three ordinal states (`CONVERGENT` / `DIVERGENT` / `UNGROUNDED`) from three observable sources (cross-check agreement, gap-namability, weakest evidence label), with verbalized self-confidence named and rejected as an input; (2) a reversibility × autonomy threshold matrix selecting PROCEED / PAUSE-TO-LEARN / ESCALATE per cell, refusing a single global numeric cutoff, with two cross-axis invariants (IRREVERSIBLE never auto-proceeds on a non-`CONVERGENT` signal; the matrix lowers ceremony but never raises an action's autonomy tier); and (3) a bounded pause-to-learn loop (default 1 cycle, hard cap 2) importing the discovery knowability triage as its gap-closer with four exit conditions guaranteeing termination — plus a six-domain application table, the named live consumer, and four FM entries. **Define — ADR (#2287):** `core/ADRs/ADR-047-decision-confidence-compose-posture.md` records the extend-and-compose posture with a per-doc relationship table agreeing cell-for-cell with the spec's §7. **Create — gate + loop (#2288):** `core/disciplines/autonomous-execution-model.md` registers a Pause-to-Learn Pattern as the third pre-action sibling to Retry/Escalate (competence-anchored, self-closing, chaining to Escalate only on budget-exhaustion), and `core/specs/reversibility-protocol.md` promotes its confidence-pairing from a label into an active self-gate as the cost-of-error axis. **Create — live consumer (#2289):** `release/references/pipeline/stage-04-planning.md` Phase A0 runs the gate before rendering the bundle-currency refresh outcome (no-op / amend / re-bundle / defer). **Create — anti-theater guard (#2290):** `core/disciplines/decision-discipline.md` adds G8 making the pause loop's three load-bearing properties (new-signal-injected / bounded / has-exit) observable, with a co-located falsification test-case pair. `routine` class; five separate integration PRs (one per deliverable); the `.version` stamp, CHANGELOG append, RELEASE_LOG/INDEX/DIGEST entries, and the release note are the Stage 13 close-out completions the automated tool drops for theme-named milestones. CHEAP / `git revert` per PR.

#### v2.33

Initiative-roadmap vocabulary and home — roadmap instances get a shipped in-repo home that survives every platform update, and the label guide stops implying a hierarchy tier the platform does not have; additive — a new git-ignored `/roadmaps/` folder, a token-default rewire to it across the SSOT surfaces, and a one-line label-taxonomy reframe, no behavior or data change. **In-repo roadmap home (#416):** the platform ships a `/roadmaps/` folder — folder + README tracked, roadmap instances git-ignored and never committed, absent from the update/regeneration manifest so platform updates never overwrite operator content; the `<OPERATOR_INSTANCE_ROADMAPS_PATH>` token default moves to the in-repo folder across both sources of truth (the `depersonalization-spec.md` registry and the `operator.toml.template` resolver), staying repointable as an override; **ADR-046** records the decision and supersedes-in-part the operator-local placement of ADR-012 + ADR-017 (authored content ships in-repo + git-ignored, operator runtime state stays operator-local). **Work-hierarchy single-source + Initiative reframe (#1038):** `label-taxonomy.md` stops describing an initiative label as a binding tier between work items and states it is a *grouping*, citing the single methodology-invariant work-item-hierarchy source with `initiative-roadmap-framework.md` reconciled to it — a documentation-truth correction, no labels added, removed, or re-applied. `routine` class (`project:methodology-packs`); the `.version` stamp, CHANGELOG append, RELEASE_LOG/INDEX entries, and the release note are the Stage 13 close-out completions the automated tool drops for theme-named milestones (#2048). MODERATE / `git revert -m 1`.

#### v2.32

Skill-registry identity and currency — the already-shipped skill registry (`core/skills/registry.md`, ADR-038) is made self-checking against the deployed roster, and the consumer surface that cited a phantom catalog file is corrected; an additive build check plus citation corrections, no data migration or stored-content move. **Row-currency, always-enforce (#1811):** `deploy.sh --check` gains `Check 5(d): Registry-currency (rows)`, appended after Check 5(c) and reusing Check 5's roster machinery, asserting against the deployed roster (`OPERATIONS_SKILLS + RELEASE_SKILLS + CORE_SKILLS`, `CANARY_SKILLS` excluded per ADR-04): (i) every registry row name ∈ roster, (ii) every roster member ∈ registry rows (FAIL on asymmetry, both directions), (iii) every registry row name resolves to a live `SKILL.md`; structural / zero-FP, findings increment `ISSUES` directly, with the `cmd_check` guards inherited and a broken parse degraded to a single loud audit-baseline FAIL. **Field-currency, warn-mode-initial (#1658):** a per-row inner assertion extends the row loop at one marked nesting seam (both layers share one roster source, cannot fork) checking recorded modes/dependencies (set-equality) + trigger-surface presence against the live `SKILL.md`, emitting `WARN` without gating until `registry-field-currency.mode` flips to `enforce` (per the Check 14/15/18/19 shakedown precedent); a companion fix reconciled the `pmo-architect` composition set (frontmatter + registry row) to its true 2-skill set so the field layer reads clean. **Consumer re-point (#1211):** 13 files across `core/` / `operations/` / `release/` that cited a phantom `dependency-graph.md` (never shipped) now cite the live `registry.md`, and one net-new `registry.md` citation in `skill-workspace-location.md` is de-linked to avoid a forward-reference — pure reference correction. `routine` class; the shared `Check 5(d)` design ships as a design-note (no ADR) per the Stage 5 scope-lock; 4 drifted `.skill` packages rebuilt at release-cut. CHEAP / `git revert -m 1`.

#### v2.31

Knowledge-management discipline — the platform's memory gets one written cross-surface read/write SSOT contract, and the QA platform-health audit gains systematic knowledge-management scanning; additive throughout (new docs + reviewed skill edits), no data migration or stored-content move. **Memory-type taxonomy + cross-surface contract (#1073 / #1074):** `knowledge-architecture.md §2.1` reconciles the four memory types (Work / Knowledge / People / Learning) against the K1–K5 universality, Context-Tier read-precedence, and Document-Tier write-approval axes — demonstrated as four orthogonal composing coordinates, not a fourth stacked axis; a new sibling discipline `memory-architecture.md` is the per-surface SSOT contract table (surface · memory-type · reader · writer · write-authority bound to the Autonomy Tier enum · cadence · class {read-only

#### v2.30

Governance cross-reference currency — display-text path citations across the corpus that still named the pre-reorganization `pmo-platform/{reference,skills,governance,engineering}/…` folders are reconciled to their verified live homes under `core/` / `release/` / `operations/`, so internal doc and skill-file links resolve instead of dead-ending; documentation/governance citation-accuracy only, no behavior/schema/runtime-check change. **Path-citation reconciliation (#121 / #399 / #677):** the 13 catalog `SKILL.md` files go from 67 stale `pmo-platform/…` citations to zero (#677); a 28-file non-corpus sweep rewrites genuine display-text drift while preserving legitimate occurrences — immutable ADR history, deploy-path globs, the rewriter's own examples, test fixtures, frozen audit line-refs, operator-instance roadmap paths (#399); one single-token QA↔Dev-Testing threshold `≥3` → `>3` is harmonized in `operating-model.md` (#121); and two `module-apis.md` standards files mis-listed under Specs with dead links move to the Standards list. The #677 scope correction (the drift was at `reference/standards/`, and the destination lives at `core/standards/`, not the issue's stated `core/specs/`) was verified against the live tree before any rewrite. **Two no-code dispositions (#2080 / #2081):** the #N-validity gate #2081 asks for is already enforced by the repository-integrity Issue-reference validity gate (no second check added); #2080's stale-#N sweep was re-measured live (176 unresolvable refs across 273 of 419 open issues) and the residual documented-and-kept since the re-version's old→new mapping is unrecoverable per reference; umbrella #753 closes on both children. `cross-cutting` class; additive/citation-correction throughout; the cosmetically text-edited skills' packages rebuilt at release-cut (theme-named milestone). CHEAP / `git revert -m 1`.

#### v2.29

Corpus-drift reconciliation — ten places where a reference or governance doc had drifted from its canonical source are corrected to match; documentation/governance accuracy only, no behavior change. **Count/enumeration drift (#127 / #1100 / #2095 / #107):** `core/CLAUDE.md.template` Review-discipline preference drops the hardcoded "10 anti-laziness rules" for the count-agnostic "its anti-laziness rules" (the set grew 10→14; the count is owned by `review-discipline-principles.md`); `build-reviewer/SKILL.md` Section 1 drops the stale "(10 rules)" summary and defers to the canonical set (`version: v1.10 → v2.29`); `core/rules/skill-deployment.md` replaces its hand-maintained Drift Check enumeration (which left Checks 16/17/19/21/22/24/26/27/31/32/34–46 unlisted) with a `deploy.sh`-derivation rule + reads enforcement mode from `.claude/hooks/deploy-check.mode`; `lifecycle-states-canonical.md` §6.2 drops an already-satisfied forward-consumer row. **Reference-term / field-name reconciliation (#49 / #305 / #857):** `stage-06-engineering.md` corrects "CI/CD = pre-commit hooks" → "PreToolUse hooks (`.claude/hooks/`)"; `core/CLAUDE.md.template` syncs intake tier-selection wording to `stage-01-intake.md` § 5 Path A; `navigation-layer-schema.md` reconciles its remaining `methodology` reference → canonical `delivery_approach`. **Persona-card reconciliation (#881):** the Stage 8 QA persona "no conditional pass" anti-pattern is reconciled to the evolved `stage-08-qa-testing.md` verdict enum (a CONDITIONAL ACCEPT is a defined Phase E verdict with listed defects + an Override Record; only an unconditioned one is the anti-pattern). **Duplicate-basename single-source (#94):** `pmo-skill-editor`'s near-duplicate `regression-checks.md` is removed from source and single-sourced into the package at build time from `core/standards/regression-checks.md` (new `TEMPLATE_SYNC_MAP` entry + explicit-basename resolver rule). **Validator-surfaced cleanup (#498):** corpus-content drift surfaced by the v3.20 validators — 5 historical notes + a v1.03 INDEX-drift + filename findings. `routine` class; additive/text-correction throughout; the 2 changed-skill packages rebuilt at release-cut (theme-named milestone). CHEAP / `git revert -m 1`.

#### v2.28

Generated-vs-source provenance — instance-side provenance markers for AI-generated artifacts (single card #205; siblings #200 absorbed / #201 superseded, already closed). `core/schemas/frontmatter-schema.md` Category 3 adds the instance-population `generated_by` (skill **name + semantic version**, distinct from `created_by`) plus the reciprocal dual-semantics note that closes the `template-protocol.md` §8 dangling both-locations forward-reference, defines `source_inputs` as the canonical cross-domain provenance carrier (`TR-### \

#### v2.27

Ticket information architecture — three coupled cards establish how tickets carry confidence, titles, and information. **#436 (staleness-confidence standard + ADR-043):** a new `core/specs/staleness-confidence-standard.md` defines the ONE canonical four-band ordinal **depth** scale (`S0-NONE` < `S1-SUPERFICIAL` < `S2-SUBSTANTIVE` < `S3-STRUCTURAL`, optionally backed by a projected score) that every ticket-facing staleness mechanism maps onto — five incompatible shapes collapse to one vocabulary; founding slim-kernel ADR-043 records the ordinal-depth-over-time-decay decision (which reverses the originating issue's lean); `delivery-engine` backlog-aging reports on it (age alone never reaches `S3`). **#545 (human-informative titles + G1-01/Check-22 floor):** the three work-item templates drop the `[...]:` title prefix (type travels on the label), `gate-criteria-spec.md` repurposes **G1-01** in place into a title-informativeness floor (syntactic — no bracket prefix + substance floor; same ID, schema v1.15→v1.16), `deploy.sh` Check 22 enforces it **warn-tier** (legacy prefixed issues WARN, never FAIL — no forced bulk re-title), `intake-style-guide.md` §7 carries the semantic rubric, and `intake-desk` elicits to it. **#211 (ticket information model + Status/Stage invariant):** a new `release/references/specs/ticket-information-architecture.md` captures the Three-Layer Model (Source of Truth = issue body / Stage Reviews = comments

#### v2.25

Hub-spoke orchestration discipline — the orchestration failure-modes the platform learned by trial-and-error are codified into `release/references/how-to/hub-spoke-bridge.md`, the bridge reference the hub reads at runtime. **Spoke A (#2016)** adds a cascade-completeness chip block so a spoke whose spec changes a count / enumeration / threshold enumerates every stale occurrence (#78), a necessity/value-add 7th hub-eval dimension so the hub can diverge from accurate-but-inert spoke recommendations rather than rubber-stamp them (#210), a durable-corpus de-fragile authoring pre-check at the bridge + `reference-durability-standard.md` (#879), four chip disciplines lifted memory→corpus — verbatim chip-source, issues-as-spoke-contract, hook-safe git idioms, concurrent-spoke contention recovery (#1669), and a mid-Stage-5 cross-ticket scope-detection rule + hub escalation handler anchored by founding ADR-041 (#31). **Spoke B (#2017)** ships the #189 empirical hook-inheritance suite — a Layer-A CI regression + a Layer-B operator probe + the new Check 46 `agent-tools-list-conformance` `deploy.sh --check` + a `subagent-security-posture.md` §1/§3 reconcile, anchored by founding ADR-042; empirical finding: subagent hook-inheritance CONFIRMED at workspace root, the worktree-session hook-load residual surfaced and tracked as #1472. `novel` class; additive throughout (no `SKILL.md` edited); two disjoint single-writer spokes. CHEAP / `git revert -m 1` per PR.

#### v2.24

Corpus conventions and standards hygiene — a documentation/governance hygiene release (no executable surface). A blast-radius relocation hub (#77) moves `failure-mode-standard.md` + `process-flow-diagram-standards.md` into `core/standards/` with a live-verified 90-file inbound reference sweep re-grepped to 0 dangling `specs/` refs (ran first; rewrites a ref in `triage-design-rereview.md` (#81) + relocates the diagram standard (#113)). Four net-new corpus docs land: a minimal-addition authoring discipline (`core/disciplines/minimal-addition-discipline.md`, #751), the third domain-best-practices domain (`domain-best-practices/process.md`, #764), an audit-recommendation status-badge convention (`audit-recommendation-status-badges.md`, #171), and a Claude Code runtime-state catalog (`claude-code-runtime-state.md`, #163). Three drift fixes reconcile reference docs to live state: skill-deployment roster counts (#580), framework-catalog `canonical_doc` paths + km-governance count (#663), and the `triage-design-rereview §7` `size:XS` omission (#81). A `type:spike` substrate-neutrality audit (#1094) confirms the bare-toolkit substrate (no methodology vocabulary hardcoded as universal beyond `delivery_approach`; findings register under `pmo-platform/analysis/`; precursor to K1 methodology-pack authoring #1090). `novel` class; additive doc/governance only; the 37 changed-skill `.skill` packages rebuilt at release-cut (#77's ~30 inbound-ref `SKILL.md` rewrites + the version bump). 10/10 ACs PASS. **Re-versioned v2.23→v2.24** at Stage 12 (the `v2.23-functional-people-graph` sibling claimed v2.23; next-free minor per defer-to-merge). CHEAP / `git revert -m 1`.

#### v2.26

People-graph activation — install/update + setup-workspace seed the operator-instance people-roster create-once (never-clobber) at three sites behind a new `pmo_people_roster()` accessor (#2040); GETTING_STARTED gains a people-graph adoption section with the per-tier `ref->Person` owner flow (#2041); a binary-judge eval verifies the four consuming skills resolve read-only from a populated roster (#2042). Activates the structure shipped in v2.23. `routine` class; additive; CHEAP-MODERATE / `git revert -m 1`.

#### v2.23

Functional people-graph — PMO agents now resolve people from one maintained, never-committed functional people-graph. A closed-allow-list operator-instance roster (`people-roster.yaml`, `{value, source, last_verified}` provenance + a `status` tombstone; only a de-identified template ships in-repo) is the data surface (#315); a read-time composed VIEW over the roster + the frozen Person/Resource entities (compose-not-absorb, no committed store) answers who-does-what / who-covers-whom / coverage-by-capability, with Tier-1 ambient maintenance + a clarification queue (leg C, #1166); the four leadership-owner fields (project / portfolio / program owner + initiative sponsor) are type-lifted from free text to `ref→Person` with an external fallback, governed by ADR-040 (mutual-exclusion + field-conditional resolution; #1898); `comms-writer` / `tracker-manager` / `ppm-agent` / `delivery-engine` read names / owners / coverage read-only (leg D, #1899); the graph + its maintenance + its consumption are registered in OPERATIONS.md (leg E, #1900); spike-gated (#1897). The filled roster is protected by out-of-tree placement (primary) + a `.gitignore` stray-copy backstop + a PII pre-commit needle extractor. `novel` class; additive; functional-coordination only — explicitly not an HR/performance system. MODERATE / `git revert -m 1`.

#### v2.22

Comms-and-facilitation reference substrate — facilitation techniques get a governed K1 home (`core/standards/facilitation-techniques/`, seeded with its first delivery-lifecycle domain, Estimation, and surfaced silently by `delivery-engine` Mode D; #56), the canonical meeting-agenda + meeting-recap output-format specs are authored once and single-sourced out of `comms-writer` (Types 2/3 + `channel-formats.md` + the `MTG-###` agenda field all cite-by-path, no inline duplicate; #317), and a recap↔follow-up boundary contract keeps a recap a reference to discrete follow-up records rather than their sole container (artifact-catalog split + `ppm-agent` emit-path rewire on canonical `operational-artifacts.md`; #378). 5 skills materially edited + bumped to v2.22 (packages rebuilt in-merge); `novel` class; #56 decomposed to a corpus-foundation card (9 per-domain cards deferred to epic #1173); QA PASS on all 3 cards, 0 defects. MODERATE / `git revert -m 1`.

#### v2.20

Field-lifecycle and CMDB automation — entity fields get a governed, agent-driven lifecycle: shared + portfolio entity-lifecycle transition protocols (G8, #156/#1155), a G10 write-permission matrix governing who writes which field per stage aligned with the Agent Write Permissions table (#154/#208), and skills that emit/consume the transitions (#1156). The `artifact_state` model is reconciled onto the canonical `lifecycle_state` + Domain model (`promotion_state` carve-out; dual-read removed; artifact-generator + artifact-lint migrated; new `artifact-workflow-protocol.md`) (#1865/#1866). The skill registry is evolved into the platform CMDB (all deployed skills are CIs; Option A, **ADR-038 supersedes ADR-035 §Decision part 4**) with a per-project Artifact Register (#202), and a project-tracking integrity sweep adds dormancy (10bd) / overdue-decision (3–5bd) / evidence-backed-gate discipline (#752). 9 deliverables; `cross-cutting`; QA ACCEPT (~48/48 ACs, 0 defects); MODERATE / `git revert -m 1`. *(Numbering: `v2.20` tagged after `v2.21` — concurrent release claimed the higher number first; claim order non-monotonic by version; self-heals via the claimed-set.)*

#### v2.21

Hub Decision Briefings made auditable along the agent-operating-principles axis — information-sufficient (pre-load spec content, enumerate the full option space, render-before-AskUserQuestion), design-principle-conformance-scored via a new K1 register + per-option D-Gate subsection, and shaped by standing per-gate-class framing directives (ADD-never-SUBTRACT). 3 cards; `cross-cutting`; MODERATE.

#### v2.19

Co-management governance cleanup — retires the legacy co-management compatibility shim (the operator-specific naming leaves `project-initiator` entirely; it survives only as a one-line `project-schema §7` migration breadcrumb + frozen test fixtures), genericizes the residual K4 operator-config field names to `co_management_*`, and **single-sources the Dual-Framing Bridge rule in `OPERATIONS.md`** — removing the redundant `core/CLAUDE.md.template` copy and repointing the 7 citations, closing the duplicate-source drift that had left the two copies disagreeing (the drift surfaced during this release). Follows v2.18's decouple; finishes de-broadcasting the operator-specific naming from the public codebase. **Migration:** a `PROJECT.md` on the legacy co-management key must rename it to `dual_framing_enabled` (one-line; identical semantics; the shim no longer reads it) — the 3 operator-local files were migrated alongside. 2 issues (#1846 shim + token, #1853 bridge single-source). Back-compat **contract change** (shim retirement) → minor, not patch; CHEAP / `git revert -m 1`.

#### v2.18.1

Operator-instance path decoupling (.env pattern) — a single resolver (`${CLAUDE_WORKSPACE_ROOT:-$HOME/Claude}` per ADR-032, no new variable) for the operator needle/instance path, a tracked `.env`-style template + create-once scaffold (gitignored content), persistence across `update.sh`, and a **fail-closed** PII guard (warn-mode-initial) that closes the silent fail-open. Sequenced onto #1847's path-portability enforcement (rebased + Check-43-allowlisted). 1 story (#1830). Patch off v2.18; MODERATE / `git revert -m 1`.

#### v2.18

Hybrid co-management decouple + Hybrid-Two — the `Hybrid` methodology is now defined cleanly as a **user-configurable two-archetype combination** (`delivery_approach: [A, B]`, e.g. `[Scrum, Kanban]`) and decoupled from the co-management arrangement it was previously tied to. The legacy co-management field is renamed `dual_framing_enabled` (legacy key still accepted via a deprecation shim), the dual-framing status artifact is renamed the "Dual-Framing Bridge", and the operator-specific token survives only as operator-local (K4) config; dual-framing behavior is unchanged. Touches a K1 schema field, a registered artifact + registries, two governance files, 26 skills, and the XC-06 gate. Per-space methodology split deferred to #1788. Non-breaking (legacy-key shim); CHEAP–MODERATE / `git revert -m 1`.

#### v2.17

Architecture-altitude discipline — every new capability is interrogated for its abstraction altitude (extend an existing platform seam, or solve point-wise?) as a forcing function at Stage 4/5, closing the gap that let a host-concrete design land where a host-agnostic capability + adapter was the right seam. A Stage-4 T3 obligation rider, an `altitude` distinctness axis (point-fix / extend-seam / new-abstraction) + ≥2-band rule, a blocking design-review §4.7 seam-composition gate, the `HOST-BINDING-LEAK` leakage class + a warn-mode Check 42 detector, a scoped A6.5 adversarial altitude exception, and `[adapters].repo_host` cross-references in the release specs. Additive governance-doc + one detector (no skills/packages); CHEAP / `git revert -m 1`.

#### v2.16

Concurrent-release version-claim determinism — a release's version is now claimed atomically at the merge tag (defer-to-merge + ref-CAS) instead of reserved early, so two releases in flight can no longer silently collide on a number. Defined host-agnostic via a config-selected `repo_host` adapter (GitHub/git the v1 reference); founding ADRs (ADR-036/037), host-adapter + grammar/semantics standards, the `claim-version.sh` atomic-claim tool, a CI freeness gate, and a machine-readable re-version ledger + recovery doctrine. Additive (no skills/packages touched); CHEAP / `git revert -m 1`.

#### v2.15

Role-Specialist suite GA — 5 Release-to-Sustain role agents (Release Manager, Tier-1/2 Support, OCM Lead, Knowledge Manager) + the capstone PMO Skill Router reading the new `core/skills/registry.md` logical skill registry. Closes epic #284 Phases 4–5; the suite reaches its full 19-Specialist roster, addressable as one routable unit. Additive (6 new skills + registry + ADR-035); CHEAP / `git revert -m 1`.

#### v2.09

**Deploy + release-tooling health — checks that silently skipped now run, scripts re-pointed at the live tree** — a `novel`-class release of 11 surgical fixes to the platform's own deploy and release scripts, sequenced D-C SINGLE (one branch, one commit per issue) because five issues edit `core/deploy/deploy.sh`. **deploy.sh cluster (5):** **#1104** corrected the Check-9 `MIRROR_PAIRS` source path (`release/rules/` → `release/governance/release-process.md`) so the rules-mirror pair is enforced instead of silently SKIPped, + a missing-source WARN guard; **#984** applied the #88 `remove_mirror_subtree`/root-cause pattern to the supplementary-copy loop so a read-only-target copy failure reports an actionable cause; **#661** added an additive `18d`-class framework-catalog `canonical_doc` path-resolution sub-check (warn-mode-initial) to the Check 18 block + `check-version-anchors.py`; **#1036** tombstoned the silent Check 24 numbering gap, attached reactivation anchors to the dormant Checks 11/30, and added a check-retirement registry (the issue body's wrong "Check 9 dormant" citation was corrected before Stage 5 — Check 9 is the live rules-mirror sync); **#86** was largely pre-resolved on live main (Check 19 `\

#### v2.04

**Close-out retrospective register template + a decided release-corpus public-vs-instance home (ADR-032 design; migration deferred)** — a `cross-cutting`-class release that ships the per-release close-out ceremony as a public template and ratifies which release-corpus surfaces are public capability vs operator-instance content, *without* executing the corpus move. **#1412 (DESIGN ONLY this release):** `core/ADRs/ADR-032-release-corpus-public-vs-instance-split.md` (Accepted) applies ADR-017's S1-Package-vs-S3-State cut to the release corpus — `RELEASE_LOG.md` / `RELEASE_DIGEST.md` / `notes/` / `plans/` are S3 operator-instance content; `CHANGELOG.md` + an empty `RELEASE_INDEX.md` seed are the thin S1 public surface; templates / schema / pipeline / tools / deploy-checks are S1 public mechanism (load-bearing rule: *the capability ships; the content instances*). It resolves #1412's 5 open questions, pins the instance-root resolver to the canonical `${CLAUDE_WORKSPACE_ROOT:-$HOME/Claude}` (not the ADR-017-named orphan `PMO_INSTANCE_PATH`), and chooses **HEAD-only removal over an irreversible history scrub** (non-PII operational detail; `git filter-repo` is force-push-denied + `refs/pull/*`-leaky + buys nothing the threat model needs). The corpus **migration execution** (`.gitignore` + `git rm --cached` + move + re-point 4 tools + 2 reference docs + converge 21 `deploy.sh` `PMO_INSTANCE_PATH` fallthroughs) is **DEFERRED** to a complete-by-construction follow-up issue filed at Stage 13. ADR authored as ADR-029 in Stage 5, renumbered to ADR-032 at Stage 6 (ADR-029/030/031 landed first; `check-adr-numbers.py` confirmed 032 gap-free). **#360 + #361 (combined template):** one `release/references/templates/release-learnings-register-template.md` ships PUBLIC carrying a full Kerth retrospective (Prime Directive + 3-question + ritual of closure) + full PMBOK 7 lessons-learned (Situation / Outcome / Lessons / Next-cycle Actions); `release/references/pipeline/stage-13-close.md` §6 names the register and states its **COEXIST** relationship to the machine-generated `#### Release Learnings v<X.Y>` triple H4 block (directional: triple → register; the triple stays authoritative for the cross-release pattern-detector and unaffected by the register's absence). **Half-migration friction (flagged):** the deferred migration means the operator-instance corpus root `${CLAUDE_WORKSPACE_ROOT:-$HOME/Claude}/personal/pmo-instance/releases/` is not yet bootstrapped (the `install.sh` skeleton is part of the deferred execution) and `<OPERATOR_INSTANCE_RELEASE_LOG_PATH>` still resolves to the in-repo `release/releases/RELEASE_LOG.md` per the `release-notes-standard.md` footnote — so all 5 corpus surfaces this release are written to their in-repo paths under the pre-extraction resolution rule (no corpus migration / `.gitignore` / toolkit re-point performed). The first filled close-out register (the v2.04 dogfood, satisfying #360/#361 AC6) was written to the canonical instance path, created at close because the bootstrap has not run. `cross-cutting` class, CHEAP reversibility / HIGH confidence (additive — a NEW template + a NEW design-only ADR + a `stage-13-close.md` §6 edit; whole-release rollback = `git revert -m 1` of PR #1533; the new template has no prior consumer and no release records were moved). **Version reclamation:** scoped `v2.07` at Stage 4 → provisional `v2.10` at Engineering Commit 0 → reclaimed the original abandoned `v2.04` slot at the Stage 12 gate (operator decision 2026-06-20; PR #1407 never merged; `git ls-remote --tags origin v2.04` confirmed the slot free) — a deliberate backwards-version reclamation numerically below the shipped v2.05–v2.08, operator-accepted `git describe` non-monotonicity. The release branch `release/v2.10-close-out-registers` is retained for traceability only (deleted at Stage 13 cleanup); the tag, plan filename, PR title, and commit scope all carry v2.04. PR #1533 merged to main via merge commit `cc10af42dfb2d4771b74107513c0e9cdd050486b`, signed-annotated (ED25519) `v2.04` tag at the merge SHA. The 3 cards are marked closed at Stage 13 (PR #1533 used safe phrasing; not auto-closed from the release PR).

#### v2.07

**Ambient intake automation — one operator-set automation dial, scheduled sweeps, a consolidated digest, and a runtime enforcement hook** — a `novel`-class release that turns the daily-processing cycle from a manual, unguarded ritual into a governed, scheduled, self-reporting capability bounded by a single plain-language ceiling. **#322 (C0 — the dial)** adds an `[automation]` table with `automation_level` (`off` / `recommend` / `bounded_auto`) to `core/config/operator.toml.template`, a workspace-wide ceiling read by all ambient automation per `effective = min(automation_level, per-action max)` — the irreducible-human set is never unlocked regardless of dial position. **#1159 (C1)** specifies a watched ambient inbox drop-zone + content-hash dedup cursor (`core/standards/c1-ambient-inbox-cursor.md`) so the same artifact is never ingested twice. **#1160 (C2 — Path A)** specifies the scheduled capture→classify→register intake sweep over the inbox + drive transcripts/emails (`core/standards/c2-intake-sweep-path-a.md`), clamped by `automation_level` (a `recommend`-ceiling sweep surfaces drafts and writes nothing above Tier 1). **#1161 (C3 — Path B)** specifies the snapshot-diff external-sync (Jira / Confluence / Smartsheet MCP) that reconciles tracker state and proposes **evidence-gated** auto-closes (`core/standards/c3-external-sync-path-b.md`). **#1162 (C4)** rolls every sweep into one consolidated daily-status digest with a **sweep-health heartbeat** so a silent sweep failure is impossible (`operations/skills/daily-status/SKILL.md` + `references/ambient-sweep-digest.md`). **#1163 (C5)** ships a unified payload-triggered PreToolUse **autonomy-ceiling hook** (`core/hooks/block-autonomy-ceiling.sh`, 442 lines, + cache-primer + tests), registered in `core/settings.json.template`, **warn-mode-initial** via `core/hooks/.autonomy-mode.template` — it blocks above-ceiling actions in the payload-detectable Tier-0 classes (governance / cross-domain) once the operator flips it to enforce after shakedown. The C5 reconciliation **R-C5RECON** resolved to this single unified hook (**ADR-031**, Accepted), which **supersedes** the `subagent-security-posture.md §4` subagent-tier design. `novel` class (plan-declared — the milestone carries no `release-class:` label). MODERATE reversibility / HIGH confidence — the config dial, the four C1–C4 specs, and the SKILL.md edit are additive (CHEAP); the C5 PreToolUse hook mutates the runtime guardrail surface and is the moderate element (it ships warn-mode-initial and is operator-flipped to enforce), so whole-release rollback = `git revert -m 1` of PR #1460 + delete the `v2.07` tag, after which `.autonomy-mode` returns to its pre-release state and the hook no longer fires. **Post-deploy operator actions:** run `update.sh` to deploy the C5 hook into the runtime; flip `.autonomy-mode` warn→enforce after shakedown. Dev Testing PASS-with-adjustments + QA 35/35 ACs PASS; Stage 9 Plan Review GO. **Combined Stage 12+13 close** — no separate Stage 12 chore PR landed a DEPLOYED row, so the RELEASE_LOG row is authored directly at VERIFIED (Phase A8 outcome-bound close; the binding is on the output set). The `daily-status.skill` package + content-hash sidecar are present on main (Check 7); runtime mirror sync via `./core/deploy/deploy.sh --deploy daily-status` is the operator/hub post-merge step. PR #1460 merged to main via merge commit `3d3dd508b5042e1bbde97e510ef14e00b7b750d5`, signed-annotated (ED25519) `v2.07` tag at the merge SHA. **Deferred from v2.07:** #1164 (C6 Teams connector) → backlog (unmet governed-Teams-MCP dependency, design in #1441); #296 (K4 onboarding) → milestone #52. Hook-scope observation: #1472.

#### v2.08

**Artifact lineage graph + a recommend-only generated-surface lint** — a `novel`-class release that gives the platform's generated-artifact surface (the `08-Generated/` staging area + promoted folders) a lineage graph and a graph-integrity lint, so duplicate, orphaned, stale, or displaced AI-generated artifacts are surfaced for operator approval instead of silently accumulating. **#334** wires the horizontal-lineage scalar fields **`parent_artifact` / `sibling_topic` / `supersedes`** (with the `supersedes`/`superseded_by` inverse pair) into `core/schemas/frontmatter-schema.md` across **both Domain A (formal-baseline) and Domain C (synthesis)** so an A↔C chain resolves, adds the **lineage-vs-provenance distinction table** fixing the boundary (lineage = horizontal edges *between generated artifacts*; provenance = the *upstream human evidence* an artifact derives from — `source_inputs` scoped out as a separate provenance concept not yet in-schema), records the **field-vs-verb composition note** (the scalars compose against the 7 MVP `relationships[]` verbs — no new enum value, the 7-MVP-type constraint unchanged), and specifies **sidecar `.meta.yml` parity** plus a **WARN-not-BLOCK dangling-lineage rule**; syncs `core/standards/lifecycle-states-canonical.md §3.2` (the `DRAFT → REVIEWED → APPROVED → PROMOTED → ARCHIVED` Artifact Workflow restatement gains the three lineage fields, with the schema-home cross-pointer) and reconciles `core/standards/template-protocol.md` §9 (`topic` → `sibling_topic`). **#1165** authors the NEW **`operations/skills/artifact-lint/`** skill — **Autonomy Tier 1, recommend-only** — running **five graph-integrity checks (orphan / sibling-duplicate / stale-draft / displaced-content / version-chain)** that read the #334 lineage frontmatter and stage a single report to `08-Generated/artifact-lint-YYYY-MM-DD.md` with per-finding proposed action + reversibility tier + confidence + cited evidence, performing **no file moves and no deletes** (the operator approves every action); ships the **17-row work-plan taxonomy** (`references/work-plan-taxonomy.md` — Canonical-10 ≡ the Domain-C synthesis `artifact_type` set + Situational-7 ≡ the plan-type set + waterfall/cutover rows) the displaced-content Check 4 uses as the canonical-target-folder lookup; registered in `OPERATIONS_SKILLS`, shipping its own `.skill` package + `.sha256` content-hash baseline; the per-skill output contract added to `core/schemas/per-skill-output-contracts.md`. **FINDING-1 — dual state-read (the load-bearing input rule):** the state-keyed checks read **`artifact_state` primary → `lifecycle_state` fallback**, because `artifact_state` (the §3.2 Artifact Workflow enum stamped by `artifact-generator`) is absent from `frontmatter-schema.md` while `lifecycle_state` is the schema's REQUIRED field — keying on one silently mis-reads every artifact carrying the other. **FINDING-2 — 17-row taxonomy provenance:** no literal 17-row taxonomy survived in the canonical corpus (the original split card was deleted), so the taxonomy is an **evidence-grounded reconstruction operator-confirmed at Stage 9 Collective Review** as authoritative — Check 4 therefore treats a canonical-home mismatch as a firm (still recommend-only) finding. **Stage 5 caught a third #334 reconcile target** (`template-protocol.md`) beyond the two named at planning. `novel` class (plan-declared — the milestone carries no `release-class:` label), CHEAP reversibility / HIGH confidence (additive-only — a NEW skill + new optional schema fields + a synced restatement; whole-release rollback = `git revert -m 1` of PR #1468 + delete the `v2.08` tag; the new skill has no prior consumer, the optional lineage fields default-absent, and the schema additions have no external reader to unwind; re-run `./core/deploy/deploy.sh --deploy artifact-lint` post-revert to re-sync the skill mirror). **Combined Stage 12+13 close** — no separate Stage 12 chore PR landed a DEPLOYED row, so the RELEASE_LOG row is authored directly at VERIFIED (Phase A8 outcome-bound close; the binding is on the output set). The v2.08 branch merged current main in (incorporating #1461/#1474) before merge; the `artifact-lint.skill` package + content-hash sidecar are present on main (post-main-merge rebuild, Check 7). PR #1468 merged to main via merge commit `17955bc2cee02beac08606ac0f4bd56f1471ca74`, signed-annotated (ED25519) `v2.08` tag at the merge SHA. Forward-binding follow-up: `core/artifact-workflow-protocol.md` (planned co-located protocol doc; §3.2 pointer updates at its Stage 13).

#### v2.06.1

**Deploy `--deploy` detection hotfix** — point release on the v2.06 milestone repairing two `deploy.sh --deploy` detection bugs that v2.06's new content-hash sidecar baselines exposed in the previously-untested `--deploy` path: (1) sidecar mis-enumeration (the `.skill` `sed` was unanchored, so the new `<name>.skill.sha256` content-hash baselines were mis-parsed) and (2) a BSD-`sed` alternation no-op in skill-name detection (the known R1). `cross-cutting` (same milestone), CHEAP reversibility / HIGH confidence — a deploy-script bug repair, additive to the gate apparatus shipped in v2.06; whole-release rollback = `git revert -m 1` of PR #1474. PR #1474 merged to main via merge commit `450b7e15d03774818fce6aa93590a605ea4e5517`, signed-annotated `v2.06.1` tag at the merge SHA. Fixes #1475.

#### v2.06

**CI/deploy gate trustworthiness + parallel-PR safety** — a cross-cutting hardening of the platform's own automated-assertion gates so a green verdict *means* its claimed invariant holds, anchored on a new gate-efficacy contract; plus a hook-registry restructure that ends the parallel-PR cascade-conflict class. **Keystone #1101** authors `core/standards/gate-efficacy-standard.md` — the normative contract for the `deploy.sh --check` + `.github/workflows/*` gates (scoped apart from the LLM-judged pipeline stage-transition gates) — naming three requirements (**assert by content not proxy**, **self-declare required-vs-advisory**, **be auditable for false-confidence + coverage-gap**) plus a red-blocks posture rule; converts **Check 7 (package freshness)** from an `mtime` proxy to a **content-manifest-hash-vs-committed-baseline** assertion (the near-miss: a checkout-reset timestamp passing a stale package green), adds **required-vs-advisory declarations across the 14 workflow jobs**, and brings Checks 6/7/9/37/38 under the standard. **#673** adds a **Check-6 CI mirror** (`skill-canonical-structure-check.yml` invoking the single-sourced predicate `core/deploy/tools/check-canonical-structure.sh` shared with deploy-time Check 6) closing the run-context detection gap where a red-on-main structural breach sat undetected between deploys, bundled with a `daily-status` `references/` reconciliation (v2.01→v2.06). **#18 (ADR-030, Accepted)** breaks the ~32 KB `bypass-mode-readiness.md` monolith into **per-hook drop-in sources** + cross-cutting fragments with the canonical index **generated + committed** by `build-hook-registry.py`, adds a **completeness check (Check 37, advisory/warn-mode)** reconciling every `block-*.sh` against its declared owning doc — making the prior **5/7/9 three-way registry drift structurally impossible** — and an **index-freshness check (Check 38, always-enforce)**, extending Check 9's directory-walk; this kills the parallel-PR cascade-conflict class (concurrent hook-adding PRs no longer collide on one shared file, N=2 within one release). **#90** is a decision spike concluding the pre-commit localized-context layer is **NOT needed** (DC1/DC6 already gated pre-merge; residual stable), closed with a residual-risk register, not code. `cross-cutting` class, CHEAP reversibility / HIGH confidence (additive standard + checks + a documentation restructure into a generated index — whole-release rollback = `git revert -m 1`; the report-only Check 37 blocks nothing and the index regenerates deterministically). A follow-up **v2.06.1** (PR #1474, fixing #1475) repaired two `deploy.sh --deploy` detection bugs the new content-hash baselines exposed in the previously-untested `--deploy` path (sidecar mis-enumeration via an unanchored `.skill` `sed`; a BSD-`sed` alternation no-op — the known R1). **Combined Stage 12+13 close** — no separate Stage 12 chore PR landed a DEPLOYED row, so the RELEASE_LOG rows are authored directly at VERIFIED (Phase A8 outcome-bound close). PR #1461 merged to main via merge commit `a74f814baa736494231fd01bc06fa1c9ea03d32d`, signed-annotated `v2.06` tag at the merge SHA; the v2.06.1 hotfix tag points at `450b7e15d03774818fce6aa93590a605ea4e5517`. Follow-ups filed (reference): #1471 · #1476 · #1477.

#### v2.05

**Agent-discipline codification + the memory↔corpus SSOT boundary** — a cross-cutting cluster of 10 issues that promotes working agreements from operator memory into the durable corpus and establishes the contract for how knowledge graduates. **Keystone #530** ships `core/ADRs/ADR-029` (the *Memory SSOT model*, framed on a four-type memory architecture — Work / Knowledge / People / Learning — scoped to the Knowledge↔corpus cut), `core/disciplines/knowledge-architecture.md` §6 (SSOT assignment + no-shadow-SSOT invariant + ENCODE→ARCHIVE→VERIFY-CORPUS→EVICT lifecycle + three drift classes), a "Single-source-of-truth for knowledge" Universal Preference in `CLAUDE.md`, a warn-mode `deploy.sh` Check 36, and a drift-audit how-to. **#527 / #531** encode untied disciplines (read-before-edit, in-place bias, flag≠consent, +) and operator-flagged toolkit knowledge (engagement discipline, worktree boundary, system/toolchain references) across `git-workflow.md`, `depersonalization-spec.md`, `reversibility-protocol.md`, `initiative-roadmap-framework.md`, `pmo-skill-editor`, and a new `toolchain-operational-reference.md`. **#63** codifies AskUserQuestion timing; **#58 / #502** add failure-mode entries (chip-arithmetic-without-verification; perpetuate-structure-instead-of-redesign); **#426 / #427 / #546** codify repo-integrity authoring guidance, intake assumption-tag downstream resolution, and the advisory Solutioning pre-read convention; **#132** refreshes the CLAUDE.md tree. `cross-cutting` class, CHEAP reversibility / HIGH confidence (additive doc/governance only — whole-release rollback = `git revert -m 1`). Re-versioned v2.03→**v2.05** (mainline advanced mid-window) and the keystone ADR renumbered 028→**029** (main claimed 028). The 3 memory-tied References (#527/#530/#531) closed manually at Stage 13; their staging memories retired operator-side per ADR-029.

#### v2.03

**Role-skill factory foundation + first two pilots** — the role-Specialist factory exists end-to-end and ships its first two products; 2 skill-build cards (Phase 0 foundation + Phase 1 pilots), no application logic beyond the skills themselves. **#186 (Phase 0 — factory foundation)** wires a **feeding-document consumption** path into `release/skills/pmo-skill-refiner/SKILL.md` as the pre-Interview input branch of Mode 2 (Create New, authored via `pmo-skill-editor` discipline on the migrated skill), authors the 14-section format reference `release/skills/pmo-skill-refiner/references/pmo-feeding-document-v02.md`, authors the **5 `operations/skills/_shared/*.md`** building-block files (`behavioral-markers`, `anti-pattern-catalog`, `five-model-variations`, `deployment-strategies`, `lifecycle-gates`) every later role skill consumes, and adds a **fail-CLOSED `core/deploy/deploy.sh` Check 5(a) skip** as an exact-match `== _shared` guard (NOT a fail-open `_*` wildcard) + convention comment so the new `_`-prefixed sibling dir does not trip skill-roster-drift while a future stray `_`-prefixed dir still FAILs unless explicitly allowlisted. **#185 (Phase 1 — pilots + format lock)** builds the 2 pilot skills via `pmo-skill-refiner` consuming their feeding docs — `operations/skills/pmo-technical-program-manager/SKILL.md` (composes `pmo-technical-analyst` + `delivery-engine`) and `operations/skills/pmo-program-coordinator/SKILL.md` (composes `tracker-manager` + `daily-status`) — each composing by invocation (not re-implementation), carrying the `skill_discipline_migrated_v10_2` flag, registered in `OPERATIONS_SKILLS`, shipping its own `.skill` package; then **locks the feeding-document format** (a post-Phase-1 "changes require a governance update" note added to `pmo-feeding-document-v02.md`). Strict serialization **#186 → #185** (format-lock gate — #185 cannot start until #186's foundation exists). **Scope change: #450 (`pmo-architect`) DEFERRED to `04-ROLE-delivery-coverage`** — its compose-the-mode seam converges with a stub until `pmo-principal-engineer` (#1116) ships in milestone 04; decision recorded on #450. `novel` class (Standard engagement / Deep Stage-9 review / 30-day outcome window), CHEAP reversibility / HIGH confidence (additive-only — whole-release rollback = `git revert -m 1` of the release PR; the 5 `_shared/` files have no prior consumer to unwind, the deploy.sh guard reverts cleanly; re-run `./core/deploy/deploy.sh --deploy` post-revert to re-sync the skill mirrors). **Combined Stage 12+13 close** — no separate Stage 12 chore PR landed a DEPLOYED row, so the RELEASE_LOG row is authored directly at VERIFIED (Phase A8 outcome-bound close; the binding is on the output set). The 3 `.skill` packages (`pmo-skill-refiner` rebuilt + the 2 pilots built) were confirmed fresh on main at Stage 13 (a Stage-13 rebuild reproduced byte-identical content — no package re-commit). The 2 cards are marked closed at Stage 13 (PR #1280 used `References #186, #185`; not auto-closed from the release PR). PR #1280 merged to main via merge commit `cc094b4409825ed95f2e1dd1d60f7b5b4e9aa4be`, signed-annotated `v2.03` tag at the merge SHA. 30-day Stage 13 outcome window per the `novel` class.

#### v2.02

**Bundling capacity + sizing/parse-rate gates** — the pipeline's Bundle stage enforces capacity and sizing as gates, not guidance; 4 governance/spec/schema cards (no application logic). **#290 (capacity TARGET)** adds a **risk-weighted release-capacity model** to `bundle-composition-doctrine.md §3 Step 5` where a higher-ceremony release's raw story points count for more against the same band (`effective_pts = round_half_up(Σpts × class_weight)`), **consolidates** the previously-scattered sizing guidance (item-count vs point-band) into that one cross-referenced source, **pins the rounding mode** `round_half_up` at the definitional home, and ships a `release_class_capacity_weights` inline table `{routine 1.0 / novel 1.15 / cross-cutting 1.3 / hotfix 0.9}` as explicitly-labelled MEDIUM-confidence `[CALIBRATE-AFTER-3]` seeds in `platform-config.toml.template` (+ config-schema doc + Stage-3 A4 cross-ref + `deploy.sh` Check 33 allowlist extension so the field is gated). The establishing **ADR-027 (Accepted)** records the risk-weight-keys-on-Release-Class decision (lowest-blast-radius canonicalization over a net-new per-issue Tier field). **#294 (per-milestone size gate — G3-15)** asserts `effective_pts ≤ band-upper` at a numbered gate in `gate-criteria-spec.md` and routes an oversize milestone to the split / tight-merge disposition (non-dead-end), with the enforcement cross-cite added to doctrine §3 Step 5 (serialized after #290). **#281 (RELEASE_LOG velocity instrument)** adds an additive velocity convention capturing per-release planned-vs-delivered points, files-changed, and allocation actuals alongside the already-shipped `Cycle-Time:` field — codified in NET-NEW `release/references/standards/release-velocity-tracking.md` (field schema + label→work-class map + N/A semantics + N=3 recalibration linkage) with the producing tool `release/tools/compute-release-velocity.sh`; the data instrument that feeds #290's recalibration. **#293 (Mode-A parse-rate gate — G3-14)** holds the `release-planner` Mode A dependency-graph parse rate to a **≥90% floor** against the conformant-bundleable denominator before Stage 3/4 may pass (documented operator override), with the Mode A AC cross-ref added to `release-planner` SKILL.md. `gate-criteria-spec.md` schema bumped v1.11 → v1.12 additive; warn-then-enforce gates with `[CALIBRATE-AFTER-3]` thresholds; gate-ID set {#293 → G3-14, #294 → G3-15, #292 → G3-16 reserved}. `novel` class (new RELEASE_LOG velocity instrument + two new gate criteria + multiple D-class planning decisions), CHEAP reversibility / HIGH confidence (additive-only — whole-release rollback = `git revert -m 1` of the release PR; every addition ships a default and no existing field/row/enum is mutated; ADR-027 + `release-velocity-tracking.md` have no external reader to unwind; re-run `./core/deploy/deploy.sh --deploy release-planner` post-revert to re-sync the skill mirror). **Combined Stage 12+13 close** — no separate Stage 12 chore PR landed a DEPLOYED row, so the RELEASE_LOG row is authored directly at VERIFIED (Phase A8 outcome-bound close; the binding is on the output set). The `release-planner.skill` package is rebuilt at Stage 13 (Check 7 package-freshness, resolving the deferred staleness). The 4 cards are marked closed at Stage 13 (not auto-closed from the release PR). PR #1278 merged to main via merge commit `a081fec36b6e5551df8564d7fa2457e27d8f00c5`, signed-annotated `v2.02` tag at the merge SHA. 30-day Stage 13 outcome window per the `novel` class.

#### v2.01

**Skill-function hardening (estimation / lifecycle / status)** — seven existing function-skills deepened across delivery-engine, ppm-agent, tracker-manager, weekly-status-rollup, daily-status, pmo-process-designer, and pmo-technical-analyst; 12 EXTEND-class stories, no new skills. The whole release is **enforcement-wiring, not logic-invention** — every story adds discipline-enforcement to skills whose reference substrate (estimation-standards, lifecycle-stages, gate-definitions, metric-registry, watermelon-detection, intake-governance, escalation-thresholds) was already built. **delivery-engine** (5-card serial chain on one SKILL.md) gains **estimation enforcement** establishing buffer/variance vocabulary (#264), **15-stage lifecycle tracking** (#265), **10-gate lifecycle enforcement** with PASS/CONDITIONAL/FAIL verdicts sitting between the stages (#266), a **tech-debt capacity floor + aging + rework-rate** with a "not computable" fallback when the rework-input source is absent (NET-NEW `references/tech-debt-capacity.md`, #366), and **Fowler-quadrant classification + Cost of Delay** ranking against that floor with a lexicographic sort (NET-NEW `references/tech-debt-classification.md`, registers a `framework-catalog.md` row, #180). **ppm-agent** (2-card serial chain) gains **intake governance + WSJF scoring** (NET-NEW `references/ppm-intake-governance.md`, renamed to avoid the intake-desk collision; WSJF consumed by reference, #253) and **org-model detection** (from `delivery_approach`) **+ stale-RAID escalation** wiring the existing `escalation-thresholds.md` (#254). **tracker-manager** gains **RAID deduplication** (≥0.70 similarity) **+ cascade detection** (#251). **weekly-status-rollup** gains **watermelon detection** consumed **via-public-api** from the canonical 8-signal `watermelon-detection.md` (owned by `pmo-qa-auditor` — referenced, not forked) **+ metric governance**, with an optional `investment_class` project-template field (absent → "Unclassified" coverage gap) (#256). **daily-status** gains **formula-driven RAG + buffer tracking**, wired to existing registries with no new `references/` dir (SKILL-only, #260). **pmo-process-designer** gains **INVEST scoring + Given-When-Then + NFR taxonomy** (ISO/IEC 25010) quality enforcement (#263). **pmo-technical-analyst** gains **ADR-immutability + FDD quality scoring** (4.0/3.0 thresholds) **+ Accelerate/DORA metrics** (#259). A supporting **ADR-028** (formerly ADR-024) records the #256 via-public-api-not-fork decision (operations consume a core safety-control by role-name rather than copying it), reversing the Stage-4 local-copy recommendation. `novel` class (≥1 new reference doc + ≥1 D-class decision; cross-cutting count-gates not met), CHEAP reversibility / HIGH confidence (additive-only — whole-release rollback = `git revert -m 1` of the release PR; the 3 NET-NEW reference docs + ADR-028 have no external reader to unwind; re-run `./core/deploy/deploy.sh --deploy` post-revert to re-sync the 7 skill mirrors). DT/QA: 12/12 PASS, 72/72 ACs. The 12 cards are marked closed at Stage 13 (not auto-closed from the release PR). PR #1240 merged to main via merge commit `97b66e12dd82ecb2131614577d9acf9314ba206e`, signed-annotated `v2.01` tag at the merge SHA. 30-day Stage 13 outcome window per the `novel` class.

#### v2.00

**Skill-function hardening** — five existing function-skills deepened across change-management, release-executor, pmo-qa-auditor, artifact-generator, and release-planner; 11 build cards, all EXTEND except the release-planner RECONCILE card (no new skills). **change-management** gains a named **Mode G (Adoption Tracking)** surfacing ADKAR barrier-assessment + training-timing validation (#1127), champion-ratio / sponsor-coverage / valley-of-despair prep (#1128), and change-fatigue + outcome measurement (#1129) — all consuming the single-source change-methodology framework by reference (no duplicated scale/rule); and a **Mode D (Hypercare)** upgrade — a risk-classification step with a stable 3-tier set (HC-T1 Critical / HC-T2 Elevated / HC-T3 Routine, scored Severity × Likelihood × Blast-Radius; Severity+Likelihood adopted by reference from `delivery-engine/references/raid-templates.md`, only Blast-Radius net-new) (#1130) plus the tracking+reporting half on top of it: each hypercare incident recorded as an existing-prefix RAID entry with a met/breached verdict against the tier's committed SLA, a per-tier compliance report, and an **open SLA breach as a hard, non-waivable Mode-D exit gate** (#1131). **release-executor** gains a canonical 3-tier **Quality-Gate Ladder** (T1 schema validation → T2 cross-reference integrity → T3 stakeholder approval) that fires in order and short-circuits on the first hard fail, wired at both execution entry points (the git-native PR-merge pre-execution surface and the Cowork snapshot-apply path) from one source and reusing existing instruments (`lint_release_corpus.py --check schema` + `check-doc-links.py`; an unverifiable surface is a FAIL) (#244); a 3-cycle **progressive-rollout** model (shadow → warn → enforce) wrapping the ladder via a per-gate `rollout-cycle` attribute (default + fail-safe `enforce`; per-rule append-only outcome-log; operator-gated Advance Checklist; only an `enforce` gate short-circuits) (#245); and a **Mode D (Close Release)** documented build-out (not a rebuild) — an explicit issue-closure audit (non-zero open-issue count = blocking finding), carry-forward / deferred-item tracking as a named Step-6 output reported every close, and a new close-out checklist reference doc (#212). **pmo-qa-auditor** (P1) gains an **8-detector failure-mode battery** (automation complacency / faceless PMO / echo chamber / quality drift / SPOF / breadth burnout / AI hallucination / trust erosion) authored to the failure-mode 5-field template with corpus-precedent thresholds, plus a **RACI validation gate (G9)**; the auditor's own Domain-Specific Failure Modes section is untouched and the mode count stays 5 (#248). **artifact-generator** gains **lifecycle state tracking + zombie detection** adopting the platform-canonical Artifact Workflow vocabulary — the field **`artifact_state`** (replacing `status`) with the canonical 5 states DRAFT → REVIEWED → APPROVED → PROMOTED → ARCHIVED stamped DRAFT on emit, a documentation-debt register listing stale (>30-day, non-`ARCHIVED`) artifacts with recommended actions, and `references/lifecycle-states.md` re-specced as an application layer over the canonical state machine (the contained field rename verified to break no external reader) (#252). **release-planner** gains **typed artifact linking** — each in-release plan edge labelled with one of four §Category-4 relationship types (**GENERATES / DEPENDS_ON / BLOCKS / SUPERSEDES**) adopted verbatim from the ADR-018-frozen `frontmatter-schema.md` vocabulary, added as a **second, independent edge attribute** that the CPM longest-path math never reads (provably additive), with `bundle-issues-parser.py --json` read-only-extended by an additive `artifact_relationships` field (#246). `novel` class (≥1 new reference doc + ≥1 D-class decision; cross-cutting count-gates not met), MODERATE reversibility / HIGH confidence (whole-release rollback = `git revert -m 1` of the release PR; the one behavior change to unwind is the artifact-generator `artifact_state` field default, contained — no other skill reads the value; re-run `./core/deploy/deploy.sh --deploy` post-revert to re-sync the 5 skill mirrors). Key D-decision **D-246-Blocker resolved INCLUDE** — #246's "Blocked by #409" edge was reconciled-stale (the card needs only the already-frozen 7-type relationship vocabulary; #409 stays an open epic that does not close this release). **Version mapping:** the plan was drafted against the v3.x line (`.version` v3.19, latest tag v3.20) and **re-versioned to v2.00 at Engineering Commit 0** per the repo re-versioning lineage; `.version` reconciled v3.19 → v2.00 in the Stage 13 chore PR. The 11 cards are marked closed at Stage 13 (the release plan phrases per-card closure SAFE; not auto-closed from the release PR). PR #1200 merged to main via merge commit `42ee22cf1279ffff1c0172d95de092a159025baa`, signed-annotated `v2.00` tag at the merge SHA. 30-day Stage 13 outcome window per the `novel` class.
