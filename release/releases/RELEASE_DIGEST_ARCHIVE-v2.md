<!-- repo-integrity: allow-issue-ref -->
<!-- reference-durability: allow-link -->
<!-- reference-durability: allow-url -->
# RELEASE_DIGEST_ARCHIVE-v2

Archive segment of [`RELEASE_DIGEST.md`](RELEASE_DIGEST.md) — the **v2** release family.

This file is the same record as its parent ledger, relocated. It is a **Vital**
record under `core/governance/RECORDS_POLICY.md`, retained permanently, and it
inherits its parent's class: a segment is a disposition *destination*, never
itself a disposition *source*. Nothing here is a lesser record for having aged
out of the working set.

It lives in the same directory as its parent deliberately, so `grep -r` over
that directory still finds this content exactly as it did before the move. Each
entry below retains its heading in the parent ledger, with a pointer here.

Entries are appended by `release/tools/sweep-release-corpus.py`; the file is
append-only and is never itself swept.

---
### v2.41 (2026-06-29) — Governance-hygiene: the root-cause-analysis method is wired by citation to every failure-owning surface its own trigger table named, the hub's rule-computed version step stops surfacing as an operator click-gate, and two reference-doc accuracy fixes land. `routine`-class; slim cherry-picked milestone (release-corpus migration sliced off to #1549) — documentation / governance / skill-text only, no behavior / schema / runtime change.

A `routine`-class release on milestone #242 `99-rca-and-corpus-anchor-hygiene` (4 issues — #1562 / #1883 / #1918 / #2218; bump-class minor) — a **slim** release carrying only the four independent RCA-coverage + corpus-anchor-hygiene fixes, cherry-picked onto current `main` (off `origin/main` after v2.39/v2.40 shipped) and re-prefixed v2.39 → v2.41 with zero cherry-pick conflicts; the release-corpus public-vs-instance migration was sliced off to the corpus-adapter seam (#1549) and deferred, so the release PR touches no `release/releases/` corpus content. **RCA coverage extension (#1883):** the invokable root-cause-analysis method (`core/disciplines/root-cause-analysis.md`, shipped via #754) was invoked or handed-off at only four surfaces; this wires it by citation to the surfaces its own §2 trigger table named but that lacked a recorded binding — `ppm-agent` (invoke-or-hand-off when a processed defect/blocker surfaces), `pmo-devops-sre` (originate an RCA record on a regression that triggers rollback, Mode 3), and the failure-owning pipeline stage(s) — extending §4 invocation + §7 consumer-binding alongside `five-function-spine-and-process-flows.md` Change Control Flow, `operations/skills/ppm-agent/SKILL.md`, `release/skills/pmo-devops-sre/SKILL.md`, and the `release-process.md` feedback/rollback lines; coverage-completion of an already-decided method (ADR-016) — binding by citation, no new ADR; the review-class linkage decision (review skills stay format-linked via `review-discipline-principles.md` §2, not method-linked) is recorded. **D-Version recorded determination (#1918):** `release/references/how-to/hub-spoke-bridge.md` § Recurring D-decisions reframes D-Version from a full A/B/C click-gate to a **recorded determination** by default — stating the bump-class, the rule-computed provisional number (`anchor()` + bump-class floor → next-free), and the re-verify rungs (Engineering Commit 0 + the Stage 12 atomic claim) without surfacing an operator gate — escalating to an actual operator gate only on (B) a deliberate version-less / forced-collision close-out or (C) a known concurrent release claiming the computed slot, with the rule-determined-vs-judgment distinction stated explicitly (D-Version rule-determined, D-ReleaseClass judgment); the (B)/(C) override valves remain documented and reachable, and the version-computation rule itself is unchanged. **Extinct-path correction (#1562):** `core/standards/practice-efficacy-framework.md` drops the extinct `pmo-platform/engineering/tools/` directory prefix in a Drift practice-table cell → the bare `health-check.sh` form the same file already uses at §9, leaving zero `pmo-platform/engineering/` references. **Corpus-hygiene sweep (#2218):** (a) the malformed escalation-cap cell in `core/schemas/handoff-coordinator-spec.md` (`(DT↔Engineering, )` with a dangling empty parenthetical + a double-space) is corrected to `(DT↔Engineering)` with single spacing, and (c) `core/ADRs/README.md` now enumerates every PR-time durable-corpus integrity/durability gate + its override marker (allow-memory-ref / allow-link / allow-version-ref / allow-url / the no-marker bare-`#N` reference-block positional rule), with a spoke-facing pointer from `hub-spoke-bridge.md` Procedure 3 + a cross-ref to `reference-durability-standard.md`; sub-finding (b) (a Check-33 spec-vs-reality divergence) was trimmed — it does not reproduce at HEAD (the spec and `deploy.sh` already agree). All four changes are documentation / governance / skill text — additive bindings, link corrections, and gate-marker enumeration; no behavior, schema, or runtime-check change. Merged via merge commit `5bfffe1e2bb4a2265f1d3a39ff043557b66e5b06`; signed-annotated `v2.41` tag at the merge SHA; GitHub Release published with the frontmatter-stripped note body. **Version note:** authored as a provisional v2.39 on the theme-named branch `release/v2.41-rca-coverage-and-hygiene` and re-prefixed to v2.41 (v2.39/v2.40 having shipped from concurrent siblings); v2.41 was free at the claim and no version was abandoned by *this* release (no `RELEASE_REVERSIONS.md` row — the re-prefix was a clean cherry-pick onto post-v2.40 `main`, not a mid-pipeline tag collision). The corpus close-out — the RELEASE_LOG row recorded directly at VERIFIED (no DEPLOYED-row Stage-12 chore PR landed for this slim release), the `.version` stamp (v2.40 → v2.41), the CHANGELOG append, the RELEASE_INDEX/DIGEST entries, the release note, and the `chore/v2.41-stage-13-corpus-update` branch — was assembled via the governed Phase B chore-PR fallback per `stage-13-close.md` § Phase A8 (the automated close-out tool's preflight could not run — no DEPLOYED row had landed for v2.41). #1562 / #1883 / #1918 / #2218 closed at Stage 13 with verification-evidence comments; milestone #242 closed at Stage 13 by the hub. CHEAP reversibility / HIGH confidence — additive / corrective documentation, governance, and skill-text edits; no data migration, no schema/behavior change to existing surfaces, no breaking change; whole-release rollback = `git revert -m 1` of PR #2438. 30-day Stage-13 outcome window per the `routine` class.

### v2.40 (2026-06-29) — Transcript & meeting processing: the implicit transcript-handling flow is formalized as one named pipeline with explicit automation boundaries, and meeting materials get a governed lifecycle state plus a scannable redesign. `routine`-class; formalizes existing flow + adds one tracker field + scannable meeting materials — no data migration, no stored-content move, no new project-facing primitive.

A `routine`-class release on milestone #189 `17-per-project-processing-orchestration` (4 issues — #237/#1157 transcript-pipeline pair + #243/#1158 meeting pair; bump-class minor; planned 8 pts / delivered 8 pts) — **D-C SINGLE** topology, one release PR (#2426) preserving the 4-commit history. **Three-tier automation boundaries (#237):** `core/governance/OPERATIONS.md` § Transcript Processing Protocol § Three-Tier Output Model sorts transcript-derived outputs into **auto-write** (Document Tier 2 operational trackers — flow without a per-output prompt), **approval** (Document Tier 1 stakeholder-facing / RAID changes — stop for operator approval), and **user-triage** (action items surfaced for disposition, never auto-logged on the operator's behalf); `operations/skills/ppm-agent/SKILL.md` tags each output block with its tier so the downstream write path knows whether it flows, stops, or surfaces — composing with the existing global Document-Tier rules rather than duplicating them. **Named pipeline stages (#1157):** the same section names the implicit flow as one pipeline — **Entry → Routing → Core-Tracker → PPM → Stakeholder-Review → Action-Triage** (with a terminal resolution gate) — each stage cross-referencing its Daily-Processing-Cycle step number(s) and using the five object-typed Context-Lifecycle state names verbatim from `core/disciplines/context-lifecycle-model.md §2` (`Context-Captured` / `Context-Structured` / `Context-Reviewed` / `Context-Decided` / `Context-Closed`, no aliases); Stakeholder-Review and Action-Triage are promoted from prose buried in the cycle's approval/next-step steps to first-class named stages, applied uniformly across all projects (the shared 01-08 layout means no per-project variants). **Meeting lifecycle state (#243):** `core/schemas/tracker-schemas.md` adds a `lifecycle_state` field to the Open Meetings Tracker — the **frozen 3-state** Axis-1 machine `scheduled → held | cancelled` (V-MTG-05 enum), with the human-readable `Status` display field retaining richer sub-steps (e.g. `NEEDS SCHEDULING`) via an explicit display↔governed map; the stage-transition rules + evidence bar (`held`/`cancelled` terminal, no re-open, same bar as a tracker CLOSE) are defined and `ppm-agent` meeting extraction sets the entry state (Cascade B). A reconcile note pins the canonical 3-state machine against a legacy 6-stage proposal so a body-only reader cannot build a competing 6-state machine. **Human-behavior-aware meeting content (#1158):** `core/standards/meeting-agenda-format.md` + `core/standards/meeting-recap-format.md` + `operations/skills/comms-writer/SKILL.md` (+ `references/audience-profiles.md`) redesign pre-reads, agendas, invites, and context blocks for a 5-second scan — no pre-read references a raw transcript without an inline summary, and every invite passes a scan test, so an operator absorbs what a meeting needs from them at a glance instead of opening source files. Merged via merge commit `9b50b403fa5ea5cf5ba11dc42a6f45b3b934dc01`; signed-annotated `v2.40` tag at the merge SHA (SSH signature present), green CI (24/24 checks). **Version note:** planned provisional **v2.39** at the `routine` bump-class on the theme-named branch `release/v2.39-per-project-processing-orchestration` (defer-to-merge — the durable declaration is the bump-class, not a number); at the Stage 12 atomic claim v2.39 was already taken by a concurrent sibling (`60-audit-cadence-and-learning`, release PR #2421 — its signed `v2.39` tag was pushed at Phase B3 but its GitHub Release was not yet published, so `releases/latest` still read v2.38 and the freeness probe surfaced the collision at Phase A.5.6), so the atomic claim bound to **v2.40** above the claimed `v2.39` tag (recorded in `RELEASE_REVERSIONS.md` with one abandoned row, v2.39, no tag cut by this release). The commit messages + branch name retain the historical `v2.39` provisional — preserved, not rewritten; the binding is the `v2.40` tag plus the RELEASE_LOG row. #237 / #1157 / #243 / #1158 closed at Stage 13 with verification-evidence comments; the Stage-6/12/13 sub-tasks #2404 / #2410 / #2411 closed; milestone #189 closed at Stage 13. The `.version` stamp (v2.38 → v2.40), the CHANGELOG append, the RELEASE_LOG/INDEX/DIGEST entries, the release note, and the `chore/v2.40-stage-13-corpus-update` branch are the Stage 13 close-out completions assembled in this Stage 13 close-out (the automated close-out tool's preflight could not run — no DEPLOYED row had landed for v2.40 — so the canonical output set was produced via the governed Phase B chore-PR fallback per `stage-13-close.md` § Phase A8). Deployable skill changes (`comms-writer`, `ppm-agent`) require an install-sync (`deploy.sh --deploy` + `.skill` rebuild) on the operator's primary checkout — handed off (Layer-2 install-sync runs outside the release worktree). CHEAP reversibility / HIGH confidence — additive governance/schema/standards edits plus reviewed edits to two skills; no data migration, no schema/behavior change to existing surfaces, no breaking change; whole-release rollback = `git revert -m 1` of PR #2426 (the skill edits additionally revert via the skill-edit path + package rebuild). 30-day Stage-13 outcome window per the `routine` class.

### v2.39 (2026-06-29) — The platform now audits itself on a defined cadence and records where its advice differed from your call

A `novel`-class release on milestone #151 `60-audit-cadence-and-learning` (3 issues — #167/#168/#46; effective 18, band 15–25). Two new protocols give the release process-fitness audit (#167; PMBOK 7 / DORA / Stage-Gate / ITIL 4 / Lean / CD) and the structural audit (#168; Diátaxis / NARA / ISO 15489 / orphans / cross-refs) documented *when-to-re-run* cadences — named event triggers + a 90-day fallback, sitting atop `AUDIT_FRAMEWORK.md`'s how-to-run methodology — and `platform-health-audit-framework.md` gains a sibling cross-reference establishing the **3-axis audit-cadence set** (Base-vs-Build + process-fitness + structural). #46 adds a signal-only `recommendation-choice-delta` `event_subtype` (the rec/chose/delta/why + via 4-tuple in the existing 10-column payload), a `--window` trailing-N-versions look-back read in `query-pipeline-event.sh`, and a Stage-13 **Phase A7.1** roll-up into the F7 retro / lessons-learned register; ≥3 same-pattern `diverged` rows promote to an `improvement.yml` CANDIDATE via the governance gate, never an auto-change. Integration PR #2421 merged at `711f81d`; signed-annotated `v2.39` tag at the merge SHA; green CI. **Stage 12 was completed as a remediation** — the initial hub-driven merge skipped the B3 tag-claim + B5 DEPLOYED-row steps; both completed 2026-06-29 (tag + chore PR #2430), root cause + the durable Mode-O routing fix tracked in #2428. #1097 trimmed from the milestone; #912 deferred (different capability); the per-session-retrospective idea filed #2423 for a later release. CHEAP reversibility / HIGH confidence — additive protocol docs + one signal-only `event_subtype` + a read-only tool flag + a reference-shard edit; whole-release rollback = `git revert -m 1` of PR #2421. 30-day Stage-13 outcome window per the `novel` class.

### v2.38 (2026-06-28) — Finishing a release is now reliable and produces the same complete record every time

### v2.36 (2026-06-28) — Triage-and-bundling signals: six Stage 1–6 triage/bundling judgment calls the release pipeline used to re-derive each run — how an approved ticket's structure is treated downstream, how priority is read, when related issues are grouped, and how sub-tasks are tracked — are converted into codified pipeline outputs, and one repeatedly hand-derived step (the native-dependency mirror) is promoted to a tracked tool. `novel`-class; additive throughout (spec/governance/schema edits plus one new tool and one enforcing gate) — no data migration, no stored-content move, no behavior change to any project-facing surface.

A `novel`-class release on milestone #148 `69-triage-and-bundling-signals` (6 issues — #500/#283/#662 on the serialized `stage-02-triage.md` contention chain + file-disjoint #280/#292/#225; effective 23, band 15–25, G3-15 PASS) — **D-C SINGLE** topology, one release PR (#2284) preserving the 12-commit history. **Directional-not-authoritative handoff (#500):** `release/references/pipeline/stage-02-triage.md` §4 Inputs states that an approved ticket authorizes the problem/outcome, not its structural premise — downstream stages treat the ticket structure as a proposal to confirm or overturn at Solutioning; `stage-01-intake.md` §6 Outputs carries the forward-pointer and `core/schemas/stage-io-contracts.md` records the Stage 1 → Stage 2 boundary. Principle statement only — the enforcing gate is #501 on milestone `68-stage-gate-criteria-completeness`; composed-with, not duplicated. **Priority-model decomposition (#283):** `stage-02-triage.md` splits priority's former triple duty (urgency / importance / scope) into urgency vs value with a dependency-count triage signal, and `.github/ISSUE_TEMPLATE/improvement.yml` is reconciled to match; the public corpus stays methodology-neutral — no WSJF or other formula is hardwired. **Native-dep mirror → tracked tool (#662):** the Stage 2 A3.5 native-dependency mirror, previously re-derived each run, is promoted to a tracked tool (AS2) at `release/tools/native-dep-mirror.py` with a fixture test under `release/tools/tests/` — a line-directional parser that ignores relates-to/sibling edges and is comma-safe against real-format input. **Cluster threshold + tracking selection (#280):** `stage-03-bundle.md` documents a Stage-3 related-issue-cluster detection threshold and a tracking-mechanism selection rule over the existing epic/cluster/sub-issue primitives — a rule over what already exists, not a new primitive. **A6 per-milestone + gate G3-17 (#292):** `release/governance/release-process.md` makes the Stage-3 A6 New-Track Placement Rationale fire per-milestone rather than once per MAJOR track, and `core/schemas/gate-criteria-spec.md` adds the enforcing gate **G3-17** (G3-13 retired per ADR-012; G3-14/G3-15 shipped via closed #293/#294; G3-16 reserved for the cross-milestone gate); this edit changed `release-planner`'s source and triggered its release-cut package rebuild. **Sub-task container threshold (#225):** `stage-06-engineering.md` defines a threshold for the Stage-6 sub-task container choice (formal GitHub sub-issues vs a lighter PR checklist), sitting on a forward edge to #213 (milestone `74-controlled-deployment-environment-and-modes`) and shipping regardless of #213's state. Merged via merge commit `1a1d51af6f96875f782ce40325a5ba9e231e7643`; signed-annotated `v2.36` tag at the merge SHA ("Good signature"), green CI (25/25 checks); GitHub Release published and marked Latest with the frontmatter-stripped note body. **Version note:** planned provisional **v2.33** at the `novel` bump-class on the theme-named branch `release/69-triage-and-bundling-signals` (defer-to-merge — the durable declaration is the bump-class, not a number); three concurrent siblings claimed v2.33 / v2.34 / v2.35 during the run, so the Stage 12 atomic claim bound to **v2.36** above the shipped anchor v2.35 (v2.36 free at claim time), recorded in `RELEASE_REVERSIONS.md` with one abandoned row (v2.33, no tag cut). The commit messages retain the historical `v2.33` provisional — preserved, not rewritten; the binding is the `v2.36` tag plus the RELEASE_LOG. The `release-planner` `.skill` rebuild (AI-1), the `.version` stamp (v2.35 → v2.36), the CHANGELOG append, the RELEASE_LOG/INDEX/DIGEST entries, the release note, and the `chore/v2.36-stage-13-closeout` branch are the Stage 13 close-out completions the automated tool drops for theme-named (non-version-named) milestones, completed in the Stage 13 chore PR. #500 / #283 / #662 / #280 / #292 / #225 closed at Stage 13 with verification-evidence comments; the Stage 12/13 sub-tasks #2278/#2279 closed; milestone #148 closed at Stage 13. Post-deploy verification: AI-1 cleared the `release-planner — source content changed` FAIL (`deploy.sh --check` now reports `OK: release-planner (content current — staged rebuild matches committed baseline)`); two pre-existing, out-of-scope hard FAILs remain on main and are NOT this release's scope (`delivery-engine — source content changed`; `roadmap-curator — .skill package missing`, the latter introduced by the v2.34 sibling via #453) — neither skill was touched by v2.36. CHEAP reversibility / HIGH confidence — additive spec/governance/schema edits plus one new tool; no data migration, no schema/behavior change to existing surfaces, no breaking change; whole-release rollback = `git revert -m 1` of PR #2284. 30-day Stage-13 outcome window per the `novel` class.

### v2.35 (2026-06-27) — Agent decision-confidence: a written protocol lets an autonomous agent calibrate its own confidence at a non-obvious decision point and decide proceed / pause-to-learn / escalate — from observable cross-check agreement rather than self-reported confidence, scaled to how reversible the action is, with a bounded learn-loop that must inject new outside information and an anti-theater guard that flags a pause which learns nothing. The first live consumer is the release pipeline's Stage-4 bundle-currency check. `routine`-class; additive throughout (a new spec, a new ADR, and four edits wiring the gate into existing disciplines and one pipeline stage) — no data migration, no stored-content move, no breaking change.

A `routine`-class release on milestone #248 `104-agent-decision-confidence` (a Define→Create lean slice — Define task #2283 spec+ADR via sub-tasks #2286/#2287; Create story #2285 build+integrate via sub-tasks #2288/#2289/#2290; Research #1612 done upstream; bump-class minor) — shipped as five separate integration PRs (#2336 / #2337 / #2348 / #2350 / #2352) rather than one release PR. **Define — spec (#2286):** `core/specs/decision-confidence-protocol.md` operationalizes the proceed-vs-pause-vs-escalate question as a compose-not-redefine layer over five existing docs. Its three load-bearing parts: (1) a **confidence signal** collapsed to three ordinal states (`CONVERGENT` / `DIVERGENT` / `UNGROUNDED`) from three observable sources — cross-check agreement (CS-1), gap-namability (CS-2), and the weakest evidence label (CS-3) — with verbalized self-confidence named and rejected as the untrustworthy input; (2) a **reversibility × autonomy threshold matrix** selecting PROCEED / PAUSE-TO-LEARN / ESCALATE per cell, refusing a single global numeric cutoff, guarded by two cross-axis invariants (I1: IRREVERSIBLE never auto-proceeds on a non-`CONVERGENT` signal; I2: the matrix lowers ceremony but never raises an action's autonomy tier — confidence is a brake, not an accelerator); and (3) a **bounded pause-to-learn loop** (default 1 cycle, hard cap 2) whose gap-closer imports the discovery-discipline knowability triage and whose four exit conditions (resolved / not-knowable-now / budget-exhausted→escalate / cost-changed) guarantee termination. A six-domain application table (Deferrals, Estimations, Slicing, Internal Knowledge Depth, Tool Use, Design & Architecture) and four FM entries (self-confidence smuggling, unbounded pause, global-cutoff regression, self-elevation via the gate) round it out. **Define — ADR (#2287):** `core/ADRs/ADR-047-decision-confidence-compose-posture.md` records the extend-and-compose decision (a layer across the decision-quality docs, not a standalone discipline) with a per-doc relationship table agreeing cell-for-cell with the spec's §7; the slug is disambiguated from the staleness-confidence ADR (artifact-staleness vs agent self-competence confidence). **Create — gate + loop (#2288):** `core/disciplines/autonomous-execution-model.md` registers a Pause-to-Learn Pattern as the third pre-action sibling to Retry and Escalate — competence-anchored and self-closing (its default resolution is the agent closing the gap, not handing the decision to the operator), firing before an above-CHEAP action when grounds are weak and chaining to Escalate only on budget-exhaustion; the companion label→gate edit in `core/specs/reversibility-protocol.md` promotes its confidence-pairing into an active self-gate supplying the cost-of-error axis. **Create — live consumer (#2289):** `release/references/pipeline/stage-04-planning.md` Phase A0 runs the proceed-vs-pause gate before rendering the Bundle Mutability refresh outcome (no-op / amend / re-bundle / defer), computing the signal, consulting the matrix, and running the bounded loop (re-read the milestone description, re-walk the dependency state via `gh`, re-run the refresh-trigger scan) when routed to PAUSE — additive to the existing A0 currency checks, no new trigger/path/escalation surface, cutover-gated to releases entering Stage 4 after this introducing release. **Create — anti-theater guard (#2290):** `core/disciplines/decision-discipline.md` adds G8, extending the G1–G7 ceremony-management posture to the pause loop — a pause is load-bearing only if it can demonstrate all three observable signals (a specific new external fetch was injected, the loop stayed within its cycle cap, it terminated on a named exit condition); a pause that cannot is a guard violation, with a co-located falsification test-case pair. Merged in strict order #2336 → #2337 → #2348 → #2350 → #2352 to main; signed-annotated `v2.35` tag at the final merge SHA `3bfd391ad4b6dfd466f59d8b79b82691d0444571` ("Good signature"), green CI on every PR; GitHub Release published with the frontmatter-stripped note body. **Version note:** v2.34 was taken by a concurrent sibling (`97-knowledge-and-decision-confidence`, roadmap/lenses theme) so the atomic claim bound to **v2.35** above the shipped anchor (v2.35 free at claim time); the `.version` stamp + CHANGELOG append + RELEASE_LOG/INDEX/DIGEST entries + the release note are the Stage 13 close-out completions the automated tool drops for theme-named (non-version-named) milestones, completed in the Stage 13 chore PR. #2286 / #2287 / #2288 / #2289 / #2290 (and parents #2283 / #2285) closed at Stage 13 with verification-evidence comments; milestone #248 closed at Stage 13. CHEAP reversibility / HIGH confidence — a new spec, a new ADR, and additive edits to existing discipline and pipeline docs; no data migration, no schema/behavior change to existing surfaces, no breaking change; per-deliverable rollback = `git revert` of the corresponding PR. 30-day Stage-13 outcome window per the `routine` class.

### v2.32 (2026-06-27) — Skill-registry identity and currency: the already-shipped skill registry (`core/skills/registry.md`, ADR-038) is made self-checking against the deployed roster — `deploy.sh --check` now fails the build at the row level when the catalog and the deployed roster disagree (a registry row with no roster member, a roster member with no registry row, or a row that resolves to no live `SKILL.md`), with a second per-row field-currency layer that logs (warn-mode-initial) when an entry's recorded modes/dependencies/triggers drift from its skill — and the consumer surface that cited a phantom `dependency-graph.md` (a file that never shipped) is corrected to cite the live registry. `routine`-class; an additive build check plus citation corrections — no data migration, no stored-content move, no breaking change; four drifted `.skill` packages rebuilt at release-cut.

A `routine`-class release on milestone #247 `104-skill-registry-identity-and-currency` (3 issues — #1811 row-currency, #1658 field-currency, #1211 consumer re-point; bump-class routine) — **D-C SINGLE** topology, one release PR (#2249). **Row-currency, always-enforce (#1811):** `deploy.sh --check` gains `Check 5(d): Registry-currency (rows)`, appended after Check 5(c) and reusing Check 5's roster machinery, asserting three things against the deployed roster (`OPERATIONS_SKILLS + RELEASE_SKILLS + CORE_SKILLS`, with `CANARY_SKILLS` excluded per ADR-04 because the source-only canary has a source dir but no CI row): (i) every registry row name is a roster member, (ii) every roster member has a registry row — FAIL on asymmetry in both directions, (iii) every registry row name resolves to a live `SKILL.md`. The row layer is structural and zero-false-positive, so findings increment `ISSUES` directly; it inherits the `cmd_check` `set +e` body and `set -u` empty-array guards, and a 0-row parse (a registry reformat or moved file) degrades to a single loud audit-baseline FAIL rather than a fail-storm. **Field-currency, warn-mode-initial (#1658):** a per-row inner assertion extends the row loop at a single marked nesting seam — so the two layers share one roster source and cannot fork — checking, for each role-Specialist row, recorded modes/dependencies (set-equality) and trigger-surface presence against the live `SKILL.md`, emitting `WARN` without gating until `registry-field-currency.mode` is flipped to `enforce` (per the established Check 14/15/18/19 shakedown precedent); a companion fix reconciled the `pmo-architect` composition set — its frontmatter and its registry row — to its true 2-skill set so the field layer reads a clean row on a pristine roster. **Consumer re-point (#1211):** 13 files across `core/`, `operations/`, and `release/` that cited a phantom `dependency-graph.md` (a file that was never created) now cite the live `core/skills/registry.md`, and one net-new `registry.md` citation in `core/standards/skill-workspace-location.md` is de-linked to avoid a forward-reference — a pure reference correction with no behavior change, confirmed by the cross-reference-integrity gate. Merged via merge commit `42f029553574e8c69e4e795e68b65b10829f856c`; signed-annotated `v2.32` tag at the merge SHA ("Good signature"), green CI; GitHub Release published and marked Latest with the frontmatter-stripped note body. **Version note:** planned provisional at the `routine` bump-class on the theme-named branch `release/v2.32-skill-registry-currency` (defer-to-merge — the durable declaration is the bump-class, not a number); the concrete number bound to **v2.32** at the Stage 12 atomic claim above the shipped anchor v2.31 (v2.32 free at claim time). The shared `Check 5(d)` design ships as a design-note in the committed release plan (no ADR), per the Stage 5 scope-lock; the 4 drifted `.skill` packages (`build-reviewer`, `pmo-architect`, `pmo-skill-editor`, `pmo-skill-refiner`) were rebuilt at release-cut on the integration branch, and the `.version` stamp + CHANGELOG append + the `chore/v2.32-stage-13-closeout` branch are the manual completions the automated close-out tool drops for theme-named milestones, addressed in this Stage 13 close-out. #1811 / #1658 / #1211 closed at Stage 13 with verification-evidence comments; milestone #247 closed at Stage 13. Post-deploy verification: `Check 5(d)` is clean in both layers (`43 rows <-> roster`, symmetric, every row resolves to a live `SKILL.md`, field-currency asserted per role-Specialist row at `mode=warn`); the incremental `deploy.sh --deploy` cleared the drift for the 5 v2.32-touched skills, and the sole remaining hard FAIL is the pre-existing, out-of-scope `pmo-knowledge-manager — D-Refs` (not a v2.32 defect). CHEAP reversibility / HIGH confidence — an additive build check plus corrected citations; no data migration, no schema/behavior change, no breaking change; whole-release rollback = `git revert -m 1` of PR #2249 (the skill edits additionally revert via the skill-edit path + package rebuild). 30-day Stage-13 outcome window per the `routine` class.

### v2.31 (2026-06-27) — Knowledge-management discipline: the platform's memory gets one written cross-surface read/write SSOT contract — a per-surface table naming, for every place the platform stores a fact (the corpus, the governance context files, the `~/.claude/memory/` store, the `projects/_config/` state files, the operational trackers, the People surface, and `operator.toml`), which surface is the source of truth and who may write it under what Autonomy Tier — and the QA platform-health audit gains a systematic knowledge-management scan that reports stale and missing documentation. `novel`-class; additive throughout (one new discipline doc, one new standard, one new ADR, and reviewed edits to two skills + governed docs) — no data migration, no stored-content move, no breaking change; the two edited skills have `.skill` packages rebuilt at release-cut.

A `novel`-class release on milestone #138 `16-knowledge-management-discipline` (6 issues — a four-issue memory-architecture cluster #1073→#1074→{#1075, #1076, #1077} plus standalone #249; bump-class minor) — **D-C SINGLE** topology, one release PR (#2188). **Memory-type taxonomy + cross-surface contract (#1073 / #1074):** `knowledge-architecture.md §2.1` reconciles the four memory types — Work / Knowledge / People / Learning — against the three classification axes already in use (K1–K5 universality, Context-Tier read-precedence, Document-Tier write-approval), with an axis-collision-resolution subsection demonstrating that a single artifact (e.g. a CORRECTIONS entry: Learning / K5 / Tier-1.7 / operator-write-only) carries one value on each axis simultaneously with no contradiction — the four are orthogonal composing coordinates, not a fourth axis stacked on three. The linchpin is a new sibling discipline `core/disciplines/memory-architecture.md`: the single per-surface SSOT contract table (surface · memory-type · reader · writer · write-authority bound to the canonical Autonomy Tier 0–3 enum · cadence · read-only/auto-write/operator-write-only class · trigger), which absorbs the no-shadow-SSOT invariant verbatim and generalizes it from ADR-029's Knowledge cut to all four memory types; `OPERATIONS.md` and the seed `CLAUDE.md.template` link the contract, and `core/rules/governance-files.md` is the citing consumer satisfying the ≥1-consumer AC. **ADR-045** (`core/ADRs/ADR-045-cross-surface-memory-contract.md`) records the decision and supersedes ADR-029 — discharging the revisit trigger ADR-029 set for itself when it deferred the cross-surface contract. **People reconcile-and-cite (#1075):** the contract enumerates a People row that cites the functional people-graph shipped earlier (an operator-local roster read through the in-tree graph view) — no new surface was built and no repository-tracked personal data was introduced. **Operational-state lifecycle + CORRECTIONS graduation (#1076):** `operational-artifact-inventory.md` gains an additive surface-class-keyed §8 (growth-pattern · cleanup-trigger · archive-path · retention; the frozen §1–§7 schema, decision tree, 48 rows, and derived-signals untouched), and `OPERATIONS.md` gains a thin CORRECTIONS Graduation & Expiry Protocol — it binds the corrections file into the existing observation-emergence bar (N=2 same-(domain, theme) within 180 days, cited not redefined) and the existing Pattern-Review cadence rather than adding a parallel mechanism: a matured correction graduates onto the existing release rails and is then evicted, an obsolete one is retired archive-first, and the rest stay active; no new ADR was authored (the protocol is the corrections-type instance of the same lifecycle, surfaced through the superseding ADR-045). **Adoption audit + wired pilot (#1077):** a new `core/standards/memory-adoption-requirement.md` records which skills read and write memory and sets a forward-only SHOULD that new skills declare their adoption, and `daily-status` is the wired pilot — its inputs and its post-confirmation status-log append are now annotated against the contract (read each surface from its SSOT per its class; the append is the authorized auto-write of the skill's own surface, never an inline write to a higher-tier surface). **KM scanning in QA Mode E (#249):** `pmo-qa-auditor` Mode E gains a four-scan knowledge-management check-set over the durable corpus (a documentation-debt audit, a staleness scan ranked by criticality, an artifact lifecycle-state check, and an in-flight-capture verification) emitting two new audit artifacts — a doc-debt register and a staleness report — plus an in-flight-capture findings subsection; every threshold, weight, band, and state is consumed from `km-protocols.md` §2/§5/§6/§1 and `lifecycle-states-canonical.md` §4.4 (the check-set redefines nothing), and any artifact missing a required field is reported INDETERMINATE rather than silently passed. Merged via merge commit `bca1f86`; signed `v2.31` tag at the merge SHA, green CI. **Version note:** planned at the MINOR bump-class on the theme-named branch `release/16-knowledge-management-discipline` (defer-to-merge — the durable declaration is the bump-class, not a number); the concrete number bound to **v2.31** at the Stage 12 atomic claim above the shipped anchor v2.30. The two edited skills (`pmo-qa-auditor`, `daily-status`) bump `version: → v2.31`, route through the skill-edit discipline, and rebuild their `.skill` packages at release-cut via the Stage 13 close-out chore PR (the merge shipped source, not packages). #1073 / #1074 / #1075 / #1076 / #1077 / #249 marked closed at Stage 13 (auto-closed from the release PR); milestone #138 closed at Stage 13. The memory-contract enforcement gate is deliberately DEFERRED to a follow-up — the contract ships doc-only. CHEAP reversibility / HIGH confidence — additive docs + reviewed skill edits; no data migration, no schema break; whole-release rollback = `git revert -m 1` of PR #2188 (the two skill edits additionally revert via the skill-edit path + package rebuild). 30-day Stage-13 outcome window per the `novel` class.

### v2.30 (2026-06-26) — Governance cross-reference currency: display-text path citations across the corpus that still named the platform's pre-reorganization folders (`pmo-platform/{reference,skills,governance,engineering}/…`) are reconciled to their verified current homes under `core/` / `release/` / `operations/`, so internal links and citations in the docs and skill files resolve instead of dead-ending — the 13 catalog `SKILL.md` files drop from 67 stale citations to zero, a 28-file non-corpus sweep rewrites genuine drift while preserving legitimate historical occurrences, and one single-token QA↔Dev-Testing threshold (`≥3` → `>3`) is harmonized. `cross-cutting`-class; documentation/governance citation-accuracy only — no skill behavior, schema, or runtime-check change; the cosmetically text-edited skills have `.skill` packages rebuilt at release-cut.

A `cross-cutting`-class release on milestone #123 `38-governance-cross-reference-currency` (6 issues — 3 reference-currency fixes #121 / #399 / #677, 2 no-code dispositions #2080 / #2081, 1 umbrella #753; bump-class minor) — **D-C SINGLE** topology, one release PR (#2122). **Path-citation reconciliation (#121 / #399 / #677):** the modular-monolith restructure flattened the old `pmo-platform/{reference,skills,governance,engineering}/…` tree into `core/` / `release/` / `operations/`, but display-text path citations across the corpus still named the old locations; each was reconciled to its verified live home with locate-then-verify (`test -e` per target, no blind prefix swaps) — the 13 catalog `SKILL.md` files (#677) went from 67 stale `pmo-platform/…` citations to zero, and a 28-file non-release-corpus sweep (#399) rewrote genuine display-text drift while deliberately preserving legitimate occurrences (immutable ADR history, deploy-path globs, the rewriter's own tool-usage examples, test fixtures, frozen audit line-references, operator-instance roadmap paths). A single-token internal-consistency fix harmonized the QA↔Dev-Testing escalation threshold (`≥3` → `>3` round-trips) in `core/disciplines/operating-model.md` (#121); and the user-facing API reference `docs/module-apis.md` had two standards files mis-listed under Specs with dead links moved to the Standards list where the files actually live. **Scope correction folded into #677:** the acceptance check named the `reference/specs/` prefix, but the drift used `reference/standards/` and the issue's stated destination `core/specs/failure-mode-standard.md` does not exist (the file lives at `core/standards/`); verifying each destination against the live tree (49 corpus references agree on `core/standards/`) caught this before any rewrite, and the operator approved folding all 13 files' path drift into #677 so nothing was orphaned between the two sweeps. **Two items closed without code (#2080 / #2081):** #2081's "fail a PR that cites a non-resolving issue number" is already enforced by the repository-integrity Issue-reference validity gate (required, blocks merge, added-lines only) — no second check added; #2080's one-time stale-issue-reference sweep was re-measured live (419 open issues; 176 distinct unresolvable references across 273 descriptions, replacing a stale prior catalog) and, because the re-versioning's old→new number mapping is unrecoverable per reference, the residual is documented and kept rather than guessed (a wrong reference is worse than a dead one); the recurrence it guarded is already prevented by the gate above, and umbrella #753 closes once both children land. Merged via merge commit `07a5e9cd951c6e995e7c06ab917245636a54b6c7`; signed `v2.30` tag at the merge SHA ("Good signature"), green CI (24/24 checks). **Version note:** planned provisional v2.28 (bump-class minor) on the theme-named branch `release/38-governance-cross-reference-currency`; the concrete number drifted v2.28 → v2.29 → **v2.30** at the Stage 12 atomic claim as concurrent siblings shipped v2.28 (`15-generated-vs-source-provenance`) and pushed a Release-less v2.29 tag mid-run — defer-to-merge re-verified next-free at each step (version identity only; no scope/design change). The cosmetically text-edited skills' `.skill` packages rebuild at release-cut via the Stage 13 close-out chore PR (the merge shipped source, not packages). #121 / #399 / #677 / #2080 / #2081 / #753 marked closed at Stage 13 (auto-closed from the release PR); milestone #123 closed at Stage 13. CHEAP reversibility / HIGH confidence — display-text path/citation corrections + cosmetic skill edits; no data migration, no schema/behavior change, no breaking change; whole-release rollback = `git revert -m 1` of PR #2122. 30-day Stage-13 outcome window per the `cross-cutting` class.

### v2.29 (2026-06-26) — Corpus-drift reconciliation: ten places where a reference or governance doc had drifted from its canonical source — a stale rule count, a divergent forward-consumer list, an old field name, an imprecise term, an incomplete check enumeration, and a duplicate file — are corrected so each affected K1 doc reads consistently with `deploy.sh`, `review-discipline-principles.md`, and the canonical schemas. `routine`-class; documentation/governance accuracy only — no skill behavior, schema, or runtime-check change; the 2 text-edited skills (`build-reviewer`, `pmo-skill-editor`) have packages rebuilt at release-cut.

A `routine`-class release on milestone #137 `27-corpus-drift-reconciliation` (10 `type:task` drift fixes, sizes XS–M; Knowledge Architecture · corpus-drift reconciliation slice; bump-class minor) — **D-C SINGLE** topology, one release PR (#2124). **Count/enumeration drift (#127 / #1100 / #2095 / #107):** `core/CLAUDE.md.template` Review-discipline preference drops the hardcoded "10 anti-laziness rules" for the count-agnostic "its anti-laziness rules" — the shared set grew 10→14, so a hardcoded count re-drifts on the next addition (the count is owned by `review-discipline-principles.md`); `release/skills/build-reviewer/SKILL.md` Section 1 likewise drops the stale "(10 rules)" summary and defers to the canonical set, scoping its original ten explicitly while noting rules 11+ come from the shared discipline (`version: v1.10 → v2.29`); `core/rules/skill-deployment.md` replaces its hand-maintained Drift Check enumeration — which had left Checks 16/17/19/21/22/24/26/27/31/32/34–46 unlisted — with a derivation rule pointing at `deploy.sh` as the single source of truth (and reads enforcement mode from `.claude/hooks/deploy-check.mode` rather than restating it); `core/standards/lifecycle-states-canonical.md` §6.2 drops a forward-consumer row ("Knowledge Architecture doc") that had already been satisfied, so the planned-consumer table no longer lists a shipped consumer as pending. **Reference-term / field-name reconciliation (#49 / #305 / #857):** `release/references/pipeline/stage-06-engineering.md` corrects its CI/CD compression analogy from the imprecise "pre-commit hooks" to "PreToolUse hooks (`.claude/hooks/`)" — the platform's actual hook surface (the architecture-overview half was already resolved, so the fix narrowed to the one remaining line); `core/CLAUDE.md.template` syncs its intake tier-selection wording to `stage-01-intake.md` § 5 Path A (symmetric content-shape routing — route by what the insight is, not by which fields can be filled); `core/schemas/navigation-layer-schema.md` reconciles its remaining `methodology` field reference to the canonical `delivery_approach`. **Persona-card reconciliation (#881):** `release/references/specs/release-personas.md` updates the Stage 8 QA persona's "no conditional pass" anti-pattern to match the evolved `stage-08-qa-testing.md` spec — a CONDITIONAL ACCEPT is a defined Phase E verdict that lists specific defects and carries an Override Record per NOT-MET/PARTIAL criterion; only an unconditioned or undocumented conditional pass remains the anti-pattern. **Duplicate-basename single-source (#94):** `release/skills/pmo-skill-editor/references/regression-checks.md` was a near-duplicate (a small localization diff) of `core/standards/regression-checks.md`; the source copy is removed and the canonical single-sourced into the package at build time via a new `core/deploy/deploy.sh` `TEMPLATE_SYNC_MAP` entry + the explicit-basename `resolve_template_sync_source` rule, with a scope note on the canonical distinguishing it from `release/references/specs/skill-suite-regression-checks.md` (different basename, different purpose — not duplicates, not single-sourced to each other). **Validator-surfaced cleanup (#498):** the corpus-content drift the v3.20 release-corpus validators surfaced is cleaned up — 5 historical release notes (v2.17 / v2.18.1 / v2.19 / v3.18 / v3.19) corrected against §3.2 plus a v1.03 INDEX-drift and filename findings (with a supporting `lint_release_corpus.py` extension). Merged via merge commit `92494ed447b43b79924f4a92cf4d3c2593247506`; signed-annotated `v2.29` tag at the merge SHA. **Version note:** planned provisional v2.28 (bump-class minor) on the theme-named branch `release/v2.29-corpus-drift-reconciliation`; the concrete tag bound at Stage 12 above the v2.28 ceiling (version identity only). The 2 consumption `.skill` packages (`build-reviewer`, `pmo-skill-editor`) rebuild at release-cut via the Stage 13 close-out chore PR (the merge shipped source, not packages) — the close-out also re-aligned `build-skill-packages.sh`'s parallel canonical resolver, which the #94 merge had left without the new `regression-checks.md` basename rule. #49 / #94 / #107 / #127 / #305 / #498 / #857 / #881 / #1100 / #2095 marked closed at Stage 13 (auto-closed from the release PR); milestone #137 closed at Stage 13. CHEAP reversibility / HIGH confidence — documentation/governance text corrections + one single-sourced duplicate removal; no data migration, no schema/behavior change, no breaking change; whole-release rollback = `git revert -m 1` of PR #2124. 30-day Stage-13 outcome window per the `routine` class.

### v2.28 (2026-06-26) — Generated-vs-source provenance: when a skill writes a generated artifact, its frontmatter now records which skill + version produced it (`generated_by`) and what upstream evidence it drew from (`source_inputs`), plus a stable `id`, so generated content is auditable to its source and cleanly distinguished from human-authored source artifacts. The single card also closes a long-dangling cross-doc contract — `template-protocol.md` §8's `generated_by` dual-semantics flag now exists on both the template and the instance sides. `novel`-class; additive throughout (every field `Required: No`, forward-only no back-fill); 5 emitter/governance/QA skills bumped to v2.28 with packages rebuilt at release-cut.

A `novel`-class release on milestone #190 `15-generated-vs-source-provenance` (single card #205 after the milestone collapsed to one card; Project Data Architecture, sized/bundled slice) — **D-C SINGLE** topology, one release PR (#2091). Design pre-locked by the operator: **D2** canonicalize `source_inputs` + deprecate `synthesis_scope` → alias; **D3** add a stable artifact `id`. **#205 (provenance markers):** `core/schemas/frontmatter-schema.md` Category 3 gains the instance-population **`generated_by`** (skill name + semantic version — distinct from the person-or-skill `created_by`, which carried no version; the audit's "0/10 have skill/model attribution" gap is the version specifically), defines **`source_inputs`** as the canonical cross-domain provenance carrier (`TR-### | MSG-### | path`) in place of the prior "not yet defined" placeholder, and adds a stable **`id`** field (`<type>-<project>-<YYYY-MM-DD>-<nn>`, filename-independent), all `Required: No` per the forward-only no-back-fill lock. The headline contract-closure: `core/standards/template-protocol.md` §8 mandated the `generated_by` dual-semantics flag in **both** the template schema and the instance schema, but the instance side was silent — a dangling forward-reference; this release adds the instance-side field + the reciprocal flag so the both-locations contract is satisfied on both sides (`grep -l generated_by` returns both files; template-protocol itself needs no functional edit). Domain-C `synthesis_scope` is marked DEPRECATED → alias of `source_inputs` (one live field for the concept; `trigger_source` stays distinct); the §Scope generated-vs-source one-liner is added (the `domain`/`folder` split confirmed already present — sibling **#200 absorbed**, sibling **#201 superseded**, both already CLOSED on main). The four emitting skills (`artifact-generator`, `comms-writer`, `daily-status`, `ppm-agent`) stamp `generated_by` + `source_inputs` on write with a missing-header → regenerate-with-header rule (AC-4); `core/governance/OPERATIONS.md` records the marker requirement + the forward-only no-back-fill policy (AC-5); `core/skills/pmo-qa-auditor/SKILL.md` Mode D adds a `generated_by`-presence spot-check (random ~5/week sample, AC-6). All 6 AC greps return ≥1 hit on main. Merged via merge commit `0fdd8b763b1e5bbd9e731d52d1e0a44d17d21dee`; `v2.28` tag at the merge SHA. **Version note:** planned provisional v2.28 (bump-class minor) on the theme-named branch `release/15-generated-vs-source-provenance`; the concrete tag bound at Stage 12 above the v2.27 ceiling (version identity only). The 5 materially-edited `SKILL.md` files bump to `version: v2.28` and their `.skill` packages rebuild at release-cut via the Stage 13 close-out chore PR (the merge shipped source, not packages); the structured `source_ref` tracker field + `relationships[]` edge population are routed to follow-up **#1770**. #205 marked closed at Stage 13; milestone #190 closed at Stage 13. CHEAP reversibility / HIGH confidence — additive throughout (new instance-side fields + an alias deprecation + emitter/governance/QA wiring; every field `Required: No`, forward-only no back-fill, no data migration, no breaking change); whole-release rollback = `git revert -m 1` of PR #2091. 30-day Stage-13 outcome window per the `novel` class.

### v2.27 (2026-06-26) — Ticket information architecture: three coupled cards establish how a ticket carries confidence, titles, and information. One canonical four-band ordinal staleness-confidence **depth** scale (`S0-NONE` < `S1-SUPERFICIAL` < `S2-SUBSTANTIVE` < `S3-STRUCTURAL`) replaces five incompatible shapes so an operator reads one vocabulary and "old" no longer auto-reads as "structurally broken" (#436); work-item titles drop the redundant `[...]:` type prefix (type lives on the label) behind a repurposed G1-01 title-informativeness floor + a warn-tier `deploy.sh` Check 22 that never forces a bulk re-title of legacy issues (#545); and the Three-Layer ticket information model (Source of Truth / Stage Reviews / State Anchors) is captured with the backlog view rendering pipeline-Stage granularity (#211). `cross-cutting`-class; additive throughout; 2 consumption-skill edits with packages rebuilt at release-cut.

A `cross-cutting`-class release on milestone #199 `22-ticket-information-architecture` (3 BUILD cards #436/#545/#211 + #229 record-only; Project Data Architecture, a sized/bundled slice) — **D-C SINGLE** topology, one release PR (#2070), write-serialized Engineering #436 → #545 → #211 on a single theme-named branch. **#436 (staleness-confidence standard + ADR-043):** new `core/specs/staleness-confidence-standard.md` defines the **one** shared confidence-representation — a four-band ordinal **depth** scale (`S0-NONE`/`S1-SUPERFICIAL`/`S2-SUBSTANTIVE`/`S3-STRUCTURAL`) that every ticket-facing staleness mechanism (binary AC-Drift, 3-tier context-drift severity, 3-class mid-pipeline drift verdict, PT-1..PT-4 taxonomy, the one weighted time-decay score, age-threshold backlog-aging) maps its own states onto, optionally backed by a projected score whose `score→band` projection reaches `S3` only via a contradiction finding (never a high time-decay value alone). The standard governs the representation axis only — bounded away from detection triggers (each mechanism's spec) and response posture (`reconcile-dont-annotate.md`). Founding slim-kernel **ADR-043** records why an ordinal depth scale and not the existing time-decay score — a decision that **reverses** the originating issue's stated lean; the number was re-verified next-free (041/042 taken by the concurrent v2.25). `delivery-engine` backlog-scan Aging now reports on the scale (>14d same-status / >30d open → `S1`, >60d → `S2`, >90d → `S2` with escalation; age alone never implies `S3`). **#545 (human-informative titles + G1-01/Check-22 floor):** all three work-item templates (`bug.yml`/`improvement.yml`/`observation.yml`) drop the bracketed title prefix to a prefix-less `title: ""` (type travels on the label); `core/schemas/gate-criteria-spec.md` **repurposes G1-01 in place** from "Title follows `[Category]: Description`" (prefix REQUIRED) to a title-informativeness floor (no `[...]:` prefix + a substance floor of ≥2 words / not a bare slug / ≥N chars), holding the ID and every column value so the Gate 1→2 `structural_pass_rate` denominator is unchanged (criterion-shape change, no renumber; schema v1.15→v1.16; Adapters G1-01-Bug/G1-01-Obs RETIRED — no per-template prefix literal remains); `core/deploy/deploy.sh` Check 22 enforces the floor **warn-tier** (legacy prefixed issues WARN, never FAIL `--check` — bulk re-title is a separately-authorized follow-up), backed by new `core/deploy/tests/test_g1_title_floor.sh`. The floor is the **syntactic** minimum only — the **semantic** informativeness bar (lead with the change / name object+action / disambiguate from neighbours / spend the bought-back space on specificity) lives in `release/references/how-to/intake-style-guide.md` §7 as judgment (`intake-desk` elicits to it; no LLM grading added); `operations/skills/intake-desk/SKILL.md` + `references/output-contract.md` + `evals/evals.json` carry the elicitation + the title-informativeness check before the confirm. **#211 (ticket information model + Status/Stage invariant):** new `release/references/specs/ticket-information-architecture.md` defines the **Three-Layer Model** every ticket carries — Source of Truth (issue body; read first, updated in-place), Stage Reviews (append-only comments single-pass or sub-issues multi-pass; the immutable audit trail), State Anchors (Projects fields / labels / Milestones; machine-readable pipeline position) — the information model that ticket lifecycle, system configuration, field lifecycle, and I/O contracts implement against; recorded as a slim `## Design rationale` (no standalone ADR). `core/disciplines/github-projects-guide.md` adds the **Stage** column to the backlog triage view: Status is the coarse lifecycle axis and `Bundled` spans Stages 3–5, so without Stage visible every `Bundled` issue collapses into one group with no way to tell Bundle from Planning from Solutioning apart. Merged via merge commit `38905e4b6a1315cca85bf3cdb4f55a28d02cd807`; signed-annotated `v2.27` tag at the merge SHA. **Version note:** planned version-agnostic on `release/22-ticket-information-architecture` (provisional-display v2.25); v2.23/v2.24/v2.25/v2.26 shipped concurrently mid-session, so the Stage-12 atomic claim took the next-free **v2.27** above the v2.26 ceiling (version identity only). The 2 consumption `.skill` packages (`delivery-engine`, `intake-desk`) rebuild at release-cut via the Stage 13 close-out chore PR (the committed packages were stale — the merge shipped source, not packages). #436 / #545 / #211 marked closed at Stage 13; milestone #199 closed at Stage 13. CHEAP reversibility / HIGH confidence — additive (new spec + new ADR + an in-place criterion-shape change + a warn-tier gate + read-only/elicitation skill edits; no data migration, no breaking change — G1-01 keeps its ID and the gate is warn-tier so no existing issue fails); whole-release rollback = `git revert -m 1` of PR #2070. 30-day Stage-13 outcome window per the `cross-cutting` class.

### v2.25 (2026-06-26) — Hub-spoke orchestration discipline: the orchestration failure-modes the platform learned by trial-and-error are codified into `hub-spoke-bridge.md` — the bridge reference the hub reads at runtime — so a class of cross-ticket / cascade / chip-safety failures is caught at authoring time rather than after red CI or a mis-routed spoke. Cascade-completeness chip block, a necessity/value-add hub-eval dimension, de-fragile durable-corpus authoring, four memory→corpus chip disciplines (verbatim-source / issues-as-spoke-contract / hook-safe git idioms / concurrent-spoke contention recovery), and a mid-Stage-5 cross-ticket detection rule (Spoke A); a #189 empirical hook-inheritance suite — CI regression + operator probe + Check-46 deploy-check (Spoke B). `novel`-class; additive throughout (no `SKILL.md` edited); two disjoint single-writer spokes.

A `novel`-class release on milestone #168 `76-hub-spoke-orchestration-discipline` — **D-C SINGLE** topology realized as **two disjoint single-writer spokes** (two branches, two PRs, zero overlapping files): **Spoke A** (PR #2016) bundles five cards that co-edit `release/references/how-to/hub-spoke-bridge.md` (single-writer serialization, not a logical dep); **Spoke B** (PR #2017) is the disjoint #189 empirical suite. **Spoke A — #78 (cascade-completeness):** a chip-launch cascade block in `hub-spoke-bridge.md` Procedure 3 so a spoke whose spec changes a count / enumeration / threshold enumerates every stale occurrence (parallel to R1). **#210 (necessity/value-add):** a 7th hub-eval dimension in Procedure 4 Step 5 letting the hub diverge from accurate-but-inert spoke recommendations rather than rubber-stamp them. **#879 (de-fragile authoring):** a durable-corpus de-fragile pre-check at the bridge's Stage-6 chip guidance + `core/standards/reference-durability-standard.md`. **#1669 (four chip disciplines, memory→corpus):** verbatim chip-source, issues-as-spoke-contract-not-chat-blocks, hook-safe git idioms, and concurrent-spoke contention recovery codified as four bridge subsections + Stage-6 anti-pattern bullets in `release/references/specs/release-personas.md` (the four memory files evicted operator-side on merge). **#31 (mid-Stage-5 cross-ticket detection):** a detection heuristic + hub escalation handler in the bridge + a Phase-0.7 protocol section in `release/references/pipeline/stage-05-solutioning.md`, anchored by founding **ADR-041** (slim-kernel). **Spoke B — #189 (empirical hook-inheritance suite):** a Layer-A CI regression (`test_agent_tools_conformance.sh` + 3 fixtures wired into `install-tests.yml`), a Layer-B operator probe (`subagent-hook-inheritance.test.sh` + probe doc under `core/hooks/tests/`), the new **Check 46 `agent-tools-list-conformance`** `deploy.sh --check`, a `core/standards/subagent-security-posture.md` §1/§3 reconcile, and founding **ADR-042**. **Empirical finding:** subagent hook-INHERITANCE is CONFIRMED at workspace root; the worktree-session hook-LOADING residual is *surfaced* (not masked, per the card's AC) and tracked as #1472. Merged via two merges — Spoke A PR #2016 at `3f8dd91f248c8c6a877bb7cd31298a68aac6479f`, then Spoke B PR #2017 at `029b5c6320bb873f681c5f7a04195112ef99040e` (the release SHA); signed-annotated `v2.25` tag at the PR #2017 merge SHA (Good ED25519 signature). **Re-versioned `v2.23 → v2.25` at Stage 12** — two sibling releases (`v2.23-functional-people-graph`, `v2.24-corpus-conventions-and-standards-hygiene`) merged mid-build and claimed v2.23/v2.24, so this stepped to the next-free above the v2.24 floor; the two founding ADRs were renumbered alongside (ADR-040→041→042 cascade across `33d359e` / `086493e`) — version + ADR identity only, no scope/design change; branch slugs retain the original `v2.23-` prefix as stable refs. #31 / #78 / #189 / #210 / #879 / #1669 marked closed at Stage 13; milestone #168 closed at Stage 13. CHEAP reversibility / HIGH confidence — additive throughout (new doc subsections + new test fixtures + one deploy-check + a doc reconcile; no `SKILL.md`/package/schema touch); per-PR rollback = `git revert -m 1` of each PR (no ordering constraint — disjoint surfaces). No `SKILL.md` edited → empty `.skill`-rebuild surface this release. 30-day Stage-13 outcome window per the `novel` class.

### v2.24 (2026-06-26) — Corpus conventions and standards hygiene: two standards docs relocate into `core/standards/` (with a 90-file inbound reference sweep), four net-new corpus docs land (a minimal-addition authoring discipline, the third domain-best-practices domain, an audit status-badge convention, a Claude Code runtime-state catalog), three reference docs are reconciled to live state, and a substrate-neutrality audit confirms the bare toolkit. `novel`-class; additive doc/governance only — no skill behavior, schema, or deploy-state change.

A `novel`-class release on milestone #140 `26-corpus-conventions-and-standards-hygiene` (10 cards spanning epics #1171 KA-Standards, #1173 KA-Reference, #1174 KA-Discipline; Knowledge Architecture project) — **D-C SINGLE** topology, one release PR (#2019), Engineering commits in implementation-sequence order (#77 first). **#77 (standards relocation hub, blast-radius):** `git mv` of `core/specs/failure-mode-standard.md` + `core/standards/process-flow-diagram-standards.md` into `core/standards/`, with a **live-verified 90-file inbound reference sweep** rewritten to the new paths and re-grepped to **0** dangling `specs/` references — sequenced first because its sweep rewrites a ref inside `triage-design-rereview.md` (the file #81 edits) and relocates the diagram standard (the file #113 audits); the ~30 mechanical `SKILL.md` inbound-ref rewrites (plus the release-cut `version:` bump) drove the `.skill` package rebuild at close. **#751 (NEW):** `core/disciplines/minimal-addition-discipline.md` codifies a minimal-addition authoring discipline for the governance & reference corpus, cross-linked from `duplicate-source-discipline.md`. **#764 (NEW):** `core/standards/domain-best-practices/process.md` adds the missing third domain-best-practices domain. **#171 (NEW):** `core/standards/audit-recommendation-status-badges.md` defines a per-recommendation status-badge convention. **#163 (NEW):** `core/references/reference/claude-code-runtime-state.md` catalogs Claude Code runtime state surfaces (backup snapshots / session storage / env-var precedence), registered in the Governance File Map. **#580 (drift fix):** `core/rules/skill-deployment.md` reconciles the roster counts that drifted from the live `deploy.sh` arrays (12 vs 13; bootstrap example `intake-desk` omission). **#663 (drift fix):** `core/specs/framework-catalog.md` + `core/standards/km-governance-framework.md` correct stale `canonical_doc` paths on the INTERNAL rows + the km-governance count narrative. **#81 (drift fix):** `core/standards/triage-design-rereview.md` adds `size:XS` to its §7 size enumeration. **#1094 (substrate-neutrality spike, no corpus mutation):** a `type:spike` audit confirming the "bare toolkit" substrate carries no methodology vocabulary (sprint / story-points / velocity / burndown / phase-gate) hardcoded as universal beyond the `delivery_approach` resolver — output is a findings register staged under `pmo-platform/analysis/`, the precursor (P0) that clears K1 methodology best-practice pack authoring (#1090, out of this milestone). DT + verification both PASS (10/10 ACs across the 10 cards; doc-link + Check-14 clean on the relocated paths). Merged via merge commit `fb0d88af4de5a45612564942636022537a1108d0`; signed-annotated `v2.24` tag at the merge SHA (Good ED25519 signature). **Version note:** planned + built as **v2.23** (branch `release/v2.23-corpus-conventions-and-standards-hygiene`); the v2.23 slot was claimed mid-pipeline by the concurrent `v2.23-functional-people-graph` release (PR #2020), so the Stage-12 atomic ref-CAS claim took the next-free minor **v2.24** per the defer-to-merge allocation rule (version identity only). The 37 changed-skill `.skill` packages rebuild at release-cut via the Stage 13 close-out chore PR. #77 / #81 / #113 / #1094 / #751 / #764 / #171 / #163 / #580 / #663 marked closed at Stage 13; milestone #140 closed at Stage 13. CHEAP reversibility / HIGH confidence — doc/governance only (no migrations / deploy-state / data); whole-release rollback = `git revert -m 1` of PR #2019. 30-day Stage-13 outcome window per the `novel` class.


### v2.26 (2026-06-26) — People-graph activation: the people-graph that shipped structurally in v2.23 is now active on a fresh install — install/update seed the operator-instance roster (create-once, never-clobber), the adoption path is documented, and a consumption eval verifies the four consuming skills resolve read-only from a populated roster. `routine`-class; additive; activation layer only.

A `routine`-class release on milestone #230 `85-people-graph-activation` (3 cards; Project Data Architecture) — D-C SINGLE topology, one release PR (#2072). **#2040 (seed):** install/update + setup-workspace seed the operator-instance `people-roster.yaml` create-once from the de-identified template at three sites (setup-workspace PRIMARY with the rollback ledger, install belt-and-suspenders, update preserve-on-update) behind new `pmo_people_roster()` / `pmo_people_roster_for()` accessors in `lib-instance-path.sh`; the never-clobber `[ -f ] -> PRESERVED` guard is the load-bearing IRREVERSIBLE protection (QA HARD gate: a filled roster is byte-unchanged after install+update re-run). **#2041 (adoption docs):** GETTING_STARTED gains a people-graph adoption section (create -> fill -> per-tier `ref->Person` owner flow -> 4-skill consumption), cross-linked from FIRST_STEPS. **#2042 (consumption eval):** a binary-judge eval under `core/disciplines/evals/people-graph-consumption/` proves comms-writer / tracker-manager / ppm-agent / delivery-engine each resolve a specific value from a populated fixture roster + assert read-only + non-triviality (empty roster -> FAIL). Stage 5 activated for #2040 only (harness + never-clobber surface); no ADR. Merged via merge commit `2426d92520bda725b9e96c8cca205edfdff239f8`; annotated `v2.26` tag at the merge SHA. **Version note:** a concurrent release published `v2.24` mid-session and a live-but-unpublished `v2.25` tag sat on mainline; the version was claimed manually as the verified-free **v2.26** (the claim tool mis-excluded the mainline `v2.25` tag — tracked as #2075). #2040 / #2041 / #2042 marked closed at Stage 13; epic #2039 + milestone #230 closed at Stage 13. CHEAP-MODERATE reversibility / HIGH confidence — additive; whole-release rollback = `git revert -m 1` of PR #2072; the runtime roster clobber branch is prevented (create-once guard), not recovered. 30-day Stage-13 outcome window.

### v2.23 (2026-06-26) — Functional people-graph: PMO agents resolve people from one maintained, never-committed graph — a closed-allow-list roster (data surface) composed at read-time with the frozen Person + Resource entities into a capability/coverage view, with the four leadership-owner fields carried as typed `ref→Person` instead of free text. ≥4 skills read names / owners / coverage read-only; the graph + its maintenance + its consumption are registered governance; a de-identified template ships in-repo while the filled roster stays out-of-repo and commit-protected. `novel`-class; additive; functional-coordination only — not an HR/performance system.

A `novel`-class release on milestone #188 `14-functional-people-graph` (six cards re-split from the original 2 per the 2026-06-24 G2-11 oversize gate; Project Data Architecture) — **D-C SINGLE** topology, one release PR (#2020), serial Engineering #315 → #1166 → #1898 → #1899 → #1900 after the findings-gating spike #1897. **#1897 (spike, no code):** returned three findings that gated the build — operationalize Person by reference via a declarative config-layer (no frozen-entity touch), a typed Person reference plus external fallback with four new owner rules (founding ADR mandatory, expensive-once-consumed), and an operator-instance never-committed roster plus a de-identified template. **#315 (data surface):** a closed-allow-list roster (`operations/templates/people-roster-template.yaml` de-identified template; the filled `people-roster.yaml` lives out-of-repo) with `{value, source, last_verified}` provenance + a `status` tombstone, a `core/deploy/extract-roster-needles.sh` PII needle-extractor feeding the gitignored needle list, and `project-entity-model.md` §10/§8 operationalizing Person/Resource by reference. **#1166 (graph layer, leg C):** `core/disciplines/people-coverage-graph.md` — a read-time composed VIEW over the roster + the frozen entities (compose-not-absorb; no committed store) answering who-does-what / who-covers-whom / coverage-by-capability, with Tier-1 ambient maintenance (auto-invoking the needle extractor to close the staleness window) and a clarification queue. **#1898 (owner reconciliation):** the project / portfolio / program owner + initiative-sponsor fields type-lifted from free text to `ref→Person` with an external fallback, governed by founding **ADR-040** (mutual-exclusion + field-conditional on-unresolved resolution); the frozen-schema `core/schemas/entity-field-schemas.md` carries the four new owner X-rules (Tier-2, single-owner) and `project-schema.md` + `project-entity-model.md` §4 carry the field type-lifts — the design's draft ADR-019 collided and was re-numbered next-free at authoring. **#1899 (consumption, leg D):** `comms-writer`, `tracker-manager`, `ppm-agent`, and `delivery-engine` cite one shared graph view read-only (each edit via the `pmo-skill-editor` discipline) + three `TEMPLATE_SYNC_MAP` entries. **#1900 (governance/docs, leg E):** the graph + its ambient maintenance + its consumption registered in `core/governance/OPERATIONS.md` (artifact / owner / cadence); Stage-5-skipped (mechanical registration). All three Release Outcome Statement Success Indicators verify on main (≥4 read-only consumers; the four owner fields resolve `ref→Person` + external fallback; `git ls-files | grep -i roster` returns the template + needle script but no filled instance). Merged via merge commit `ca2015a8dd025601f52f1c86ba79fc9529bf9e34`; signed-annotated `v2.23` tag at the merge SHA (Good ED25519 signature; re-versioned from a concurrently-claimed `v2.22` at the Commit-0 authoritative re-verify). #1897 / #315 / #1166 / #1898 / #1899 / #1900 marked closed at Stage 13; milestone #188 closed at Stage 13. MODERATE reversibility / HIGH confidence — additive (new corpus + schema owner X-rules + read-only skill edits); whole-release rollback = `git revert -m 1` of PR #2020; the never-committed roster's accidental-commit branch is made unreachable (out-of-tree placement + `.gitignore` backstop + needle extractor) rather than recoverable. The 4 consumption `.skill` packages rebuild at release-cut via the Stage 13 version-bump chore PR. 30-day Stage-13 outcome window per the `novel` class. Functional-coordination only — not an HR/performance system.

### v2.22 (2026-06-25) — Comms-and-facilitation reference substrate: facilitation techniques get a governed K1 home a delivery skill can cite in-context, the canonical meeting-agenda + meeting-recap output-format specs live once (single-sourced out of `comms-writer`), and a recap↔follow-up boundary keeps a recap a *reference* to discrete follow-up records rather than their sole container. `novel`-class; additive; QA PASS on all 3 cards, 0 defects.

A `novel`-class release on milestone #139 `25-comms-and-facilitation-reference-substrate` (three cards under Epic #1173 KA-Reference, all `project:knowledge-corpus`) — **D-C SINGLE** topology, one release PR (#1919), Engineering commits #56 → #317 → #378. **#317 (canonical meeting-artifact formats):** authors new `core/standards/meeting-agenda-format.md` (6 required elements) + `core/standards/meeting-recap-format.md` (fixed body order Decisions → Action Items → Notes → Key Roadblocks + the `[RECAP]` subject convention), rewires `comms-writer` Types 2/3 + `references/channel-formats.md` to cite-by-path (no inline duplicate remains), and reconciles the `MTG-###` `Agenda` field in `tracker-manager/references/tracker-schemas.md` so the agenda lives in two homes (canonical + pointer), not three. **#378 (recap↔follow-up boundary):** inserts the boundary clause into #317's recap spec, splits the Recap-vs-Follow-Ups rows in `artifact-generator/references/artifact-catalog.md`, rewires the `ppm-agent` emit path to discrete follow-up records, and lands the canonical inventory edit on `core/standards/operational-artifacts.md` (R9 — operator-authorized [ADJUST]; the comms-writer + ppm-agent runtime mirrors regenerate on deploy). **#56 (facilitation-techniques foundation):** creates the K1 `core/standards/facilitation-techniques/` corpus (README index + first domain Estimation, a conforming 9-field card), registers it in `core/specs/framework-catalog.md`, and wires a silent-by-default surfacing trigger into `delivery-engine` Mode D; the story was **decomposed** (size:L) — only the corpus-foundation card shipped, the remaining 9 activity domains deferred to per-domain follow-on cards under Epic #1173 (each seeded *with* its consuming skill surface). 5 skills materially edited + bumped `version: v2.22` (`comms-writer`, `ppm-agent`, `artifact-generator`, `tracker-manager`, `delivery-engine`) with their `.skill` packages rebuilt in-merge (`49356ce`). DT + QA both PASS (all 3 cards; 0 defects). **Version note:** recorded version-less at Stage 4, reconsidered to **v2.22 (minor)** at Stage 5 Collective Review — the 5-skill `version:`-bump surface left no valid version-less provision. Merged via merge commit `1e5b6bace819a672faaedde24504c6101d986c72`; signed-annotated `v2.22` tag at the merge SHA (Good ED25519 signature). #317 / #378 / #56 marked closed at Stage 13; milestone #139 closed at Stage 13. MODERATE reversibility / HIGH confidence — additive; whole-release rollback = `git revert -m 1` of PR #1919. 30-day Stage-13 outcome window per the `novel` class.

### v2.21 (2026-06-23) — Decision-rendering standardization: makes the hub's operator-facing Decision Briefing auditable along the "alignment with agent operating principles" axis (previously unstructured). Three coupled cards — information-sufficient briefings (#337: pre-load referenced spec content, enumerate the full option space incl. stance-implied options, render-before-`AskUserQuestion` with a `[STRUCTURAL-DEFECT: unrendered-gate]` marker), a K1 design-principle register + per-option `### Design-Principle Conformance` D-Gate subsection mirroring the live `Upstream compatibility` mechanism (#320, keystone), and standing per-gate-class framing directives that ADD-never-SUBTRACT (#321). `cross-cutting`; 3 issues; MODERATE / HIGH.

`decision-rendering-standardization`-class release on milestone #160 (Epic ENG-Closeout-Automation, Pipeline Engine & Automation initiative) — **D-C SINGLE** topology, one release PR (#1877) carrying all 3 cards across sequential commits #337 → #320 → #321 on the shared `hub-spoke-bridge.md` surface (field-distinct regions; no collision). **#337 (info-sufficiency):** adds the 5-gate Decision Briefing Information Sufficiency sub-section to `hub-spoke-bridge.md` § Operating Principle (pre-load spec content / enumerate full option space incl. stance-implied / render-before-AskUserQuestion / stance-scan / spec-content-loaded self-check), a worked compliant briefing + `preview`-field affordance, the under-loading OUT worked example in `failure-mode-standard.md`, and a Stage 9 anti-pattern set in `release-personas.md`; cites `decision-discipline.md § 2.1` (does not redefine). **#320 (keystone):** creates the K1 `core/standards/design-principle-register.md` (index-only; DP-1..DP-7 indexed from `build-philosophy.md`, all `governing_doc` anchors verified) + the `### Design-Principle Conformance` D-Gate subsection (ALIGNED / `**CONFLICT.**` / N-A, reversibility-tier-gated, omission-on-matching-surface = `[STRUCTURAL-DEFECT]` per `decision-discipline.md § 5 G2`) + `deploy.sh` Check 45 (mechanism-presence + entry-row-scoped `governing_doc` drift guard + consumer-DP-id resolution, warn-mode initial) + a Stage 8 QA conformance-evidence dimension. **#321:** adds the per-gate-class framing directive schema (`gate_class` / `require_options` / `surface_dimensions` / `principles_emphasis` / `pre_decided_default`) to `engagement-charter.md`, homed in the milestone description, ADD-never-SUBTRACT; wired through `release-class-taxonomy.md` (confirm-and-cite the already-LIVE touchpoint inventory + add the directive as the third LIVE sibling) and into `hub-spoke-bridge.md` D-Gate + Procedure 5 gate-rendering + Pre-decided auto-population; `principles_emphasis` binds to the #320 register DP-ids. **Review:** Stage 5 ran an independent A6.5 Adversarial Design Review — its mandatory finding (PRF-1, an AC-invisible mis-anchor that would have placed #337's Stage 9 anti-pattern in the Stage 10 card) was corrected, and three strengthenings folded in (FMF-1 entry-row regex scoping, FMF-2 consumer-DP-id resolution, FMF-3 render-time-invariant framing); PRF-2 (deeper-enforcer originator secondary-trace) is a documented deferred residual. **ADR:** none — both #320 and #321 are faithful clones of ratified mechanisms (`Upstream compatibility` + the `:55` per-release-override convention), rationale folded into the artifacts per the slim-to-kernel discipline. MODERATE reversibility / HIGH confidence — additive (scope-predicate / Release-Class-gated); whole-release rollback = `git revert -m 1` of PR #1877. Merged via merge commit `ae0413665a3402dfc4afcbd54b3fa1b313c575ab`; signed-annotated `v2.21` tag at the merge SHA (v2.20 excluded as in-flight on a sibling release). #320 + #321 + #337 auto-closed on merge; milestone #160 closed at Stage 13. 30-day Stage 13 outcome window per the `cross-cutting` class.

### v2.20 (2026-06-23) — Field-lifecycle and CMDB automation: entity fields get a governed, agent-driven lifecycle and the skill registry becomes the platform's configuration-management catalog. Nine deliverables — shared + portfolio entity-lifecycle transition protocols (G8, #156/#1155), a G10 write-permission matrix governing who writes which field per stage aligned with the Agent Write Permissions table (#154/#208), skills that emit/consume the transitions (#1156), the `artifact_state`→canonical `lifecycle_state`+Domain reconciliation (`promotion_state` carve-out, dual-read removed, artifact-generator + artifact-lint migrated; #1865/#1866), the registry-as-CMDB evolution + per-project Artifact Register (Option A, ADR-038; #202), and a project-tracking integrity sweep (dormancy 10bd / overdue 3–5bd / evidence-backed gate; #752). `cross-cutting`; 9 deliverables; QA ACCEPT (~48/48 ACs, 0 defects); MODERATE / HIGH.

`13-field-lifecycle-and-cmdb-automation`-class release on milestone #198 — **D-C SINGLE** topology, one release PR (#1878) carrying all 9 deliverables across 29 commits (36 files, +2402/−308). **Entity-lifecycle transition protocols (#156/#1155):** `core/standards/entity-lifecycle-protocol.md` + `entity-lifecycle-protocol-shared-portfolio.md` + `lifecycle-states-canonical.md` define the agent-driven G8 per-entity state machine at both project and shared/portfolio scope. **G10 write-permission matrix (#154) + ticket-table alignment (#208):** `core/schemas/entity-field-lifecycle-matrix.md` declares write permissions per field × entity × stage; the `OPERATIONS.md` Agent Write Permissions table is aligned to it (single-sourced), and `release/references/specs/ticket-information-architecture.md` is reconciled to the matrix. **Skill integration (#1156):** the active branchers emit/consume transitions (`ppm-agent`, `tracker-manager`, `artifact-generator`, `weekly-status-rollup`, et al.), with `core/schemas/per-skill-output-contracts.md` + `entity-field-schemas.md` + `frontmatter-schema.md` + `tracker-schemas.md` carrying the field/contract changes. **Artifact-state reconciliation (#1865/#1866):** the separate `artifact_state` is reconciled into the canonical `lifecycle_state` + Domain model with a `promotion_state` carve-out for the staging→promoted axis; a new `core/artifact-workflow-protocol.md` documents the workflow; `artifact-generator` + `artifact-lint` are migrated onto the reconciled model and the transitional dual-read path is removed. **Registry-as-CMDB (#202, Option A):** `core/skills/registry.md` is evolved into the single configuration-management catalog (every deployed skill is a CI carrying lifecycle-state / dependency / owner; version + roster-existence are CITED from `SKILL.md` + `deploy.sh`, never stored); **ADR-038** records it and **supersedes ADR-035 §Decision part 4** (role-Specialists-only population) while PRESERVING ADR-035 parts 1/2/3/5; `pmo-skill-router` classifies against a `kind == role-Specialist` filtered VIEW so routing stays unblurred; a per-project **Artifact Register** template (`operations/templates/artifact-register-template.md`) lands. **Project-tracking integrity sweep (#752):** `delivery-engine` gains dormancy detection (10 business days), overdue-decision escalation (3–5 business days), and evidence-backed phase-gate completion. **QA:** verdict ACCEPT — ~48/48 ACs across the 9 deliverables verified, **0 defects**. MODERATE reversibility / HIGH confidence — additive corpus + 11 `SKILL.md` edits; whole-release rollback = `git revert -m 1` of PR #1878. Merged via merge commit `193a523eae4542e282600e911fe2dc6a79ac0893`; annotated `v2.20` tag at the merge SHA. **Non-monotonic claim order:** `v2.20` was tagged AFTER `v2.21` (the concurrent `decision-rendering-standardization` release merged first and claimed the higher number; the operator kept `v2.20` for this release) — the claim order is non-monotonic by version, benign, and self-heals on the next allocation via the claimed-set (next-free is computed against the live published set, not a monotonic counter). The 9 deliverables auto-closed on merge; milestone #198 closed at Stage 13. 30-day Stage 13 outcome window per the `cross-cutting` class.

### v2.19 (2026-06-22) — Co-management governance cleanup: finishes de-broadcasting the operator-specific naming from the public codebase (follow-up to v2.18) and single-sources the dual-framing status rule. Retires the legacy co-management compatibility shim (`project-initiator` + `project-schema §7` + the "accepted via shim" clauses), genericizes the residual K4 operator-config field names to `co_management_*`, and removes the redundant `CLAUDE.md.template` copy of the Dual-Framing Bridge rule (repointing its 7 citations to the one `OPERATIONS.md` home). Minor (shim retirement is a back-compat contract change); 2 issues #1846 + #1853; CHEAP / HIGH.

`comanagement-shim-retirement`-class cleanup on milestone #226 — **D-C SINGLE** topology, one release PR (#1851) carrying both issues across two commits. **#1846 (shim + token):** retires the legacy co-management deprecation shim — `project-initiator` Step-1 logic, the `project-schema §7` contract (rewritten to a terse migration note keeping one breadcrumb of the legacy key for downstream migration), and the "accepted via shim" clauses in `OPERATIONS.md` ×2 + the methodology specs — and genericizes the residual K4 field names to `co_management_smartsheet_id` / `co_management_sharepoint_folder` (`OPERATIONS.md` ×2 config block + `c3-external-sync-path-b.md`, with the inline gate label + the failure-mode root-cause quote reworded), finishing the v2.18 rename by modernizing the remaining stale legacy-key references to the current `dual_framing_enabled` (config template, failure-mode examples, regression-check strings). **#1853 (single-source):** the **Dual-Framing Bridge** rule was duplicated across `core/CLAUDE.md.template` and `OPERATIONS.md § Dual-Framing Bridge` — the exact `duplicate-source-discipline` failure that left the CLAUDE.md copy stale ("SPM Bridge") after v2.18 updated OPERATIONS.md + the schema; v2.19 removes the redundant template copy and repoints the 7 citations (`project-schema.md` ×5, `applicability-framework.md` ×2) to the one OPERATIONS.md home (the consuming-skill implementations are not rule duplicates and are untouched). **Back-compat / migration:** the shim retirement is a contract change (→ minor), behavior-neutral in practice — all 3 live `PROJECT.md` files carrying the legacy key were `false` and were migrated to `dual_framing_enabled: false` operator-locally; a downstream `PROJECT.md` on the legacy key must rename it (one-line, identical semantics). The `project-initiator/SKILL.md` edit ran through `pmo-skill-editor` Mode A; the XC-06 dual-framing regression check is intact on `dual_framing_enabled` (both byte-paired copies); no gate enforced the shim's existence (verified pre-removal). CHEAP reversibility / HIGH confidence — whole-release rollback = `git revert -m 1` of PR #1851. PR #1851 merged to main via merge commit `85d3f247a33e5e01713b1aa7acd99c672ae43dcd`; annotated `v2.19` tag at the merge SHA. #1846 + #1853 + milestone #226 close at Stage 13. **Post-deploy operator action:** run `update.sh` to re-render the workspace `CLAUDE.md` from the de-duplicated template (durable config preserved).

### v2.18.1 (2026-06-22) — Operator-instance path decoupling: the operator needle/instance path now resolves through a single resolver (`pmo_instance_path()`/`pmo_localized_needles()`, `${CLAUDE_WORKSPACE_ROOT:-$HOME/Claude}` canonical per ADR-032, no new variable) instead of ~6 inconsistent inline literals; ships a tracked `core/config/localized-context-needles.txt.example` + create-once scaffold (gitignored content), persistence byte-identical across `update.sh` (pre-update backup), and a **fail-closed** PII pre-commit guard (warn-mode-initial) closing the silent fail-open. `cross-cutting`; 1 story #1830; advances #529/#1492/#383, absorbs #1826, design-bound ADR-032, resolver boundary coordinated with #1412; sequenced onto #1847 (Check-43-allowlisted). Patch off v2.18; MODERATE / HIGH.

### v2.18 (2026-06-21) — Hybrid co-management decouple + Hybrid-Two: the `Hybrid` methodology is now defined cleanly as a **user-configurable two-archetype combination** (`delivery_approach: [A, B]`, e.g. `[Scrum, Kanban]`) and decoupled from the co-management arrangement it was previously tied to — the legacy co-management field is renamed `dual_framing_enabled` (legacy key still accepted via a deprecation shim), the dual-framing status artifact is renamed the "Dual-Framing Bridge", the operator-specific token survives only as operator-local K4 config, and dual-framing behavior is unchanged; 2 issues, K1 schema + a registered artifact + registries + two governance files + 26 skills + the XC-06 gate (deploy syncs skills + rebuilds packages)

`cross-cutting`-class release on milestone #224 `86-hybrid-comanagement-decouple` (under the methodology-corpus correctness line; RELATES_TO #1094 corpus-assumption audit, #1090 methodology plug-and-play) — **D-C SINGLE** topology, one release PR (#1797) carrying both issues across seven write-serialized slices, the 26 migrated `SKILL.md` edits routed through `pmo-skill-editor` Mode A. Two issues that are **one conceptual correction with two consequences**: (a) **decouple** — the `§3 Hybrid` semantic anchor (`release/references/specs/methodology-parameterization-v1.md`) is fully rewritten so the methodology classification stops naming an operator-specific co-management arrangement, made consistent across `core/schemas/project-schema.md` / `core/governance/OPERATIONS.md` + `operations/OPERATIONS.md` / the archetype matrix; and (b) **genericize** — `Hybrid` moves from the rigid one-iterative+one-phased co-management dual-track to a **user-configurable two-archetype combination** declared `delivery_approach: [A, B]` (two distinct archetypes from the 6-set; the literal `Hybrid` retained as a single-enum value for back-compat). Both land on the same anchor, which is why they are unified into one release. **The rename (S1/S2):** the legacy co-management trigger field → **`dual_framing_enabled`** (schema decl/presence/semantics/Migration §7 + the array union-type + the V2-validation amended in place — **no new V13**, the tail stays at V12 so the downstream PDA-schema keystone keeps V13); the dual-framing status artifact → **"Dual-Framing Bridge"** (its template under `operations/templates/` renamed to `dual-framing-bridge-template.md`, audience labels "PMO view" / "Sponsor view", the `core/CLAUDE.md.template` clause, both `OPERATIONS.md` clauses, and the five registries). **The preserved capability (the load-bearing constraint):** this is decouple + genericize + parameterize, **never capability removal** — the co-management dual-framing *output* survives as an orthogonal operational capability gated by the separate `dual_framing_enabled` flag; XC-06 (renamed, byte-paired with its `pmo-skill-editor` mirror) still asserts dual Agile/Waterfall framing emits when the flag is true; pmo-qa-auditor + pmo-skill-editor D5 audits pass. **Back-compat:** a live `PROJECT.md` carrying the legacy co-management key is still accepted via the `project-initiator` Mode C/A deprecation shim (one-line warning, treated as `dual_framing_enabled`) — so the rename ships **non-breaking**. **K4 parameterization (S5):** the partner-team identity + the related operator-config fields relocate to operator-local config (outside the public tree); the operator-specific token survives only as operator-local K4 config. **The new capability (S6, #32 Hybrid-Two):** the array-validation logic (length==2 / distinct / 6-set) finalized in `project-schema.md` §5 + a §5 CASE 1-ARRAY consumption pattern in `methodology-parameterization-v1.md` (dual-framed, union per work-org-mapping §2.5, phased-governs-gates / timeboxed-governs-iterations, render-both-on-contention, never silent-pick) + the §6.5 `[Scrum, Kanban]` worked example + the Hybrid-Two-vs-Custom partition rule (array = two canonical archetypes as-is; Custom = a named modification/fusion); the per-space methodology split (`operational_methodology` / `release_methodology`) is **deferred to #1788** (coordinated with the PDA-schema keystone #351 + #262). **ADR:** no standalone ADR — the durable kernel folds into the project-schema §7 prose + the expanded §3 anchor (per the durability discipline). CHEAP–MODERATE reversibility / HIGH confidence — additive methodology-corpus edits (field + artifact rename, decouple prose, the array capability) with a back-compat shim; whole-release rollback = `git revert -m 1` of PR #1797 (the only non-trivial undo is an operator `PROJECT.md` that adopted the new field, protected by the shim). PR #1797 merged to main via merge commit `4fdaf160f1bccc41a01185ac00ad2a16e087ce35`; signed-annotated (ED25519) `v2.18` tag claimed at the merge SHA via `claim-version.sh` ref-CAS. The build sub-task spokes auto-closed on merge; parent issues #1778 + #32 and milestone #224 close at Stage 13. 30-day Stage 13 outcome window per the `cross-cutting` class.

### v2.17 (2026-06-21) — Architecture-altitude discipline: every new capability is interrogated for its abstraction altitude (does it extend an existing platform seam, or solve point-wise?) as a forcing function at Stage 4/5 — closing the gap that let the version-claim design land host-coupled until the operator caught it off-pipeline; a Stage-4 T3 obligation rider + an `altitude` distinctness axis (point-fix / extend-seam / new-abstraction) + a blocking §4.7 design-review gate + a HOST-BINDING-LEAK leakage class with a warn-mode detector + a scoped A6.5 adversarial exception + `[adapters].repo_host` cross-references; 7 work items, governance-doc + one detector, no skills or packages touched (deploy no-op)

`cross-cutting`-class release on milestone #223 `architecture-altitude-discipline` (epic #1763, under the #572 Pipeline Definition & Fitness spine) — **D-C SINGLE** topology, one release PR (#1812) carrying all 7 work items, Stage 6 executed hub-inline. Closes the gap surfaced when the version-claim-determinism design (v2.16) was framed host-coupled and the operator caught the mis-altitude **off-pipeline** at Collective Review: the platform had a forcing function for *decomposition* altitude (sizing/splitting) but none for *abstraction* altitude. **The forcing function (E1, sliced #1774/#1775/#1776):** a Stage-4→5 obligation rider on the T3 trigger requires a structural design to name the platform seam it extends or justify solving point-wise with a cited seam-search (`core/standards/planning-solutioning-handoff.md` §3.1); design-exploration gains `altitude` as a fifth distinctness axis with three bands — point-fix / extend-seam / new-abstraction — plus a ≥2-band rule that forces the seam-extension candidate into the generated set on any new-mechanism design, and an altitude-diverging worked example (`release/references/standards/design-exploration.md`); and a blocking **§4.7 seam-composition gate** in the design-review checklist fails a design that does not discharge the seam question — omission is a fail — wired into the Section-4 Pass + Fail (`release/references/templates/design-review-checklist.md`). **The named class (E4, #1767):** `core/disciplines/knowledge-architecture.md` §4.1 registers **HOST-BINDING-LEAK** — a host tool hardcoded as *the* canonical mechanism in K1 governance where an adapter seam exists, the host-axis sibling of the path-portability class — plus a warn-mode `deploy.sh` Check 42 detector (`core/deploy/tools/check-host-binding.py`). **The independent surface (E2, #1765):** the Stage-5 A6.5 adversarial review, previously estopped from challenging pre-decided premises, may now flag a wrong-altitude premise as an advisory **Premise-Altitude-Finding** (no-autonomous-reversal guard explicit), routed to Collective Review scope-lock — the same decision home the §4.7 gate uses. **Discoverability (E3, #1766):** the `release/` pipeline specs now cross-reference the `[adapters].repo_host` host-operation seam, so it is discoverable from within the release module. **ADR:** cites the already-ratified ADR-022 (no new ADR, per the durability discipline). CHEAP reversibility / HIGH confidence — additive governance-doc edits + one warn-mode detector; whole-release rollback = `git revert -m 1` of PR #1812 strands no consumer (no skills/packages changed). PR #1812 merged to main via merge commit `fae1f8d450c3768207d7f89e5204e8d8c4068aba`; signed-annotated (ED25519) `v2.17` tag claimed at the merge SHA via `claim-version.sh` ref-CAS. The 7 build issues auto-closed on merge; milestone #223 closes at Stage 13. 30-day Stage 13 outcome window per the `cross-cutting` class.

### v2.16 (2026-06-21) — Concurrent-release version-claim determinism: a release's version is now claimed atomically at the merge tag (defer-to-merge + ref-CAS) instead of reserved at Stage 4, so two releases in flight can no longer silently collide on a number and the slower one lose its slot — defined as a host-agnostic capability with a config-selected `repo_host` adapter (GitHub/git the v1 reference), founding ADRs + corpus standards + new release-claim tooling + a CI freeness gate + a machine-readable re-version ledger; 14 issues, governance/standards/pipeline/tooling, no skills or packages touched (deploy no-op)

`cross-cutting`-class release (FINAL — ratified at the Stage-4 gate 2026-06-20) on milestone #222 `release-version-claim-determinism` — **D-C SINGLE** topology, one release PR (#1768) carrying all 14 cards, 26 changed surfaces and no `SKILL.md`/`packages/` touched (deploy no-op; validation carried by the green PR CI, 25/25). The capability slug is the durable identity (milestone `<slug>`, branch `release/<slug>`, plan `<slug>_RELEASE_PLAN.md`); the version is intent-to-bump in-pipeline and claimed atomically at the merge tag. **Founding ADRs (#1697):** `release/ADRs/ADR-036-version-claim-determinism.md` records the determinism contract — slug-primary identity, intent-to-bump, defer-to-claim, atomic compare-and-swap at the merge tag, defense-in-depth — and `release/ADRs/ADR-037-version-slot-cross-release-contended-axis.md` models the version slot as a cross-release contended axis (extending ADR-024). **Host-agnostic adapter (#1675, #1673):** `core/standards/repo-host-adapter-versioning.md` defines a `repo_host` adapter exposing four operations — `anchor()` / `claimed_set()` / `atomic_claim()` / `lineage()` — selected via `operator.toml [adapters].repo_host`; GitHub/git is the v1 reference adapter (`anchor()` = `releases/latest`, `atomic_claim()` = signed-tag ref-CAS push), implemented by the new `release/tools/claim-version.sh` atomic claim-with-retry; the authoritative allocation rule reconciles the semantic-bump doctrine with `git describe` practice. **Grammar + semantics (#1676, #65, #66, #1674):** a canonical version grammar (X.Y / X.Y.Z / suffix) is applied to the freeness checks (`release/references/standards/version-grammar.md` + `release/tools/version-grammar.sh`), milestone-numbering semantics gain a chronological caveat (`core/standards/version-field-semantics.md`), and parallel-release semantics are specified (the v-number is a slot identifier; ship-order = merge-order). **Defense-in-depth (#769, #1008, #1677):** the Stage-12 pre-merge check re-verifies the version is still unclaimed against fetched/authoritative refs, the hub D-Version check verifies next-free + re-verifies at Commit 0, and a new CI workflow (`.github/workflows/version-freeness.yml` + `release/tools/test_version_freeness.sh`) gives proactive pre-merge collision observability. **Recovery + audit (#1678, #1679, #1092):** a post-tag re-version recovery doctrine (`release/references/how-to/re-version-recovery.md`), a machine-readable re-version ledger (`release/releases/RELEASE_REVERSIONS.md`), and the concurrent-release Stage-13 corpus conflict-resolution doctrine in `stage-13-close.md`; the close-out tooling (`automated-closeout.sh`, `cleanup-orphan-state.sh`) was reworked for the slug-primary identity. Source observation **#950** (version assigned at Stage 4 but claimed at Stage 12) closes as the prevention + detection set ships. **Dogfood:** this release claimed v2.16 by the very mechanism it ships — the signed-annotated `v2.16` tag was created Tag-SHA-Direct on the merge SHA after a final ref-CAS next-free re-verification (v2.13/v2.14/v2.12 already held by sibling releases; v2.16 free above the v2.15 ceiling), exercising the atomic-claim path end-to-end. CHEAP reversibility / HIGH confidence — additive corpus standards + two founding ADRs + new release-claim tooling + a new CI freeness workflow; whole-release rollback = `git revert -m 1` of PR #1768 strands no consumer (no skills/packages changed). PR #1768 merged to main via merge commit `44b09d4ba647b7917f9b376a1f1575e8cd29ed31`; signed-annotated (ED25519) `v2.16` tag at the merge SHA. The 14 build issues + milestone #222 close at Stage 13. 30-day Stage 13 outcome window per the `cross-cutting` class.

### v2.15 (2026-06-20) — Role-Specialist suite GA: the final five Release-to-Sustain role agents (Release Manager, Tier-1/2 Support, OCM Lead, Knowledge Manager) plus the capstone PMO Skill Router — the platform's role-Specialist suite reaches its full 19-Specialist roster, addressable as one routable unit, with a router that reads a single logical skill registry to send a role-shaped request to the Specialist that owns it; 6 new thin Specialists + the registry + ADR-035, additive, no application logic

`novel`-class release on milestone #216 `05-ROLE-sustain-coverage-router` — **D-C SINGLE** topology, one release PR (#1700) carrying the suite GA, write-serialized Engineering. Closes the final two phases of the role-Specialist build (epic #284): the **Phase 4 Release-to-Sustain wave** — **#1120** `pmo-release-manager` (composes `release-planner` + `release-executor`), **#1121** `pmo-tier-1-support` (triage + known-issue resolve; owns no RCA), **#1122** `pmo-tier-2-support` (owns the RCA method + authors runbooks so a problem becomes first-line-resolvable), **#1123** `pmo-ocm-lead` (composes `change-management` across a go-live's lifecycle), **#1124** `pmo-knowledge-manager` (composes `artifact-generator` + `file-router`) — and the **Phase 5 capstone**, **#181** the `pmo-skill-router` + its L4 suite-integration run. **#1564** establishes `core/skills/registry.md`, the logical skill registry: one row per role-Specialist (`name` / `module` / `trigger surface` / `modes`), authored centrally — a Specialist registers by having its row in the table, not by adding a field to its own `SKILL.md`; the router enumerates no skill list of its own, so routing changes by editing the registry, never the router (the by-path-consumption posture `pmo-qa-auditor` already uses against `per-skill-output-contracts.md`). #1564 also backfilled the 14 prior-wave rows + core skills, bringing the registry to its full **19-Specialist** roster at GA. Per ADR-019 + ADR-007 each Specialist **composes** shipped function-skills (no re-implementation). ADR-035 (registry-as-classification-source) records the decision, renumbered from a branch-local ADR-034 behind main's progressive-rollout ADR-034. **Version note:** the branch + milestone are named for provisional **v2.12**; re-versioned up to **v2.15** at tag time because `v2.12`/`v2.13`/`v2.14` were taken by sibling releases (#1699/#1657/#1636) shipped mid-pipeline (versioning-lineage / no-version-less-as-hedge discipline). CHEAP reversibility / HIGH confidence — purely additive (6 new skill directories + packages, the registry, 1 new ADR, registration entries; whole-release rollback = `git revert -m 1` of PR #1700 strands no consumer, and `./core/deploy/deploy.sh --deploy` post-revert re-syncs the skill mirror). PR #1700 merged to main via merge commit `b8ce4f3540035a28f8ebfffbadb05ca453c3e5c7`; signed-annotated (ED25519) `v2.15` tag at the merge SHA. The 7 build issues auto-closed on merge. 30-day Stage 13 outcome window per the `novel` class.

### v2.14 (2026-06-21) — Autonomy-phase-out foundation: one canonical pipeline-wide rollout convention (`shadow → warn → enforce → removed`) plus a companion operator-touchpoint inventory + phase-out-plan schema — the language and structure for introducing, watching, enforcing, and eventually retiring a pipeline change are lifted from scattered per-mechanism homes into a single convention, and a schema is added for inventorying every human-in-the-loop checkpoint and planning each one's retirement; 2 `type:task` cards, additive governance/schema, no application logic

`novel`-class release (Deep Stage 9) on milestone #158 `71-autonomy-phaseout-foundation` — **D-C SINGLE** topology, one release PR (#1636) carrying both cards, #164 leading at Engineering (its phase enum is the E1 dependency #165's `current_phase` consumes by name). **#164 (RECONCILE-and-LIFT):** `core/standards/progressive-rollout-convention.md` lifts the three-cycle `shadow / warn / enforce` model that previously lived only inside the release-executor and adds the terminal `removed` rung (the autonomy-phase-out telos); each of the four phases carries all four contract elements (observable behavior, telemetry contract, advance criteria, retreat trigger), and an audit table reconciles the two pre-existing in-platform usages (the release-executor rollout ladder and the security-hook `warn / enforce / off` machine) into one place graded PARTIAL — it does NOT author a parallel enum (duplicate-source) and does NOT retrofit the hook layer (out of scope). Recorded in ADR-034 (lift-and-extend). The release-executor `references/progressive-rollout.md` was re-pointed to cite the convention as canonical via `pmo-skill-editor`; the `release-executor.skill` package was rebuilt at this close (Check 7 green). **#165:** `core/schemas/touchpoint-phaseout-schema.md` defines the 16-field operator-touchpoint inventory (`touchpoint_id` … `last_phase_advanced`) plus a phase-out-plan FMEA overlay (`success_criteria` / `slo` / `risks` with severity+likelihood+detectability / `rollback_path` / `phase_sequence`); `current_phase` references the #164 enum by name rather than hard-coding the list, keeping the two consistent by construction. A new `deploy.sh --check` Check 40 validates an operator-local instance's structure when present, skips cleanly when the instance is absent (fresh clone / CI), and runs warn-mode-initial. The populated inventory (≥1 row per pipeline stage + pilot phase-out rows) is a K4 instance authored operator-local and git-ignored per ADR-012 — the corpus ships the grammar, not the content. **Canonical-state note:** a single ADR (ADR-034) shipped covering the convention; no ADR-035 exists on main. **Version note:** provisional v2.12 → re-versioned up to **v2.14** at Stage 12 (a sibling shipped v2.13 mid-flight, so tagging v2.12 would have broken the next release's `git describe`); `origin/main` advanced during the pipeline and a forward-merge reconciled one `deploy.sh` check-number collision — this release's touchpoint check became **Check 40** (the #1643 `.version`-drift Check 39 already on main was kept). CHEAP reversibility / HIGH confidence (additive new files + `(future)`→LIVE reference flips; whole-release rollback = `git revert -m 1` of PR #1636 strands no consumer; re-run `./core/deploy/deploy.sh --deploy release-executor` post-revert to re-sync the skill mirror). PR #1636 merged to main via merge commit `404037103e265f6548fef106f1c38d9ba9694bd8`; signed-annotated (ED25519) `v2.14` tag at the merge SHA. #164 and #165 auto-closed on merge. 30-day Stage 13 outcome window per the `novel` class.

### v2.09 (2026-06-20) — Deploy + release-tooling health: 11 surgical fixes to the platform's own deploy and release scripts — several built-in checks that silently skipped their own job now run, and several maintenance scripts that pointed at pre-reorganization folder paths now resolve the live tree — sequenced D-C SINGLE (one branch, one commit per issue) because five issues edit `core/deploy/deploy.sh`; all additive or corrective, no application logic

`novel`-class release (Standard engagement / Deep Stage 9 / Stage 5 ALL / 30-day outcome window) on milestone #115 `37-deploy-sh-script-health` — 11 CHEAP-to-MODERATE surgical fixes to the deploy/release tooling, no new file classes. **deploy.sh write-contention cluster (5, serialized):** **#1104** corrected the Check-9 `MIRROR_PAIRS` source path (`release/rules/release-process.md` → `release/governance/release-process.md`) so the rules-mirror pair is enforced instead of silently SKIPped, + a missing-source WARN guard. **#984** applied the `remove_mirror_subtree`/root-cause pattern (the helper #88 introduced) to the supplementary-copy loop so a read-only-target copy failure reports an actionable cause rather than an opaque generic message. **#661** added an additive `18d`-class framework-catalog `canonical_doc` path-resolution sub-check (warn-mode-initial) to the Check 18 block + `check-version-anchors.py`. **#1036** tombstoned the silent Check 24 numbering gap, attached reactivation anchors to the dormant Check 11 and Check 30, and added a check-retirement registry section; the issue body's incorrect "Check 9 is dormant" assertion was corrected before Stage 5 (Check 9 is the live rules-mirror sync; the dormant checks are 11 and 30). **#86** was largely pre-resolved on live main (the Check 19 `|| echo 0` → `0\n0` arithmetic crash was already fixed); its scope was reconciled (G-PL1) to a verify-the-landed-fix-holds + a residual sweep of the two `|| echo 0` sites at 2837/2995 (both `jq 'length'` consumers, documented N/A). **Independent-file issues (6):** **#400** rewrote `blast-radius.sh detect_mirror_pairs` against the live `core/rules` topology (the detector guarded a dead `pmo-platform/engineering/rules` path and returned zero pairs) + `blast-radius-protocol.md` self-reference typo fixes (mirrors #1104's corrected topology). **#758** fixed extinct-path resolution in `compute-cycle-time.sh` + `synthesize-release-learnings.sh` (REPO_ROOT depth + an env → `operator.toml` → `${CLAUDE_WORKSPACE_ROOT}` instance-path ladder) and codified the `OPERATOR_INSTANCE_EVALS_RESULTS_PATH` token in `depersonalization-spec.md §4` + the operator-config template. **#760** replaced `check-doc-links.py`'s hardcoded `WORKSPACE_ROOT` with a param/env override so a relocated/sandboxed deployed tree can be validated. **#659** added absolute/worktree-glob allowlist forms for the six registered release tools in `script-execution-allowlist.txt` that had relative-only forms. **#1089** extended `cleanup-orphan-state.sh --release-close` to match version-prefixed `release/vX.Y-<slug>` branches and `agent-*` worktrees (previously `claude/*`-only). **#665** anchored/demoted `audit-mcp-usage.sh` per the operator's Stage-5 disposition (no runtime behavior change). **Verification:** all 11 re-verified file:line against the live 4977-line `deploy.sh` at Stage 4; three scope-shaping findings — #86 largely pre-resolved (verify-residual not re-fix), #1036's Check-9 dormancy citation factually wrong and corrected before engineering, #659 partially pre-resolved (`blast-radius.sh` already had absolute forms; six other tools did not). **Topology / over-band:** D-C SINGLE branch (one commit per issue → per-issue `git revert` granularity even under one PR); 11 > the 5–8 bundle-composition band with an operator-recorded override on capability-coherence grounds (one deploy/script-health theme, low dependency-edge density, all CHEAP–MODERATE surgical fixes; a split would fracture the deploy.sh cluster or separate the naturally-paired #758⇄#760 and #1104→#400 work). CHEAP–MODERATE reversibility / HIGH confidence — surgical corrections + additive checks (warn-mode-initial); whole-release rollback = `git revert -m 1` of PR #1551. PR #1551 merged to main via merge commit `581863b3d611356e4b88ed3891cc9e7e642a5dde`; `v2.09` tag at the merge SHA. The 11 parents are marked closed at Stage 13 (PR #1551 used safe phrasing; not auto-closed from the release PR). 30-day Stage 13 outcome window per the `novel` class.

### v2.04 (2026-06-20) — Close-out retrospective register template + a decided release-corpus public-vs-instance home: the per-release close-out ceremony ships as a public combined template (full Kerth retro + full PMBOK 7 lessons-learned) landed onto a ratified ADR-017-aligned decision (ADR-032) classifying every release-corpus surface S1-public-capability vs S3-operator-instance-content — #1412 designed-here (ADR; corpus migration execution DEFERRED), one combined #360+#361 template, `stage-13-close.md` §6 naming the register + its COEXIST relationship to the machine-generated learnings triple — 3 cards, two work-classes, no application logic

`cross-cutting`-class release (Tight engagement / Deep Stage 9 / Stage 5 ALL / 30-day outcome window) on milestone #154 `62-close-out-registers` — 3 cards in two work-classes, all corpus authoring (a NEW design-only ADR + a NEW public template + a `stage-13-close.md` §6 edit), no application logic. **#1412 (DESIGN ONLY this release):** `core/ADRs/ADR-032-release-corpus-public-vs-instance-split.md` (Accepted) applies ADR-017's S1-Package-vs-S3-State cut to the release corpus across five sub-decisions — `RELEASE_LOG.md` / `RELEASE_DIGEST.md` / `notes/` / `plans/` are **S3 → operator-instance content**; `CHANGELOG.md` (root) + an **empty `RELEASE_INDEX.md` seed** are the **thin S1 → public** surface (preserving public navigation shape + a concise changelog without the maintainer's per-release narrative); `release/references/templates/*`, `standards/*`, `pipeline/*`, `release/tools/*`, and the `core/deploy/` checks are **S1 → public** mechanism. The load-bearing rule: **the capability ships; the content instances.** The ADR resolves #1412's 5 open questions, pins the instance-root resolver to the canonical **`${CLAUDE_WORKSPACE_ROOT:-$HOME/Claude}`** (explicitly NOT the ADR-017-named orphan `PMO_INSTANCE_PATH` — the #504/#528/#529 path-portability defect), and chooses a **HEAD-only removal** (`.gitignore` + `git rm --cached` + physical move) over an **irreversible git-history scrub** — the rollback-infeasibility note records that a `git filter-repo` rewrite would be IRREVERSIBLE on a public repo, force-push is denied in repo settings, and `refs/pull/*` retain pre-rewrite commits regardless, so a rewrite is *insufficient* to flip content private while HEAD-only stays MODERATE / `git revert`-reversible (the content is non-PII operational detail; the threat model is "don't ship it in new clones"). The corpus **migration EXECUTION** (`.gitignore` block + negations; `git rm --cached` + move of LOG/DIGEST/notes/plans; `RELEASE_INDEX.md` → empty public seed; re-point Check 32 + `generate_release_index.py` + `automated-closeout.sh` + `stage-13-close.md` §5 Phase B + `release-corpus-schema.md`; converge the 21 `deploy.sh` `PMO_INSTANCE_PATH` fallthroughs onto `CLAUDE_WORKSPACE_ROOT`; `install.sh` bootstrap) is **DEFERRED** to a complete-by-construction follow-up issue filed at Stage 13, enumerated in full in the ADR's deferred-migration plan. ADR number provenance: authored as **ADR-029** in the Stage 5 design, renumbered to **ADR-032** at Stage 6 because ADR-029 (Memory SSOT), ADR-030 (hook-registry drop-in), and ADR-031 (autonomy-ceiling hook) all landed first during the engineering window (`check-adr-numbers.py` confirmed 032 as the gap-free next number); `core/ADRs/README.md` carries the catalog entry. **#360 + #361 (combined template — D-RegisterShape "one combined template" + D-Right-Sizing "full ceremony"):** one `release/references/templates/release-learnings-register-template.md` ships PUBLIC carrying a **full Kerth retrospective** (Prime Directive recited first + the 3-question framework what-went-well / what-we-learned / what-we'd-change + the ritual of closure) and a **full PMBOK 7 lessons-learned** block (Situation / Outcome / Lessons table / Next-cycle Actions table). `release/references/pipeline/stage-13-close.md` §6 names the register and states its **COEXIST** relationship to the machine-generated `#### Release Learnings v<X.Y>` triple H4 block — directional: the triple's three fields (`Surprise` / `Would-change` / `Watch-for`) are **seed inputs** the operator transcribes and expands into the register's Kerth + PMBOK sections; the triple remains machine-generated and authoritative for the cross-release pattern-detector (`synthesize-release-learnings.sh --mode pattern-detect`) and is unaffected by the register's presence or absence. **Half-migration friction (flagged, not resolved):** because the #1412 migration is deferred, the operator-instance corpus root `${CLAUDE_WORKSPACE_ROOT:-$HOME/Claude}/personal/pmo-instance/releases/` is NOT yet bootstrapped (the `install.sh` skeleton is part of the deferred execution) and `<OPERATOR_INSTANCE_RELEASE_LOG_PATH>` still resolves to the in-repo `release/releases/RELEASE_LOG.md` per the `release-notes-standard.md` footnote — so all five corpus surfaces this release (RELEASE_LOG VERIFIED transition + INDEX + DIGEST + NOTES + CHANGELOG) are written to their **in-repo** paths under the pre-extraction resolution rule; no corpus migration, `.gitignore` edit, or toolkit re-point was performed. The first filled close-out register (the v2.04 dogfood, satisfying #360/#361 AC6) was written to the canonical instance path, which had to be **created at close** because the bootstrap has not run. CHEAP reversibility / HIGH confidence (additive-only — a NEW template + a NEW design-only ADR + a `stage-13-close.md` §6 edit; whole-release rollback = `git revert -m 1` of PR #1533; the new template has no prior consumer to unwind, and no release records were moved). Stage 7/8 reduced to `deploy.sh --check` (green) + LLM-graded conformance per the governance-release Stage Applicability Matrix; Stage 9 Plan Review GO (`cross-cutting` → Deep depth). **Version reclamation:** scoped `v2.07` at Stage 4 → re-versioned up to a provisional `v2.10` at Engineering Commit 0 (v2.07/v2.08 had shipped, `v2.07_RELEASE_PLAN.md` was taken by a different release — Ambient Intake Automation — and two `release/v2.09-*` branches were in flight) → **reclaimed the original abandoned `v2.04` slot** at the Stage 12 gate (operator decision 2026-06-20; v2.04 was the held v2.04-close-out-registers attempt, PR #1407 never merged; `git ls-remote --tags origin v2.04` confirmed no v2.04 tag — slot free). A deliberate backwards-version reclamation numerically below the shipped v2.05–v2.08; the operator accepted the `git describe` non-monotonicity per the no-versionless-as-hedge + decide-on-merits discipline. The release branch `release/v2.10-close-out-registers` is retained for traceability only (deleted at Stage 13 cleanup); the tag, plan filename, PR title, and commit scope all carry **v2.04**. PR #1533 merged to main via merge commit `cc10af42dfb2d4771b74107513c0e9cdd050486b`; signed-annotated (ED25519) `v2.04` tag points at the merge SHA (signature verified `Good "git" signature`, zero signing bypasses). The 3 cards are marked closed at Stage 13 (PR #1533 used safe phrasing; not auto-closed from the release PR). 30-day Stage 13 outcome window per the `cross-cutting` class.

### v2.07 (2026-06-20) — Ambient intake automation, governed by one operator-set dial: the daily-processing cycle becomes a governed, scheduled, self-reporting capability bounded by a single plain-language `automation_level` ceiling (off / recommend / bounded_auto) per `effective = min(automation_level, per-action max)` — an `operator.toml` dial (#322), an ambient inbox + dedup cursor (#1159), scheduled Path-A intake sweeps (#1160) and Path-B external-sync with evidence-gated auto-close (#1161), a consolidated daily-status digest + sweep-health heartbeat (#1162), and a unified payload-triggered PreToolUse autonomy-ceiling enforcement hook shipped warn-mode-initial (#1163, ADR-031) — 6 cards, governance/spec/config + one hook + one SKILL.md edit, no application logic

`novel`-class release (plan-declared — the milestone carries no `release-class:` label; Standard engagement / Deep Stage 9 / Stage 5 ALL / 30-day outcome window) on milestone #187 `10-ambient-intake-automation` — 6 cards taking ambient intake from a manual, unguarded ritual to a governed, scheduled, self-reporting capability. **#322 (C0 — `automation_level` governor):** adds an `[automation]` table with the plain-language `automation_level` ceiling (`off` / `recommend` / `bounded_auto`) to `core/config/operator.toml.template`, a workspace-wide ceiling all ambient automation reads per `effective = min(automation_level, per-action max)`; the irreducible-human set is never unlocked regardless of dial position. **#1159 (C1 — ambient inbox + dedup cursor):** `core/standards/c1-ambient-inbox-cursor.md` specifies a watched inbox drop-zone + a content-hash dedup cursor so the same artifact is never ingested twice across sweeps. **#1160 (C2 — Path-A scheduled intake sweep):** `core/standards/c2-intake-sweep-path-a.md` specifies the scheduled capture→classify→register sweep over the ambient inbox + drive transcripts/emails, clamped by `automation_level` (a `recommend`-ceiling sweep surfaces drafts and writes nothing above Tier 1). **#1161 (C3 — Path-B external sync + evidence-gate auto-close):** `core/standards/c3-external-sync-path-b.md` specifies the snapshot-diff external-sync (Jira / Confluence / Smartsheet MCP) reconciling tracker state and proposing **evidence-gated** closes (a close fires only when the gate evidence is present). **#1162 (C4 — consolidated digest + heartbeat):** `operations/skills/daily-status/SKILL.md` + `operations/skills/daily-status/references/ambient-sweep-digest.md` roll every sweep into one consolidated daily-status digest with a **sweep-health heartbeat** so a silent sweep failure is impossible (a stalled sweep shows as a missing/last-run heartbeat). **#1163 (C5 — PreToolUse autonomy-ceiling hook v2):** a unified payload-triggered `core/hooks/block-autonomy-ceiling.sh` (442 lines) + `core/hooks/prime-autonomy-ceiling-cache.sh` + test fixture, registered in `core/settings.json.template` PreToolUse, shipped **warn-mode-initial** via `core/hooks/.autonomy-mode.template` — it blocks above-ceiling actions in the payload-detectable Tier-0 classes (governance / cross-domain) once the operator flips it to enforce after shakedown. **R-C5RECON** resolved to this single unified hook (the establishing **ADR-031**, `core/ADRs/ADR-031-autonomy-ceiling-unified-payload-triggered-hook.md`, Accepted), which **supersedes** the `core/standards/subagent-security-posture.md §4` subagent-tier design (the §4 region was reworked to cite ADR-031); the hook-registry drop-in `core/rules/bypass-mode-readiness/block-autonomy-ceiling.md` + the regenerated `bypass-mode-readiness.md` index landed alongside (ADR-030 registry from v2.06). MODERATE reversibility / HIGH confidence — the config dial, the four C1–C4 specs, and the SKILL.md edit are additive (CHEAP); the C5 PreToolUse hook mutates the runtime guardrail surface and is the moderate element (ships warn-mode-initial, operator-flipped to enforce), so whole-release rollback = `git revert -m 1` of PR #1460 + delete the `v2.07` tag, after which `.autonomy-mode` returns to its pre-release state and the hook no longer fires. **Post-deploy operator actions:** run `update.sh` to deploy the C5 hook; flip `.autonomy-mode` warn→enforce after shakedown. Dev Testing PASS-with-adjustments + QA 35/35 ACs PASS; Stage 9 Plan Review GO. **Combined Stage 12+13 close:** no separate Stage 12 chore PR landed a DEPLOYED row, so the RELEASE_LOG row is authored **directly at VERIFIED** (Phase A8 outcome-bound close — the binding is on the output set). The `daily-status.skill` package + content-hash sidecar are present on main (Check 7); runtime mirror sync via `./core/deploy/deploy.sh --deploy daily-status` is the operator/hub post-merge step. PR #1460 merged to main via merge commit `3d3dd508b5042e1bbde97e510ef14e00b7b750d5`; signed-annotated (ED25519) `v2.07` tag points at the merge SHA. The 6 cards are marked closed at Stage 13 (not auto-closed from the release PR). **Deferred from v2.07:** #1164 (C6 Teams connector) → backlog (unmet governed-Teams-MCP dependency, design in #1441); #296 (K4 onboarding) → milestone #52. Hook-scope observation: #1472.

### v2.08 (2026-06-20) — Artifact lineage graph + a recommend-only generated-surface lint: the generated-artifact surface (`08-Generated/` + promoted folders) gets a lineage graph and a graph-integrity lint, so duplicate / orphaned / stale / displaced AI-generated artifacts surface for operator approval instead of silently accumulating — horizontal-lineage frontmatter fields wired into both Domain A and Domain C of the artifact schema (#334) and a NEW Autonomy-Tier-1 recommend-only `artifact-lint` skill running five graph-integrity checks (#1165) — 2 issues, additive (a new skill + new optional schema fields), no existing field/file/behaviour removed or renamed

`novel`-class release (plan-declared — the milestone carries no `release-class:` label) giving the platform's generated-artifact surface a lineage graph and a graph-integrity lint on milestone #195 `11-artifact-lineage-and-generated-surface-governance` — 2 issues, the whole release being artifact-graph governance plus the recommend-only skill that reads it, not application logic. **#334 (horizontal-lineage frontmatter fields):** wires the horizontal-lineage scalar fields `parent_artifact` / `sibling_topic` / `supersedes` (with the `supersedes`/`superseded_by` inverse pair) into `core/schemas/frontmatter-schema.md` across **both Domain A (formal-baseline) and Domain C (synthesis)** so an A↔C lineage chain resolves; adds the **lineage-vs-provenance distinction table** fixing the boundary (lineage = horizontal edges *between generated artifacts* — parent→child, supersede; provenance = the *upstream human evidence* an artifact derives from, with `source_inputs` scoped out as a separate provenance concept not yet in-schema); records the **field-vs-verb composition note** (the scalars **compose against** the 7 MVP `relationships[]` verbs rather than extending them — a `supersedes:` scalar denotes the same edge as a `type: SUPERSEDES` relationship entry, no new enum value, the 7-MVP-type constraint unchanged); and specifies **sidecar `.meta.yml` parity** (lineage fields carry identically on non-markdown artifacts so edges stay grep-discoverable) plus a **WARN-not-BLOCK dangling-lineage rule** (an unresolvable lineage path is a flag, consistent with relationship-target resolution). Syncs `core/standards/lifecycle-states-canonical.md §3.2` (the `DRAFT → REVIEWED → APPROVED → PROMOTED → ARCHIVED` Artifact Workflow restatement gains the three lineage-graph fields, with a schema-home cross-pointer to `frontmatter-schema.md`) and reconciles `core/standards/template-protocol.md` §9 (`topic` → `sibling_topic`). **#1165 (artifact-lint — NEW skill):** authors `operations/skills/artifact-lint/SKILL.md`, a NEW **Autonomy Tier 1 (Recommend), recommend-only** skill that scans a project's generated-artifact surface (`08-Generated/` + promoted folders) and runs **five graph-integrity checks** reading the #334 lineage frontmatter — **orphan** (no recorded source → set-source or archive), **sibling-duplicate** (strict composite-key match → keep-current + supersede-edge, or merge), **stale-draft** (a draft older than the staging threshold → promote / archive / refresh), **displaced-content** (folder contradicts the type's canonical home → correct-folder), **version-chain** (an ordered `_v1.._vN` set → confirm head, archive superseded members) — staging a single report to `08-Generated/artifact-lint-YYYY-MM-DD.md` with per-finding proposed action + reversibility tier + confidence + cited frontmatter evidence, and performing **no file moves and no deletes** (the operator dispositions every finding). Ships the **17-row work-plan taxonomy** (`references/work-plan-taxonomy.md` — Canonical-10 ≡ the Domain-C synthesis `artifact_type` value set + Situational-7 ≡ the plan-type set + waterfall/cutover rows) that the displaced-content check uses as the canonical-target-folder lookup, plus `references/edge-cases.md`; registered in `core/deploy/deploy.sh` `OPERATIONS_SKILLS` (and the `build-skill-packages.sh` `ALL_SKILLS` roster), shipping its own `.skill` package + `.sha256` content-hash baseline; the per-skill output contract added to `core/schemas/per-skill-output-contracts.md` (4-section staged report). **FINDING-1 (dual state-read — the load-bearing input rule):** the stale-draft and version-chain checks read **`artifact_state` primary → `lifecycle_state` fallback**, because `artifact_state` (the §3.2 Artifact Workflow enum stamped by `artifact-generator`) is absent from `frontmatter-schema.md` while `lifecycle_state` is the schema's REQUIRED field — keying on one silently mis-reads every artifact carrying the other (false-negative stale drafts; phantom version-chain heads). **FINDING-2 (17-row taxonomy provenance):** no literal 17-row taxonomy survived in the canonical corpus (the original split card was deleted), so the taxonomy is an **evidence-grounded reconstruction operator-confirmed at Stage 9 Collective Review** as authoritative — Check 4 therefore treats a canonical-home mismatch as a firm (still recommend-only) finding. **Stage 5 caught a third #334 reconcile target** (`template-protocol.md` §9, `topic`→`sibling_topic`) beyond the two named at planning. CHEAP reversibility / HIGH confidence (additive-only — a NEW skill + new optional schema fields + a synced restatement; whole-release rollback = `git revert -m 1` of PR #1468 + delete the `v2.08` tag; the new skill has no prior consumer, the optional lineage fields default-absent, and the schema additions have no external reader to unwind; re-run `./core/deploy/deploy.sh --deploy artifact-lint` post-revert to re-sync the skill mirror). Stage 7/8 reduced to `deploy.sh --check` (green) + LLM-graded conformance per the governance-release Stage Applicability Matrix; Stage 9 Plan Review GO. **Combined Stage 12+13 close:** no separate Stage 12 chore PR landed a DEPLOYED row, so the RELEASE_LOG row is authored **directly at VERIFIED** (Phase A8 outcome-bound close — the binding is on the output set, not on a DEPLOYED→VERIFIED two-step). The v2.08 branch merged current main in (incorporating #1461/#1474) before merge; the `artifact-lint.skill` package + content-hash sidecar are present on main (post-main-merge rebuild, Check 7). PR #1468 merged to main via merge commit `17955bc2cee02beac08606ac0f4bd56f1471ca74`; signed-annotated (ED25519) `v2.08` tag points at the merge SHA. The 2 cards are marked closed at Stage 13 (not auto-closed from the release PR). **Forward-binding follow-up:** `core/artifact-workflow-protocol.md` is a planned co-located protocol doc — when it ships, `lifecycle-states-canonical.md §3.2`'s authoritative-source pointer updates to cross-reference it as the operational-protocol home (handled at that release's Stage 13).

### v2.13 (2026-06-20) — Finding-disposition discipline: finding-disposition and deferral handling become uniform across the pipeline — behavioral acceptance criteria survive the gates intact, requirements-clarity rejects route upstream, and deferred items gain validity criteria plus a re-evaluation cadence. Milestone `63-finding-disposition-discipline` (#100 / #215 / #221 / #285), PR #1657 (merge `b2c12ad`). CHEAP reversibility — `git revert -m 1` of PR #1657.

### v2.12 (2026-06-20) — Field-first intake enforcement: Stage 1 intake moves to field-level form enforcement and a field-first data architecture — structured Projects/Jira fields become the source of truth over label/body parsing, and the intake template gains explicit domain representation. Milestone `12-field-first-intake-enforcement` (#27 / #54 / #344), PR #1699 (merge `28860f1`). MODERATE reversibility — `git revert -m 1` of PR #1699.

### v2.11 (2026-06-20) — Role-Specialist delivery coverage: twelve principal-level role-Specialist agents — Product Owner, Business Analyst, Program/Project/Portfolio Manager, Scrum Master, Release Train Engineer, Principal/Software Engineer, Architect, QA Lead, DevOps/SRE — each composing the platform's existing function-skills rather than re-implementing them. Milestone `04-ROLE-delivery-coverage` (#450 + #1109–#1119), PR #1555 (merge `78ea10a`). CHEAP reversibility — `git revert -m 1` of PR #1555.

### v2.10 (2026-06-20) — Agent artifact disciplines: two agent editing disciplines are codified — canonical-form application and reconcile-don't-annotate — and wired into the decision discipline. Milestone `24-agent-artifact-disciplines` (#144 / #413), PR #1556 (merge `cb81165`). CHEAP reversibility — `git revert -m 1` of PR #1556.

### v2.06.1 (2026-06-20) — Deploy-detection hotfix: repairs `deploy.sh --deploy` change-detection broken by a sidecar mis-enumeration and a BSD-`sed` alternation no-op, so the deploy path again correctly detects and syncs edited skills on macOS. Hotfix of milestone `36-ci-gate-trustworthiness-and-parallel-pr-safety` (#1475), PR #1474. CHEAP reversibility — `git revert -m 1` of PR #1474.

### v2.06 (2026-06-20) — CI-gate trustworthiness + parallel-PR safety: the platform's automated checks get a stated contract — a green check must mean the thing it claims is actually true, verified against real content rather than a stand-in signal — plus parallel-PR safety hardening. Milestone `36-ci-gate-trustworthiness-and-parallel-pr-safety` (#1101 / #673 / #18 / #90), PR #1461 (merge `a74f814`). CHEAP reversibility — `git revert -m 1` of PR #1461.

### v2.05 (2026-06-19) — Agent-discipline codification: a cluster of working agreements that had guided the platform's agents session-to-session now live in the platform's own rules, and a new architecture decision defines how knowledge moves from an agent's memory into the durable corpus — and how the redundant copy is cleaned up once it lands. Milestone `35-agent-discipline-codification` (#58 / #63 / #132 / #426 / #427 / #502 / #527 / #530 / #531 / #546), PR #1409. CHEAP reversibility — `git revert -m 1` of PR #1409.

### v2.03 (2026-06-18) — Role-skill factory foundation + first two pilots: the role-Specialist factory exists end-to-end and ships its first two products — a feeding-document consumption path wired into `pmo-skill-refiner` (#186), the 14-section `pmo-feeding-document-v02.md` format reference and 5 `operations/skills/_shared/` building-block files all role skills consume (#186), the 2 pilot agents pmo-technical-program-manager (composes pmo-technical-analyst + delivery-engine) and pmo-program-coordinator (composes tracker-manager + daily-status) built via the refiner (#185), and the feeding-document format locked post-Phase-1 (#185) — 2 skill-build cards, no application logic beyond the skills themselves

`novel`-class release (Standard engagement / Deep Stage-9 review / 30-day outcome window) standing up the **role-Specialist factory and its first products** on milestone #284 (epic `skill-role-build`) — 2 skill-build cards in a strict #186 → #185 serialization (the format-lock gate: #185 cannot start until #186's foundation exists). **#186 (Phase 0 — factory foundation):** wires a **feeding-document consumption** path into `release/skills/pmo-skill-refiner/SKILL.md` as the pre-Interview input branch of Mode 2 (Create New), authored through `pmo-skill-editor` discipline (impact + coherence + regression on the migrated skill) with a `version:` bump; authors the **14-section format reference** `release/skills/pmo-skill-refiner/references/pmo-feeding-document-v02.md` as the contract the factory reads; authors the **5 `operations/skills/_shared/*.md`** building-block files (`behavioral-markers`, `anti-pattern-catalog`, `five-model-variations`, `deployment-strategies`, `lifecycle-gates`) as the durable shared-resource surface every later role skill consumes; and adds a **fail-CLOSED `core/deploy/deploy.sh` Check 5(a) skip** as an **exact-match `== _shared` guard** (NOT a fail-open `_*` wildcard) plus a convention comment, preserving the gate's fail-closed property — a future stray `_`-prefixed directory still FAILs roster-drift unless explicitly allowlisted. **#185 (Phase 1 — pilots + format lock):** builds the 2 pilot skills via `pmo-skill-refiner` consuming their feeding docs — `operations/skills/pmo-technical-program-manager/SKILL.md` (composes `pmo-technical-analyst` + `delivery-engine`; modes Technical-Delivery Risk + Release-Readiness Orchestration) and `operations/skills/pmo-program-coordinator/SKILL.md` (composes `tracker-manager` + `daily-status`; modes Cadence Sync + Tracker Reconciliation) — each **composing its constituent skills by invocation, not re-implementation**, carrying the `skill_discipline_migrated_v10_2` migration flag so future edits route through `pmo-skill-editor`, registered in `OPERATIONS_SKILLS`, shipping its own `.skill` package; then **locks the feeding-document format** (a post-Phase-1 "changes require a governance update" note added to `pmo-feeding-document-v02.md`) so the factory reads a stable contract going forward. Engineering Commit 0 landed the committed release plan `release/releases/plans/v2/v2.03_RELEASE_PLAN.md`. **Scope change — #450 (`pmo-architect`) DEFERRED to `04-ROLE-delivery-coverage`:** the third role-Specialist was deferred because its compose-the-mode seam converges with a stub until `pmo-principal-engineer` (#1116) ships in milestone 04; the deferral is recorded on #450, which carries the next milestone. CHEAP reversibility / HIGH confidence (additive-only — whole-release rollback = `git revert -m 1` of the release PR; the 5 `_shared/` files have no prior consumer to unwind, the deploy.sh `== _shared` guard reverts cleanly; re-run `./core/deploy/deploy.sh --deploy` post-revert to re-sync the skill mirrors). Stage 7/8 reduced to `deploy.sh --check` (green) + LLM-graded conformance per the governance-release Stage Applicability Matrix; Stage 9 Plan Review GO (`novel` → Deep depth). **Combined Stage 12+13 close:** no separate Stage 12 chore PR landed a DEPLOYED row, so the RELEASE_LOG row is authored **directly at VERIFIED** (Phase A8 outcome-bound close — the binding is on the output set, not on a DEPLOYED→VERIFIED two-step). PR #1280 merged to main via merge commit `cc094b4409825ed95f2e1dd1d60f7b5b4e9aa4be`; signed-annotated `v2.03` tag points at the merge SHA. The 3 `.skill` packages (`pmo-skill-refiner` rebuilt + the 2 pilots built) were confirmed fresh on main at Stage 13 — a Stage-13 rebuild reproduced byte-identical content, so no package re-commit was needed. The 2 cards are marked closed at Stage 13 (PR #1280 used `References #186, #185`; not auto-closed from the release PR). 30-day Stage 13 outcome window per the `novel` class.

### v2.02 (2026-06-18) — Bundling capacity + sizing/parse-rate gates: the pipeline's Bundle stage enforces capacity and sizing as gates rather than guidance — a risk-weighted release-capacity model keyed on Release Class with sizing-guidance consolidation and a pinned rounding mode (#290, ADR-027), a per-milestone story-point-sum gate that routes oversize milestones to disposition (#294, G3-15), a Mode-A ≥90% parse-rate gate against the conformant-bundleable denominator (#293, G3-14), and a RELEASE_LOG velocity instrument capturing planned-vs-delivered / files-changed / allocation actuals (#281) — 4 governance/spec/schema cards, no application logic

`novel`-class release (introduces a new RELEASE_LOG velocity instrument + two new gate criteria + multiple D-class planning decisions) making the pipeline's Bundle stage enforce capacity and sizing as **gates, not guidance** — 4 governance/spec/schema cards on milestone #153 (Pipeline Definition & Fitness), all corpus authoring (doctrine, gate-criteria spec, config schema, RELEASE_LOG instrument), no application logic. Two cards were re-scoped on verify-before-recommend: #290's 15–25-pt sizing band already existed (doctrine §3 Step 5) so its residual is the **risk-weighted multiplier + consolidation**, and #281's cycle-time half already shipped so its residual is the **other three velocity signals**. **#290 (capacity TARGET):** a **risk-weighted release-capacity sub-block** in `release/references/standards/bundle-composition-doctrine.md §3 Step 5` where a higher-ceremony release's raw story-point sum counts for more against the same band (`effective_pts = round_half_up(Σpts × class_weight)`); the previously-scattered sizing guidance (item-count vs point-band) **consolidated** into that one cross-referenced source; the **rounding mode pinned** `round_half_up` at the definitional home so the producer / enforcer / recorder cannot diverge at half-integer boundaries; and a `release_class_capacity_weights` inline table `{routine 1.0 / novel 1.15 / cross-cutting 1.3 / hotfix 0.9}` shipped as explicitly-labelled MEDIUM-confidence `[CALIBRATE-AFTER-3]` seeds in `core/config/platform-config.toml.template` (+ `core/schemas/platform-config-schema.md` field doc + `release/references/pipeline/stage-03-bundle.md` A4 cross-ref + `core/deploy/deploy.sh` Check 33 config-field allowlist extension so the new field is **gated, not merely uncovered** — FM3). The establishing **ADR-027 (Accepted)** records that the risk-weight axis **keys on the shipped Release Class, not a net-new per-issue Tier field** (the lowest-blast-radius canonicalization — Release Class already declares ceremony weight and is gate-enforced per milestone), with multiplier-over-additive-budget chosen on scale-invariance. **#294 (per-milestone size gate — G3-15):** a `G3-15` per-milestone story-point-sum gate in `core/schemas/gate-criteria-spec.md` asserting `effective_pts ≤ band-upper` and routing an oversize milestone to the **split / tight-merge disposition** (non-dead-end routing), with the enforcement cross-cite added to doctrine §3 Step 5 (same region #290 edits — serialized after #290 under D-C SINGLE); warn-then-enforce with a `[CALIBRATE-AFTER-3]` threshold. **#281 (RELEASE_LOG velocity instrument):** an additive velocity convention in `release/releases/RELEASE_LOG.md` capturing per-release velocity actuals (planned-vs-delivered points, files-changed, allocation) **alongside the already-shipped `Cycle-Time:` field**, codified in NET-NEW `release/references/standards/release-velocity-tracking.md` (field schema, label→work-class map, explicit-N/A discipline, grandfather/no-backfill policy, N=3 recalibration linkage) with the producing tool `release/tools/compute-release-velocity.sh` (`--self-test` green); the Stage-12/13 visible-H4 emit templates (`stage-12-execute.md`, `stage-13-close.md`, `hub-spoke-bridge.md`) gained the `**Velocity:**` field append step. The instrument is the empirical input feeding #290's `[CALIBRATE-AFTER-3]` recalibration after ≥3 tracked releases. **#293 (Mode-A parse-rate gate — G3-14):** a `G3-14` gate in `core/schemas/gate-criteria-spec.md` holding the `release-planner` Mode A dependency-graph parse rate to a **≥90% floor against the conformant-bundleable denominator** before Stage 3/4 may pass (documented operator override), with the Mode A AC cross-ref added to `release/skills/release-planner/SKILL.md`. `gate-criteria-spec.md` schema bumped **v1.11 → v1.12** additive (both gate rows + version-history block updated); gate-ID set `{#293 → G3-14, #294 → G3-15, #292 → G3-16 reserved}` (G3-13 is the ADR-012 tombstone, excluded). Engineering Commit 0 landed the committed release plan `release/releases/plans/v2/v2.02_RELEASE_PLAN.md`. CHEAP reversibility / HIGH confidence (additive-only — whole-release rollback = `git revert -m 1` of the release PR; every addition ships a default and no existing field/row/enum is mutated; ADR-027 + `release-velocity-tracking.md` + `compute-release-velocity.sh` are new files with no external reader to unwind; re-run `./core/deploy/deploy.sh --deploy release-planner` post-revert to re-sync the skill mirror). Stage 7/8 reduced to `deploy.sh --check` (green) + LLM-graded conformance per the governance-release Stage Applicability Matrix; Stage 9 Plan Review GO (`novel` → Deep depth). **Combined Stage 12+13 close:** no separate Stage 12 chore PR landed a DEPLOYED row, so the RELEASE_LOG row is authored **directly at VERIFIED** (Phase A8 outcome-bound close — the binding is on the output set, not on a DEPLOYED→VERIFIED two-step). PR #1278 merged to main via merge commit `a081fec36b6e5551df8564d7fa2457e27d8f00c5`; signed-annotated `v2.02` tag points at the merge SHA. The `release-planner.skill` package is rebuilt at Stage 13 (Check 7 package-freshness, resolving the deferred staleness — #293 edited the SKILL.md); runtime mirror sync via `./core/deploy/deploy.sh --deploy release-planner` is the operator/hub post-merge step. The 4 cards are marked closed at Stage 13 (PR #1278 used `References #281, #290, #293, #294`; not auto-closed from the release PR). **Calibration debt (operator-awareness, not a blocker):** the `release_class_capacity_weights` seeds + both gate thresholds remain MEDIUM-confidence `[CALIBRATE-AFTER-3]` until the 3-release recalibration; the velocity instrument's capture viability is unproven (the sibling Cycle-Time field has not yet produced a non-N/A datum). 30-day Stage 13 outcome window per the `novel` class.

### v2.01 (2026-06-16) — Skill-function hardening (estimation / lifecycle / status): seven existing function-skills deepened (delivery-engine estimation enforcement + 15-stage lifecycle tracking + 10-gate enforcement + tech-debt capacity floor + Fowler-quadrant/CoD classification; ppm-agent intake-governance/WSJF + org-model/stale-RAID; tracker-manager RAID dedup + cascade; weekly-rollup watermelon via-public-api + metric governance; daily-status formula RAG + buffer; process-designer INVEST/GWT/NFR; technical-analyst ADR-immutability/FDD/DORA) — 12 EXTEND-class stories, no new skills

`novel`-class release (≥1 new reference doc + ≥1 D-class decision; cross-cutting count-gates not met) hardening seven existing PMO function-skills with estimation, lifecycle, gate, status, and quality-scoring enforcement — 12 EXTEND-class stories across delivery-engine, ppm-agent, tracker-manager, weekly-status-rollup, daily-status, pmo-process-designer, and pmo-technical-analyst (no new skills). The whole release is **enforcement-wiring, not logic-invention**: all 12 stories add discipline-enforcement to skills whose reference substrate (`estimation-standards`, `lifecycle-stages`, `gate-definitions`, `metric-registry`, `watermelon-detection`, `intake-governance`, `escalation-thresholds`) was already built — Stage-5 design confirmed every story is ~80–85% wiring of pre-existing, in-corpus-grounded parameters. **delivery-engine** (5 cards, serialized on one SKILL.md): **estimation enforcement** establishing buffer/variance vocabulary against `references/estimation-standards.md` (#264); **15-stage lifecycle tracking** in `references/lifecycle-stages.md` (#265); **10-gate lifecycle enforcement** with PASS/CONDITIONAL/FAIL verdicts, gates sitting between the #265 stages in `references/gate-definitions.md` (#266); a **tech-debt capacity floor + aging + rework-rate** (NET-NEW `references/tech-debt-capacity.md`) where the rework-rate uses a **"not computable" fallback** when its input source is absent rather than inventing data (#366); and **Fowler-quadrant classification + Cost of Delay** (NET-NEW `references/tech-debt-classification.md`) ranking against #366's floor with a lexicographic sort and registering a `core/specs/framework-catalog.md` row (#180). **ppm-agent** (2 cards): **intake governance + WSJF scoring** (NET-NEW `references/ppm-intake-governance.md` — renamed to avoid the intake-desk basename collision; WSJF consumed *by reference* to `intake-desk/references/intake-governance.md §2`, not restated) (#253); **org-model detection** (derived from `delivery_approach`) **+ stale-RAID escalation** wiring the existing `references/escalation-thresholds.md` (#254). **tracker-manager**: **RAID deduplication** (≥0.70 similarity) **+ cascade detection** (SKILL.md + `references/tracker-schemas.md`) (#251). **weekly-status-rollup**: **watermelon detection** consumed **via-public-api** from the canonical 8-signal set `core/skills/pmo-qa-auditor/references/watermelon-detection.md` (referenced by role-name, *not* forked — the operations→core direction is pre-authorized cohesion) **+ metric governance**, with an **optional** `investment_class` project-template field (absent → "Unclassified" coverage gap) (#256). **daily-status**: **formula-driven RAG + buffer tracking** wired to existing registries with **no new `references/` dir** — SKILL-only edit (the `novel`-trigger new-dir input dissolved at scope-lock; class still `novel` on the 3 NET-NEW files elsewhere) (#260). **pmo-process-designer**: **INVEST scoring + Given-When-Then + NFR taxonomy** (ISO/IEC 25010) quality enforcement (SKILL.md + `references/process-checklist.md`) (#263). **pmo-technical-analyst**: **ADR-immutability + FDD quality scoring** (FDD 4.0/3.0 thresholds) **+ Accelerate/DORA metrics** (SKILL.md + `references/technical-review-checklist.md`) (#259). **Supporting ADR — ADR-028, formerly ADR-024 (Accepted):** operations skills consume a core safety-control reference **via-public-api by role-name, not by fork** — codifies the #256 watermelon reuse and reverses the Stage-4 plan's local-copy recommendation in favor of the Stage-5 design, on the reasoning that a forked green-masking control silently diverges the moment the owner refines a signal. CHEAP reversibility / HIGH confidence (additive-only — whole-release rollback = `git revert -m 1` of the release PR; per-card rollback = revert the card's commit; the 3 NET-NEW reference docs + ADR-028 are new files with no external reader to unwind; re-run `./core/deploy/deploy.sh --deploy` post-revert to re-sync the 7 skill mirrors). DT/QA: 12/12 PASS, 72/72 ACs. The 12 cards are marked closed at Stage 13 (not auto-closed from the release PR). PR #1240 merged to main via merge commit `97b66e12dd82ecb2131614577d9acf9314ba206e`; signed-annotated `v2.01` tag points at the merge SHA. The 7 `.skill` packages (`delivery-engine`, `ppm-agent`, `tracker-manager`, `weekly-status-rollup`, `daily-status`, `pmo-process-designer`, `pmo-technical-analyst`) are rebuilt at Stage 13 (Check 7 package-freshness); runtime mirror sync via `./core/deploy/deploy.sh --deploy …` is the operator/hub post-merge step. 30-day Stage 13 outcome window per the `novel` class.

### v2.00 (2026-06-15) — Skill-function hardening: five existing function-skills deepened (change-management Mode G adoption tracking + tiered Mode D hypercare with SLA compliance; release-executor quality-gate ladder + progressive rollout + documented close-out; pmo-qa-auditor 8-detector failure-mode battery + RACI gate; artifact-generator canonical lifecycle states + zombie detection; release-planner typed artifact linking) — 11 build cards, all EXTEND except the release-planner RECONCILE card; no new skills

`novel`-class release (≥1 new reference doc + ≥1 D-class decision; cross-cutting count-gates not met) hardening five existing PMO function-skills with structured adoption-science, hypercare, execution-gate, close-out, and detector capability — 11 build cards across change-management, release-executor, pmo-qa-auditor, artifact-generator, and release-planner, all EXTEND/RECONCILE (no new skills). **change-management** (5 cards, serialized on one SKILL.md): a named **Mode G — Adoption Tracking** surfaces an operator-runnable ADKAR barrier-assessment + training-timing validation (#1127), champion-ratio / sponsor-coverage / valley-of-despair prep (#1128), and change-fatigue + outcome measurement (#1129) — the methodology consumed by reference from the existing single-source framework doc (no duplicated scale or rule); **Mode D — Hypercare** gains a risk-classification step + a stable 3-tier set (HC-T1 Critical / HC-T2 Elevated / HC-T3 Routine, scored `Severity × Likelihood × Blast-Radius`; Severity+Likelihood adopted by reference from `delivery-engine/references/raid-templates.md §5`, only Blast-Radius net-new; band edges `[INFERRED]` for Stage-9 ratification) (#1130) plus the tracking+reporting half on top of it — each hypercare incident recorded as an existing-prefix `R-CM-###`/`I-CM-###` RAID entry with a met/breached verdict against the tier's committed SLA, a per-tier compliance report (events / met-vs-breached / breach detail), and an **open SLA breach as a hard, non-waivable Mode-D exit gate** (#1131). The new hypercare risk-classification + SLA sections *reference, do not replace* the existing 3-Tier Support model + S1–S4 severity→SLA matrix (orthogonal management-posture axis). **release-executor** (3 cards): a canonical 3-tier **Quality-Gate Ladder** (T1 schema validation → T2 cross-reference integrity → T3 stakeholder approval) that fires in order and short-circuits on the first hard fail, wired at both execution entry points (the git-native PR-merge pre-execution surface *and* the Cowork snapshot-apply path) from one source and reusing existing instruments (`lint_release_corpus.py --check schema` + a SKILL-frontmatter field-set assertion for T1; `check-doc-links.py --require-targets` for T2, where an unverifiable surface is a FAIL) — no new detectors invented; each FAIL emits a 5-field actionable finding (#244); a 3-cycle **progressive-rollout** model (**shadow** = evaluate+log, never block → **warn** = evaluate+notice, never block → **enforce** = evaluate+block/short-circuit) wrapping the ladder via a per-gate `rollout-cycle` attribute (default + fail-safe `enforce` so adoption is behavior-preserving; shadow/warn hits persisted to a per-rule append-only outcome-log; advancement an operator-gated Advance Checklist, never auto-promoted; only an `enforce` gate short-circuits, so #244's ordering holds scoped to enforce gates) — the one net-new tier is `shadow` (#245); and a **Mode D (Close Release)** documented build-out (RECONCILE — Mode D + `automated-closeout.sh` already exist): an explicit Step-2.5 issue-closure audit (a non-zero open-issue count is a blocking finding routing to manual close or carry-forward, never a silent close-over-open), carry-forward / deferred-item tracking as a named Step-6 close-out output reported every close (`none (clean close)` when empty), and a new `references/close-out-checklist.md` enumerating the Stage-13 checklist Mode D follows (#212). **pmo-qa-auditor** (P1): an **8-detector failure-mode battery** (automation complacency PROC / faceless PMO OUT / echo chamber INPUT / quality drift OUT / SPOF TRIG / breadth burnout PROC / AI hallucination INPUT / trust erosion HAND) in the new `references/failure-mode-detectors.md`, each authored to the failure-mode 5-field template + category tag + observable signature + numeric threshold + current-status read (so G7 structural conformance holds), with thresholds lifted from corpus precedent (SPOF `>=3 contexts`; push-to-resolve `>=80%`; echo-chamber round-over-round divergence; RACI single-Accountable; hallucination zero-tolerance) and the 2 operator-specific absolutes config-referenced, plus a **RACI validation gate (G9)**; the auditor's own Domain-Specific Failure Modes section is untouched (the 8 detectors are detection infrastructure, not the auditor's authoring surface — G7 self-conformance preserved) and the mode count stays 5 (#248). **artifact-generator**: **lifecycle state tracking + zombie detection + a documentation-debt register** adopting the platform-canonical Artifact Workflow vocabulary — the field **`artifact_state`** (replacing `status`) with the canonical 5 states **DRAFT → REVIEWED → APPROVED → PROMOTED → ARCHIVED** stamped `DRAFT` on emit (object-typed `Artifact-<STATE>`), the original 6-state intent mapped onto the canonical 5 (Active → PROMOTED) and the former `Superseded` value represented via the debt register rather than a frontmatter value, the health-check zombie step flagging artifacts unreferenced **>30 days** that are not yet `ARCHIVED` (flag-not-transition) into a documentation-debt register with recommended action + reversibility·confidence, and `references/lifecycle-states.md` re-specced as the artifact-generator application layer **over** the canonical state machine (references, does not restate it); the contained field rename verified to break no external reader (#252). **release-planner** (RECONCILE): **typed artifact linking** — a generated release plan labels each in-release edge with one of four §Category-4 relationship types (**GENERATES / DEPENDS_ON / BLOCKS / SUPERSEDES**) adopted verbatim from the ADR-018-frozen `core/schemas/frontmatter-schema.md` vocabulary (referenced, not redefined, so planner output and backlog relations cannot diverge), added as a **second, independent edge attribute** alongside the existing `edge_type` (FS/SS/FF/SF scheduling-precedence axis the CPM forward-pass consumes) — the two compose on one edge and the **CPM longest-path math never reads the artifact-relationship field**, so the regression delta is provably additive; edges are typed deterministically with default-to-`DEPENDS_ON` as the weakest-correct-claim safety net, and `release/tools/bundle-issues-parser.py --json` is read-only-extended with an additive `artifact_relationships` field (#246). **Key D-decision — D-246-Blocker resolved INCLUDE:** #246's "Blocked by #409" edge was reconciled-stale (the card needs only the already-built-and-frozen 7-type relationship vocabulary; #409 is an open `type:epic` umbrella whose own body carves #246 out as *composing against* the built vocab, and it does not close this release). MODERATE reversibility / HIGH confidence (whole-release rollback = `git revert -m 1` of the release PR; per-card rollback = revert the card's commit; the one behavior change to unwind is the artifact-generator `artifact_state` field default, contained — no other skill reads the value; re-run `./core/deploy/deploy.sh --deploy` post-revert to re-sync the 5 skill mirrors). **Version mapping:** the plan was drafted against the v3.x line (`.version` v3.19, latest tag v3.20) and **re-versioned to v2.00 at Engineering Commit 0** per the repo re-versioning lineage; `.version` reconciled v3.19 → v2.00 in the Stage 13 chore PR. The 11 cards are marked closed at Stage 13 (the release plan phrases per-card closure SAFE; not auto-closed from the release PR). PR #1200 merged to main via merge commit `42ee22cf1279ffff1c0172d95de092a159025baa`; signed-annotated `v2.00` tag points at the merge SHA. The 5 `.skill` packages (`change-management`, `release-executor`, `pmo-qa-auditor`, `artifact-generator`, `release-planner`) are rebuilt at Stage 13 (Check 7 package-freshness); runtime mirror sync via `./core/deploy/deploy.sh --deploy …` is the operator/hub post-merge step. 30-day Stage 13 outcome window per the `novel` class.

### v2.37 (2026-06-28) — **Published release notes can no longer silently drift from the note we wrote** — a `routine`-class release-notes close-out conformance slice making the close-out conformant on **every** authoring path (script AND hub-direct) and keeping the public GitHub Release page faithful to the in-repo source note. **#2120** extends the note-content lint to catch the posting defects the structural check missed (un-versioned Release title; repo-relative body links that 404 on the published page) via a whole-body link-purity check (lint check 13), reconciles the un-versioned title-format descriptions across `release-notes-standard.md` + `stage-12-execute.md` to the single canonical `vX.Y — <headline>` form, adds two `gh release view`-based LIVE-Release assertions to the Stage 13 close runbook, and syncs the eval rubric 12→13. **#2082** binds the §3.2 note-content lint to the close EVENT (script + hub-direct paths), so a finding for the closing version blocks milestone close regardless of how the release is finished — the RCA fix for the v2.26 governance failure. **#2085** makes the published Release body an enforced deterministic frontmatter-strip transform of the source note (never hand-assembled) with a standing drift gate (`check-release-body-drift.sh` / `deploy.sh` Check 47) asserting body-equals-note after the fact. **#1290 (absorbed, ms 72; ADR-048)** establishes that rigor is an invariant of scaffolding — a named Rigor-Invariance Principle + canonical-checklist attestation at the Procedure 1 scaffolding seam plus a scaffold-independent `deploy.sh` **Check 48** that asserts the complete Stage-13 output set on main for every `VERIFIED` `RELEASE_LOG` row (warn-mode-initial, dormant behind a cutover sentinel), so an abbreviated scaffold can no longer drop a codified close step undetected. Additive standard / lint / check / governance edits + one founding ADR; v2.37's own new gates are cutover-anchored after this merge (reflexive-loop). CHEAP / `git revert -m 1` of PR #2378.

#### Legacy `### Releases` table (migrated verbatim)

| Version | Date | Headline |
|---------|------|----------|
| v2.05 | 2026-06-19 | **Agent-discipline codification + the memory↔corpus SSOT boundary (ADR-029)** — a cross-cutting cluster of 10 issues promotes working agreements (read-before-edit, in-place bias, flag≠consent, AskUserQuestion timing, +) from operator memory into the durable corpus; the keystone frames a four-type memory architecture (Work / Knowledge / People / Learning) and fixes the Knowledge↔corpus SSOT cut with an encode-and-evict graduation lifecycle plus a warn-mode drift audit. Additive doc/governance; CHEAP / `git revert -m 1`. |
| v2.06 | 2026-06-20 | **CI/deploy gate trustworthiness + parallel-PR safety (gate-efficacy standard + ADR-030)** — a cross-cutting hardening so a green automated-assertion gate *means* its claimed invariant holds: the keystone gate-efficacy standard requires every `deploy.sh --check` + workflow gate to **assert by content not proxy**, **self-declare required-vs-advisory**, and be **auditable for false-confidence + coverage-gap** (#1101 — Check 7 freshness converted `mtime`→content-manifest-hash, required/advisory across 14 workflows); a Check-6 CI mirror sharing one predicate closes the run-context detection gap (#673); and ADR-030 breaks the bypass-mode hook monolith into per-hook drop-in sources + a generated-and-committed index with a warn-mode completeness check (Check 37) + always-enforce freshness check (Check 38), ending the parallel-PR cascade-conflict class and the 5/7/9 registry drift (#18). A pre-commit-layer spike closed NOT-needed (#90). Additive standard + checks + doc restructure; CHEAP / `git revert -m 1`. Hotfix v2.06.1 repaired two `--deploy` detection bugs (#1475). |
| v2.08 | 2026-06-20 | **Artifact lineage graph + a recommend-only generated-surface lint** — a `novel`-class release giving the generated-artifact surface (`08-Generated/` + promoted folders) a lineage graph and a graph-integrity lint, so duplicate / orphaned / stale / displaced AI-generated artifacts surface for operator approval instead of silently accumulating. **#334** wires the horizontal-lineage scalar fields **`parent_artifact` / `sibling_topic` / `supersedes`** (inverse `superseded_by`) into `frontmatter-schema.md` across **both Domain A and Domain C** (so an A↔C chain resolves), adds the **lineage-vs-provenance distinction table** (lineage = edges *between generated artifacts*; provenance = *upstream human evidence*), a field-vs-verb composition note (composes against the 7 MVP `relationships[]` verbs — no new enum), sidecar `.meta.yml` parity + a WARN-not-BLOCK dangling-lineage rule; syncs `lifecycle-states-canonical.md §3.2` and reconciles `template-protocol.md` §9 (`topic`→`sibling_topic`). **#1165** authors the NEW **`operations/skills/artifact-lint/`** skill (**Autonomy Tier 1, recommend-only — no file moves, no deletes**) running **five graph-integrity checks (orphan / sibling-duplicate / stale-draft / displaced-content / version-chain)** that stage a single report with per-finding action + reversibility tier + confidence + evidence; ships the **17-row work-plan taxonomy** the displaced-content check uses, registered in `OPERATIONS_SKILLS` with its own `.skill` + `.sha256`. **FINDING-1:** dual state-read **`artifact_state` primary → `lifecycle_state` fallback** (keying on one mis-reads the other). **FINDING-2:** the 17-row taxonomy is an evidence-grounded reconstruction operator-confirmed at Stage 9. Stage 5 caught a third #334 reconcile file (`template-protocol.md`). Additive NEW skill + optional schema fields; CHEAP / `git revert -m 1` of PR #1468 + delete the `v2.08` tag. |
| v2.07 | 2026-06-20 | **Ambient intake automation — one operator-set dial, scheduled sweeps, a consolidated digest, and a runtime enforcement hook** — a `novel`-class release taking ambient intake from a manual, unguarded ritual to a governed, scheduled, self-reporting capability bounded by a single plain-language `automation_level` ceiling. **#322 (C0)** adds an `[automation]` table with `automation_level` (`off` / `recommend` / `bounded_auto`) to `operator.toml.template`, a workspace-wide ceiling read per `effective = min(automation_level, per-action max)` (the irreducible-human set never unlocks). **#1159 (C1)** specifies a watched ambient inbox + content-hash dedup cursor. **#1160 (C2 — Path A)** specifies the scheduled capture→classify→register intake sweep clamped by `automation_level`. **#1161 (C3 — Path B)** specifies the snapshot-diff external-sync (Jira / Confluence / Smartsheet MCP) with **evidence-gated** auto-close. **#1162 (C4)** rolls every sweep into one consolidated daily-status digest with a **sweep-health heartbeat** (a stalled sweep can't fail silently). **#1163 (C5)** ships a unified payload-triggered PreToolUse **autonomy-ceiling hook** (`block-autonomy-ceiling.sh`, registered in `settings.json.template`, **warn-mode-initial**) blocking above-ceiling actions in the Tier-0 classes (governance / cross-domain) once flipped to enforce; **R-C5RECON** resolved to this single unified hook (**ADR-031**, Accepted), which **supersedes** `subagent-security-posture.md §4`. MODERATE reversibility / HIGH confidence — additive config + specs + SKILL.md edit (CHEAP) plus the C5 runtime-guardrail hook (the moderate element, warn-mode-initial → operator-flipped to enforce); whole-release rollback = `git revert -m 1` of PR #1460 + delete the `v2.07` tag. Post-deploy: run `update.sh` to deploy the hook; flip `.autonomy-mode` warn→enforce after shakedown. Deferred: #1164 (C6 Teams) → backlog; #296 (K4) → milestone #52. |
| v2.10 | 2026-06-20 | **Two editing disciplines are now part of the platform's core** |
| v2.11 | 2026-06-20 | **Twelve role-Specialist agents now cover the core delivery roles** |
| 63-finding-disposition-discipline | 2026-06-21 | **Finding-disposition and deferral discipline, applied uniformly across the pipeline** |
| release-version-stamping (version-less) | 2026-06-20 | **The platform version file stays accurate after a release** — a `Routine`-class version-less release fixing #1643: the repo-root `.version` source-of-truth had frozen at `v2.08` *inside* the `v2.09`/`v2.10` tags (the bump was an unwritten manual habit that became permanently absent once Stage 13 was automated), so `notify-version-skew.sh` reported a perpetual "update available" no `git pull`/`update.sh` could clear (the stale value is committed at the source; `update.sh` only re-propagates it per ADR-017). 3-layer fix: **(L1)** corrects the stale value so `.version` tracks the shipped release line; **(L2)** makes the stamp an owned Stage 13 step — a new `phase_bump_version` in `release/tools/automated-closeout.sh` (dry-run/PR-body wired, `.version` added to the chore-commit staging list, idempotent) + declaring `.version` in the `release/references/pipeline/stage-13-close.md` output set + a `docs/UPDATE.md` clarification; **(L3)** adds `core/deploy/deploy.sh` **Check 39** asserting `.version` equals the latest reachable release tag (FAIL ≥2 minors behind, WARN-tolerant of the legitimate 1-version Stage 12→13 window), with regression coverage in `core/deploy/tests/test_version_stamping.sh` wired via `.github/workflows/install-tests.yml`. Version-less per operator Stage 4 decision following the `public-flip-install-blockers` / `cross-reference-integrity-ci` precedents — no tag, no GitHub Release. Additive close-out tooling + a warn-tolerant check; CHEAP / `git revert -m 1` of PR #1693. |
| 12-field-first-intake-enforcement | 2026-06-21 | **Intake checks more of a work item's form, and keeps its priority in sync** |
| 38-governance-cross-reference-currency | 2026-06-26 | **Stale internal links across the docs and skill files are corrected** |
| v2.37 | 2026-06-28 | **Published release notes can no longer silently drift from the note we wrote** — a `routine`-class release-notes close-out conformance slice making the close-out conformant on **every** authoring path (script AND hub-direct) and keeping the public GitHub Release page faithful to the in-repo source note. **#2120** extends the note-content lint to catch the posting defects the structural check missed (un-versioned Release title; repo-relative body links that 404 on the published page) via a whole-body link-purity check (lint check 13), reconciles the un-versioned title-format descriptions across `release-notes-standard.md` + `stage-12-execute.md` to the single canonical `vX.Y — <headline>` form, adds two `gh release view`-based LIVE-Release assertions to the Stage 13 close runbook, and syncs the eval rubric 12→13. **#2082** binds the §3.2 note-content lint to the close EVENT (script + hub-direct paths), so a finding for the closing version blocks milestone close regardless of how the release is finished — the RCA fix for the v2.26 governance failure. **#2085** makes the published Release body an enforced deterministic frontmatter-strip transform of the source note (never hand-assembled) with a standing drift gate (`check-release-body-drift.sh` / `deploy.sh` Check 47) asserting body-equals-note after the fact. **#1290 (absorbed, ms 72; ADR-048)** establishes that rigor is an invariant of scaffolding — a named Rigor-Invariance Principle + canonical-checklist attestation at the Procedure 1 scaffolding seam plus a scaffold-independent `deploy.sh` **Check 48** that asserts the complete Stage-13 output set on main for every `VERIFIED` `RELEASE_LOG` row (warn-mode-initial, dormant behind a cutover sentinel), so an abbreviated scaffold can no longer drop a codified close step undetected. Additive standard / lint / check / governance edits + one founding ADR; v2.37's own new gates are cutover-anchored after this merge (reflexive-loop). CHEAP / `git revert -m 1` of PR #2378. |
