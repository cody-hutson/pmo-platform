<!-- repo-integrity: allow-issue-ref -->
<!-- reference-durability: allow-link -->
<!-- reference-durability: allow-url -->
# RELEASE_LOG_ARCHIVE-v3

Archive segment of [`RELEASE_LOG.md`](RELEASE_LOG.md) — the **v3** release family.

This file is the same record as its parent ledger, relocated. It inherits its
parent's records class under `core/governance/RECORDS_POLICY.md`: a segment is a
disposition *destination*, never itself a disposition *source*. Nothing here is
a lesser record for having aged out of the working set, and nothing here is
eligible for a disposition its parent is not.

It lives in the same directory as its parent deliberately, so a recursive grep
over that directory still finds this content exactly as it did before the move.
Each block below retains its heading in the parent ledger, with a pointer here,
so every anchor into the parent still resolves.

Blocks are appended by `release/tools/sweep-release-corpus.py`, and this file is
never itself swept. Existing blocks are not rewritten, with one named exception:
the Stage-13 close-out injects an `**Outcome:**` field in place when a retro
`--outcome` names a release whose body already lives here. That injection is
idempotent, adds a single field to a single block, and removes nothing.

---
#### Deployment Log v3.97
**Files deployed:** build-philosophy-corpus (novel class) — one release PR #4133 (`release/v3.97-build-philosophy-corpus → main`, merge-commit `c290126c`), 2 delivery slices (#2601, #2379) on one branch (D-C SINGLE, fully serial), 5 files (+331/−17). The release gives the build-philosophy charter its **seventh** first-class value, **Portability** — the governed core binds to an external system only through an adapter, the external system's model lives inside that adapter and never redefines a governed concept, and host mechanism never leaks into host-agnostic capability text; the design-review litmus is *"does the governed concept survive a substrate swap?"* **#2379** lands the value as a value-table row in `core/disciplines/build-philosophy.md` plus a full five-surface coverage-matrix row with honest GAP markings, an enforcer-citations bullet, a §Systemic-finding paragraph, the frontmatter enum, the value count `Six`→`Seven`, and the `core/disciplines/README.md` value enum; indexes it as **DP-9** (index-only) in `core/standards/design-principle-register.md`, where **all nine** `governing_doc` pins are recomputed against the final file (never trusted from a projection — the precedent release shifted two pins by one line each) and DP-3's drifted `statement` is reconciled in-pass to regain `extend-before-create`; and founds **ADR-098** recording the decision kernel plus the **R-1 naming decision** (`Portability` supersedes the ticket's coined `substrate-independence`, which has zero corpus occurrences, while `substrate` already carries two other established senses — the litmus phrasing is retained verbatim). **#2601** flips the **BP — governance** and **BP — process** coverage cells from a stale `*(skill-wiring pending)*` to skill-wired citing verified consuming specialists, and reconciles the §Systemic-finding note to **zero residual** markers. The value ships **paired with a pre-existing enforcer** (the host-binding-leak detector + its registered leakage class, shipped 2026-06-21), satisfying ADR-081's never-naming-only invariant — which is why the Skills cell reads as enforced rather than GAP. **Premise-currency finding recorded:** #2379's "no conformance axis exists" premise is partly stale; the slice survives narrowed — the detector catches a leak *mechanically*, DP-9 supplies the *design-review* axis. No skill, hook, package, or runtime surface is touched.
**Mechanism:** git merge-commit (PR #4133 → main at `c290126c0250f2466f9a5dcf2dbf320f006a6f86`, **plain `gh pr merge --merge`, no `--admin` required** — branch protection did not block this release; `git revert -m 1` rollback convention, two-parent merge commit verified); signed-annotated tag `v3.97` at the merge SHA (annotated tag object `505cdbbd`, SSH-signed ED25519, `git tag -v` → *Good signature*; unique claim bound at the Stage-12 atomic ref-CAS via `claim-version.sh --bump minor`, zero signing bypasses). Phase B4: **no-op** — the changed set is 5 markdown/corpus files under `core/` plus the plan file; zero `skills/`, `hooks/`, `packages/`, or `core/rules/` paths, so no S-2 copy, no `.skill` rebuild, and no `deploy.sh --deploy` propagation is triggered (Phase J.5 correspondingly empty).
**Timestamp:** 2026-07-27 (release merge 2026-07-27T13:07:24Z UTC; signed-annotated tag `v3.97` at the merge SHA, tagger 2026-07-27T13:08:33Z UTC)
**Cycle-Time:** N/A — (T_GO=n/a; T_DEPLOY=n/a; mechanism: compute-cycle-time.sh) — content-only corpus release; no `gate-outcome/plan-review-go` anchor and zero `deployment-status/deploy-skill` / `deploy-harness` events logged for v3.97, which is the standard's defined `N/A` condition rather than a missing measurement.
**Velocity:** planned 2 pts / delivered 2 pts (1.00); files-changed 5; allocation 0/2/0 pts (feature/debt/protocol-slack); class novel (mechanism: `compute-release-velocity.sh v3.97 --milestone 277 --merge-sha c290126c`; recomputed after the Stage-13 D-1 manual close transitioned both membership issues to closed — the first read, taken while both were still open, returned delivered 0. Only #2379 carries a `size:*` label, so planned/delivered count one of the two membership issues; #2601 is unsized and contributes 0 pts to both sides, which is why the ratio is 1.00 rather than a partial).
**Result:** SUCCESS — release PR #4133 merged to main via a true two-parent merge commit (D-C SINGLE); v3.97 signed-annotated tag valid at the merge SHA; CI **39/39 pass**, MERGEABLE/CLEAN, `isDraft=false` at the merge instant. Pre-merge gates all cleared on the live head: Phase A.4 skipped per A.4.1 (no merge strategy declared in the plan; repo allows merge/squash/rebase, default `--merge` semantics applied), Phase A.5.1 divergence **0** (skip to A.5.6), **Phase A.5.6b FREE** (`claim-version.sh --dry-run` recomputed next-free = v3.97 against freshly-fetched authoritative refs, equal to the carried version — no re-version needed), Phase A.6.2 PASS (MERGEABLE/CLEAN, no polling required), Phase A.7.2 PASS (ready, not draft), Phase B0.2 zero dependent PRs (skip base-shift; Option A default, no stacked-base posture declared). Stage 9 punch list AI-1 / AI-2 / AI-5 / AI-6 cleared and re-verified pre-merge. Deferred and explicitly out of scope: AI-3 (`release-body-drift` — proven pre-existing on `main`; this PR touches `release/releases/notes/` zero times), AI-4 (DP-4 cosmetic index delta), AI-7 (§Systemic-finding ¶1 enumeration — pre-existing, byte-identical at merge-base).
**Outcome:** SUCCESS — **ATTAINED**, ALIGNED with the Release Outcome Statement (AFTER: `core/disciplines/build-philosophy.md` names **Portability** as a design principle — DP-9 in the register, index-only — so adapter/external-binding work has a review-time conformance axis (#2379); and the governance + process BP×Skills coverage cells reflect their actual wired/gap state rather than a stale *"pending"* (#2601)). Both Success Indicators verified on post-deploy `main`: the register carries a **DP-9 Portability** row whose `governing_doc` resolves to `core/disciplines/build-philosophy.md:54`, the line that actually carries the value's name; and the two BP-domain cells now read `*(skill-wired — …)*` naming their consuming specialists. **CIAC-1** PASS — `grep -c 'skill-wiring pending'` → **0** (baseline 3). **CIAC-2** PASS — whole-population name-match probe over all **9** DP pins: 9/9 resolve to a line carrying that principle's name, with a deliberate off-by-one negative control confirming the probe detects a shifted pin (the pin-shift precedent that motivated the check was therefore not repeated). The value ships **paired with a pre-existing enforcer** rather than naming-only. Recorded, non-blocking: the `*(skill-wired)*` claim remains a hand-maintained assertion with no automated guard; QC4-05 and G-CL6 are **N/A** (the plan declares no AV-N invariants and no Tier-A activated design artifacts); G-CL8 is **PARTIAL** in warn-mode — #2379's two declared docs both resolve and were modified in the release range, while #2601 declares *"None beyond the matrix itself"*, which carries a rationale but not the canonical `None — no documentation impact (rationale: …)` form. No downstream follow-ups filed.

#### Deployment Log v3.96
**Files deployed:** agent-finops-foundation (novel class) — one release PR #4032 (`release/v3.93-agent-finops-foundation → main`, merge-commit `ac31eb29`), 2 delivery slices (#3909 C1, #3910 C2, both `size:L`) on one branch (D-C SINGLE, one hard C1→C2 edge with a schema-freeze checkpoint between), 31 files (+2,090/−1). The release establishes the **data foundation** for Agent token-spend FinOps (epic #1494): a new `finops-usage-extractor` skill reads local Claude Code session transcripts **read-only** and normalizes per-session / per-in-transcript-subagent token counts (input / output / cache-write tier-split / cache-read / tool-use + cost-determining `service_tier`) into a config-resolved, git-ignored, operator-local JSONL **derived cache**, then attributes + rolls that spend up subagent→agent→session→work-item. **#3909 (C1)** adds the tracked K1 schema `core/schemas/finops-usage-store-schema.md` (frozen **v1.0.0** — `meta` / `session` / `subagent` kinds; `provider` reserved-optional; cache-write tier split + `service_tier` folded pre-freeze), the extraction module (`scripts/extract-usage.sh` — `--rebuild` DEFAULT idempotent/atomic-`mv`, `--incremental` opt-in keyed upsert, `#16` word→token heuristic as usage-less fallback), the OFF-by-default plan-gated `provider-usage-connector.sh`, SYNTHETIC session test-fixtures + `.expected.json` oracle, the `<OPERATOR_INSTANCE_FINOPS_STORE_PATH>` config token (`operator.toml.template [paths]` + `depersonalization-spec.md` §4 registration guarding Check-25 orphan + defensive `.gitignore` stems + `docs/platform-config-reference.md`), the skill on the `deploy.sh` `CORE_SKILLS` roster, the `script-execution-allowlist.txt` invocation forms, and founds **ADR-096** (D-DataHome). **#3910 (C2)** extends the schema **additively to v1.1.0** (`rollup` + `coverage` record kinds + optional `session.branch_switch` / `git_branches`), adds the attribution standard `core/standards/finops-attribution-convention.md` (ordered LOCAL-ONLY resolver + session→work-item mapping + explicit `unattributed` fail-visible bucket + coverage health threshold + summation invariant), the roll-up module (`scripts/rollup-attribution.sh`), and founds **ADR-097** (D-AttributionConvention). **Scope grain:** ships at **milestone-grain** per the Stage-5 r2 lean revision — milestone-grain attribution is reliable from local data alone; reliable **issue-grain** attribution + a hub-vs-spoke role split need a hub-emitted spawn-ledger marker and are deferred to the sibling milestone `agent-finops-intelligence` (milestone #293: estimation #3911, reporting #3912, calibration #3329). The built `.skill` package (`packages/finops-usage-extractor.skill` + `.sha256`) is rebuilt in-PR.
**Mechanism:** git merge-commit (PR #4032 → main at `ac31eb29f514eabab96b0b9198ee0971e317f2db`, `--admin` past branch protection; `git revert -m 1` rollback convention); signed-annotated tag `v3.96` at the merge SHA (annotated tag object `e048c5d9`, SSH-signed; unique claim bound at the Stage-12 atomic ref-CAS after forward re-versioning from provisional v3.93). Phase B4: the `finops-usage-extractor` `.skill` package rebuilt in-PR with a refreshed `.sha256` baseline; `deploy.sh --deploy` copies the new skill to the runtime skills root (roster SSOT = `CORE_SKILLS`).
**Timestamp:** 2026-07-26 (release merge 2026-07-26T07:39:38Z UTC; signed-annotated tag `v3.96` at the merge SHA, tagger 2026-07-26T07:40:30Z)
**Cycle-Time:** N/A (novel data-pipeline/skill release; no `plan-review-go` / `deploy-skill` / `deploy-harness` pipeline event logged for v3.96; mechanism: compute-cycle-time.sh)
**Velocity:** planned 16 pts / delivered 16 pts (1.00); files-changed 31; allocation 16/0/0 pts (feature/debt/protocol-slack); class novel (mechanism: `compute-release-velocity.sh v3.96 --milestone 292 --merge-sha ac31eb29`; delivered/allocation reconcile at the Stage-13 milestone parent-close — the 2 in-scope issues #3909 / #3910 are the complete membership, both closing at this Stage 13).
**Result:** SUCCESS — release PR #4032 merged to main via merge commit (D-C SINGLE, `--admin`; `git revert -m 1` rollback convention); v3.96 signed-annotated tag valid at the merge SHA; CI **24/24 pass**, MERGEABLE/CLEAN. Full staged pipeline ran per plan (novel class → Deep Stage 9): Stage 5 Solutioning (r1 + r2 lean/milestone-grain revision; Collective Review operator ADJUST→lean ratified 0 blocker / 3 Major adversarial-design fixes pre-schema-freeze — PR-2 cost-tier split + `service_tier`; FM-1 `--rebuild`-default + defined keyed upsert; **FM-2 resolve-time `git check-ignore` fail-closed exfil guard, exit 4** — the security-leak remediation: the r1 static-`.gitignore`-only control was fail-OPEN for a custom in-repo store path, converted to a fail-CLOSED write precondition; plus **CIAC-3 data-hygiene hardening** — synthetic session fixtures only, no real session-data value in any committed/published artifact); Stage 6 build (serial C1 #3909 → schema-freeze checkpoint → C2 #3910 on one branch); Stage 7 Dev Testing (extraction correctness, no-hardcoded-path grep on module SOURCE, `git status` git-ignore clean, FM-2 exit-4 + FM-3 exit-5 preflight guards, idempotence); Stage 8 QA (per-AC ACCEPT across #3909 / #3910 + CIAC-1 summation-reconciliation / CIAC-2 shared-path / CIAC-3 data-hygiene).
**Outcome:** SUCCESS
**Version lineage:** provisional-display **v3.93** (Stage-4 D-Version recorded determination) → re-versioned forward **v3.93 → v3.94 → v3.95 → v3.96** at the Stage-12 atomic ref-CAS, after concurrent sibling releases claimed v3.93 (#4029), v3.94 (#4030), and v3.95 (#4037) on `main` during this release's window (next-free minor above anchor v3.92; forward-only re-version-up, exactly the anticipated contention path — Deviation-Log (a)). The binding claim is the Stage-12 atomic ref-CAS at the tag. The two finops ADRs were provisionally **093/094** and renumbered forward through **095** to final **ADR-096** (D-DataHome) + **ADR-097** (D-AttributionConvention) as sibling releases merged ADR-093/094/095 first (global-monotonic allocation at Stage 6; contiguous per `check-adr-numbers.py`). The branch NAME `release/v3.93-agent-finops-foundation` retains the provisional v3.93 per the no-retroactive-rename rule. **No v3.93 / v3.94 / v3.95 tag is cut by this release** (those belong to #4029 / #4030 / #4037); no abandoned slot was tagged by this release → `disposition: none`, no `RELEASE_REVERSIONS` row.

#### Deployment Log v3.95
**Files deployed:** runtime-config-and-posture (novel class) — one release PR #4037 (`release/v3.93-runtime-config-and-posture → main`, merge-commit `67f1c0c5`), 5 delivery slices (#1212, #102, #340, #310, #339) on one branch (D-C SINGLE, P0 fully-serial), 39 files (+1,655/−32). The release lets an operator set the platform's runtime posture through canonical, config-resolved surfaces that survive `update.sh`, and back-fills the platform-config keys v2.00's features already referenced. **#1212** adds `[failure_mode_detectors]` (D6 `concurrency_ceiling = 5` / D8 `walk_back_rate_floor = 0.15`) + the `[progressive_rollout]` rollout-log surface to `core/config/platform-config.toml.template` on the existing sibling-section pattern (additive, disjoint); the schema/catalog mirror (`core/schemas/platform-config-schema.md` + `docs/platform-config-reference.md`) was completed in-release via #340's config-surface work (Stage-7 AI-001), so all four new sections carry documented defaults across template + schema + catalog. **#102** rewords **G2-06 AND G2-12** (symmetric twins; the D-G2-12 fold) in `core/schemas/gate-criteria-spec.md` + `release/references/pipeline/stage-02-triage.md` so the Stage-2 backlog-decision surface resolves *by config-field role* (GitHub Projects the unchanged default) — de-hardcoded from a single board. **#340** establishes the `[spoke_runtime]` canonical model/effort surface (`default_spoke_model` + `chip_model`; effort as a POINTER — no shadow-SSOT), rewires `deploy.sh` Check 27 to read it (de-hardcodes the prior `opus` literal), and founds **ADR-095** (supersedes-in-part the prior "two surfaces by design" stance; the two Anthropic-owned runtime carriers preserved as derived/verified). **#310** adds install opt-in security-hook activation (default OFF, update-durable via the XDG individual surface) + a durable master-enable shared-lib (`core/hooks/lib/master-enable.sh`, collapsing the 13–14 hook edits to one lib), under the **D-R9 SECURITY-EXCLUDED** floor — master-OFF governs workflow-class hooks only and never silently disables the egress / gh-path-leak / PII / credential guards; adds `deploy.sh` **Check 60** (master-enable ↔ D-R9 security-registry reconcile, warn-mode initial). **#339** adds the `core/hooks/verify-session-config.sh` SessionStart verifier (workflow-class, non-blocking) checking the launched session's model/effort against `[spoke_runtime]` (reads #340's surface, honors #310's master-OFF), registered alongside the two existing SessionStart hooks; the hook-registry glob was generalized so posture-hook fragments assemble into the mirror.
**Mechanism:** git merge-commit (PR #4037 → main at `67f1c0c5f2b37aa3a195eb517b79593f207f510c`, `--admin` past branch protection; `git revert -m 1` rollback convention); signed-annotated tag `v3.95` at the merge SHA (annotated tag object `8773f140`, SSH-signed; unique claim bound at the Stage-12 atomic ref-CAS after re-versioning from provisional v3.93). Phase B4: no `.skill` package rebuilt — the release touches config / hook / governance / schema surfaces, zero skill source; `deploy.sh --deploy` copies the runtime hooks + config template.
**Timestamp:** 2026-07-26 (release merge 2026-07-26T07:06:21Z UTC; signed-annotated tag `v3.95` at the merge SHA, 2026-07-26T07:07:05Z)
**Cycle-Time:** N/A (novel config/hook/governance release; mechanism: compute-cycle-time.sh)
**Velocity:** planned 22 pts / delivered 22 pts (1.00); files-changed 39; allocation 16/4/2 pts (feature/debt/protocol-slack); class novel (mechanism: `compute-release-velocity.sh v3.95 --milestone 183 --merge-sha 67f1c0c5`, recomputed after the Stage-13 Milestone parent-close transitioned the 5 membership issues to Done).
**Result:** SUCCESS — release PR #4037 merged to main via merge commit (D-C SINGLE, `--admin`; `git revert -m 1` rollback convention); v3.95 signed-annotated tag valid at the merge SHA; CI **44/44 pass**, MERGEABLE/CLEAN. Full staged pipeline ran per plan (novel class → Deep Stage 9): Stage 5 Solutioning (per-issue ASSUMPTION-CONFIRM; Collective Review scope-lock ratified the integrated config-surface design, effort-as-pointer, the D-R9 SECURITY-EXCLUDED scope, and the D-G2-12 fold); Stage 6 build (serial #1212 → #102 → #340 → #310 → #339; C1 `bypass-mode-readiness.md` + C4 `platform-config.toml.template` shared-surface contention resolved by build-order); Stage 7 Dev Testing (PASS — 5/5 per-issue AC sets MET, both CIACs MET, the D-R9 security-floor proof empirically demonstrated); Stage 8 QA (ACCEPT — 5/5 AC sets MET, both CIACs MET, 30 runtime assertions re-run, no Blocker/NOT-MET/PARTIAL); Stage 9 GO. Stage-12 conflict-resolve merged 33 advancing main commits KEEP-BOTH with three mechanical concurrent-collision resolutions (see Version lineage).
**Outcome:** SUCCESS — ALIGNED with the Release Outcome Statement (AFTER: an operator sets runtime posture — the Stage-2 backlog-decision surface (#102), security-hook activation (#310), and spoke model/effort (#340/#339) — through canonical, config-resolved surfaces that survive `update.sh`; and the platform-config keys v2.00's features already reference now exist with documented defaults (#1212)). Success Indicator met — all 5 issues' ACs MET (Stage-8 ACCEPT), both CIACs MET. Two new config-surface controls ship warn-mode-initial: `deploy.sh` **Check 60** (master-enable ↔ D-R9 reconcile) and the #339 SessionStart verifier (non-blocking). No downstream follow-ups filed; the stale plan Deviation-Log (b) was reconciled to the as-built state in this Stage-13 close-out chore PR (operator-directed inline resolution, not an issue).
**Version lineage:** provisional-display **v3.95** (Stage-4 recorded determination was v3.93) → re-versioned to **v3.95** at the Stage-12 conflict-resolve, after sibling releases claimed v3.93 (#4029) and v3.94 (#4030) on `main` during this release's Stage 7–9 window (next-free minor = v3.95; tags occupied v3.90–v3.94). One forward re-version hop, applied on-branch pre-merge; the binding claim is the Stage-12 atomic ref-CAS. Two mechanical numbering collisions were resolved KEEP-BOTH in the same conflict-resolve: **ADR-093 → ADR-095** (siblings merged ADR-093 [acceptance-fit] + ADR-094; full reference cascade — file rename + 5 referrers + 2 link paths; contiguous 001..095 per `check-adr-numbers.py`) and **`deploy.sh` Check 59 → Check 60** (sibling #3107 claimed Check 59 for slug-primary release-identity conformance; #310's master-enable ↔ D-R9 reconcile renumbered to Check 60). The branch NAME `release/v3.93-runtime-config-and-posture` retains the provisional v3.93 per the no-retroactive-rename rule. **No v3.93/v3.94 tag is cut by this release** (those belong to #4029/#4030); no abandoned slot was tagged by this release → `disposition: none`, no `RELEASE_REVERSIONS` row.

#### Deployment Log v3.94
**Files deployed:** intake-and-gate-protocol-hardening (routine class) — one release PR #4030 (`release/v3.93-intake-and-gate-protocol-hardening → main`, merge-commit `5aed5e60`), 4 parent issues (#779, #3303, #792, #1007) as delivery slices on one branch (D-C SINGLE, P0 fully-serial), 20 files (+633/−22) — 2 new ADRs + 2 `.skill` packages rebuilt. **#779** adds the Stage-2 acceptance-fit gate **G2-13** (scoped conditional-binding) to `core/schemas/gate-criteria-spec.md`, an **A4.6 Acceptance-Fit Determination** sub-step + a "reject is the expected default" doctrine to `stage-02-triage.md`, a `<!-- provenance: agent-authored -->` marker emitted by `intake-desk` Mode C (`stage-01-intake.md` + `operations/skills/intake-desk/SKILL.md`), an "acceptance is not a default" §2c doctrine in `intake-style-guide.md`, and founding **ADR-093**; ships **enforce from v3.94** (the shadow/recommend rollout was flipped to day-one enforce at the Stage-9 GO gate 2026-07-25), introducing-release-exempt (binds issues entering Stage 2 strictly after the v3.94 merge SHA). **#3303** extends the comment author-association trust boundary (ADR-076) to issue-body ingestion by the auto-executing intake/triage surface (`stage-01-intake.md` + `stage-02-triage.md` + `release-process.md` + `pipeline-triage` SKILL.md), ordered as an earlier Phase-A sub-step than A4.6 (the A0 ⊳ A4.6 ordering lock — trust decided before provenance); trust is read solely from the host author-association, never the body/marker (a spoofed marker only raises scrutiny). **#792** adds a module skill-consultation forcing function (self-check → in-house-first / Anthropic-backup) via a new `core/specs/skill-consultation-map.md` + `core/specs/anthropic-base-vs-build-registry.md` + a `release-process.md` clause (cites ADR-023). **#1007** adds an affirmative "extend-before-create" criterion at Stage-5 Solutioning as **SR-G6** — extending the existing SR-G structural-gate family over minting a new Gate-5 — in `gate-criteria-spec.md` + `stage-05-solutioning.md` + `core/disciplines/build-philosophy.md`, founding **ADR-094**; ships enforce from v3.94.
**Mechanism:** git merge-commit (PR #4030 → main at `5aed5e6015e47a5ad919f1815e0518b27b3d80e6`, `--admin` past branch protection; `git revert -m 1` rollback convention); signed-annotated tag `v3.94` at the merge SHA (annotated tag object, noreply tagger; unique claim bound at the Stage-12 atomic ref-CAS — re-versioned from provisional v3.93 when sibling #4029 tagged v3.93 first). Phase B4: `.skill` packages (`intake-desk`, `pipeline-triage`) rebuilt in-PR with refreshed `.sha256` baselines.
**Timestamp:** 2026-07-26 (release merge 2026-07-26T06:05:11Z UTC; signed-annotated tag `v3.94` at the merge SHA, tagger 2026-07-26T06:07:49Z)
**Cycle-Time:** N/A (routine intake/gate protocol release; mechanism: compute-cycle-time.sh)
**Result:** SUCCESS — release PR #4030 merged to main via merge commit (D-C SINGLE, `--admin`; `git revert -m 1` rollback convention); v3.94 signed-annotated tag valid at the merge SHA; CI **57/57 pass**, MERGEABLE/CLEAN. Full staged pipeline ran per plan (routine class → Standard Stage 9): Stage 5 Solutioning (per-issue; #779 PT-3 binding-vs-advisory resolved Option C + ADR kernel; #3303 A6.5 adversarial security review); Stage 6 build (serial #779 → #3303 → #792 → #1007, shared-surface contention resolved by build-order); Stage 7 Dev Testing (grep/link/structural ACs per slice + the #3303 adversarial-injection fixture — all PASS, 4/4); Stage 8 QA (per-AC ACCEPT across all four issues — 4/4; #779 = 6 ACs); Stage 9 GO (Standard readiness scan + CIAC-1..4 graded on the merged PR; enforce-from-v3.94 operator-ratified at the GO gate).
**Outcome:** SUCCESS — ALIGNED with the Release Outcome Statement (AFTER: the Stage-2 acceptance-fit gate (#779), the issue-body author-association trust boundary (#3303), the module skill-consultation forcing function (#792), and the fix/extend-before-net-new gate (#1007) are added to the intake/gate surface; each fires at its named stage). Success Indicator met — all four issues' ACs MET (Stage-8 ACCEPT). Two gate controls — **G2-13** (acceptance-fit) and **SR-G6** (extend-before-create) — enforce from v3.94 per the Stage-9 ratification; the security control (A0 issue-body trust boundary) and the skill-consultation forcing function fire at intake/triage. No downstream follow-ups filed.
**Version lineage:** provisional **v3.93** (Stage-4 D-Version) → **v3.93** (Commit-0 re-verify 2026-07-25 — unclaimed at tag/ledger level; PROCEED) → **v3.94** (pre-merge re-version 2026-07-26 — sibling #4029 tagged v3.93 first; forward-only next-free, exactly the anticipated contention path). One forward-only hop; **no v3.93 tag is cut by this release** (v3.93 belongs to #4029); the abandoned v3.93 slot was claimed by another release, not orphaned by this one → `disposition: none`, no `RELEASE_REVERSIONS` row.

#### Deployment Log v3.93
**Files deployed:** release-identity-and-plan-naming (novel class) — one release PR #4029 (`release/release-identity-and-plan-naming → main`, merge-commit `01e102ad`), 5 delivery slices (#2548, #3993, #3107, #3119, #3307) on one slug-primary branch (D-C SINGLE, P0 fully-serial; Hybrid dogfood — a slug identity carried through all 13 stages, the version bound only at the Stage-12 atomic claim). ~28 files. **#2548** (core) — `claim-version.sh` gains an additive post-CAS stamping pass (resolve `{{RELEASE_VERSION}}` + rename `<slug>_RELEASE_PLAN.md` → `vX.Y_RELEASE_PLAN.md` on the CAS-win path; the existing atomic ref-CAS untouched); `plans/README.md` + `RELEASE_PROTOCOL.md` + `release-process.md` mirror extend "binds no concrete value" from the tag to the plan-file/branch for versioned releases (composing with the `{versioned, version-less}` identity mode, #3016); founding **ADR-092** (plan-file claim-time stamping). AC5 (Check#/ADR# tokenization) was CARVED at Stage 5 as infeasible-as-bundled (a `{{ADR_NUM}}` filename is MALFORMED under the gap-free `check-adr-numbers.py`; `{{CHECK_NUM}}` breaks Check-57's concrete-integer roster contract) → routed to #3713; both identifiers are already cheaply claim-deferred via renumber-at-Engineering. **#3993** (sibling, carved from #2548 §6 SPLIT, lockstep same-PR) — migrates the executing-skill gates (`release-planner`/`release-executor` reference files) + broad prescribing surfaces (`git-workflow.md` + 6 P2 docs) to the slug-primary pre-claim form (per-occurrence pre/post-claim discrimination — tag, RELEASE_LOG, shipped-corpus refs left); `release-planner.skill` + `release-executor.skill` rebuilt. **#3107** — a slug-primary release-identity conformance check as `deploy.sh` **Check 59** (warn-mode initial) backed by `core/deploy/tools/check-identity-conformance.py` (git-context-aware, window-gated on `release/*` pre-claim; the lint-check alternative rejected as structurally git-context-free). **#3119** — the misnamed/duplicated 86-methodology plan de-duped + renamed to `plans/v3/v3.60_RELEASE_PLAN.md`; a second stale `v3.54` file was content-verified as the 80-solutioning plan (shipped v3.57) and renamed to `plans/v3/v3.57_RELEASE_PLAN.md` (no data loss). **#3307** — the `CANONICAL_FILENAME_RE` in `lint_release_corpus.py` widened (one coordinated change with #2548's slug-form) to admit the patch form `vX.Y.Z` and the slug pre-claim form, still rejecting malformed; 21 now-regex-admitted allowlist entries pruned.
**Mechanism:** git merge-commit (PR #4029 → main at `01e102ad6f9515804880dfa8abd9740b0fa16dde`, `--admin` past branch protection; `git revert -m 1` rollback convention); signed-annotated tag `v3.93` at the merge SHA (annotated tag object, ED25519, noreply tagger; unique claim bound at the Stage-12 atomic ref-CAS). Hybrid dogfood: the slug plan `release-identity-and-plan-naming_RELEASE_PLAN.md` renamed to `plans/v3/v3.93_RELEASE_PLAN.md` + 6 `{{RELEASE_VERSION}}` tokens resolved hub-direct at claim (this release did NOT self-exercise its own just-merged substitution engine — introducing-release-exempt). Phase B4: `.skill` packages (`release-planner`, `release-executor`) rebuilt in-PR.
**Timestamp:** 2026-07-25 (release merge 2026-07-25T23:33:05Z UTC; signed-annotated tag `v3.93` at the merge SHA)
**Cycle-Time:** N/A (novel governance + tooling release; mechanism: compute-cycle-time.sh)
**Result:** SUCCESS — release PR #4029 merged to main via merge commit (D-C SINGLE, `--admin`; `git revert -m 1` rollback convention); v3.93 signed-annotated tag valid at the merge SHA; CI **45/45 pass**, MERGEABLE/CLEAN. Full staged pipeline ran per plan (novel class → Deep Stage 9): Stage 5 Solutioning (ALL-activation; the pivotal AC5 feasible-vs-carve verdict → CARVE; #3107 check-location Evidence-Grounded to a deploy.sh Check; a Stage-4 Contention-Map error re: the Check-9 mirror pair corrected); Stage 6 build (serial #2548 → #3993 → #3307 → #3119 → #3107); Stage 7 Dev Testing (all self-tests re-run green: claim-version U-0..U-13 collision-safety, check-identity-conformance 4 fixtures, ADR contiguity 001..092, regex accept/reject matrix); Stage 8 QA (per-AC ACCEPT across all 5 slices, CIAC-1/3 MET, CIAC-2 PARTIAL → reconciled in-PR before merge → MET); Stage 9 GO (Deep readiness scan + hub first-hand empirical verification).
**Outcome:** SUCCESS — ALIGNED with the Release Outcome Statement (AFTER: the monotonic version identifier binds at the claim, not authoring-time (#2548, dogfood-proven — this release's own plan carried 6 `{{RELEASE_VERSION}}` tokens, zero bound `vX.Y`, until the Stage-12 claim); release branches/plans slug-primary with a conformance check (#3107); plan filenames single-canonical + re-versioned (#3119); the plan-filename regex admits patch `vX.Y.Z` (#3307)). Success Indicator met. Check#/ADR# binding-at-claim carved to #3713 (already cheaply claim-deferred). Downstream follow-ups: #3713 (codify the renumber-at-Engineering discipline for Check#/ADR#), #4031 (audit the v3.55/v3.56 plan-file drift).
**Version lineage:** slug-primary throughout the pipeline (no provisional `vX.Y` bound to any branch/plan/commit — the Hybrid dogfood); provisional-display **v3.93** (Stage-4 recorded determination) claimed cleanly at the Stage-12 atomic ref-CAS — v3.93 was free at the tag level (3 sibling releases sat on `release/v3.93-*` branches but none had tagged; the atomic claim won v3.93 by tagging first). `disposition: none`, no `RELEASE_REVERSIONS` row (no forward hop).

#### Deployment Log v3.92
**Files deployed:** architecture-baseline (novel class) — one release PR #3923 (`release/v3.91-architecture-baseline → main`, merge-commit `3892473c`), 3 parent issues (#877, #912, #1097) as delivery slices on one branch (D-C SINGLE, P0 fully-serial), 24 files (+1,569/−21) — 5 net-new files + 2 `.skill` packages rebuilt. **#877** authors `core/disciplines/actor-model-and-governance-as-contract.md` — a forward-looking / target-state consolidation of the actor model (orchestrator / specialist-skill / free-agent classes) plus governance-as-contract; **no ADR minted** (adversarial CD-1 — nothing ratified to anchor one), and every shared-file edit (`architecture-overview.md`, `operating-model.md`, `disciplines/README.md`, `terminology-glossary.md`) is append-only to protect the 31/37 inbound refs (`orchestrator` disambiguated inline → no glossary term-count cascade). **#912** authors `core/disciplines/cross-chain-architecture-map.md` — an index mapping every management chain to its governing data model / process flow / holding gate (references #877 as one indexed entry, does not restate); second-writer edits to `architecture-overview.md` + `disciplines/README.md` and a reciprocal pointer in `five-function-spine-and-process-flows.md`. AC5 gate-wiring is out of scope (DEPENDS_ON #779, OPEN). **#1097** extends `pmo-qa-auditor` with a new **Mode I — As-Built Architecture-Conformance Audit** (OBSERVE-only; reads the release record and audits *delivered* work against the platform's own architecture, detecting conformance-drift + the cross-release fragmentation the forward per-ticket architecture-fit gate is structurally blind to) — SKILL.md + 2 new `references/` (mode-spec + dimension-rubric), a new 4th audit-cadence axis `release/references/protocols/architecture-conformance-cadence.md`, a committed hand-off surface `release/releases/architecture-conformance-summary.md` that `health-check` `full` consumes, the `per-skill-output-contracts.md` reconcile, and a seeded behavioral fixture; `pmo-qa-auditor.skill` + `health-check.skill` rebuilt. CD-A: the ADR corpus is the PRIMARY conformance baseline and the #912 index a SECONDARY routing aid; fragmentation ships as a candidate-grade, ADR-citation-bounded heuristic; the mode auto-files nothing.
**Mechanism:** git merge-commit (PR #3923 → main at `3892473cbdbbc3fe6266dba4d18b5ce2925b1465`; `git revert -m 1` rollback convention); signed-annotated tag `v3.92` at the merge SHA (annotated tag object `17eae360`; unique claim bound at the Stage-12 atomic ref-CAS). Phase B4: `deploy.sh --deploy` copies the runtime skills — the two in-scope packages (`pmo-qa-auditor.skill`, `health-check.skill`) were rebuilt in-PR with refreshed `.sha256` baselines so the deployed copies carry Mode I + the conformance feed.
**Timestamp:** 2026-07-25 (release merge 2026-07-25T17:04:14Z UTC; signed-annotated tag `v3.92` at the merge SHA)
**Cycle-Time:** N/A (novel corpus + skill-mode release; mechanism: compute-cycle-time.sh)
**Result:** SUCCESS — release PR #3923 merged to main via merge commit (D-C SINGLE; `git revert -m 1` rollback convention); v3.92 signed-annotated tag valid at the merge SHA; CI **43/43 pass**, MERGEABLE/CLEAN. Full staged pipeline ran per plan (novel class → Deep Stage 9) — Phase-0 spike #3617 resolved artifact homes + affected-files; Stage 5 Solutioning (ALL-activation; adversarial Phase A6.5 deferred ADR-090, dual-grounded #877 AC3 on mixed-determinism + ADR-019, and made #1097's confidence/severity model orthogonal); Stage 6 build (serial #877 → #912 → #1097, append-only shared-file edits); Stage 7 Dev Testing (grep/link ACs for #877/#912 + the #1097 behavioral fixture "two divergent deliveries → exactly one fragmentation finding" + zero-issues-created check); Stage 8 QA (per-AC ACCEPT across all three issues, 20 ACs); Stage 9 GO (Deep 13-dimension readiness scan).
**Outcome:** SUCCESS — ALIGNED with the Release Outcome Statement (AFTER: one authored architecture baseline (#877 actor-model + governance-as-contract), an index mapping every management chain to it (#912 cross-chain index), and a retrospective as-built conformance audit that detects drift (#1097 Mode I)). Success Indicator met — the baseline + index exist and the audit runs against them. Downstream: the audit-cadence family is now **four axes** (architecture-conformance joins process-fitness / structural / Anthropic base-vs-build); `health-check` `full` surfaces a platform-context conformance flag from the committed summary surface (producer→consumer seam, compose-not-absorb per ADR-019); the forward architecture-fit gate (Stage-2, deferred pending #779) gains its retrospective complement. The pre-existing 5-skill package-drift cluster (delivery-engine, pmo-process-designer, pmo-skill-refiner, release-planner, eval-writer) is inherited, WARN-mode, and out of this release's scope.
**Version lineage:** provisional **v3.89** (Stage-4 D-Version) → **v3.91** (Commit-0 re-verify 2026-07-25 — v3.89 tag-claimed by `template-system-governance-wave-1` (milestone #275, release PR #3913); v3.90 branch-claimed by `blast-radius-scan-correctness`) → **v3.92** (Stage-12 pre-merge freeness check — provisional v3.91 claimed by sibling #3925 `cross-platform-install-experience`, MERGED + tagged during the Stage 7–9 window). Two forward-only hops; **no v3.91 tag is cut by this release** (v3.91 belongs to #3925); neither abandoned slot (v3.89/v3.90/v3.91) was tagged by this release → `disposition: none`, no `RELEASE_REVERSIONS` row.

#### Deployment Log v3.91
**Files deployed:** Cross-platform install experience (cross-cutting class) — one release PR #3925 (`release/v3.90-cross-platform-install-experience → main`, merge-commit `da9fc42a`), 6 parent issues (#382, #299, #303, #302, #384, #304) as delivery slices on one branch (D-C SINGLE), 45 files (+4,286/−37). **#382** fixes the non-repo-root Phase-5 partial-update failure by having `orchestrate.sh phase_deploy_skills` subshell-`cd` to its `BASH_SOURCE`-derived repo root before invoking `deploy.sh` (option a; deploy.sh guard byte-unchanged) — hardens both `install.sh` Phase 2 and `update.sh` Phase 5 in one edit, with a 4-assertion non-repo-root regression test. **#299** adds a QA-as-code regression module `core/deploy/qa/` (`@check` registry, 12 findings→12 checks 1:1, read-only) + root `qa.sh` (standalone-only). **#303** adds Windows entry-points `install.ps1`/`update.ps1` with a pinned `-DryRun` bash-free proxy contract (assert Python + preflight + a real `compose.py` round-trip) and relaxes both non-Darwin hard-fail gates (`setup-workspace.sh` + `validate-install.sh`, opt-in `PMO_ALLOW_NON_DARWIN=1`, `exit 78` default retained); the full `orchestrate.py` port is deferred to #301 (coordination path). **#302** adds `doctor.sh` — a strict-read-only two-layer operator install self-diagnosis reusing #299's read-only registry (CIAC-1, 0 forked check bodies). **#384** supersedes the `[adapters].ticketing` scalar with an `operator.toml` `[trackers.<id>]` named-subtable schema (Option B — round-trips through the #383 durability contract; INT-1 proven) and adds a NEW `core/hooks/block-scope-segregation.sh` PreToolUse gate with the A6.5 fail-CLOSED amendments (CD-1 fail-closed routing default, CD-2 mode-independent always-block on identity→public, CD-3 fail-closed content-extraction), warn-mode-first + a new 8th allowlist (7→8 cascade), documented in **ADR-091**. **#304** refactors `.github/workflows/install-tests.yml` into a tiered cross-platform matrix (macOS full / ubuntu + windows portable legs, non-vacuous), exercising `qa.sh` + `install.ps1 -DryRun` across all three OSes.
**Mechanism:** git merge-commit (PR #3925 → main at `da9fc42aecc1f33cfd7a12bc750014adbecd3f12`; `git revert -m 1` rollback convention); signed-annotated tag `v3.91` at the merge SHA (tag object `13cd1634`; unique claim verified via the Stage-12 atomic re-verify — v3.91 won after v3.90 was claimed by the concurrent `blast-radius-scan-correctness`). Phase B4: `deploy.sh --deploy` copies the runtime skills (`intake-desk.skill` rebuilt in lockstep for the `[trackers.<id>]` routing awareness).
**Timestamp:** 2026-07-25
**Cycle-Time:** N/A (mechanism: compute-cycle-time.sh)
**Result:** SUCCESS — release PR #3925 merged to main via merge commit; v3.91 signed-annotated tag valid (noreply tagger, ED25519); CI **43/43 pass**, MERGEABLE/CLEAN. Full staged pipeline ran per plan — Stage 5 Solutioning (6 spokes) + Phase A6.5 Adversarial Design Review (#384 + #303, which caught a real fail-open in the security hook → fixed fail-closed), Collective Review scope-lock (an expansion to #301/#342/#44 was considered and reverted to the coordination path on evidence), Stage 6 build (6 slices + 2 Stage-7 DevTest fix-forwards — a doctor.sh false-positive and a no-op idempotency regression found beyond CI), Stage 7 Dev Testing, Stage 8 QA (ACCEPT-WITH-NOTES; #304 AC-3 disposed as ratify-posture-+-fix-header), Stage 9 GO (13-dimension scan, goal-conformance surfaced honestly).
**Outcome:** SUCCESS — delivers the cross-platform install foundation: non-repo-root update fix, QA-as-code + `qa.sh`, Windows entry-points + `-DryRun` proxy + relaxed platform gates, `doctor.sh` self-diagnosis, multi-destination `[trackers.<id>]` + a fail-closed scope-segregation hook, and a 3-OS CI matrix. Per the ratified coordination path, a *working* bash-free Windows install (full `deploy.sh` de-bashing) is delivered by #47/#48 under epic #1182; this release ships the entry-point + DryRun proof + CI gate.
**Version lineage:** provisional **v3.89** (Stage-4 D-Version) → **v3.90** (v3.89 claimed by `template-system-governance-wave-1` #3913, caught at Commit-0 re-verify pre-build) → **v3.91** (v3.90 claimed by `blast-radius-scan-correctness` #3924, caught at the Stage-12 atomic re-verify pre-tag). Two forward-only hops; neither abandoned slot was tagged by this release → `disposition: none`, no `RELEASE_REVERSIONS` rows.

#### Deployment Log v3.90
**Files deployed:** blast-radius scan-correctness (novel class) — one release PR #3924 (`release/v3.90-blast-radius-scan-correctness → main`, merge-commit `09eda3bd`), 4 parent issues (#3300, #3291, #675, #3120) as delivery slices on one branch (D-C SINGLE), 11 files (+767/−14). All four fixes edit `release/tools/blast-radius.sh` on four disjoint regions. **#3300** adds `".claude/worktrees/"` to `DEFAULT_EXCLUSIONS` (mirroring `domain-blast-radius.sh:134`) so the default scan root is the canonical tree only — worktree repo copies no longer inflate first-order counts (~13.8× observed with 14 live copies; the pre-fix depth-2 run broke outright with `jq: Argument list too long`). **#3291** replaces the bare-basename over-match in `find_first_order` with an adaptive uniqueness pre-count over the (worktree-excluded) scan list — a unique basename keeps prior match behavior (AC-2 byte-identical), a non-unique basename path-anchors only (over-match eliminated). **#675** adds a script-level `export LC_ALL=C` adjacent to the PATH pin, covering every `sort` (incl. the two previously-bare sorts) so no locale-exposed sort remains. **#3120** adds a net-new `--mode=structural` path-move consumer tracer (directory-prefix targets supported; reuses the scan-list / `DEFAULT_EXCLUSIONS` / `grep -F` machinery + `lib/schema-v1-emit.sh`) surfacing the hard-coded path-literal consumers a directory move would silently break — the class behind the #230→#3118 Stage-13-Release-emit miss — wired into a new Stage-5 Phase A3.3 + gate criterion **SR-G5** (shipped **shadow**; soft update-or-accept per D-2), documented in `blast-radius-protocol.md`, with a regression fixture reproducing the #3118 miss. **ADR-090** records the extend-vs-sibling decision (qualifying ADR-068: same-scanner→extend, different-scanner→sibling) + the D-2 rollout. Commit-split: #3120 mode code (CHEAP) vs gate wire-in (MODERATE) — independently revertable.
**Mechanism:** git merge-commit (PR #3924 → main at `09eda3bdc06d3f3682baf339b4958ed697a9b24b`); signed-annotated tag `v3.90` at the merge SHA (unique claim verified via Stage-12 atomic ref-CAS on first attempt — v3.90 won the race against the concurrent `release/v3.90-cross-platform-install-experience`). Phase B4: no `deploy.sh --deploy` — the changed surface is a release tool + pipeline/protocol docs + an ADR + a test fixture; no skill packages affected.
**Timestamp:** 2026-07-25
**Cycle-Time:** N/A (tool + corpus release; mechanism: compute-cycle-time.sh)
**Result:** SUCCESS — release PR #3924 merged to main via merge commit (D-C SINGLE; `git revert -m 1` rollback convention); v3.90 signed-annotated tag valid; CI **38/38 pass**, MERGEABLE. Full staged pipeline ran per plan — Stage 5 design (FULL for #3120, REDUCE for the three fixes) with 2 operator D-decisions, Stage 6 build (6 commits + ADR-090), Stage 7 Dev Testing (PASS, 0 defects, A/B proofs), Stage 8 QA (13/13 ACs MET, 0 FAIL), Stage 9 GO (13-dimension scan ALL-PASS, goal-conformance ALIGNED).
**Outcome:** SUCCESS
**Version lineage:** provisional **v3.89** (Stage-4 D-Version) → re-versioned to **v3.90** pre–Stage-6, after `template-system-governance-wave-1` (#3913) claimed v3.89 — caught at the pre-flight / Commit-0 version re-verify *before any build write* → v3.90 claimed at the Stage-12 atomic ref-CAS on first attempt. One forward-only hop; the abandoned v3.89 slot was never tagged by this release → `disposition: none`, no `RELEASE_REVERSIONS` row.

#### Deployment Log v3.89
**Files deployed:** Template-system governance wave 1 (routine class) — one release PR #3913 (`feat/v3.89-template-system-governance-wave-1 → main`, merge-commit `5fff643a`), 3 parent issues (#3380, #3381, #2855), 9 files (+252/−3). **#3380** adds `operations/templates/change-impact-matrix-template.md` — a reusable 9-column Change Impact Matrix template (Topic / Current State / Future State / Change Summary / Level of Impact / Customer Impact / Impacted Stakeholders / Change Mgmt Plan / Notes) carrying the L4 provenance header (`domain: project`, `canon: PMBOK 7 §Stakeholder`, `canon_compat: none`). **#3381** adds `operations/templates/training-plan-template.md` — a reusable 6-column Training Plan template (Team / Lead / Approach / Content-Deliverables / Topics / Notes), column model aligned with the change-management skill's `references/training-plan.md`. Both registered in `core/governance/OPERATIONS.md` (operational-artifacts table) + the `operations/templates/README.md` registry, and their `template_family` grounded in `core/standards/template-taxonomy.md` §3.1 Stakeholder + §6 canon-map (rows 10–11, P5 path c-i). **#2855** reconciles `operations/templates/raid-log-template.csv`'s header to the live `raid-log.schema.json` field set (14→15 fields, names + order: `RAID_ID … source_ref, Section`); `packages/project-initiator.skill` rebuilt in lockstep (the canonical template sync-injects into that package via `TEMPLATE_SYNC_MAP`).
**Mechanism:** git merge-commit (PR #3913 → main at `5fff643ade4fa7124466a23852d7108a2d8ca6d4`); signed-annotated tag `v3.89` at the merge SHA (tag object `2973a3eb`; unique claim verified via Stage-12 atomic ref-CAS on first attempt — no re-version). Skill-package rebuild (`project-initiator.skill`) rides the merge; `deploy.sh --deploy` copies the runtime skills.
**Timestamp:** 2026-07-25
**Cycle-Time:** N/A (content/corpus release; mechanism: compute-cycle-time.sh)
**Result:** SUCCESS — release PR #3913 merged to main via merge commit (D-C SINGLE; `git revert -m 1` rollback convention); v3.89 signed-annotated tag valid; CI **38/38 pass**, MERGEABLE/CLEAN. Staged pipeline ran per plan — Stage 5 (light) design notes on #3380/#3381, Stage 6 build, Stage 7/8 per-AC verification (all 6 ACs + CIAC-1/2 met) closed before the Stage 9 GO.
**Outcome:** SUCCESS

#### Deployment Log v3.88
**Files deployed:** deploy/tooling/test defect-cleanup (routine class) — one release PR #3794 (`release/v3.86-deploy-and-tooling-defect-cleanup → main`, merge-commit `3368d98e`), 6 parent issues, 13 files. **#2341** corrects the singular `reference/` subdir naming to `references/` in `canonical-skill-structure.md` (§4/5/9). **#3210** makes the `test_sandbox_roots.sh` assertion helpers SIGPIPE-safe (here-string + `grep -m1`) — root-caused to the L53 `assert_contains` helper, not just the sub-task's named L63. **#2439** makes `deploy.sh --check` credit nested `references/<subdir>/` injections (no false project-initiator reference-drift; genuine drift still caught by Check 1 / Check 13). **#3578** derives `check-skill-count-imp.py`'s roster from the `deploy.sh` arrays instead of a filesystem walk, self-correcting the stale count (live public roster **53**); a genuine falsification regression test is added. **#3722** refactors `eval_review.html` row rendering from an escaped-string DOM sink to DOM construction (`createElement`/`textContent`), removing 2 `nosemgrep` suppressions — structurally eliminating the high-severity CodeQL `js/xss-through-dom` alert #2 (closes on the next `main` scan post-merge); `packages/pmo-skill-refiner.skill` rebuilt in lockstep. **#2232** makes the CLAUDE.md maintenance contract honest (Path b — drops the false `regenerated by update.sh` marker, names the true `setup-workspace.sh substitute_templates()` install-time refresh path) across `CLAUDE.md.template` + `composition-surface-spec.md` + `docs/UPDATE.md` + `docs/INSTALL.md`.
**Mechanism:** git merge-commit (PR #3794 → main at `3368d98ebf4a2b822e2ebfb68a20aa07b486eb77`); signed-annotated tag `v3.88` at the merge SHA (unique claim verified via Stage-12 atomic ref-CAS). Phase B4: no `deploy.sh --deploy` skill-copy in this session (the skill-source edit + rebuilt package ride the merge).
**Timestamp:** 2026-07-24
**Cycle-Time:** N/A (content release; mechanism: compute-cycle-time.sh)
**Result:** SUCCESS — release PR #3794 merged to main via merge commit (D-C SINGLE; `git revert -m 1` rollback convention); v3.88 signed-annotated tag valid; CI **42/42 pass**, MERGEABLE/CLEAN; install/onboarding/update regression gate green.
**Outcome:** SUCCESS
**Version lineage:** provisional **v3.86** (Engineering Commit 0) → re-versioned to **v3.87** after `structure-records-and-review-signals` claimed v3.86 (#3797) → re-versioned to **v3.88** after `design-artifact-backfill` claimed v3.87 (#3796) — two forward-only hops in one day (binding claim = the Stage-12 atomic ref-CAS; re-version-up is expected under parallel work). No tag was cut for the abandoned v3.86/v3.87 slots → `disposition: none`, no `RELEASE_REVERSIONS` row. The lone merge conflict (`packages/pmo-skill-refiner.skill`, #3798's concurrent package rebuild vs. #3722's) was resolved by rebuilding the package from the merged source.


#### Deployment Log v3.87
**Files deployed:** Design-artifact backfill (novel class, MINOR bump) — one release PR #3796 (`release/v3.87-design-artifact-backfill → main`, merge-commit `27c7d5ef`), 66 files (+809/−22), **14 `.skill` packages rebuilt** (skill-flow SKILL.md marker edits). The release makes the platform's design-artifact set enumerable and backfills its declaration surface. **#3725 (identification mechanism)** amends `core/standards/design-artifact-standard.md` (+94): §9 promotes `depicts:` / `flow_class:` from deferred to required for dedicated files, §2 corrects the Human-process current-state cell, and a new §12 adds per-flow-type detection criteria (table-aware for the three table-rendered types) plus the §12.2 enumeration query; the #64 pilot `core/diagrams/architecture-platform-structure.md` gains its `flow_class:` / `depicts:` frontmatter (AC4 proof target). Carries **ADR-089** (embedded design-artifact declaration via a section-level `<!-- design-artifact: … -->` HTML-comment marker; `Proposed` at merge → `Accepted` at Stage 13, verified via its `status:` field). **#3614 (spike completion)** produces the D1 gap table as a query result of #3725's mechanism and renders the **D3 confirm-or-collapse** decision → **COLLAPSE**; the D2 corrected-AC template is homed OUTSIDE the standard at `release/references/how-to/design-artifact-backfill-completion-condition.md` (+131) to avoid the dual-write with #3725. **#3798 (declare-sweep — collapsed from #3437–#3442 ×5)** marks the existing embedded artifacts across five already-covered flow types (data-flow, concept-model, agent-process, decision-tree, skill-flow) with the §9 marker — 24 embedded-marker declarations at HEAD; the fission collapsed to declare-not-author for these types. **#3441 (human-process — kept as a full build)** adds the one genuinely-new artifact, `docs/release-record-keeping.md` (+101, dedicated `flow_class: human-process`), for the zero-§2-baseline flow type. Post-backfill the design-artifact set is machine-queryable: **24 embedded-marker + 2 dedicated-frontmatter** declarations (architecture pilot + human-process) across the seven flow types.
**Mechanism:** git merge-commit (PR #3796 → main at `27c7d5efc0259946d5990e7706bbbcd41f64cd3d`); signed-annotated tag `v3.87` at the merge SHA (tag object `a3d41f53`; unique claim verified). **Version lineage:** planned as provisional **v3.86** at Stage 4, re-rendered **v3.86 → v3.87** at Engineering Commit 0 after v3.86 was claimed by a concurrent release; the Stage-12 atomic ref-CAS then claimed **v3.87** at the merge SHA (won on first attempt). No `v3.86` tag was cut by this release → `disposition: none`, no `RELEASE_REVERSIONS` row (per the `append_reversions` orphan-tag gate). Skill-source marker edits + the 14 rebuilt packages ride the merge; `deploy.sh --deploy` copies the runtime skills.
**Timestamp:** 2026-07-24 (release merge 2026-07-25T01:30:11Z UTC; signed-annotated tag `v3.87` at the merge SHA)
**Cycle-Time:** N/A — (mechanism: compute-cycle-time.sh; content/corpus release — no `gate-outcome/plan-review-go` or `deployment-status/deploy-skill` event carried a `v3.87` version tag)
**Result:** SUCCESS — release PR #3796 merged to main (D-C SINGLE topology; `git revert -m 1` rollback convention); signed-annotated tag `v3.87` pushed at the merge SHA. Staged pipeline ran per the plan — Stage-5/6/7/8 sub-tasks for #3725, #3614, #3798, #3441 all closed pre-merge (#3782–#3789, #3800–#3803, #3810–#3813) and Stage-9 GO cleared before merge. D-FissionScope rendered **COLLAPSE**: 5 of 6 flow-type builds folded into declare-sweep #3798; human-process #3441 kept as the one full build.
**Outcome:** SUCCESS

#### Deployment Log v3.86
**Files deployed:** structure-records integrity + drift-detection slice (routine class) — one release PR #3797 (`release/v3.88-structure-records-and-review-signals → main`, merge-commit `e1b63141`), 3 parent issues, 9 files. **#2225** adds `release/tools/check-analysis-staleness.py` — an operator-local, non-blocking lint (embedded `--self-test`; confirm-gated archive) flagging past-`sunset` + missing-frontmatter analysis artifacts, siblinged to the existing `release/tools/check-*.py` family (NOT `deploy.sh`, avoiding contention with concurrent release milestone #281); the analysis-workspace standard is untouched (enforcement was the deferred half of #1492). **#2021** thin-wires the methodology type-pack read into `tracker-manager` + `weekly-status-rollup` (prose citation of the OPERATIONS.md Methodology Awareness Protocol; `weekly-status-rollup`'s methodology-neutral scope boundary preserved verbatim), with `delivery-engine` + `ppm-agent` confirmed already-conformant (verify-only); both `.skill` packages rebuilt (Check-7). **#681** homes the Layer-1 boundary-violation detection-signal fingerprint in `core/disciplines/concurrency-safeguards.md` (which already owns Layer-1 concurrent-write detection, L49/L59); AC2's originating flow is Cowork-side/off-git → accept-risk recorded (writer-side twin of the checker bug ADR-085 fixed).
**Mechanism:** git merge-commit (PR #3797 → main at `e1b631414c7fc36b38ed07e5de36f1057ffe63b8`); signed-annotated tag `v3.86` at the merge SHA (unique claim verified). Skill-source edits + rebuilt packages ride the merge; `deploy.sh --deploy` copies the runtime skills.
**Timestamp:** 2026-07-24
**Cycle-Time:** N/A — (mechanism: compute-cycle-time.sh; content release — no `deployment-status/deploy-skill` event carried a `v3.86` version tag)
**Result:** SUCCESS
**Outcome:** SUCCESS
**Version lineage:** scoped at provisional **v3.88** on the branch/labels (a conservative next-free that over-counted the sibling release branches milestone #281 `v3.86` / milestone #291 `v3.87`, which hold branches but no tags) → claimed **v3.86** at the Stage-12 atomic ref-CAS (the adapter's true next-free, since `claimed_set()` counts published tags + DEPLOYED rows, not branches). No tag was cut for v3.88 → `disposition: none`, no `RELEASE_REVERSIONS` row.

#### Deployment Log v3.85
**Files deployed:** Close-out-hardening bundle (wave 2) — one release PR #3729 (`release/v3.85-close-out-hardening-wave-2 → main`, merge-commit `68736d5e`), 4 parent issues, 13 files (+1032/−80). Corpus/governance/tooling release — the merge deploys no runtime skill behavior; the single skill-source edit (the eval-writer release-notes-eval rubric) has its `packages/eval-writer.skill` rebuild **deferred to this Stage-13 close-out chore PR**, the deliberate live dogfood of #3322's new package-rebuild phase. **#3118** flips the Release-body drift gate from dormant-and-advisory to active-and-enforcing — `deploy.sh` Check 47 ships a committed complete-token cutoff (`v3.78`) routed through a per-check gating helper defaulting to `enforce`, and `automated-closeout.sh`'s post-emit drift phase blocks closure on genuine drift, reconciling the implementation to the posture `stage-13-close.md` already specified. **#3322** adds the `.skill` package-rebuild phase to `automated-closeout.sh`, downstream of the ledger guard and before the chore-PR commit, so a release that stales a packaged skill ships the rebuilt package with the corpus before Milestone close. **#2600** codifies the RELEASE_NOTES sample-block mandate for schema/format-change releases (`release-notes-standard.md` §2.8 + a new advisory lint check + a `--sample-block-advisory` flag), with the eval-writer rubric synced to the new check count (D-5). **#3215** backfills v3.57 into all four append-ledgers (RELEASE_LOG, CHANGELOG, RELEASE_INDEX, RELEASE_DIGEST) and repairs the one dangling `notes/v3/…` link in RELEASE_INDEX (D-6).
**Mechanism:** git merge-commit (PR #3729 → main at `68736d5e6340838c5cd15512e42f9c1f0260a7db`); signed-annotated tag `v3.85` at the merge SHA (unique claim verified). Phase B4 skill deploy is limited to the eval-writer rubric reference doc; the packaged-skill rebuild rides Stage 13 per #3322.
**Timestamp:** 2026-07-24 (release merge 2026-07-24T14:23:32Z UTC; signed-annotated tag `v3.85` at the merge SHA)
**Cycle-Time:** N/A — (mechanism: compute-cycle-time.sh; content release — no `gate-outcome/plan-review-go` or `deployment-status/deploy-skill` event carried a `v3.85` version tag)
**Result:** SUCCESS
**Outcome:** SUCCESS
**Version lineage:** scoped at provisional **v3.83** (Engineering Commit 0) → re-versioned forward to **v3.84** after `pipeline-telemetry-tail` claimed v3.83 (#3657), then to **v3.85** after `version-identity-and-corpus-ledgers` claimed v3.84 (#3654) — two forward-only hops in two days (binding claim is the Stage-12 atomic ref-CAS; re-version-up is expected under parallel work). This release cut **no** tag for either abandoned version, so per #3109's SLIM gate its `disposition: none` / no orphan tag correctly appends **no** `RELEASE_REVERSIONS` row.

#### Deployment Log v3.84
**Files deployed:** No runtime/skill files deployed — corpus/governance/tooling release. One release PR #3654 (`release/v3.80-version-identity-and-corpus-ledgers -> main`, merge-commit `1dd1ca01`), 3 parent issues. **#3016** created Gate-3 criterion **G3-19** (version-identity-mode validity, `field`/`judgment`/`recommend`) in `core/schemas/gate-criteria-spec.md` + schema bump 2.2 -> 2.3, reconciled the release-class taxonomy hotfix trigger, added the identity-mode sub-block to `RELEASE_PROTOCOL.md` Versioning, and cited G3-19 in the `release-process.md` Gate-3 enumeration. **#3108** added a new root `.gitattributes` binding `merge=union` to the three pure-additive ledgers (`RELEASE_INDEX.md`, `RELEASE_DIGEST.md`, `CHANGELOG.md`); `RELEASE_LOG.md` and `RELEASE_REVERSIONS.md` are deliberately EXCLUDED as state-bearing (a line-level union duplicates rows with contradictory DEPLOYED/VERIFIED status — demonstrated empirically at Stage 5 and re-proved at Stage 8), plus +83 lines of self-test only. **#3109** SLIMmed `RELEASE_REVERSIONS.md` to an orphan-tag recovery record — `phase_append_reversions` now gated to `disposition=tag-orphaned`, collision-rate section deleted, header reframed; all 25 historical rows and the 12-column schema byte-unchanged. **ADR-088** records both boundaries as one decision: *bind state at the moment it becomes true, and resolve mechanically only what carries no state.*
**Mechanism:** git squash-merge (PR #3654 -> main at `1dd1ca01a8dbf471f389bdd8a388775b5899e4ea`; **`--admin` override used** — base-branch policy prohibits a plain merge even with all contexts green); annotated tag `v3.84` at the merge SHA (`3167c30`, unique claim verified); `deploy.sh --deploy` correctly a NO-OP (no skill/package/harness changes).
**Timestamp:** 2026-07-24 (release merge 2026-07-24T00:35:46Z UTC; annotated tag at the merge SHA; deploy no-op same session)
**Cycle-Time:** N/A — (mechanism: compute-cycle-time.sh; not run — no `gate-outcome/plan-review-go` telemetry event was emitted for this release)
**Result:** SUCCESS
**Outcome:** SUCCESS
**Version lineage:** scoped at provisional **v3.80** (Stage 4) -> lost to the concurrent `close-out-reliability-hardening` release -> re-versioned forward to **v3.84** at the Stage-12 pre-merge relabel (A.5.6 freeness re-verify PROCEED; main at v3.83, v3.84 free + next-free). The v3.80-v3.83 gap for this lineage is permanent and benign. Branch name retains the provisional `release/v3.80-...` per the no-retroactive-rename rule; the tag, plan filename, and PR title all carry v3.84. Per #3109's new SLIM gate this release's `disposition: none` / no orphan tag correctly appends **no** `RELEASE_REVERSIONS` row — the shipped mechanism working as designed, not a missed append.

#### Deployment Log v3.83
**Files deployed:** Pipeline-telemetry-tail bundle — one release PR #3657 (`release/v3.80-pipeline-telemetry-tail → main`, merge-commit `4dcf8298`), 6 parent issues, 33 files (+2898/−64) across 14 commits (Stages 4–9). **1 `.skill` package deployed** — the new `session-retro` skill via `deploy.sh --deploy session-retro` (package `eb156a12`); the release's other package rebuilds (Check-7 baseline) landed as in-PR commits on the release branch. **#3301** — pipeline-event-log schema fix on the decision-subtype extension axis (the payload axis is deferred per the subtype-scope reading, tracked #3704). **#2646** — indicator determination record. **#2647** — close-class telemetry read-model (`close-class-telemetry.md` → v1.02; Indicator-6 now excludes terminal Stage-12/13 sub-tasks from its denominator) — AC-3 PARTIAL under an operator Override Record. **#2645** — phase-cluster telemetry standards (front + middle) reconciled to their compute tools, with discriminating fixtures. **#2423** — the new `session-retro` skill + `Stop`-hook re-entry class (ADR-087); the hook ships **inert by design** (AC4 PARTIAL; activation gated on #3705, conditions PA-1/2/3). **#166** — the 30-day post-deploy indicator review (a Stage-12 action, not a build). ADR-086 (event-log schema) was amended before ratification; ADR-087 (Stop-hook re-entry class) was authored.
**Mechanism:** git merge-commit (PR #3657 → main at `4dcf8298c33b79bffc6efebebb7b53bafd631869`; **`--admin` override used** — base-branch policy prohibits a plain merge even when CLEAN/MERGEABLE, CI green 38/38 at merge). The atomic ref-CAS then computed next-free = **`v3.83`** against the mainline anchor and pushed a **signed-annotated** tag at the merge SHA — **won on first attempt, no collision**. **Re-version ledger — provisional `v3.80` → `v3.83`:** while this release ran Stages 4–12, three concurrent releases consumed the intervening slots — `close-out-reliability-hardening` claimed **v3.80** (#3618), `version-identity-and-parser-ssot` claimed **v3.81** (#3656), and `governance-doc-reconciliation` claimed **v3.82** (#3667) — so the label was re-anchored forward to **v3.83**, forward-only (re-version-up is expected under parallel work, not an error). **No `v3.80` tag was ever cut by this release** (provisional-name abandonment). The A.5.6c pre-merge relabel corrected the version label on the branch — `.version`, plan filename + contents, the ADR-087 `release:` line, and the `session-retro` skill stamp + rebuilt package — BEFORE the merge, so the merge commit carries `v3.83`. Residual per [`re-version-recovery.md`](../references/how-to/re-version-recovery.md) R-2 (immutable history, accepted-as-documented): the branch name `release/v3.80-pipeline-telemetry-tail` and the 14 on-branch commit subjects retain their as-authored `v3.80` provisional label (an in-place restamp would require a force-push). Stage-7 DevTest all-PASS · Stage-8 QA (4 PARTIALs, each with a tracked residual) · Stage-9 **GO** (operator-authorized 2026-07-22, with an Operator Override Record on #2647 AC-3).
**Timestamp:** 2026-07-22 18:58 CDT (release merge 2026-07-22T23:45:12Z UTC; signed-annotated tag `v3.83` at the merge SHA; skill deploy 18:58 CDT)
**Cycle-Time:** N/A — (mechanism: compute-cycle-time.sh; returned N/A — no `gate-outcome/plan-review-go` event and no `deployment-status/deploy-skill` event carried a `v3.83` version tag)
**Result:** SUCCESS
**Outcome:** SUCCESS

#### Deployment Log v3.82
**Files deployed:** Governance/skill drift-reconciliation bundle — one release PR #3667 (`release/v3.81-governance-doc-reconciliation → main`, merge-commit `a0fe5e61`), 7 issues, 33 files (+468/−41), **10 `.skill` packages rebuilt** (4 direct + 6 canonical-injection consumers — see Mechanism). Every slice aligns a describing document to a capability the platform had already shipped. **#2472** — `release/references/pipeline/stage-04-planning.md`: the `domain:` class e.g.-list now names the shipped `process` guide, and the clause states which four domains ship a guide today (`software`, `governance`, `process`, `support`). **#2473** — `core/standards/sior-escalation-protocol.md` § Decision-Owner Mapping upgraded from free-text heuristic to **structured lookup**: select the Stakeholder Register row where `Decision Owner = yes` AND `Authority = <decision domain>` (Tracker 8 schema in `core/schemas/tracker-schemas.md` + the committed `operations/templates/stakeholder-register-template.csv`, shipped via #206), with the free-text `## Key People` path retained as the graceful-degradation fallback and warn-and-route-to-PgM as the terminal case; the residual note is rewritten — it no longer asserts the structured authority field is unshipped. **#2854** — three surfaces reconciled to the **two-tier CORRECTIONS model** (program `projects/_config/CORRECTIONS.md` = platform-wide redirects, read after CLAUDE.md; per-project `[Project]/CORRECTIONS.md` = project-scoped redirects, read after PROJECT.md, overriding the program tier on the same claim): `core/specs/operational-artifact-inventory.md` §8 (new two-tier lifecycle note + the narrower-is-safer scope test), `core/governance/OPERATIONS.md` § CORRECTIONS Graduation & Expiry Rule 1 (governs both tiers identically — tier placement is a scope test, not a lifecycle difference), and `core/disciplines/memory-architecture.md` (Learning-type row + §4 session-start read order). CLAUDE.md deliberately untouched — it already described the model (#241 shipped it). **#3289** — `core/standards/template-protocol.md` §6 P5(c) split into **(c-i)** `domain: project` with no plugin counterpart and **(c-ii)** `domain: software` whose required `canon:` names an external canon with no plugin counterpart, so a software no-plugin family (e.g. RFC / IETF 7322) finally has a satisfiable `canon_compat: none` path and no §6 family is stranded; `core/standards/template-taxonomy.md` §7 re-expressed as **convention-anchors** carrying the 2026-07-03 re-survey (`~/.claude/plugins/installed_plugins.json` + the official-marketplace roster found the `engineering:*` / `product-management:*` / `operations:*` suites **absent**), reconciling the L173/L190 "installed" assertions with the Row-9 addendum. **#2805** — `operations/skills/comms-writer/references/voice-guide.md` gains an Executive Compression & Concreteness section + 2 anti-pattern rows; `operations/skills/comms-writer/SKILL.md` gains a 5-step compression pass on the Executive-brief type (cause→clause; consequence→concrete+clock; impact→executive unit; expected-vs-unexpected; cut a paragraph) and 2 five-field Domain-Specific Failure Modes (abstract-consequence-where-a-concrete-one-is-knowable → **OUT**; partially-expected-outcome-framed-as-pure-defect → **INPUT**). **#3385** — `operations/skills/artifact-lint/SKILL.md` Check 4 keys the displaced-content rule on the **ADR-080 folder union** (`promotion_state: promoted ⇒ folder ∉ {08-generated, _generated}`), so a promoted-but-not-moved artifact is caught in EITHER staging bin across the taxonomy-migration window; the issue's "ADR-079" citation was a misattribution and is corrected to ADR-080 in both the edit and the plan. **#3386** — `operations/skills/daily-status/SKILL.md` + `operations/skills/weekly-status-rollup/SKILL.md` carry an explicit **read-side aggregation contract**: aggregate from the tracked (Domain-B) layer — `DEC-###` / `ACT-###` / `MTG-###` entries, `MSG-##` rows, RAID rows — and cite each entry's `source_inputs` / `source_ref` provenance rather than re-scanning raw transcripts; daily-status' single same-day triggering-transcript intake is preserved as the deliberate **write-side** exception.
**Mechanism:** git merge-commit (PR #3667 → main at `a0fe5e61de1894239133c18e55f9d1da63819f86`; **`--admin` override used** — base-branch policy prohibits a plain merge even when the PR is CLEAN/MERGEABLE, CI green 34/34 at merge). The atomic ref-CAS then computed next-free = **`v3.82`** against the mainline anchor and pushed a **signed-annotated** tag at the merge SHA — **won on first attempt, no collision**. **Phase B4 deploy is NOT a no-op** (unlike v3.79–v3.81): this release touches skill sources, and **10 `.skill` packages were rebuilt** — **4 for the direct skill edits** (`comms-writer` + `artifact-lint` for #2805/#3385; `daily-status` + `weekly-status-rollup` for #3386) and **6 consumer packages** (`delivery-engine`, `eval-writer`, `pmo-process-designer`, `pmo-skill-refiner`, `project-initiator`, `release-planner`) that bundle the two canonicals #3289 edited (`template-taxonomy.md`, `template-protocol.md`) via build-time injection. That canonical-injection fan-out was not visible in the Stage-4 File Change Matrix (which scoped the rebuild to the 2 directly-edited skills); the operator signed off on the in-PR scope addition, and the final Check 7 package-drift audit reads **52 packages, 52 OK, 0 FAIL**. **Re-version ledger — provisional `v3.80` → `v3.81` → `v3.82`:** while this release ran Stages 4–12, two concurrent releases consumed the intervening slots — `close-out-reliability-hardening` claimed **v3.80** (#3618) and `92-version-identity-and-parser-ssot` claimed **v3.81** (#3656) — so the label was re-anchored twice, forward-only, per `bundle-composition-doctrine` §5.3/§9.1 (re-version-up is expected runtime behavior under parallel work, not an error). **No `v3.80` or `v3.81` tag was ever cut by this release** (provisional-name abandonment, both hops). Residual per [`re-version-recovery.md`](../references/how-to/re-version-recovery.md) R-2 (immutable history, accepted-as-documented): the branch name `release/v3.81-governance-doc-reconciliation` and the on-branch commit subjects retain their as-authored `v3.80` / `v3.81` provisional labels; the plan file was moved to the slug-qualified `v3.82-governance-doc-reconciliation_RELEASE_PLAN.md` form before the merge, but its **content restamp was missed at that time** — the H1, Version line, and Re-version Log still carried the provisional `v3.81` labels at the merge — and was completed afterward in a follow-up correction. **PR lifecycle:** the original release PR **#3658** was auto-closed when the branch-rename API deleted its head ref during the re-version; **#3667** superseded it with identical reviewed content (same head branch, same diff) — the close is a GitHub side effect of the rename, not a rejection. Stage-7 DevTest all-PASS · Stage-8 QA **7/7 AC MET + CIAC-1 MET** · Stage-9 **GO** (operator-authorized). QA residual **R-1** — `template-taxonomy.md` §4 (L96) still framed the plugin cross-refs as install claims, contradicting the §7 convention-anchors #3289 had just reconciled — was **swept in-PR before the gate** (commit `9d15729`), so the release shipped with zero open QA residuals.
**Timestamp:** 2026-07-22 12:35 CDT (release merge 2026-07-22T17:35:01Z UTC; signed-annotated tag at the merge SHA)
**Cycle-Time:** N/A — (mechanism: compute-cycle-time.sh; returned N/A — no `gate-outcome/plan-review-go` event and no `deployment-status/deploy-skill` or `deploy-harness` event carried a `v3.82` version tag; the 10 package rebuilds landed as in-PR commits on the release branch rather than a `deploy.sh --deploy` invocation)
**Result:** SUCCESS
**Outcome:** SUCCESS

#### Deployment Log v3.81
**Files deployed:** Version-identity + parser-SSOT bundle — one release PR #3656 (`release/v3.80-version-identity-and-parser-ssot → main`, merge-commit `e1bcb48f`), 5 issues, 9 files, **0 `.skill` packages rebuilt — this release touches zero skill sources**. **#2075** — `claim-version.sh` `_host_release_log_deployed()` off-by-one corrected (`st=$7`→`$8`): the RELEASE_LOG State column is awk field 8 (field 7 is Tag), so `claimed_set()` category-3 (DEPLOYED rows) never fired; the dead third safety-net is restored, the `:113` "State = field 7" header comment corrected, and a self-test fixture asserts a `State: DEPLOYED` row is now counted. **#1800** — `deploy.sh` Check 39 migrated off its duplicate `sed -nE` version parser onto the `version-grammar.sh` SSOT (`version_parse`/`version_cmp`) with a `set -e`-safe sourcing pattern, completing the "one grammar, zero drift" goal; the issue's original second target (`test_version_stamping.sh`) no longer exists — superseded by `test_version_freeness.sh`, which already sources the SSOT (scope trimmed at readiness). **#1801** — three v2.16 follow-ups: recovery-runbook forward-pointers into `stage-12-execute.md` / `stage-13-close.md`, `automated-closeout.sh` `validate_version` tightened onto the grammar SSOT (suffix forms rejected; three-component `vMAJOR.MINOR.PATCH` correctly retained), and a `git-workflow.md` signed-tag forward-pointer to `claim-version.sh`. **#2048** — version-less / theme-named close-out now emits the full Stage-13 output set (RELEASE_INDEX + RELEASE_DIGEST version-less marker, `_unversioned` notes link, marker-aware idempotency, CHANGELOG SKIP). **#1286** — orphan v7.5 lineage anchors dropped from `gate-evaluation-spec.md` + `handoff-coordinator-spec.md`.
**Mechanism:** git merge-commit (PR #3656 → main at `e1bcb48fa4ba55cf6f6ce8403fdd76864054eb9d`; **`--admin` override used** — base-branch policy prohibits a plain merge even when CLEAN/MERGEABLE, CI green 36/36). Phase B4 deploy = **no-op**: zero skill sources touched (no S-2 copy, no `.skill` rebuild). Re-anchored from provisional **`v3.80`→`v3.81`**: this release was **HELD at the Stage-12 gate** under an operator-rendered cross-PR sequencing decision. A Stage-4 Cross-PR Overlap Audit defect (the query filtered by milestone title instead of enumerating open `release/*` PRs) had recorded "Open PRs in scope: none"; the Phase A.5.6 pre-check surfaced three sibling v3.80 PRs, and `close-out-reliability-hardening` (#3618 — CLEAN, imminent, 286 overlapping lines in `automated-closeout.sh`) was allowed to land first. After #3618 merged and claimed v3.80, this branch reconciled with main (one conflict, in the `self_test()` summary strings, resolved by combining both sides' assertions), both self-test suites re-ran green, and the **A.5.6c pre-merge relabel** corrected the label on the branch (`git mv` plan v3.80 → v3.81 + restamp) BEFORE the merge — so the merge commit carries the correct version. The atomic ref-CAS then recomputed next-free = **`v3.81`** against anchor `v3.80` and pushed a signed-annotated tag at the merge SHA — won on first attempt, no collision. No `v3.80` tag was ever cut by this release (provisional-name abandonment). Residual per `re-version-recovery.md` R-2 (immutable history, accepted-as-documented): the branch name `release/v3.80-version-identity-and-parser-ssot` and the on-branch commit subjects retain the as-authored `v3.80` provisional label. **Reflexive note:** this release edits `claim-version.sh` — the very Stage-12 claim tool — so v3.81 was claimed by the tool *as-is* (pre-fix); #2075 touches only the dead category-3 net, never the live claim path. Stage-7 DevTest all-PASS · Stage-8 QA 5/5 AC MET · Stage-9 GO (operator-authorized).
**Timestamp:** 2026-07-22 19:38 (release merge at `e1bcb48f`; tag at merge SHA)
**Cycle-Time:** N/A — (mechanism: compute-cycle-time.sh; returned N/A — tooling-only release: no `deployment-status/deploy-skill` or `deploy-harness` event carried a `v3.81` version tag)
**Result:** SUCCESS
**Outcome:** SUCCESS

#### Deployment Log v3.80
**Files deployed:** Close-out reliability hardening bundle — one release PR #3618 (`release/v3.80-close-out-reliability-hardening → main`, merge-commit `4f9420c7`), 4 issues, 9 files across 9 commits, **0 `.skill` packages rebuilt — this release touches zero skill sources**. **#2539** — `automated-closeout.sh` `extract_milestone_slug` hardened a2→a4 (`^[a-z][a-z0-9-]*$`, last branch, first-match-wins) so pure-alpha / hyphen-less theme-named milestones resolve to the slug (field 2), never the Version (field 1); AC-3 unified the dry-run/apply row-match predicate (`log_row_match`); D-3 added a fail-loud preflight assertion using a true `findall` count (not a capped `subn(count=1)`) so an over-match fails loud instead of silently mis-resolving. **#2435** — `claim-version.sh --self-test` AND (E-1) `check-release-body-drift.sh --self-test` wired into `.github/workflows/release-tooling-smoke.yml`, CI-gating the U-1…U-10 + drift fixtures that previously ran only manually. **#2540** — new `core/deploy/tools/check-registry-currency.sh` + `.github/workflows/skill-registry-currency-check.yml`, the CI mirror of `deploy.sh --check` Check 5(d) (roster authority = the `deploy.sh` `OPERATIONS_SKILLS`/`RELEASE_SKILLS`/`CORE_SKILLS` arrays, canary excluded, per ADR-008; array-shape guard rejects the one-line garbage form; enforce posture) plus a gate-coverage register row in `core/standards/gate-efficacy-standard.md`. **#2678** — `check-release-body-drift.sh` now reads the canonical note from `origin/main` (not the working tree) via a `REPO_ROOT` seam (the production-reachable `DRIFT_CANONICAL_REF` knob was dropped), and the exit-2 consumers in `automated-closeout.sh` + `deploy.sh` were corrected to diagnose "git capability absence" instead of a false "gh offline".
**Mechanism:** git merge-commit (PR #3618 → main at `4f9420c72dfea27f7f857fa48dcbd6be0adf9614`; **`--admin` override used** — base-branch policy prohibits a plain merge even when CLEAN/MERGEABLE, required_reviews=0, CI green 36/36). Phase B4 deploy = **no-op**: zero skill sources touched (no S-2 copy, no `.skill` rebuild). Re-anchored from provisional **`v3.78`→`v3.79`→`v3.80`**: while this release ran Stages 4–9, concurrent releases claimed the intervening slots — `pda-rollup-and-portfolio` took **v3.78** and `95-deploy-tooling-resolver-and-test-parity` (#3543) took **v3.79**, each caught at the Commit-0 / claim-time re-verify (a HALT at Stage-6 Commit-0 re-rendered D-Version v3.79→v3.80); the atomic ref-CAS then recomputed next-free = **`v3.80`** against anchor `v3.79` and pushed a signed-annotated tag at the merge SHA — won on first attempt, no collision. **Reflexive self-heal:** this milestone is itself pure-alpha-named (`close-out-reliability-hardening`), so its own Stage-13 close-out exercises #2539's a4 fix against the now-fixed `main` tool; D-3 fails loud if slug resolution ever returns n=0. Stage-7 DT all-PASS · Stage-8 QA ACCEPT (34 edge cases, 0 failed) · Stage-9 GO (operator-authorized, 2026-07-21).
**Timestamp:** 2026-07-21 (release merge 2026-07-22T00:40:23Z UTC; tag at merge SHA)
**Cycle-Time:** N/A — tooling-only release (no `deployment-status/deploy-skill` event carried a `v3.80` version tag)
**Result:** SUCCESS
**Outcome:** SUCCESS

#### Deployment Log v3.79
**Files deployed:** Deploy/gate tooling de-duplication + test-parity bundle — one release PR #3543 (`release/v3.76-deploy-tooling-resolver-and-test-parity → main`, merge-commit `53c28fbe`), 4 issues, 30 files (+878/-243) across 11 commits. **#1477** — new `core/hooks/tests/block-fragile-refs.test.sh` (+1 file): fixture-based, runs the real hook at its deployed path, toggles the shared `.mode` through enforce/warn/off and restores on exit; asserts a positive case per detector class (Class L link, Class V cutover, positional bare issue-ref, Class U raw URL) plus clean-inline / out-of-scope-path / wrong-tool / allow-marker / bypass pass-throughs — restoring 12 `block-*.sh` ⇄ 12 `block-*.test.sh` parity. **#1476** — hook ownership derived by **enumeration**: a line-anchored `# hook-owner: <repo-relative-path>` magic comment now rides each of the 12 `core/hooks/block-*.sh` headers, `core/deploy/deploy.sh` Check 37 was rewritten to derive ownership from them, and **both** shared arrays (`C37_BYPASS_HOOKS` + `C37_OTHER_OWNERS`) are deleted — bypass membership is derived (owner == bypass index path), not separately declared; adjacent in-band reconcile of `core/rules/bypass-mode-readiness/_header.md` "Two further scripts" → 4 (+ regenerated `core/rules/bypass-mode-readiness.md` index, Check 38 green). **#2158** — the last duplicated template-sync resolver single-sourced to a new functions-only sourceable fragment `core/deploy/lib-template-sync-source.sh` (modeled on the already-sourced `lib-instance-path.sh`); `deploy.sh` and `core/deploy/tools/build-skill-packages.sh` both source it and delete their inline copies (function name `resolve_template_sync_source` retained so deploy.sh's 3 call sites are untouched), covered by a new `core/deploy/tests/test_resolve_canonical_source.sh` regression test (sources the fragment + asserts the #94 fixture case + a drift guard) wired into `.github/workflows/install-tests.yml`. **#1471** — the two markdown-link checkers reconciled onto **one canonical resolution rule** (**ADR-085**, superseding the fallback-establishing ADR-009): a link resolves relative to its source file's directory and a leading `/` denotes the workspace root (matching GitHub's rendered-blob behavior); the bare module-prefix workspace-root fallback is retired from `core/deploy/tools/check-doc-links.py`, `release/tools/check-release-links.py` now resolves leading-`/` against the repo root (stops skipping), the 19 workspace-rooted links in `core/CLAUDE.md.template` were rewritten bare → leading-`/` (verdict-preserving), and `core/standards/doc-link-maintenance-protocol.md` + `core/rules/doc-link-maintenance.md` + `core/deploy/tools/README.md` document the single rule (correcting the "matches GitHub rendering" attribution). **0 `.skill` packages rebuilt — this release touches zero skill sources.**
**Mechanism:** git merge-commit (PR #3543 → main, plain `gh pr merge --merge` at `53c28fbe05107f41b3be8e8f0bfebde7c2124a96`; **no `--admin` override needed** — all 11 commits `verified=true`/`valid`, `mergeStateStatus` CLEAN, CI green 49/49 check-runs). Phase B4 deploy = **no-op**: zero skill sources touched (no S-2 copy, no `.skill` rebuild), Check 11 harness-sync SKIPs (`HARNESS_LIST` empty per the Phase-3 account-switcher extraction), and the changed `core/rules/` files are read from the repo rather than mirrored. Re-anchored from provisional **`v3.76`**: while milestone #238 ran Stages 6–9 two concurrent releases claimed the intervening slots — `knowledge-corpus-hygiene` took **v3.76** (tag at `da4c116`) and `pda-rollup-and-portfolio` took **v3.78** (tag at `3994508`). Unlike the v3.74/v3.76/v3.77 re-anchors — which surfaced at the atomic claim — this collision was caught by the **Stage-12 Phase A.5.6c pre-merge freeness detector**, so the label was corrected **on the branch before the merge** (plan file `git mv` v3.76 → v3.79 + restamp, commit `337cc77`) and the merge commit carries the correct version; the atomic claim then recomputed next-free = **`v3.79`** and pushed a signed-annotated tag at the merge SHA — won on first attempt, no collision. No `v3.76` tag was ever cut by this release (provisional-name abandonment). Residual per `re-version-recovery.md` R-2 (immutable history, accepted-as-documented): the branch name `release/v3.76-deploy-tooling-resolver-and-test-parity` and 8 of the 11 commit subjects retain the as-authored `v3.76` provisional label. Stage-7 DevTest + Stage-8 QA cleared; Stage-9 GO (operator-authorized, 2026-07-17).
**Timestamp:** 2026-07-17 08:48 CDT (release merge 2026-07-17T13:48:14Z UTC; tag at merge SHA)
**Cycle-Time:** N/A — (mechanism: compute-cycle-time.sh; returned N/A — tooling-only release: no `deployment-status/deploy-skill` or `deploy-harness` event carried a `v3.79` version tag)
**Result:** SUCCESS
**Outcome:** SUCCESS

#### Deployment Log v3.78
**Files deployed:** Deterministic PORTFOLIO rollup bundle — one release PR #3549 (`release/pda-rollup-and-portfolio → main`, merge-commit `39945082`), 5 issues, 13 files (+1764/-7) across 8 commits. **#2578** — operational tier taxonomy (portfolio / program / project neutral structure) codified into `core/CLAUDE.md.template` (+16) and `core/governance/OPERATIONS.md` (+27). **#157** — the G7 project→portfolio rollup contract: new `core/standards/portfolio-writeback-contract.md` (+100; the bundle's `novel` surface) plus the `operations/templates/project-rollup-template.md` composed-entity template (+46) and its `operations/templates/README.md` registration, with the `core/disciplines/project-entity-model.md` emit/consume wiring. **#1771** — `core/deploy/tools/compose-portfolio.py` (+970), the deterministic PORTFOLIO.md execution engine, covered by `proj-alpha` / `proj-beta` rollup fixtures (+77) and wired into CI via `.github/workflows/release-tooling-smoke.yml`. **#276** — right-sized portfolio health score with freshness hard-gate + capacity / R-G-T / RAID-shell sections, and **#1169** — methodology-neutral cross-component risk aggregation, both landing in `operations/skills/weekly-status-rollup/SKILL.md` (+162/-4); `operations/skills/ppm-agent/SKILL.md` (+26/-1) consumes the contract. 2 `.skill` packages rebuilt (`ppm-agent`, `weekly-status-rollup`).
**Mechanism:** git merge-commit (PR #3549 → main at `399450827f07ace9e1f11e2f3ec76fc8a3d68a81`) + S-2 direct copy via `deploy.sh --deploy` (2 skills, 2 packages, 0 harness artifacts) + `build-skill-packages.sh` rebuild. Shipped **version-agnostic** by design: the Stage-4 provisional `v3.75` was claimed mid-Stage-6 by the concurrent `101-skill-and-data-entity-hygiene` release (milestone #244), so branch / PR / plan labels carried no version and the atomic ref-CAS claim recomputed next-free = **`v3.78`** against anchor `v3.77`, pushing a signed-annotated tag at the merge SHA — won on first attempt, no collision. Stage-7 DT 5/5 PASS · Stage-8 QA 24/24 MET · Stage-9 GO (operator-authorized, 2026-07-17).
**Timestamp:** 2026-07-17 08:29 CDT (release merge 2026-07-17T10:02:05Z UTC; tag at merge SHA)
**Cycle-Time:** N/A — (mechanism: compute-cycle-time.sh; returned N/A — no `deployment-status/deploy-skill` event carried a `v3.78` version tag at deploy time)
**Result:** SUCCESS
**Outcome:** SUCCESS

#### Deployment Log v3.77
**Files deployed:** Skill-suite hardening bundle — one release PR #3540 (`release/v3.75-skill-hardening → main`, merge-commit `b885d736`), 3 slices. **#155** — `health-check` gains Mode 8 `rollup` (`--scope portfolio|project`, `--depth full|status`): compose-not-absorb (invokes `weekly-status-rollup` Section 6 for PORTFOLIO.md composition, stages the proposal to `08-Generated/_health-check/`, never writes the bridge file); new `references/rollup-mode.md` + 3 sub-mode evals; the 7 existing modes + the 5-section output contract byte-identically preserved. **#2699** — semantic routing-conflict harness `run_routing_audit.py` (three-tier Jaccard pre-filter → embed+cosine → optional agent judge; lazily-imported optional embedding backend + stub fallback preserving the zero-dep offline CI contract) + **ADR-084** (first optional-heavy-dependency precedent); `run_eval_audit.py` imported read-only. **#3114** — `canonical-skill-structure.md §5` re-baseline (a multi-mode skill carrying a non-empty `references/` may exceed the soft ~500-line guideline; the hard Check-6 gate is the `references/` requirement at `C6_LINE_THRESHOLD=400`); additive, no file splits. 2 `.skill` packages rebuilt (`pmo-skill-refiner`, `health-check`).
**Mechanism:** git merge-commit (PR #3540 → main, `gh pr merge --merge` at `b885d7361e524cab0c2da2edfcace6a05e8a6984`; commits re-signed so no admin override needed — `mergeStateStatus` CLEAN, CI green 32/32). Re-anchored from provisional `v3.75`: while milestone #267 ran Stages 6–9, concurrent releases claimed `v3.75` (milestone #244) and `v3.76` (milestone #269); the atomic version claim recomputed next-free = **`v3.77`** and pushed a signed tag at the merge SHA — no collision. All three slices Stage-8 ACCEPT (#2699 4/4, #3114 3/3, #155 6/6); Stage-9 GO.
**Outcome:** SUCCESS — all three slices delivered as accepted; no rollback, no post-deploy defect.

#### Deployment Log v3.76
**Outcome:** SUCCESS
**Files deployed:** Knowledge-corpus hygiene bundle — one release PR #3545 (`release/knowledge-corpus-hygiene → main`, squash-merge `da4c116`), 5 issues. **#2221** — frontmatter backfill on 38 non-Tier-A `core/` docs + `deploy.sh` Check 50 flipped to global committed-default enforce (Approach D: `c50_mode="enforce"` hardcoded, `DEPLOY_CHECK_MODE=off` kill-switch retained, tier partition collapsed, precise authored-doc globs, `core/ADRs/` routed out to a separate effort). **#2702** — `[PROJECT_KEY]` classified as example-data (`depersonalization-spec.md §1.3`, citing `universal-vs-localized-context.md` DC3). **#2701** — `AUDIT_FRAMEWORK.md` REFRESHED, not retired (Collective Review reversed the initial retire lean on adversarial evidence): 19 stale `Projects/…` read-list paths → current split-arch homes, `[PROJECT_KEY]`→`[Project]` (bracketed), stale skill-count parameterized; the 12-dimension methodology retained. **#2917** — `estimation-standards.md §7` SPI RAG bands single-sourced by reference to the canonical owner (`channel-formats.md`). **#380** — vision / role model / build surfaces codified as 3 new `architecture-overview.md` H2 sections + ADR-083 (Canonical install model, extends ADR-017) + a `bundle-composition-doctrine.md §3` cross-reference. 7 `.skill` packages rebuilt.
**Mechanism:** squash-merge (PR #3545 → main, `gh pr merge --admin --squash` at `da4c116`; branch protection required admin override, CI 35/35 green). Provisional `v3.75` was contested by concurrent release milestone #244 (`skill-and-data-entity-hygiene`), which merged first (#3544, tag `v3.75` at `1975d42`); the atomic version claim recomputed next-free = **`v3.76`** and pushed a signed tag at the merge SHA — no collision. Three independent adversarial reviews preceded the Collective Review scope-lock; #2701 reversed RETIRE→REFRESH on that evidence.

#### Deployment Log v3.75
**Files deployed:** Skill-packaging + data-architecture entity-model hygiene bundle — one release PR #3544 (`release/skill-and-data-entity-hygiene → main`, merge-commit `1975d42`), 4 issues. **#2166** — the `Finding` entity (#19) added to the entity roster (Tier-2 amendment 18→19 per ADR-044): `entity-field-schemas.md` §3.19 field schema (8 level-marked V-FND rules + 4 negative tests), `project-entity-model.md` §6 owning-agent row (`build-reviewer / pmo-qa-auditor / pmo-technical-analyst → pmo-qa-lead`) + §7 storage row; the remaining no-entity-home skill outputs (exec status report, weekly roll-up, decision briefing, daily-status Teams, comms drafts) reclassified as ownerless renderings in `operational-artifact-inventory.md`; ADR-044's draft `severity` enum reconciled in-flight to `{CRITICAL,HIGH,MEDIUM,LOW}`. **#2168** — an ownership-collision check wired as `deploy.sh` Check 54 (ADR-044 I1/I3/I4): reconciles `per-skill-output-contracts.md` ↔ the §6 owning-agent matrix, ESCALATE on a second maintainer, renderings exempt (I4); live run 0 collisions / 0 false-positives across 19 entities, 97 producers reconciled. **#2074** — `extract-roster-needles.sh` converged on the `pmo_people_roster()` accessor (no literal roster path; resolution-equivalent across default / `PMO_INSTANCE_PATH` / `PMO_PEOPLE_ROSTER`). **#2066** — `design-exploration.md` §7 ASCII flow-block retrofit to a Mermaid flowchart per `process-flow-diagram-standards.md`. (Earlier-shipped siblings: #2069/#2160 knowledge-manager Check 1/6, and #2118 ownership model closed-subsumed via ADR-044.)
**Mechanism:** git merge-commit (PR #3544 → main, `gh pr merge --admin --merge` at `1975d42afdfeb934ab6b2a7f73653f7681a45a6e`; branch protection required admin override past the BLOCKED policy, CI green — 35/35 check-runs). Resumed from a crashed prior hub session (Stages 4–8 already complete on the branch); this session added the #2066 Stage-6 recovery-commit + a repo-integrity issue-ref-gate fix, then drove Stage 9→13. Signed-annotated `v3.75` tag claimed at the merge SHA; atomic re-check confirmed next-free = `v3.75` (concurrent milestone #267 is also provisional-v3.75 but unmerged → it re-anchors to v3.76), no collision.
**Timestamp:** 2026-07-16 CDT (release merge 2026-07-17T04:26:32Z UTC; tag at merge SHA)
**Outcome:** SUCCESS

#### Deployment Log v3.74
**Files deployed:** Durable CI regression-prevention gates for the two build-security advisory classes (GHSA-9cjm hook fail-open + GHSA-rw36 eval-viewer stored XSS) — the prevention layer the v3.73 security patch deferred ("handled separately"). Three slices on one PR #3428: **#3407** — a glob-derived fail-closed hook test (`core/hooks/tests/hook-fail-closed.test.sh`, enumerates `block-*.sh`, asserts every jq-hook fails closed `exit 2` at all three resolver paths) + a fix to `block-autonomy-ceiling`'s enforce-mode fail-open (jq gate reordered mode-aware) + **S2**: Security elevated to build-philosophy's **6th first-class value** (ADR-081) with a `software.md §Security` cluster. **#3409** — a 3-rule custom `template-context-XSS` semgrep set (keys on the missing context-aware encoder, so the fixed generator passes un-annotated) + a reviewed nosemgrep baseline + **S1** output-encoding standard + hardening of three residual eval-viewer sinks (`viewer.html:849/1300`, `eval_review.html:63`). **#3408** — transport + jsdom rendered-DOM XSS regression tests + a net-new `eval-viewer-tests.yml` CI workflow + ADR-082 (jsdom canonicalization). Original slice #3410 (sourced-lib deploy-completeness) DROPPED — subsumed by the concurrent v3.73 GHSA-g9g6 fix. `pmo-skill-refiner.skill` rebuilt.
**Mechanism:** git merge-commit (PR #3428 → main, `gh pr merge --admin --merge` at `6d70303c246b006616aea78c56cc7062f811758a`; branch protection required admin override past the unsigned-commits + BLOCKED policy, CI green — 32/32). A merge commit (not squash) deliberately preserves the mid-pipeline reconcile history: a concurrent v3.73 hotfix took the provisional version + subsumed slice #3410 during the build, so the branch was merged onto new main (both security sides preserved — additions-only on the shared `viewer.html` / `security.yml`) and re-versioned **v3.73 → v3.74**; atomic re-check confirmed next-free = `v3.74` (main had reached v3.73.1), no collision.
**Timestamp:** 2026-07-12 CDT (release merge 2026-07-13T00:45:28Z UTC; tag at Stage-13 close-out SHA)
**Outcome:** SUCCESS

#### Deployment Log v3.73.1
**Files deployed:** Delivery-path hotfix for the v3.73 security patch — `update.sh` did not refresh the deployed `.claude/hooks` bundle, so a hook/helper security fix (e.g. the v3.73 GHSA-g9g6 awk-primitive co-deploy) never reached an already-installed workspace via `update.sh`; only a full `setup-workspace.sh` re-run delivered it (#3430). Fixed as a single branch/PR/merge (PR #3431, `fix/3430-update-refresh-hooks → main`, GitHub-signed squash commit at `421658d395a207d6525f596cbf71440d115127ac`, 2026-07-12T21:59Z). Release Class: **hotfix** (code + one regression test + CI wiring only — no skill/governance/schema/ADR change). Adds `setup-workspace.sh --refresh-hooks`: a non-interactive, checksum-aware refresh of ONLY the security-hook bundle (hooks + co-shipped primitives + `lib/`), reusing the single-sourced `install_hooks` co-deploy list — an unedited platform hook (matches its recorded baseline, or has none) is refreshed to source; an operator-edited hook (diverged from baseline) is preserved + warned, never clobbered; `INSTALL_COMPLETE=1` prevents the EXIT-trap from rolling back the co-deployed primitives. Adds an `update.sh` Phase 5c that delegates to it and flips the "changed" flag ONLY on a genuinely REFRESHED hook, preserving the `EX_NOCHANGE` (64) no-op contract (the primitives' plain-`cp` logs `INSTALLED` even when byte-identical). No `.skill` package rebuild (no skill files touched). Regression: new 7-case `core/deploy/tests/test_refresh_hooks.sh` (stale-hook refresh · never-deployed-primitive co-deploy · operator `.mode` preserved · operator-edited-hook preserved+warned · no-op zero-`REFRESHED` · `--dry-run` no-mutation · missing-workspace fail-closed), wired into `install-tests.yml`; GHSA-g9g6 hook suite 23/23 and install end-to-end 12/0 both unchanged.
**Mechanism:** git squash-merge (PR #3431 → main, `--admin --squash`; branch protection required admin override past the signed-commits + conversation-resolution policy, CI green — 27/27 check-runs success) + the `v3.73.1` tag + published GitHub Release placed at the Stage-13 close-out commit (which carries the `.version` bump to `v3.73.1` alongside the corpus). Patch path from `v3.73`; dry-run confirmed next-free = `v3.73.1`, no collision.
**Timestamp:** 2026-07-12 CDT (fix merge 2026-07-12T21:59:01Z UTC; tag at Stage-13 close-out SHA)
**Outcome:** SUCCESS

#### Deployment Log v3.73
**Files deployed:** Coordinated-disclosure security patch for two advisories — **GHSA-g9g6-28c9-vrx5** (LOW, CWE-693) and **GHSA-fxcr-7x63-3g94** (MEDIUM, CWE-59/200/352) — bundled into one release PR #3426 (`release/security-g9g6-fxcr → main`, squash-merged at `874f59abee96965a4561a032bb186d7d2377715f`, 2026-07-12T21:00Z). Release Class: **security-patch**. **GHSA-g9g6:** the warn/enforce hooks `block-gh-path-leak.sh` + `block-fragile-refs.sh` fail-opened (`exit 0`) when a co-shipped internal primitive (`path-leak-patterns.sh` / `lib/positional-issueref.awk`) was absent — regardless of `.mode`, including enforce — and `positional-issueref.awk` was never co-deployed (its detector had never run on a deployed workspace). Fixed by extending the GHSA-9cjm / ADR-078 mode-coupled posture to internal primitives (enforce fail-closed `exit 2`, warn/off degrade), verifying the primitive API (`command -v` / an awk canary, not mere file existence), a caller-owned `exit 2`, and co-deploying the awk in `setup-workspace.sh` + `setup-ci-layout.sh`; ADR-078 amended (D6–D8 + accepted over-block residual). **GHSA-fxcr:** the eval-viewer generator (`release/skills/pmo-skill-refiner/eval-viewer/generate_review.py`) followed symlinks + hardlinks out of the workspace (CWE-59/22), embedded secrets verbatim (CWE-200/532), and accepted unauthenticated writes (CWE-352). Fixed by confining every read to the resolved workspace root + rejecting hardlinks, size-capping + best-effort-redacting embedded text (incl. `.svg`/`.env`/no-extension text files), and gating `POST /api/feedback` behind a loopback `Host` + same-origin `Origin` + a per-run CSRF token (`hmac.compare_digest`). `pmo-skill-refiner.skill` rebuilt at close-out. Verified via three adversarial refutation passes + a clean confirmation sweep; new regression tests (hook 23 cases + eval-viewer 47 cases) + a focused `eval-viewer-security` CI job; full hook suite 426/0. The build-security-hardening milestone (durable CI prevention gates for these classes) is handled separately.
**Mechanism:** git squash-merge (PR #3426 → main, GitHub-signed squash commit at `874f59ab`; CI green — 27/27 check-runs success; `--admin` past the signed-commits + conversation-resolution branch policy). The `v3.73` tag + published GitHub Release are placed at the Stage-13 close-out commit (which carries the rebuilt `pmo-skill-refiner.skill` alongside the source fix, so the tagged package is not the vulnerable eval-viewer). One CodeQL alert dismissed as a test-fixture false positive (`used in tests` — fake tokens exercising the redaction pass).
**Timestamp:** 2026-07-12 CDT (merge 2026-07-12T21:00:52Z UTC; tag at merge SHA)
**Outcome:** SUCCESS

#### Deployment Log v3.72
**Files deployed:** release-hub Mode R/O hardening on milestone `release-hub-mode-r-and-o` (milestone #258) — 5 issues (#2680, #2657, #2576, #2355, #2428) as delivery slices on a single branch/PR/merge (release PR #3405, `release/release-hub-mode-r-and-o → main`, merge-committed at `61b0ae1d95830aea5e2d3281a8744cd8f20506ec`, 2026-07-12T16:16Z). Release Class: **routine**. #2574 + #2575 were deferred at the Stage-5 scope-lock (owner-extension gap — composing their checks compliantly requires extending `release-planner` + `triage-design-rereview`, beyond this milestone's release-hub scope; re-bundle as a coordinated follow-up, integrated blueprint preserved on sub-task #3394). Delivers: **#2680** the Mode R read-only guardrail — recommend-only Output Contract + the recommend→act boundary-crossing protocol + a PROC failure-mode entry; **#2657** the Mode R terminal verdict→next-action route (GO → `Mode O on <milestone>`; NO-GO → dispositions-as-cleanup + the R→cleanup→re-run-R→Mode O loop) + an OUT failure-mode entry; **#2576** the Mode R methodology-neutrality + structural-cascade checklist group (group 7 — composes bundle-composition-doctrine frame-pluggability + ADR-033, zero inline logic); **#2355** the Mode O per-wave pre-spawn concurrent-PR collision check (Procedure 2 + spoke-launch guard); **#2428** the Mode O Stage-12 tail routing (B1+B3+B5, not a bare merge) + the merged-without-DEPLOYED-row guard. `release-hub.skill` rebuilt at close-out. Full hub-and-spoke run (release-hub Mode O): Stage 4 Planning → Stage 5 Solutioning (arch co-design + A6.5 adversarial review + scope-lock) → Stage 6 Engineering → Stage 7 DevTest + Stage 8 QA (15/16 AC MET, 1 non-defect PARTIAL, 0 defects; one count-cascade regression self-repaired mid-DT) → Stage 9 GO. Corpus close-out (RELEASE_NOTES + LOG VERIFIED transition + INDEX + DIGEST + CHANGELOG + published GitHub Release) via the Stage 13 chore PR.
**Mechanism:** git merge (PR #3405 → main, merge-commit strategy at `61b0ae1`; CI green — 27/27 check-runs success) + `v3.72` SSH-signed annotated tag at the merge SHA (via `claim-version.sh`; v3.72 next-free above the concurrent v3.71 PDA claim — no collision at the atomic ref-CAS claim instant).
**Timestamp:** 2026-07-12 CDT (merge 2026-07-12T16:16:03Z UTC; tag at merge SHA)

#### Deployment Log v3.71
**Files deployed:** Project Data Architecture (PDA) data-plane on milestone `pda-folder-intake-and-provenance` (milestone #263) — 4 issues (#2374, #2375, #2376, #2377) as delivery slices on a single branch/PR/merge (release PR #3391, `release/v3.70 → main`, merge-committed at `080876925c7d0624ea3e765d48d36188c56fb472`, 2026-07-12T02:51Z). Release Class: **novel**. Provisional **v3.70** at plan time (branch/PR names retained as build-record labels); the v3.70 slot was claimed by a concurrent pipeline-freshness release, so the Stage-12 atomic claim forward-versioned to **v3.71** (next-free). Delivers: **#2374** the uniform closed-set project scaffold + born-aligned frontmatter + the additive-union folder-enum migration (legacy 8-folder → closed 5-bin taxonomy) across the schema/contract corpus (founding **ADR-080**, Accepted); **#2375** the single `_inbox` intake path — classify / stamp / file + the raw→tracked extraction trigger (file-router retargeted to the 5-bin set; 11 bin-manifest templates); **#2376** Domain-C generated-artifact provenance stamping + the promotion strip→preserve lifecycle; **#2377** the Domain-B tracker frontmatter layer + raw→tracked provenance edges (RAID reconciled to the 15-column schema-of-record). All 4 `.skill` packages rebuilt. Full 13-stage hub-and-spoke run (release-hub Mode O): Stage 5 Solutioning (4 spokes + A6.5 adversarial review + Collective Review scope-lock; ADR-080 operator-ratified) → Stage 6 Engineering → Stage 7 DevTest (20/23 AC PASS, 0 defects) → Stage 8 QA (20 ACCEPT / 3 ACCEPT-WITH-RESIDUAL / 0 REJECT; CIAC-1/2/3 hold). Corpus close-out (RELEASE_NOTES + LOG VERIFIED transition + INDEX + DIGEST + CHANGELOG + published GitHub Release) via the Stage 13 chore PR.
**Mechanism:** git merge (PR #3391 → main, merge-commit strategy at `08087692`, 2 parents; CI green — 14/14 check-runs success) + `v3.71` SSH-signed annotated tag at the merge SHA (via `claim-version.sh`; provisional v3.70 forward-versioned to next-free v3.71 at the atomic ref-CAS claim after the concurrent release claimed v3.70 — no collision at the claim instant).
**Timestamp:** 2026-07-11 CDT (merge 2026-07-12T02:51:00Z UTC; tag at merge SHA)
**Cycle-Time:** N/A — data-architecture capability release; no version-keyed `plan-review-go` / `deploy-skill` event emitted in the operator-instance pipeline event log (`compute-cycle-time.sh v3.71` → N/A).
**Velocity:** planned 28 pts / delivered 28 pts (1.00) — class novel; 4 delivery slices (uniform closed 5-bin scaffold + folder-enum migration · single-inbox intake + extraction · generated provenance + promotion lifecycle · tracker frontmatter + raw→tracked edges); size mix 3×L + 1×M; 46 files changed; 0 deferred / 0 cut; 3 non-blocking fast-follows filed (#3385, #3386, #3387).
**Result:** SUCCESS — release PR #3391 merged (state MERGED, mergeCommit `08087692`); `v3.71` SSH-signed annotated tag valid on origin at the merge SHA; Stage-7 DevTest 20/23 AC PASS (0 defects) + Stage-8 QA 20 ACCEPT / 3 ACCEPT-WITH-RESIDUAL / 0 REJECT (CIAC-1/2/3 hold); pre-merge `deploy.sh --check` zero real failures (Check-7 green, per Stage-8 QA). Issues #2374/#2375/#2376/#2377 transition to closed at Stage 13 (Issue References used safe phrasing — no auto-close keywords).
**Outcome:** SUCCESS

#### Deployment Log v3.70
**Files deployed:** Pipeline-freshness + spoke-safety capability on milestone `98-pipeline-freshness-and-spoke-safety` (milestone #241) — 5 issues (#1685, #1960, #2083, #2215, #1561) as delivery slices on a single branch/PR/merge (release PR #3379, `release/v3.70-pipeline-freshness-and-spoke-safety → main`, squash-merged at `452dc96645d25f4c8191db5bf39c2802993dd9e8`, 2026-07-12T00:35Z). Release Class: **novel** (2 P2 cards + 1 Stage-5 ADR). Delivers: **#1960+#2083** the Phase A0.8 / G-PL4 bundle-entry freshness re-verification gate (re-runs each bundled card's repro/AC against a pinned live-`main` baseline through one 3-outcome routing table — admit-still-valid / close-resolved / re-scope-changed; #2083 = the drift/reconciliation-class no-skip tightening within the single gate; CIAC-1 MET); **#2215** hub-owned sub-task close (spoke posts output, hub closes — removes the #1342 authorized-close false-positive in-git) + the read-only-write-prohibition clause + ADR-079; **#1685** the Check-22 warn-log path citation correction (`core/hooks/` → `$(pmo_instance_path)/deploy-check-warn-log.jsonl`, matching `deploy.sh:2793`); **#1561** the synthesize-release-learnings `--self-test` hermetic fix (root cause = fixture coupling to the mutable operator-instance event-log, not the counting logic). Full 13-stage hub-and-spoke run (release-hub Mode O): Stage 5 design (2 parallel spokes + A6.5 adversarial review + Collective Review scope-lock) → Stage 6 (4 serial Engineering chips + 1 DT-remediation) → Stage 7 DT (caught + remediated 2 Blockers — a branch stale behind 6 origin/main security commits, and an ADR-078 number collision with core's security ADR, renumbered 078→079) → Stage 8 QA (18/18 ACs ACCEPT, CIAC-1 MET). Corpus close-out (RELEASE_NOTES + LOG VERIFIED transition + INDEX + DIGEST + CHANGELOG + published GitHub Release) via the Stage 13 chore PR.
**Mechanism:** git merge (PR #3379 → main, `--squash --admin`; branch protection required admin override, CI 29/29 green) + `v3.70` annotated tag at merge SHA `452dc966` (via `claim-version.sh`, minor bump from the v3.69.x anchor); version re-verified free at Stage-12 (latest release v3.69.1; no v3.70 collision; branch fresh — 0 unreachable origin/main commits at merge).
**Timestamp:** 2026-07-11 CDT (merge 2026-07-12T00:35:00Z UTC; tag at merge SHA)
**Cycle-Time:** N/A — governance/pipeline capability release; no version-keyed deploy event emitted in the operator-instance pipeline event log.
**Velocity:** class novel; 5 delivery slices (freshness gate G-PL4 · hub-owned close + ADR-079 · warn-log path fix · self-test hermetic fix); 10 files changed (pipeline/governance/schema docs + 1 tool + 1 ADR); #1686 enforce-flip deferred at Mode R pre-flight (unmet 128-issue G1 backlog, observation #3352); 5 non-blocking follow-up observations filed (#3352, #3354, #3355, #3383, #3388).
**Result:** SUCCESS — release PR #3379 merged (state MERGED, mergeCommit `452dc966`); `v3.70` annotated tag valid on origin at the merge SHA; goal ATTAINED (all 5 Outcome-Statement AFTER clauses delivered; the Success Indicator — a stale-repro card flagged before its spoke launches — was proven live when Stage-7 DT caught this release's own branch shipping stale). Issues #1685/#1960/#2083/#2215/#1561 transition to closed at Stage 13 (Issue References used safe phrasing — no auto-close keywords).
**Outcome:** SUCCESS

#### Deployment Log v3.69.1
**Files deployed:** Coordinated-disclosure security patch for two advisories — **GHSA-9cjm-v22x-4x33** (High, CVSS 7.3) and **GHSA-rw36-5pf9-w2vc** (Medium, CVSS 5.0) — plus the delivery-path fix that made the first one reachable. Release Class: **security-patch**. 40 files changed (+1,551/−277) across three merges to `main`. **GHSA-9cjm:** the PreToolUse security hooks failed **open** when `jq` was unresolvable under the pinned PATH, silently disabling the perimeter on documented Homebrew installs. Fixed by a shared absolute-path `jq` resolver (`core/hooks/lib/dep-resolve.sh`, new) plus **mode-coupled fail-closed** posture applied across the whole hook class — `block-autonomy-ceiling`, `block-credential-reads`, `block-destructive`, `block-egress`, `block-fragile-refs`, `block-fs-boundary`, `block-gh-path-leak`, `block-mcp-writes`, `block-rm-prefer-trash`, `block-shell-injection`, `block-skill-direct-edit`, and `audit-mcp-usage`. A **second fail-open** was found and closed in the same pass: `block-fs-boundary` permitted `..` traversal when `python3` was unavailable. Per-hook regression coverage landed for eleven of the twelve hooks under `core/hooks/tests/` — one new suite file (`audit-mcp-usage.test.sh`; suite count 12 → 13) plus ten existing suites extended with the advisory regression block; `block-fragile-refs` has no per-hook suite at `075390e5`. A shared class-wide guard `core/hooks/tests/check-hook-dep-hardening.sh` runs in `.github/workflows/security.yml`; ADR-078 records the security-hook dependency-resolution posture. **GHSA-rw36:** stored XSS in the eval-viewer — attacker-influenced eval output reached `innerHTML` render sinks unescaped. Fixed by **per-sink** escaping in `release/skills/pmo-skill-refiner/eval-viewer/viewer.html` plus defense-in-depth transport escaping and xlsx hyperlink sanitization in `generate_review.py`; `pmo-skill-refiner.skill` rebuilt in the same merge. **Delivery-path fix (#3384):** `lib/dep-resolve.sh` was not co-deployed into `.claude/hooks/lib/`, so the hardened hooks could not resolve their shared helper in the deployed layout — the hardening shipped but did not reach an installed workspace. Co-deploy added to `docs/scripts/setup-workspace.sh` and `core/hooks/tests/setup-ci-layout.sh`.
**Mechanism:** three merges to `main`, tag placed at the last. The two advisory fixes merged from **private security-advisory forks** (`8304098c` GHSA-9cjm at 2026-07-11T21:49:37Z; `849e3ec5` GHSA-rw36 at 2026-07-11T21:50:03Z) — fork merges carry no PR number, which is why the Release PR column names only #3384. The delivery-path fix merged as PR #3384 (`hotfix/deploy-dep-resolve-lib → main`) at `075390e5`, 2026-07-11T22:33:24Z. Tag `v3.69.1` is an annotated tag object (`d122240a`) resolving to `075390e5`. Patch path from `v3.69`; no collision at the claim.
**Timestamp:** 2026-07-11 CDT (final merge 2026-07-11T22:33:24Z UTC; GitHub Release published 2026-07-11T22:42:37Z; tag at the merge SHA)
**Result:** SUCCESS — both advisories closed and published; tag `v3.69.1` valid at `075390e5`; GitHub Release live. The delivery-path gap that would have left the hook hardening undeployed was caught and fixed **before** the Release was published, not after.
**Outcome:** SUCCESS — **backfilled** at Stage 6 of `closeout-output-set-integrity` (milestone #304, issue #3727). The release itself shipped correctly (signed tag, published GitHub Release, both advisories resolved); the Stage-13 corpus output set was never written. This row plus the Deployment Log block, the `RELEASE_INDEX` / `RELEASE_DIGEST` / `CHANGELOG` entries, and the authored `v3.69.1_RELEASE_NOTES.md` restore the complete output set. Same defect class as the `v3.57` backfill above; discovered by that backfill's own Stage-7 cross-check.

#### Deployment Log v3.69
**Files deployed:** `deploy.sh --check` drift drainage on milestone `94-deploy-check-drift-remediation` (milestone #237) — 4 issues (#3198, #2213, #2217, #3375) as delivery slices on a single branch/PR/merge (release PR #3376, `release/v3.69-deploy-check-drift-remediation → main`, merge-committed at `68740d58e947ed17ed6fcbed0c36986531460e54`, 2026-07-11T01:46Z). Release Class: **routine** (cleanup-debt drainage; no new capability). Drains accumulated Check 5/6/7 source drift to literal `deploy.sh --check` green (0 hard FAILs), sequenced source-edits-first / package-rebuild-last per the #3198→#2217 build dependency: **#3198** D-Refs `references/` split of `pmo-program-coordinator` (25,733→23,321 B) + `pmo-tier-2-support` (25,951→24,039 B) mirroring the #2069 precedent (Check 6 green); **#2213** designated `core/governance/OPERATIONS.md` as SSOT and reduced the drifted dual-home `operations/OPERATIONS.md` (105,499 B) to a 1,001 B pointer stub (verified a lossless stale-subset — 0 unique sections, no content-loader), reshaped deploy.sh Check 9 from the OPERATIONS mirror-pair to an SSOT+pointer duplicate-home check, tokenized 29 hardcoded `pmo-platform/analysis/` refs across 11 files to the operator-instance analysis-path token, and pruned the stale OPERATIONS pair from the blast-radius Check-9 shadow (test 19/19); **#2217** re-measured the stale set on the assembled branch and rebuilt 32 content-stale `.skill` packages (terminal — the set grew from the planning-time 25 as #2213's injected canonical edits re-staled additional packages); **#3375** (an unpackageable-skill discovery surfaced by the #2217 terminal rebuild, folded in at the Stage-9 scope-expansion gate) trimmed 2 over-1024-char SKILL.md descriptions (`context-budget-auditor` 1203→853, `skill-compliance-auditor` 1706→961 — all trigger phrases preserved) and built their 2 previously-missing packages, clearing the last 2 Check-7 `package missing` FAILs. Proportionate scaffold (Claude Code path — the release PR + git history as the audit surface; no per-stage sub-task issues). Corpus close-out (RELEASE_NOTES + LOG VERIFIED transition + INDEX + DIGEST + CHANGELOG + published GitHub Release) via the Stage 13 chore PR.
**Mechanism:** git merge (PR #3376 → main, `--merge --admin`; branch protection required admin override, CI green — CodeQL + governance gates all pass) + `v3.69` signed-annotated tag at merge SHA `68740d5` (SSH ED25519, Good signature; tagger is the noreply identity — the signature-verification gmail mapping is local allowed-signers only, absent from the tag object); version re-verified free at Stage-12 (latest release v3.68; no v3.69 collision; origin/main undivergent at `c62fadc`).
**Timestamp:** 2026-07-10 CDT (merge 2026-07-11T01:46:15Z UTC; tag 01:46Z)
**Cycle-Time:** N/A — governance/tooling drift-remediation release; no version-keyed deploy event emitted in the operator-instance pipeline event log.
**Velocity:** class routine; 4 delivery slices (D-Refs split · governance SSOT · package rebuild · folded-in description fix); files-changed: 3 SKILL.md + OPERATIONS pointer + deploy.sh + blast-radius + 11 tokenized corpus files + 34 rebuilt/new `.skill` package pairs.
**Result:** SUCCESS — release PR #3376 merged (state MERGED, mergeCommit `68740d5`); `v3.69` signed-annotated tag valid on origin at the merge SHA (SSH ED25519, Good signature); zero signing bypasses; literal `deploy.sh --check` green (0 hard source FAILs — Check 6/7 clean, Check 9 pointer OK). Issues #3198/#2213/#2217/#3375 transition to closed at Stage 13 (Issue References used safe phrasing — no auto-close keywords). The in-passing discovery #3375 (2 rostered skills unpackageable on over-limit descriptions) was folded into scope at the Stage-9 gate and resolved in-release, so the milestone achieves its literal `--check` green Outcome.
**Outcome:** SUCCESS

#### Deployment Log v3.68
**Files deployed:** QA dev-test/acceptance/fitness modes + automated eval execution on milestone `79-qa-devtest-modes-and-automated-eval-execution` (milestone #161) — 4 issues (#222, #219, #2226, #172) as P0 fully-serial delivery slices on a single branch/PR/merge (release PR #3314, `release/v3.67-qa-devtest-modes-and-automated-eval-execution → main`, merge-committed at `3103304260d6b2b930e2477fa4825a220a61a369`, 2026-07-10T20:08Z). **Re-versioned twice on the version axis (branch/PR names retained as cosmetic build-record labels):** provisional **v3.66** at plan time collided with the merged `software-domain-templates` release (Commit-0 re-verify forward-versioned to **v3.67**, 2026-07-09); at Stage-12 entry the concurrent `87-methodology-pack-catalog` release merged and claimed the `v3.67` tag first, so the Phase A.5.6 pre-merge freeness re-check (adapter dry-run) recomputed next-free = **v3.68**, applied on-branch pre-merge (plan renamed `v3.68_RELEASE_PLAN.md`; SKILL.md frontmatter re-stamped; plan Deviation Log row 10; the A.5.6c disposition was operator-pre-rendered at the Stage-12 authorization). Sibling merge had also introduced an add/add conflict on the shared provisional plan-file name — dissolved by the re-version rename; zero content-file overlap. **Stage-12 A.7 deviation:** the release PR was found still-draft at Stage-12 entry (the Stage-9 Phase A8 draft→ready transition had not been executed mechanically; Stage-9 itself completed with GO); remediated in-stage per the A.7.3 codified remediation (`gh pr ready`), then MERGEABLE/CLEAN. Release Class: **novel** (new reference/eval/script file classes under pmo-qa-auditor; D-class decisions D-C/D-Concurrency/D-ModeLetters). Ships the full Stage-7/Stage-8 executor surface on pmo-qa-auditor: **#222 Mode G — Dev Testing** (Stage-7 executor: PR + release plan → structural→contract→content-quality→integration eval-assertion ladder → quality report as PR comment; + release-wide D-4 count-free conventions, CD-2 scoped-pointer report header, Change-5 staleness remediation); **#219 Mode H — Acceptance Review** (Stage-8 executor: GitHub-Issue AC → per-criterion verdicts under the Stage-8 enum, consuming the acceptance-assertion contract — no rubric re-definition; FM-3 gap-census fix); **#2226 Mode F — Release-Process Fitness Audit** (producer mode: 13-dimension rubric + mode spec via the D-5 1→2 split, characterization fixtures, search primitives, script-execution allowlist registration, cadence §6 dispatched-column writer); **#172 Automated Eval Invocation Protocol** (stage-07 extended-protocol H2 + EI-S8 registration block: trigger rows `EI-S7-01`/`EI-S7-02`/`EI-S8-01`, result→gate projection with the `PASS / FAIL / SKIP / EXCEPTION` record schema, FAIL/EXCEPTION escalation wired to the Inter-Stage Feedback Protocol, Autonomy Tier-2 Bounded-Auto declaration with Tier-1 descent — register-don't-build: zero new runner/verdicts/gates). **AC5 pilot executed live during this release's own DT/QA:** `EI-S7-01` fired at Stage-7 entry and returned **FAIL** (branch-freshness: 4 unreachable base commits, Blocker F-01) → Tier-1 [ADJUST] → fix (merge `c73445b`) → `EI-S7-02` re-fired on Pass-2 → PASS; `EI-S8-01` PASS (structural 1/0 · acceptance 3/0 · judgment 1/0) — the full projection chain exercised end-to-end. Dev Testing PASS (Pass-2 after the F-01 Blocker; F-02/F-03 factual corrections absorbed); QA acceptance PASS across the 4 issues, zero overrides. **Operational deploy (S-2, Stage 12):** pmo-qa-auditor deployed to the Cowork install path + user-local mirror (SKILL.md `8885ca59` + 11 references/ files + evals/ + scripts/); `.skill` package rebuilt ONCE at release-cut (R6 mitigation) and committed via the Phase J.5 chore PR #3323; installed roster verified carrying the Mode F/G/H triggers post-deploy. Corpus close-out (RELEASE_NOTES + LOG VERIFIED transition + INDEX + DIGEST + CHANGELOG + RELEASE_REVERSIONS rows + published GitHub Release) via the Stage 13 chore PR.
**Mechanism:** git merge (PR #3314 → main, `--merge`; plain merge — base policy satisfied by 26/26 green CI) + `v3.68` signed-annotated tag at merge SHA `3103304` (SSH ED25519, Good signature; `claim-version.sh` atomic ref-CAS; A.5.6 pre-merge dry-run recomputed next-free = v3.68 after the sibling claimed v3.67; no collision at the claim instant).
**Timestamp:** 2026-07-10 UTC (merge 20:08:17Z; tag 20:08:45Z)
**Cycle-Time:** N/A — no `gate-outcome/plan-review-go` or version-keyed `deployment-status/deploy-skill` event rows for v3.68 in the operator-instance pipeline event log (`compute-cycle-time.sh v3.68` verdict N/A; the S-2 deploy executed at Stage 12 without version-keyed event emission).
**Velocity:** planned 24 pts / delivered 24 pts (1.00); files-changed 15; allocation 4/20/0 pts (feature/debt/protocol-slack); class novel; mechanism: compute-release-velocity.sh (delivered/allocation manually computed from the milestone membership per the standard's manual-fill clause — the tool's live-state run at close predated this same close-out's Stage-13 issue closures; #172 `enhancement` → feature 4; #222/#219 `skill-update` → debt 12; #2226 default → debt 8).
**Result:** SUCCESS — release PR #3314 merged (state MERGED, mergeCommit `3103304`); `v3.68` signed-annotated tag valid on origin (SSH ED25519, Good signature) at the merge SHA; zero signing bypasses; S-2 deploy + mirror completed; package rebuild committed (chore PR #3323). Issues #222/#219/#2226/#172 transition to closed at Stage 13 (the release PR Issue References block used `References` — no auto-close keywords).
**Outcome:** SUCCESS

#### Deployment Log v3.67
**Files deployed:** Methodology-pack catalog on milestone `87-methodology-pack-catalog` (milestone #229) — 5 issues (#1085, #1067, #1788, #1803, #1973) as delivery slices on a single branch/PR/merge (release PR #3316, `release/v3.67-methodology-pack-catalog → main`, merge-committed at `883baa6ca27ea0936bfc5d7a8ff23930f99953dd`). Provisional **v3.66** at plan time; the sibling `software-domain-templates` release claimed v3.66 first, so the Commit-0 re-verify forward-versioned to **v3.67** (next-free — plan risk R1 materialized as designed), re-verified free at the Stage-12 atomic claim with no collision. Release Class: **cross-cutting** (spans a skill, a protocol, two schemas, and intake templates atop the 86 pack foundation). Populates the methodology-pack catalog at the intake seam: **#1085** intake-desk resolves the active methodology (a Phase-2-entry resolution step reading `operational_methodology ?? delivery_approach` via the 5-rung resolver) and applies the work-organization-mapping-framework §4.2 place-and-relate; **#1067** codifies the deterministic type→template routing rule (a new intake-style-guide §1b ahead of the 5-test) with a determinate-classification-decided-tool-blind guardrail in decision-discipline; **#1788** adds the optional per-space `operational_methodology` + `release_methodology` fields (V16 + V17 appended to the live V15 tail, meta-archetypes excluded per-space) with the CIAC-3 precedence contract; **#1803** specifies the reserved §1.2.1 Arm-3 `control-field` body + a pack-level `[[controls]]` cross-cutting control concept coupled to the shipped SR-G architecture/DoR gate (founding **ADR-077**, Accepted), grammar not re-opened; **#1973** ships the Epic/Story intake templates (template-per-type: `epic.yml` + `story.yml` structurally stamp `type:epic`/`type:story` + `status: proposed`; the selector arm was rejected and `improvement.yml` left unchanged). Two readiness-trimmed members (#1777 doc-subsumed, #1971 design-contradicted by the shipped v3.60 kanban pack) were returned to triage before the run. Dev Testing 5/5 PASS (with a branch-freshness sync of the disjoint v3.66 close-out and a `fix(dt):` one-line count correction to `docs/FIRST_STEPS.md`, re-verified); QA acceptance 5/5 ACCEPT at score 1.000, zero Override Records (incl. #1788's 9/9 negative-fixture rejections and #1803's §7.4 self-fire defense). One `SKILL.md` body changed (`intake-desk`) → the `intake-desk.skill` package rebuilds (R-7) at Stage 13. Corpus close-out (RELEASE_NOTES + LOG VERIFIED transition + INDEX + DIGEST + CHANGELOG + published GitHub Release) via the Stage 13 chore PR.
**Mechanism:** git merge (PR #3316 → main, `--squash --admin`; branch protection required admin override, CI 26/26 green) + `v3.67` signed-annotated tag at merge SHA `883baa6` (SSH ED25519, Good signature; `claim-version.sh` atomic ref-CAS; dry-run confirmed next-free = v3.67, no collision at claim).
**Timestamp:** 2026-07-10 UTC
**Cycle-Time:** N/A — governance/reference release (skill + schema + protocol + intake-template edits; no `deploy-skill`/`deploy-harness` cutover event emitted).
**Result:** SUCCESS — release PR #3316 merged (state MERGED, mergeCommit `883baa6`); `v3.67` signed-annotated tag valid on origin (SSH ED25519, Good signature) at the merge SHA; zero signing bypasses. The five parent issues (#1085, #1067, #1788, #1803, #1973) auto-transitioned to closed at merge via the release PR Issue References block.
**Outcome:** SUCCESS

#### Deployment Log v3.66
**Files deployed:** Software-domain artifact templates on milestone `software-domain-templates` (milestone #268) — tracking umbrella #71 + 2 wave slices (#2149, #2150) on a single branch/PR/merge (release PR #3308, `release/v3.66-software-domain-templates → main`, merge-committed at `671e0bf609e08b59d60f683b0875c3f488552881`; provisional **v3.66** claimed clean at cut — dry-run confirmed next-free = v3.66 at merge, no re-version). Release Class: **novel** (7 new reference-doc-class template files + 2 D-class decisions D-MirrorRegistration / D-TaxonomyRowShape). Authors the 7 software-domain template files the shipped 5-Layer Template Architecture (`template-taxonomy.md` / `template-storage.md` / `template-protocol.md`) mapped to canons but left `(none — gap)`: **Wave 1** (#2149) ADR (Nygard 2011) / Runbook (Google SRE §Runbook Design) / Design doc (Google design-doc convention, FDD/TDD/HLD/LLD variant discriminator); **Wave 2** (#2150) RFC (IETF RFC 7322 + Rust RFC) / PRD (`product-management:write-spec` convention) / Postmortem (Google SRE §Postmortem Culture) / Test plan (PMBOK 7 §Quality + `engineering:testing-strategy`). Each carries the 15-field L4 provenance header at `review_status: DRAFT` (P3 proof-of-utility gate unmet — never APPROVED/PROMOTED); `canon_compat` uses anticipated-path-at-DRAFT semantics with the negative live-plugin evidence recorded (the plugin suites are not installed / not in the marketplace roster as of the release). Flips `template-taxonomy.md` §4.1–§4.7 Gap Status + §6 rows 1–6 and adds a new §6 row 9 (Test plan) with an atomic §6-count / §7-note / §2-gap-count sweep (taxonomy §6 now 9 rows, 0 residual gaps). D-MirrorRegistration ruled **canonical-only** — zero `TEMPLATE_SYNC_MAP` entries this release (registration follows the first runtime consumer). Independent Phase-A6.5 adversarial review on both Stage-5 designs returned Major/0-Blocker (advisory, ingested at Collective Review). Dev Testing PASS both waves (3 Tier-1 findings — branch-freshness rebase + `domain_practice` provenance + template blank-line — remediated via a `fix(dt):` commit and cleared on independent re-review); QA acceptance ACCEPT ×3 at score 1.00, zero overrides. The 6 `template-taxonomy.md` consumer-skill `.skill` packages rebuild (R-7) at Stage 13. Corpus close-out (RELEASE_NOTES + LOG VERIFIED transition + INDEX + DIGEST + CHANGELOG + published GitHub Release) via a Stage 13 chore PR.
**Mechanism:** git merge (PR #3308 → main, `--merge`) + `v3.66` signed-annotated tag at merge SHA `671e0bf` (SSH ED25519, Good signature; `claim-version.sh` atomic ref-CAS; dry-run confirmed next-free = v3.66, no collision).
**Timestamp:** 2026-07-09 UTC
**Cycle-Time:** N/A — governance/reference release (template files + taxonomy registry edits; no `deploy-skill`/`deploy-harness` cutover event emitted).
**Result:** SUCCESS — release PR #3308 merged (state MERGED, mergeCommit `671e0bf`); `v3.66` signed-annotated tag valid on origin (SSH ED25519, Good signature) at the merge SHA; zero signing bypasses. Issues #71 / #2149 / #2150 close at Stage 13 (release PR carried no auto-close keywords — Issue References used `References`).
**Outcome:** SUCCESS

#### Deployment Log v3.65.1
**Files deployed:** Comment author-association trust boundary on milestone `109-comment-trust-boundary` (milestone #270) — single card #3261 as one branch/PR/merge (release PR #3304, `release/v3.65.1-comment-trust-boundary → main`, merge-committed at `814c9db3cfa7ff39608cce518f578cae979ba65c`; patch path — `patch_base` v3.65 → **v3.65.1**, dry-run confirmed next-free, no re-version). Release Class: **hotfix** (operator override rendered at the Stage-3 B1 D-ReleaseClass gate; the cross-cutting mechanical trigger — 5 `pipeline/stage-*.md` files touched — was surfaced and weighed at the Stage-9 gate and **hotfix held**, because the Deep-review substance was already satisfied by an independent Phase-A6.5 adversarial review + blast-radius + N-way consistency). Codifies an author-association gate (trusted set `OWNER`/`MEMBER`/`COLLABORATOR`) at the pipeline's comment-as-stage-I/O seam — untrusted-association comments are surfaced to the operator and never read as stage content, instructions, requirements, or evidence — plus a Stage-13 lock-at-close moderation posture (Phase C5) for finished sub-task threads, and corrects a falsified "no review comments arrive" dormancy claim across 4 carriers. Independent Phase-A6.5 adversarial review returned **SURVIVES-WITH-FINDINGS**; all 9 adopted findings implemented (incl. FMF-1 AC verification-scoping that prevents a deterministic Stage-8 self-referential false-fail). Dev Testing 11/11, QA acceptance 11/11 (zero overrides). Follow-up scope routed: issue-body ingestion gate (upstream of comments) and 2 platform defects filed as separate proposed items. Corpus close-out (RELEASE_NOTES + LOG VERIFIED transition + INDEX + DIGEST) via a Stage 13 chore PR.
**Mechanism:** git merge (PR #3304 → main, `--merge`) + `v3.65.1` signed-annotated tag at merge SHA `814c9db` (SSH signature; `claim-version.sh` atomic ref-CAS, patch path from `patch_base` v3.65; dry-run confirmed next-free = v3.65.1, no collision).
**Timestamp:** 2026-07-04 UTC
**Cycle-Time:** N/A — governance/reference hotfix (pipeline references, governance doc, one ADR, one catalog entry); no `deploy-skill`/`deploy-harness` cutover event emitted.
**Result:** SUCCESS — release PR #3304 merged (state MERGED, mergeCommit `814c9db`); `v3.65.1` signed-annotated tag valid on origin at the merge SHA; zero signing bypasses. Parent #3261 marked closed at Stage 13 via the release PR Issue References block.
**Outcome:** SUCCESS

#### Deployment Log v3.65
**Files deployed:** Verification execution surface on milestone `70-verification-execution-surface` (milestone #144) — 4 issues (#99, #170, #103, #227) as delivery slices on a single branch/PR/merge (release PR #3205, `release/v3.65-verification-execution-surface → main`, merge-committed at `b1a21527d315929b4a0ae1f4d684cd8122dae283`; provisional **v3.65** claimed clean at cut — dry-run confirmed next-free = v3.65, no re-version). Release Class: **cross-cutting** (spans Stages 4/6/7/8/9/13 + shared verification infrastructure; #99 changes the invariant-re-verification mechanism every gate's verdict relies on). Sequence follows bundle-composition-doctrine §9 (foundation → schema → surface → executor): #99 → #170 → #103 → #227. **#99** — QC4-05/AV-1 invariant re-verification made structurally sound: replaces the grep/string-match mechanism (proven false-FAIL, possible false-PASS) with region-scoped AV re-verification returning correct PASS/FAIL verdicts — the foundation that protects every gate's verdict. **#170** — cross-issue acceptance criteria expressed as testable predicates plus a release-scoped integration check that verifies them as an integrated artifact before the Stage-9 gate (previously cross-issue cohesion lived in prose and was never tested). **#103** — a domain verification execution surface for Stages 7/8 beyond doc/content review: Stage 8 consumes a domain-selected verification run as acceptance evidence and behavioral ACs get a worked verification method (the Stage-7 runtime-suite half shipped separately; this completes the Stage-8 + behavioral-AC worked-example half). **#227** — the verification-automation script that executes a release plan's Verification Plan programmatically — the shared executor for #170 and #103, built last. Corpus close-out (RELEASE_NOTES + LOG VERIFIED transition + INDEX + DIGEST + CHANGELOG) via a Stage 13 chore PR.
**Mechanism:** git merge (PR #3205 → main, `--squash`) + `v3.65` signed-annotated tag at merge SHA `b1a2152` (SSH ED25519, Good signature; `claim-version.sh` atomic ref-CAS; dry-run confirmed next-free = v3.65, no collision).
**Timestamp:** 2026-07-03 UTC
**Cycle-Time:** N/A — verification-infrastructure release (pipeline references, specs, and verification tooling); no `deploy-skill`/`deploy-harness` cutover event emitted (mechanism: compute-cycle-time.sh).
**Result:** SUCCESS — release PR #3205 merged (state MERGED, mergeCommit `b1a2152`); `v3.65` signed-annotated tag valid (SSH ED25519, Good signature on origin) at the merge SHA; zero signing bypasses. Issues close at Stage 13 with per-issue Stage-8 QA evidence (release PR carried no auto-close keywords).
**Outcome:** SUCCESS

#### Deployment Log v3.64
**Files deployed:** Executable acceptance testing — the first executors of the Stage-8 acceptance machinery — on milestone `84-executable-acceptance-testing` (milestone #204) — 3 issues (#218, #217, #197) as delivery slices on a single branch/PR/merge (release PR #3202, `release/v3.64-executable-acceptance-testing → main`, merge-committed at `4a63bfafbf3302d5ed7218458e95ade0ae750d47`; provisional **v3.62** collided at Commit-0 re-verify with concurrent releases `31-immutable-adr-system` (#3193, claimed v3.62) and v3.63 (active worktree), forward-versioned to next-free **v3.64** at Stage 12 — rule-determined, forward-only; v3.62/v3.63 are claimed siblings, not gaps). Release Class: **novel** (new eval assertion type + new template file + new auditor gate + D-class decisions). Topology D-C SINGLE (Engineering Commit 0), P0 fully-serial. Sequence #218 → #217 → #197 (#218 builds the assertion framework the report renders; #217 authors the template that renders #218's output; #197 the independent auditor gate). **#218** — the `acceptance` assertion type for Stage-8 QA: AC-ingestion from a GitHub Issue → per-criterion two-judgment grading (gradability-class + binary satisfaction, reconciling the eval-writer binary-judge consensus with the Stage-8 six-value drift-aware enum) → acceptance-score roll-up with an all-drift-out denominator, aligned to the Stage-8 verdict enum (`core/skills/eval-writer/SKILL.md`, `core/skills/eval-writer/references/acceptance-assertion-type.md`, `core/skills/eval-writer/references/rubric-templates.md`, `core/skills/eval-writer/evals/fixtures/acceptance-sample-issue.md`, `core/skills/eval-writer/evals/stage-gates/stage-08-qa-testing/evals.json`, `core/skills/eval-writer/evals/stage-gates/README.md`), founded on **ADR-071** (`core/ADRs/ADR-071-acceptance-assertion-type.md`, Accepted — the two-judgment grading + all-drift-out score decision). **#217** — the acceptance-report template that renders #218's output (`operations/templates/qa-acceptance-report-template.md`: three reader tiers — verdict / detail / evidence — carrying the six Stage-8 output sections, with a machine-parseable acceptance-matrix block whose columns + all-drift-out score are the co-design contract with the `acceptance` assertion type), wired into the Stage-8 Outputs spec (`release/references/pipeline/stage-08-qa-testing.md`). **#197** — pmo-qa-auditor gate **G11** (conditional-on-ambiguity AUQ-presence detection): catches an ask-when-ambiguous skill silently losing its `AskUserQuestion` fallback, coexisting with G1–G10 (`core/skills/pmo-qa-auditor/SKILL.md`, `core/skills/pmo-qa-auditor/references/conditional-auq-presence-detection.md`). Two `SKILL.md` bodies changed (`eval-writer`, `pmo-qa-auditor`) → the two `.skill` packages (`packages/eval-writer.skill`, `packages/pmo-qa-auditor.skill` + `.sha256`) are rebuilt at Stage 13 (R-7 post-merge package rebuild).
**Mechanism:** git merge (PR #3202 → main, `--merge`) + `v3.64` signed-annotated tag at merge SHA `4a63bfa` (SSH ED25519, Good signature; `claim-version.sh` atomic ref-CAS; provisional v3.62 forward-versioned to v3.64 at Stage 12 on the concurrent-release collision — v3.62/v3.63 are claimed siblings, not gaps).
**Timestamp:** 2026-07-03 UTC
**Cycle-Time:** N/A — eval-framework + template + auditor-gate release; the two `.skill` packages rebuild at Stage 13 (R-7), not via a separate `deploy-skill`/`deploy-harness` cutover event.
**Result:** SUCCESS — release PR #3202 merged (state MERGED, mergeCommit `4a63bfa`); `v3.64` signed-annotated tag valid (SSH ED25519, Good signature) at the merge SHA; zero signing bypasses. Issues close at Stage 13 with per-issue Stage-8 QA evidence (release PR carried no auto-close keywords).
**Outcome:** SUCCESS

#### Deployment Log v3.63
**Files deployed:** Release-manager skill-mode ↔ pipeline-spec reconciliation + GitHub-native PR-review surfaces on milestone `74-controlled-deployment-environment-and-modes` (milestone #164) — 3 issues (#213, #214, #379) as delivery slices on a single branch/PR/merge (release PR #3199, `release/v3.63 → main`, merge-committed at `3d8780ffd2f8f3371c183e94593f9472a22356fa`; provisional **v3.63** — the Stage-4 provisional was **v3.62**, re-versioned **forward** to **v3.63** at the Stage-4/5 boundary when concurrent release issue #431 claimed `release/v3.62` on origin; forward-only renumbering, `release/v3.62` left as #431's active work). Release Class: **routine**. Topology D-C SINGLE (Engineering Commit 0), P0 fully-serial. Sequence #213 → #214 → #379 (skill-core layer; #379 last as the largest and the one reading the most shared files; #214-before-#379 is the load-bearing ordering constraint on the shared `stage-09-plan-review.md`). **#213** — reconcile `release-executor` **Mode A (Execute Release)** with `release/references/pipeline/stage-12-execute.md`: Mode A now names + consumes the **Delivery Strategy** input (merge approach / tag convention / S-2 targets / rollback strategy), documents the git-native execution path (`gh pr merge` → capture merge SHA → signed-annotated tag → S-2 deploy → Phase-B5 chore PR writes the `DEPLOYED` RELEASE_LOG row) delegating merge/tag mechanics to `stage-12-execute.md` Phases A–B, and the stale P3 gap note is reconciled to "Coverage" consistent with `stage-to-skill-mode-mapping.md` row 12 (`release/skills/release-executor/SKILL.md`, `stage-12-execute.md`, `stage-to-skill-mode-mapping.md`). **#214** — reconcile `pmo-release-manager` **Mode 1 (Go/No-Go Evidence, Stage 9)** with `stage-09-plan-review.md` §5 Phase A: Mode 1 now names the **Stage-7 quality-report + Stage-8 acceptance-report** as the A1 upstream-report inputs it synthesizes and cites the §5 Phase A (A1–A5) structure + the A6 Release Readiness Scan spec; the stale `stage-09-plan-review.md` gap note is closed ("resolved"); `stage-to-skill-mode-mapping.md` row 9 distinguishes the Tier-1 evidence-assembly mode from the Tier-3 GAP-BY-DESIGN decision gate; and `pmo-devops-sre/SKILL.md`'s "future `pmo-release-manager` (not built)" framing is flipped to "sibling (built) — trigger-deconflicted" (`release/skills/pmo-release-manager/SKILL.md` + `references/composition-contract-release-manager.md`, `stage-09-plan-review.md`, `stage-to-skill-mode-mapping.md`, `release/skills/pmo-devops-sre/SKILL.md` + `references/composition-contract-devops-sre.md`). **#379** — GitHub-native PR-review surfaces added to the pipeline: **F1** the release PR is created in **draft** at Stage 6 Phase C2 (`gh pr create --draft`) and transitioned to ready-for-review at the Stage-9 gate (new `stage-09-plan-review.md` Phase A8; `stage-12-execute.md` draft-state pre-merge guard); **F2** GitHub PR review-comments are documented as an alternate QA/feedback arrival surface mapped to `[ADJUST]` / `[SCOPE CHANGE]` / `[PLAN REJECTION]` tiers (`stage-08-qa-testing.md` Phase D + `release-process.md` § Inter-Stage Feedback Protocol); **F3** native review-decision states (`APPROVED` / `CHANGES_REQUESTED`) are surfaced as a **NON-GATING advisory** Stage-9 input (new `stage-09-plan-review.md` Phase A9, modeled on the shipped A6.5 version-dimension advisory, never displacing the operator's Tier-3 GO). Each of the three PR-review facets degrades to an honest no-op under the single-operator reviewer convention (`OPERATOR == @me`, `--reviewer` dropped). Three `SKILL.md` bodies changed (`release-executor`, `pmo-release-manager`, `pmo-devops-sre`) → the three `.skill` packages (`packages/release-executor.skill`, `packages/pmo-release-manager.skill`, `packages/pmo-devops-sre.skill` + `.sha256`) were rebuilt inside the release PR — no separate post-merge package rebuild required.
**Mechanism:** git merge (PR #3199 → main, `--merge --admin`) + `v3.63` signed-annotated tag at merge SHA `3d8780f` (`claim-version.sh` atomic ref-CAS; provisional v3.62 re-versioned forward to v3.63 at the Stage-4/5 boundary on the #431 `release/v3.62` collision — v3.62 is a claimed sibling, not a gap).
**Timestamp:** 2026-07-03 UTC
**Cycle-Time:** N/A — governance/skill-doc reconciliation release; the three `.skill` packages shipped inside the release PR, not via a separate `deploy-skill`/`deploy-harness` cutover event.
**Result:** SUCCESS — release PR #3199 merged (state MERGED, mergeCommit `3d8780f`); `v3.63` signed-annotated tag valid at the merge SHA. Issues close at Stage 13 with per-issue Stage-8 QA evidence (release PR carried no auto-close keywords).
**Outcome:** SUCCESS

#### Deployment Log v3.62
**Files deployed:** Immutable ADR system on milestone `31-immutable-adr-system` (milestone #130) — 4 issues (#431, #15, #541, #3200) as delivery slices on a single branch/PR/merge (release PR #3193, `release/v3.62 → main`, merge-committed at `0c4a3f8fc72954bbfa555eebc42fb0798562dae4`; provisional **v3.62** claimed clean at cut — no re-version). Release Class: **novel** (new operator-invocable capability atop foundational ADR governance). Topology D-C SINGLE (Engineering Commit 0). Sequence #431 → #15 → #541; #3200 independent hygiene. **#431** — canonical ADR authoring guide (`core/standards/adr-authoring-guide.md`: when-to-write rubric + copy-paste template + supersession/immutability policy) + dangling ADR-governance reference repoints across `core/disciplines/decision-discipline.md`, `core/standards/initiative-roadmap-framework.md`, `release/ADRs/README.md`, `release/references/pipeline/stage-05-solutioning.md`, and an `.github/ISSUE_TEMPLATE/adr.yml` pointer. **#15** — `adr-helper` skill (`core/skills/adr-helper/SKILL.md` + `references/scaffolding-procedure.md`) that scaffolds `ADR-NNN.md` at decision moments, citing the #431 guide's triggers + template as authority; registered in `core/skills/registry.md` + `deploy.sh` roster; `.skill` package built on-branch (`packages/adr-helper.skill` + `.sha256`). **#541** — ADR-017 + ADR-019 index rows backfilled into `core/ADRs/README.md` (the two shipped ADRs previously missing from the index). **#3200** — 5 residual decision-discipline § 3 ADR-threshold citations repointed to the new guide across `core/disciplines/operating-model.md` + `release/references/standards/solutioning-output-template.md` (outcome grep = 0 residual on the live corpus). One `SKILL.md` body added (`adr-helper`) with its `.skill` package built in the release PR — no separate post-merge package rebuild required.
**Mechanism:** git merge (PR #3193 → main, `--merge`) + `v3.62` signed-annotated tag at merge SHA `0c4a3f8` (ED25519; `claim-version.sh` atomic ref-CAS; dry-run confirmed next-free = v3.62, no collision).
**Timestamp:** 2026-07-03 UTC
**Cycle-Time:** N/A — content-and-skill release; no `deploy-skill`/`deploy-harness` cutover event emitted (the `adr-helper` `.skill` package shipped inside the release PR, not via a separate deploy event).
**Result:** SUCCESS — release PR #3193 merged (state MERGED, mergeCommit `0c4a3f8`); `v3.62` signed-annotated tag valid (ED25519, Good signature on origin) at the merge SHA; zero signing bypasses. Issues close at Stage 13 with per-issue Stage-8 QA evidence (release PR carried no auto-close keywords).
**Outcome:** SUCCESS

#### Deployment Log v3.61
**Files deployed:** PDA (project-document-architecture) tail-closeout — node-graph warehouse capability + tooling on milestone `107-pda-tail-closeout` (milestone #256) — 4 issues (#3081, #1770, #1769, #2424) as delivery slices on a single branch/PR/merge (release PR #3098, `release/v3.55-pda-tail-closeout → main`, merge-committed at `e25eb5147816253f8f9df128241667344aaf2c76`; provisional **v3.55** re-versioned forward to **v3.61** across a heavy concurrent-release window — v3.54 and v3.55 are permanent gaps, claimed by concurrent releases during orchestration). Release Class: **novel**. Topology D-C SINGLE (Engineering Commit 0). Sequence #3081 → #1770 → #1769 (FK chain); #2424 independent. **#3081** — node-frontmatter classify-and-stamp tool (`core/deploy/tools/stamp-node-frontmatter.py`, a stdlib classifier that derives + stamps `domain`/`type`/`lifecycle_state` across the operational corpus; capability + tool only — the corpus write is gated). **#1770** — `relationships[]` edge emission wired at write-time into three skill output-contracts (`file-router`, `ppm-agent`, `tracker-manager` `SKILL.md` edits) + the RAID `source_ref` field (`core/schemas/raid-log.schema.json`) + a `core/deploy/tools/backfill-relationship-edges.py` backfill writer. **#1769** — SQLite index builder (`core/deploy/tools/build-doc-index.py`, materializes the derived warehouse from frontmatter) documented by `core/schemas/sqlite-index-schema.md` + `core/schemas/frontmatter-schema.md` + `core/disciplines/document-ecosystem-design.md`. **#2424** — scheduled-task→Agent spawn feasibility probe — **VERDICT PASS**: per-project orchestration design learnings banked; the subagent-security-posture amendment it surfaced is routed as a #242 follow-up, not this release. The three stdlib node-graph tools + the three skill output-contract edits + the `raid-log.schema.json` `source_ref` addition + the three schema/design docs land on `main` directly; the F2 CI wiring (`.github/workflows/release-tooling-smoke.yml` node-graph-tools-selftest job) rides along. Three `SKILL.md` bodies changed (`file-router`, `ppm-agent`, `tracker-manager`) → `.skill` package rebuild for the three edited skills is the operator/tail post-merge deploy step. The migrated `domain` enum (source/managed/generated) is used consistently across the added surfaces.
**Mechanism:** git merge (PR #3098 → main, `--merge --admin`) + `v3.61` signed-annotated tag at merge SHA `e25eb51`. Provisional v3.55→v3.61 across the concurrent-release window (rule-determined forward-only re-version; v3.54/v3.55 permanent gaps claimed by concurrent releases).
**Timestamp:** 2026-07-02 UTC
**Cycle-Time:** N/A — capability-and-tooling release; the Layer-2 corpus backfill (both corpus-writers) is deferred, so no `deploy-skill`/`deploy-harness` cutover event was emitted for the warehouse materialization.
**Result:** PARTIAL — the release ships the node-graph warehouse *capability + tooling* (classify-and-stamp, edge-emission + backfill, index builder, CI self-test), with both corpus-writers gated behind `--i-am-at-stage-12`. The **Layer-2 corpus backfill (the ~749-file node-stamp + edge-population, D-C7) is DEFERRED** by operator decision at Stage 12 (merge-only): the live warehouse is NOT yet materialized — it is pending a separate backfill go. Do NOT run the backfill tools as part of this close-out.
**Outcome:** PARTIAL
**Outcome rationale:** Layer-2 corpus backfill deferred at Stage 12 (merge-only); capability+tooling shipped, live warehouse pending follow-up #'3123'

#### Deployment Log v3.60
**Files deployed:** Methodology-pack foundation on milestone `86-methodology-pack-foundation` (milestone #228) — 3 issues (#1967, #1968, #1970) as delivery slices on a single branch/PR/merge (release PR #3097, `release/v3.54-methodology-pack-foundation → main`, merge-committed at `7d251f53590e6b90be7c8e01d17a45a117679723`; provisional v3.50 re-versioned forward across a heavy concurrent-release window — v3.50–v3.53 shipped during design, v3.54–v3.59 during Engineering/testing — to **v3.60** at cut). Release Class: **cross-cutting** (Deep Stage-9). Topology D-C SINGLE (Engineering Commit 0). **#1967** — founding methodology-pack ADR (`core/ADRs/ADR-069-methodology-pack-composing-unit.md`, Option 1: a pack is the plug-and-play composing unit at `core/packs/<archetype>/`) + composition-grammar sibling (`core/ADRs/ADR-070-methodology-pack-composition-grammar.md`, widens the meta-schema, extends ADR-018); renumbered 068/069→069/070 after a concurrent ADR-068 collision. **#1968** — type-pack meta-schema widened (`core/schemas/work-item-type-schema.md` §1.1/§1.1.1: `role` discriminator + `extends` delta-inheritance, `[[labels]]` contribution facet, work-status projection, `kinds` relaxed-to-optional; additive, meta-schema stays v1) + `core/packs/{_common,scrum,kanban}/pack.toml` + README scaffolded (grammar-conformant; Kanban WIP set-aggregate gate a documented stub). **#1970** — `core/specs/label-taxonomy.md` restructured grammar-only (`type:*` documented as a namespace pattern) + per-pack rows relocated into pack `[[labels]]` + parity gate (`core/deploy/tools/check-label-parity.py` / `deploy.sh` Check 51) reads the doc+pack union (no orphan) + `type:` registered. No skills deployed (governance/schema/config consumed in-place).
**Mechanism:** git merge (PR #3097 → main, `--merge`) + `v3.60` signed-annotated tag at merge SHA `7d251f5` (`claim-version.sh` atomic ref-CAS). Provisional v3.50→v3.54→v3.60 across the concurrent-release window (rule-determined forward-only re-version; ADRs 067→068/069→069/070 under the same pressure). Stage-6 Engineering + Stage-12 execute run hub-direct after spoke rate-limit deaths; two classifier outages navigated with nothing lost.
**Timestamp:** 2026-07-02 UTC
**Cycle-Time:** N/A — orchestrated via release-hub Mode O (Mode R readiness → plan → scaffold → Stage-5 design + adversarial → Collective-Review scope-lock → Stage-6 build → DevTest/QA → Stage-9 GO → Stage-12 execute).
**Result:** SUCCESS — release PR #3097 merged (27/27 CI green); `v3.60` signed tag at `7d251f5`; published GitHub Release created.
**Outcome:** SUCCESS

#### Deployment Log v3.59
**Files deployed:** Repo-structure & filesystem hygiene on milestone `44-repo-structure-and-filesystem-hygiene` (milestone #117) — 3 issues (#73, #230, #1079) as delivery slices on a single branch/PR/merge (release PR #3011, `release/v3.46-repo-structure-and-filesystem-hygiene → main`, merge-committed at `bb4b461bee97b1e000d9b0bc214947b4840cbd9d`; provisional v3.46 re-versioned forward across a heavy concurrent-release window — v3.46 claimed by intake-governed-file-movement, v3.47–v3.58 shipped since — to **v3.59** at cut). Release Class: **novel** (Standard engagement, Deep Stage-9). Topology D-C SINGLE (Engineering Commit 0). **#73** — `## §7 Git-Tracking-Decision Policy` appended to `core/standards/public-repo-vs-operator-instance-taxonomy.md` (7 per-class criteria + 4 verdict tokens + Layer-1/2 reconciliation + pattern-level audit over 911 tracked files; `packages/*.skill`+`.sha256` = POLICY-AMBIGUOUS→KEEP-TRACKED). **#230** — `release/releases/plans|notes/` foldered into major-version subfolders (~190 history-preserving `git mv`) + recursive `generate_release_index.py`/`lint_release_corpus.py` + reconciled RELEASE_LOG/INDEX/DIGEST path refs. **#1079** — v2-vocabulary reconciliation (SKILL_LIST→per-module arrays) + two-skill bump via pmo-skill-editor Mode A (canary v1.10→v1.11, pmo-skill-refiner v2.03→v2.04) + flat-path doc sweep.
**Mechanism:** git merge (PR #3011 → main, `--merge --admin`) + `v3.59` annotated tag at merge SHA `bb4b461`. Provisional v3.46→v3.49→…→v3.58→v3.59 across the concurrent-release window; each collision a release-plan-filename add/add resolved by the version-bump rename (content version-agnostic). Stage-12 recovered hub-direct after repeated spoke session-limit/rate-limit deaths; the tight reconcile→checkpoint-push→`--admin`-merge sequence won a clean window. Governed gate-fix (commit-message depersonalization filename false-positive, #3015) shipped as PR #3089 to unblock.
**Timestamp:** 2026-07-02 UTC
**Cycle-Time:** N/A — orchestrated via release-hub Mode O (Mode R readiness → plan → scaffold → Stage-5 design → Stage-6 build → DevTest/QA → Stage-9 GO → Stage-12 execute).
**Result:** SUCCESS — release PR #3011 merged (no red checks; corpus-integrity + commit-message gates green post-remediation); `v3.59` annotated tag at `bb4b461`; #73/#230/#1079 auto-closed.
**Outcome:** SUCCESS

#### Deployment Log v3.58
**Files deployed:** Parameterized System-Specialist learning-methodology template + WMS pilot on milestone `09-FRONTIER-system-business-specialists` (milestone #218) — 1 issue (#407) on a single branch/PR/merge (release PR #3099, `release/v3.58-frontier-system-business-specialists → main`, merge-committed at `424ae10bec36e25e776dd05d4f6247029bda4fac`; provisional **v3.53** re-versioned forward to **v3.54** then **v3.58** across a heavy concurrent-release window — v3.53 claimed by milestone-100, v3.54 by milestone-80, v3.55 by an open PR, v3.56/57 shipped out of order). Release Class: **novel** (Standard engagement, Deep Stage-9). Topology D-C SINGLE (Engineering Commit 0). **commit-0** — ADR-019 ratified `Proposed→Accepted` (resolves #2934; INDEPENDENTLY NON-REVERTIBLE — 13 live Specialists depend on it). **#407** — parameterized template `operations/skills/_templates/system-specialist/SKILL.md` (learn-a-system method as SSOT, no per-system facts) + instantiation procedure + WMS pilot instance (`pmo-wms-specialist`, distinct trigger + illustrative corpus) + `deploy.sh` Check 5(a) `_templates` allowlist exemption + registry row. The template is non-deployed (absent from every roster array + registry); the pilot INSTANCE is roster+registry registered.
**Mechanism:** git merge (PR #3099 → main, `--merge --admin`) + `v3.58` signed-annotated tag at merge SHA `424ae10` (ED25519). Provisional v3.53→v3.54→v3.58 across the concurrent-release window; each collision was a release-plan-filename add/add resolved by the version-bump rename (content version-agnostic). `.skill` package for the pilot rebuild is the operator/tail post-merge deploy step.
**Timestamp:** 2026-07-02 21:00 UTC
**Cycle-Time:** N/A — orchestrated via release-hub Mode O (Mode R readiness → plan → scaffold → Stage-5 design → Stage-6 build → DevTest/QA → Stage-9 GO → Stage-12 execute); no pipeline-event-log gate events emitted this run.
**Result:** SUCCESS — release PR #3099 merged (28/28 CI green); `v3.58` signed-annotated tag valid (ED25519, noreply tagger); zero signing bypasses. Stage-6 build recovered hub-direct after a spoke usage-window session-limit; re-versioned twice (v3.54, v3.58) on concurrent collisions.
**Outcome:** SUCCESS — an operator onboarding a system (WMS/ERP/CRM) can stand up a principal-level Specialist from the parameterized template; the WMS pilot validates instantiation end-to-end (AC-4 R1–R5 PASS). Residual: RR-2 (production instances extend the out-of-corpus eval set); the template body is method-pure (AC-1 verified).

#### Deployment Log v3.57
**Files deployed:** Domain-aware impact analysis + git-native-aware release-executor on milestone `80-solutioning-and-engineering-skill-modes` (milestone #165) — 2 issues (#674, #505) as delivery slices on a single branch/PR/merge (release PR #3082, merge-committed at `e2893a05ebdb7dc4ff1d1eaab2f2c112dcdb0216`; provisional **v3.54** re-versioned forward to **v3.57** across a concurrent-release window — the PR title retained its `feat(v3.54):` prefix pre-rename). Release Class: **routine**. Topology D-C SINGLE (Engineering Commit 0). **#505** — `domain-blast-radius.sh` traces change impact via the code import graph (`from x import y` edges) for `domain: software` deliverables, emitting the same schema-v1 surface the design-review checklist consumes; the markdown-tree `blast-radius.sh` default is unchanged and now shares a common emitter library (architecture recorded in **ADR-068**). **#674** — the release-executor Dry-Run-Record halt no longer dead-ends git-native releases (PR diff = dry-run review); Cowork-lineage plans still halt and route to release-planner Mode C, fixed at both the Step-1 input-validation check and the failure-mode entry.
**Mechanism:** git merge (PR #3082 → main) + `v3.57` signed-annotated tag at merge SHA `e2893a0`. Provisional v3.54 → v3.57 across the concurrent-release window (forward-only per bundle-composition-doctrine §5.3; the `feat(v3.54):` PR-title prefix predates the re-version).
**Timestamp:** 2026-07-02 23:03 UTC (GitHub Release published 2026-07-02T23:03:41Z; PR #3082 merged 2026-07-02T20:35:11Z)
**Cycle-Time:** N/A — no gate-outcome/plan-review-go or deploy event recorded for v3.57 in the pipeline event log (mechanism: compute-cycle-time.sh).
**Result:** SUCCESS — release PR #3082 merged (state MERGED, mergeCommit `e2893a0`); `v3.57` signed tag + GitHub Release published; #674/#505 closed with ship evidence.
**Outcome:** SUCCESS — backfilled at Stage 13 of `close-out-hardening-wave-2` (#3215). The release itself shipped correctly (notes file, signed tag, GitHub Release, milestone closed 2026-07-02); only the four corpus ledgers were missing, and this row plus the INDEX/DIGEST/CHANGELOG entries restore the complete Stage-13 output set.

#### Deployment Log v3.56
**Files deployed:** Stage-3 bundling composer + version-identity on milestone `81-stage3-bundling-composer-and-identity` (milestone #166) — 4 issues (#25, #30, #52, #415) as delivery slices on a single branch/PR/merge (release PR #3096, `release/81-stage3-bundling-composer-and-identity → main`, merge-committed at `328069f1bfbfcc6563b0b2c2e33b2f7d41e85cc0`; version-agnostic branch, provisional v3.51 published by a concurrent release mid-run → claimed **v3.56** at cut, skipping the live in-flight v3.54/v3.55 branch-claims). Release Class: **novel** (Standard engagement). Topology D-C SINGLE (Engineering Commit 0); P0 fully-serial build order #25 → #30 → #52 → #415; 22 pts. **#25** — ADR-019 Mode A/B co-location decision-record in `release-planner/SKILL.md` (records that the Stage-3-Bundle ↔ Stage-4-Planning separation is delivered via persona-split + doctrine, not a skill fission). **#30** — `deploy.sh` Check 53 approved-queue-depth monitor (warn-mode) + `check-approved-queue-depth.py` primitive + the stale `--check-lifecycle` registry repair (dormant Checks 11 & 30 preserved). **#52** — `compute-milestone-positions.py` (Kahn's-BFS milestone-position derivation, `--dry-run`/`--apply`, idempotent, self-test) + `deploy.sh` Check 52 (position-drift, warn-mode) — producer for the live-but-dormant G3-07 override tier. **#415** — `version-less` release-identity mode codified in `stage-03-bundle.md` §4/§7 + `bundle-composition-doctrine.md` §5/§3, reconciled to the live `version_canonical` gate; core scope, enforcement fissioned to #3016 (open). One `SKILL.md` body changed (`release-planner`) → `.skill` package rebuild is the operator/tail post-merge deploy step.
**Mechanism:** git merge (PR #3096 → main, `--merge --admin`) + `v3.56` signed-annotated tag at merge SHA `328069f` (version-agnostic branch; provisional re-anchored forward to next-free v3.56 to skip the live in-flight v3.54/v3.55 claims).
**Timestamp:** 2026-07-02 18:53 UTC
**Cycle-Time:** N/A — orchestrated via release-hub Mode O (readiness fix → plan → scaffold → Stage-5 design/adversarial-review/scope-lock → build); no pipeline-event-log gate events emitted this run.
**Result:** SUCCESS — release PR #3096 merged (28/28 CI green); `v3.56` signed-annotated tag valid (ED25519); zero signing bypasses. Stage-6 card #52 was recovered hub-direct after a spoke usage-window session-limit.
**Outcome:** ALIGNED with the Release Outcome Statement — Stage 3 Bundling operates as a self-contained composer: queue-depth trigger (#30), automated milestone-position derivation (#52), a distinct bundle-composer identity record (#25), and a first-class version-less release-identity mode (#415). Residual: #3016 (version-identity enforcement/mirror surfaces) open; #52 priority reporting is label-derived pending a Projects-field read.

#### Deployment Log v3.53
**Files deployed:** Canonical ADR frontmatter schema + consumer alignment on milestone `100-adr-and-frontmatter-schema` (milestone #243) — 5 issues (#2156, #2155, #2157, #2068, #1487) as delivery slices on a single branch/PR/merge (release PR #3049, `release/v3.51-100-adr-and-frontmatter-schema → main`, merge-committed at `c0e8b44ce416a23ed0a9d23f66b81c442022296d`; provisional **v3.51** re-versioned forward to **v3.53** on concurrent-release collisions — v3.51 claimed by milestone-106 and v3.52 by milestone-83 during orchestration). Release Class: **routine** (ADR corpus + frontmatter-schema + durability hook + one CI-gate carve-out; no new component). Topology D-C SINGLE (Engineering Commit 0), build order W1 #2156 → W2 #2155/#2157 → W3 #2068 → W4 #1487. **#2156** — NEW canonical `core/schemas/adr-schema.md` (7-field frontmatter + 6 body-section data contract) that both ADR READMEs cite instead of restating, resolving the #2155 item-2 dangling pointer by construction. **#2155** — `frontmatter-schema.md` category count six→seven. **#2157** — human-readable `domain` values (source/managed/generated) via forward-only alias across 7 consumers (sqlite `CHECK` union) + minimal-inline `generated_by` keep-decision (no ADR). **#2068** — durability issue-ref classifier excludes hex-color prefixes via a companion `HEXCOLOR_RE` mask in `positional-issueref.awk` (`ISSUEREF_RE` byte-unchanged across 3 surfaces) + allowlist stopgap removed. **#1487** — scoped depersonalization carve-out permitting the operator literal name only on an ADR `deciders:` line (`.github/workflows/repo-integrity.yml` + `depersonalization-spec.md` + `git-workflow.md`). No `SKILL.md` bodies changed → no skill-copy deploy required this session.
**Mechanism:** git merge (PR #3049 → main, `--merge`) + `v3.53` annotated tag at the merge SHA `c0e8b44` (provisional v3.51 taken by concurrent releases → re-versioned forward to next-free v3.53; the `v3.51_RELEASE_PLAN.md` add/add path collision was resolved by the re-version rename). Deploy is git-native for the tracked corpus — all 21 files land on `main` directly.
**Timestamp:** 2026-07-02 15:15 UTC
**Result:** SUCCESS — release PR #3049 merged (state MERGED, mergeCommit `c0e8b44`); v3.53 annotated tag created + GitHub Release published; two accepted monitoring residuals recorded (#2068 colon-abutted-pure-digit false-negative, corpus grep = 0; #1487 `deciders:`-prefix-in-ADR-body edge, non-real). Issues close at Stage 13.
**Outcome:** SUCCESS

#### Deployment Log v3.52
**Files deployed:** Completes the eval framework on milestone `83-eval-framework-completion` (milestone #205) — 4 issues (#199, #209, #279, #367) as delivery slices on a single branch/PR/merge (release PR #3060, `release/v3.52-83-eval-framework-completion → main`, merge-committed at `6a647c6493b68688d8ee3fcb7dbbf0505b01c699`). Release Class: **novel** (new eval-asset files + release-specific D-class decisions; Stage 9 depth Deep, Stage 5 activation ALL, 30-day outcome-window per the Release Outcome Statement). 16 files land git-native on `main`: **#279** — new Stage-3 Bundle eval set `core/skills/eval-writer/evals/stage-gates/stage-03-bundle/evals.json` (+ the stage-gates `README.md` and a Stage-07 eval set) with escape-rate metric, runnable in the resolved eval home codified by **ADR-067** (`core/ADRs/ADR-067-stage-gate-eval-set-home.md`). **#199** — pmo-skill-refiner eval-framework extensions: `assert_branch_fresh.py` (branch-freshness assertion), `improve_description.py` regex handling for `phrases like` description formats, and `run_eval_audit.py` auditing both `Triggers:` and `phrases like` formats (calibration-data aggregation dated-deferred per the card's AC, tracked in the stage-gate eval index). **#367** — hermetic unit tests for the preserved refiner eval scripts (`run_eval.py`, `run_loop.py`, `run_eval_audit.py`) via new `test_eval_scripts.py`; CI-wiring follow-up filed as a separate issue (deferred). **#209** — chain-aware QA integration-testing layer for dependent issues: integration ACs at Solutioning (`stage-05-solutioning.md`, `solutioning-output-template.md`) + chain-aware Plan Review (`stage-09-plan-review.md`) + `stage-07-dev-testing.md` + `hub-spoke-bridge.md` updates (Option-A activation). No `SKILL.md` bodies changed → no S-2 skill-copy deploy required this session; Stage 13 may run `./deploy.sh --check` for drift detection.
**Mechanism:** git merge (PR #3060 → main, `--merge`) + `v3.52` signed-annotated tag (ED25519, "Good signature" verified on origin; annotated tag object type; zero signing bypasses) at the merge SHA `6a647c6` via `claim-version.sh` (atomic ref-CAS; `--bump minor` off anchor v3.51; dry-run confirmed next-free = v3.52, no collision). Deploy is git-native for the tracked corpus — the eval-asset, script, and pipeline-doc edits land on `main` directly.
**Timestamp:** 2026-07-02 04:42 UTC
**Cycle-Time:** N/A — `compute-cycle-time.sh v3.52` returns N/A (no gate-outcome/plan-review-go event and no deploy-skill/deploy-harness event recorded for v3.52 in the pipeline event log; mechanism: compute-cycle-time.sh).
**Result:** SUCCESS — release PR #3060 merged (state MERGED, mergeCommit non-null); v3.52 tag created signed-annotated (annotated object type, ED25519 signature Good on origin); zero signing bypasses used; cards #199/#209/#279/#367 auto-closed via PR closing references.
**Outcome:** SUCCESS

#### Deployment Log v3.51
**Files deployed:** Routine tail-closeout on milestone `106-skill-suite-tail-closeout` (milestone #255) — 2 issues as delivery slices, single branch/PR/merge (release PR #3013, `release/v3.51-skill-suite-tail-closeout → main`, merge-committed at `1139a062d22c369e229541932c31c45321e95a06`; provisional **v3.49** re-versioned to **v3.51** at the tag on concurrent-release collisions — v3.49 published and v3.50 shipped during orchestration — forward-only per bundle-composition-doctrine §5.3). Release Class: **routine** (two additive documentation cross-references; no new capability, no schema/behavior change). Topology **D-C SINGLE** (Engineering Commit 0), Parallelism Posture **P0 fully-serial**; build order #931 → #2208. **#931** — `raid-templates.md` §9 Anti-Patterns (Stale-RAID row) generic `>30d = auto-escalate` replaced with the canonical type-split summary (issues >14d warn / >30d escalate; risks >30d warn / >60d escalate) + cite the doc-of-record `ppm-agent/references/escalation-thresholds.md` §3, resolving the RAID-age divergence surfaced during v1.18. **#2208** — `pmo-tier-2-support/SKILL.md` gains a `## Reference docs` section citing `core/disciplines/root-cause-analysis.md` (its RCA method) + `core/standards/domain-best-practices/support.md` (the now-shipped support best-practice guide); `support.md`'s `consumers:` already listed tier-2 so no edit there; the `pmo-tier-2-support.skill` package rebuilt (Check-7 content-hash verified). Closes the last tail of skill-function-hardening (#1105) and skill-bp-anchoring (#2164).
**Mechanism:** git merge (PR #3013 → main, `--merge`) + `v3.51` signed-annotated tag (ED25519, Good signature verified on origin) at the merge SHA `1139a06` via `claim-version.sh` (atomic ref-CAS; provisional v3.49 was taken by concurrent releases, so the claim re-versioned forward to the next-free minor v3.51). Deploy is git-native for the tracked corpus — the `raid-templates.md` + `pmo-tier-2-support/SKILL.md` edits land on `main` directly; the `pmo-tier-2-support.skill` package + `.sha256` rebuilt via `build-skill-packages.sh` (content-hash `d398a2c3…`).
**Timestamp:** 2026-07-02 UTC
**Cycle-Time:** N/A — `compute-cycle-time.sh v3.51` returns N/A (no gate-outcome/plan-review-go event and no deploy-skill/deploy-harness event recorded for v3.51 in the pipeline event log; mechanism: compute-cycle-time.sh).
**Result:** SUCCESS
**Outcome:** SUCCESS

#### Deployment Log v3.50
**Files deployed:** Completes the platform's stage-gate criteria surface on milestone `68-stage-gate-criteria-completeness` (milestone #155) — 7 issues as delivery slices, single branch/PR/merge (release PR #2928, `release/68-stage-gate-criteria-completeness → main`, merge-committed at `d5e0883ed20751e0f242a0d3b920d6c1b7f9a4d5`; provisional **v3.49** re-versioned to **v3.50** at the tag on a concurrent-release collision — `08-INTAKE-governed-file-movement` claimed v3.49 first — forward-only per bundle-composition-doctrine §5.3). Release Class: **cross-cutting** (authors/edits across three foundation schemas — `gate-criteria-spec.md`, `gate-evaluation-spec.md`, `stage-io-contracts.md` — plus ≥3 pipeline shards; class_weight 1.3, effective 33 pts, operator size-target override 2026-07-01, scope LOCKED at 7). Topology **D-C SINGLE** (Engineering Commit 0), Parallelism Posture **P0 fully-serial**; build order #28 → #96 → #118 → #29 → #501 → #889 → #119 (layer-ordered foundation→consumers; #119's shared-table refactor landed last). **#28** — G4 Plan Readiness (G4-01..05) + G6 Engineering Completeness (G6-01..05) discrete gate IDs, each with a matching Self-Repair block + Inheritance row (warn-mode); `gate-criteria-spec.md` schema v1.21→v1.22; G4 bound into `stage-04-planning.md` §7, G6 into `stage-06-engineering.md` §7. **#96** — G1-04 criterion made bidirectional (under- AND over-specification scorable from the criterion text alone), reaching the already-shipped two-branch self-repair; schema v1.22→v1.23. **#118** — anchored the existing §7 prose judgment dimensions of Stage 9/12/13 to their structural criteria (G-PR\*/G-EX\*/G-CL\*) via a new Per-Boundary Judgment-Dimension Anchors subsection in `gate-evaluation-spec.md` (schema v1.1→v1.2); prose Path-B convention (no J-\* IDs). **#29** — authored the 5 remaining Stage-I/O boundary contracts (2→3, 6→7, 7→8, 8→9, 9→12) in `stage-io-contracts.md` per the 7-field schema, pruning Future Boundaries to a header + append-note. **#501** — composite Stage-5 structural-premise review gate: §3.2 Structural-Premise-Review Obligation (rider on T3), §7.2 SR-G1..SR-G4 gate-blocking criteria, Stage 5→6 Structure-review determination row + Validation Rule 4, blocking checklist check 4.8 (`planning-solutioning-handoff.md`, `stage-05-solutioning.md`, `stage-io-contracts.md`, `design-review-checklist.md`). **#889** — new `core/disciplines/ticket-architecture-reconciliation.md` discipline (when-fires + 4-step procedure + ticket-age staleness signal + composition map + 3 failure modes), filling #501's two reserved seam hooks + adding the disciplines README row, process.md index row, Stage 4/5 bindings, and a build-philosophy BP-process cite. **#119** — added a Validation method column to every gate-criteria table (Shape-1 5→6, Shape-2 8→9) + a 6th Schema field row, lifting inline CLI out of ~24 criterion cells verbatim; no ID renumbered (78 unique IDs pre/post); schema v1.23→**v2.0** (BREAKING major — column addition per the spec's own rule; consumer-inert). **Sibling reconciliation:** the branch was rebased onto `main` after the v3.48 release merged mid-build (v3.48 added G3-18 to `gate-criteria-spec.md`); the version chain on that file re-sequenced v1.20 → v1.21 (G3-18, v3.48) → v1.22 (#28) → v1.23 (#96) → v2.0 (#119), and #119's column migration was extended to give G3-18 its Validation-method cell — no v3.48 content lost.
**Mechanism:** git merge (PR #2928 → main, `--merge`) + `v3.50` signed-annotated tag (ED25519, Good signature verified on origin) at the merge SHA `d5e0883` via `claim-version.sh` (atomic ref-CAS; provisional v3.49 was taken by a concurrent release, so the claim re-versioned forward to the next-free minor v3.50). Deploy is git-native for the tracked corpus — the `gate-criteria-spec.md` (v2.0), `gate-evaluation-spec.md` (v1.2), `stage-io-contracts.md` edits, the new `ticket-architecture-reconciliation.md` discipline, the Stage 04/05/06/09/12/13 shard bindings, and the disciplines/process/build-philosophy index edits all land on `main` directly. No skill `.skill` package rebuild in scope (schema/pipeline/discipline authoring; no operations-module skill body changed).
**Timestamp:** 2026-07-02 UTC
**Cycle-Time:** N/A — `compute-cycle-time.sh v3.50` returns N/A (no gate-outcome/plan-review-go event and no deploy-skill/deploy-harness event recorded for v3.50 in the pipeline event log; mechanism: compute-cycle-time.sh).
**Result:** SUCCESS
**Outcome:** SUCCESS

#### Deployment Log v3.49
**Files deployed:** 08-INTAKE-governed-file-movement (milestone #211) — one capability slice, single branch/PR/merge (release PR #2861, `release/v3.46-intake-governed-file-movement → main`, merge-committed at `ee649ad3f42719860b5a5f565672e3c8590283cc`; provisional v3.46 re-versioned to **v3.49** at the tag on a concurrent-release collision, forward-only per bundle-composition-doctrine §5.3 — v3.46/v3.47-relative gaps benign). Release Class: **routine** — three EXTEND-mode changes to existing operations-module skills, no new skill. Topology **D-C SINGLE** (Engineering Commit 0), Parallelism Posture **P0 fully-serial**; build order #240 → #565 → #289 (contention on `intake-desk/SKILL.md` serialized #565 before #289). **#240** — `file-router` extended from inbound-only to all four movement directions (inbound / generated-staging / promotion / cross-project); resolves the target + cites artifact-generator's existing PROMOTE/REVISE/REJECT gate (owns a new gate only for cross-project-out), two gate-types (confidence for the inbound family, flat-approval for Domain-C). **#565** — `intake-desk` Phase-2 container-altitude existing-owner scan (hard-gate, containers only) + FM #7 reconciled in place (stays 9 FMs, no 10th). **#289** — `intake-desk` **Mode C — Ambient Auto-Log** (non-interactive detect→create governed by the `automation_level` ceiling, with an honest no-hook-backstop safety framing and the per-item→standing weakening stated, not softened; 9→12 FMs); the `core/CLAUDE.md.template` auto-logging rule rewired to invoke Mode C. `.skill` packages for intake-desk + file-router rebuilt.
**Mechanism:** git merge (PR #2861 → main, `--merge`, admin — branch protection `enforce_admins:false`/`required_reviews:0`, all 27 checks green) + `v3.49` signed-annotated tag at merge SHA `ee649ad` via `claim-version.sh` (atomic ref-CAS, anchor = published v3.48, claimed cleanly). Deploy is git-native for the tracked corpus — both SKILL.md sources + references + `core/CLAUDE.md.template` + the rebuilt `.skill` packages land on `main`; the S-2 skill deploy pushes intake-desk + file-router to the operator instance's `~/.claude/skills`.
**Timestamp:** 2026-07-02 UTC
**Cycle-Time:** N/A — `compute-cycle-time.sh v3.49` returns N/A (no gate-outcome/plan-review-go event and no deploy-skill/deploy-harness event recorded for v3.49 in the pipeline event log; mechanism: compute-cycle-time.sh).
**Result:** SUCCESS
**Outcome:** SUCCESS

#### Deployment Log v3.48
**Files deployed:** Knowledge-corpus tail close-out on milestone `105-knowledge-corpus-tail-closeout` (3 cards, single branch/PR/merge — the residual tail of two sibling Knowledge-Architecture epics: KA-Standards #1171 → #930, KA-Discipline #1174 → #1087 + #1093) — release PR #2918 (`release/v3.48-knowledge-corpus-tail-closeout → main`) merge-committed at `dd9c2843ab80e08740c3f77032b54b02cd7cb039`. Release Class: **novel** — adds 2 net-new ADR files (ADR-065 health-RAG band canonical home; ADR-066 design-gate `delivery_approach` conditioning), each rendering a D-class design decision with a Stage-5 ADR, firing all `novel` triggers. Topology **D-C SINGLE** (Engineering Commit 0), Parallelism Posture **P0 fully-serial** — the three cards are independent (disjoint file sets, no cross-dependency) but serialize as commits under SINGLE-branch topology; build order #930 → #1087 → #1093. **#930** — recorded the health-RAG band canonical home: authored `core/ADRs/ADR-065-health-rag-band-canonical-home.md` (channel-formats.md is the single-source owner of the health-RAG bands + watermelon concept; other surfaces cite it, never restate the band thresholds) + single-sourced the watermelon concept by adding a see-also cross-reference in `operations/skills/comms-writer/references/channel-formats.md` and `operations/skills/ppm-agent/references/evidence-quality.md` pointing at the canonical owner (band thresholds unchanged — reference-only edits, no threshold values moved). **#1087** — conditioned the design-before-slicing gate on `delivery_approach`: registered criterion **G3-18** (Archetype-appropriate design invocation conditioned on `delivery_approach`) in `core/schemas/gate-criteria-spec.md` (1 `field`/`judgment`/`recommend` criterion row + 1 self-repair row, section-local to § Gate 3; schema bump **v1.20 → v1.21**, non-breaking minor/additive, no ID renumber) following the live G3-05 `deliverable_type` conditioning precedent — CASE 1 (Scrum/XP/Lean → design-first before slicing; Waterfall/PRINCE2 → phase-gate; Kanban → continuous, no sprint-slice gate), CASE 3/absent/`Custom` → base behavior UNCHANGED via an explicit no-op default (never a silent default-fire) — and authored `release/ADRs/ADR-066-design-gate-delivery-approach-conditioning.md` recording the decision. **#1093** — codified the notes-non-authoritative rule + destructive-action guardrail as a Universal Preferences bullet in `core/CLAUDE.md.template` (operator notes are directional inputs not authoritative specs; differences vs corpus render as non-authoritative observations; an agent MUST NOT perform a gross deletion / mass-restructure based on simple notes alone without reconciling against the SSOT and confirming with the operator; canonical-corpus findings are unaffected). All three cards ship as one coherent capability slice → one milestone branch / one PR / one merge.
**Mechanism:** git merge (PR #2918 → main, `--merge`) + `v3.48` signed-annotated tag at the merge SHA `dd9c284` via `claim-version.sh` (SSH/ED25519 signature valid, zero signing bypasses; atomic ref-CAS, anchor = published v3.47, no collision — the branch was rebased onto `origin/main` pre-merge to inherit ADR-064 and clear the ADR-number contiguity gate). Deploy is git-native for the tracked corpus — ADR-065, ADR-066, the `gate-criteria-spec.md` G3-18 row + v1.21 bump, the `core/CLAUDE.md.template` Universal Preferences bullet, and the two `references/*.md` see-also cross-references all land on `main` directly; the S-2 skill deploy (`./deploy.sh --deploy comms-writer ppm-agent`) pushes the two edited skills (watermelon see-also references) to the operator instance's `~/.claude/skills`.
**Timestamp:** 2026-07-02 UTC
**Cycle-Time:** N/A — `compute-cycle-time.sh v3.48` returns N/A (no `gate-outcome/plan-review-go` event and no `deployment-status/deploy-skill` / `deploy-harness` event recorded for v3.48 in the pipeline event log; mechanism: compute-cycle-time.sh).
**Velocity:** N/A — `compute-release-velocity.sh v3.48 --milestone 254 --merge-sha dd9c284` returns N/A (no `size:*` labels on the milestone membership, so points cannot be derived); files-changed 7 recorded; Release Class **novel** (excluded from the calibration ratio).
**Result:** SUCCESS — release PR #2918 merged with green CI (26/26 checks PASS after the pre-merge rebase); `v3.48` tag created at the merge SHA `dd9c284` (signed-annotated, ED25519, verified on origin). ADR-065, ADR-066, the G3-18 criterion row + self-repair row (gate-criteria-spec v1.21), the `core/CLAUDE.md.template` notes-non-authoritative Universal Preferences bullet, and the two watermelon see-also cross-references (`comms-writer/references/channel-formats.md`, `ppm-agent/references/evidence-quality.md`) are all verified present on `main`. The release PR's auto-close parser did not fire (empty `closingIssuesReferences`), so the 3 cards (#930, #1087, #1093) are manually closed with per-card verification evidence at Stage 13 close-out (D-1 auto-close-anomaly path).
**Outcome:** SUCCESS

#### Deployment Log v3.47
**Files deployed:** Operational-tracker + comms + session-config interface hardening on milestone `23-tracker-comms-session-config` (5 cards, single branch/PR/merge under Epic #1154 PDA-Interface) — release PR #2860 (`release/v3.47-tracker-comms-session-config → main`) merge-committed at `1ac0ed1d3d4047bf989ffc367942774932e52346`. Release Class: **novel** — introduces a first-of-kind K1 dual-format document-model standard (`core/standards/dual-format-document-model.md`) + a net-new ADR (ADR-064) carrying a D-Design decision (executor selection: artifact-generator vs comms-writer vs both, resolved artifact-generator), and edits five deployed skills. Topology **D-C SINGLE** (Engineering Commit 0), Parallelism Posture **P0 fully-serial** — one hard internal edge (#234→#233 on `core/schemas/tracker-schemas.md`) plus a #247→#241 contention pair on `core/CLAUDE.md.template`; the other cards are independent but serialize as commits under SINGLE-branch topology; build order #234 → #247 → #241 → #239 → #233. **#234** — authored `core/standards/dual-format-document-model.md` (K1 standard: a source-definition block + a per-target translation-map + a version/drift-tracking rule keyed on the Artifact Register) + `core/ADRs/ADR-064-dual-format-document-model.md` (governing model + artifact-generator executor selection, rejecting comms-writer / both), cited the RAID-Confluence dual-format handling as proof-of-concept in `core/schemas/tracker-schemas.md`, bound the model to the Artifact entity §4/9 reconciliation seam in `core/disciplines/project-entity-model.md` (no field-list change), and added the dual-format render mode + orphan-guard failure mode to `operations/skills/artifact-generator/SKILL.md` (v2.28→v2.29). **#247** — added the SESSION_STATE per-project rotation-focus field to `core/CLAUDE.md.template` § Session Management: rotation focus stored as a `focus_since` anchor date, weeks-in-focus **derived** at a week boundary (never per-session), honoring single-record-overwrite + no-shadow-SSOT. **#241** — moved CORRECTIONS to project scope with a cross-project pattern detector: `core/CLAUDE.md.template` (Tier 1.7 two-scope row — program file = platform-wide, `[Project]/CORRECTIONS.md` = project-scoped — plus the Session-Management project-scoped read and the Governance File Map, RETAIN-NARROWED) + `operations/skills/weekly-status-rollup/SKILL.md` (a cross-project correction-recurrence detector firing at N≥2 distinct projects, routing into the existing intake path, with a single-project no-fire failure mode; v2.20→v2.21). **#239** — added communication escalation for unresponsive actionable comms: `operations/skills/comms-writer/SKILL.md` (2-day auto-draft on existing Tracker-2 fields; v2.28→v2.29) + `operations/skills/daily-status/SKILL.md` (5-day SIOR escalation as a PMO-internal prep-note preserving the Reversibility-Scope opt-out; v2.31→v2.32) — behavior-only, no new tracker columns, informational comms exempt. **#233** — made the RAID Log an agent-native surface while retaining CSV (Option B): `core/schemas/tracker-schemas.md` Tracker 5 keeps `Format: CSV` and gains an agent-native-surface annotation + a cross-ref to the #234 model (14-column table byte-unchanged) + `operations/skills/tracker-manager/SKILL.md` (native read/write on the agent path, export via the #234 translation-map; v2.23→v2.24); `raid-log.schema.json` is unchanged (EAD derivation + entity authority preserved). All five cards ship as one coherent capability slice → one milestone branch / one PR / one merge.
**Mechanism:** git merge (PR #2860 → main, `--merge`) + `v3.47` signed-annotated tag at the merge SHA `1ac0ed1` via `claim-version.sh` (SSH/ED25519 signature valid, zero signing bypasses; atomic ref-CAS, anchor = published v3.45, provisional v3.45 re-provisioned to v3.47 at the Stage-6 version gate — v3.46 claimed by an in-flight release — no collision at claim). Deploy is git-native for the tracked corpus — the new standard + ADR, the `CLAUDE.md.template` edits, the `tracker-schemas.md` / `project-entity-model.md` edits, and the five skill SKILL.md sources all land on `main` directly; the S-2 skill deploy (`./deploy.sh --deploy artifact-generator comms-writer daily-status tracker-manager weekly-status-rollup`) pushes the five edited skills to the operator instance's `~/.claude/skills`.
**Timestamp:** 2026-07-02 UTC
**Cycle-Time:** N/A — `compute-cycle-time.sh v3.47` returns N/A (no `gate-outcome/plan-review-go` event and no `deployment-status/deploy-skill` / `deploy-harness` event recorded for v3.47 in the pipeline event log; mechanism: compute-cycle-time.sh).
**Result:** SUCCESS — release PR #2860 merged with green CI; `v3.47` tag created at the merge SHA `1ac0ed1` (signed-annotated, ED25519, verified on origin). The net-new `core/standards/dual-format-document-model.md` standard, ADR-064, the two `core/CLAUDE.md.template` Session-Management edits (#247 rotation field + #241 corrections-scope), the `weekly-status-rollup` cross-project detector, the #239 comms-escalation edits (comms-writer 2-day draft + daily-status 5-day SIOR), and the #233 RAID agent-native surface (tracker-schemas Tracker-5 annotation + tracker-manager native path) are all verified present on `main`; `raid-log.schema.json` and the tracker-schemas 14-column table are byte-unchanged as designed. The release PR's auto-close parser did not fire (empty `closingIssuesReferences`), so the 5 cards (#234, #247, #241, #239, #233) are manually closed with per-card verification evidence at Stage 13 close-out (D-1 auto-close-anomaly path).
**Outcome:** SUCCESS

#### Deployment Log v3.45
**Files deployed:** Pipeline stage-purpose contract + G-PR8 Gate-9 registry row on milestone `42-pipeline-skill-doc-reconciliation` (2 cards, single branch/PR/merge — the residual live slice of the pipeline-doc-reconciliation epic whose sibling members #306/#500/#502 already shipped) — release PR #2851 (`release/v3.45-pipeline-skill-doc-reconciliation → main`) squash-merged at `1986f0d02246eec447e5ba3f1b302627181d3ada`. Release Class: **routine** — triggers (c) zero new files (both cards EDIT existing files) + (d) zero new D-class decisions both fire; the `cross-cutting` trigger (a) technically fires on #499's ≥3 `pipeline/stage-*.md` touches, but the substance is a single coherent wording-clarification rippled across sibling stage docs (one contract expressed in N places, no gate-behavior change), operator-resolved to `routine` per the cross-cutting anti-pattern. Topology **D-C SINGLE** (Engineering Commit 0), Parallelism Posture **P0 fully-serial** — the two cards' file sets are disjoint (`core/schemas/*` vs `release/references/pipeline/*`), so order is contention-convenience not dependency; build order #887 → #499. **#887** — registered **G-PR8** (Stage 9 Phase A6.5 mid-pipeline-divergence, File dimension — HALT-eligible) in `core/schemas/gate-criteria-spec.md`: 1 criteria row (`validation` / `structural` / `auto`; verdicts CLEAN / DIVERGED-RELEASE-FILES-UNTOUCHED / DIVERGED-RELEASE-FILES-TOUCHED) + 1 Self-Repair row, filling the pre-existing G-PR1..G-PR9 registry hole the G-PR9 addition left open (max ID already G-PR9 — no new max); schema bump v1.19 → v1.20 (non-breaking minor, additive, no ID renumber); authored by reading the live `stage-09-plan-review.md § Phase A6.5` prose (already on main via #306) as source-of-truth so the row and the relocated prose stay consistent. **#499** — made the pipeline stage-purpose contract explicit: added the four-part `map/design/build/validate` contract to `release/references/pipeline/README.md` + clarified the §1 Purpose line in `stage-01-intake.md` / `stage-04-planning.md` (map current state / surface gaps), `stage-05-solutioning.md` (design), `stage-06-engineering.md` (build/tweak), `stage-08-qa-testing.md` (validate) — wording clarification not replacement (each edited §1 line preserves its existing stage-specific detail per AC#4). Both cards ship as one coherent capability slice → one milestone branch / one PR / one squash-merge.
**Mechanism:** git merge (PR #2851 → main, `--squash`) + `v3.45` signed-annotated tag at the merge SHA `1986f0d` via `claim-version.sh` (SSH/ED25519 signature valid, zero signing bypasses; atomic ref-CAS, anchor = published v3.44, no collision). Deploy is git-native for the tracked corpus — the `gate-criteria-spec.md` schema row + the `pipeline/*.md` stage-doc edits land on `main` directly; no `.skill` package and no S-2 skill deploy (the changed surfaces are a governance schema + pipeline reference docs, not a deployed skill).
**Timestamp:** 2026-07-01 UTC
**Cycle-Time:** N/A — `compute-cycle-time.sh v3.45` returns N/A (no `gate-outcome/plan-review-go` event and no `deployment-status/deploy-skill` / `deploy-harness` event recorded for v3.45 in the pipeline event log; mechanism: compute-cycle-time.sh).
**Result:** SUCCESS — release PR #2851 squash-merged with green CI; `v3.45` tag created at the merge SHA `1986f0d` (signed-annotated, ED25519, verified on origin). The G-PR8 criteria row + Self-Repair row (schema v1.20), the README four-part stage-purpose contract, and the five stage-doc §1 Purpose clarifications are all verified present on `main`. The release PR's auto-close parser fired cleanly (`closingIssuesReferences` = #499, #887), so both cards auto-closed COMPLETED at merge — no D-1 manual-close anomaly this release.
**Outcome:** SUCCESS

#### Deployment Log v3.44
**Files deployed:** Stage-2 triage-analysis skill + auto-execute default on milestone `78-pipeline-triage-automation` (2 cards, single branch/PR/merge under epic #1188 Pipeline Triage & Bundling) — release PR #2801 (`release/v3.44-pipeline-triage-skill → main`) merge-committed at `adf41b10a76cce7223d5ae235fc0896642d85974`. Release Class: **novel** — introduces a first-of-kind Stage-2 `pipeline-triage` skill (new invokable execution surface + new registry CI row + new `.skill` package) and carries a D-Design decision (host selection: standalone-vs-extend `delivery-engine`/`release-planner`, resolved standalone on #2752, recorded in ADR-063). Topology **D-C SINGLE** (Engineering Commit 0), Parallelism Posture **P0 fully-serial** — a hard #286→#282 dependency edge on the single shared file `release/skills/pipeline-triage/SKILL.md`. **#286** — authored `release/skills/pipeline-triage/SKILL.md` to the pmo-skill-refiner standard (7 PMO injection fields; ≥3 domain-specific failure modes) executing the Stage-2 A1–A6 sequence by citing `stage-02-triage.md` §5 (no re-authored phase definitions) + `release/skills/pipeline-triage/references/triage-execution.md`; registered all four surfaces in one commit — the CI row in `core/skills/registry.md`, the output-contract in `core/schemas/per-skill-output-contracts.md`, the `pipeline-triage` member in the `RELEASE_SKILLS` array in `core/deploy/deploy.sh`, and the `packages/pipeline-triage.skill` package (+ `.sha256` sidecar, deterministic `build-skill-packages.sh`); updated the `stage-to-skill-mode-mapping.md` G2 row GAP → coverage; authored `core/ADRs/ADR-063-standalone-triage-skill.md`. **#282** — bound auto-execute as the operative default + the Close/Reject confirmation gate onto the skill body (scope-locked to execution-binding only; the `stage-02-triage.md` §8 auto-execute definition STANDS, referenced not rewritten) + `release/governance/release-process.md`. Both cards ship as one coherent capability slice → one milestone branch / one PR / one merge.
**Mechanism:** git merge (PR #2801 → main, `--merge`) + `v3.44` signed-annotated tag at the merge SHA `adf41b1` via `claim-version.sh` (SSH/ED25519 signature valid, zero signing bypasses; atomic ref-CAS, no collision). Skill deploy is S-2 (`./deploy.sh --deploy pipeline-triage`) to the operator instance's `~/.claude/skills`; the tracked corpus (SKILL.md source, references, ADR, registry, deploy.sh, `.skill` package) lands on `main` directly.
**Timestamp:** 2026-07-01 UTC
**Cycle-Time:** N/A — `compute-cycle-time.sh v3.44` returns N/A (no `gate-outcome/plan-review-go` event and no `deployment-status/deploy-skill` / `deploy-harness` event recorded for v3.44 in the pipeline event log; mechanism: compute-cycle-time.sh).
**Result:** SUCCESS — release PR #2801 merged with green CI (28/28 checks PASS); `v3.44` tag created at the merge SHA `adf41b1` (signed-annotated, ED25519, verified on origin). ADR-063, the net-new `pipeline-triage` skill (SKILL.md + references + `.skill` package), the four registration surfaces (registry.md, per-skill-output-contracts.md, deploy.sh `RELEASE_SKILLS`, package), the `stage-to-skill-mode-mapping.md` G2 coverage, and the #282 auto-execute binding are all verified present on `main`. The release PR's auto-close parser did not fire (empty `closingIssuesReferences`), so the 2 cards (#286, #282) are manually closed with per-card verification evidence at Stage 13 close-out (D-1 auto-close-anomaly path).
**Outcome:** SUCCESS

#### Deployment Log v3.43
**Files deployed:** Spoke-execution safety on milestone `67-spoke-execution-safety` (6 cards, single branch/PR/merge under epic spoke-execution-safety) — release PR #2799 (`release/v3.43-spoke-execution-safety → main`) squash-merged at `fb3dc6b873009da2b43943836d9b4b1a126b01cf`. Release Class: **novel** — new ADR (#307) + new failure-mode class (#47) + first-of-kind Stage-5 chip step; multiple D-class decisions (D-Version v3.43, D-C Branch Topology SINGLE, D-Concurrency Posture P0) fire the novel triggers. Topology **D-C SINGLE** (Engineering Commit 0), Parallelism Posture **P0 fully-serial** — driven by a hard write-contention edge on `hub-spoke-bridge.md` (3-way: #47 ∩ #1642 ∩ #59) and the two Cluster-B security surfaces. **#307** — authored ADR-062 (`core/ADRs/ADR-062-substrate-vs-canonical-precedent.md`: canonical-spec edit wins over substrate-body mutation; issue bodies remain historical-record) + wired the cross-repo-citation-translation discipline into `stage-05-solutioning.md` A1 scope-assessment + indexed ADR-062 in `core/ADRs/README.md` (no orphan ADR). **#47** — added the Stage-5 M1.1 substrate-drift reconciliation chip step (one canonical home in `hub-spoke-bridge.md`, emitting a structured 4-field drift report: `drift_class` / `body_claim` / `current_state` / `gap_age_days`) + the `intake-substrate` PROC failure-mode entry (`core/standards/failure-mode-standard.md`); the OPTIONAL `check-substrate-drift.sh` primitive was scoped OUT (the chip-spec + failure-mode entry are the load-bearing deliverables). **#1639** — corrected the BLOCK-DESTRUCTIVE-019 worktree-exemption base in `core/hooks/block-destructive.sh` from workspace-root to repo-root (`${PRIMARY_ROOT}/pmo-platform/.claude/worktrees/`, derived per ADR-017) + retargeted 13 allowlist worktree-glob entries + header example (`[CLAUDE_WORKSPACE_ROOT]` → `[PMO_PLATFORM_ROOT]`) + repointed the hook-test fixtures to the repo-rooted base with both-direction gate-teeth (repo-rooted worktree ALLOW; retired workspace-rooted BLOCK; primary cwd BLOCK) — a LIVE always-enforce defect fixed before #1531's hook-loading wiring lands. **#1642** — added the spoke-template no-`cd`/never-primary clause in `hub-spoke-bridge.md` (AC-1) + the `git-workflow.md § Primary Checkout Discipline` spoke-facing line (AC-3); AC-2 subagent-hook coverage is deliberately deferred to #1531. **#1351** — added the CLAUDE.md Tier-1 governance self-check + Tier-2 structural local-work entry point rooted outside `pmo-platform/` (`core/CLAUDE.md.template`; T1+T2a this release, runtime teeth T2b/T2c fast-follow once #1531/#1642 land). **#59** — added the `ssh-add -l` pre-flight in `hub-spoke-bridge.md` commit-producing-spoke spawn procedures (Stage 6 / Stage 12 / Stage 13 chore-PR spokes) + a user-side handoff on empty-agent. All members ship as one coherent capability slice → one milestone branch / one PR / one squash-merge.
**Mechanism:** git merge (PR #2799 → main, `--squash`) + `v3.43` signed-annotated tag at the merge SHA `fb3dc6b` via `git tag -s` (SSH/ED25519 signature valid, zero signing bypasses). Deploy is git-native for the tracked corpus (governance/spec/ADR/hook edits land on `main` directly); the `block-destructive.sh` hook and the `CLAUDE.md.template` surface deploy to the operator instance via the install/update path (`docs/` install-sync), not the release PR — that is the designed propagation route for operator-instance runtime files, not a missing deploy step.
**Timestamp:** 2026-07-01 UTC
**Cycle-Time:** N/A — no `deploy-skill` / `deploy-harness` deploy event fired for v3.43 at Stage 12 (the changed surfaces are governance/spec/ADR docs + a security hook, not a deployed `.skill` package); `compute-cycle-time.sh v3.43` returns N/A.
**Result:** SUCCESS — release PR #2799 squash-merged with green CI; `v3.43` tag created at the merge SHA `fb3dc6b` (signed-annotated, ED25519). ADR-062, the `intake-substrate` failure-mode class, the M1.1 substrate-drift chip, the `block-destructive.sh` worktree-base fix, the `git-workflow.md` + `CLAUDE.md.template` guards, and the `ssh-add -l` pre-flight are all verified present on `main`. The release PR's auto-close parser did not fire (empty `closingIssuesReferences`), so the 6 cards (#307, #47, #1639, #1642, #1351, #59) were manually closed with per-card verification evidence at Stage 13 close-out (D-1 auto-close-anomaly path).
**Outcome:** SUCCESS

#### Deployment Log v3.42
**Files deployed:** `cleanup-orphan-state.sh` reliability cluster on milestone `85-cleanup-orphan-tooling-reliability` (7 cards, single branch/PR/merge) — release PR #2789 (`release/v3.42-cleanup-orphan-tooling-reliability → main`) squash-merged at `71c773bc042d76cec4bf1569ae63dca151cad150`. Release Class: **routine** — cleanup-debt drainage against one existing tool (`release/tools/cleanup-orphan-state.sh`, + `core/rules/git-workflow.md § Step 10` for #684); no cross-component surface, no novel mechanism, no new dependency. **#2790** (keystone) — `gh` is now resolved absolutely via `resolve_gh_bin()` (mirroring `resolve_lsof_bin`) so it is reachable under the script's pinned `PATH=/usr/bin:/bin`; without it #2216's MERGED-branch rescue and #655's PR-state prefetch were inert on the canonical host, so squash-merge reaping now actually functions. **#2216** — `--release-close` reaps squash-merged release/chore branches (ancestry SKIP no longer pre-empts the PR-state rescue; `--release-close` runs `require_pr=0`). **#684** — `--release-close` scope now covers `chore/vX.Y-stage-*` branches (additive to v2.09's four slug-anchored patterns; owns the cleanup-tool fix for the #2048-item-3 cross-milestone duplicate; carries the release's only Stage-10 Docs touch, `git-workflow.md § Step 10`). **#683** — `--apply` deletes a merged branch from a behind-HEAD checkout without `--force` (origin/main-relative eligibility instead of HEAD-relative `-d`). **#655** — batch-prefetch PR state (O(1) `gh` calls per run instead of ~2 per branch). **#670** — no duplicate local-branch rows under `--all` (presence-check on the candidate set). **#669** — trimmed residual: end-anchored `--help` usage window + self-test assertion (`--help` primary defect resolved incidentally by #1678). All members touch the single file `release/tools/cleanup-orphan-state.sh` (+ `core/rules/git-workflow.md` for #684) → one milestone branch / one PR / one squash-merge.
**Mechanism:** git merge (PR #2789 → main, `--squash`) + `v3.42` signed-annotated tag at the merge SHA via `claim-version.sh` atomic claim (SSH/ED25519 signature valid, zero signing bypasses).
**Timestamp:** 2026-07-01 UTC
**Cycle-Time:** N/A — no `deploy-skill` / `deploy-harness` deploy event fired for v3.42 at Stage 12 (the changed file is an operator-run script, not a deployed skill surface); `compute-cycle-time.sh v3.42` returns N/A.
**Result:** SUCCESS — release PR #2789 squash-merged with green CI; `v3.42` tag claimed at the merge SHA `71c773b` (signed-annotated, ED25519). The 7 cards (#2216, #684, #683, #670, #655, #669, #2790) auto-closed on the squash-merge and are marked done with per-card verification evidence at Stage 13 close-out.
**Outcome:** SUCCESS

#### Deployment Log v3.41
**Files deployed:** Skill-infrastructure hygiene + measurement on milestone 07-INFRA-hygiene-measurement (9 slices, 2 waves under epic `skill-infra-measurement`) — release PR #2704 (`release/v3.39-infra-hygiene-measurement → main`) merged at `7f293f2eed8390802661267d694864239d0bc9d7`. **Re-versioned forward v3.39 → v3.41 pre-merge (A.5.6c lineage):** the release was provisionally cut as v3.39, but the branch descends from v3.40 (v3.37 + v3.38 having shipped after a prior concurrent-release collision on provisional v3.37), so the recomputed next-free minor is v3.41; the branch was relabeled before the merge (plan file `v3.41_RELEASE_PLAN.md`, PR title `v3.41`) and `claim-version.sh` then bound `v3.41` at the merge SHA with no CAS collision. Release Class: **novel** — two net-new `core/` measurement skills + a new ADR. **Wave A — catalog-hygiene sweep:** **#1135** defined the canonical mode-heading separator standard (em-dash) in `canonical-skill-structure.md §4`; **#1136** applied the em-dash separator across the multi-mode SKILL.md set (colon-form → em-dash); **#1132** consolidated vestigial Mode-Detection sections (project-initiator + eval-writer); **#1133** brought the SKILL.md catalog to full `## Guardrails (Platform)` conformance (12 inserts + 26 renames of bare `## Guardrails`, pmo-qa-auditor's `## Guardrails (Extended)` preserved) and fixed a folded `failure-mode-standard.md` drift. **Wave B — measurement builds + reconciliations:** **#571** cross-referenced delivery-engine DoR/DoD to G1/G3 + DT-2; **#676** reconciled the stale canary entry in `per-skill-output-contracts.md §Skill 12`; **#16** built the new `context-budget-auditor` skill (`core/skills/context-budget-auditor/` — SKILL.md + `measure-context-budget.sh`) measuring token consumption across loaded platform components; **#17** built the new `skill-compliance-auditor` skill (`core/skills/skill-compliance-auditor/` — SKILL.md + `scenario-and-calibration-schema.md` + `measure-skill-compliance.sh`) quantifying trigger-rate accuracy, plus **ADR-061** (trigger-rate metric class + eval-writer boundary); **#704** specified routing-conflict detection beyond Jaccard (SPEC-ONLY; deferred implementation → #2699). 52 files changed in the release PR: the 2 new skills (6 files) + ADR-061 + the plan file + `registry.md` / `deploy.sh` / `OPERATIONS.md` new-skill registration + ~40 SKILL.md Guardrails-conformance edits (operations/ + release/ + core/) + `canonical-skill-structure.md` / `failure-mode-standard.md` / `per-skill-output-contracts.md` reconciliations. No `.skill` package committed in-PR (source SKILL.md only; package rebuild deferred to deploy/close-out).
**Mechanism:** git merge (PR #2704 → main, `--merge`) + `v3.41` signed-annotated tag at the merge SHA via `claim-version.sh` atomic claim (SSH/ED25519 signature valid, zero signing bypasses).
**Timestamp:** 2026-07-01 UTC
**Cycle-Time:** N/A — no `deploy-skill` / `deploy-harness` deploy event has fired for v3.41 at Stage 12 (the 2 new skills' `.skill` packages build + deploy at close-out); `compute-cycle-time.sh v3.41` returns N/A.
**Result:** SUCCESS — release PR #2704 merged with green CI (27/27 checks PASS); `v3.41` tag claimed at the merge SHA `7f293f2` (signed-annotated, ED25519). The 9 slices (#1135, #1136, #1132, #1133, #571, #676, #16, #17, #704) carry plain `Addresses` references (no PR close-keywords) and are marked done with per-slice verification evidence at Stage 13 close-out.
**Outcome:** SUCCESS

#### Deployment Log v3.40
**Files deployed:** Doc-link drift drainage + project-path placeholder unification on milestone 30-doc-link-drift-drainage (4 cards, #108 ∥ #131 ∥ #2684 ∥ #2700) — release PR #2712 (`release/v3.40-doc-link-drift-drainage → main`) merged at `366e4bcc271beabe9dc9f4a4b3231a16456dc4b9`. Release Class: **routine** — all issues ≤ size:M, no P1-P2, zero new files, zero new D-class decisions, disjoint 0-edge DAG. All four cards are additive/corrective documentation edits draining stale references from the corpus; no application code, no schema change, no behavior change (all guards and historical records preserved). **#108 (doc-link-protocol log-surface drift)** reconciled `doc-link-maintenance-protocol.md` §8 against the shared warn-log the protocol actually writes (vs `deploy.sh` Check 14). **#131 (Projects/ → projects/ casing)** lowercased the genuine capital-P `Projects/` prose references in `core/governance/OPERATIONS.md` + `operations/OPERATIONS.md` (re-scoped 2026-06-30 to the real OPERATIONS prose refs; the changelog line naming the historical casing is preserved intact). **#2684 (drain last live IMPROVEMENTS.md / IMP-### site)** routed the dead IMPROVEMENTS.md/IMP-### Self-Update Protocol instruction in `routing-rules.md` to the GitHub-Issues intake path. **#2700 (unify project-path placeholder token)** unified the project-path placeholder to the canonical `projects/[Project]/` form across `frontmatter-schema.md`, `navigation-layer-schema.md`, `sqlite-index-schema.md`, `health-check-specification.md`, and `project-initiator/SKILL.md` (owns all placeholder-token variants per the BROAD boundary resolution surfaced by the #131 re-scope). 9 corpus files changed in the release PR (7 K1 corpus docs + the migrated `project-initiator/SKILL.md` + the release plan), plus the rebuilt `project-initiator.skill` package (+ content sidecar) so the deployed skill artifact stays atomic with the #2700 SKILL.md edit.
**Mechanism:** git merge (PR #2712 → main, `--merge`) + `v3.40` signed-annotated tag at the merge SHA (SSH/ED25519 signature valid, zero signing bypasses); `project-initiator.skill` package rebuilt in-PR via `build-skill-packages.sh project-initiator` (deterministic content sidecar changed → committed on the release branch pre-merge).
**Timestamp:** 2026-06-30 UTC
**Cycle-Time:** N/A — documentation/protocol release (a `.skill` package was rebuilt in-PR, but no `deploy-skill` / `deploy-harness` deploy event fires for v3.40).
**Result:** SUCCESS — release PR #2712 merged with green CI (27/27 checks PASS, including a re-green after the in-PR `.skill` rebuild); `v3.40` tag claimed at the merge SHA `366e4bc` (signed-annotated, ED25519). The 4 cards (#108, #131, #2684, #2700) auto-closed via the release PR's Issue References block; per-card verification evidence recorded at Stage 13 close-out.
**Outcome:** SUCCESS

#### Deployment Log v3.38
**Files deployed:** Release-identity & spec-hardening on milestone 66-release-identity-and-spec-hardening (3 cards, #3 ∥ #226 ∥ #75) — release PR #2683 (`release/66-release-identity-and-spec-hardening → main`) merged at `27ddc19bcba3d9c112baf864e87ee25fb4ac9c88`. **Re-versioned forward v3.37 → v3.38 at the Stage-12 pre-merge freeness check (A.5.6c):** the provisional v3.37 was claimed by a concurrent sibling release (milestone 21-shared-entity-storage-layout, PR #2676, its signed tag on origin) between this release's Stage 9 GO and Stage 12 Execute; the pre-merge version-freeness check caught the collision (the sibling's plan file also occupied the same `v3.37_RELEASE_PLAN.md` path), the branch was relabeled to the recomputed next-free v3.38 before the merge (plan file renamed, D-Version + R1 records updated), and `claim-version.sh` then bound `v3.38` at the merge SHA with no collision. R1 fired and resolved exactly as the release plan anticipated (forward-only, CHEAP, no data loss). Release Class: **novel** — the commit-group card carries a genuine D-class enforce/audit/advisory decision (rendered ADVISORY at Stage 5). All three cards are additive doc/protocol edits to the pipeline reference corpus; no application code, no new file class, no schema. **#3 (placement-forward-check)** inserted Phase A0.7 / G-PL3 (a Stage-4 placement forward-check using the unified `git log <base>..origin/main --name-status --find-renames` predicate for dir-crossing renames + in-scope deletions) into stage-04-planning.md plus a governance summary in release-process.md. **#226 (commit-group-decision)** added the commit-group traceability advisory note to stage-06-engineering.md Phase B1 (documented simplification — advisory, not enforced; with the monitored-residual rollup-vs-commit-history note) and an enforcement-posture sentence to sub-task-methodology.md §5. **#75 (frozen-spec-precision)** added the frozen-spec prose-artifact precision rule as a required-when-triggered block in solutioning-output-template.md `## Blast Radius` (with a DEFERRED escape + re-adjudication-cost note) plus a routing pointer in stage-05-solutioning.md §6; a stale target-path in the milestone description (`templates/` vs the real `standards/` home) was corrected into the change matrix at Stage 4. 8 files changed in the release PR (the 6 corpus files + the plan file + its re-version rename). No `.skill` package touched.
**Mechanism:** git merge (PR #2683 → main, `--merge`) + `v3.38` signed-annotated tag at the merge SHA via `claim-version.sh` atomic claim (SSH/ED25519 signature valid, zero signing bypasses).
**Timestamp:** 2026-07-01 UTC
**Cycle-Time:** N/A — documentation/protocol release (the `release-process.md` → `.claude/rules/` byte-mirror re-syncs at deploy, but no deploy-skill / deploy-harness event fires for v3.38).
**Result:** SUCCESS — release PR #2683 merged with green CI (26/26 checks PASS); `v3.38` tag claimed at the merge SHA `27ddc19` (signed-annotated, ED25519). The 3 cards (#3, #226, #75) carry plain `References` (no PR close-keywords) and are marked done with per-card verification evidence at Stage 13 close-out.
**Outcome:** SUCCESS — finalized at Stage 13 VERIFIED. Post-deploy main exhibits the AFTER state of the Release Outcome Statement: the release-pipeline reference corpus now carries the Stage-4 placement forward-check (Phase A0.7 / G-PL3 in stage-04-planning.md + release-process.md), the Stage-6 commit-group advisory posture (stage-06-engineering.md B1 + sub-task-methodology.md §5), and the Stage-5 frozen-spec prose-vs-artifact precision rule (solutioning-output-template.md `## Blast Radius` + stage-05-solutioning.md §6 pointer) — all live on main. A class of late-Stage-12 placement/identity/precision reconciliations is now caught earlier at Stages 4/5/6. Explicitly a documentation/protocol-precision batch — no skill behavior, schema, or runtime-check change. Re-versioned forward v3.37 → v3.38 at the Stage-12 pre-merge freeness check (a sibling claimed v3.37); the re-version ledger row is appended in this chore PR. `novel` release class, CHEAP reversibility / HIGH confidence, 30-day Stage 13 outcome window. The signed `v3.38` tag points at the merge SHA `27ddc19bcba3d9c112baf864e87ee25fb4ac9c88`. Outstanding operator action (non-blocking): the #75 secondary AC — marking the placement-collision observation absorbed in the operator-local decision-discipline observation log (operator-write-only).

#### Deployment Log v3.37
**Files deployed:** Shared-entity storage layout on milestone 21-shared-entity-storage-layout (3 stories, #159 ∥ #362 → #363) — release PR #2676 (`release/21-shared-entity-storage-layout → main`) merged at `f174d1909fdbab5d64f73be9d0421187e366f276`. **No re-version:** the provisional v3.37 was free at the Stage-12 atomic claim (anchor = published v3.36; next-free minor = v3.37, no collision). Release Class: **novel** — first-of-kind project-data-architecture layout (net-new `_pmo/` shared-entity model + composed-index PROJECT.md + typed plan sub-entities). **#362** established the `projects/_pmo/{people,systems,vendors,workstreams,decisions,dependencies}/` shared-entity layout with six entity-page templates (Person / System / Vendor / Workstream / Decision / cross-project Dependency & Conflict), aligned the alias-tracking convention with ADR-040 `person_id` + people-roster (compose, not fork), and added a `project-initiator` Mode A `_pmo/` bootstrap step. **#363** redesigned PROJECT.md as a thin composed wiki-link index that links INTO #362's entity pages, added the ≤50-line composed-index template + a `project-initiator` Mode A Step-3 template-ref swap, and defined the 4-step live-migration protocol with a backwards-compat consumer table. **#159** modeled plans as typed sub-entities (comms / training / hypercare / cutover / change-mgmt), resolved the deferred `plan_type` G5 enum membership, and dropped `plan_subtype`; #363's `## Plans` section points at these typed plan entities. **AC-5 live-project migration DEFERRED** — the merge ships the capability only; the gated POC migration of ≥1 live PROJECT.md (git-ignored ops tree, out-of-diff, snapshot-before/verify-after) is a follow-up per the release plan's R1 mitigation. The `projects/_pmo/*` directories + live-project PROJECT.md are executor-tracked (git-ignored), not in the PR diff. No `.skill` package touched (structure + schema + templates only).
**Mechanism:** git merge (PR #2676 → main, `--merge`) + `v3.37` signed-annotated tag at the merge SHA via `claim-version.sh` atomic claim.
**Timestamp:** 2026-06-30 UTC
**Cycle-Time:** N/A — structure + schema release (no deploy-skill / deploy-harness event for v3.37).
**Velocity:** N/A — 3 stories carry `size:L` (×2, #362/#363) + `size:M` (×1, #159); no points-scale label model on this milestone, so planned/delivered points N/A. Files-changed: release PR #2676 (+ entity templates + composed-index template + plan-entity schema); no `.skill` package touched. Release Class: novel.
**Result:** SUCCESS — release PR #2676 merged with green CI (27/27); `v3.37` tag claimed at the merge SHA `f174d19` (signed-annotated, ED25519). The 3 deliverable stories (#159, #362, #363) did not carry PR close-keywords (auto-close anomaly) and are closed with per-story verification evidence at Stage 13 close-out.
**Outcome:** SUCCESS — `_pmo/` shared-entity layout + composed-index PROJECT.md + typed plan sub-entities landed on main; capability shipped, live-project migration (AC-5) deferred per plan R1.

#### Deployment Log v3.36
**Files deployed:** Process-domain skill best-practice anchoring + support-domain guide + change-domain decision on milestone 90-skill-anchoring-process-support-change (6 issues) — release PR #2648 (`release/v3.35-skill-anchoring-process-support-change → main`) merged at `32a2f16b2ce3435fdcdcbd546b7b6ce6b162c9b9`. **No re-version:** the provisional v3.36 was free at the Stage-12 atomic claim (anchor = published v3.35; next-free minor = v3.36, no collision). The `release/v3.35-skill-anchoring-process-support-change` branch name is retained per the no-retroactive-rename rule. Release Class: **routine** — the process-anchor sequel to v3.30 (software) and v3.33 (governance), plus one net-new support-domain guide and one self-bundled change-domain decision. **Process anchors (#2177 / #2178 / #2179)** added a single-anchor `## Reference docs` pointer to `core/standards/domain-best-practices/process.md` in pmo-scrum-master, pmo-release-train-engineer, and pmo-business-analyst (compose-by-reference, ADR-019; no content absorption). **#2180** gave pmo-product-owner a dual-anchor block pointing to BOTH `process.md` and `governance.md` (value framing + accept/reject span both domains), mirroring pmo-technical-program-manager. **#2210** added the net-new `core/standards/domain-best-practices/support.md` K1 guide (ITIL 4 / tiered incident & escalation / SRE, with the paired escalation-applicability note) and three EXTERNAL + one INTERNAL row in `core/specs/framework-catalog.md`. **#2211 (change-domain decision)** kept the change-domain best-practice body self-bundled in `change-management/references/` with `framework-catalog.md` as its registry — no shared `change.md` created, pmo-ocm-lead unchanged (transitive via ADR-019); recorded in ADR-057 (`status: Proposed`) with a reversal trigger, plus a `build-philosophy.md` "BP — change" coverage row and one `change-management/SKILL.md` Reference-docs line. The `consumers:` fields of `process.md` and `governance.md` were appended accordingly. The four edited process skills' `.skill` packages were rebuilt at the Stage-12 cut and committed in this chore PR (the `build(vX.Y)` convention) so the content-freshness check (Check 7) passes. 11 files changed in the release PR (+166/−2); 8 package files in the rebuild.
**Mechanism:** git merge (PR #2648 → main, `--merge`) + `v3.36` signed-annotated tag at the merge SHA via `claim-version.sh` atomic claim.
**Timestamp:** 2026-06-30 UTC
**Cycle-Time:** N/A — release-pipeline skill/governance release.
**Result:** SUCCESS — release PR #2648 merged with green CI (25/25); `v3.36` tag claimed at the merge SHA `32a2f16` (signed-annotated, ED25519). All 6 issues (#2177, #2178, #2179, #2180, #2210, #2211) auto-closed via PR close-keywords.
**Outcome:** SUCCESS

#### Deployment Log v3.35
**Files deployed:** Records-management policy + artifact naming + 08-Generated cleanup on milestone 20-records-management-naming-and-cleanup (4 cards, #372 + #369 + #277 + #232) — release PR #2610 (`release/v3.33-records-management-naming-and-cleanup → main`) merged at `47228d5ba391bbc432ef1b1f1eb5d88458b8e078`. **Re-versioned forward v3.34 → v3.35 at the Stage-12 atomic claim (A.5.6c):** the provisional v3.34 was claimed by a concurrent pipeline-telemetry release (PR #2621) published between this release's Stage 9 GO and Stage 12 Execute; the pre-merge version-freeness check caught the collision, the branch was relabeled to the recomputed next-free v3.35 before the merge, and `claim-version.sh` then bound `v3.35` at the merge SHA with no collision. The `release/v3.33-records-management-naming-and-cleanup` branch name is retained per the no-retroactive-rename rule. Release Class: **novel** — three first-of-kind artifacts. **#372** added `core/governance/RECORDS_POLICY.md` (ISO 15489-1 value-based retention / 4-class classification / disposition / authenticity; ADR-054). **#369** added `core/standards/artifact-naming-standard.md` (POSIX-safe syntax + controlled type-vocabulary semantics + explicit folder-naming rules + validation regex; wired into 7 emitting skills + OPERATIONS.md + pmo-qa-auditor; ADR-055). **#277** added the new `operations/skills/generated-cleanup/` skill (+ rebuilt `.skill` package) — grouped-candidate cleanup keyed on lifecycle states with an unconditional user-approval gate and `_archived/` disposition; ADR-056. **#232** extended `operations/skills/project-initiator/SKILL.md` with a Mode A pre-scaffold folder-name validation gate citing #369's folder rules (`_`-prefix infra carve-out). 25 files changed. +1294/−15.
**Mechanism:** git merge (PR #2610 → main, `--merge`) + `v3.35` signed-annotated tag at the merge SHA via `claim-version.sh` atomic claim.
**Timestamp:** 2026-06-30 UTC
**Cycle-Time:** N/A — governance + skill release (no deploy-skill / deploy-harness event for v3.35; the `generated-cleanup` `.skill` package was built and committed in the release PR).
**Result:** SUCCESS — release PR #2610 merged with green CI (28/28); `v3.35` tag claimed at the merge SHA `47228d5`. The 4 deliverable cards (#372, #369, #277, #232) did not carry PR close-keywords (auto-close anomaly) and are closed with per-card verification evidence at Stage 13 close-out.
**Outcome:** SUCCESS

#### Deployment Log v3.34
**Files deployed:** Pipeline telemetry suite on milestone 65-pipeline-telemetry-suite (3 issues, #7 + #143 + #2612) — release PR #2621 (`feat/pipeline-telemetry-suite → main`) merged at `2b813f3c71e5e5e86df93b0d0f1dbc85712abf1e`. **No re-version:** the provisional v3.34 was free at the Stage-12 atomic claim (anchor = published v3.33; next-free minor = v3.34, no collision). Release Class: **novel** — first-of-kind telemetry instrumentation (net-new schema + net-new compute tooling, no prior pattern to extend). The release adds the 4 DORA key metrics (deployment frequency / lead time for changes / change-failure rate / MTTR) at the Build+Deploy layer and Close-class observability (retro conformance, lessons-learned population, carry-forward closure, cross-release pattern emergence) at the Close layer. **#7 (DORA-4 anchor)** added `release/references/standards/dora-telemetry.md` + `release/tools/compute-dora-metrics.sh`. **#143 (Close-class)** added `release/references/standards/close-class-telemetry.md` + `release/tools/compute-close-class-telemetry.sh` (Indicators 1/2 build against the shipped #360/#361 close-out registers). **#2612 (Sub-class-D aggregation)** extended `release/tools/synthesize-release-learnings.sh` to emit `cross_release_pattern_emergence_rate` (Indicator 4), consuming #143's rows. Stage 6/12/13 pipeline references gained telemetry-emission citations. No skill or `.skill` package touched (tooling + schema only). +1517/−3.
**Mechanism:** git merge (PR #2621 → main, `--merge`) + `v3.34` signed-annotated tag at the merge SHA via `claim-version.sh` atomic claim.
**Timestamp:** 2026-06-30 UTC
**Cycle-Time:** N/A — release-pipeline tooling/schema release.
**Result:** SUCCESS — release PR #2621 merged with green CI (27/27); `v3.34` tag claimed at the merge SHA `2b813f3`. All 3 issues (#7, #143, #2612) auto-closed via PR close-keywords.
**Outcome:** SUCCESS

#### Deployment Log v3.33
**Files deployed:** Skill best-practice anchoring on milestone 89-skill-anchoring-governance (6 issues) — release PR #2614 (`release/v3.32-skill-anchoring-governance → main`) merged at `3daa866de44382dc0af5121d2a4bcb6ae5ffdc97`. **Re-versioned forward v3.32 → v3.33 at the Stage-12 atomic claim:** the provisional v3.32 was claimed by a concurrent release (64-hub-autonomy-conformance) ahead of this one; the anchor recomputed next-free against the published v3.32 → **v3.33**, no collision. The `release/v3.32-skill-anchoring-governance` branch name is retained per the no-retroactive-rename rule. Release Class: **routine**. The six governance-domain specialist skills (pmo-project-manager, pmo-program-manager, pmo-program-coordinator, pmo-portfolio-manager, pmo-knowledge-manager, release-planner) gain a `## Reference docs` pointer to `core/standards/domain-best-practices/governance.md` (compose-by-reference, ADR-019; no content absorption); the guide's `consumers:` field lists them. This is the governance-domain sequel to v3.30's software-domain anchoring; the six rebuilt `.skill` packages are committed in the same PR (the `build(vX.Y)` convention) so the content-freshness check (Check 7) passes. +142/−7.
**Mechanism:** git merge (PR #2614 → main, `--merge`) + `v3.33` signed-annotated tag at the merge SHA via `claim-version.sh` atomic claim.
**Timestamp:** 2026-06-30 UTC
**Cycle-Time:** N/A — release-pipeline skill/governance release.
**Result:** SUCCESS — release PR #2614 merged with green CI (27/27); `v3.33` tag claimed at the merge SHA `3daa866`. All 6 issues (#2172, #2173, #2174, #2175, #2176, #2181) auto-closed via PR close-keywords.
**Outcome:** SUCCESS

#### Deployment Log v3.32
**Files deployed:** Hub-autonomy conformance on milestone 64-hub-autonomy-conformance (2 issues, #381 + #377) — release PR #2589 (`release/v3.29 → main`) merged at `bdadfae4592fa460ca33a354a5861043ec866338`. **Re-versioned forward four times to v3.32 at the Stage-12 atomic claim:** the Stage-4 plan recorded a provisional v3.28, re-targeted to v3.29 during the run; concurrent releases then shipped v3.29, v3.30, and v3.31 (the last an orphan tag from a redundant claim on the v3.30 release) ahead of this one, so the anchor recomputed next-free at each rung and bound **v3.32** at the merge commit, no collision. The `release/v3.29` branch name is retained per the no-retroactive-rename rule. Release Class: **novel** (ADR-053 records the Gate-0 pre-eligibility precondition placement). **#381 (CODIFY)** wrote the full-phase-scope rule into `release/governance/release-process.md` (generalizing the Stage-2 form in `stage-02-triage.md`), citing `core/specs/autonomy-tiers.md`. **#377 (ENFORCE)** added the R1 Gate-0 forcing-function precondition into `release/references/how-to/hub-spoke-bridge.md` (+ hub persona in `release/references/specs/release-personas.md`), the R3 dedup-guard against the existing `core/standards/hub-session-continuity.md` Surface A queue, and the R4 unauthorized-gate metric into `core/disciplines/decision-discipline.md`; ADR-053 committed.
**Mechanism:** git merge (PR #2589 → main, `--merge`) + `v3.32` signed-annotated tag at the merge SHA via `claim-version.sh` atomic claim.
**Timestamp:** 2026-06-30 UTC
**Cycle-Time:** N/A — release-pipeline governance/spec release.
**Result:** SUCCESS — release PR #2589 merged with green CI (26/26); `v3.32` tag claimed at the merge SHA `bdadfae`. Both issues (#381, #377) auto-closed via PR close-keywords. Fourth version re-target driven by concurrent-release contention; no collision at the atomic claim.
**Outcome:** PENDING — Stage 13 transitions DEPLOYED → VERIFIED after gate-passage verification.

#### Deployment Log v3.30
**Files deployed:** Skill best-practice anchoring on milestone 88-skill-anchoring-software (6 issues) — release PR #2596 (`release/v3.29-skill-anchoring-software → main`) merged at `6264b001986c5327deae531152a53334f7ae8d9f`. **Re-versioned forward v3.29 → v3.30 at the Stage-12 atomic claim:** the provisional v3.29 was claimed by a concurrent release (73-concurrent-execution-safety) mid-build; the anchor recomputed next-free → **v3.30**, no collision. The `release/v3.29-skill-anchoring-software` branch name is retained per the no-retroactive-rename rule. Release Class: **routine**. The five software-domain specialist skills (pmo-architect, pmo-principal-engineer, pmo-software-engineer, pmo-devops-sre, pmo-technical-program-manager) gain a `## Reference docs` pointer to `core/standards/domain-best-practices/software.md` (compose-by-reference, ADR-019; no content absorption); the guide's `consumers:` lists them; pmo-technical-program-manager is dual-domain (also `governance.md`); and `core/disciplines/build-philosophy.md` BP×Skills cells reconciled (software skill-wired; governance/process skill-wiring-pending). +131/−5.
**Mechanism:** git merge (PR #2596 → main, `--merge`) + `v3.30` signed-annotated tag at the merge SHA via `claim-version.sh` atomic claim.
**Timestamp:** 2026-06-30 UTC
**Cycle-Time:** N/A — release-pipeline skill/governance release.
**Result:** SUCCESS — release PR #2596 merged with green CI (28/28); `v3.30` tag claimed at the merge SHA. All 6 issues auto-closed via PR close-keywords. A redundant claim-tool invocation created an orphan `v3.31` tag (no GitHub Release) pointing at the same merge commit; it is left in place (benign per the no-tag-deletion rule — the next release claims v3.32).
**Outcome:** SUCCESS

#### Deployment Log v3.29
**Files deployed:** Release-pipeline governance + skill release on milestone 73-concurrent-execution-safety (single card #19) — release PR #2585 (`release/v3.28 → main`) merged at `0fe190564c4066962f8d0c9af6235d7a70ca4266`. **Re-versioned forward v3.28 → v3.29 at the Stage-12 atomic claim:** the provisional v3.28 became taken when milestone 41-label-taxonomy-canonicalization-backfill (PR #2586) published v3.28 first; the anchor recomputed next-free against the published v3.28 → **v3.29**, no collision. The `release/v3.28` branch name is retained per the no-retroactive-rename rule. Release Class: **novel** (ADR-052 founds the engineering-parallelism-posture taxonomy + a net-new `parallelism-posture-taxonomy.md`). #19 — Stage-6 Engineering parallelism postures: ADR-052 + the new `release/references/standards/parallelism-posture-taxonomy.md`, wired into `release/references/pipeline/stage-04-planning.md`, `release/references/pipeline/stage-06-engineering.md`, `release/governance/release-process.md`, `release/references/how-to/hub-spoke-bridge.md`, `core/standards/failure-mode-standard.md`, and the `release-planner` skill (SKILL.md + rebuilt `.skill` package). +443/−1.
**Mechanism:** git merge (PR #2585 → main, `--merge`) + `v3.29` tag at the merge SHA (`gh release create --target main`).
**Timestamp:** 2026-06-30 13:46 UTC
**Cycle-Time:** N/A — release-pipeline governance/skill release.
**Result:** SUCCESS — release PR #2585 merged; `v3.29` GitHub Release published (not draft). #19 auto-closed via the PR close-keyword. Milestone 73-concurrent-execution-safety left **OPEN** (2 issues remain — #19 is a single-card slice of a multi-card milestone).
**Outcome:** SUCCESS

#### Deployment Log v3.28
**Files deployed:** Governance + tooling release on milestone 41-label-taxonomy-canonicalization-backfill — release PR #2586 (`feat/v3.28-label-taxonomy-canonicalization → main`) merged at `70f6de8667e14038993844fe6ff84daa21f5e1d9`. Release Class: **cross-cutting** (a label-taxonomy governance spec + a Stage-3 pipeline reference + a new deploy-check tool & primitive). 4-file surface (+275/−15): **(#80)** `core/specs/label-taxonomy.md` — cluster orthogonality protocol (domain clusters one-per-issue vs the `cluster: cross-cutting` span-marker), §Rules item 3 update, cluster color-column reconciliation to live colors, and registered `cluster: security` (12 clusters, was 11); **(#74)** `core/specs/label-taxonomy.md` promotion-strip rule + the title↔category parity invariant (§Rules item 7), cross-referenced from the Stage-3 Template-Conversion Rule in `release/references/pipeline/stage-03-bundle.md` (the card's Stage-2 target was corrected — the strip already fires at Stage 3); **(#749)** new `core/deploy/tools/check-label-parity.py` + `core/deploy/deploy.sh` Check 51 (label-taxonomy ↔ GitHub label-set parity; enforce-capable MISSING / warn-only ORPHAN; warn-mode-initial; source-agnostic `--source`). No skill mirror / `.skill` rebuild — no `SKILL.md` body changed.
**Mechanism:** git merge (PR #2586 → main, `--merge`, preserving branch history) + `v3.28` tag at the merge SHA (`gh release create --target main`). Orchestrated end-to-end via release-hub Mode O.
**Timestamp:** 2026-06-30 05:30 UTC
**Cycle-Time:** N/A — governance/tooling release; no deploy-skill/deploy-harness event recorded for v3.28 in the operator-instance pipeline event log (mechanism: compute-cycle-time.sh).
**Result:** SUCCESS — release PR #2586 merged to main; `v3.28` GitHub Release published (not draft). #80, #74, #749 closed at Stage 13; milestone #120 closed (7 closed / 0 open). The release PR used safe phrasing (no close-keywords) — the merge did NOT auto-close the issues; they were closed explicitly at close-out. Check 51's first warn-run surfaced pre-existing label drift (missing `status: in-progress`/`done` labels → #1828; unregistered `type:*`/`size:*`/`layer:*` → #1777) and the #153 prefix-strip was deferred to triage (ambiguous `improvement` + `type:observation` + `status: deferred`).
**Outcome:** SUCCESS

#### Deployment Log v3.27
**Files deployed:** No runtime/skill files deployed — documentation + governance/CI release (platform-doc frontmatter standardization). Single release-PR merge on milestone 28-corpus-frontmatter-standardization — release PR #2545 (`release/v3.25-corpus-frontmatter-standardization → main`) merged at `46b1541f1423c9f4859d28a66494b2c357f11d51`. Re-versioned forward **v3.25 → v3.27** at the Stage-12 atomic claim: the provisional v3.25 (itself a Commit-0 re-render up from the Stage-4 v3.22) became a permanent benign gap when concurrent releases claimed v3.24 and v3.26 while this branch was in flight; `claim-version.sh` recomputed next-free against anchor = published Release `v3.26` → **v3.27**, accepted with no CAS collision (no `RELEASE_REVERSIONS.md` row — the re-version was a forward pre-merge relabel of the close-out artifact, not a tag-push collision). The branch name and historical Engineering commit messages retain the provisional v3.25 per the no-retroactive-rename rule. Release Class: **novel** (#295 authors the net-new platform-doc frontmatter standard + the net-new CI gate tool; two net-new files trigger the literal `novel` (a) condition). 148-file surface, additive/corrective throughout (+1991/−42): **(#295 — keystone)** new `core/standards/platform-doc-frontmatter-standard.md` defining the per-class required-field set + the canonical singular `type` enum, scope-disjoint from `frontmatter-schema.md`; **(#109 — Tier-A backfill)** mechanical additive frontmatter backfill across ~140 no-frontmatter framework reference docs under `core/disciplines/`, `core/schemas/`, `core/rules/`, `core/governance/`, `release/references/`; **(#2220 — warn-mode CI gate)** new `core/deploy/tools/check-doc-frontmatter.py` + `core/deploy/tools/_frontmatter.py` helper + `core/deploy/allowlists/skip-doc-frontmatter-check.txt` allowlist, wired into `core/deploy/deploy.sh` (Check 50, enforce Tier-A / warn elsewhere) and `core/deploy/tools/check-version-anchors.py`. **(release artifacts)** `release/releases/plans/v3/v3.27_RELEASE_PLAN.md` (Commit 0; renamed from the provisional `v3.25_RELEASE_PLAN.md` + in-file version tokens reconciled to v3.27 at Stage 12 Phase B5).
**Mechanism:** git merge (PR #2545 → main, `--merge`, preserving the branch history) + signed-annotated `v3.27` tag at the merge SHA (Tag-SHA-Direct pattern per stage-12-execute.md § Phase B tag-SHA-direct rationale; governed `claim-version.sh` atomic-claim semantics; SSH/ED25519 signature valid — "Good git signature" — zero signing bypasses). No skill mirror / `.skill` rebuild — no `SKILL.md` body changed in this release.
**Timestamp:** 2026-06-30 03:49 UTC
**Cycle-Time:** N/A — documentation/governance/CI release; no deployment-status/deploy-skill or deploy-harness event recorded for v3.27 in the operator-instance pipeline event log (mechanism: compute-cycle-time.sh)
**Result:** SUCCESS — release PR #2545 merged to main at `46b1541f1423c9f4859d28a66494b2c357f11d51`; the `v3.27` signed-annotated tag points at the merge SHA (created via the governed `claim-version.sh` atomic claim, annotated object type, ED25519 signature valid; zero signing bypasses). #295, #109, #2220 are marked closed at Stage 13 (the release PR uses safe phrasing — `closingIssuesReferences` empty — so the merge did NOT auto-close them).
**Outcome:** SUCCESS

#### Deployment Log v3.26
**Files deployed:** 13 governance/CI surfaces via release PR #2529 merge — three NEW warn-mode governance-drift gates + cross-reference-integrity hardening: governance-file-map cross-check job (#133) + capital-`P` `Projects/` casing gate (#135) appended to `.github/workflows/repo-integrity.yml`; the 6 genuine `Projects/_governance/` → `projects/_config/` reconcile hits (#135) across `core/schemas/frontmatter-schema.md`, `core/governance/OPERATIONS.md`, `operations/OPERATIONS.md`, `release/governance/RELEASE_PROTOCOL.md`, `operations/skills/project-initiator/SKILL.md`; `core/deploy/deploy.sh` Check 49 + new `core/deploy/tools/check-convention.sh` (#228); cross-reference-integrity pre-flip hardening (#757) across `core/deploy/tools/check-skill-count-imp.py`, `.github/workflows/reference-durability.yml`, `core/hooks/run-fragile-ref-fixtures.sh`. Plus a `.skill` package rebuild for `project-initiator` (`packages/project-initiator.skill` + `.sha256`) — its SKILL.md changed via the #135 reconcile, so the package was rebuilt to stay current (per stage-12-execute.md § Phase J.5).
**Mechanism:** git merge (PR #2529 → main, `--merge`, preserving branch history; re-baselined onto `origin/main` mid-Stage-12 after a concurrent fix PR landed — `Base branch was modified`) + signed-annotated `v3.26` tag at the merge SHA (Tag-SHA-Direct pattern per stage-12-execute.md § Phase B tag-SHA-direct rationale; SSH/ED25519 signature valid — "Good git signature" — zero signing bypasses). Provisional **v3.26** at authoring confirmed free at claim time: anchor = latest published Release `v3.24`; `v3.25` claimed by an in-flight `release/v3.25-*` branch (invisible to `claim-version.sh` `claimed_set()`, which reads only tags ∪ published Releases ∪ DEPLOYED rows); next-free = `v3.26`. The tag push was the atomic CAS — accepted with no collision, so the provisional and claimed version coincide (no `RELEASE_REVERSIONS.md` row).
**Timestamp:** 2026-06-29 UTC
**Cycle-Time:** N/A — no deployment-status/deploy-skill or deploy-harness event recorded for v3.26 in the operator-instance pipeline event log (mechanism: compute-cycle-time.sh)
**Result:** SUCCESS — release PR merged; `v3.26` tag created in signed-annotated form (annotated object type, ED25519 signature valid); zero signing bypasses; all 4 release issues (#133, #135, #228, #757) auto-closed via the release PR's close-keywords.
**Outcome:** SUCCESS

#### Deployment Log v3.24
**Files deployed:** New `release-hub` orchestrator skill (the whole-release control plane — Mode R Milestone Readiness + Mode O Orchestrate Release) — the release that introduces the orchestrator surface. Single release-PR merge on milestone #252 `release-hub-orchestrator-skill` — release PR #2497 (`feature/2115-release-hub-skill → main`) merged at `25f46256bd2747d1eded751771b2e8f024c7d1c0`. Provisional **v3.24** at authoring; the mainline ran hot (v3.22 and v3.23 were both claimed by concurrent sibling releases while this branch was in flight — two prior provisional advances), so the version was carried as a bump-class declaration and resolved at the Stage-12 atomic claim. The `claim-version.sh` atomic claim confirmed **v3.24** free at the claim instant (anchor = highest mainline-published Release `v3.23`; `--bump minor` → next-free `v3.24`; no collision re-version at the claim — the provisional and the claimed version coincide, so no `RELEASE_REVERSIONS.md` row). Combined slice of both child issues on one branch per the milestone=one-PR model: **(#2115 — Mode R, Milestone Readiness)** the pre-flight that confirms a bundled milestone is ready to START — composes triage/dup, staleness/architecture, dependency, and bundle-coherence checks into one GO / NO-GO with per-finding dispositions; **(#2212 — Mode O, Orchestrate Release)** full hub-and-spoke orchestration that takes a milestone and drives it through the pipeline by composing the stage skills, owning sequencing and readiness-gating (never the stage work itself). Surface (9 files): `release/skills/release-hub/SKILL.md` + 4 references (`milestone-readiness-checklist.md`, `orchestration-playbook.md`, `spoke-launch.md`, `decision-briefing.md`); `core/deploy/deploy.sh` RELEASE_SKILLS roster entry; `packages/release-hub.skill` (+ `.sha256` baseline).
**Mechanism:** git merge (PR #2497 → main, `--merge`, preserving the branch history) + signed-annotated `v3.24` tag at the merge SHA (Tag-SHA-Direct pattern per stage-12-execute.md § Phase B tag-SHA-direct rationale; governed `claim-version.sh` atomic-claim semantics; SSH/ED25519 signature valid, zero signing bypasses). Skill mirror + `.skill` package deploy to install targets via `deploy.sh --deploy release-hub` (S-2) — Layer-2 install-sync runs outside the release worktree.
**Timestamp:** 2026-06-30 00:41 UTC
**Cycle-Time:** N/A — no deployment-status/deploy-skill or deploy-harness event recorded for v3.24 in the operator-instance pipeline event log (mechanism: compute-cycle-time.sh)
**Velocity:** 2 build issues (#2115 Mode R, #2212 Mode O); class `novel` (net-new orchestrator skill — the whole-release control plane; the `novel` triggers fire); 9 files changed (additive net-new skill surface — SKILL.md + 4 references + deploy roster + `.skill` package + `.sha256`).
**Result:** SUCCESS — release PR #2497 merged to main at `25f46256bd2747d1eded751771b2e8f024c7d1c0`; the `v3.24` signed-annotated tag points at the merge SHA (created via the governed `claim-version.sh` atomic claim, ED25519 signature valid); #2115 + #2212 auto-closed at merge. The `release-hub` skill is rostered in `deploy.sh` and the `.skill` package + `.sha256` are on main.
**Outcome:** ATTAINED — post-merge `main` exhibits the AFTER state: the `release-hub` orchestrator skill is on `main` (`release/skills/release-hub/SKILL.md` present with both modes — Mode R Milestone Readiness pre-flight and Mode O Orchestrate Release), rostered in `deploy.sh` RELEASE_SKILLS, and packaged (`packages/release-hub.skill` + `.sha256`). Reversibility CHEAP / HIGH — additive net-new skill; single-PR `git revert -m 1` of #2497 removes the whole surface; the orchestrator composes existing stage skills and gates the operator at the named pipeline checkpoints, so it adds no new autonomous mutation surface. 30-day Stage-13 outcome window per the `novel` class.

#### Deployment Log v3.21
**Files deployed:** No runtime/skill files deployed — documentation + governance release (terminology & controlled-vocabulary canonicalization). Single release-PR merge on milestone #131 `34-terminology-and-controlled-vocabulary` — release PR #2487 (`release/v2.42-terminology-and-controlled-vocabulary → main`) merged at `26c32a4e0e1888ab17c99a2de13166249090d7b2`. Re-versioned forward v2.42 → **v3.21** at the Stage-12 atomic claim (the v3.20 published Release on the mainline first-parent spine elevated the `claim-version.sh` major-max frontier; operator accepted the governed v3.21 output 2026-06-29 — see `RELEASE_REVERSIONS.md`). Release Class: **novel** (#432 authors ADR-049 + carries the D-432-Wording canonical-vocabulary decision; all three `novel` triggers fire). 8-file surface, additive/corrective throughout: **(#68 — glossary refresh, keystone)** `core/specs/terminology-glossary.md` gains discoverable frontmatter, Role tier-anchoring, four first-class actor terms (`{#term-hub}` / `{#term-spoke}` / `{#term-skill}` / `{#term-sub-agent}`), an Appendix-A "agent"-overload resolution row, and a Total-terms bump 19 → 23; **(#128 — tier-disambiguation extension)** `core/specs/autonomy-tiers.md` adds a Hierarchy Tier row + namespace-overlap collision note + out-of-scope prose for the four named conventions and cascades the convention count four → five, and `core/CLAUDE.md.template` L310 syncs the 2nd "four" → five (1st "four" = Autonomy's own 0–3 preserved); **(#432 — initiative/roadmap canonicalization + ADR-049)** `core/specs/terminology-glossary.md` adds canonical `{#term-initiative}` + `{#term-roadmap}` rows, rewrites Appendix B (the "not modeled" assertion removed), and bumps Total-terms 23 → 25; `core/standards/initiative-roadmap-framework.md` gains an additive canonical-scope note (one-initiative narrative preserved, surgical); `core/specs/label-taxonomy.md` reconciles the §Initiative Labels grouping line to the canonical term and replaces three dead `initiative:*` rows with the live `epic:*` / `project:*` grouping namespaces; and the new `core/ADRs/ADR-049-canonical-initiative-roadmap-vocabulary.md` records the canonical Initiative/Roadmap/milestone vocabulary + the `initiative:` → `epic:`/`project:` label mapping, registered in `core/ADRs/README.md`. **(release artifacts)** `release/releases/plans/v2/v2.42_RELEASE_PLAN.md` (Commit 0; retains the as-authored v2.42 filename per the re-version convention).
**Mechanism:** git merge (PR #2487 → main) + signed-annotated tag push (Tag-SHA-Direct pattern per stage-12-execute.md § Phase B tag-SHA-direct rationale; governed `claim-version.sh` atomic claim)
**Timestamp:** 2026-06-29 21:48 UTC
**Cycle-Time:** N/A — documentation/governance release; no deployment-status/deploy-skill or deploy-harness event recorded for v3.21 (mechanism: compute-cycle-time.sh)
**Velocity:** 3 build issues (#68 size:M, #128 size:M, #432 size:M; raw 12 → ×1.15 novel weight ≈ 14 effective); class `novel`; 8 files changed (additive/corrective; the new ADR-049 + the release plan included).
**Result:** SUCCESS — release PR #2487 merged to main; `v3.21` tag points at the merge SHA `26c32a4e0e1888ab17c99a2de13166249090d7b2`, created signed-annotated (ED25519 signature valid; zero signing bypasses) via the governed claim mechanism.
**Outcome:** ATTAINED — post-merge `main` exhibits the Release Outcome Statement AFTER state: the K1 glossary now carries discoverable frontmatter + four first-class actor terms (`grep -cE '\{#term-(hub|spoke|skill|sub-agent)' core/specs/terminology-glossary.md` = 4), the Tier Disambiguation Table covers Hierarchy Tier (`grep -ci 'hierarchy tier' core/specs/autonomy-tiers.md` ≥ 1), canonical `Initiative`/`Roadmap` terms are present (`grep -cE '\{#term-(initiative|roadmap)' core/specs/terminology-glossary.md` = 2) reconciling the glossary, the initiative-roadmap framework, and the live `epic:*` label namespace, Appendix B no longer reads "not modeled" (`grep -ci 'not modeled'` = 0), and ADR-049 records the canonical vocabulary + `initiative:` → `epic:`/`project:` label mapping. Reversibility MODERATE / MEDIUM at the release level (#432 ripples canonical vocabulary into the framework + label-taxonomy + ADR-049; runtime blast radius none — no code consumes the terms); single-PR `git revert -m 1` of #2487 un-does all three slices atomically.

#### Deployment Log v3.22
**Files deployed:** No runtime/skill files deployed — documentation + governance/schema release (PMBOK artifact coverage + PROJECT.md schema axes). Single release-PR merge on milestone 18-pmbok-coverage-and-project-schema — release PR #2489 (`release/18-pmbok-coverage-and-project-schema → main`) merged at `3aded2afa122f8e24b63d7ec89dced27b31582cb`. Carried a provisional v2.42 label; claimed **v3.22** at the Stage-12 atomic claim (v3.21 was the highest mainline tag; v3.22 free at the claim instant — no collision re-version). Release Class: **cross-cutting** (Deep Stage-9 review). 19-file surface, additive throughout (+686/−14; the deletions are intentional count-cascade + gloss-fix rewrites), by deliverable: **(#371 — foundation)** new `core/disciplines/lifecycle-tailoring.md` mapping the 3-state agent model → PMBOK 5-process-group convention (citing ADR-004) + 3 pointers; **(#206 — foundation)** 5 PMBOK templates (charter, lessons-learned, change-log, RACI, stakeholder-register) + Tracker 8/9 schemas + OPERATIONS.md registration + deploy-sync wiring; **(#351 — keystone)** `deliverable_type` open-enum axis + V13 schema rule + §5A consumption pattern + G3-05 + founding ADR-049-deliverable-domain-axis (Proposed → Accepted at Stage-9 review); **(#262 — infrastructure)** `org_structure_type` (V14) + `team_roster` (V15, ref-only) appended off the keystone's post-V13 tail. **(release artifacts)** `release/releases/plans/v2/v2.42_RELEASE_PLAN.md` (retains the as-authored provisional filename per the re-version convention).
**Mechanism:** git merge (PR #2489 → main) + signed-annotated tag push (Tag-SHA-Direct pattern per stage-12-execute.md § Phase B tag-SHA-direct rationale; governed `claim-version.sh` atomic-claim semantics)
**Timestamp:** 2026-06-29 22:27 UTC
**Cycle-Time:** N/A — documentation/governance/schema release; no deployment-status/deploy-skill or deploy-harness event recorded for v3.22 (mechanism: compute-cycle-time.sh)
**Result:** SUCCESS — release PR #2489 merged to main; `v3.22` tag points at the merge SHA `3aded2afa122f8e24b63d7ec89dced27b31582cb`, created signed-annotated (zero signing bypasses) via the governed claim mechanism.

#### Deployment Log v3.23
**Files deployed:** New `health-check` function-skill (the project health-check skill) — first net-new runtime/skill deploy of the release. Single release-PR merge on milestone #215 `06-HEALTH-project-health-check` — release PR #2488 (`release/06-HEALTH-project-health-check → main`) merged at `97310f9457c5ce25f2543dd9a7beed54941ef149`. Carried a provisional v2.42 label (the branch and its sub-tasks were authored when v2.42 was the next slot and ADR-049 the next ADR number); a mid-pipeline `chore(reconcile)` commit (`2d59d98`) merged `main`, added ADR-051, re-versioned the branch to **v3.23**, and resolved the plan-file conflict; the Stage-12 atomic claim confirmed **v3.23** free (highest mainline tag = `v3.22`; no collision re-version at the claim instant). Release Class: **novel** (net-new skill + founding ADR-051). Combined slice of both child issues on one branch per the milestone=one-PR model: **(#1125 — v1 foundation)** builds the full 7-mode contract once (4-intent block convention + 5-section title-case output `## Confirmed` / `## Auto-Actionable` / `## Decisions` / `## Unknowns` / `## Rollup-Diffs` + a never-auto-applied `TRACKER_UPDATES:` block + S0–S3 confidence+band labeling) and implements the 3 v1 modes (`full`, `timeline`, `attribution`); **(#1126 — v2 extended modes)** implements the 4 v2 modes (`comms`, `plan <name>`, `raid`, `sources`). Surface: `operations/skills/health-check/SKILL.md` + 3 references (`mode-intents.md`, `evidence-matrix.md`, `confidence-framework.md`) + the evals suite (7 judges, 11 cases, 3 fixtures, `evals.json`); `commands/health-check.md`; founding `core/ADRs/ADR-051-health-check-mcp-primary-source-set.md` (MCP-primary / local-fallback canonical source set + graceful-degradation contract; Accepted at the Stage-5 Decision Gate 2026-06-29); `core/skills/registry.md` catalog row; `core/deploy/deploy.sh` roster entry; `packages/health-check.skill` (+ `.sha256` baseline). ADR-049 + ADR-050 already on `main` (the reconcile pulled them; this release adds only ADR-051 cleanly). **(release artifacts)** `release/releases/plans/v3/v3.23_RELEASE_PLAN.md` (re-versioned filename, carries the `## Change Description` for Stage-9 Plan Review).
**Mechanism:** git merge (PR #2488 → main, `--merge`, preserving the 9-commit branch history) + signed-annotated `v3.23` tag at the merge SHA (Tag-SHA-Direct pattern per stage-12-execute.md § Phase B tag-SHA-direct rationale; governed `claim-version.sh` atomic-claim semantics; ED25519 signature valid, zero signing bypasses). Skill mirror + `.skill` package deploy to install targets via `deploy.sh --deploy health-check` (S-2).
**Timestamp:** 2026-06-30 00:24 UTC
**Cycle-Time:** N/A — no plan-review-go gate-outcome and no deployment-status/deploy-skill or deploy-harness event recorded for v3.23 in the operator-instance pipeline event log (mechanism: compute-cycle-time.sh)
**Velocity:** planned 16 pts / delivered 16 pts (1.00); files-changed 32; allocation 0/16/0 pts (feature/debt/protocol-slack); class `novel` (#1125 size:L, #1126 size:L; mechanism: compute-release-velocity.sh).
**Result:** SUCCESS — release PR #2488 merged to main at `97310f9457c5ce25f2543dd9a7beed54941ef149`; `v3.23` tag points at the merge SHA, created signed-annotated via the governed claim mechanism; #1125 + #1126 auto-closed at merge; epic #1106 stays open.
**Outcome:** ATTAINED — post-merge `main` exhibits the Release Outcome Statement AFTER state: the `health-check` skill is on `main` and deployed (`grep -c '^name: health-check' operations/skills/health-check/SKILL.md` = 1; mirror + `.skill` package verified current at `deploy.sh --check`), the `/health-check` command and the registry catalog row are present, ADR-051 is `Accepted` on `main` (and is the only ADR this release adds), and all seven modes are implemented on one shared contract (the five fixed output sections + `TRACKER_UPDATES:` + S0–S3 bands). Reversibility CHEAP / HIGH — additive net-new skill + one ADR; single-PR `git revert -m 1` of #2488 removes the whole surface; the skill only reads sources and stages recommendations, so running it cannot mutate project state.

#### Deployment Log v3.20
**Files deployed:** Single release PR #496 merge (shared-release-PR append model, D-C SINGLE). Release-corpus verification surface — the two corpus tools rewired to the live `core/`/`release/` layout with their deploy-checks made fail-loud: `core/deploy/tools/lint_release_corpus.py` (Check 20 now lints the real notes dir; stale cutover-floor exemption removed) and `core/deploy/tools/generate_release_index.py` (Check 23 `--verify` now exits non-zero on path-resolution failure); `core/deploy/deploy.sh` (deploy-check class exits non-zero on path-resolution failure — no more green-on-broken); the two companion link/anchor checkers `core/deploy/tools/check-doc-links.py` and `core/deploy/tools/check-version-anchors.py`; `release/references/standards/release-corpus-schema.md` and `core/rules/doc-link-maintenance.md` + `core/rules/skill-deployment.md` doc updates. `release-planner` Mode A heterogeneous-schema parsing hardened via `release/tools/bundle-issues-parser.py` (with the new fixtures `release/tools/tests/conformant_bundleable_sample.json` + `release/tools/tests/test_bundle_issues_parser.py`) reaching the ≥90% combined-clean target on the live conformant-bundleable substrate; `release/skills/release-planner/SKILL.md` updated to match. `release-executor` retargeted to live-taxonomy labels only: `release/skills/release-executor/SKILL.md` + `release/tools/automated-closeout.sh`. Engineering Commit 0 landed the release plan `release/releases/plans/v3/v3.20-release-corpus-verification-surface_RELEASE_PLAN.md`; the PR also carried the prior-release v1.03 RELEASE_LOG/RELEASE_INDEX backfill rows. Rebuilt skill packages `packages/release-executor.skill` + `packages/release-planner.skill`. Corpus close-out (RELEASE_NOTES + LOG `DEPLOYED → VERIFIED` + INDEX + DIGEST + CHANGELOG + Surface 1 Release) via the Stage 13 chore PR.
**Mechanism:** git merge (PR #496 → main) + signed-annotated tag push (Tag-SHA-Direct pattern per stage-12-execute.md § Phase B; ED25519).
**Timestamp:** 2026-06-07 19:17 UTC (PR #496 merge; MERGE_SHA `42656514caa32e8c03bc6ce19c29ff524fecf285`)
**Cycle-Time:** N/A — no gate-outcome/plan-review-go event for v3.20 in pipeline event log; no deployment-status/deploy-skill or deploy-harness event recorded for v3.20 (content/tooling release; mechanism: compute-cycle-time.sh)
**Result:** SUCCESS — release PR #496 merged to main; `v3.20` tag points at the merge SHA `42656514caa32e8c03bc6ce19c29ff524fecf285`, created signed-annotated (ED25519 signature valid; zero signing bypasses). Stage 12 Phase A.6 mergeability CLEAN; Phase B0 zero dependents; primary fast-forwarded to the merge SHA.
**Outcome:** (finalized at Stage 13 VERIFIED) — 5 issues #83 / #85 / #291 / #425 / #459; `novel` release class (D-class re-scope for #83/#85/#291 after the modular-monolith restructure overtook the original ACs), Deep Stage 9 review, 30-day outcome window. #459 (deploy-check fail-loud hardening) pulled in at Stage 4 plan review.

#### Deployment Log v3.19
**Files deployed:** Merge-ahead release — no single release PR; the five backstops landed via four direct-to-main PRs (#588 schema-aware `extract_milestone_slug` + Keychain credential fallback in `release/tools/automated-closeout.sh` and the `.github/workflows/release-tooling-smoke.yml` smoke gate; #591 mandatory outcome-bound close-out text in `release/references/pipeline/stage-13-close.md` + `release/references/how-to/hub-spoke-bridge.md` and `deploy.sh` Check 32 release-corpus-completeness gate in `core/deploy/deploy.sh`; #592 `release/tools/cleanup-orphan-state.sh` stale-ref prune + chore-branch scope + post-apply verify; #598 #581 rework to outcome-bound close mandate per adversarial review). Corpus close-out (RELEASE_LOG `DEPLOYED → VERIFIED` transition + INDEX + DIGEST + RELEASE_NOTES + CHANGELOG) via this Stage 13 chore PR.
**Mechanism:** four `gh pr merge` direct-to-main merges (no Stage 12 release-PR gate); Stage 13 close-out produced via the documented Phase B chore-PR fallback per stage-13-close.md § Phase A8 (the `automated-closeout.sh` single-`--pr` preflight does not fit a multi-PR merge-ahead; the binding is on the output set, not the mechanism). No signed-annotated tag at deploy time — `v3.19` tag + GitHub Release backfilled at the operator authorization gate.
**Timestamp:** 2026-06-03 UTC (PR merges 19:15–19:53 UTC)
**Result:** SUCCESS — all four PRs merged to main with green CI; the repaired close-out automation (`automated-closeout.sh --self-test` + `--check-paths`) validated live on main during this close (capstone). Close-out corpus assembled via the Phase B chore-PR fallback; `deploy.sh --check` Check 32 + the Procedure 7 Step 4 completion-verification table are the satisfying gates.
**Outcome:** SUCCESS — all five issues' acceptance criteria MET (PASS per issue; see the Stage 13 close-out verification). The milestone hardens Stage 13 close-out reliability: a mechanical completeness gate (#576), an outcome-bound mandatory-close mandate + documented merge-ahead path (#581, reworked by #598), a CI smoke gate so the close-out tooling cannot rot unexercised (#583), schema-aware slug resolution + locked-Keychain graceful degradation (#584), and a working `--apply` removal path with chore-branch scope + stale-ref prune + post-apply verify (#556). This release is the first reflexive consumer of its own repaired close-out automation, closed on its own merge-ahead path.
