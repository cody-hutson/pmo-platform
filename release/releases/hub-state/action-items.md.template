---
schema_version: "v1.0"
milestone: "<milestone-slug>"
created_at: "<ISO 8601 UTC of first row enqueue>"
last_updated: "<ISO 8601 UTC of most recent row mutation>"
last_session_id: "<worktree>__<ISO-start>__<short-sha>"
---

# Action Items — <milestone-slug>

<!--
Hub-tracked action-item ledger (AI-NNN).
Canonical schema: core/standards/hub-action-tracking.md § 2.

CLASSIFICATION (per core/standards/public-repo-vs-operator-instance-taxonomy.md):
  THIS .template file is CUSTOMIZABLE-PUBLIC (tracked in pmo-platform).
  The RUNTIME INSTANCE the hub writes to is OPERATOR-INSTANCE — it lives at
  <OPERATOR_INSTANCE_HUB_STATE_PATH>/<milestone-slug>/action-items.md and is NOT
  git-tracked. Reason: action items mutate on every routing decision and
  every resolution event; carry-forward semantics live in the
  hub-action-tracking standard, not in the runtime substrate file itself.

HUB INSTALL CONTRACT:
  On first action-item emit for a release, the hub copies this template
  to <OPERATOR_INSTANCE_HUB_STATE_PATH>/<milestone-slug>/action-items.md (creating
  the per-release directory lazily, keyed on the milestone slug),
  substitutes the milestone slug into the frontmatter, and appends the
  first AI-NNN row.

HARD GATE: per core/standards/hub-action-tracking.md, Stage 13 Close fails
if any row remains status=open at release close. Cross-release carry-forward
requires an explicit `superseded` transition with a successor AI-NNN in the
next release's action-items.md — implicit carry-forward is prohibited.

TABLE BELOW IS APPEND-ONLY. Rows mutate IN-PLACE only to update status +
resolved_at + resolution. Field semantics per the canonical schema spec.

The three enums below are RESTATED from the canonical schema, not authored
here — they ship into every runtime ledger so a reader can pick a conformant
value without the corpus open. The standard is the derived-from side: edit
core/standards/hub-action-tracking.md first, then re-derive these lines.
Held in parity by deploy.sh --check Check 68 (enum-parity), which registers
each (template, field) pair against its owning section.

status enum: open / in-flight / done / cancelled / superseded
category enum: deferred-edit / reminder / cleanup / decision-to-post /
               cross-issue-merge / verification / decision-deferred
trigger_type enum: stage-boundary / event / time / cross-issue-merge
-->

| id | created_at | source_stage | source_sub_task | category | owner | description | trigger_type | trigger_detail | target | status | resolved_at | resolution |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
