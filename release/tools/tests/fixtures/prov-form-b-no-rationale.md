---
milestone: prov-fixture-form-b-no-rationale
release_class: novel
---

# prov-form-b-no-rationale — FIXTURE (provenance-survival)

Case **P8** — Form B with **no** `rationale:` sibling, the negative arm paired
with `prov-form-b.md`.

`UNSOURCED-DOMAIN` with no rationale is a flag that announces a gap and then
declines to say what the gap is. It satisfies presence and the date and domain
limbs, so only the rationale limb can catch it — which is the point of grading
four limbs separately and naming the one that failed rather than emitting a bare
non-conformant verdict.

### Domain Practice Provenance

domain_practice: { source: UNSOURCED-DOMAIN, date: 2026-08-24, domain: process }

## File Change Matrix

```
#### Read-only inputs
release/tools/verify-release-plan.sh                                        READ
```
