# FIXTURE (test asset, not executed) — a file that does NOT hard-code the old notes-dir path.
# The structural blast-radius mode MUST NOT flag this file (negative control: no false positive).
SOME_OTHER_DIR="${REPO_ROOT}/release/releases/plans"
