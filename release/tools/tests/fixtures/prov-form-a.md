---
milestone: prov-fixture-form-a
release_class: novel
---

# prov-form-a — FIXTURE (provenance-survival)

Case **P7-A** — the SOURCED form, and the sensitivity arm for the Form-A limb.

The `source:` value is a repo-relative path ending in a tracked extension,
followed by a section anchor. That trailing-qualifier shape is the one the plan
corpus actually authors, so accepting it is a real requirement and not a
courtesy: the single Form-A value in the corpus at introduction was
`core/standards/template-taxonomy.md §6 …`. Form A carries the `name:` companion.

### Domain Practice Provenance

domain_practice: { source: core/standards/domain-best-practices/governance.md §2 internal-deliverable practice, date: 2026-08-24, name: platform-internal-deliverable-practice, domain: governance }

## File Change Matrix

```
#### Read-only inputs
release/tools/verify-release-plan.sh                                        READ
```
