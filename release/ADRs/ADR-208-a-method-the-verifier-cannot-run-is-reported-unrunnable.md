---
title: "ADR-208 — A method the plan verifier cannot run is reported UNRUNNABLE: a fifth verdict that does not fail the run, and the release's one outcome partition"
status: Accepted (rendered by the operator at the Stage-5 D-6848 gates as decisions D21–D24, with the Collective Review's partition and single-record decisions D37 and D47; this file records them)
date: 2026-09-25
release: verifier-grades-what-plans-declare
deciders: "Workspace owner (operator), rendering the Stage-5 D-6848 gates as decisions D21–D24 against the Stage-5 design and its Phase A6.5 adversarial review, whose command-shape counter-design and scope guards the operator adopted with the gates, and the Collective Review scope-lock as decisions D37 (the outcome partition) and D47 (this file as the release's single partition record), and the Stage-5 D-6854 gates as decisions D26–D28 (Decisions 9–11) with the review fixes the operator adopted with them, and the Collective Review's D38 for the per-issue guard and the reader-semantics table, and the Stage-5 D-6685 gates as decisions D31–D32 (Decision 12) with the review fixes the operator adopted with them, and the Stage-5 D-6236 gate as decision D25 (Decisions 13–16) with the review's counter-designs and text fixes the operator adopted with it and the Collective Review's D37, D38 and D46 lines for that card, and the operator's Stage-6 decision D55 (Decision 17) + Stage 5 Solutioning spokes (Principal Engineer — Architecture Assessment) + the Stage 6 Engineering spokes that recorded them"
tags: [architecture, release-pipeline, verify-release-plan, verdict-enum, unrunnable, outcome-partition, scope-family, command-shape, shell-operator, schema-version, amends-adr-075, ciac-authoring-lint, qc3-5-reading]
supersedes: none
source_observations:
  - "At the Stage-5 pin, 206 rows declined as tool invocations graded SKIP beside 36 declared deferrals, and the declined 'verb' was the method's first bare word: it named 105 distinct things across the corpus, mostly labels, file names, hashes and words. A failing UNRUNNABLE would have flipped 8 otherwise-clean plans from exit 0 to exit 3 by relabel alone, and a second measurement on a different mask flipped 25. Measured by the Stage-5 design and reproduced by its Phase A6.5 review."
  - "The review found that at least 29 of the 249 rows the design's first form named named a mention rather than an invoked command, 17 of them created by the residual step. Under the invocation-shape test the operator adopted (D22, D24), UNRUNNABLE rows fell from 249 to 211 and ERROR-to-UNRUNNABLE moves from 119 to 98, with 0 new FAIL, ERROR, PASS or executed rows and 0 plans moving from exit 0 to exit 3; about 17 genuine single-token script invocations lose their name. Measured by the review's replica over 1,438 parsed rows."
  - "Re-measured at Stage 6 on the step that lands this record, by a record-stream differential of the previous step's executor against this step's, in one stub root with the deploy check stubbed, over the release branch's 217 plans and 50 test-fixture files. 468 plan records change and none newly reads PASS: 115 tool declines move from SKIP to UNRUNNABLE, 16 of them under a different name (11 had named an identifier), and 3 whole-method script rows are newly named; 101 refusals that named an identifier (91) or a one-token mention (10) become the no-command SKIP; 98 rows move from an unclassified ERROR to UNRUNNABLE naming the tool their command invokes; 84 partially run rows move from the SKIP slot to UNRUNNABLE, 22 more reach it because a tool span is now a command, and 9 keep FAIL or ERROR and now name it; 12 rows whose designated command carries shell syntax read UNRUNNABLE naming the operator, 11 of them from ERROR and 1 from PASS. The other 24 are the deploy-route rows that the sibling record on how a method cell declares its route governs (its Decision 7). 6 plans stop failing the masked measure (FAIL or ERROR outside the deploy, delivery and provenance families): v3.70, v3.96, v4.01, v4.06, v4.21 and v4.67.1. None starts."
  - "The named-tool census at the same step: of the 2,587 records the 217 plans emit, 263 name a declined tool, with 38 distinct names — 19 catalog words and 19 script basenames — and 0 identifiers. The previous step's executor, over the same records, names 102 identifier mentions (92 distinct: file names, labels, function names and commit hashes), so the census discriminates."
  - "The script-name cost at the same step: 78 plan records carry a one-token script span. 26 of them name a tool; 28 name nothing and read an unclassified ERROR (22) or the no-command SKIP (6). A hand read classes 14 of those 28 as clear invocations (for example 'Run `check-label-parity.py` against a fixture' and 'through `test-runner.sh`'), 11 as mentions and 3 as unclear — the cost the review estimated at about 17."
  - "No historical plan row is a scope assertion: at the Stage-5 pin no corpus `git diff` span carried a comparator, and at the Stage-6 step no plan row moves into the scope family. Its verdicts, and each of its guards, are exercised by the release's own suite fixture."
  - "Decision 9, measured by the Stage-5 D-6854 design and reproduced by its review: at the pin 501 per-issue rows in 55 plans read an unclassified ERROR, each read and none inspected by a handler, and a command-less row that happened to carry a keyword such as 'present' or 'grep' already read the no-command SKIP. Re-measured at Stage 6 on the step that adds Decisions 9 to 11, by a record-stream differential of the previous step's executor against this step's in one stub root over the release branch's 217 plans and 51 test-fixture files: 429 plan rows in 53 plans reached the residual, and after it 428 read the no-command SKIP and 1, a pipeline, reads UNRUNNABLE naming its operator; none executes and none is left unclassified. 407 of the 429 are the pin's rows the design predicted; 8 are rows the loop-isolation step recovered, 7 are command-less rows the declared deploy route released, and 6 sit in a plan added after the pin. The per-issue guard moves 1 more plan row, ERROR to the no-command SKIP."
  - "Decision 9's stated residual, measured at the same step over the masked measure (FAIL or ERROR outside the deploy, delivery and provenance families): 34 plans stop failing — the 27 the D26 record named, 1 more through the per-issue guard, and 6 whose other pin-failing rows earlier steps of the release cleared — and 1 starts, through Decision 11's operand rule, on a lone `test -f` that read PASS. 438 plan and fixture rows move from FAIL or ERROR to SKIP or UNRUNNABLE, every one through Decision 9 on a byte-identical method cell."
  - "Decision 10, measured by the Stage-5 D-6854 design and its review, and by the presenter defect's report: at the pin the roll-up's per-issue count disagreed with the population its counters span on 214 of 215 plans, and the markdown block did not show 186 records with no issue value in 22 plans. At the Stage-6 step the previous executor's markdown block showed fewer rows than it counted on 25 of 268 inputs, and this step's on 0; over all 268 the five counters sum to the records and the three SKIP reasons to the SKIPs. The per-issue, cross-issue and always-on populations sum to the records on every plan; the one input where they do not carries a table-unindexable block record, which belongs to none of the three."
  - "Decision 11, measured by the Stage-5 D-6854 design and its review at the pin: a missing file under head, cat, wc or ls read PASS count=0, two corpus rows ran a lone `test -f` and read PASS, and cat or ls with no comparator read PASS on a claim nobody evaluated. At the Stage-6 step the table moves 12 plan records: 7 pattern-less greps change their reason to no-operand (ERROR both before and after), 3 lone `test -f` rows read ERROR (1 from PASS, 2 from the partially run outcome), 1 ls on a missing path reads ERROR from FAIL, and 1 cat with a textual expectation reads ERROR from PASS."
  - "Decision 12, measured by the Stage-5 D-6685 design and reproduced by its Phase A6.5 review at the pin: the three documented-decision rows the card cites, on the plan it cites, read an unclassified ERROR while that plan's own documented-decision CIAC read its named SKIP; one documented-decision method took 8 dispositions across 8 wordings; the per-issue no-command SKIP was emitted by 30 corpus rows in 14 plans and its spelling had no reference outside the executor; and none of the 7 CIAC records in the suite's fixtures carried the documented-decision shape, so the card's 'existing CIAC-1-shaped fixture' was absent."
  - "Decision 12, re-measured at Stage 6 on the step that adds it, which changes no executor byte and so moves no record: the three cited rows read the per-issue no-command SKIP, and the cited plan's CIAC-1 the documented-decision SKIP. A documented-decision fixture reads the declared-deferred SKIP on its two declared rows, the no-command SKIP on its undeclared row and the documented-decision SKIP on its CIAC, and exits 0. Its malformed-method control twin reads UNRUNNABLE on both the keyword route and the residual route, because the handler refuses an unterminated quote before it runs, and ERROR no-operand on the padded test primary. The verb-initial wording a Stage-5 review found read as a command (1 per-issue row and 7 cross-issue criteria) reads the no-command SKIP on that per-issue row and on 4 of 4 per-issue twins; the 7 cross-issue criteria still read ERROR (3) or UNRUNNABLE (4), because the cross-issue handler keeps the bare-string reading a table-form criterion needs."
  - "Decisions 13 to 16, measured by the Stage-5 D-6236 design and reproduced by its Phase A6.5 review at the pin: 303 of 374 emitted cross-issue records read SKIP — 139 refusing a tool, 162 carrying no runnable command, and 2 declaring a deferral; 8 were graded on a span the author never designated, 3 of them PASS on prose; one plan declared 2 criteria the parser never read; and 25 historical plans exited 0 on every family a stub root can evaluate while declining through a refused tool, which a failing can't-run outcome would have flipped to exit 3."
  - "The review's finding on a shape-only lint, at the same pin: 13 of the 374 were count commands graded on their exit status, and 4 of those passed every shape rule the design's first lint applied and were misgraded — three zero-expectations that held read FAIL, and a count below its bound read PASS. The lint that ships reads the grader's own comparator, and those four shapes are its regression fixture."
  - "Re-measured at Stage 6 on the step that adds Decisions 13 to 17, by a record-stream differential of the previous step's executor against this step's in one stub root, over the release branch's 217 plans and 53 test-fixture files: 3 records change and no exit code does. Two are Decision 17's, the malformed-method control twin's unterminated-quote rows, UNRUNNABLE to ERROR on a fixture that already failed the run; one is a count of the executor's own lines, which grows and stays PASS. The span-closure rule the same step applies moves 0 records."
  - "The lint at the same step, over the 217 plans: 392 cross-issue entries read 37 CLEAN and 355 flagged, and 2 plans lint clean with at least one entry. A lint pass over the 217 delegates nothing: the stub's deploy check logged 15 calls during a normal run over the corpus that holds them, and 0 more during the lint pass."
  - "Decision 17's population, at the same step: of the 12 plan rows whose designated command the handlers' refusal read as shell syntax at the step that introduced it, 0 carried an unterminated quote. Each names a pipe (5), a list (4) or a redirect (3) and stays UNRUNNABLE; the only quote-driven rows in the record stream are the control twin's two."
  - "Consequences of Decisions 13 to 17, carried here because an Accepted record's Consequences are closed to in-place edits: a declared criterion is graded by the operator at Phase C from its named evidence, so the declared route is no NO-GO input before its grade can exist; a flagged criterion nobody repairs reads NOT MET at Stage 9 unless overridden; and a plan whose designated command carries an unterminated quote exits 3 again, as it did before the handlers' refusal."
---

# ADR-208 — A method the plan verifier cannot run is reported UNRUNNABLE: a fifth verdict that does not fail the run, and the release's one outcome partition

## Status

**Accepted.** Rendered by the operator at the Stage-5 D-6848 gates as decisions D21–D24, which adopted the Phase A6.5 review's command-shape counter-design and its scope guards. The Collective Review then reconciled the release's outcome vocabulary into one partition (D37) and made this file the release's single partition record (D47). Recorded at Stage 6 Engineering for the `verifier-grades-what-plans-declare` release, in the slice that implements Decisions 1–7. Decision 8 is the Decision line of the card whose multi-command reading the partition's partially run row rests on (D29), carried from that card's slice. Decisions 9–11 are the Decision lines of the card that gives a row no family claims its named SKIP and states what the roll-up and each reader mean (D26–D28), with D38's per-issue guard and reader-semantics table, which the Collective Review placed in that card's slice; they were added in that slice. Decision 12 is the Decision line of the card that asked for a documented-decision method on a per-issue row to reach the named SKIP its cross-issue form already had (D31–D32); it adds no executor behaviour, and was added in that card's slice. Decisions 13–16 are the Decision lines of the card that makes a cross-issue criterion gradable or declared at authoring and gives Stage 9 one reading of every emitted outcome (D25, with the review's counter-designs and text fixes, and the Collective Review's D37, D38 and D46 lines for that card); the Collective Review placed them here rather than in a record of their own (D47). Decision 17 is the operator's Stage-6 ruling on an unterminated quote in a designated command (D55). Both were added in that card's slice, the release's last.

**Amends ADR-075 decision 5.** This record contests nothing ADR-075 decided. Decision 5 made the emitted evidence, its verdict enum included, a versioned contract, so that a change to it is a detectable schema bump rather than a silent break. This record is such a change: the enum gains UNRUNNABLE under the release's one `SCHEMA_VERSION` bump, and the exit rule counts UNRUNNABLE with PASS and SKIP.

**Numbering provenance.** Claimed as **208** against an anchor of **206** on the mainline, read from the repository's own ADR-numbering tool at authoring, with **207** already held on this release's branch by the record {{ADR:a-rows-grading-route-is-declared-in-its-method-cell}}. The number binds at the Stage-12 claim, and in-release prose cites this record by its slug token.

## Context

The plan verifier runs only a closed set of read-only verbs, and that closure is a security boundary. A method needing any other tool was reported SKIP, the same verdict a plan uses to declare a row another runner's job, so a plan whose most rigorous criterion never ran read like one that deferred a judgement call. The refusal named its "tool" from the method's first bare word, and so named labels, file names and hashes as readily as tools. A `git diff` scope assertion had no family at all.

Several of the release's cards each designed a part of one outcome vocabulary: the can't-run verdict, the partially run row, the method with nothing to run, and the reading Stage 9 gives each. The Collective Review reconciled them into the one partition this record carries. The measurements behind every decision are in `source_observations`.

## Decision Drivers

- Keep `RUNNABLE_VERBS` untouched: it is the security boundary.
- No historical plan may flip from exit 0 to exit 3 by relabel alone. As of the D21 decision, a failing token would have flipped 8 otherwise-clean plans.
- Every reader keys on the verdict value: the exit predicate, QC3.5, G-PR10 and the roll-up.
- A refusal names a real tool, never a label, file name, hash or word the method merely mentions.
- One vocabulary: an outcome means the same thing to the executor, to its roll-up and to every gate that reads it.

## Decision

1. **UNRUNNABLE is a fifth verdict value** (can't-run-here): the row is read, but a command it names cannot be run here. It is neither a named SKIP (not this runner's job, or nothing to run) nor ERROR (could not read).
2. **It does not fail the run.** The exit stays non-zero only on FAIL or ERROR. A roll-up counter and a stderr note make it visible.
3. **The declined tool is named only from an invocation-shaped span** (D22, D24).
   - A span qualifies when it has two or more tokens, is a bare interpreter, or is the whole method; shell keywords and builtins also need an argument.
   - The name comes from a closed catalog plus the script shape. A backticked identifier or a mention is never named as a tool, and an identifier stops the surrounding prose from being run as a bare command.
   - A designated command that carries shell syntax outside quotes — a pipe, a list, a redirect or a substitution — is UNRUNNABLE as well, naming the operator: the executor runs no shell, so the syntax would reach the verb as literal arguments. One shared quote-aware predicate decides it for the router, the handlers and the scope step (D38).
4. **An otherwise-unclassified row carrying an invocation-shaped recognised command is UNRUNNABLE.**
5. **A native scope family** grades `git diff --name-only <release range> -- <pathspec>…` against the release diff: every change the release makes, across all of its cards (D23).
   - It reuses the fcm-delivery family's fixed git call and matches pathspecs in-process, so no plan-authored byte reaches git.
   - It accepts exactly one comparator, read through the shared comparator vocabulary, and refuses a `<placeholder>` pathspec.
   - For `==` and `<=` assertions, every included pathspec must select at least one existing or changed path.
   - An empty or unresolvable diff, an ambiguous comparator, a placeholder, or a pathspec that selects nothing is UNRUNNABLE, never a pass.
6. **The new enum value contributes to the release's single `SCHEMA_VERSION` bump, 4 → 5** (D40). The bump lands with the release's first executor change; this record's slice is a later contributor and adds no second bump.
7. **The release's outcomes form one partition** (D37), stated in the table below. QC3.5 reads UNRUNNABLE, a partially run row included, as NOT MET (unverified). An unexecuted command is reported as "did not run (reason)". There is no separate partial outcome: a row whose other commands did not run is unverified, which is what can't-run-here already says.
8. A method that names more than one command is graded on its designated command — the first allowlisted verb that carries an argument — against the comparator written after that command. Every other command it names is reported as "did not run (reason)", and the row is never PASS: a FAIL or ERROR from the designated command stands, and a designated PASS takes the can't-run outcome (UNRUNNABLE), naming the command that ran and each one that did not. A bare verb (a tool named in prose, with no argument) is never a command, and the further commands are not run.
9. **A per-issue row no family arm claims is inspected by the per-issue handler, never reported unreadable** (D26). The classifier's residual names the per-issue family: its handler runs a runnable command, declines a tool by name, or reads a method that names nothing to run — a named read, a judge-rubric, a described behaviour — as the no-command SKIP, however it is worded and whether or not a keyword routed it. The handler runs only a backticked span, so prose that merely opens with an allowlisted verb is a named read and never runs as a command (D38's per-issue guard). ERROR is reserved for a row the executor tried to evaluate and could not, and a family no handler owns is an internal inconsistency, never an unreadable row.
   - **The fail-closed sign-off (DP-8, default EXPENSIVE), given at the D26 gate with its residual stated.** A command-less row stops failing the run, so a plan whose only failing rows were command-less now exits 0 (as of the D26 decision, 27 plans on the pinned corpus). A row whose command fails can be rewritten as prose and stop failing: possible before through a routing keyword, and under this decision without one. The criterion stays fail-closed where it is graded — at Stage 8 per criterion, and at QC3.5, which reads a SKIP as not a pass — and the Collective Review made the rewrite detectable at every release's Stage 7, where a row that moves from FAIL or ERROR to SKIP or UNRUNNABLE is a finding (D46).
10. **The verdict roll-up states the population its counters span, and keeps what the run did not grade apart from what it could not evaluate** (D27). One line: `P PASS / F FAIL / S SKIP / U UNRUNNABLE / E ERROR over R record(s) (N per-issue row(s), C cross-issue, A always-on) — not graded by this run: S SKIP (D declared-deferred, X no command in method, O other) and U UNRUNNABLE; could not evaluate: E ERROR`, where P+F+S+U+E = R and D+X+O = S. The JSON roll-up mirrors it, and one reader feeds both presenters and the stderr note. The markdown block shows a record that carries no issue value under a `(plan)` header, so every record the roll-up counts appears in a table. It is a later contributor to the release's single `SCHEMA_VERSION` bump (D40) and adds no second bump.
11. **What an allowlisted verb's result means is stated once, in one per-verb reader table, and every reader asks it** (D28, with the review's operand and exit-0 rules; D38's reader-semantics table).
   - Exit 1 is a legitimate zero only for `grep` (no match) and `test` (false); for `ls`, `head`, `wc` and `cat` it is an unreadable operand, ERROR `matcher-exit-1`.
   - A command that names no operand its verb needs — `grep` with no pattern, `test` with no expression or a unary primary with no operand, `ls` with no path — is refused before it runs, ERROR `no-operand:<verb>`; `head`, `wc` and `cat` with no file keep the stdin refusal, which comes first.
   - With no comparator, exit 0 is the claim only for `grep`, `test`, and an `ls` naming files, which is an existence check; `cat`, `head`, `wc` and an `ls` listing a directory read ERROR `no-comparator:<verb>`, because the executor tried to evaluate the claim and could not.
   - A count is read as the table says: `grep`'s count flag, `wc`'s first field, otherwise the output's lines.
   - A seventh verb, or a flag that changes a verb's reading, is a row or a cell in that table, never a point rule in one reader.
12. **A documented-decision method reads a named SKIP on a per-issue row as it does on a cross-issue criterion, named by the route it takes** (D31–D32). A named read of a named surface, with no command this executor runs:
   - declared in its method cell, reads the declared-deferred SKIP, by the declaration form of the sibling record {{ADR:a-rows-grading-route-is-declared-in-its-method-cell}};
   - left undeclared on a per-issue row, reads the no-command SKIP, through the residual (Decision 9);
   - on a cross-issue criterion, keeps the documented-decision SKIP, byte-identical.

   No token is coined and none is renamed: one partition slot carries both no-command spellings, and the roll-up counts them in one clause (Decision 10). The parity needs no family, hatch or executor line of its own. A genuinely malformed per-issue method is never that SKIP, on the keyword route or the residual route, so the parity is not reached by making every row the executor does not run a SKIP.
13. **A cross-issue criterion is authored gradable, as designated, by its sole runner, or in the declared form naming its evidence surface, and the executor's own lint checks which at authoring** (D25). The lint (`--ciac-lint`, Gate 4 criterion G4-06) reads the criteria through the grading path itself and stops before execution: the grading parser, the one span splitter, the limb reader, the designated-command pick, the verb check, the shared quote-aware shell-syntax predicate, the stdin and operand refusals, and the comparator the grader will apply, read through the one comparator vocabulary (the review's counter-design, adopted with D25).
   - It runs no command, reads no deploy check, writes no event and emits no evidence record.
   - It reads a deferral outside every span led by an allowlisted verb, as the executor does (D38), and a span no backtick closes as prose, as the handlers do.
   - It prints the plan's declared set from an id scan independent of the parser, so a criterion the parser never reads is flagged rather than absent.
   - It is read at three points: by the hub at Gate 1, at Stage 6 entry on the committed plan, and at Stage 9 Phase A3.6 against the final head.
   - A criterion naming more than one command is flagged: only its designated command runs (Decision 8).
14. **QA Checkpoint 3.5 reads every emitted outcome into the Stage-8 per-criterion enum through one table, and adds no verdict value to either enum** (D25, D37). The table in `release-process.md` is the single home of that reading; the Stage-4, Stage-9 and G-PR10 texts cite it and restate none of its rows. A declared criterion whose declaration names its evidence surface is *awaiting operator grade* at Phase A3.6; its evidence is presented at Phase B as an operator action, and G-PR10 is graded at Phase C over the emitted readings and the operator's recorded grades. An undeclared decline — the can't-run outcome, a partially run criterion included, and any other SKIP — reads NOT MET (unverified), and a DEGRADED run is re-emitted before any criterion in it is read.
15. **A declaration names where its guarantee can be read, and one issue's criterion is not evidence for a predicate over several** (D25). The evidence surface is a repository path, a suite arm label, or a namespace-qualified criterion for each issue the criterion spans, in the reference forms of the sibling record {{ADR:acceptance-verdicts-name-their-criterion-namespace}}; a path or an arm label covers the release. A reason alone names no surface: a per-issue row may defer with its reason, but a cross-issue declaration that names no surface reads NOT MET.
16. **A decline stays outside the exit-failing set, whatever it is named, and the single-runner rule and the closed verb set are retained** (D25). Renaming declined rows must not turn a plan's exit 0 into 3: a SKIP, an UNRUNNABLE and any later can't-run-here value is weighed at Stage 9, not at the exit. A family that newly executes a row it used to decline grades PASS or FAIL only where it can establish the evaluation context the method presumes, and otherwise emits the can't-run outcome. The executor remains the sole runner of every criterion; the lint and the operator's grade add no second runner.
17. **An unterminated quote in a designated command reads ERROR, naming the quote, and fails the run** (D55). The command cannot be split into the words its author meant, which is input the executor could not read. A shell operator outside quotes — a pipe, a list, a redirect or a substitution — stays UNRUNNABLE, naming the operator, as Decision 3 states. The shared quote-aware predicate is unchanged: only the verdict the handlers give a quote it reports changes, on every route the refusal reaches — the per-issue handler, the cross-issue handler and the designated command of a method naming several — and the lint flags it by the same token.

**The partition (Decision 7).**

| Outcome | Verdict | What the executor found | Fails the run | Typical `observed` text |
|---|---|---|---|---|
| Met | PASS | The designated command ran and met its comparator | No | `count=N (op want)`, `command-succeeded` |
| Not met | FAIL | The designated command ran and did not meet its comparator | Yes | `count=N (op want)`, `command-exit-N` |
| Not this runner's job | SKIP | The method declares another runner | No | `declared-deferred` |
| Nothing to run | SKIP | The method names no command | No | `no-executable-command-in-method`, `documented-decision-method (…)` |
| Can't run here | UNRUNNABLE | The method names a command this executor will not run, or a scope input that is vacuous or mis-bound | No | `tool-invocation-outside-executor-allowlist:<tool>`, `shell-operator:<op>`, `scope-diff-empty`, `scope-pathspec-selects-nothing` |
| Partially run | UNRUNNABLE | The designated command passed, and another command the method names did not run | No | `partial-execution: limbs run 1 of N: …` |
| Could not read | ERROR | The executor could not read or evaluate the row | Yes | `count-unreadable:…`, `stdin-reader:<verb>`, `no-operand:<verb>`, `no-comparator:<verb>`, `unterminated-quote:<q>`, `method-cell-empty` |

A designated command that fails keeps FAIL or ERROR whatever else the method names: only a designated PASS becomes the partially run outcome.

## Alternatives Considered

- **Keep SKIP and distinguish by reason.** The strongest alternative, because it needs no enum change. Not chosen: every reader keys on the verdict.
- **UNRUNNABLE that fails the run, or an opt-in failing flag.** Rejected: the first turns historical plans red by relabel alone, and the second is off unless asked for.
- **ERROR for a command the executor will not run.** Rejected: ERROR means the input could not be read, and these rows were read.
- **Name the tool lexically, or by a PATH lookup.** Rejected: lexical naming admits English words, and a PATH lookup grades differently per machine.
- **Add `git` to the verb set, or validate the author's git command.** Rejected: either opens an authored-command channel into the tool that grades the release's own plan.
- **A declared scope grammar.** Rejected: a third in-method grammar beside the declared-deferred form and the command.
- **Name a tool from any backticked span whose first token is a catalog word or a script path** (the Stage-5 design's own form, before its review). Rejected, as of the D22/D24 decision: at least 29 of the rows it named named a mention, not a command.
- **A separate PARTIAL outcome for a partially run row.** Dropped at the Collective Review (D37): the row is unverified, which the can't-run outcome already states.
- **Grade a per-issue scope row on its own card's commits** (the review's per-card scope counter-design). Not taken beyond the minimum (D44): the family grades the release diff, and a claim about one card's own changes is not a scope row.
- **Declared scope only: a row no family claims stays ERROR, and the roll-up splits ERROR into "no route" and "could not read"** (the strongest alternative to Decision 9, because it keeps such a row failing closed). Not chosen (D26): ERROR would keep two meanings, the run would fail on method forms Stage 4 admits, and whether the row read SKIP or ERROR would still turn on a keyword in its prose.
- **A separate family for the residual, a residual read as UNRUNNABLE, an authoring lint alone, or the class column wired as the residual.** Rejected at the D26 gate: the first is a second copy of the per-issue handler's inspection; the second names a tool, and prose names none; the third leaves the executor's verdict wrong; the fourth reopens the route the sibling record removed.
- **A roll-up that appends the no-command count to the old line, or a second line** (the alternatives to Decision 10). Not chosen (D27): the old line's per-issue count misstated the population its counters span, and two lines are two anchors for one fact.
- **SKIP `no-comparator:<verb>` for a command whose exit status is not its claim.** Not chosen: the command ran, so the row is neither another runner's job nor a method with nothing to run; the executor tried to evaluate it and could not, which is ERROR.
- **The operand rule as a token count** (a verb given no other word). Not chosen (D28, with the review's operand rule): `test -f` alone has two words and read PASS on any path.
- **The per-issue row emits the cross-issue spelling, or one new token names both** (the alternatives to Decision 12's naming). Not chosen (D32): the cross-issue spelling must stay byte-identical, a rename would move the observed text of every command-less row, and "documented-decision" over-claims for a judge-rubric, a described behaviour or an identifier-only row.
- **A documented-decision keyword arm, a separate family, or command-less per-issue rows sent to the cross-issue handler** (the alternatives to Decision 12's route). Rejected at the D-6685 gate: a keyword arm is a per-family hatch that defers the next non-machine class; a family duplicates a disposition the per-issue handler already emits; and the cross-issue family is the sole runner of cross-issue criteria, which Stage 9 reads.
- **A strict route: no declared form, every cross-issue outcome but PASS dischargeable only by override** (the strongest alternative to Decisions 13–15, because Stage 9 would stay purely executor-driven). Not chosen (D25): a predicate no command can grade could then only be dropped or overridden, and overrides as the normal path erode the gate.
- **A permissive route: every decline operator-graded, with no declaration at authoring**, and **a dynamic lint that executes each criterion**. Not chosen (D25): the first grades an undeclared decline as if it had been planned; the second flags files the release has not added yet, and its verdicts depend on the shell version.
- **A prose-only rule, an always-on gradability record, a standalone linter script, a gate-side reclassification of declines, a new verdict value for a guarantee held elsewhere, or a runner registry** (the D-6236 design's other candidates). Rejected on a constraint each breaks: the card asks for a check at authoring, not prose alone; a new record source owes a schema bump and rejects nothing; a separate script cannot share the grading path's readers; reclassifying at the gate moves the fix away from authoring; a new value changes an enum other stages consume, where the existing values suffice; and a registry is a third declaration grammar that routes cross-issue criteria to per-issue grading, the category error the cross-issue check's own record rejects.
- **A lint that judges a criterion by its shape** (the design's first lint). Replaced by the review's counter-design (D25): four criteria that passed every shape rule were misgraded, because the shape rules never read the comparator the grader applies.
- **Reading a declared criterion's pending grade as NOT MET at Phase A3.6.** Replaced by the review's counter-design (D25): the phase renders before the grade can exist, so the declared route would be a structural NO-GO input.
- **Accepting one issue's criterion as evidence for a predicate over several.** Rejected (D25): it is the category error of grading a release-scoped predicate on one issue, reached through the evidence route instead of the grading route.
- **Extending the lint to per-issue rows.** Not taken (D46): the laundering guard at Stage 7 already makes a failing row rewritten into a decline a finding.
- **An unterminated quote read as can't-run-here**, the reading the handlers' refusal first gave every shape the shared predicate reports (the alternative to Decision 17). Not chosen (D55): the command was not read as its author wrote it, which is could-not-read, and a malformed method must still fail the run.

## Consequences

- **Positive:**
  - Can't-run-here is visible at the verdict, in the roll-up and on stderr.
  - A named tool is a command the method runs, and a mention is never named.
  - A command that carries shell syntax is never run with that syntax as literal arguments.
  - Scope assertions execute with no code-execution channel, and cannot pass on a vacuous or mis-bound input.
  - Every gate reads one partition: a partially run row, a tool the executor declines and a vacuous scope input all read the same way, as unverified.
- **Negative:**
  - Plans whose only failing rows were readable tool commands now exit 0, with those rows counted as UNRUNNABLE. No gate reads per-issue verdicts today: Stage 7 keys on the exit status and Stage 8 grades each criterion independently. This residual is stated, not hidden.
  - About 17 genuine single-token script invocations lose their name under the invocation-shape test, as of the D22/D24 decision.
  - A real tool missing from the catalog reads as a method with no command until it is added.

## Reversibility

**MODERATE.** A revert restores the four-value enum, and plans regrade with no data loss. It changes routing and the emitted contract of a tool every release runs, so its effect reaches every plan the tool grades.

Decisions 13–16 are **MODERATE** (D25): a gate reading every release applies, and the lint mode is additive; a revert restores QA Checkpoint 3.5's implicit fail-closed reading. Decision 17 is **CHEAP** (D55): one verdict mapping in the handlers' refusal, and its mirror in the lint.

## Related ADRs

- ADR-075 — the plan-verification executor's shared contract. Amended: decision 5's verdict enum gains UNRUNNABLE under decision 5's own versioning rule, and the family registry gains the scope and unrunnable families.
- ADR-073 — the cross-issue release-integration check at Stage 9. Its single-runner rule is unchanged; Stage 9 reads the emitted UNRUNNABLE as Decision 7 states, and every emitted outcome through the one table Decision 14 names. Its rejection of grading a release-scoped predicate per issue is what Decision 15 applies to evidence.
- {{ADR:acceptance-verdicts-name-their-criterion-namespace}} — the sibling record in this release on criterion namespaces. Its reference forms are the ones a cross-issue declaration uses to name a per-issue criterion as evidence (Decision 15).
- ADR-168 — a verification claim is a named schema column. Untouched.
- ADR-181 — ADR citations bind at the claim, not at authorship. In-release prose cites this record by its slug token.
- {{ADR:a-rows-grading-route-is-declared-in-its-method-cell}} — the sibling record in this release, on how a method cell declares its grading route. Its declared deploy row that also names another command takes this partition's partially run outcome.

## References

- #6848 — the card this record carries (Decisions 1–7).
- #7647 — the ADR issue holding this record's decision set.
- #7606 — the Stage-5 design sub-task, carrying the D21–D24 decision record.
- #7643 — the design's Phase A6.5 adversarial review, whose command-shape counter-design and scope guards D22–D24 adopted.
- #7547 — the Stage-4 planning sub-task, carrying the Collective Review record (D37, D38, D40, D44, D47).
- #7531 — the loop-body isolation the scope family's git call runs inside, and the release's single `SCHEMA_VERSION` bump (D40).
- #6837 and #7599 — the card whose Decision line is Decision 8 (D29), and its Stage-6 slice, which rendered that line.
- #7834 — the round-2 design sub-task, whose D50 record assigned the handlers' shell-operator refusal to this record's slice.
- #6854 — the card whose Decision lines are Decisions 9–11 (D26–D28), with D38's per-issue guard and reader-semantics table.
- #7610 — its Stage-5 design sub-task, carrying the D26–D28 decision record with the DP-8 sign-off and its stated residual.
- #7648 — that design's Phase A6.5 adversarial review, whose operand and exit-0 rules D28 adopted and whose reader-table counter-design D38 took.
- #7665 — the review whose per-issue guard counter-design D38 took, and whose fixes D31 adopted for Decision 12; #7834 carries the hub's correction placing that guard in #6854's slice.
- #7635 — the markdown presenter defect the `(plan)` header in Decision 10 closes.
- #6685 and #7603 — the card whose Decision line is Decision 12 (D31–D32), and its Stage-6 slice, which rendered that line.
- #7602 — its Stage-5 design sub-task, carrying the D31–D32 decision record.
- #6236 and #7623 — the card whose Decision lines are Decisions 13–16 (D25), and its Stage-6 slice, which rendered them with Decision 17; the D55 decision record is on #7623.
- #7622 — its Stage-5 design sub-task, carrying the D25 decision record.
- #7645 — that design's Phase A6.5 adversarial review, whose two counter-designs and three text fixes D25 adopted.
