# Allowlist for universal-vs-localized-context authoring guardrail (deploy.sh Check 25).
#
# Format: one pattern per line. Trailing slash matches directories.
# '#' introduces comments. Empty lines ignored.
#
# Rationale per entry must document WHY the path is allowlisted —
# not WHY the operator wants to silence warnings (anti-pattern per
# core/config/allowlists/skip-doc-link-check.txt convention).
#
# Initial state: empty. Operator adds entries during the warn-mode
# shakedown per the bypass-mode-readiness Shakedown → Enforce
# Transition Checklist. Each added entry MUST document its rationale.
#
# Sister-allowlist precedent: skip-doc-link-check.txt (Check 14 + Check 15).

# ─── DC6 carve-out class seeds ───
# These seeds correspond to the 3 carve-out classes documented in
# core/standards/universal-vs-localized-context.md §10.4.
# They are operator-policy-grade exemptions, NOT individual file silencing.
# The rubric-level carve-out remains the binding decision; these allowlist
# rows are the deploy.sh enforcement equivalent.

# DC6 carve-out 2: Authoritative-standard provenance (per §10.4 row 2)
pmo-platform/reference/standards/release-notes-standard.md

# DC6 carve-out 3: Forward-binding provenance (per §10.4 row 3)
pmo-platform/skills/implementation-planner/SKILL.md

# DC6 carve-out 3: Forward-binding provenance (per §10.4 row 3)
pmo-platform/skills/pmo-skill-refiner/SKILL.md

# Note: DC6 carve-out 1 (Anthropic-owned URLs — *.claude.com, github.com/anthropics/*,
# anthropic.com) is enforced rubric-wide per §10.4 row 1 (structural runtime
# dependency). It is NOT a per-file allowlist entry — every K1 file that cites
# an Anthropic-owned URL passes DC6 by carve-out regardless of allowlist. Adding
# individual files for this class would be anti-pattern (governance debt +
# duplication of rubric-level decision).
