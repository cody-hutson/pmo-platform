# reference-durability-allowlist.txt
#
# Path allowlist for block-fragile-refs.sh + the reference-durability deploy-check
# check + the reference-durability CI workflow. One glob per line. A leading '#'
# introduces a comment. A trailing slash matches a directory (prefix match). All
# other lines are file/glob patterns matched against the incoming file path.
#
# Allowlist entries document WHY a path is exempt — the structural reason the path
# legitimately carries flagged constructs (links, version apparatus) — not a wish
# to silence warnings. Adding a path is governance-approved for non-trivial scope.
#
# Per core/standards/reference-durability-standard.md § The override-marker and
# allowlist mechanism. For a single file that needs a single class, prefer the
# per-file override marker over a path allowlist entry.

# Historical-by-design corpus: archived release artifacts are immutable history and
# legitimately carry references frozen at their authoring time. Never re-author them.
release/releases/archive/

# Link-resolution maps by design: pipeline-shard cross-reference indexes and the
# pipeline README are navigational maps whose whole purpose is to carry resolvable
# links between shards. The link-resolution discipline polices their liveness; the
# durability discipline does not require them to summarize inline.
release/references/pipeline/README.md

# De-reference pass — pre-existing intra-repo links on touched lines. The
# parameterization / issue-reference de-reference pass edited lines in these
# durable-corpus files that ALSO carry pre-existing intra-repo doc cross-references
# (and, in stage-04-planning, pre-existing version-cutover prose that is governance
# content, not a removable reference). Those intra-repo references are retained by
# operator decision rather than summarized inline; they are NOT net-new fragile refs
# authored by this pass — the delta gate flags them only because the de-reference
# touched their lines. Exempting the files keeps the gate from re-flagging
# pre-existing in-repo links surfaced incidentally by that edit.
core/disciplines/concurrency-safeguards.md
core/disciplines/project-entity-model.md
core/schemas/gate-criteria-spec.md
core/specs/operational-artifact-inventory.md
core/standards/canonical-skill-structure.md
core/standards/per-stage-shard-standard.md
core/standards/version-field-semantics.md
release/references/how-to/hub-spoke-bridge.md
release/references/pipeline/stage-03-bundle.md
release/references/pipeline/stage-04-planning.md
release/references/pipeline/stage-12-execute.md
release/references/specs/release-personas.md

# Release plans by design carry release-scope issue references. The release-corpus
# frontmatter schema (release/references/standards/release-corpus-schema.md) MANDATES
# an `issues: ["#N", ...]` field on every plan, and plan bodies document their scope
# by issue number — both are structural properties of the artifact class, not removable
# fragile references. The sibling repository-integrity issue-reference gate already
# exempts the whole `release/releases/*` tree "mirroring the reference-durability gate";
# this entry realizes that already-expressed parity (the durability gate had not yet
# mirrored it for plans). Directory-prefix exemption matches the `release/releases/archive/`
# precedent above and avoids per-release allowlist churn. Notes are not listed because
# the gate's plan-path predicate covers `*_RELEASE_PLAN.md` only.
release/releases/plans/
