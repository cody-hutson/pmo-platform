# PROJECT — Fixture Sample (canonical, Capitalized folder)

Fixture for stamp-node-frontmatter self-test case (a) full-core-set + (b) case-norm.
This file lives in a Capitalized `01-Governance` folder and carries NO frontmatter,
so the tool must (1) resolve `01` via case-normalization (FIX 1) and (2) create a
fresh 11-field frontmatter block. Synthetic content only — no real project data.

## Phase
Sample phase text.
