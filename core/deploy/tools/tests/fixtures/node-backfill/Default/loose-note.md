# Loose Note — Fixture Sample (orphan candidate)

Fixture for case (f): a file directly under the project root with no NN- folder
prefix. The tool cannot assign a confident domain, so it must record this as an
orphan candidate (not guess). Synthetic content only.
