Sensitivity fixture S-4 — the deprecated legacy improvement identifier.

The legacy identifier is invalid in file content wherever it appears. It is
placed under a recognized heading here on purpose: correct placement must NOT
rescue it, because the rule is about the identifier scheme, not the location.

## Issue References

- @@IMP@@-007 — the deprecated scheme; a GitHub issue number replaces it.
