Specificity fixture Z-2b — the first self-documentation path exemption.

The rules file that documents this gate necessarily quotes worked-example
references, so it is exempt by path. The body fails on purpose.

- @@REF@@909601 — does not resolve.
- @@IMP@@-015 — the deprecated legacy identifier.
