Sensitivity fixture S-2 — a reference that resolves to a DIFFERENT repository.

A transferred issue returns the destination payload with a 200, so the exit code
alone cannot tell this apart from a valid reference. The repository-url branch is
what must fire.

## Issue References

- @@REF@@909002 — transferred out; the payload names another repository.
