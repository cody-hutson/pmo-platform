Specificity fixture Z-2c — the second self-documentation path exemption.

The pull-request template shows contributors what a reference block looks like,
so it too is exempt by path. The body fails on purpose.

- @@REF@@909602 — does not resolve.
- @@IMP@@-017 — the deprecated legacy identifier.
