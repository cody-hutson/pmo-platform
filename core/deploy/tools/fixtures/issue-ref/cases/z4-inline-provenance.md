<!-- reference-durability: allow-link — fixture: the markdown link IS the construct under test -->

Specificity fixture Z-4 — the inline-provenance alternative to placement.

The placement rule has two satisfying forms, not one: sit inside a designated
block, OR carry an inline provenance marker on the same line. This fixture takes
the second form, above the block, so a zero here is the only way the alternative
is proven to survive.

- @@REF@@909200 — recorded in [the upstream decision](https://example.invalid/decision) on this very line.

## Issue References

- Nothing further.
