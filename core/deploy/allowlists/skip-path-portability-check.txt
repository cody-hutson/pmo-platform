# Allowlist for the path-portability leak detector (deploy.sh Check 43 +
# core/deploy/tools/path-leak-patterns.sh, the #1137 gh-issue-ops guard).
# One path per line; matched as a whole-line exact path; `#` = comment.
#
# These files legitimately CONTAIN the leak shapes because they DEFINE or TEST the
# detection itself (the detection-signature exclusion) — they do not leak a real
# operator path. Adjudicated legitimate per knowledge-architecture.md §4.1 (the
# path-portability leakage family).

# The detector itself: holds the PATH_LEAK_RE_* pattern definitions and the
# --self-test fixtures (which deliberately embed /Users/<op> + both operator-instance
# leaf forms — the workspace-root `pmo-instance` and the legacy `personal/pmo-instance`
# — as example strings to verify flagging).
core/deploy/tools/path-leak-patterns.sh

# The instance-path resolver DEFINES the canonical operator-instance default, so it is
# the one sanctioned home for the instance-leaf literal in either form: the current
# workspace-root `pmo-instance` and, for as long as the both-forms migration window is
# open, the legacy `personal/pmo-instance` it replaced.
core/deploy/lib-instance-path.sh
