---
title: Gate Criteria Specification
purpose: Defines the three named triage-lifecycle gates — Triage Readiness, Workflow Readiness, Release Readiness — with structured criteria for validation, automation routing, and self-repair.
type: schema
status: ACTIVE
reversibility: CHEAP / Confidence HIGH
consumers: the gate-evaluation-spec (which assesses these criteria); the handoff-coordinator; field-lifecycle-matrix.md; agents validating Triage/Workflow/Release readiness
---
<!-- repo-integrity: allow-issue-ref -->
<!-- reference-durability: allow-link -->
<!-- reference-durability: allow-version-ref -->
# Gate Criteria Specification

Defines three named gates across the triage lifecycle — Triage Readiness, Workflow Readiness, Release Readiness — with structured criteria for validation, automation routing, and self-repair.

**Relationship to the architecture stack:**
- [field-lifecycle-matrix.md](field-lifecycle-matrix.md) — defines field state per stage and structural gate requirements (Gates 1->2, 2->3, 3->4). Named gates **extend** these structural gates with metrics and judgment layers. They do not replace them.
- [stage-io-contracts.md](stage-io-contracts.md) — defines artifact deliverables per stage boundary. Gates validate field/anchor/artifact state; I/O contracts validate handoff completeness.
- [pipeline/](../../release/references/pipeline/) — Stages 1-3 §7 reference named gates by name. This file provides the structured definitions.
- `engineering/rules/release-process.md` — concise operating procedure references named gates per stage.
- [review-composition-framework.md § 3 Review Catalog + § 4 Review Map](../standards/review-composition-framework.md) — composes the gate criteria defined here into named composed reviews (RC-* IDs) and maps them to stage × posture cells. Gate criteria remain owned here; the framework references them as sub-components.

**Consumers:**
- Automated gate-validation tooling — filters by gate (e.g., `gate=G3`) and iterates criteria programmatically.
- Stage-gate evaluator — routes by check type: `structural` → auto-validate, `metrics` → compute, `judgment` → LLM-assess.
- Agents executing CER Claim phase — validate named gate criteria in addition to field-lifecycle-matrix structural gates.

---

## Schema

Each criterion uses a 6-field table:

| Field | Type | Purpose |
|-------|------|---------|
| **ID** | `G[gate]-[seq]` or `G-[stage-abbrev][seq]` | Stable identifier for programmatic reference. Numeric-gate format (G1-01 through G3-07) used for Stages 1-3 named gates. Stage-abbrev format (G-PR, G-EX, G-CL, G-BR) used for Stages 9/12/13 named gates + the cross-stage Bundle Refresh gate. |
| **Criterion** | String | Human-readable description of what is validated — the pass-predicate in plain language. Criterion cells MUST NOT embed validation CLI; the reproducible command lives in the **Validation method** column. |
| **Type** | Enum | Classification of what the criterion checks. Values: `field` (issue body field), `anchor` (state anchor — label, Projects field, status), `artifact` (deliverable — comment, file, sub-issue), `validation` (cross-issue or cross-system check). |
| **Check** | Enum | How the criterion is evaluated. Values: `structural` (deterministic — field presence, format match, state comparison), `metrics` (computed aggregate — ratio-based, threshold-compared, derived from structural criteria or GitHub state), `judgment` (requires interpretation — quality, specificity, actionability). |
| **Automation** | Enum | Who performs the check. Values: `auto` (Tier 1 — agent executes without human input), `recommend` (Tier 2 — agent evaluates and recommends, human confirms), `human` (Tier 3 — human-only judgment). Maps to pipeline automation tiers. |
| **Validation method** | String (inline code) or `—` | The exact reproducible command/procedure that validates the criterion, lifted out of the Criterion cell. `structural`/`metrics` rows carry the deterministic command (`gh …` / `git …` / `grep …` / `deploy.sh …`); `judgment` rows carry `—` or a one-phrase method (the Layer-2 rubric per gate-evaluation-spec.md, not a shell command). A `—` is a valid, non-defect value. **The Validation method column is descriptive metadata for the operator/evaluator and is NOT parsed by any consumer** — the stage-gate evaluator routes by the `Check` column per [gate-evaluation-spec.md](gate-evaluation-spec.md) Consumer Contract, and never parses the Criterion cell for embedded CLI. |

### Inheritance Rules

Named gates inherit all structural checks from the corresponding [field-lifecycle-matrix.md](field-lifecycle-matrix.md) gate. Inherited criteria are **not duplicated** in the tables below — the named gate adds criteria beyond what the structural gate already requires.

| Named Gate | Inherits From | Relationship |
|---|---|---|
| Triage Readiness | Gate 1->2 (field-lifecycle-matrix.md) | Superset — adds judgment criteria to structural checks |
| Workflow Readiness | Gate 2->3 (field-lifecycle-matrix.md) | Superset — adds routing validation and management action checks |
| Release Readiness | Gate 3->4 (field-lifecycle-matrix.md) | Superset — adds dependency chain validation and scope assessment |
| Plan Readiness | Gate 3->4 (field-lifecycle-matrix.md) | Superset — adds release-plan-artifact-presence criteria to the structural Bundle->Planning exit |
| Engineering Completeness | Gate 6->7 (field-lifecycle-matrix.md) | Superset — adds PR-completeness, commit-linkage, and verification-evidence criteria to the structural Engineering->DevTest exit |
| Plan Review | Gate 8->9 + Gate 9->12 (field-lifecycle-matrix.md) | Superset — adds evidence-package and decision-record criteria to structural entry/exit checks |
| Execute Readiness | Gate 12->13 (field-lifecycle-matrix.md) | Superset — adds deployment-execution and verification criteria to structural transition checks |
| Close Readiness | Gate 13 Exit (field-lifecycle-matrix.md) | Superset — adds operational-deployment and verification-evidence criteria to structural close checks |

**Validation sequence:** Validate inherited structural gate first (field-lifecycle-matrix.md). If structural gate fails, named gate fails without evaluating additional criteria. If structural gate passes, evaluate named gate criteria below.

---

## Gate 1: Triage Readiness

*"Is there enough here to analyze?"*

**Stage boundary:** 1->2 (Intake -> Triage)
**Inherits:** Gate 1->2 from [field-lifecycle-matrix.md](field-lifecycle-matrix.md#gate-1-2-intake---triage)

**Template-awareness precondition (applies to all G1/G2/G3 structural runs).** Resolve the issue's **form family** — F0 multi-tier / F1 governance-intake / F2 kind-form / F3 unresolved-form — via Step 0 of the Template Detection Logic block below, and for an F1 issue then identify its intake template (`improvement.yml` / `bug.yml` / `observation.yml`) via Steps 1–3, **before** evaluating any structural criterion. The G1 criteria are written against the `improvement.yml` field set; running them one-size-fits-all against a mismatched template produces both error directions — false-positive failures (a structural check fires on a field the template renames or omits, e.g. `Priority`-vs-`Severity`) and false-negative passes (a template that legitimately lacks a field is bundled into a delivery milestone where that field is required downstream). Resolve per-template applicability from the criterion's `improvement | bug | observation` column triple (`req` / `adapt:<adapter-id>` / `n/a` / `conv`) and apply the named Adapter Block for any `adapt:` cell. Template-agnostic criteria (dependency-ref validity, cycle detection) are exempt — their checks do not reference template-specific field names.

| ID | Criterion | Type | Check | Automation | improvement | bug | observation | Validation method |
|---|---|---|---|---|---|---|---|---|
| G1-01 | Title is an informative summary — **no leading `[...]:` type/category prefix** (type is carried by the label), AND meets the substance floor (≥ 2 words; not a bare slug; ≥ N chars). This is a SYNTACTIC floor; the semantic informativeness bar lives in [intake-style-guide.md §7](../../release/references/how-to/intake-style-guide.md) (not gate-enforced). | field | structural | auto | `req` | `req` | `req` | — |
| G1-02 | Tier-branched: **(a) `improvement`-labeled** — Description field is actionable, not just observation. **(b) `observation`-labeled** — the three required `observation.yml` fields (what is missing / what good looks like / which file or section) contain enough signal that a Triage agent could draft a Proposal that itself meets G1-02 branch (a) post-promotion. | field | judgment | recommend | `req` (improvement branch) | `adapt:G1-02-Bug` | `req` (observation branch) | — |
| G1-03 | Evidence carries ≥1 claim in one of **two admissible shapes**. **(a) Evidence-label form** — a bracket-form evidence token (`[SOURCE]` / `[INFERRED]` / `[CONTEXT]` / `[ASSUMPTION – CONFIRM]` / `[RECOMMENDED]`). **(b) Probe-record form** — ≥ **2 distinct** probe-record markers from the closed set `probe` · `denominator` · `control` · `sensitivity` · `specificity` · `verdict`, each in **label position** (the marker word terminated by `:` or a dash, optionally emphasised), per the probe record at [`review-discipline-principles.md` § 8.2](../disciplines/review-discipline-principles.md). Shape (b)'s structural test is the **co-presence of two distinct label-position markers** — the same co-presence discipline as G1-05a pattern (d); a body that merely *discusses* probes in prose carries no label-position marker and does not satisfy (b). **Both shapes are evaluated against the `### Evidence` section only**, not the whole body. | field | structural | auto | `req` | `req` | `n/a` (no Evidence field) | — |
| G1-04 | Proposed Change is specific at the WHAT altitude — bidirectional: it FAILS **under-specification** (names no file or protocol affected — the WHAT is missing) AND FAILS **over-specification** (prescribes mechanism — algorithm, data structure, implementation pattern, pseudocode, or line-level surgical directive — committing the HOW prematurely). PASS = names the affected file(s)/protocol(s) without committing the implementation mechanism. Both failure directions are scorable from this criterion text alone (Layer-2), matching the two-branch G1-04 self-repair (under-specified / over-specified). | field | judgment | recommend | `req` | `n/a` (bug body is Reproduction Steps + Expected/Actual) | `n/a` (no Proposed Change field) | — |
| G1-05a | Every AC bullet starts with a verifiable verb (verify/check/confirm/assert/ensure/validate), OR names a file-path+state predicate with a backtick-wrapped path and a `contains`/`includes`/`has` state verb, OR starts with `predicate:`, OR is a **behavioral/domain predicate** — a `predicate:`-style declaration naming a behavioral/domain outcome that is not reducible to a single file-content/system-state assertion without losing the AC's meaning, **paired with** a `method:` declaration naming how that outcome is verified (e.g., a gate-eval run against a fixture, a reproduction-and-observe procedure, a judge rubric, a cross-output coherence check). Pattern (d)'s structural test is the **co-presence of an outcome declaration + a `method:` declaration** (both tokens present); a behavioral assertion lacking a `method:` declaration is NOT pattern (d) and fails G1-05a. The declared method may name an executor that does not yet exist (declared, verification deferred). **On the word `domain` here:** it is used **adjectivally** — this pattern names a *kind of outcome*, never a `domain:` field. It is concept 4 of the six concepts the bare token names; the corpus index is `core/specs/domain-token-registry.md`. A reader searching for a `domain` field in this file will correctly find none. | field | structural | auto | `req` | `adapt:G1-05-Bug` | `n/a` (no AC field; conversion required) | — |
| G1-05b | AC overall are verifiable beyond the structural verb check, on two named branches. **(i) Per-bullet quality:** no placeholder leakage (unreplaced `<...>` slots), no commented-out bullets, each predicate names an observable state. **(ii) Set-level usability coverage:** when the work item is **capability-class** per the § Capability-Class Usability-AC Requirement block below, the AC set carries at least one **usability AC** — an observable end-to-end behavior the capability performs AFTER install/deploy, stated as a verb exercised against a named realistic fixture — **in addition to** its presence ACs; otherwise the set names one of that block's closed exempt classes (E1–E5). **Silence is not an exemption:** an AC set with neither a conforming usability AC nor a named exempt class is a G1-05b finding. Branch (i) grades bullets individually; branch (ii) grades the set as a whole. The usability AC is authored in the G1-05a pattern-(d) shape, so branch (ii) adds no admissible pattern to G1-05a and changes nothing about presence ACs. | field | judgment | recommend | `req` | `adapt:G1-05-Bug` | `n/a` | — |
| G1-06 | Priority set in issue body per improvement.yml Priority field (P1–P4) — label-based priority is NOT expected | field | structural | auto | `req` | `adapt:G1-06-Bug` | `n/a` (no Priority field) | — |
| G1-07 | Status = Proposed, Stage = 1-Intake | anchor | structural | auto | `req` | `req` | `req` | — |
| G1-08 | A fresh Claude Code session loaded with CLAUDE.md + `core/rules/` can implement the ticket without asking clarifying questions about terminology, cross-issue references, or scope boundaries | field | judgment | recommend | `req` | `req` | `req` | — |
| G1-09 | Intake-tier label (`improvement` / `bug` / `observation`) matches body template structure per the Template Detection Logic block below | validation | structural | auto | `req` | `req` | `req` | — |

**Applies-to column triple semantics:** `req` = criterion applies; check field per template schema. `adapt:<adapter-id>` = criterion applies but field is lexically/semantically translated per the Adapter Block below. `n/a` = criterion does not apply (the field does not exist on this template). `conv` = criterion is a Bundle prerequisite; issue must be converted to `improvement.yml` before this gate evaluates (raised at Stage 3 Phase A1 per the Template-Conversion Rule in § Gate 3).

**The triple is the F1 applicability surface.** Its three columns are the three governance-intake templates, so it answers the applicability question for **F1 issues only**. F2 (kind-form) applicability is carried by the § Kind-Form Applicability block below, and is derived per issue rather than declared per column — see that block for why a column could not express it. F0 and F3 never reach a criterion (Step 0 terminates them at G1-09), so they need no applicability surface at all.

### G1 Enforcement-Layer Split

The `auto` Automation tag on a G1 criterion conflates **two distinct enforcement surfaces** that fire at different times against different inputs. Naming them apart is the durable policy this subsection records, so no future reader treats "form-required" and "gate-checked" as the same thing:

- **Layer-A — Form HARD-STOP** (intake-time, submission-blocking). The intake template's `validations: required: true` flag on a field. It blocks a **blank form input** at issue-creation time. It is **presence-only** — it cannot inspect content (an evidence label vs. a sentence of prose, a file path vs. a directory). The surface is the `.github/ISSUE_TEMPLATE/*.yml` field set. A HARD-STOP fires the moment the author clicks Submit on an empty required field; it is the earliest and cheapest enforcement point.
- **Layer-B — Gate SOFT-WARN** (post-creation, content-validating). The `deploy.sh --check` Check 22 per-issue evaluation against a created issue **body**. It validates **content** — structural criteria emit a FAIL (gate-checked), judgment criteria emit a non-gating RECOMMEND. It runs over the open `status: bundled` population **in the deploying milestone**, after the form already accepted the submission, so it catches what the form structurally cannot. **Layer-B is two surfaces, not one** — see § Layer-B(d) / Layer-B(g) immediately below, which states what that scoping does and does not preserve.

#### Layer-B(d) detector and Layer-B(g) gate — one check, two authorities

Layer-B conflates a **standing detector** with a **release gate**, and the two answer different questions. Naming them apart is the durable policy this subsection records ([`ADR-120`](../ADRs/ADR-120-g1-enforcement-authority-is-class-scoped-and-release-scoped.md)).

- **Layer-B(d) — recommend-tier detection** (recommend-tier, never FAIL-capable). Within the evaluated population, a defect outside the gate's authority — wrong form family, `sub-task`, or a judgment criterion — is still **detected**, still logged, and still readable, emitted at recommend-tier instead of gating-tier.
- **Layer-B(g) — release gate** (release-scoped, structural, FAIL-capable). Evaluates the **milestone being deployed** and holds the only authority to block. A gate blocks the transition it guards; a backlog-wide gate blocks a release on defects belonging to *other* releases, which the operator shipping this one cannot drain.

> **The two surfaces share ONE population, and it is the deploying milestone.** Check 22 holds exactly one issue-list query and that query carries `--milestone`. The split is a split of **tier**, not of **scope**: Layer-B(d)'s tier semantics are implemented, its backlog-wide population is not. A structural G1 defect on a bundled card in another milestone is therefore not demoted to recommend-tier — it is **never evaluated and emits at no tier**. Read every `Layer-B(d)` cell below as scoped to the deploying milestone. Measured live at `2026-08-07T20:07:19Z` (pinned baseline; the population is mobile and this is not a constant): 207 open `status: bundled` issues across 48 milestone buckets, 10 in the deploying milestone, **197 across 47 other buckets outside every query the check makes**. This is a statement of what shipped, not a re-opening of the scope decision, which stands.

**Enforce-population contract.** The FAIL-capable authority of Layer-B(g) is exactly:

> `authority = structural ∧ F1 ∧ ¬sub-task ∧ in-deploying-milestone`

Each conjunct, and why it is there:

| Conjunct | Excludes | Why |
|---|---|---|
| `structural` | the four judgment criteria | Already the `Check`-column partition; judgment criteria are recommend-tier and never FAIL. A judgment criterion is outside the enforce population under **every** population choice, including any criterion added later. |
| `F1` | F0 multi-tier, F2 kind-form, F3 unresolved-form | The G1 criteria are written against the governance-intake field set. Enforcing them against a family they were not written for is the false-positive direction the template-awareness precondition above already names. F0's exclusion is a **stated consequence**, not an oversight: authority is F1, and F0 is a different family with a different verdict. |
| `¬sub-task` | any card carrying the `sub-task` label | § Gate 3 already exempts *"sub-task decompositions within an already-bundled parent"* by name; this applies that **existing governed exemption** at the surface that never received it. Without it, the pipeline's own bookkeeping cards can gate the release they are bookkeeping. |
| `in-deploying-milestone` | every card in another milestone | Layer-B(g) guards a release transition, so it evaluates that release. |

**Per-template applicability is unchanged, and it composes with authority rather than replacing it.** Within F1, applicability comes from the criterion's existing `improvement | bug | observation` triple: the map below assigns **authority**, the triple assigns **applicability**, and a criterion is enforced only where both hold. **An `n/a` cell is never enforced under any population choice** — the observation template's `n/a` on G1-03 / G1-05a / G1-06 is untouched by anything in this subsection. F2 applicability continues to come from the § Kind-Form Applicability block.

**Per-family authority map.** Rows are keyed by **form family**, never by criterion ID — the criterion-ID space is governed by the tables above and this map must not create a second, divergent enumeration of it.

Every `Layer-B(d) detection` cell below is **scoped to the deploying milestone**, per the boxed note above. Outside that milestone every cell is "not evaluated", for every family.

| Form family | Layer-B(g) authority (FAIL-capable) | Layer-B(d) detection (in the deploying milestone) |
|---|---|---|
| F1 governance-intake, **not** carrying `sub-task` | **Yes** — structural criteria, per the applies-to triple, within the deploying milestone | Yes |
| F1 governance-intake carrying `sub-task` | No | Yes — recommend-tier |
| F0 multi-tier | No | Yes — recommend-tier |
| F2 kind-form | No | Yes — recommend-tier, applicability per § Kind-Form Applicability |
| F3 unresolved-form | No | Yes — recommend-tier |
| **any family, in any OTHER milestone** | No | **Not evaluated** — outside the single query, so no finding is produced at any tier |

**Release-identity resolution — a four-state closed contract.** Layer-B(g) cannot evaluate until it knows which release is being deployed, and "cannot tell" must be a first-class answer rather than a silent default. The resolver returns exactly one of four verdicts, and each routes to exactly one emitter:

Because both surfaces read the **same single query**, the verdict gates detection as well as gating — the `Layer-B(d)` column is *not* "unaffected" in any row but the first.

| Verdict | Meaning | Layer-B(g) | Layer-B(d) |
|---|---|---|---|
| `RESOLVED` | a candidate was derived **and validated present-and-open** in the milestone set | evaluates the resolved milestone | runs over that **same** milestone — never wider |
| `NONE` | no release-identity assertion, and no release branch attached | **not applicable** — evaluates nothing, blocks nothing | **also does not run** — no finding at any tier |
| `UNRESOLVED` | a release context exists but no title could be validated, or the validator itself could not be read | fail-closed when the candidate was **asserted**; advisory when it was **detected** | **does not run** |
| `INVALID` | a candidate was derived and **rejected** — absent from the milestone set, or present but closed | fail-closed when **asserted**; advisory when **detected** | **does not run** |

A run that resolves `NONE` — a detached HEAD, or mainline after the release merge — therefore produces **no G1 signal whatsoever**, not a backlog-wide advisory sweep. The check's own `NONE`-branch message currently says otherwise; where the two disagree, this table and [`ADR-120`](../ADRs/ADR-120-g1-enforcement-authority-is-class-scoped-and-release-scoped.md) are correct and the message is a recorded, owned follow-on.

**Durability of this contract, stated precisely — it is not fail-closed in the general sense.** It is **fail-closed on an asserted-but-unresolvable or invalid release identity**, and **not-applicable when no release is in flight**. When no release identity is asserted, Layer-B(g) does not evaluate and does not block, **by design**. The property that makes the not-applicable branch safe is not a default value but the **emitter**: it routes to a helper that contains no mode branch and no issue-counter increment, and therefore cannot escalate under any future mode flip. A reader must not summarize this section as "fail-closed" — an ADR and a schema outlive the release that wrote them, and a wrong durability claim outlives them both.

**Two bounded residuals, recorded rather than closed.** (1) Validation proves a resolved milestone **exists and is open**; it does not prove it is the **right** one. A candidate that resolves to a different, concurrently-open release passes validation untouched, and concurrent release branches make that reachable. The bound is disclosure, not prevention: the resolved slug, the source that produced it, and the milestone-set denominator are all logged before any finding, so a wrong scope is legible at read time rather than silent. (2) Branch-name detection is not total over release-branch naming history, and both a detached HEAD (which the hub-spoke session protocol prescribes at session end) and post-merge `main` resolve to `NONE`. Layer-B(g)'s live window is therefore **an attached release branch during Engineering through Plan Review**, which is narrower than the stage range alone implies.

**Recorded rationale — why the population is what it is, measured rather than asserted.** Re-measured at build time by driving the **shipped** Check-22 evaluation region over the live population; read `2026-08-06 (Thursday)`, branch anchor at or after the priority-detector reconciliation commit that precedes it in the same release.

| Measurement | Value |
|---|---|
| Open `status: bundled` population | 256 |
| Structural findings **before** the class filter (backlog-wide) | 258 |
| Structural findings **after** the class filter, still backlog-wide | 150 |
| Structural findings the class filter removes from gating authority | 108 across 108 cards |
| — of which the `F1` conjunct is the cause | 108 (90 also carry `sub-task`; 18 are kind-form / unresolved-form) |
| — of which the `¬sub-task` conjunct is the **sole** cause | **0** — `F1 ∩ sub-task = 0` today, so the conjunct is a **guard**, not a change |
| Those findings by criterion | G1-09 94 · G1-05a 7 · G1-01 7 |
| Structural findings **after** the milestone filter (this release) | **1** |
| F0 multi-tier cards in the population | 0 — the cell is empty, and is held by fixture rather than by data |

The two axes are separable and both are load-bearing: the class axis alone leaves 150 gating findings across the backlog — not drainable by any single release — and the milestone axis takes that to 1. Neither axis alone produces a gate an operator can clear.

**Read this table in the right frame.** Every row above measures the **pre-change, backlog-wide** evaluation, and exists to justify *why* the population was narrowed. It is not a statement of what the shipped check emits today. Today the check evaluates only the deploying milestone, so the 108 class-filtered findings are counted here as *removed from gating authority* — not as findings still emitted at recommend-tier, which they would be only if they sat in the deploying milestone.

**Named successors, so the deferral does not decay into permanence.** (a) § Gate 3's body-compliance precondition still has no runner; a G1 re-check at the **bundling transition** is the shift-left successor to a deploy-time gate. (b) The release ledger is the eventual release-identity resolver: it carries the authoritative release↔milestone binding, and once it records in-flight state it supersedes branch-name detection. Both were reviewed and are recorded as successors, not as work this contract performs.

The `structural | judgment` partition already in the **Check** column IS the HARD-STOP / SOFT-WARN seam — it is read off, not invented. `structural` criteria are gate-checked (FAIL-capable at Layer-B); `judgment` criteria are recommend-flagged (advisory at Layer-B, never FAIL). Whether a criterion is *also* Layer-A form-required is a separate axis (a field can be presence-required at the form yet content-checked at the gate, or gate-checked only).

| Criterion | Layer-A form-required? | Layer-B (gate) | Enforcement class |
|---|---|---|---|
| G1-01 title informativeness (no bracket prefix + substance floor) | — (title string, not a body field) | structural FAIL | HARD-STOP (gate) |
| G1-02 description actionable | presence (`required: true`) | recommend-flag | SOFT-WARN |
| G1-03 evidence present | presence (`required: true`) | structural FAIL | HARD-STOP |
| G1-04 proposed-change specificity | presence (`required: true`) | recommend-flag | SOFT-WARN |
| G1-05a AC structural pattern | presence (`required: true`) | structural FAIL | HARD-STOP (gate) |
| G1-05b AC judgment | — | recommend-flag | SOFT-WARN |
| G1-06 priority (body) | **NO — `required: false`** (label-based priority is NOT expected; body is canonical) | structural FAIL (body anchor) | SOFT-WARN (gate) |
| G1-07 status/stage anchors | — (auto-label) | auto-fix | HARD-STOP (anchor) |
| G1-08 implementability | — | recommend-flag | SOFT-WARN |
| G1-09 label↔template | — (validation) | structural FAIL | HARD-STOP (gate) |

**G1-06 is gate-checked, not form-required.** The Priority field is `required: false` on `improvement.yml` by design: priority is **body-canonical** — authored in the body `### Priority` field per G1-06, validated against full backlog context at **G2-01**, and mirrored to the configured priority surface (default: the GitHub Projects Priority field) at **G2-12**. There is no `priority:` label surface, so G1-06's "label-based priority is NOT expected" stands, and a `status: approved` issue with no priority label is conformance, not a defect. Holding Priority at `required: false` is therefore the correct Layer-A posture (the body anchor is still gate-checked at Layer-B). See the Priority-Model block below for the full body-canonical / Projects-mirror / label-not-a-surface model and the G2-12 sync criterion.

**Why both layers exist.** Layer-A delivers the intake-time hard-stop on blank required fields the moment they are submitted — earlier and cheaper than any post-creation sweep. Layer-B then validates the content the form structurally cannot inspect. The two are complementary, not redundant: removing Layer-A pushes a blank-field defect downstream to a gate re-edit cycle; removing Layer-B lets a present-but-empty-of-content field pass intake. The `### Priority` field is the explicit exception that proves the rule — present at Layer-B (body anchor gate-checked), deliberately absent from Layer-A (form-optional), because its canonical home is the body, not a required form slot.

### Template Detection Logic (consumed by G1-09 + by all template-aware gates)

**Step 0 (form family — runs before Step 1, and its outcome selects which of the steps below apply).** An issue's *form family* is what determines whether the intake-template question in Steps 1–3 is even the right question. Resolve it first, in the precedence order below; the four families are exhaustive and mutually exclusive, so **every** issue resolves to exactly one and no submission can reach a criterion that was never designed to read it.

| Family | Predicate | What the rest of this block does |
|---|---|---|
| **F0 — multi-tier** | more than one intake-tier label | Steps 1–3 are skipped. Emit `G1-09 FAIL: >1 intake-tier labels` and route to self-repair (apply correct single label) — the same disposition Step 1 has always rendered for this case. |
| **F1 — governance-intake** | exactly one intake-tier label (`improvement` / `bug` / `observation`) | Steps 1–3 apply **unchanged**, and the criteria read their `improvement \| bug \| observation` applies-to triple. |
| **F2 — kind-form** | no intake-tier label, **and** a `type:<kind_id>` label whose `kind_id` is in the resolved kind set | Steps 2–3 are skipped — G1-09 is **satisfied by this resolution**, because the kind label matched against the licensed vocabulary *is* the form determination and there is no label↔body pair left to reconcile. Per-criterion applicability comes from the § Kind-Form Applicability block below. |
| **F3 — unresolved-form** | no intake-tier label **and** no licensed-kind label | Steps 1–3 are skipped. Emit `G1-09 UNRESOLVED-FORM` naming the labels the issue actually carries and the vocabulary it was measured against. This finding **must not** carry F1's remediation: telling the author of a card whose only defect is an unlicensed kind to "apply the correct single label of improvement/bug/observation" instructs them to break a correctly-labelled card. |

**Where the kind set comes from — resolved, never enumerated.** `type:<kind_id>` is the label-surface projection of the `work_item_type` discriminator, and the live set is whatever the deployment's selected packs declare (`core/packs/*/pack.toml` `kind_id` rows) — see [`label-taxonomy.md`](../specs/label-taxonomy.md) § Work-Item-Kind Labels, which refuses to enumerate the kinds for the same reason this block does. **No kind id appears in this specification or in any gate that consumes it.** A deployment that selects a different methodology pack, or adds a kind to one, changes which cards are F2 with no edit to this block. Enumerating them here would bake one methodology's archetype into a methodology-neutral gate — the conditional-where-adaptive-belongs shape [`ADR-033`](../ADRs/ADR-033-methodology-conditional-skill-activation.md) rules against — and would go stale on the next pack change.

**F1 takes precedence over F2, and that ordering is load-bearing.** A resolved kind with no dedicated form emits on the interim `improvement.yml` vehicle carrying its kind as a `type:` label (the interim-vehicle rule in the intake-desk type map). An issue carrying **both** an intake-tier label and a kind label is therefore **conformant**, not a double-category defect, and F1-precedence keeps every such card on its existing path with its existing verdict.

**Resolution unavailable is a fifth outcome, and it is not a verdict.** When the licensed kind vocabulary cannot be read, F2 and F3 are indistinguishable. The consuming gate then emits **one** finding naming that cause and withholds the per-issue verdict for every issue that is not F0 or F1 — the criterion reads NOT-EVALUATED, never FAILED, and never once per issue. Guessing a family produces a wrong verdict on a correctly-labelled card, which is the defect Step 0 exists to remove; F0 and F1 are label-only predicates and stay fully evaluated.

**Steps 1–3 below are the F1 path.** They are unchanged in text and in behavior.

Step 1 (primary): Read the intake-tier label.
  - Set of intake-tier labels: {`improvement`, `bug`, `observation`}.
  - If exactly one match: `label_template` = that label.
  - If zero or >1 matches: emit `G1-09 FAIL: 0 or >1 intake-tier labels` and route to self-repair (apply correct single label).

Step 2 (confirmation): Infer template from body markers.
  - Body marker patterns:
    - `### Severity` AND `### Reproduction Steps` ⇒ inferred = `bug`
    - `### Priority` AND `### Category` AND `### Description` ⇒ inferred = `improvement`
    - `### What is missing?` AND `### What does good look like?` ⇒ inferred = `observation`
    - none of the above match exclusively ⇒ inferred = `ambiguous`

Step 3 (reconcile):
  - If `label_template == inferred`: `template = label_template`; G1-09 PASS.
  - If `label_template != inferred` AND `inferred != ambiguous`: G1-09 FAIL with `label=<X>, body=<Y>` payload.
  - If `inferred == ambiguous`: G1-09 PASS but flag for operator review (body may have been edited post-template-rendering).

**Body-marker uniqueness verification:** `### Severity` is unique to bug.yml (improvement and observation lack the field). `### Reproduction Steps` is unique to bug.yml. `### What is missing?` is unique to observation.yml. `### Priority` is present in improvement.yml only; `### Priority` + `### Category` is doubly redundant for confirmation.

**Consumer integration:** This detection block is the canonical template-detection primitive consumed by: (1) Gate-running scripts (existing automated validation and stage-gate evaluator consumers) — resolve the form family (Step 0), then call template-detect before iterating criteria; apply per-template applicability per the matrix for F1 and the § Kind-Form Applicability block for F2. (2) CER Claim agents (Stage 1/2/3 hub-spoke) — same primitive. (3) Stage 3 Phase A1 (Bundle pre-bind) — same primitive; observation result triggers Template-Conversion Rule. (4) New G1-09 row — same primitive; a Step-1..3 mismatch is the FAIL signal on F1, and an unresolved form family is the FAIL signal on F3. A consumer that reads only Steps 1–3 is reading the F1 path and will render F1's verdict on cards that are not F1 — which is the failure Step 0 was added to close.

### Capability-Class Usability-AC Requirement (referenced from G1-05b branch (ii))

A capability can pass every acceptance criterion and all of CI while shipping **dormant** — verified structurally present, but not actually usable. An AC set that asserts only presence ("a template ships", "the consuming skills reference it", "the schema changed") is structurally incapable of catching a missing activation or wiring path: every assertion is true, and on a fresh install the capability does nothing. Presence ACs are necessary and not sufficient. This block defines who owes the additional usability AC, who does not, and what shape it takes. The intake-side **worked examples and authoring guidance** live at [`intake-style-guide.md`](../../release/references/how-to/intake-style-guide.md) §4b and are cited here, not restated.

**Registered prose exemption — the doctrine is deliberately carried on both surfaces, stated rather than cited.** The C1–C3 predicate rows, the E1–E5 exempt-class rows, the anti-patterns, the required AC shape, and the U1–U5 conformance tests appear on both this block and [`intake-style-guide.md`](../../release/references/how-to/intake-style-guide.md) §4b. That duplication is **registered here as a per-domain prose exemption** under [`duplicate-source-discipline.md`](../standards/duplicate-source-discipline.md) §1 condition 3, documented in the principle that owns the duplicated surface — this block, which §4b names as its enforcement surface. The rationale is a division of *use*, not of content: this schema is what a gate evaluator reads to **enforce** the rule; the guide is what an author reads to **write to** it; and each reader must resolve the whole rule in one read, because a reader who has to fetch the peer file to learn what C2 says will guess instead. What is *not* duplicated is the **verdict language**, which lives here only — the set-level rejection rule, the Layer-B coverage note, and the two-branch G1-05b self-repair row — and the **worked authoring examples**, which live in the guide only.

**Why the other three conditions are unavailable, stated so the exemption is not read as the path of least resistance.** Condition 1 (registered mirror, byte-identity under Check 9 / 11 / 13) does not apply: the two surfaces already diverge deliberately — U3's rejection clause, the E3 anti-pattern's closing phrase, and both table header rows are each worded for their own reader — so a byte-identity gate would fail on day one against text that is correct. Condition 4 (complementary pair, Check 13b) is structurally unavailable: that registry keys on a **shared basename**, which `gate-criteria-spec.md` and `intake-style-guide.md` do not have, and it registers *complementarity* — disjoint section ownership — where these two blocks are the same doctrine. Condition 2 (consolidate to one canonical home) was considered and declined at the plan-review gate: collapsing either surface into a cross-reference leaves one of the two readers a fetch short of the rule.

**The cost this exemption accepts, and the obligation it imposes.** A prose exemption buys standalone readability with **no mechanical drift detection** — nothing fails when the two copies diverge, which is precisely why the register-or-remove rule treats unregistered duplication as debt. The obligation is therefore explicit and is the whole consideration for the exemption: **an edit to any doctrine line above must land on both surfaces in the same change.** A one-sided edit is silent divergence, and this exemption is what makes it silent.

**On the term `capability-class` here:** it names a **per-work-item intake classification**. It is distinct from (a) the *Release Class* enum, which classifies a whole release at Stage 3 Bundle, and (b) the phrase "the capability-class the solution targets" in the Stage-5 extend-before-create criterion, which means a generic *class of capability*. A reader resolving the token in this file resolves it to this block.

**The predicate — capability-class ⇔ C1 ∧ C2 ∧ C3.** All three limbs must hold.

| Limb | Question | Fails when |
|---|---|---|
| **C1 — actor** | Is there a named actor — an agent following the platform's own instructions, a script / check / hook, a CI job, or an operator executing a documented procedure — that will *do* something because of this change? | Only a **reader** is affected |
| **C2 — trigger** | Is there a named runtime moment that fires it — a session start, a deploy run, a gate evaluation, a PR event, a stage entry, a command invocation? | Nothing reads it at runtime; adoption depends on someone remembering |
| **C3 — observable** | Is there something a third party could witness after install — output, a state change, an emitted artifact, a verdict, a blocked action — that is **not** the presence or content of the edited file itself? | The deliverable **is** the artifact |

**How to read the limbs — *ex ante*, against the item's design.** Each limb asks whether the item's design *names* an actor, a runtime trigger, and a witnessable observable. A limb does **not** ask whether that trigger was later observed to fire: whether it fired is exactly what the usability AC exists to test, so folding the observation back into the predicate makes the predicate circular and lets the block exempt the failures it exists to catch.

**Why the conjunction.** Read *ex ante*, the originating failure satisfied **all three** limbs — C1 (four skills instructed to read a roster), C2 (each reads it at its own named runtime moment), C3 (a name resolves to a record) — so it *was* capability-class, owed a usability AC, and that AC is what would have exposed the missing step. What failed was not a limb but the **delivery of C2's precondition**: nothing seeded the roster on install, so the named trigger never fired. Reading the same case *ex post* — "nothing fired, therefore C2 was not satisfied" — inverts this block, because E3 below is keyed to C2 and would route the motivating failure to an exemption. It also inverts the corollary: a weaker **disjunctive** predicate would have flagged this item too (C1 and C3 hold on either reading). The conjunction earns its place by keeping genuine reference-content, refactor, bookkeeping, and spike work out — not by being the only predicate that catches the motivating case.

**Exempt classes (closed list; each keyed to the limb it fails).** Extending this list is a governed change, not an authoring choice.

| # | Exempt class | Limb it fails | One-line test |
|---|---|---|---|
| **E1** | **Reference-content change** — doc, standard, discipline, ADR, release note, comment, prose or naming edit whose whole effect is what a **human** reader reads. **An agent-loaded instruction file is not exempt under E1 merely because its effect is textual** — if a named agent loads it at a named runtime moment and behaves differently, test it against C1 ∧ C2 ∧ C3 rather than claiming E1. | C1 | Nothing **runs** differently after install |
| **E2** | **Behavior-preserving refactor / relocation** — move, rename, extraction, dedup, reflow, reference repair, carrying a declared no-behavior-change invariant | C3 | The correct AC is a **must-NOT-change** assertion |
| **E3** | **Governance-internal rule with no runtime trigger** — doctrine or protocol prose consumed at human or agent judgment time, with no gate, check, hook, or command reading it | C2 | No executor reads it |
| **E4** | **Tracking / bookkeeping** — an issue-body edit, label / milestone / epic hygiene, backlog restructuring, corpus-metadata reconciliation | C1 + C2 | The mutation is to the tracker, not to the platform |
| **E5** | **Spike / analysis / decision record** — research, audit, gap analysis, or a decision whose deliverable is the finding or the record itself | C3 | The output is knowledge, not behavior |

**Anti-patterns (the exemption is not a general escape hatch):**

- **A mixed item is capability-class.** An item that is mostly documentation and partly a new hook is capability-class **for the hook**. The test is whether the triple holds anywhere in the item, not whether it holds for the majority of the diff.
- **"It only changes a default or a config value" is not an exemption** when a runtime actor reads that value — all three limbs hold.
- **"It ships warn-mode" is not an exemption.** A warn-mode check still runs and still emits; only the authority of its verdict is reduced.
- **E3 is an exemption from the usability AC — never a finding that the rule will be adopted.** A rule expected to change behavior should acquire a trigger (a gate, check, hook, or named stage step) rather than claim E3. E3 exempts on the absence of a runtime trigger — the same limb (C2) whose *ex ante* satisfaction made the originating failure capability-class; claiming it without justifying the absence hands that failure the exemption it must never get, and institutionalizes the very class this block exists to close.
- **A passing discrimination arm at the check's default invocation does not discharge `U5`.** A shipped count-vs-structure check carried a genuine two-armed fixture harness whose must-not-flag arm ran with the baseline disabled. The over-firing defect lives where a path-scoped invocation meets a *loaded* baseline: the stale set is computed against the full baseline while only the scoped subset is scanned, so every baselined entry outside the scope reads stale. **Both parameters were exercised individually; their combination never was.** The arm was real, non-vacuous, and blind — it ran on the content axis while the defect lived on the scope-by-comparison-set axis. Enumerate the parameters, not the fixtures.

**Required shape of the usability AC.** It is authored in the G1-05a pattern-(d) form — an outcome declaration co-present with a `method:` declaration — with the outcome anchored after install:

```
- [ ] predicate: after a fresh install/deploy, <actor> <verb-phrase> against <named, populated fixture>,
      producing <stated observable>; method: <the exact invocation or procedure that exercises it,
      and the observable that must be seen>
```

**Conformance tests — a bullet is a usability AC only when all four hold, and — where the capability is a mechanical check — `U5` additionally holds:**

| # | Test | Rejects |
|---|---|---|
| **U1** | **Post-install anchor.** The predicate names the post-install / post-deploy / fresh-session / first-run moment. | An assertion true only in the author's working tree |
| **U2** | **Actor + action verb.** The predicate names an actor and an action it performs. Verbs of existence — `exists`, `ships`, `is present`, `contains`, `includes`, `has`, `references`, `is defined`, `is documented` — do not satisfy U2 in the predicate position. | "a roster template ships" |
| **U3** | **Named, populated fixture.** The predicate names the concrete input or state the behavior runs against. No fixture, or an empty one, fails. | "an agent can resolve a name" — against which roster? |
| **U4** | **Observable is not the artifact.** The stated observable is something other than the presence or content of the file(s) the change edits. | "the file contains the rule" |
| **U5** | **Discrimination arm at the check's own parameter surface.** *Conditional — fires only when the capability is a **mechanical check**: a script, lint, hook, gate, or CI job that emits a finding.* Three limbs, all required. **(1)** The predicate names a **near-miss input the check must NOT flag** — one differing from a true positive only in the property being tested. **(2)** It states that arm's expected **zero** as part of the stated observable, rather than leaving the zero implied. **(3)** It exercises that arm at **each parameter of the check's own declared invocation surface that narrows the examined population or changes the comparison set** — not merely at the scopes the other criteria happen to mention, and not merely at the check's default invocation. This binds the specificity arm defined at `core/disciplines/review-discipline-principles.md` § 8 **PV-2c** to the AC set; that arm's definition, its PASS condition, and its vacuous-arm rider live there and are not restated here. | A must-flag fixture set with no must-not-flag member; "no false positives" asserted in the abstract, which is unfalsifiable; a discrimination arm exercised only at the check's default invocation |

**U5 limb 3 — the deferral rider for a card that ADDS a check.** Limb 3 is graded **as written at intake** on a card that **modifies** an existing check: that check's invocation surface is already declared and is enumerable from its own argument parser. On a card that **adds** a check, limb 3 is structurally unauthorable at intake — a check that does not yet exist has no declared invocation surface to enumerate, and the enforcement surface is often still an open design question at that moment. Such a card satisfies limb 3 by naming the **intended** narrowing parameters, or by marking the enumeration `Stage-5-deferred`; the explicit enumeration then **binds at Stage-5 exit and is graded there**, against the design spec that fixes the invocation surface. **Limbs 1 and 2 are graded at intake in both cases.** A `Stage-5-deferred` marker that is never discharged at Stage-5 exit is an unsatisfied U5 — not an exemption.

**U5 cutover — the obligation binds forward, not backward.** U5 applies to work items entering intake **after** the release that introduces it (`triage-and-backlog-instrumentation`); items authored before that release, and items already in flight when it shipped, are graded against the `U1 ∧ U2 ∧ U3 ∧ U4` conjunction alone and are not retroactively bound. The introducing release itself is exempt: it *produces* U5, so its own cards were authored before the obligation existed and could not have been written to it — a rule that bound them would be unsatisfiable by construction, and grading them against it records an authoring artifact as a failure rather than a defect. The release is named here by **slug** and not by version — the version binds only at the Stage-12 atomic claim (ADR-092), so a version stem written into this rule would be a guess when authored and wrong after any re-sequencing.

**Set-level rejection rule:** branch (ii) is not satisfied when no bullet in the AC set satisfies `U1 ∧ U2 ∧ U3 ∧ U4` and no exempt class is named — and, where the capability is a **mechanical check**, additionally when no bullet satisfies `U5` (limb 3 read against the deferral rider above). **The base conjunction is unchanged**, so a capability-class item that ships no mechanical check is provably unaffected by this rider.

**Explicit non-change.** G1-05a pattern (b) accepts `contains` / `includes` / `has` as a valid AC shape, and that stays correct and unchanged — presence ACs remain presence ACs. U2's negative verb list constrains only the additional usability bullet. The usability AC is **additive**.

**Layer-B coverage (stated, so its absence is not read as a defect).** `deploy.sh` Check 22 recommend-flags G1-05b through a lightweight content proxy that implements **branch (i) only**. Branch (ii) is graded by the Layer-2 judgment path defined at [`gate-evaluation-spec.md`](gate-evaluation-spec.md) § Layer 2, which scores each `Check=judgment` criterion individually, and is re-run at Stage 4 Phase A0.6 (the G-PL2 pre-plan crisping gate, which re-evaluates the G1-05 family against each bundled body). U1–U5 are stated in mechanical form so branch (ii) becomes implementable as a structural check without re-design, once its fire predicate has a recorded body declaration. `U5` is the one member whose mechanization additionally needs the item's deliverable class (mechanical check or not) recorded in the body, since that is its fire condition.

### Gate 1 Adapter Blocks (inline lexical/semantic translation)

**Adapters G1-01-Bug / G1-01-Obs — RETIRED (template-agnostic post-prefix-removal).** G1-01 no longer requires a per-template title prefix — all three work-item templates (`improvement.yml` / `bug.yml` / `observation.yml`) carry a prefix-less `title: ""` and share one informativeness floor (no `[...]:` prefix + substance floor). There is no per-template prefix literal left to translate, so the former `[Bug]:` / `[Observation]:` adapter translations are removed.

**Adapter G1-02-Bug:** bug.yml has no Description field; G1-02 evaluates against the conjunction of `### Reproduction Steps` + `### Expected Behavior` + `### Actual Behavior` (collectively "the bug narrative"). The narrative is actionable iff a fresh agent can reproduce + observe expected vs. actual. Label-missing branch unchanged.

**Adapter G1-05-Bug:** bug.yml AC follows the bug-narrative AC pattern (per `bug.yml` template lines 78-84): "reproduction steps no longer trigger actual behavior; expected behavior observed." G1-05a verb-list accepts the literal phrase pattern `reproduction steps no longer trigger actual behavior` OR `running reproduction steps produces expected behavior` as a structurally-valid AC bullet. Other G1-05a clauses (verb-first / file-path+state / explicit predicate) continue to apply unchanged.

**Adapter G1-06-Bug:** bug.yml has `### Severity` (P1-Blocker / P2-Material / P3-Annoyance / P4-Cosmetic) in lieu of `### Priority` (P1-Urgent / P2-High / P3-Medium / P4-Low). Semantic mapping: P-level digit (1-4) is canonical; the Severity P-level satisfies G1-06. A single template-agnostic detector covers both field names, and this criterion **cites** it rather than restating it: the reference implementation is `PRIORITY_FIELD_RE` / `parse_priority_body()` in [`release/tools/bundle-issues-parser.py`](../../release/tools/bundle-issues-parser.py), governed by [`ADR-111`](../../release/ADRs/ADR-111-priority-carrier-agnostic-p-level-detection.md) — *"The P-level digit is the canonical priority satisfier. The carrier is not part of the contract. Any consumer of this field reads it through the shared detector rather than authoring its own."* The P-level digit, not the field name or qualifier word, is the canonical satisfier; absent, word-only, and out-of-range values all resolve to unset.

**Why this criterion carries no regex of its own — the rule, not a note about one edit.** ADR-111's binding consequence is that a consumer reuses the shared detector instead of holding a private grammar, and a criterion that *prints* a grammar is a private grammar: it is what an implementer copies. The illustrative pattern this cell used to carry was unanchored, and run literally against the live corpus it resolved a priority from mid-prose in bodies where the shared detector correctly resolves none — so the spec and the implementation it points at disagreed, on the specificity property ADR-111 §1 names as part of the decision rather than a side effect of it. Do not re-inline a corrected pattern here: a second copy of a grammar drifts from the first by construction, which is the divergence ADR-111 exists to close. State what the criterion requires; let the cited module say how it is matched.

### Kind-Form Applicability (F2)

The applies-to triple answers "which of the three governance-intake templates does this criterion read?". For an **F2 (kind-form)** issue that question has no answer, because the issue was not authored on any of them. This block answers the F2 question instead, and it answers it with a **derivation**, not a declaration.

**The rule, in one sentence.** *A G1 criterion whose predicate reads a named body field applies to an F2 issue if and only if that issue's body declares the field's section; a criterion whose predicate reads no body field applies unconditionally.*

**Why derived and not declared — this is the load-bearing part.** Field presence **varies within the kind-form family**, so any family-wide statement about it is wrong on some member. The story kind form declares a **required** `Acceptance Criteria` field whose own description tells the author to *"start each bullet with a verifiable verb (verify/check/confirm/assert/ensure/validate) OR name a specific file/section/output AND a specific observable state"* — which is G1-05a's own pattern set, restated by the form. The epic kind form declares no Acceptance Criteria, no Evidence and no Priority. A row reading "the kind forms have no such field" would therefore exempt the story cell from a criterion the story form already binds itself to — a **silent blanket exemption**, on the one criterion where form and gate already agree. The derivation cannot make that mistake, because it never asks what family an issue is in; it asks what its body declares.

**What the derivation buys, stated as properties rather than intent:**

- **Total** — every issue has a body, so the predicate is defined for every member of the family, including the kinds that have **no dedicated form at all** and ride the interim `improvement.yml` vehicle. A rule keyed instead to "the form it was authored on" has no evaluator for those, because no signal maps their label back to an authoring form.
- **Per-kind by construction, with no kind named** — the epic/story difference falls out of the bodies; neither id appears here or in any consuming gate.
- **Self-revoking** — a form that gains a field puts that field in its bodies, and those issues enter scope with no edit to this block. An exemption that expires by itself cannot silently outlive its reason.
- **Re-checkable in one command** — the disposition for any live issue is a `grep` against its body, so an auditor reproduces a verdict without reading the gate.

| Field the criterion reads | Criterion | F2 disposition | Grounding |
|---|---|---|---|
| — (reads the title, not a body field) | `G1-01` title informativeness floor | **Applies unconditionally** — identity mapping, no adapter | All six templates carry a prefix-less `title: ""`, and the kind forms bind themselves to the same [`intake-style-guide.md`](../../release/references/how-to/intake-style-guide.md) §7 floor in their own header comments. There is no divergent title convention to translate. |
| `### Evidence` | `G1-03` evidence present | Applies **iff the body declares the section** | The kind forms declare no Evidence field; demanding evidence of a body whose form declares no place to put it fails a card for an omission its form required. What satisfies the criterion is stated once at § Gate 1 G1-03 and is deliberately not restated here — this row governs *applicability*, not the admissible set. |
| `### Acceptance Criteria` | `G1-05a` AC structural pattern | Applies **iff the body declares the section** | Present and **required** on the story kind form, absent on the epic kind form — the cell a family-wide `n/a` gets wrong. |
| `### Priority` / `### Severity` | `G1-06` P-level present | Applies **iff the body declares either section** | Applicability keys on the declared field section; the P-level itself is still resolved through the shared detector cited in Adapter G1-06-Bug, so no second priority grammar is introduced. |
| — (satisfied by Step 0) | `G1-09` label↔template match | **Satisfied by form-family resolution** | The kind label matched against the licensed vocabulary *is* the form determination; there is no label↔body pair left to reconcile. |

**Judgment-tier criteria (`G1-02` / `G1-04` / `G1-05b` / `G1-08`) do not reach F2.** They are advisory and non-gating, and extending the advisory tier to a family it was not written for would add output without adding a verdict. This is a stated scope boundary, not a pending item.

**Composition rule for any criterion-scope statement that consumes this block.** A later change may assign *enforcement authority* (FAIL / RECOMMEND / off) only to a (criterion × family) cell this block does **not** mark out of scope. Under that rule a card cannot be simultaneously exempt here and enforced elsewhere — the contradiction is structurally unreachable rather than merely checked for.

### Self-Repair Actions

| ID | On Failure | Action |
|---|---|---|
| G1-01 | Title carries a leftover `[...]:` type/category prefix | Auto-fix: **strip** the leading `[...]:` token (type is carried by the label, not the title). |
| G1-01 | Title fails the substance floor (bare slug / single token / too short) | Return to author: "Title names an area but not a change — state the object + what changes (≥ 2 words, no bare slug)." Not auto-fixable (no source field to synthesize a summary from). |
| G1-02 (improvement) | `improvement`-labeled, Description is observational | Return to author: "Description states an observation but not an actionable change. Reframe as what should change and why. If this issue is a placeholder gap-capture (no actionable change yet), re-author as `observation.yml` instead." |
| G1-02 (observation) | `observation`-labeled, ≥1 of the three required fields (what is missing / what good looks like / which file or section) is missing, vague, or non-promotable | Return to author: "Observation-tier fields must contain enough signal that a Triage agent could draft a Proposal whose Description would itself pass G1-02 branch (a). Identify which field is non-promotable: [field name]. Promotability test: would a Proposal-tier Description drafted from these three fields be actionable?" |
| G1-02 (label missing) | Neither `observation` nor `improvement` label is present (or both are present) | Return to author or triage agent: "Issue lacks a single primary intake-tier label. Apply `observation` OR `improvement` per `pipeline/stage-01-intake.md` §5 Routing + `intake-style-guide.md` §2 (5-test rule)." |
| G1-03 | No evidence in either admissible shape | Return to author: "The Evidence section needs either (a) at least one `[SOURCE]` / `[INFERRED]` / `[CONTEXT]` / `[ASSUMPTION – CONFIRM]` / `[RECOMMENDED]` label, or (b) a probe record carrying at least two of `Probe:` / `Denominator:` / `Control:` / `Sensitivity:` / `Specificity:` / `Verdict:` in label position — see gate-criteria-spec.md § Gate 1 G1-03." |
| G1-04 (under-specified) | Proposed Change names no file or protocol affected — the WHAT is missing | Return to author: "Proposed Change must name the specific file(s) or protocol(s) affected." |
| G1-04 (over-specified) | Proposed Change prescribes mechanism — algorithm, data structure, implementation pattern, pseudocode, or line-level surgical directive — committing the HOW prematurely | Return to author with the canonical WHAT-not-HOW remediation in [`intake-style-guide.md`](../../release/references/how-to/intake-style-guide.md) §5 (cite the specific Stage 5-column item; offer re-author-in-WHAT **or** `[ASSUMPTION – CONFIRM]`-defer-to-Stage-5). |
| G1-05a | AC bullet fails structural verb/predicate check | Return to author: "AC bullet `[quoted bullet]` does not match a G1-05a pattern. Rewrite as one of: (a) verb-first — 'Verify that `file/section` contains state Y', or (b) file-path+state — '`file/section` contains state Y', or (c) explicit predicate — 'predicate: <expression>', or (d) behavioral/domain — 'predicate: <behavioral/domain outcome>; method: <how it is verified>' — use this when the AC asserts a behavioral/domain outcome that a file-content/system-state rewrite would distort; the method may name an executor that does not yet exist (declared, verification deferred)." |
| G1-05b (per-bullet quality) | AC judgment failure (semantic vagueness, placeholder leakage, or commented-out bullets) | Return to author with specific vague-language call-out, unreplaced-`<...>`-slot identification, and an example rewrite matching a G1-05a pattern. |
| G1-05b (usability-AC coverage) | The item is capability-class per the § Capability-Class Usability-AC Requirement block (C1 ∧ C2 ∧ C3 all hold), and its AC set carries neither a conforming usability AC nor a named exempt class | Return to author: "This item is capability-class — a named actor performs an observable behavior at a named runtime trigger — so its AC set needs one **usability AC** in addition to its presence ACs. Add a bullet in the form `predicate: after a fresh install/deploy, <actor> <verb-phrase> against <named, populated fixture>, producing <stated observable>; method: <invocation + the observable that must be seen>`, satisfying U1 (post-install anchor) / U2 (actor + action verb — not `exists`/`ships`/`contains`) / U3 (named, populated fixture) / U4 (observable is not the edited artifact). If the item is **not** capability-class, name its exempt class instead — E1 reference-content / E2 behavior-preserving refactor / E3 governance-internal rule with no runtime trigger / E4 tracking or bookkeeping / E5 spike, analysis, or decision record. Silence is not an exemption. **If the capability is a mechanical check — a script, lint, hook, gate, or CI job that emits a finding — the set additionally owes U5:** name a concrete near-miss input the check must NOT flag (one differing from a true positive only in the property being tested), state its expected zero as part of the observable, and exercise that arm at each parameter of the check's declared invocation surface that narrows the examined population or changes the comparison set. Enumerate those parameters explicitly — a discrimination arm run only at the check's default invocation does not discharge U5. If the card ADDS the check rather than modifying an existing one, name the intended parameters or mark the enumeration `Stage-5-deferred`, which then binds and is graded at Stage-5 exit. See [`intake-style-guide.md`](../../release/references/how-to/intake-style-guide.md) §4b for the worked pair." |
| G1-06 | Priority missing | Return to author: "Set priority P1-P4 in the issue body per improvement.yml Priority field. Do NOT apply a priority label — label-taxonomy.md tracks priority in the body." |
| G1-07 | Anchors incorrect | Auto-fix: set Status = Proposed, Stage = 1-Intake, apply `status: proposed` label. |
| G1-08 | Pickup-blocking undefined terms | Agent scans issue body for jargon, abbreviations, cross-issue references, and Skills-Map terminology. A term is "undefined" if it lacks (a) an inline parenthetical definition, (b) a `#N` issue reference, or (c) a markdown link to a governance doc that defines it. If undefined count > 3, return to author: "G1-08: The following terms would require a fresh Claude Code session to ask clarifying questions. Define inline or cite governance: [list]." |
| G1-09 (F1 label-body mismatch) | An issue with exactly one intake-tier label whose body structure names a different template | Per Steps 1–3 of the Template Detection Logic block: emit `G1-09 FAIL: label=<X>, body=<Y>` payload. Two remediation paths (operator decision): **(a) Relabel** — change intake-tier label to match the body's structural template; **(b) Rewrite body** — rewrite body to match the existing label's template schema. |
| G1-09 (F0 multiple intake-tier labels) | More than one of `improvement` / `bug` / `observation` is applied | Apply the correct single intake-tier label per `pipeline/stage-01-intake.md` § Routing + `intake-style-guide.md` § 2 (5-test rule). |
| G1-09 (F3 unresolved form) | No intake-tier label **and** no licensed-kind label — the issue belongs to no form family | Return to author or triage agent naming **the labels the issue carries** and the resolved kind vocabulary it was measured against: "This issue matches no intake template and no licensed work-item kind, so no gate can determine which criteria read it. Either apply the intake-tier label for its template, or apply the `type:<kind_id>` label for a kind the configured packs declare." **Do not** hand this case F0's "apply correct single label of improvement/bug/observation" remediation: a card whose only defect is an unlicensed kind would be broken by following it. |

### Priority-Model block (referenced from G1-06 / G2-01 / G2-12)

Priority lives on **two coexisting surfaces** with one canonical source — stated here once so no future reader re-derives a false "missing-label" gap. The model is symmetric to the Decision-Date treatment (body-authored, Projects-field-mirrored at G2-06).

| Surface | Role | Where validated | Notes |
|---|---|---|---|
| Body `### Priority` field (P1-P4; `### Severity` for bug.yml, P-level digit canonical per Adapter G1-06-Bug) | **Canonical source.** | **G1-06** (structural presence at intake) → **G2-01** (validated against full backlog context at Triage) | This is the authored intake expectation per G1-06. **G1-06 is unchanged by the field-first model.** |
| Configured ticketing adapter's priority surface (default: the GitHub Projects Priority field) | **Queryable mirror**, body-derived, written at Triage. | **G2-12** (set + consistent with the body P-level; queryable via the adapter's API, not parsed from comment text) | The mirror exists for downstream automation that prefers an adapter-API field query over body parsing. Resolves per the § Gate 1 Backlog-Surface Resolution note. |
| `priority:` label | **NOT a surface.** No `priority:` label exists in the taxonomy. | — | G1-06 states "label-based priority is NOT expected." Do NOT add a priority label; do NOT enforce one. A `status: approved` issue with no `priority:` label is **conformance**, not a defect. |

**Canonical-source rule (body wins on mismatch).** When the configured priority surface (default: the GitHub Projects Priority field) and the body `### Priority` P-level disagree, the body is authoritative: G2-12 self-repair re-derives the configured priority surface from the body P-level, never the reverse. The body→surface direction is the only direction this model syncs.

**Scope note (avoids the historical stale-reference confusion).** Priority is validated at **G2-01** (and present at **G1-06**), mirrored to the Projects field at **G2-12**. It is **not** validated at **G2-04** — G2-04 is *dependency-reference validity* (`Dependencies reference valid open issues or "None"`), an unrelated criterion. Any text implying "G2-04 = priority validation" is incorrect.

### Priority-Lifecycle (referenced from G1-06 / G2-01)

The single overall P-level field has a **temporal lifecycle** — who sets it, who adjusts it, and who confirms it, across three stages. This is the temporal companion to the spatial surface-model above (that block fixes *where* priority lives; this fixes *when and by whom* it is set or changed). The governing principle: **priority is suggested, not fixed** — no stage treats an upstream P-level as immutable.

| Lifecycle stage | Actor + autonomy | Action on priority |
|---|---|---|
| **Intake (Stage 1)** | author (human or intake agent) — **agent-estimated** | Estimate an initial P-level. Optional at intake (the body field is not required at submission) — a starting suggestion, not a binding value. |
| **Triage (Stage 2 A5)** | triage agent — **agent-re-evaluated** (Tier 1) | **Re-evaluate** (validate-or-adjust) the body P-level against full-backlog context; the body field is canonical, mirrored to the Projects field at G2-12. Re-evaluation may confirm or change the intake estimate. |
| **Bundle (Stage 3)** | operator — **human-confirmed** (Tier 3) | Operator confirms or overrides the final P-level as part of the milestone-assignment decision; the confirmed P-level seeds the downstream priority-descending sequencing tie-breaker. |

**Suggested-not-fixed rule (stated once).** Priority is agent-estimated at intake, agent-re-evaluated at triage with full-backlog context, and human-confirmed at bundle. Each stage may confirm or adjust the upstream value; no stage treats the prior P-level as fixed. This composes with the directional-not-authoritative handoff principle (Stage 2 §4 Inputs) — that principle states the general intake→triage directionality; this states the priority-specific instance of it.

**Decomposition note — urgency separated from blocking/dependency position.** P1–P4 remains the **single overall priority** field (a combined urgency-and-importance judgment); it is NOT split into two columns. The decomposition the model achieves is to (a) make the P-level a re-evaluated estimate rather than a fixed triple-duty token, and (b) lift the **blocking/dependency-position signal out** of the P-level into a separate named triage output (the `Dependency-position signal` per Stage 2 A6), so "what blocks / is blocked" no longer rides hidden inside the P-level. The interpretation rule: the P-level expresses an urgency-and-importance judgment; dependency position is a distinct surfaced signal, not folded into P.

**Altitude boundary (methodology-neutral).** This pipeline-intake priority model (P1–P4 + the separate dependency-position signal) operates at the **release-pipeline altitude** — it decides which platform improvement the operator triages and bundles next. It is distinct from, and MUST NOT be conflated with, the **project-delivery** prioritization in the delivery-engine domain (Weighted-Shortest-Job-First / Cost-of-Delay sequencing for delivery work items of the PMO product, defined under that skill's backlog-health reference and the framework catalog). The two are different altitudes: intake priority gates entry into the release workflow; the delivery-engine method sequences delivery work items. This spec introduces **no** WSJF/CoD definition — it references the delivery-engine home for that altitude and keeps the public corpus methodology-neutral (no value-scoring framework is hardwired into pipeline intake).

**Cutover discipline:** Applies to all releases going forward.

### Backlog-Surface Resolution (referenced from G2-06 / G2-12)

The concrete surface a Decision-Date (G2-06) / Priority (G2-12) write targets **resolves from the configured ticketing adapter by config-field role** — `[adapters].ticketing` today, or its multi-destination `[trackers.*]` successor (#384) — never a hardcoded platform name. **Default `ticketing = "github"` → the GitHub Projects Date field (via `[projects].decision_date_field_id`) and Priority field (via `[projects].priority_field_id`)** — the shipped default, so the existing flow is unchanged. A non-`github` adapter resolves to its own decision-date / priority surface per the **ticketing-adapter mechanics (#11, milestone 59)**; until #11 ships, `github` is the only resolved adapter and the gate behaves identically to today. Resolving by role (not the literal `[adapters].ticketing` path) follows *Parameterize-over-hardcode* and survives the `[adapters].ticketing → [trackers.*]` migration.

---

## Gate 2: Workflow Readiness

*"Has this been enriched enough to enter the workflow?"*

**Stage boundary:** 2->3 (Triage -> Bundle)
**Inherits:** Gate 2->3 from [field-lifecycle-matrix.md](field-lifecycle-matrix.md#gate-2-3-triage---bundle)

| ID | Criterion | Type | Check | Automation | improvement | bug | observation | Validation method |
|---|---|---|---|---|---|---|---|---|
| G2-01 | Body `### Priority` P-level (the canonical priority surface per G1-06; `### Severity` for bug.yml) re-evaluated (validated-or-adjusted) against full backlog context per the Priority-Lifecycle sub-block. Re-evaluates the **body** field — not a label (none exists) and not the Projects mirror (that is G2-12). Per the Priority-Model block above. | field | judgment | recommend | `req` | `adapt:G2-01-Bug` | `conv` (must convert before bundling — Priority field missing) | — |
| G2-02 | Category label matches content | anchor | judgment | recommend | `req` | `req` (auto-applied `bug` label) | `conv` (observation has no category) | — |
| G2-03 | No unresolved duplicates or subsumption conflicts | validation | structural | auto | `req` | `req` | `req` | — |
| G2-04 | Dependencies reference valid open issues or "None" | field | structural | auto | `req` | `n/a` (no Dependencies field in bug.yml) | `n/a` (no Dependencies field) | — |
| G2-05 | Cluster label assigned | anchor | structural | auto | `req` | `req` | `req` | — |
| G2-06 | Decision Date set in the configured ticketing adapter's decision-date surface (default: the GitHub Projects Date field; queryable via the adapter's API, not parsed from comment text) — per the § Gate 1 Backlog-Surface Resolution note | anchor | structural | auto | `req` | `req` | `req` | — |
| G2-07 | Triage decision comment posted | artifact | structural | auto | `req` | `req` | `req` | — |
| G2-08 | Management-action items routed (if applicable) | validation | judgment | recommend | `req` | `req` | `req` | — |
| G2-09 | No unflagged synthesis-candidate pairs against the open Proposed/Approved set. Candidate pair = candidate issue + any open Proposed/Approved issue sharing (a) `cluster:*` label AND (b) ≥1 explicit cross-reference edge (Dependencies-field reciprocity OR shared upstream parent `#N` cited in both Dependencies fields). For each candidate pair: routing decision recorded (fold / decompose-into-roadmap / keep-separate-with-rationale / defer-for-coordination). Per Similarity Composite-Signal Detection block below. | validation | judgment | recommend | `req` | `req` | `req` | — |
| G2-10 | Size-driven decomposition routing recorded when `size:XL` label applied. For `size:XL`: routing decision recorded (decompose-into-slice / split-into-sub-issues / approve-as-is-with-risk-note / defer-for-pre-bundle-analysis). For `size:L`: informational flag only (no routing decision required at G2-10; carry forward to G3-09). For `size:M` / `size:S` / `size:XS`: exempt. | validation | judgment | recommend | `req` | `req` | `req` | — |
| G2-11 | Decomposition-review routing recorded when ANY oversize predicate matches (COMPOSITE-OR: **P1** `size:XL` label applied OR **P2** issue body cites declared decomposition hooks — at least 1 occurrence of literal `Decomposition hook` OR enumerated scope-split pattern `(a) ... (b) ... (c)` inside Notes/Proposed Change sections OR **P3** AC count ≥ 7 OR Affected Files count ≥ 5). For each matching predicate set: routing decision recorded per the 3-outcome enum — **kept-as-one with rationale** (operator records 1-line rationale naming the predicate(s) that fired + why issue is genuinely atomic despite firing) / **split per fission protocol** ( — invokes [fission-convention.md](../../release/references/protocols/fission-convention.md) Procedure Steps 1-4: parent body Fissions-into annotation + per-child creation + Fission Comment + parent close-as-fissioned OR convert-to-tracking) / **escalate** (Tier 2 [SCOPE CHANGE] per [`release-process.md`](../../release/governance/release-process.md) § Inter-Stage Feedback Protocol — issue stays in `status: proposed` until operator-rendered disposition). G2-11 SUBSUMES G2-10 when its predicate fires (one routing decision under G2-11 — NOT one routing under G2-10 plus a second routing under G2-11). G2-11 PASSES trivially when no predicate fires. Per Composite-OR Oversize Predicate block below. | validation | judgment | recommend | `req` | `req` | `req` | — |
| G2-12 | Priority set in the configured ticketing adapter's priority surface (default: the GitHub Projects Priority field) and consistent with the body `### Priority` P-level (queryable via the adapter's API, not parsed from comment text) — per the § Gate 1 Backlog-Surface Resolution note. The body `### Priority` field is canonical (validated at G2-01, per G1-06); the configured priority surface (default: the GitHub Projects Priority field) is its queryable mirror, written at Triage. G2-12 asserts the mirror exists and its P-level digit (1-4) matches the body P-level. **Body wins on mismatch** — self-repair re-derives the configured priority surface from the confirmed body P-level, never the reverse. Modeled on G2-06 (Decision Date in the configured decision-date surface), the in-corpus field-first precedent. Per the Priority-Model block in § Gate 1 (body-canonical · configured-priority-surface queryable-mirror · label-NOT-a-surface). | anchor | structural | auto | `req` | `adapt:G2-12-Bug` | `conv` (no Priority field — converts before bundling) | — |
| G2-13 | **Acceptance-fit determination recorded (elevated-rigor subset).** When an issue's provenance is agent-authored (body carries `<!-- provenance: agent-authored -->`) OR the architecture evaluative-lens (Stage 2 A4) flagged it as introducing/reshaping a component (net-new build or sweeping/cross-cutting change), the **Approve** verdict requires a recorded `#### Acceptance-Fit` block in the triage decision comment of the form `relates to {named anchor: ADR \| initiative/epic \| governing standard \| named discipline} — compatible because {evidence}`. Absent a credible anchor, the expected disposition is **Reject or Defer** (anchor-less ideas in the subset are expected-Reject, not a triage failure). **PASSES trivially** when neither predicate limb fires — routine operator-authored, non-component-reshaping tickets stay on the advisory architecture-lens path (no re-litigation, per the directional-not-authoritative handoff bound). The provenance marker is an acceptance-**scrutiny** signal only, never a trust/authorization input — trust is decided solely by the author-association boundary, from the API, never from the body or the marker (defense-in-depth per ADR-093). Per the Acceptance-Fit Predicate block below. | validation | structural | auto *(ENFORCE from v3.94 — a missing determination blocks the Approve verdict when a limb fires; operator-ratified at the v3.94 Stage-9 GO gate 2026-07-25 per ADR-093; introducing-release-exempt)* | `req` | `adapt:G2-13-Bug` | `conv` | grep the triage decision comment for `#### Acceptance-Fit` when the fire-predicate holds |

### Composite-OR Oversize Predicate (referenced from G2-11 / G3-12 criterion bodies)

A candidate issue triggers oversize-decomposition review when ANY of the following predicates matches:

**P1 — `size:XL` label applied.** Verifiable: `gh issue view N --json labels` filtered for `size:XL`. Subsumes existing G2-10 baseline. Backwards-compatible.

**P2 — Body cites declared decomposition hooks.** At least 1 occurrence of literal `Decomposition hook` OR enumerated scope-split pattern `(a) ... (b) ... (c)` inside Notes/Proposed Change sections. Verifiable: `grep -ci "decomposition hook\|scope may split into\|sub-scopes\?:\s*(a)\|(a)\s*[^)]*\s*(b)\s*[^)]*\s*(c)"` over issue body. Catches the ** canonical-evidence-instance gap** —  body declared `Decomposition hook for Triage: scope may split into (a)/(b)/(c)` yet was `size:M`. G2-10 missed it.

**P3 — AC count ≥ 7 OR Affected Files count ≥ 5.** Verifiable: `grep -c "^- \[ \]"` (AC list checkboxes) OR `grep -cE "^\\\`?(\\\.claude|pmo-platform|projects)/" Affected\\\ Files\\\ section` (file pointer lines) over issue body. Catches silent-mis-size — a ticket labeled `size:M`/`size:L` whose AC/file fan-out exposes oversize. Calibrate-after-3 MEDIUM-confidence thresholds (`7` / `5` chosen from current-state median+1σ).

**Tie-break (operator override):** If predicate fires but operator judges the ticket genuinely atomic (e.g., exhaustive testing AC list rather than scope multiplicity), operator records `kept-as-one with rationale` — predicate firing gates a decision, does not auto-route to split.

**Detection command (reproducible):**

```bash
# Composite-OR oversize predicate check at triage/bundle time
ISSUE=<N>
BODY=$(gh issue view $ISSUE --json body --jq '.body')
LABELS=$(gh issue view $ISSUE --json labels --jq '.labels[].name')
P1=$(echo "$LABELS" | grep -c '^size:XL$')
P2=$(echo "$BODY" | grep -ciE 'decomposition hook|scope may split into|sub-scopes?:.*\(a\)|\(a\)[^)]+\(b\)[^)]+\(c\)')
P3_AC=$(echo "$BODY" | grep -c '^- \[ \]')
P3_FILES=$(echo "$BODY" | grep -cE '^`?(\.claude|pmo-platform|projects)/')
# G2-11/G3-12 predicate fires when (P1 ≥ 1) OR (P2 ≥ 1) OR (P3_AC ≥ 7) OR (P3_FILES ≥ 5)
```

### Acceptance-Fit Predicate (referenced from G2-13 criterion body)

G2-13 is a **scoped, conditional** acceptance-quality criterion: it fires only when architectural fit is genuinely in question, so a routine operator-authored ticket is never interrogated (honoring the deliberate advisory posture of the Stage-2 A4 architecture evaluative-lens and the directional-not-authoritative handoff's "not a license to re-litigate every ticket" bound). The criterion consumes the A4 lens's advisory classification as one fire-limb — it does **not** make the A4 lens itself binding.

**Two-limb fire predicate (OR).** G2-13 fires when EITHER limb holds:

- **Limb 1 — agent-authored provenance.** The issue body carries the machine-emitted marker `<!-- provenance: agent-authored -->` (emitted by the agent auto-intake path per [`pipeline/stage-01-intake.md`](../../release/references/pipeline/stage-01-intake.md) §5/§6 and by [`intake-desk`](../../operations/skills/intake-desk/SKILL.md) Mode C). Verifiable: `grep -qF '<!-- provenance: agent-authored -->'` over the issue body.
- **Limb 2 — net-new / sweeping.** The A4 architecture evaluative-lens flagged the proposal as introducing or reshaping a component (a new skill / new Layer-1 structure / new top-level capability, or a sweeping / cross-cutting / governance-touching change). This is **consumed** from the A6 triage summary's architecture-lens surfacing — **not re-derived** here.

**Trivial PASS.** When neither limb fires (routine operator-authored, non-component-reshaping ticket), G2-13 passes with no determination required — the ticket stays on the advisory A4 path.

**The determination (when a limb fires).** The **Approve** verdict requires a `#### Acceptance-Fit` block in the triage decision comment:

```
#### Acceptance-Fit
relates to {named anchor} — compatible because {evidence}
```

where `{named anchor}` is drawn from the anchor taxonomy — **an ADR · an initiative/epic · a governing standard · a named discipline**. Absent a credible anchor, the expected disposition is **Reject or Defer**: an anchor-less idea in the elevated-rigor subset is *expected* to be Rejected, and that Reject is a correct, first-class terminal outcome — not a triage failure or a backlog-management gap. **Elevated rigor for net-new / sweeping:** a net-new build or a sweeping / cross-cutting change additionally records a one-line blast-radius / reversibility note alongside the anchor before acceptance.

**Where to look for the anchor (consultation surface — non-normative; adds no limb and no new required anchor).** When identifying the `{named anchor}`, the platform's architecture baselines and their priority order are rostered in [`architecture-conformance-cadence.md`](../../release/references/protocols/architecture-conformance-cadence.md) §5 Baseline Continuity — the as-built conformance audit this gate is the forward, per-ticket counterpart of. That roster is **cited here, not restated**, so this pointer stays correct when a baseline is added or re-tiered. Within it, [`cross-chain-architecture-map.md`](../disciplines/cross-chain-architecture-map.md) is the consolidated management-chain surface new intake work is checked for architecture-fit against — consult it **by chain name** to route the proposal to the management chain it touches and reach that chain's governing home, rather than reconstructing the chain's governance from scratch per ticket. G2-13's two-limb fire predicate, its determination form, and its `Reject or Defer` disposition are unchanged by this pointer.

**Non-authorization invariant (security boundary — defense-in-depth).** The provenance marker is an **acceptance-scrutiny** signal only; it MUST NEVER be a trust or authorization input. Firing G2-13 on a marked issue *raises* the acceptance bar (it demands an anchor; the expected disposition is Reject) — it never reduces checks or grants trust. Trust is decided **solely** by the author-association boundary, from the repository-relationship API field, never from the issue body or from this marker (per ADR-093, folding the issue-body author-association trust extension). The marker being body-spoofable is therefore harmless: a spoofed marker only self-imposes *more* scrutiny on the spoofer's own issue. No future consumer may read "agent-authored" as "skip human review / auto-approve."

**Enforcement (enforce from v3.94 — operator-ratified flip).** G2-13 **enforces from v3.94**: when a limb fires, a missing `#### Acceptance-Fit` block **blocks the Approve verdict** (gate-blocking, day-one). This supersedes the criterion's originally-authored shadow → warn → enforce graduation rollout — the flip to day-one enforce was **operator-ratified at the v3.94 Stage 9 Plan Review GO gate (2026-07-25)** per ADR-093 (reversibility MODERATE; the Automation posture is the single operator-ratification lever, now set to enforce). **Introducing-release-exempt (cutover unchanged):** v3.94 itself is not gated by G2-13 — the release shipping the flip cannot fire its own gate (reflexive-pipeline-loop discipline); enforcement binds issues entering Stage 2 Triage strictly AFTER the v3.94 introducing-release merge SHA, with pre-cutover issues grandfathered. (The original rollout mirrored the SR-G5 shadow precedent in `stage-05-solutioning.md`; SR-G5 itself **remains shadow** — the SR-G5 / SR-G6 postures are governed independently there, and the SR-G5-shadow / SR-G6-enforce asymmetry is intentional per ADR-094.)

**Detection command (reproducible):**

```bash
# Acceptance-fit fire-predicate check at triage time
ISSUE=<N>
BODY=$(gh issue view $ISSUE --json body --jq '.body')
LIMB1=$(printf '%s' "$BODY" | grep -cF '<!-- provenance: agent-authored -->')   # agent-authored provenance
# LIMB2: read the A4 architecture-lens component-introduction flag from the A6 triage summary (not re-derived)
# G2-13 fires when (LIMB1 ≥ 1) OR (LIMB2 flagged); on fire, Approve requires a `#### Acceptance-Fit` block
```

### Roadmap-Cascade Validation (referenced from G3-13 criterion body)

**RETIRED (ADR-012, 2026-06-02).** Roadmap instances are operator-local, so in-repo cascade detection no longer runs. The `G3-13` criterion is retained as a numbered tombstone in the Gate 3 table below; the former enumerate / branch / classify procedure and detection script are removed.

### Gate 2 Adapter Blocks

**Adapter G2-01-Bug:** validate Severity (not Priority) against full backlog context — semantic equivalence per Adapter G1-06-Bug (P-level digit canonical).

**Adapter G2-12-Bug:** bug.yml has `### Severity` (P1-Blocker / P2-Material / P3-Annoyance / P4-Cosmetic) in lieu of `### Priority`; G2-12 asserts the configured priority surface's (default: the GitHub Projects Priority field) P-level digit (1-4) matches the body **Severity** P-level. Semantic equivalence per Adapter G1-06-Bug — the P-level digit is canonical, not the field name or qualifier word. Body (Severity) wins on mismatch, identical to the improvement (Priority) branch.

**Adapter G2-13-Bug:** for a `bug`-labeled issue the acceptance-fit predicate's **provenance limb (Limb 1)** applies unchanged — an auto-logged bug carries the `<!-- provenance: agent-authored -->` marker, and when present the Approve verdict records the `#### Acceptance-Fit` block (a bug's anchor is typically a governing standard / named discipline / the defective component itself). The **net-new / sweeping limb (Limb 2) rarely fires for a defect** — a bug fix restores existing behavior rather than introducing or reshaping a component — so the common operator-reported bug with no provenance marker passes G2-13 trivially. No Severity↔Priority semantic translation is needed: G2-13 keys on provenance + component-introduction, not on the P-level.

**G2-04 augmentation — Native-Dep Mirror (post-validation):**

Post-validation, the Stage 2 spoke fires the A3.5 native-mirror substep per [`pipeline/stage-02-triage.md` § Native-Dep Mirror](../../release/references/pipeline/stage-02-triage.md). The mirror syncs body `FS+0d` dependencies to native `blocked-by`. A3.5 is NOT gate-blocking — G2-04 PASS/FAIL is determined solely by the validation criterion in the table row above; A3.5 is an additive sync side-effect that fires once the gate has passed. Native-mirror failures (API errors, scope issues, cap hits) surface in the A6 triage summary for operator awareness but do not block Phase B verdict. Per [`ticket-information-architecture.md § Native Dependencies`](../../release/references/specs/ticket-information-architecture.md#native-dependencies). Cutover discipline: the native-dep mirror applies to all triaged issues going forward.

**Similarity Composite-Signal Detection (referenced from G2-09 / G3-08 criterion bodies):**

A candidate issue triggers similarity-pair detection when, for any open Proposed/Approved/Bundled issue B:

1. **Cluster axis (required).** Candidate and B share at least one `cluster:*` label per [label-taxonomy.md § Cluster Labels](../specs/label-taxonomy.md).
2. **Edge axis (required — either OR).** At least one of:
   - **Reciprocity:** Candidate's Dependencies field cites `#B` OR B's Dependencies field cites `#<candidate>`.
   - **Shared parent:** A third issue `#P` cited in BOTH Candidate's Dependencies and B's Dependencies.

Both axes required. Pure cluster-label match without edge axis is NOT a candidate pair (avoids false-positive flood on hub clusters like `cluster: process-protocol`). Pure edge axis without cluster match is downgraded to advisory (cross-cluster coordination is common and rarely indicates synthesis).

**Detection command (reproducible):**

```bash
# Candidate similarity-pair search at triage time
CAND=<candidate-issue>
CLUSTER=$(gh issue view $CAND --json labels --jq '.labels[] | .name | select(startswith("cluster:"))')
gh issue list --search "label:\"$CLUSTER\" is:open" --json number,labels,body --jq \
  '.[] | select(.number != '$CAND') | {n: .number}'
# Cross-check each result against candidate Dependencies field reciprocity + shared parent
```

Soft-language exclusion: "related to" / "similar to" / "see also" in body text is NOT an edge axis hit — the field must be the Dependencies section, not free-form prose.

### Self-Repair Actions

| ID | On Failure | Action |
|---|---|---|
| G2-01 | Priority not validated | Present priority comparison against backlog to operator for confirmation or adjustment. |
| G2-02 | Category mismatch | Recommend category correction to operator based on issue content analysis. |
| G2-03 | Unresolved duplicates | Flag duplicate candidates with similarity rationale for operator resolution (merge, subsume, or dismiss). |
| G2-04 | Invalid dependency refs | Flag invalid references: closed/deleted issues, self-references, circular chains. List valid alternatives. |
| G2-05 | Cluster label missing | Recommend cluster from category taxonomy. Apply if unambiguous; flag if multiple candidates. |
| G2-06 | Decision Date not set in the configured decision-date surface | Auto-set the configured ticketing adapter's decision-date surface (default `github`: the GitHub Projects Date field) per `release/references/pipeline/stage-02-triage.md` § Phase B B2a (forcing-function and failure-handling block) at CER Resolve. On adapter-API failure: gate fails; CER Resolve produces a documented failure outcome (does not silently auto-pass). |
| G2-07 | No triage comment | Draft triage decision comment from analysis results for posting. |
| G2-08 | Routing incomplete | Present unrouted management-action items to operator with recommended routing targets. |
| G2-09 | Candidate pair detected with no routing decision | Surface candidate pair to operator with detection evidence (cluster overlap, edge axis specifics). Present 4 routing options (A fold / B decompose-into-roadmap / C keep-separate-with-rationale / D defer-for-coordination); operator renders. Record decision in Triage decision comment per § Phase B Output State Semantics + apply outcome-specific labels per [subsumption-convention.md](../../release/references/protocols/subsumption-convention.md). |
| G2-10 | size:XL ticket with no decomposition-routing decision | Surface size:XL ticket to operator with decomposition options (A decompose-into-slice / B split-into-sub-issues / C approve-as-is-with-risk-note / D defer-for-pre-bundle-analysis); operator renders. Record decision in Triage decision comment + ticket body Notes/Risks per outcome. size:L cross-stage informational flag posted as advisory comment; no required decision. |
| G2-11 | Oversize predicate (P1 `size:XL` OR P2 declared-decomposition-hook OR P3 AC ≥ 7 OR P3 files ≥ 5) fired with no 3-outcome routing decision recorded | Surface predicate-firing evidence to operator with the matched predicate(s) and the 3-outcome enum (A kept-as-one with rationale / B split per [fission-convention.md](../../release/references/protocols/fission-convention.md) / C escalate per Tier 2 [SCOPE CHANGE]); operator renders. Record decision in Triage decision comment per § Phase B Output State Semantics. On SPLIT outcome: invoke fission-convention.md Procedure Steps 1-4 (parent body Fissions-into annotation + per-child creation + Fission Comment + parent close-as-fissioned OR convert-to-tracking per its D-ParentDisposition). On KEPT-AS-ONE outcome: record 1-line rationale naming the predicate(s) that fired. On ESCALATE outcome: flag hub for operator decision per Inter-Stage Feedback Protocol Tier 2; issue stays in `status: proposed` until operator-rendered disposition. |
| G2-12 | Priority not set in the configured priority surface, OR set to a P-level inconsistent with the body `### Priority` (Severity for bug) P-level | Auto-set the configured ticketing adapter's priority surface (default `github`: the GitHub Projects Priority field) from the confirmed body P-level (for the `github` default, the `gh project item-edit … --field-id [OPERATOR_PROJECTS_VIEW_FIELD_ID] --single-select-option-id <P-LEVEL_OPTION_ID>` write at CER Resolve — the Priority Projects-field write that already exists, per `release/references/pipeline/stage-02-triage.md` § Phase B B2a + [github-projects-guide.md § Field IDs](../disciplines/github-projects-guide.md#field-ids-for-agent-integration)). **Body wins on mismatch** — re-derive the configured priority surface from the body P-level, never edit the body to match the surface. On adapter-API failure: gate fails; CER Resolve produces a documented failure outcome (does not silently auto-pass), mirroring the G2-06 Decision-Date forcing-function posture. |
| G2-13 | Fire-predicate holds (agent-authored provenance OR A4-flagged net-new/sweeping) but no `#### Acceptance-Fit` block recorded | Surface the two-limb evidence (which limb fired) + the anchor taxonomy (ADR / initiative-epic / governing standard / named discipline) to the operator. If a credible anchor exists, prompt for the `relates to {anchor} — compatible because {evidence}` determination; if none does, recommend **Reject or Defer** (anchor-less in the elevated-rigor subset is expected-Reject, not a triage failure). Record the outcome in the triage decision comment. Enforce-mode posture (from v3.94, operator-ratified 2026-07-25 per ADR-093): block the Approve verdict until the `#### Acceptance-Fit` block is recorded, operator override per the MODERATE reversibility tier. Introducing-release-exempt: v3.94's own issues and any pre-cutover issues are grandfathered (no block). |

---

## Gate 3: Release Readiness

*"Can this be responsibly assigned to a release?"*

**Stage boundary:** 3->4 (Bundle -> Planning)
**Inherits:** Gate 3->4 from [field-lifecycle-matrix.md](field-lifecycle-matrix.md#gate-3-4-bundle---planning)

**Body-compliance precondition for the `status: bundled` transition.** Before transitioning any issue from a pre-bundled status (`status: proposed` / `status: approved`) to `status: bundled`, re-run the G1 (Triage Readiness) and G3 structural checks against the issue **body** — do not infer compliance from label state. Labels reflect intent, not body-level compliance: a `status: approved` label does not imply the body satisfies G1-01..G1-08, and a title's **presence** does not imply the Priority / Affected Files / Acceptance Criteria fields exist. Verifying at bundle-time is strictly cheaper than discovering a body defect mid-Stage-4 Planning, which forces a rollback and re-triage. This precondition has highest value on the cases where label-to-body drift is most likely:

- multi-issue bundling operations (several issues bound at once);
- cross-milestone routing decisions;
- bundling of issues whose bodies may have aged since their last G1 evaluation;
- any transition that skips the standalone `status: approved` state (`status: proposed` → `status: bundled` directly);
- any bundling that pulls an `observation`-tier issue into a delivery milestone (the observation template cannot satisfy improvement-tier G1 — conversion per the Template-Conversion Rule below is required first).

Minimum re-check set when the precondition applies: **G1-01** (title-informativeness floor), **G1-03** (evidence present per § Gate 1 G1-03, per-template per the Adapter/applicability rules), **G1-05a presence** (an Acceptance Criteria section exists and carries ≥1 criterion — asserted mechanically per the G3-05 row below, independently of heading depth, template era and criterion list form), **G1-06** (Priority/Severity present, multi-line-aware per Adapter G1-06-Bug), **G3-01** (every dependency reference in a compatible state), **G3-02** (no circular dependency chain — construct the intra-bundle graph and traverse it, do not merely scan). Single-issue milestone moves, sub-task decompositions within an already-bundled parent, and operator-explicit "skip with rationale" are out of scope (defer to the Stage 4 Planning gate).

| ID | Criterion | Type | Check | Automation | improvement | bug | observation | Validation method |
|---|---|---|---|---|---|---|---|---|
| G3-01 | All dependencies in compatible states (Approved/Bundled/Done) | validation | structural | auto | `req` | `req` | `conv` | — |
| G3-02 | No circular dependency chains | validation | structural | auto | `req` | `req` | `conv` | — |
| G3-03 | Affected files identified (at least directional) | field | structural | auto | `req` | `req` | `conv` (no Affected Files field) | — |
| G3-04 | Scope is implementation-ready (specific enough for planning) | field | judgment | recommend | `req` | `req` | `conv` | — |
| G3-05 | AC are measurable (can be verified in Stage 7/8) **AND, where the project declares `deliverable_type` (project-schema.md §4), the AC verification method conforms to the project's `deliverable_type` per the matching domain guide** (`core/standards/domain-best-practices/<deliverable_type>.md`) — i.e., an AC's "verifiable" predicate is judged against the verification approach the deliverable's domain expects (a `software` deliverable's AC verified by test/CI evidence; a `governance` deliverable's by grep/file-state assertion; a `web` deliverable's by the component/UI verification its guide names), per the §5A Domain-Axis Consumption Pattern. When `deliverable_type` is absent (legacy) or has no shipped guide (CASE D-2), G3-05 evaluates the base measurability check unchanged (no domain conditioning) — the conditioning **adds** a domain-conformance lens, it never removes the base verifiability requirement. **AC PRESENCE is mechanically asserted at the `status: bundled` transition.** Measurability above stays a judgment call; whether an Acceptance Criteria section exists at all, carrying ≥1 criterion, is decided by `parse_acceptance_criteria()` in `release/tools/bundle-issues-parser.py` and consumed at two moments: Stage 3 Bundle (the transition itself) and `deploy.sh --check` Check 22's G1-05a **presence arm** (a deploy-time backstop that catches a body edited after bundling, which the bundle-time evaluation structurally cannot see). The assertion is **independent of heading depth (H2–H4), of template era, and of criterion list form** (checkbox, plain bullet or ordered) — it delegates heading matching to the shared section detector rather than keying on a template field, so a body predating the current issue template is not silently exempt for want of a field to find absent. It asserts **presence and non-emptiness only**: criterion *shape* remains G1-05a's structural check, *measurability* remains this row's judgment check, and AC *content* requirements remain with their own owners. **Cutover posture — the Check-22 arm ships in the check's existing `g1-enforcement.mode` `warn` posture:** a violation warns and the run continues, so no pre-existing bundled card is retroactively blocked, and the warn→enforce flip is the mode file rather than an edit to the check. This posture is deliberately **not** a merge-SHA grandfathering anchor: no label-application timestamp is readable inside Check 22's one-issue-list-read budget, so a SHA anchor could be written but never evaluated — a cutover clause the check cannot evaluate reads like a gate and is not one. The Stage-3 evaluation is forward-only by construction: it reads bundling candidates, so no pre-existing card is ever in its population. | field | judgment | recommend | `req` | `adapt:G3-05-Bug` | `conv` (no AC field) | — |
| G3-06 | No blocking issues in incompatible states | validation | structural | auto | `req` | `req` | `conv` | — |
| G3-07 | For every dependency edge `#A → #B` declared on an in-bundle issue, milestone-position(A's milestone) ≥ milestone-position(B's milestone), where milestone-position is computed deterministically per the Milestone-Position Resolution algorithm below. Edges registered in the candidate milestone's `## Dependency Exceptions` block PASS as governed exceptions; unregistered violating edges FAIL. | validation | structural | auto | `req` | `req` | `conv` | — |
| G3-08 | No unflagged synthesis-candidate pairs within the candidate bundle. Pair detection per Similarity Composite-Signal Detection block in § Gate 2. Each in-bundle pair: routing decision recorded in bundle rationale comment (fold / decompose-into-roadmap / keep-separate-with-rationale / defer-for-coordination). G3-08 PASSES trivially when bundle has 0 candidate pairs. Cross-bundle pairs (one issue in bundle, one outside) are advisory at G3-08; their primary surface is G2-09 at triage time. | validation | judgment | recommend | `req` | `req` | `req` | — |
| G3-09 | For every `size:XL` issue in candidate bundle: routing decision recorded in bundle rationale comment (decompose-into-slice / split-into-sub-issues / approve-as-is-with-risk-note / defer-for-pre-bundle-analysis). For every `size:L` issue with cross-stage scope (issue body references ≥2 distinct pipeline stages per `release/references/pipeline/stage-*.md` filenames): informational flag in bundle rationale, no blocking. Issues without a `size:*` label trigger G3-09 FAIL with self-repair `Apply size label per label-taxonomy.md before bundling.` G3-09 PASSES trivially when bundle has 0 `size:XL` issues and all sized issues meet routing-decision recording. | validation | judgment | recommend | `req` | `req` | `req` | — |
| G3-10 | Release Class declared in milestone description per the taxonomy. Class value MUST be one of the 4-value enum: `routine` / `novel` / `cross-cutting` / `hotfix`. Non-empty `Rationale` sub-field REQUIRED. See release-class-taxonomy.md for canonical schema + per-class differentiation matrix (engagement density / review depth / Stage 5 activation bias / Stage 13 outcome-window). Class is orthogonal to `delivery_approach` (Scrum/Kanban/Waterfall) and orthogonal to Autonomy Tier (per-action). **Cutover discipline:** Release Class declaration is required for all milestones going forward. | artifact | structural | auto | `req` | `req` | `req` | After Phase B3, grep `^## Release Class$` AND `^\*\*Class:\*\*` against milestone description (`gh api repos/<owner>/pmo-platform/milestones/<N> --jq .description`) |
| G3-11 | Release Outcome Statement heading present in milestone description per the template. Bundle blocked until satisfied. See release-outcome-statement-template.md for canonical empty-shape (REQUIRED AFTER + BEFORE; OPTIONAL Actor(s) + Success Indicator). **Graduated advisory→blocking.** The usual objection to graduating an advisory gate is that it strands in-flight work; here the graduating population was measured and it was empty. At the flip, the milestones lacking the Outcome Statement were *exactly* the milestones lacking the Release Class (19 of 42 open milestones lacked both; the two failing sets were identical), and G3-10 already blocks that set — so the graduation newly blocked **zero** milestones. **Cutover discipline:** The Release Outcome Statement heading is required for all milestones going forward. | artifact | structural | auto | `req` | `req` | `req` | After Phase B3, grep `^### Release Outcome Statement$` against milestone description (`gh api repos/<owner>/pmo-platform/milestones/<N> --jq .description`) |
| G3-12 | For every in-bundle issue: decomposition-review 3-outcome routing recorded in bundle rationale comment when ANY oversize predicate matches (COMPOSITE-OR per § Composite-OR Oversize Predicate block: **P1** `size:XL` label OR **P2** declared decomposition-hook in body OR **P3** AC count ≥ 7 OR file count ≥ 5). Routing enum: kept-as-one with rationale / split per [fission-convention.md](../../release/references/protocols/fission-convention.md) / escalate per Tier 2 [SCOPE CHANGE]. G3-12 is the drift-backstop for G2-11 — catches issues whose AC/file fan-out grew post-Triage. Issues that already routed at G2-11 (Triage) pass G3-12 trivially unless body has materially changed since Triage. G3-12 SUBSUMES G3-09 when its predicate fires (the 3-outcome enum replaces the 4-option G3-09 enum: decompose-into-slice → split; split-into-sub-issues → split; approve-as-is-with-risk-note → kept-as-one; defer-for-pre-bundle-analysis → escalate). G3-12 PASSES trivially when bundle has 0 issues matching any oversize predicate. **Cutover discipline:** Applies to all releases going forward. | validation | judgment | recommend | `req` | `req` | `req` | — |
| G3-13 | **RETIRED (ADR-012, 2026-06-02).** Roadmap-cascade validation de-scoped: roadmap instances are operator-local, so in-repo cascade detection no longer applies. Retained as a numbered tombstone so cross-references resolve. | - | - | - | - | - | - | — |
| G3-14 | **Mode A combined-clean parse-rate floor.** Assert the `release-planner` Mode A combined-clean parse rate ≥ `[bundling].mode_a_parse_rate_floor` (default ≥ **0.90**), read by config-field role (the magnitude is never restated here — parameterize-over-hardcode), computed against the **conformant-bundleable denominator** per `release-planner` SKILL.md Step 1.5 partition group (1) — `improvement`/`bug` bodies with a recognized `### Affected Files` heading; **EXCLUDES** Step-1.5 group (2) type-excluded (`sub-task`/`observation`/`adr`/`[Initiative]`/umbrella-roster) and group (3) needs-body-repair, so bodies the parser was never meant to read do not depress the gated rate. The rate is `combined_clean_parse_rate = \|{ issue ∈ conformant_set : parse_status == "clean" }\| / \|{ issue ∈ conformant_set : parse_status ∈ ("clean","failed") }\|`. `parse_status` is the parser's three-valued enum `{"clean","deferred","failed"}` (`release/tools/bundle-issues-parser.py` — `IssueRecord.parse_status`, combined from the `### Affected Files` + `### Dependencies` extractors): `"clean"` ⇔ both fields extracted without a parse-error/BLOCKING signal; `"failed"` ⇔ a determinate parse failure on ≥1 field; `"deferred"` ⇔ an operator deferral-marker left the field indeterminate. **`deferred` bodies are EXCLUDED from the denominator** (treated like needs-body-repair — set aside, not failed) so the rate counts only determinate outcomes and stays reproducible (a deferral-marker cannot inflate or deflate the rate). **Denominator-coverage co-gate (anti-gaming):** the gate also reports and co-asserts the `needs-body-repair` count alongside the rate (the count already surfaced at `release-planner` SKILL.md Step 1.5 Denominator rule); a denominator shrunk by an unexpected jump in set-aside bodies is surfaced loud, so a high rate over a hollowed-out conformant set cannot quiet-pass. Evaluated at the **Stage 3 → 4 (Bundle → Planning) boundary** BEFORE A2/A8 dep-graph output is consumed by Stage 3 Bundle and Stage 4 Planning. Trivial-PASS when the determinate denominator is empty (no `clean`/`failed` conformant bodies → no rate to assert), analogous to G3-07/G3-08/G3-09 empty-population trivial-pass — prevents a divide-by-zero false-BLOCK. On breach → route to the 3-outcome operator disposition (repair-then-recompute / proceed-with-documented-override / escalate-to-parser-fix) per the self-repair row, NOT a dead-end fail. **Posture:** warn-then-enforce — below-floor logs to the shared deploy-check warn log (`$(pmo_instance_path)/deploy-check-warn-log.jsonl`) under `check: "g3-14"` and Bundle/Planning proceeds with an operator-override rationale during the shakedown window — **specified, not yet emitting**: no check emits this gate's FAIL row, and the per-gate sink `gate-g3-14-warn-log.jsonl` this criterion previously declared was never written and is RETIRED (W1 · `core/standards/gate-efficacy-standard.md`); flip to enforce after 2-3 releases (`[CALIBRATE-AFTER-3]` on the 0.90 floor). **Cutover discipline:** applies to releases entering Stage 3 strictly AFTER this criterion's introducing-release merge SHA recorded in the release log; pre-cutover releases grandfathered; the introducing release itself exempt (reflexive-pipeline-loop discipline). | validation | metrics | recommend (warn → enforce) | `req` | `req` | `req` | — |
| G3-15 | **Per-milestone risk-weighted size bound.** Sum the bundled membership's risk-weighted points and assert against the size band: `effective_pts = round_half_up(Σ(member.pts) × class_weight) ≤ upper-bound([bundling].release_size_target_pts)` (default ≤ 25). `member.pts` uses the size-check point scale (XS=1 / S=2 / M=4 / L=8 / XL=16); `class_weight` resolves from `[bundling].release_class_capacity_weights` keyed on the milestone's `## Release Class` (G3-10-guaranteed present + enum-valid; fallback `[release_class].default_release_class`); the rounding mode is taken **by reference** from the `bundle-composition-doctrine.md § 3 Step 5 Risk-Weighting` sub-block (round-half-up, pinned at that definitional home — not re-derived here), which is also where `effective_pts` is defined. Denominator = the milestone's bundled issue set (issues carrying this milestone at `status: bundled`); an unsized member is already caught by the G3-09 precondition (apply `size:*` before bundling) and is inherited, not re-checked. The bound and the weight are read by config-field role; magnitudes are never restated (parameterize-over-hardcode), so a recalibration of `release_size_target_pts` or the weights moves this gate automatically and keeps the modeled target (§ 3 Step 5) and the enforced bound a single source. **Degraded path (safety contract):** if `[bundling].release_class_capacity_weights` is absent/unresolved at evaluation time, the gate does **not** silently pass — it computes raw `Σ(member.pts)` with `class_weight = 1.0`, **surfaces a loud WARN that the result is unweighted/degraded**, and requires operator confirmation that the unweighted bound is acceptable before the bundle proceeds (a cross-cutting bundle must never quiet-pass as routine). On breach → route to the `bundle-composition-doctrine.md § 4` tight-merge OR `§ 3 Step 5` split/merge/reframe disposition (NOT a dead-end fail) per the self-repair row. Complements the per-issue G2-11 / G3-12 family at the per-milestone-sum altitude. **Posture:** warn-then-enforce — a per-milestone breach logs to the shared deploy-check warn log (`$(pmo_instance_path)/deploy-check-warn-log.jsonl`) under `check: "g3-15"` and Bundle proceeds with an operator-override rationale during the shakedown window — **specified, not yet emitting**: no check emits this gate's FAIL row, and the per-gate sink `gate-g3-15-warn-log.jsonl` this criterion previously declared was never written and is RETIRED (W1 · `core/standards/gate-efficacy-standard.md`); flip to enforce after 2-3 releases (`[CALIBRATE-AFTER-3]` on the threshold). **Cutover discipline:** applies to milestones entering Stage 3 strictly AFTER this criterion's introducing-release merge SHA recorded in the release log; pre-cutover milestones grandfathered; the introducing release itself exempt (reflexive-pipeline-loop discipline). | validation | metrics | recommend (warn → enforce) | `req` | `req` | `req` | — |
| G3-17 | **A6 New-Track Placement Rationale present for non-identical-track-extension milestones.** For any milestone created at Stage 3 Phase B3 that is NOT an identical-track-extension — i.e., A6 fired under condition (a) no-prior-rationale OR (b) distinct-capability-scope per `release-process.md` § Stage 3 Bundle § A6 — assert the milestone description carries a `## New-Track Placement Rationale (recorded YYYY-MM-DD)` section per the A6 4-table format. For an identical-track-extension milestone (A6 SKIPped because neither (a) nor (b) held), assert the one-line `A6: identical-track-extension — fits the <track-capability> arc; no new-track rationale required` acknowledgment is recorded (milestone description or B1 bundle rationale comment). PASS requires exactly one of {the 4-table section, the acknowledgment line}; **absence of both FAILS — silence is never a pass.** Trivial-PASS when A6 SKIPped by predicate AND the acknowledgment line is present. (Detection command in Validation method.) Complements G3-10 (Release Class declaration) at the same Phase-B3 milestone-description-completeness altitude. **`[CALIBRATE]` — artifact-presence vs artifact-correctness (known soft spot):** G3-17 is a *structural* gate — it verifies an A6 artifact is **present** (exactly one of the 4-table section or the acknowledgment line) and that silence never passes. It does **not** and **cannot** verify that the operator's underlying A6 (b)-judgment was *correct* — i.e., whether a milestone the operator labeled `A6: identical-track-extension` was genuinely an identical-track-extension, or in fact introduced a distinct capability scope that should have triggered the 4-table authoring. A wrong or lazy `A6: identical-track-extension` acknowledgment line passes this structural check while defeating A6's purpose. By design: **G3-17 owns artifact-presence; the operator owns artifact-correctness** — the distinct-capability-scope (b) determination is an operator-rendered judgment the structural gate deliberately does not adjudicate. Flag this gap for calibration after **N milestones** (`[CALIBRATE-AFTER-N]`): sample acknowledgment-line milestones against their actual capability slugs to estimate the false-acknowledgment rate, and if it proves material, add a judgment-graded co-check (LLM-assessed (b)-correctness) alongside this structural presence-check rather than overloading G3-17. **Cutover discipline:** applies to milestones entering Stage 3 strictly AFTER this criterion's introducing-release merge SHA recorded in the release log; pre-cutover milestones grandfathered; the introducing release itself exempt (reflexive-pipeline-loop discipline). | artifact | structural | auto | `req` | `req` | `conv` | `gh api repos/<owner>/pmo-platform/milestones/<N> --jq .description` grep for `^## New-Track Placement Rationale` OR `^A6: identical-track-extension` |
| G3-18 | **Archetype-appropriate design invocation conditioned on `delivery_approach`.** Where the platform backlog declares a methodology preference (`default_delivery_approach` per `operator.toml`, resolved via the Config-Hierarchy Resolution Protocol in `OPERATIONS.md`; or a project's `delivery_approach` per `project-schema.md` §4), the design-invocation expectation at the **Bundle→Planning (Stage 3→4 = Gate 3) boundary** conforms to the declared approach per the **§5 Skill Consumption Pattern** in [`methodology-parameterization-v1.md`](../../release/references/specs/methodology-parameterization-v1.md): **CASE 1** (Scrum / XP / Lean-as-lightweight-Agile → **design-first before slicing**: Stage 5 Solutioning activation is expected/biased-on for design-bearing issues before Stage 6 sub-task decomposition; Waterfall / PRINCE2 → **phase-gate** — design settled at the stage boundary; Kanban → **continuous, no sprint-slice gate** — the design-first-before-slice expectation does NOT fire). **CASE 1-ARRAY** (Hybrid `[A,B]`) → the phased constituent governs the gate expectation, rendered per §5's dominance rule. **CASE 3 / absent / `Custom`+`base_archetype:null`** → **base behavior UNCHANGED** via an **explicit no-op default**: the pre-change design governance (design governed per-issue at Stage 5 via the Phase 0 Activation Gate) applies with no methodology lens; never a silent default-fire (dormant-with-`[ASSUMPTION – CONFIRM]` per [ADR-033](../ADRs/ADR-033-methodology-conditional-skill-activation.md)). Evaluate as `if delivery_approach resolves AND ∈ gating-set → fire the archetype expectation; else → trivial-PASS` — absence falls to the else-as-no-op, never an else-as-gate. The conditioning **adds** a methodology lens to *when* the design invocation is expected; it **never removes** the base design-activation check (the Stage 5 Phase 0 Activation Gate remains the unconditional floor). Mirrors the G3-05 `deliverable_type` conditioning shape (§5A Domain-Axis sibling of the §5 pattern applied here); **Type/Check/Automation columns adopt the G3-05 precedent unchanged** (`field` / `judgment` / `recommend`) — `field` because the criterion conditions on a declared axis value (`default_delivery_approach` / project `delivery_approach`), not a cross-issue/cross-system check. **Stage-8 regression gate `G3-18-absent-axis-noop`:** an absent-axis fixture MUST produce the identical verdict/routing as pre-change (trivial-PASS deferring to the base Stage 5 Phase 0 Activation Gate); a `Kanban` fixture MUST NOT fire the design-first-before-slice expectation; a `Scrum` fixture MUST fire it. **Cutover discipline:** applies to bundles/milestones entering Stage 3 strictly AFTER this criterion's introducing-release merge SHA recorded in the release log; pre-cutover evaluations grandfathered; the introducing release itself exempt (reflexive-pipeline-loop discipline). | field | judgment | recommend | `req` | `req` | `req` | — |
| G3-19 | **Release-identity-mode validity.** Assert that the release declares a valid **release-identity mode** — the closed enum `{versioned, version-less}` set at Bundle per the Release-Identity Mode declaration in [`stage-03-bundle.md`](../../release/references/pipeline/stage-03-bundle.md) — and that the declaration is consistent with what the release actually carries: in **`versioned`** mode a bump-class *intent* (`major` / `minor` / `patch`) is declared, and **no concrete version number is required at this gate**; in **`version-less`** mode the release carries **no version string** at all and its identity is the capability slug alone. **This criterion asserts identity-mode validity, NOT version freeness.** The concrete number is computed and claimed atomically at the merge moment (two-phase allocation per `RELEASE_PROTOCOL.md` § Versioning — intent declared at plan time, next-free computed and compare-and-swap-claimed at Stage 12), so a structural "version assigned (no collision)" check is architecturally unavailable at Bundle and would duplicate the Stage-12 claim; the freeness predicate stays owned downstream. A `version-less` release is **not** a malformed or empty version string — `version-less` is an *identity-axis* value, so the version-grammar canonicalization/freeness functions are **not invoked** for it and the grammar's empty-form rejection is never reached and never contradicted. Evaluate as `if the declared mode resolves to a member of the enum AND the mode-appropriate obligation above holds → PASS; else → route to the self-repair row`. An absent declaration resolves to `versioned` (the shipped default) and is reported as an inferred value, never silently failed. Adopts the G3-05 / G3-18 typing precedent unchanged (`field` / `judgment` / `recommend`) — `field` because the criterion conditions on a declared axis value, not a cross-issue/cross-system check. **Cutover discipline:** applies to bundles/milestones entering Stage 3 strictly AFTER this criterion's introducing-release merge SHA recorded in the release log; pre-cutover evaluations grandfathered; the introducing release itself exempt (reflexive-pipeline-loop discipline — it cannot fire its own new gate). | field | judgment | recommend | `req` | `req` | `req` | — |

### Gate 3 Adapter Blocks

**Adapter G3-05-Bug:** AC measurability is satisfied by either (a) the standard verifiable-AC pattern OR (b) the bug-narrative AC pattern (per Adapter G1-05-Bug — "reproduction steps no longer trigger actual behavior; expected behavior observed").

### Template-Conversion Rule

An issue with the `observation` intake-tier label cannot have the `status: bundled` label applied. Before bundling, the issue body must be rewritten in `improvement.yml` schema (all required improvement.yml fields populated) and the intake-tier label must transition `observation` → `improvement` atomically with the body rewrite. The title is rewritten to the informativeness floor (drop any `[Observation]:` prefix; no `[Category]:` prefix is added — type is carried by the label, not the title; see G1-01).

**Enforcement surface:** Stage 3 Phase A1 (Bundle entry). Phase A1 reads candidate-issue labels before evaluating G3 criteria; any candidate with `observation` label halts with the failure signal `BUNDLE-BLOCKED: observation-template requires conversion`.

**Conversion path:** route candidate back to Stage 2 Triage as a "promote observation" sub-step:
1. Triage agent re-runs G1-02 observation-branch promotability test;
2. On promotability PASS: Triage agent drafts improvement.yml body from the 3-field observation;
3. Operator approves the draft;
4. Body is rewritten + label transitions `observation` → `improvement` + title is rewritten to the informativeness floor (any `[Observation]:` prefix dropped; no prefix added);
5. Issue re-enters G1 evaluation under improvement-template applicability.

**Out-of-conversion paths (operator decisions):**
- **(A) Drop bundle binding:** issue remains `observation` label, milestone removed (issue stays open for future re-triage). Aligns with the least-destructive disposition pattern — preserves issue identity.
- **(B) Operator override:** issue is force-bundled with `[ASSUMPTION – CONFIRM]` markers on missing improvement.yml fields; operator commits to filling them at Stage 4 Planning. Override is recorded on the Milestone description's deviation log per [`release-process.md`](../../release/governance/release-process.md) § Inter-Stage Feedback Protocol Tier 2.

**Retroactive applicability:** The 2026-05-11 audit applied this rule retroactively to 5 observations ( /  /  /  /  — all promoted to improvement.yml bodies before bundling per the audit). Codifying the rule prevents the next round from depending on audit-style retroactive cleanup.

**Milestone-Position Resolution algorithm (referenced from G3-07 criterion body):**

1. **Override (primary):** If milestone description contains a `position: <integer>` line, use that integer.
2. **due_on (secondary):** Otherwise, sort milestones by `due_on` ascending (nulls last). Position = ordinal in the sorted list.
3. **Number (fallback):** Otherwise (for null `due_on`), sort by milestone `number` ascending. Position = ordinal in the sorted list.

Within a single position-equivalence-class (same `due_on` or same `position:` value), edges between members are treated as "same milestone" → G3-07 PASSES trivially (no cross-milestone-sequence concern within a single bundle). Same-milestone edges PASS by definition. When B's milestone is closed AND B's status is Done, the edge is annotated `[RESOLVED]` (not a violation). When B has no milestone (unbundled), G3-07 FAILS (cannot validate sequence).

### Self-Repair Actions

| ID | On Failure | Action |
|---|---|---|
| G3-01 | Dependency in incompatible state | List dependencies and their current states. Recommend: bundle the dependency, escalate priority, or remove dependency link. |
| G3-02 | Circular dependency detected | Visualize cycle (A -> B -> C -> A). Recommend: break weakest link or merge issues. |
| G3-03 | Affected files not identified | Flag as incomplete for planning. Recommend: Defer until files identified, or accept with `[ASSUMPTION – CONFIRM]` marker. |
| G3-04 | Scope too vague for planning | Return to triage for scope refinement. Recommend: add specific file/protocol targets to Proposed Change. |
| G3-05 (presence) | AC section absent, or present but carrying zero criteria | Author an `Acceptance Criteria` section with ≥1 criterion **before bundling**. The assertion is on section **presence + non-emptiness**, not on template-field presence — so a body predating the current issue template repairs by adding the section at whatever heading depth the body already uses (H2–H4 all match), in whatever criterion list form it already uses (checkbox, plain bullet or ordered). Do **not** repair by re-shaping the body to the template: template conformance is a different criterion, and re-shaping to satisfy this one is a larger edit than the finding warrants. A card that reaches this row has no acceptance criteria at all, so there is nothing yet to judge for measurability — repair presence first, then the measurability row below applies. |
| G3-05 (measurability) | AC not measurable | Return to triage for AC refinement. Recommend ONE of: **(a) file-state branch** — rewrite the AC as an assertion verifiable against file content or system state (the existing path; use when the AC's meaning survives a file/state assertion); OR **(b) behavioral/domain branch** — restate the AC as a **behavioral/domain predicate** with a paired declared verification method (`predicate: <outcome>; method: <how verified>`), per G1-05a pattern (d). Use branch (b) when a file-content/system-state rewrite would lose the AC's meaning (a behavioral or domain outcome not reducible to one file/state assertion). The declared method may name an executor that does not yet exist ("declared, verification deferred") — measurability at the gate is satisfied by the declared method, not by an already-built executor. |
| G3-06 | Blocking issue in incompatible state | List blocking issues and their states. Recommend: resolve blocker first, adjust bundle sequence, or remove from bundle. |
| G3-07 | Edge violates milestone-sequence inequality without exception registration | List violating edges with source/target milestone positions and gap. Offer remediation menu: (a) bundle target into source milestone, (b) re-sequence source to later milestone, (c) remove dependency link, (d) register exception in milestone description's `## Dependency Exceptions` block with rationale + authorizer + date. |
| G3-08 | In-bundle candidate pair with no routing decision | Surface in-bundle pair to operator with detection evidence. Present 4 routing options (A fold / B decompose-into-roadmap / C keep-separate-with-rationale / D defer-for-coordination); operator renders. Record decision in bundle rationale comment + ticket bodies per outcome. Cross-bundle pairs surfaced as advisory; primary surface is G2-09. |
| G3-09 | size:XL ticket in bundle with no decomposition-routing decision OR ticket missing size:* label | Surface size:XL to operator with decomposition options; operator renders. Record in bundle rationale comment. For unsized tickets: return to Triage with "Apply size label per label-taxonomy.md before bundling" self-repair. size:L cross-stage informational flag posted to bundle rationale; no required decision. |
| G3-10 | Release Class heading missing from milestone description, OR Class value outside 4-value enum, OR Rationale sub-field empty | Author `## Release Class` heading with `Class:` field (value from `routine` / `novel` / `cross-cutting` / `hotfix` enum) + non-empty `Rationale` sub-field per release-class-taxonomy.md § 2. PATCH milestone description via `gh api repos/.../milestones/<N> -X PATCH -f description=...`. Auto-mode posture: structural-required; bundle blocked until satisfied. Pre-cutover-or-exempt releases: no action — Class declaration is grandfathered as inferable from milestone scope. |
| G3-11 | Release Outcome Statement heading missing from milestone description | Author the heading + REQUIRED AFTER + BEFORE paragraphs per release-outcome-statement-template.md class-varying shape table; PATCH milestone description via `gh api repos/.../milestones/<N> -X PATCH -f description=...`. Auto-mode posture: structural-required; bundle blocked until satisfied (matching G3-10, alongside which this criterion is always evaluated). Pre-cutover-or-exempt releases: no action — existing `## Goal` / `**Goal (AFTER/BEFORE):**` form is grandfathered for read-purposes (Stage 9 G-PR7 + Stage 13 QC4-06 accept the sibling form). |
| G3-12 | In-bundle issue matches oversize predicate (P1 / P2 / P3) with no 3-outcome routing decision recorded in bundle rationale comment | Surface predicate-firing evidence to operator with the matched predicate(s) per § Composite-OR Oversize Predicate. Present the 3-outcome enum (A kept-as-one with rationale / B split per [fission-convention.md](../../release/references/protocols/fission-convention.md) / C escalate per Tier 2 [SCOPE CHANGE]); operator renders. Record decision in bundle rationale comment. On SPLIT outcome AT BUNDLE TIME: Tier 2 [SCOPE CHANGE] per § Inter-Stage Feedback Protocol — re-bundle with fission-children replacing parent (children re-enter Stage 2 Triage per fission-convention.md Re-triage State; parent close-or-convert per its D-ParentDisposition). On KEPT-AS-ONE: record 1-line rationale. On ESCALATE: return to operator for priority/capacity re-discussion, defer to later release, explicit override-with-rationale, or downstream re-Triage with refined scope. |
| G3-13 | RETIRED (ADR-012): roadmap-cascade validation de-scoped; roadmap instances operator-local. | No self-repair action - the criterion no longer fires. |
| G3-14 | Mode A combined-clean parse rate below the floor (conformant-bundleable `clean`-rate < `[bundling].mode_a_parse_rate_floor`, default 0.90), OR the denominator-coverage co-gate fires (the `needs-body-repair` count is anomalously high so the conformant denominator is hollowed out) | Surface the breach to the operator with the computed evidence: the determinate-denominator size (`clean` + `failed` conformant bodies), the `clean` count, `combined_clean_parse_rate`, the floor, AND the `Type-excluded` + `Needs-body-repair` + `deferred`-excluded counts (so the operator sees the denominator is the conformant determinate set, not all bodies, and can judge whether a shrunk denominator — not a genuinely low rate — drove the breach). Present the **3-outcome disposition menu (NOT a dead-end fail): (A) Repair-then-recompute** — route the failing-`clean` conformant bodies (and any large `needs-body-repair` cohort) through the Step-1.5 `needs-body-repair` queue (`gh issue edit --body` body-conformance fixes, reusing the Gate-1 self-repair remediations to add/repair the `### Affected Files` heading), then re-run Mode A and re-evaluate G3-14. **(B) Proceed-with-documented-override** — operator records a one-line rationale accepting the sub-floor rate for this run (e.g. a known cluster of legacy bodies whose repair is out of scope this release), and the failing bodies carry forward as risk items to Planning per the Stage 3 operator-override pattern. **(C) Escalate-to-parser-fix** — when the low rate traces to a parser defect (the heading-alias/suffix matcher missing a legitimate `### Affected Files` variant) rather than malformed bodies, file/route a `bundle-issues-parser.py` robustification per Tier 2 [SCOPE CHANGE] (the parser is the sibling robustification surface owned outside this skill per Step 1.5); the rate is re-asserted after the parser fix lands. Warn-mode posture: log to the shared deploy-check warn log (`$(pmo_instance_path)/deploy-check-warn-log.jsonl`) under `check: "g3-14"` and Bundle/Planning proceeds with the recorded disposition/override — **specified, not yet emitting**: no check emits this gate's FAIL row, and the per-gate sink `gate-g3-14-warn-log.jsonl` this row previously declared was never written and is RETIRED (W1 · `core/standards/gate-efficacy-standard.md`). Enforce-mode (post-shakedown): block the Stage 3 → 4 transition until a disposition is recorded; operator override per the MODERATE reversibility tier (one-line `Override: <rationale>` in the milestone-description deviation log / bundle rationale comment). Mirrors the G2-11 / G3-12 / G3-15 3-outcome decision surface but routes to the parse-quality dispositions (body-repair / override / parser-fix) rather than the milestone-composition or work-item-fission ones — the correct disposition for a low parse rate vs an oversize bundle or ticket. |
| G3-15 | Milestone `effective_pts` exceeds the band upper bound (risk-weighted membership sum > 25 default), OR the `release_class_capacity_weights` config is unresolved (degraded evaluation) | Surface the breach to the operator with the computed evidence: raw `Σ(member.pts)`, the resolved `class_weight` (and which Release Class drove it), `effective_pts`, and the band upper bound. **Degraded case first:** if the weights config was unresolved, emit a loud WARN that the result is unweighted (`class_weight = 1.0`, raw `Σ(member.pts)` asserted) and obtain operator confirmation that the degraded bound is acceptable before proceeding — never silently pass a cross-cutting bundle as routine. **On breach, present the doctrine disposition menu (NOT a dead-end fail): (A) Split by sub-capability** — re-apply § 3 Step 5 Steps 1-4 per sub-capability; sub-slices land as separate milestones (per the § 3 Step 5 `> 25 pts` disposition row). **(B) Tight-merge per § 4** — re-apply § 4 Tight-Merge Mechanics (re-split the parent, count internal dep edges, build the merge-graph, connected-components = milestones) so internal-dep-coupled sub-slices rejoin and loose ones split out. **(C) Reframe / keep-with-rationale** — operator records a one-line rationale that the milestone is genuinely a single capability whose realization legitimately exceeds the band, accepting the oversize with documented reversibility. On (A)/(B) the resulting sub-milestones re-enter Gate 3 (including a fresh G3-15) at Bundle. Warn-mode posture: log to the shared deploy-check warn log (`$(pmo_instance_path)/deploy-check-warn-log.jsonl`) under `check: "g3-15"` and Bundle proceeds with the recorded disposition/override — **specified, not yet emitting**: no check emits this gate's FAIL row, and the per-gate sink `gate-g3-15-warn-log.jsonl` this row previously declared was never written and is RETIRED (W1 · `core/standards/gate-efficacy-standard.md`). Enforce-mode (post-shakedown): block the `status: bundled` transition until a disposition is recorded; operator override per the MODERATE reversibility tier (one-line `Override: <rationale>` in the milestone-description deviation log). Mirrors the G2-11 / G3-12 3-outcome decision surface but routes to the milestone-composition dispositions (§ 4 / § 3 Step 5) rather than the work-item-fission ones — the correct disposition for an oversize bundle vs an oversize ticket. |
| G3-17 | Milestone is a non-identical-track-extension (A6 fired under (a) or (b)) but the description lacks the `## New-Track Placement Rationale` section, OR is an identical-track-extension but lacks the `A6: identical-track-extension …` acknowledgment line (neither artifact present) | Run A6 per `release-process.md` § Stage 3 Bundle § A6: evaluate the (a)/(b) predicate. If (a) or (b) holds → author the 4-table New-Track Placement Rationale section and PATCH the milestone description via `gh api repos/.../milestones/<N> -X PATCH -f description=...`. If neither holds (identical-track-extension) → record the one-line `A6: identical-track-extension — fits the <track-capability> arc; no new-track rationale required (prior rationale: <milestone title or #>)` acknowledgment. Auto-mode posture: structural-required; bundle blocked until exactly one artifact is present. Pre-cutover-or-exempt milestones: no action — grandfathered. |
| G3-18 | A design-bearing milestone whose resolved `delivery_approach` is in the gating set (Scrum / XP / Lean / Waterfall / PRINCE2 / a Hybrid whose phased constituent gates) reaches the Bundle→Planning boundary without the archetype-appropriate design expectation settled (Scrum/XP/Lean → Stage 5 Solutioning not yet biased-on for the design-bearing issues before Stage 6 slicing; Waterfall/PRINCE2 → phase-gate design not settled at the stage boundary) | Recommend (Tier 2 — warn-class, non-blocking) that the archetype-appropriate design invocation be settled before Stage 6 slicing: for CASE-1 approaches, bias Stage 5 Solutioning on for the design-bearing issues (per the Stage 5 Phase 0 Activation Gate) ahead of sub-task decomposition; for Waterfall/PRINCE2, confirm the phase-gate design at the stage boundary. Resolve `delivery_approach` through the Config-Hierarchy Resolution Protocol (do NOT hardcode a bare `operator.toml` read). **When the axis is absent / `Custom` / `Kanban`: no action — trivial-PASS** (Kanban is continuous-flow with no sprint-slice gate; absent/`Custom` defers wholly to the base Stage 5 Phase 0 Activation Gate, unchanged from pre-G3-18 behavior). Never fire the design-first expectation on an unresolved axis (the `G3-18-absent-axis-noop` guarantee). |
| G3-19 | Release-identity mode is undeclared, resolves outside the `{versioned, version-less}` enum, or contradicts what the release carries (a `version-less` release carrying a version string; a `versioned` release with no declared bump-class intent) | Surface the mismatch to the operator with the evidence: the declared (or inferred-default) mode, the enum, and the conflicting artifact. Recommend (Tier 2 — warn-class, non-blocking) ONE of: **(a) declare the mode explicitly** in the milestone description per the Release-Identity Mode declaration in `stage-03-bundle.md`, when the mode was merely absent and the `versioned` default was inferred; **(b) declare the bump-class intent** (`major` / `minor` / `patch`) for a `versioned` release — the *intent* only, never a concrete number, which is claimed atomically at Stage 12 per the two-phase allocation; **(c) drop the version string** for a `version-less` release so its identity is the capability slug alone. **Do NOT "repair" a `version-less` release by synthesizing a placeholder or empty version string** — `version-less` is an identity-axis value, not a malformed version, and feeding an empty string to the version-grammar validator is the failure this criterion exists to prevent. Never assert version freeness here: a colliding-or-free verdict is the Stage-12 compare-and-swap's to render, and a Gate-3 freeness claim would go stale before the merge moment. Pre-cutover-or-exempt releases: no action — grandfathered. |

### Cross-Stage Decomposition Coverage (per AC #5)

The platform enforces decomposition review at three distinct altitudes — intake-altitude (when authored), Triage/Bundle-altitude (when Approved/Bundled), and Engineering-altitude (when implemented). The three altitudes operate on disjoint problems: no overlap, no gap.

| Altitude | Stage | Trigger | Mechanism | Forcing function | Status |
|---|---|---|---|---|---|
| **Intake-altitude** (author's-pen) | Stage 1 + Stage 2 A1 DoR | 5-test T1 atomicity fails per [intake-style-guide.md § 2](../../release/references/how-to/intake-style-guide.md) | Author self-applies 5-test; Triage A1 confirms via G1-04 / G1-05 DoR | Self-applied rule + G1 gate | SHIPPED |
| **Triage/Bundle-altitude** (release-altitude) | Stage 2 A5.5 + Stage 3 A1 | G2-11 / G3-12 composite-OR oversize predicate fires (P1 `size:XL` OR P2 declared-decomposition-hook OR P3 AC ≥ 7 OR file ≥ 5) | Triage/Bundle spoke surfaces 3-outcome enum; operator renders kept-as-one / split / escalate | G2-11 / G3-12 gate-blocking (judgment-recommend) | **NEW** |
| **Engineering-altitude** (implementation-altitude) | Stage 6 sub-task scaffolding | Engineering spoke decomposes bundled issue into N sub-tasks per file-touch / capability-axis basis | Hub spawns per-sub-task spokes; sub-tasks live within ONE parent issue + ONE milestone | In-skill structural protocol | SHIPPED |

**Disjoint problem semantics:**

- **Intake altitude:** "Was this ticket atomic when authored?" (1 ticket; pre-Triage)
- **Triage/Bundle altitude:** "Is this Approved/Bundled ticket a single piece of work?" (1 ticket; pre-Engineering)
- **Engineering altitude:** "How do we mechanically chunk implementation of this one ticket?" (1 ticket → N sub-tasks within it; post-bundle)

**Subsumption rule:** G2-11 SUBSUMES G2-10 when its predicate fires (one routing decision under G2-11; G2-10's 4-option enum maps to G2-11's 3-outcome enum: decompose-into-slice → split, split-into-sub-issues → split, approve-as-is-with-risk-note → kept-as-one, defer-for-pre-bundle-analysis → escalate). G3-12 SUBSUMES G3-09 by the same mapping at Bundle. Backwards-compatible — G2-10 / G3-09 stay in spec as the explicit-`size:XL` forcing function; G2-11 / G3-12 stay as the broader-predicate forcing function.

**Retroactive Review Path (per AC #6 — operator-elective):**

The G2-11 / G3-12 gates apply to issues entering Triage / Bundle going forward. For latent-debt protection on the existing backlog, a standing operator-elective retroactive scan path is available:

- **R1 — Operator-elective `--retro-scan` invocation.** Single `gh issue list` query enumerates candidates by `status: approved` / `status: bundled` + body grep for the P2 / P3 predicates. Operator-invoked, not auto-running. NOT a deploy-time check, NOT a `deploy.sh --check` gate.
- **R2 — Per-candidate routing recommendation.** Agent generates a 3-outcome routing recommendation as a `Retroactive Oversize Review` comment template (mirrors the post-cutover Oversize-Review Outcome format); operator renders kept-as-one / split / escalate per the standard 3-outcome enum.
- **R3 — Per-outcome execution.** When operator elects split, the standard fission protocol fires retroactively. When operator elects kept-as-one, rationale recorded on issue. When operator elects escalate, Tier 2 [SCOPE CHANGE] applies per [`release-process.md`](../../release/governance/release-process.md) § Inter-Stage Feedback Protocol.

**Maintenance-affordance note:** R1-R3 is a maintenance affordance, not a re-enforcement. Previously bundled issues remain governed under their original Approval — the protocol does NOT retroactively fail them.

---

## Gate 4: Plan Readiness

*"Is the release plan complete enough to hand to Engineering?"*

**Stage boundary:** 4->5/6 (Planning -> Solutioning/Engineering)
**Inherits:** Gate 3->4 from [field-lifecycle-matrix.md](field-lifecycle-matrix.md) (the structural Bundle->Planning exit)

| ID | Criterion | Type | Check | Automation | Validation method |
|---|---|---|---|---|---|
| G4-01 | **Implementation Sequence present.** Release plan file contains a `## Implementation Sequence` (or `### Implementation Sequence`) section that is non-empty. | artifact | structural | auto | `grep -qE '^#{2,3} Implementation Sequence' <release-plan>` |
| G4-02 | **File-Change Matrix present.** Release plan file contains a `## File Change Matrix` (or `### File Change Matrix`) section that is non-empty. | artifact | structural | auto | `grep -qE '^#{2,3} File Change Matrix' <release-plan>` |
| G4-03 | **Risk Register present.** Release plan file contains a `## Risk Register` (or `### Risk Register`) section that is non-empty. | artifact | structural | auto | `grep -qE '^#{2,3} Risk Register' <release-plan>` |
| G4-04 | **Verification Plan present.** Release plan file contains a `## Verification Plan` (or `### Verification Plan`) section that is non-empty. | artifact | structural | auto | `grep -qE '^#{2,3} Verification Plan' <release-plan>` |
| G4-05 | **Delivery Strategy present.** Release plan file contains a `## Delivery Strategy` (or `### Delivery Strategy`) section naming branch + merge approach + rollback. | artifact | structural | auto | `grep -qE '^#{2,3} Delivery Strategy' <release-plan>` |

### Self-Repair Actions

| ID | On Failure | Action |
|---|---|---|
| G4-01 | Implementation Sequence section absent or empty | Return to Stage 4 Planning: author the `## Implementation Sequence` section (dependency-ordered issue list) per [stage-04-planning.md](../../release/references/pipeline/stage-04-planning.md) §6 before handoff to Stage 5/6. Warn-mode: log to the shared deploy-check warn log (`$(pmo_instance_path)/deploy-check-warn-log.jsonl`) and proceed — **specified, not yet emitting**: no check emits this gate's FAIL row. |
| G4-02 | File Change Matrix section absent or empty | Return to Stage 4 Planning: author the `## File Change Matrix` (per-issue affected-files table) per §6. Warn-mode: log + proceed. |
| G4-03 | Risk Register section absent or empty | Return to Stage 4 Planning: populate the `## Risk Register` (named risk + owner + mitigation, no passive-risk voice per the platform guardrail). Warn-mode: log + proceed. |
| G4-04 | Verification Plan section absent or empty | Return to Stage 4 Planning: author the `## Verification Plan` (per-issue AC->method mapping per §6). Warn-mode: log + proceed. |
| G4-05 | Delivery Strategy section absent or missing branch/merge/rollback | Return to Stage 4 Planning: author `## Delivery Strategy` naming branch + merge approach + rollback strategy per §6. Warn-mode: log + proceed. |

---

## Gate 6: Engineering Completeness

*"Is the implementation complete and reviewable enough to hand to Dev Testing?"*

**Stage boundary:** 6->7 (Engineering -> Dev Testing)
**Inherits:** Gate 6->7 from [field-lifecycle-matrix.md](field-lifecycle-matrix.md) (PR created + Engineering sub-tasks closed + Assignee set + Status = In Progress)

| ID | Criterion | Type | Check | Automation | Validation method |
|---|---|---|---|---|---|
| G6-01 | **PR exists for the release.** A pull request exists for the release branch. | artifact | structural | auto | `gh pr list --head <release-branch> --json number` returns >=1 PR (or `gh pr view <PR> --json state` resolves) |
| G6-02 | **Commits link source issues.** Every release-scoped issue has >=1 commit on the release branch referencing its `#N`; `issues_with_commits / total_release_issues = 1.0`. | validation | structural | auto | `git log --grep="#<N>"` against the Milestone issue list |
| G6-03 | **PR body is parser-clean.** The PR body parses without a BLOCKING/parse-error signal (required metadata sections resolvable: implementation summary, per-issue status) — mirrors the release-planner parse-status `clean` semantics. | artifact | structural | auto | `gh pr view <PR> --json body` feeds the parse; PR-body parse yields no parse-error signal |
| G6-04 | **Verification evidence present.** The PR body contains a Verification Evidence section with per-issue PASS/FAIL results; `issues_with_verification_block / total_issues = 1.0`. | artifact | structural | auto | `gh pr view <PR> --json body` -> `grep -qE 'Verification Evidence'` |
| G6-05 | **Change Description section present.** The release plan FILE contains a `## Change Description` section (authored at Stage 6 Phase C1 per RELEASE_PROTOCOL § Change Description Protocol), committed on the release branch before PR ready-for-review. | artifact | structural | auto | `grep -qE '^#{2} Change Description' <release-plan>` |
| G6-06 | **Skill packages content-fresh (PR-CI).** A PR that edits any rostered skill's `SKILL.md` or `references/` has its compiled `.skill` package rebuilt and content-fresh in the same PR — the committed package equals what source would build now, mirroring `deploy.sh` Check 7's content-hash verdict pre-merge (closing the class defect where skill-source edits merged green while the `.skill` package stayed stale, caught only at Stage-13 close-out). Initial **warn-mode** posture per [`bypass-mode-readiness.md`](../rules/bypass-mode-readiness.md) Shakedown → Enforce Transition Checklist precedent — a stale package logs and Engineering proceeds; it flips to enforce when the committed `.github/skill-package-freshness.enforce` token → `enforce` AND the CI job enters branch-protection required checks. Backed by the `skill-package-freshness` CI gate (`deploy.sh --check-package-freshness`, single-engine with Check 7). **Cutover discipline:** applies to skill-editing releases going forward; the introducing release itself is exempt (reflexive-pipeline-loop discipline) yet dogfoods it green (its eval-writer reference edit + package rebuild is the live regression signal). | artifact | structural | auto (warn → enforce) | `bash core/deploy/deploy.sh --check-package-freshness` (FRESH → exit 0; STALE → exit 2 advisory, or exit 1 when the enforce sentinel is set; never exit 0 on STALE); backed by `.github/workflows/skill-package-freshness.yml` |

### Self-Repair Actions

| ID | On Failure | Action |
|---|---|---|
| G6-01 | No PR for the release branch | Return to Stage 6 Engineering Phase C2: create the PR with full metadata per [stage-06-engineering.md](../../release/references/pipeline/stage-06-engineering.md) C2. Warn-mode: log to the shared deploy-check warn log (`$(pmo_instance_path)/deploy-check-warn-log.jsonl`) and proceed — **specified, not yet emitting**: no check emits this gate's FAIL row. |
| G6-02 | An issue has no commit referencing its `#N` | Return to Stage 6 Engineering: land a commit referencing the issue per Phase B1 commit convention, or record a documented deviation in the release-plan deviation log if the issue was descoped. Warn-mode: log + proceed. |
| G6-03 | PR body fails parse (missing/malformed required sections) | Return to Stage 6 Engineering Phase C2: repair the PR body to the metadata template (implementation summary + per-issue status). Warn-mode: log + proceed. |
| G6-04 | Verification Evidence section absent from PR body | Return to Stage 6 Engineering Phase C4: author the Verification Evidence section with per-issue PASS/FAIL results before handoff. Warn-mode: log + proceed. |
| G6-05 | Change Description section absent from release plan | Return to Stage 6 Engineering Phase C1: author the `## Change Description` section in the release plan FILE per RELEASE_PROTOCOL § Change Description Protocol; commit on the release branch before PR ready-for-review. Warn-mode: log + proceed. |
| G6-06 | Skill package stale for a skill-editing PR | Return to Stage 6 Engineering: rebuild the stale package(s) via `core/deploy/tools/build-skill-packages.sh <skill>` and commit the package + its `.sha256` sidecar on the release branch. Warn-mode: log to the shared deploy-check warn log (`$(pmo_instance_path)/deploy-check-warn-log.jsonl`) and proceed — **specified, not yet emitting**: no check emits this gate's FAIL row. Enforce-mode (post-shakedown): block until the package is rebuilt. |

---

## Gate G-BR: Bundle Refresh Readiness

*"Has the bundle drifted enough since creation to require operator decision, and is the refresh decision recorded?"*

**Stage boundary:** Intra-window — fires at refresh-trigger events during Stage 3 Phase B3 → Stage 5 Collective Review approval
**Inherits:** No formal inheritance — composes alongside Gate 3 (the bundle being refreshed must have originally passed Gate 3). Re-validates Gate 3 criteria after amend/re-bundle path.

| ID | Criterion | Type | Check | Automation | Validation method |
|---|---|---|---|---|---|
| G-BR1 | Refresh trigger condition valid (T1 ≥3 new Approved theme-matching issues OR T2 priority shift OR T3 dep-state change OR T4 Stage 4 boundary check) | field | structural | auto | — |
| G-BR2 | Churn magnitude computed (composition_delta_pct + theme_preserved Y/N) | field | structural | auto | — |
| G-BR3 | Refresh outcome path selected (no-op / amend / re-bundle / defer) with required evidence per [release-process.md](../../release/governance/release-process.md) § A7 — Bundle Mutability Protocol outcome-path table | field | judgment | recommend | — |
| G-BR4 | Decision recorded on Milestone (description amendment AND/OR `[BUNDLE *]` comment per outcome-path recording mechanism) | artifact | structural | auto | — |
| G-BR5 | **Conditional — fires only when the selected outcome path is `amend` AND `issues_added > 0`.** The target Milestone's composition-lock state is not `LOCKED`, and the eligibility basis is recorded, per the act-typed rule defined in [release-process.md](../../release/governance/release-process.md) § A7 § Composition lock. That rule and its three-valued `lock_state` resolution are defined there and cited here — this criterion restates neither. `UNMARKED` is **not** affirmative eligibility: it passes only with an explicit basis stated in the `[BUNDLE AMENDMENT]` comment. Removal-amends, zero-delta re-sequence-amends, and the `no-op` / `re-bundle` / `defer` paths pass trivially. | validation | structural | auto | Resolve `lock_state(M)` per the resolution defined in `release-process.md` § A7 § Composition lock → Boundary state; expect a non-`LOCKED` state **with a recorded basis**. Fail-closed: an indeterminate read is non-eligibility |

### Self-Repair Actions

| ID | On Failure | Action |
|---|---|---|
| G-BR1 | No trigger valid | Path (1) no-op; close refresh check with `[BUNDLE REFRESH: no-op]` comment. |
| G-BR2 | Churn not computed | Re-run computation; missing computation blocks operator decision. |
| G-BR3 | Outcome path selected without required evidence | Return to operator with evidence-gap list; block recording until evidence complete. |
| G-BR4 | Recording absent post-decision | Author the `[BUNDLE *]` comment + Milestone-description update per outcome-path table. Decision is INVALID without recording — Stage 4/5 spokes treat unrecorded refresh as "no decision made" and re-prompt operator. |
| G-BR5 | An `amend` carrying `issues_added > 0` targets a `LOCKED` Milestone — or targets an `UNMARKED` one with no recorded eligibility basis | **The addition** is unavailable for that Milestone; the `amend` path itself remains available for that Milestone's removals and zero-delta re-sequences. Re-render the disposition for the added issues only: route them to a pre-Stage-4 or new Milestone via a Stage-3 Phase-B bundling pass, or defer; record the running bundle's own outcome as path (1) `no-op` with the composition-lock annotation. On the `UNMARKED` arm the operator may instead record the eligibility basis in the `[BUNDLE AMENDMENT]` comment and re-evaluate. Never add on an indeterminate eligibility read. |

---

## Gate 9: Plan Review

*"Is the evidence package complete enough for the operator to render Go/No-Go?"*

**Stage boundary:** 9->12 (Plan Review -> Execute)
**Inherits:** Gate 8->9 + Gate 9->12 from [field-lifecycle-matrix.md](field-lifecycle-matrix.md#gate-8-9-qa---plan-review)

| ID | Criterion | Type | Check | Automation | Validation method |
|---|---|---|---|---|---|
| G-PR1 | Evidence package complete (Phase A1-A6 all populated per [pipeline/stage-09-plan-review.md](../../release/references/pipeline/stage-09-plan-review.md) §5 Phase A; Phase A6 = the Release Readiness Scan per [release-readiness-scan-spec.md](../../release/references/specs/release-readiness-scan-spec.md), whose dimension set that spec owns; the Phase A6 Release Readiness Scan applies to all Stage 9 releases going forward) | artifact | structural | auto | — |
| G-PR2 | All upstream reports present (Stage 7 quality report + Stage 8 acceptance report + Stage 6 PR + Stage 4 release plan) | artifact | structural | auto | — |
| G-PR3 | PR scope matches release plan (implementation table conformance to release plan §Implementation Sequence) | validation | structural | auto | — |
| G-PR4 | Risk register reviewed (all risks mitigated or accepted; status flag set) | field | structural | auto | — |
| G-PR5 | Deployment readiness checklist all PASS — PR mergeable, branch current, metadata complete, rollback available | validation | structural | auto | `gh pr view --json mergeable` (PR mergeable); branch-current / metadata / rollback checked |
| G-PR6 | Decision record posted (per the decision-record format on gate sub-task + parent issue) | artifact | structural | auto | — |
| G-PR7 | Release plan implementation conforms to declared Release Outcome Statement per the template. Hub reads the `### Release Outcome Statement` H3 block from the GitHub Milestone description, reads the PR scope, reads the release plan §Implementation Sequence + File Change Matrix, AND reads each release-scoped issue AC. Renders a 1-paragraph synthesis citing AC-trace evidence: per-issue AC list maps to the AFTER capability; PR scope (file-changes-matrix) implements the AFTER capability; no scope drift since Stage 3. Verdict: **ALIGNED** / **DIVERGED-WITH-RATIONALE** / **MISALIGNED**. LLM-graded recommend check; operator may accept divergence with documented rationale in Decision Record. **Cutover discipline:** The goal-conformance check applies to all Stage 9 releases going forward. | judgment | judgment | recommend | `gh pr view <PR> --json files,additions,deletions` (PR scope, fed to the synthesis) |
| G-PR8 | **Mid-pipeline divergence re-check (File dimension — HALT-eligible).** At Phase A6.5 per [pipeline/stage-09-plan-review.md](../../release/references/pipeline/stage-09-plan-review.md) §5 Phase A6.5 (and [`release-process.md`](../../release/governance/release-process.md) § Mid-Pipeline Divergence Re-Check), the hub compares this release's declared release-plan files against commits landed on freshly-fetched `origin/main` since the recorded baseline. Verdict: **CLEAN** (zero post-baseline commits) → record `G-PR8 = CLEAN`, advance to Phase A7 / **DIVERGED-RELEASE-FILES-UNTOUCHED** (post-baseline commits exist but none touch release-plan files) → record `G-PR8 = DIVERGED-RELEASE-FILES-UNTOUCHED` + informational note, advance to Phase A7 / **DIVERGED-RELEASE-FILES-TOUCHED** (a post-baseline commit touches release-plan files) → HALT pre-GO. This is the *syntactic File-Change-Matrix-touch* axis; it composes with — and does not duplicate — G-PR9 (a sibling whose mover-set invalidates a path this release's edit-set references, which need not be in the File Change Matrix) and the Phase A6.5 advisory version dimension (the absolute version-freeness predicate). The version dimension of Phase A6.5 is advisory, not HALT — only the File dimension recorded here is HALT-eligible. **Cutover discipline:** applies to releases entering Stage 9 strictly AFTER this criterion's introducing-release merge SHA recorded in the release log; the introducing release itself is exempt (reflexive-pipeline-loop discipline — it cannot fire its own new gate). | validation | structural | auto | — |
| G-PR9 | **GO baseline-currency.** The Stage 9 GO records the **baseline SHA** it is rendered against (the release-branch base at GO time, persisted in the release plan's `## Cross-PR Overlap Audit` → `### Baseline SHA` H3), AND the hub evaluates the **sibling-merge revalidation predicate**: the sibling-merge revalidation command (see Validation method), intersecting the touched / renamed / deleted path-set against this release's structural surface `SURFACE(R)` (the F1 / F2 / F3 / F5 + in-tree-F6 union per the Stage 3 Bundle spec § A9.6.1 structural-blast-radius axis; F4 excluded). The predicate is `--name-status --find-renames` (NOT `--merges`) so it is merge-style-agnostic — the repo lands sibling releases via both merge commits AND squash / fast-forward non-merge commits, and a `--merges`-only filter would return a false CURRENT for a squash landing. Verdict: **CURRENT** (no commit since the recorded baseline intersects `SURFACE(R)`) / **STALE-REVALIDATE** (a sibling merged; revalidation required — re-run the A4 structural sub-audit + Phase A6.5 divergence check against new `main`) / **STALE-VOID** (revalidation surfaces a `SURFACE(R)` intersection → NO-GO, re-baseline). Composes with G-PR8 (Phase A6.5 syntactic File-Change-Matrix-touch check) as a second axis/window — G-PR8 catches a sibling touching this release's *declared* files; G-PR9 catches a sibling whose *mover-set invalidates a path this release's edit-set references* (which need not be in the File Change Matrix) AND records the SHA that makes the GO falsifiable. **Cutover discipline:** applies to releases entering Stage 9 strictly AFTER this criterion's introducing-release merge SHA recorded in the release log; the introducing release itself is exempt (reflexive-pipeline-loop discipline — it cannot fire its own new gate; its Stage 9 runs under the pre-existing G-PR1..G-PR8 checks). | validation | structural | auto | `git log <recorded-baseline-SHA>..origin/main --name-status --find-renames` (`--name-status --find-renames`, NOT `--merges`, so it is merge-style-agnostic) |
| G-PR10 | **Release-integration cross-issue AC check (QC3.5 / Phase A3.6).** The hub reads every Cross-Issue Acceptance Criterion (CIAC-N) verdict the release's verification-execution executor emitted for the merged PR, per [pipeline/stage-09-plan-review.md](../../release/references/pipeline/stage-09-plan-review.md) §5 Phase A3.6 + [release-process.md](../../release/governance/release-process.md) QA Checkpoint 3.5. **Read-only, single-runner:** the executor runs each CIAC's declared method (grep / anchor-resolution / runtime-dispatch) once and emits the verdict; this gate consumes the emitted verdict — it does NOT re-run the method. **Evidence-freshness guard (mirrors G-PR9 baseline-currency):** a consumed verdict is valid only if emitted against the final PR head SHA; a stale verdict (a later commit touched a CIAC-relevant file) re-triggers the executor to re-emit before the gate reads it. Verdict: **RELEASE-CONSISTENT** (all CIAC-N read MET) → advance / **RELEASE-INCONSISTENT** (≥1 NOT MET or unresolved PARTIAL) → NO-GO recommendation input (operator may override with recorded rationale) / **N/A** (zero CIACs — regression-safe no-op). Uses the Stage-8 per-criterion verdict enum verbatim. Composes with the Phase A3.5 `INT-N` chain validation (dependency pairs) — G-PR10 covers release-scoped predicates over any ≥2 issues. **Cutover discipline:** applies to releases entering Stage 9 strictly AFTER this criterion's introducing-release merge SHA recorded in the release log; the introducing release itself is exempt (reflexive-pipeline-loop discipline — it cannot fire its own new gate; its Stage 9 runs under the pre-existing G-PR1..G-PR9 checks). | validation | judgment | recommend | CIAC-N verdicts emitted by the release's verification-execution executor against the merged PR head SHA, read read-only |
| G-PR11 | **Declared-vs-delivered File Change Matrix check (QC3.5 / Phase A3.7).** The hub reads the `fcm-delivery` verdict the release's verification-execution executor emitted for the merged PR, per [pipeline/stage-09-plan-review.md](../../release/references/pipeline/stage-09-plan-review.md) §5 Phase A3.7. Closes the **inbound** half of scope-boundary verification: Stage 7 DT and Stage 8 QA both confirm nothing OUTSIDE the approved matrix was touched, and neither confirms everything INSIDE it landed. **Read-only, single-runner:** the executor parses the matrix and runs the `git diff --name-status --no-renames` comparison once and emits the verdict; this gate consumes it — it does NOT re-parse the matrix or re-run the diff. **Always-on at the executor**, not plan-declared, so a release cannot omit the check by not declaring it; the executor emits an `FCM-COVERAGE` record carrying `declared / interpreted / obligations / excluded / conditional / uninterpreted` on every run, so "found nothing" is distinguishable from "never ran" and that record's absence is itself the finding. **Fail-closed:** an absent, empty, truncated or uninterpretable matrix reads MATRIX-UNDELIVERED, never N/A — "no matrix" and "no declared ADDs" are indistinguishable, so the gate refuses to distinguish them. **Fixture-mode refusal:** the executor's `--fcm-diff-file` determinism seam is refused (ERROR) against any plan under `release/releases/plans/`, so a live release can never be graded against an author-supplied diff set. **Evidence-freshness guard (mirrors G-PR9/G-PR10):** a consumed verdict is valid only if emitted against the final PR head SHA. Verdict: **MATRIX-DELIVERED** (every unconditional declared ADD present as an addition in the merged diff, or carrying a `NOT DELIVERED` Deviation-Log row; zero uninterpreted and zero pathless rows) → advance / **MATRIX-UNDELIVERED** → NO-GO recommendation input / **N/A** (run target is not a release plan). Composes with — does not duplicate — G-PR10: G-PR10 grades cross-issue predicates, G-PR11 grades matrix delivery. **Cutover discipline:** applies to releases entering Stage 9 strictly AFTER this criterion's introducing-release merge SHA recorded in the release log; the introducing release itself is exempt (reflexive-pipeline-loop discipline). The **check** is not gated by this cutover — it is forward-only and run-target-triggered, never a corpus sweep. | validation | structural | auto | the `fcm-delivery` records the release's verification-execution executor emitted against the merged PR head SHA, read read-only |
| G-PR12 | **Release-PR draft-state cleared at the GO instant.** The release PR is not in draft when Phase C renders GO. **Two recorded observations, both required, taken at two named call sites** per `release/references/pipeline/stage-09-plan-review.md` § 5 Phase A8 and Phase A9: **(i)** the **Phase A8 read-back**, taken from the host immediately after `gh pr ready <release-PR>` and recorded on the anchor line `**Phase A8 draft-state read-back:**` carrying the observed `isDraft`, the read timestamp, and the PR number; **(ii)** the **Phase A9 gate-instant read**, carried on the host call Phase A9 already makes so it costs **no additional API call**, and recorded on the anchor line `**Phase A9 gate-instant draft-state read:**` carrying the observed `isDraft`, the PR's `updatedAt`, the read timestamp, and the PR number. The criterion resolves both records by their bolded anchor labels in the Stage 9 sub-task comment set — the label is a resolvable address, not a description of one, so a third party can reproduce the grading. Phase A9 supplies the call site only; its advisory `reviewDecision` contract does not extend to these fields, whose gating authority is this row. **Evidence-freshness guard — the mutable-subject analogue of the G-PR9 / G-PR10 / G-PR11 SHA-pinned guard.** `isDraft` moves without a commit, so there is no head SHA to pin and the sibling guards' anchor does not exist on this subject; the guard binds the PR's `updatedAt` instead, because every event that can change draft state also moves it. Before grading, the hub compares the recorded `updatedAt` against the PR's live value; if the live value has advanced, the observation is **stale**, and the Phase A9 read is re-run and re-recorded before the criterion is graded. A stale-but-unrefreshed observation is not a valid satisfied input. **Verdicts. DRAFT-CLEARED** — both observations present, both read `false`, and the gate-instant observation is current → satisfied. **DRAFT-BLOCKING** — either observation reads `true`, which includes the case where the two **disagree** and thereby records the window in which draft state changed → non-satisfied. **DRAFT-UNVERIFIED** — an observation is absent, or a read is unresolvable after the bounded settle allowance at `release/references/specs/release-readiness-scan-spec.md` § 5.1 → non-satisfied. **Live gate-instant cross-check — the grading-time `isDraft` is a verdict input, not a fetched-and-discarded field.** The grading-time call returns `isDraft` in the same response as the `updatedAt` this guard compares, so consuming it costs **no additional API call**; that live value is read **and graded**. If the live `isDraft` reads `true`, the criterion is **DRAFT-BLOCKING** whatever the two recorded observations say — a recorded `false` that the host contradicts at the grading instant is a false record, not a satisfied input. If the live read **disagrees** with the recorded Phase A9 gate-instant observation, the criterion is likewise **DRAFT-BLOCKING**, on the same disagreement rule the two recorded observations already carry between themselves. Without this clause the freshness guard binds `updatedAt` **alone**, which catches a party who *fabricates* a timestamp but not one who copies the live `updatedAt` faithfully and never reads the draft state sitting beside it in the same response — the value would be retrieved and never consumed, which is the "declared control with no observing step" shape this release exists to eliminate. **Fail-closed:** an unread, unrecorded, or unresolved draft state is never graded as "not draft", and DRAFT-UNVERIFIED is never dispositioned **N/A** — "the PR is ready" and "we could not confirm the PR is ready" must stay distinguishable outputs. N/A applies only to a release with no PR at all. **Why two observations rather than one:** a record the acting party writes about its own action and then consumes minutes later is byte-identical whether the read was performed or the expected value was transcribed, so a single self-consumed record observes nothing; the second observation at a different instant, the read timestamps, and the freshness comparison against live host state are jointly what make a skipped read detectable. Reading only at Phase A8 would also **maximize** the check-to-use window rather than close it — the measured Phase-A8-to-merge span runs a median of 8 minutes and a maximum of 60 with the GO inside it. Composes with — does not duplicate — **G-PR5**, which reads `mergeable` and is **draft-blind** (a draft PR reports `MERGEABLE`, so G-PR5 grades PASS on the exact state this criterion exists to catch); and with the **Stage-12 Phase A.7** release-PR draft-state guard, which re-reads the same predicate at the merge instant as the **backstop, not the primary control**, and which this criterion leaves in place unchanged. **Cutover discipline:** this criterion binds a release that enters Stage 9 strictly after the commit at which it landed on the mainline, as recorded in the release log. The release that introduces it is **exempt**, because a gate cannot fire on the run that creates it: this release's own Stage 9 is graded under the pre-existing G-PR1..G-PR10 checks (**G-PR11 is excluded from that baseline because it ships in this same release** — naming a sibling criterion from its own release as "pre-existing" would declare this criterion exempt relative to a check that did not exist on the mainline either), and the Stage-12 Phase A.7 backstop covers it unchanged. | validation | structural | auto | the `**Phase A8 draft-state read-back:**` and `**Phase A9 gate-instant draft-state read:**` anchor lines resolved in the Stage 9 sub-task comment set and graded together, with **both** live fields from one `gh pr view <release-PR> --json isDraft,updatedAt` consumed at grading time: the recorded `updatedAt` compared against the live `updatedAt` for freshness, **and** the live `isDraft` graded against the recorded Phase A9 observation per the live gate-instant cross-check |

### Self-Repair Actions

| ID | On Failure | Action |
|---|---|---|
| G-PR1 | Phase A evidence missing | Return to Phase A; block Phase C decision until evidence assembly complete. |
| G-PR2 | Upstream report missing | Escalate per [release-process.md](../../release/governance/release-process.md) Inter-Stage Feedback Protocol Tier 2 (SCOPE CHANGE) — upstream stage owes return-to-Stage 9. Block decision. |
| G-PR3 | PR scope diverges from release plan | Tier 1 `[ADJUST]` — Engineering amends OR operator approves deviation per release plan Deviation Log. |
| G-PR4 | Risks unaccepted | Return to Stage 4 Planning for risk-mitigation amendment OR explicit "accepted with documented rationale" annotation in Decision Record. |
| G-PR5 | PR not mergeable | Return to Stage 6 Engineering per [release-process.md](../../release/governance/release-process.md) Inter-Stage Feedback Protocol Tier 1/2. |
| G-PR6 | Decision Record missing | Operator posts; Decision Record IS the closure artifact (no separate escalation tier). |
| G-PR7 | Goal-conformance MISALIGNED OR DIVERGED-WITH-RATIONALE | Tier 2 `[SCOPE CHANGE]` per [release-process.md](../../release/governance/release-process.md) § Inter-Stage Feedback Protocol — Outcome was drafted at Stage 3 but scope drifted during execution; operator decides: (a) amend Outcome with documented rationale in Decision Record (DIVERGED-WITH-RATIONALE path), (b) defer specific issues + amend Outcome accordingly, (c) NO-GO and re-bundle (MISALIGNED path). Recommend-tier: operator may also accept ALIGNED-with-divergence finding with documented rationale. |
| G-PR8 | Divergence detected (verdict ≠ CLEAN) | Record the Phase A6.5 verdict in the Plan Review comment. On **DIVERGED-RELEASE-FILES-UNTOUCHED**: record verdict + informational note; advance to Phase A7 (not a HALT). On **DIVERGED-RELEASE-FILES-TOUCHED**: HALT pre-GO via Tier 2 `[SCOPE CHANGE]` per [release-process.md](../../release/governance/release-process.md) § Inter-Stage Feedback Protocol — a sibling commit touched this release's declared files; operator re-baselines the release branch on current `main` and re-runs the A4 structural sub-audit + Phase A6.5 before a fresh GO. |
| G-PR9 | GO baseline SHA unrecorded OR predicate = STALE-REVALIDATE / STALE-VOID | Record the baseline SHA in the release plan `### Baseline SHA` H3 if absent. On STALE-REVALIDATE: re-run the A4 structural sub-audit + Phase A6.5 divergence check against current `main`; if clean, re-render the GO with the new baseline SHA. On STALE-VOID: Tier 2 `[SCOPE CHANGE]` per [release-process.md](../../release/governance/release-process.md) § Inter-Stage Feedback Protocol — re-baseline the release branch on `main` and return to Stage 9 for a fresh GO (fresh-Stage-9-GO-mandatory precedent). |
| G-PR10 | RELEASE-INCONSISTENT (≥1 CIAC NOT MET / unresolved PARTIAL), OR a consumed CIAC verdict is stale (emitted against a superseded head SHA) | On a stale verdict: re-trigger the verification-execution executor to re-emit each CIAC verdict against the final PR head SHA, then re-read. On RELEASE-INCONSISTENT: compile a per-CIAC finding (issues spanned, shared surface, predicate, observed vs. expected) and route non-MET to the finding-disposition framework — fix-now returns to Stage 6 Engineering; otherwise surface as a NO-GO recommendation input. Recommend-tier: operator may accept a PARTIAL with recorded rationale (G-PR7 DIVERGED-WITH-RATIONALE precedent). |
| G-PR11 | MATRIX-UNDELIVERED (≥1 unconditional declared ADD absent from the merged diff with no `NOT DELIVERED` Deviation-Log row; OR the matrix is absent / empty / truncated; OR the coverage record reports uninterpreted or pathless rows; OR no `fcm-delivery` record was emitted at all), OR a consumed verdict is stale (emitted against a superseded head SHA) | **Tier 1 [ADJUST].** On a stale verdict: re-trigger the verification-execution executor to re-emit against the final PR head SHA, then re-read. On a missing declared ADD: **either** Engineering delivers the declared file, **or** the operator records the `NOT DELIVERED` Deviation-Log row naming the path (or, for a conditional row, its condition token) — then the executor re-emits and the gate re-reads. On an absent / empty / truncated matrix or uninterpreted rows: the plan's matrix is repaired against the Stage-4 authoring contract; this is an authoring defect, and it is never dispositioned as N/A. On **no record emitted at all**: treat as a wiring failure in the executor, not as a pass — the absence of the check is the finding. |
| G-PR12 | **DRAFT-BLOCKING** (an observation reads `true`, the two observations disagree, or the grading-time live `isDraft` reads `true` or disagrees with the recorded Phase A9 observation), **DRAFT-UNVERIFIED** (an observation is absent, or a read is unresolvable after the settle allowance), OR a recorded observation is **stale** (the PR's live `updatedAt` has advanced past the recorded value) | **On a stale observation:** re-run the Phase A9 gate-instant read, re-record the anchor line, and re-grade. This branch terminates on the refreshed read and renders no verdict of its own. **On DRAFT-BLOCKING — Tier 1 `[ADJUST]`, one operator-mediated cycle, then terminal.** Surface the still-draft state to the operator naming the one-command remedy (`gh pr ready <release-PR>`, which is the Phase A8 transition), and after the operator acts perform **exactly one** re-read and re-record. **Terminal state:** if that single post-remedy re-read still returns `true`, or the operator declines the remedy, the criterion is non-satisfied and **terminal** — it surfaces as a **NO-GO recommendation input** naming the still-draft PR, and no further agent re-attempt is prescribed. The agent issues **no autonomous retry** at any point on this branch, for three reasons that are stated rather than assumed: Stage 9 is a pure human gate whose retry posture is NO per `core/disciplines/autonomous-execution-model.md` § Per-Stage Application; the operator is present at this gate by construction, so the remedy needs no agent-side loop to reach a human; and an uncapped `[ADJUST]` loop at a human gate consumes the gate instead of escalating to it. **On DRAFT-UNVERIFIED:** the criterion remains non-satisfied and surfaces as a **NO-GO recommendation input** naming the absent anchor line or the unresolvable read (the operator may override with recorded rationale, per the G-PR7 DIVERGED-WITH-RATIONALE precedent). Never record an unread draft state as ready, and never substitute the expected value for an observation that was not taken — a transcribed value is the defect this criterion exists to detect, not an acceptable stand-in for it. |

---

## Gate 12: Execute Readiness

*"Did execution produce a verified, audit-trail-complete deployment?"*

**Stage boundary:** 12->13 (Execute -> Close)
**Inherits:** Gate 12->13 from [field-lifecycle-matrix.md](field-lifecycle-matrix.md#gate-12-13-execute---close)

| ID | Criterion | Type | Check | Automation | Validation method |
|---|---|---|---|---|---|
| G-EX1 | PR MERGED | artifact | structural | auto | `gh pr view --json state` returns `MERGED` |
| G-EX2 | Tag exists on main | artifact | structural | auto | `git tag --list <vX.Y>` returns the tag |
| G-EX3 | All S-2 deployments completed with zero diff | validation | structural | auto | `core/deploy/deploy.sh --check` exits 0 |
| G-EX4 | Deployment log appended to RELEASE_LOG.md | artifact | structural | auto | `grep "vX.Y" <OPERATOR_INSTANCE_RELEASE_LOG_PATH>` returns the entry |
| G-EX5 | Release row in RELEASE_LOG.md present (table row + HTML comment block per RELEASE_LOG.md format) | artifact | structural | auto | — |
| G-EX6 | All Phase C verification PASS (C1-C5 results documented per [pipeline/stage-12-execute.md](../../release/references/pipeline/stage-12-execute.md) §5 Phase C) | validation | structural | auto | — |
| G-EX7 | No Layer 2 leakage — no projects/ files committed | validation | structural | auto | `git status` clean (no projects/ files staged/committed) |
| G-EX8 | Deferred items documented (release plan Deferred Items section populated) | field | structural | auto | — |
| G-EX9 | ADR-number reconciliation tooled and complete. **Conditional — fires only when the release renumbered ≥1 ADR at Phase A.5.7.** The renumber was performed by `release/tools/renumber-adr.py` and is complete on all six steps: the record renamed (git records a rename), branch-scoped citations rewritten with zero dangling, every applicable index surface updated, the `core/ADRs/README.md` § Renumber log appended, and a `## Status` **Numbering provenance** note present on the renumbered record. The provenance note is the criterion's load-bearing half — it is the one step the hand-performed recovery reliably skipped, which is why the gate asserts the artifact rather than the intent. A release that authored no ADR, or whose ADR number bound unchanged at A.5.7b, **passes trivially**. Canonical authority: [`ADR-115`](../../release/ADRs/ADR-115-adr-number-claim-binds-at-merge.md). **Cutover discipline:** applies to releases entering Stage 12 going forward; the introducing release itself is exempt (reflexive-pipeline-loop discipline). | validation | structural | auto | `python3 release/tools/renumber-adr.py --detect` reports `BINDS` for every claim AND `python3 release/tools/check-adr-numbers.py` exits 0 AND each renumbered record matches `\*\*Numbering provenance — .\d{3} → \d{3}.\.\*\*`. Fail-closed: an indeterminate read is non-satisfaction |

### Self-Repair Actions

| ID | On Failure | Action |
|---|---|---|
| G-EX1 | PR not MERGED | Retry `gh pr merge` on transient (HTTP 5xx, lock conflict). Rollback via `git revert` on systemic failure. |
| G-EX2 | Tag missing | Re-tag via signed-annotated form `git tag -a -m "v<X.Y>-<milestone-slug> — <N> issues; release SHA = merge of PR #<n>" vX.Y "$MERGE_SHA" && git push origin vX.Y`. |
| G-EX3 | Diff-non-zero on deployed copy | Re-run `core/deploy/deploy.sh --deploy <skill>` per [skill-deployment.md](../rules/skill-deployment.md) post-merge steps. Rollback via `git revert` on systemic failure. |
| G-EX4 | RELEASE_LOG.md entry absent | Append entry; validate format per RELEASE_LOG.md §"Deployment Log format". |
| G-EX5 | Release row malformed | Re-author row per RELEASE_LOG.md template. |
| G-EX6 | Phase C verification incomplete | Re-run failing verification (C1 merge / C2 deployed-copy / C3 functional / C4 layer-boundary / C5 rollback) per Phase C protocol. |
| G-EX7 | Layer 2 leakage | `git restore --staged projects/*` and `git checkout -- projects/*` to clean working tree. Escalate per Inter-Stage Feedback Protocol Tier 1 `[ADJUST]`. |
| G-EX8 | Deferred items missing | Populate release plan Deferred Items section before Stage 13 entry. |
| G-EX9 | ADR renumber incomplete | Re-run `python3 release/tools/renumber-adr.py --renumber <old> <new> --apply`. It is idempotent, and when the rename is already in place it enters **completion mode** and performs only the steps still outstanding — so a hand-performed renumber that skipped the `## Status` provenance note is repaired by re-running the tool, never by hand-patching the note. Do not hand-write the note: a checklist step that was already documented twice and still missed is not repaired by a third restatement. If the tool exits non-zero it has reverted its whole staged set (zero partial application); read its R1/R6 message and escalate Tier 2 per Inter-Stage Feedback Protocol. |

---

## Gate 13: Close Readiness

*"Is the release fully closed — issues, milestone, log, verification, operational deployment?"*

**Stage boundary:** 13 Exit (Release Close)
**Inherits:** Gate 13 Exit from [field-lifecycle-matrix.md](field-lifecycle-matrix.md#gate-13-exit-release-close)

| ID | Criterion | Type | Check | Automation | Validation method |
|---|---|---|---|---|---|
| G-CL1 | All issues CLOSED or documented-deferred — no open milestone issues, OR all open issues carry a deferred-rationale in the release plan | validation | structural | auto | `gh issue list --milestone <X> --state open` returns empty (else each open issue has documented deferred-rationale) |
| G-CL2 | Milestone CLOSED | anchor | structural | auto | `gh api repos/<owner>/<repo>/milestones/<N>` returns `state: closed` |
| G-CL3 | **The release's terminal state is recorded and verified in the ledger appropriate to its close class.** Resolve the close class first, per the § Close-Class Conditioning Disposition block below, then evaluate the matching branch. **Deployable branch** — the default, and the branch every release takes unless it declares otherwise: RELEASE_LOG.md status VERIFIED (transitioned DEPLOYED → VERIFIED via grep on entry status field). This branch is **byte-unchanged** from the pre-conditioning criterion; a deployable-class release evaluates exactly what it evaluated before, with no new required step. **`task-artifact` branch:** no RELEASE_LOG release row is written, and the criterion is satisfied instead by the **Artifact-Acceptance Record** (AAR) in the release plan's Verification Evidence section, per [pipeline/stage-13-close.md](../../release/references/pipeline/stage-13-close.md) § Phase A7.5. **The obligation is re-anchored, never removed:** class resolves `task-artifact` and no AAR is present ⇒ **FAIL**, never N/A — an unrecorded acceptance is an unclosed release, exactly as an unrecorded deployment is. **Rejected alternatives, recorded so they are not re-proposed:** manufacturing a RELEASE_LOG row for a release that deployed nothing (forbidden — it is the fabrication the class exists to avoid); forking this spec into a second close criterion set (forbidden — one gate spec); and extending the RELEASE_LOG status lattice with an accepted state, which would force a RELEASE_LOG.md convention edit, collide with the Stage-13 concurrent-merge per-row `max` rule over `{DEPLOYED < VERIFIED}`, and *still* require a row. **PRE-COMMIT COMPLETENESS LIMB (deployable branch).** The terminal record is not verified merely by existing — it is verified by existing *behind its own output set*. The `DEPLOYED → VERIFIED` transition is written into the working tree several phases before that tree is committed, so a close whose producing phases dropped an output can commit the stamp anyway and the record then claims a completeness it does not have. This limb binds the moment: the close-out asserts every `required` (and every armed `required-if`) member of the canonical Stage 13 output set — the Step 4 Verification table in `hub-spoke-bridge.md` Procedure 7, never re-enumerated here — against the tree about to be committed, and a missing member BLOCKS before the commit. **A `required-if` predicate that cannot be evaluated is INDETERMINATE and BLOCKS**; it is never graded not-owed, because an output recorded satisfied *because its membership test could not run* is the same silent absence this limb exists to close. **A `**Not-produced:**` marker is evidence, never an exemption** — it records why a member is absent and satisfies nothing. **Why a limb on G-CL3 rather than a G-CL10:** G-CL3 already owns *the release's terminal state is recorded and verified*, and this is the assertion that the terminal state was not recorded ahead of its own output set. A separate criterion would fork one concern across two IDs. The post-merge `deploy.sh` Check 48 sub-checks stay the detective lane and do not discharge this limb — they select rows already reading `VERIFIED`, so by construction they cannot evaluate before the stamp. **Cutover discipline:** the class conditioning applies to releases entering Stage 13 Close strictly AFTER its introducing release's merge SHA recorded in the release log; the introducing release itself is exempt (reflexive-pipeline-loop discipline — it cannot fire its own new gate). The pre-commit completeness limb carries the same cutover on its own introducing release. | field | structural | auto | — |
| G-CL4 | Verification evidence persisted (release plan Verification Evidence section populated with PASS/FAIL per check, post-merge commit). **This base check is unchanged for every close class.** **Additive class requirement:** the same section additionally carries an **Artifact-Acceptance Record** (AAR) — one row per declared task-artifact deliverable, per the block schema in [pipeline/stage-13-close.md](../../release/references/pipeline/stage-13-close.md) § Phase A7.5 — in two situations. **(a) REQUIRED** when the resolved close class is `task-artifact`: this is the surface G-CL3's terminal-record substitution reads, and without it G-CL3 has nothing to be satisfied by. The pairing is load-bearing in one direction — G-CL3 substitutes onto a surface that G-CL4 is what mandates. **(b) ADDITIVE** when a *deployable*-class release nonetheless declares task-artifact deliverables: the release's own G-CL3 takes the unchanged deployable path, and the AAR records per-deliverable acceptance alongside it. **ABSENT otherwise** — an explicit no-op, no ceremony, no empty-section requirement. The conditioning **adds a lens and never removes the base check**, so a release that declares no task-artifact deliverable is graded exactly as before. **Cutover discipline:** applies to releases entering Stage 13 Close strictly AFTER its introducing release's merge SHA recorded in the release log; the introducing release itself is exempt (reflexive-pipeline-loop discipline) **yet dogfoods branch (b)** — it declares task-artifact deliverables and records their acceptance additively while its own close takes the deployable path. | artifact | structural | auto | — |
| G-CL5 | Operational deployment manifest fully executed (all Phase B-OPS manifest entries PASS or exception-documented per [pipeline/stage-13-close.md](../../release/references/pipeline/stage-13-close.md) §5 Phase B-OPS) | validation | structural | auto | — |
| G-CL6 | Design artifacts refreshed when applicability fired during this release. For each Tier-A activated artifact declared in the release plan's "Tier-A activated design artifacts" section, the artifact's last commit SHA is within the release branch commit range AND the diff is non-trivial (>3 line delta, excludes frontmatter-only). Canonical authority: [`design-artifact-standard.md`](../standards/design-artifact-standard.md) § 8. Initial warn-mode posture per [`bypass-mode-readiness.md`](../rules/bypass-mode-readiness.md) Shakedown → Enforce Transition Checklist precedent; flip-to-enforce at 2-3 release threshold. **Cutover discipline:** The design-artifact refresh-gate applies to all Stage 13 releases going forward. | artifact | structural | auto | — |
| G-CL7 | Goal-attainment verification recorded per the QC4 protocol. The Stage 13 spoke renders QC4-06 verdict (ATTAINED / PARTIALLY-ATTAINED / NOT-ATTAINED) per Phase A10 of [`pipeline/stage-13-close.md`](../../release/references/pipeline/stage-13-close.md) — 1-paragraph attainment narrative citing Change Description (per the template) + Success Indicator (when present) + ≥1 verifiable evidence anchor — appended to release plan Verification Evidence section. The criterion asserts presence of the verdict + narrative, not the verdict value. Initial warn-mode posture per [`bypass-mode-readiness.md`](../rules/bypass-mode-readiness.md) Shakedown → Enforce Transition Checklist precedent — per-release FAIL logs to the shared deploy-check warn log (`$(pmo_instance_path)/deploy-check-warn-log.jsonl`) and Milestone close proceeds — **specified, not yet emitting**: no check emits this gate's FAIL row. Flip to enforce after 2-3 release shakedown. Composes with decision-outcome capture: ATTAINED → SUCCESS / PARTIALLY-ATTAINED → PARTIAL / NOT-ATTAINED → SUCCESS-document-divergence OR ROLLBACK per operator. **Cutover discipline:** Goal-attainment verification applies to all Stage 13 releases going forward. | artifact | structural | auto (warn → enforce) | — |
| G-CL8 | Documentation Impact resolved for every closed issue in the release per the field spec. For each issue closed by the release PR, the issue body's `Documentation Impact` field is satisfied: declared docs exist and were modified within the release branch's commit range (verified via the Validation method command), OR the field reads exactly `None — no documentation impact (rationale: <phrase>)`. The criterion asserts presence + resolution of the declaration; the *absence* of any Documentation Impact value (not the explicit "None — no documentation impact (rationale: ...)" answer) is the gate-failing state. Backed by Check 28 (`doc-impact-resolution-presence`; command in Validation method). **This row is the authority for G-CL8's scope**; the Stage-1 and Stage-13 restatements in the release-process governance file are derivative and inherit from here. **Issue-scope boundary — which issues G-CL8 reads (a derived property, never a template or kind literal):** G-CL8 is in scope for a closed issue **if and only if its body carries a `### Documentation Impact` section**. An issue whose body carries no such section was authored on a form that declares no such field, and it is **out of scope with recorded rationale** — a criterion cannot grade the resolution of a declaration the author was never offered a place to make. The key is the **body**, deliberately, and the alternative was considered and rejected: keying to "the form the issue was authored on" is not evaluable for a work-item kind that has no dedicated form and emits on the interim `improvement.yml` vehicle, and it wrongly exempts those issues even though their bodies do carry a fully-authored declaration. The body key is **total** (every issue has a body), **per-form by construction** (no template or kind is named), and **self-revoking** (a form that gains the field puts it in its bodies, and those issues enter scope with no edit here). "Field present but empty of content" is *in* scope and is the gate-failing state — the exemption reaches absence of the section, never absence of an answer. **Corpus-scope boundary:** K1 codified corpus only — `core/rules/`, `core/`, `core/governance/`, `release/skills/*/SKILL.md` + `references/`, `CLAUDE.md`. Initial warn-mode posture per [`bypass-mode-readiness.md`](../rules/bypass-mode-readiness.md) Shakedown → Enforce Transition Checklist precedent — per-issue FAIL logs to the shared deploy-check warn log (`$(pmo_instance_path)/deploy-check-warn-log.jsonl`) and Milestone close proceeds — **specified, not yet emitting**: no check emits this gate's FAIL row; Check 28 emits template-surface drift rows, not this per-issue row. Flip to enforce after 2-3 release shakedown OR warn-log drained to < 10 entries (whichever first). **Cutover discipline:** The documentation-impact resolution gate applies to all issues entering Stage 1 Intake and all releases entering Stage 13 Close going forward. | artifact | structural | auto (warn → enforce) | `git log --follow <docs> origin/main..HEAD` (declared docs modified in the release branch range); backed by `deploy.sh` Check 28 |
| G-CL9 | Promised ADR `Proposed → Accepted` flips landed for every ADR this release's review ratifies. For each ADR whose recorded status declares a **conditional ratification** — a `status:` beginning `Proposed` whose value or `## Status` section promises transition at a named review ("flips to Accepted at …", "ratified at …") — and whose named ratifying review is **the review this release just closed**, confirm the record now reads `Accepted` with provenance. The criterion asserts the flip landed for the ADRs *this* release ratifies; an ADR whose ratifying review is still in flight is **correctly** `Proposed` and is not a failure. **Why release-scoped, not global:** the ratifying reference is free text and its wording varies per record (some carry it inside the `status:` line itself), so mapping it to a milestone and resolving that milestone's closed-state is not reliably mechanizable. The operator closing a release knows which review just closed — that judgment is the high-precision catching mechanism, and it is where this criterion sits. **Backed by** `deploy.sh` Check 58 (`adr-flip-verify`) as a standing **advisory** signal only: that check surfaces Proposed-with-promise records and their age, but is structurally incapable of enforcement (it cannot see whether a review has closed) — **this criterion is the authority; Check 58 never is.** Mirrors the G-CL8 + Check-28 pairing. Initial warn-mode posture per [`bypass-mode-readiness.md`](../rules/bypass-mode-readiness.md) Shakedown → Enforce Transition Checklist precedent — per-release FAIL logs to the shared deploy-check warn log (`$(pmo_instance_path)/deploy-check-warn-log.jsonl`) and Milestone close proceeds — **specified, not yet emitting**: no check emits this gate's FAIL row; Check 58's advisory rows report flip-promise presence and age, not this row. Flip to enforce after 2-3 release shakedown. **Cutover discipline:** the ADR-flip verification applies to releases entering Stage 13 Close going forward; the introducing release itself is exempt (reflexive-pipeline-loop discipline — it cannot fire its own new gate). | artifact | structural | auto (warn → enforce) | `grep -lE '^status:[[:space:]]*Proposed' core/ADRs/*.md release/ADRs/*.md` returns no ADR whose named ratifying review is the review this release closed; backed by `deploy.sh` Check 58 |

### Close-Class Conditioning Disposition (referenced from G-CL3 + G-CL4 criterion bodies)

The § Composite-OR Oversize Predicate / § Acceptance-Fit Predicate / § Capability-Class Usability-AC Requirement placement precedent: a referenced predicate block sited beside the gate table it serves, carrying the resolution rule and the per-criterion disposition, so neither is restated in nine criterion cells.

**Close-class resolution — a three-rung ladder with an explicit no-op default.**

```
rung 1   release plan  domain_practice.domain        <- Stage-4 A1.5, mandatory on every plan
rung 2   PROJECT.md    deliverable_type              <- the authoritative source rung 1 reads,
                                                        where a project declares one
rung 3   (unresolved)                                <- EXPLICIT NO-OP DEFAULT

if  resolved value == `task-artifact`   ->  TASK-ARTIFACT branch
else                                    ->  DEPLOYABLE branch   (base behavior, byte-unchanged)
```

Three properties are load-bearing, and each is stated because a later simplification could quietly remove it:

1. **Declaration-driven, never inferred.** The branch fires only on the literal recognized value. An unrecognized, misspelled, absent, or free-form value resolves **DEPLOYABLE** — today's exact behavior. This is the `G3-18` rule applied verbatim: absence falls to the else-as-no-op, never to an else-as-gate. A close class is a thing a release *declares*; it is never guessed from a file list, a label, or a milestone name.
2. **The fall-through is surfaced, not silent.** A release whose resolved class is neither `task-artifact` nor a guide-backed deliverable class is **reported** at close — one line in the close-out report, per [pipeline/stage-13-close.md](../../release/references/pipeline/stage-13-close.md) § Phase A7.5. Routing to the default is correct; routing there **quietly** is what turns a typo into a wrong close. This makes the default pathway load-bearing rather than invisible.
3. **`task-artifact` is a NAMED class, not a free domain name.** A value a gate branches on must be resolvable in one lookup — which is the stated purpose of [domain-token-registry.md](../specs/domain-token-registry.md) §2 Concept 1, where it is registered. A free-form domain name remains permitted for every other purpose; it simply does not select this branch.

**Per-gate disposition — all nine close gates, no gate unstated.**

An unstated gate is a gap, not a no-op: a reader cannot distinguish "considered and deliberately left alone" from "never considered". Both warn-mode gates (`G-CL8`, `G-CL9`) are therefore recorded explicitly, because a warn-mode gate that the branch mis-conditions degrades to a log line rather than a block — the miss would be silent.

| Gate | Mode | Disposition | Why this, and why not the alternative |
|---|---|---|---|
| **G-CL1** All issues CLOSED / documented-deferred | block | **NO-OP (explicit)** | The criterion asserts *that* an issue is closed, never *how its work landed*. A task-artifact issue closes identically. Recorded explicitly because this is the gate a reader most expects to need conditioning — the record exists to stop a later editor adding one. |
| **G-CL2** Milestone CLOSED | block | **NO-OP (explicit)** | Terminal act of every close class. Class-independent by construction. |
| **G-CL3** Terminal record verified | block | **CONDITIONED — class-appropriate terminal-record substitution** | The crux. Re-expressed at its true altitude, then branched: deployable keeps `DEPLOYED → VERIFIED` byte-unchanged; `task-artifact` substitutes the AAR. Obligation re-anchored, never removed. See the criterion body for the three rejected alternatives. |
| **G-CL4** Verification evidence persisted | block | **CONDITIONED — additive (adds a required sub-block)** | The purest criterion-body-refinement mirror: the base check is unchanged for **all** classes and the branch only **adds** the AAR requirement. Paired with G-CL3 by construction — G-CL3's substitution has a surface to land on only because G-CL4 mandates it. |
| **G-CL5** Operational deployment manifest executed | block | **NO-OP (explicit) — the existing empty-set path already covers it** | A task-artifact release declares an empty manifest and passes on the **existing** path ([pipeline/stage-13-close.md](../../release/references/pipeline/stage-13-close.md) Phase B-OPS1: *empty manifest = skip to C1*). **Deliberately not conditioned:** adding a class branch would duplicate a working empty-set trivial-pass and create two routes to one verdict. Verified against the phase text, not assumed. |
| **G-CL6** Design artifacts refreshed (Tier-A) | **warn** | **NO-OP (explicit)** | Keyed off the release plan's Tier-A declaration, which is class-independent. A task-artifact release with Tier-A artifacts refreshes them identically. No conditioning ⇒ no silent-miss surface added to a warn-mode gate. |
| **G-CL7** Goal-attainment (QC4-06) | **warn** | **NO-OP (explicit)** | Reads the Milestone's Release Outcome Statement — class-independent, verdict enum unchanged. Verified from the criterion text rather than assumed: its evidence-anchor list already admits `grep`, and artifact-presence *is* a grep, so a task-artifact release satisfies the anchor requirement with no change. |
| **G-CL8** Documentation Impact resolution | **warn** | **NO-OP (explicit) — the tempting change is REJECTED** | Highest-risk gate; the correct answer is to leave it alone. The temptation is to exempt task-artifact issues on the reasoning that *the artifact is the doc*. Rejected: G-CL8's scope key is a **derived body property**, chosen over form/kind literals precisely because a literal key is not evaluable for a work-item kind with no dedicated form. A deliverable-class exemption re-introduces exactly that defect — and a gate must not stop asserting on a field that governance has not exempted. Mechanism verified intact: the backing check needs only a branch commit range, which every close class has. |
| **G-CL9** Promised ADR ratification flips | **warn** | **NO-OP (explicit) + second-order check performed** | Keyed to *the review this release just closed* — class-independent. The second-order risk is real and was checked rather than assumed: if a task-artifact close waived the Stage-9 review, G-CL9's referent would vanish and the gate would pass vacuously. **No such waiver exists** — the typed branch changes only the close-**evidence anchor**, never the review path, and Stage 9 runs identically for both classes. Referent intact; no conditioning needed. |

**Result: 2 conditioned · 7 explicit no-ops · 0 unstated.** Minimal conditioning is the goal rather than a shortcut — every gate left alone is one fewer place two paths can drift, and the two that *are* conditioned are conditioned in the criterion body, so no criterion ID is added and the ID enumeration is untouched.

**Adding a future deliverable class costs one value and nine dispositions** — no new gate ID, no new spec, no new file. That bound is a property of branching on a *value* rather than on a class-specific code path, and it is why this table is the extension point.

### Self-Repair Actions

| ID | On Failure | Action |
|---|---|---|
| G-CL1 | Issue not auto-closed | Manual close via `gh issue close <N>` when PR `closes #N` keyword failed (e.g., issue moved milestone post-merge). |
| G-CL2 | Milestone open | Re-run G-CL1 prerequisite; close Milestone via `gh api repos/.../milestones/<N> -X PATCH -f state=closed` once G-CL1 PASS. |
| G-CL3 (deployable class) | RELEASE_LOG.md status not VERIFIED | Edit RELEASE_LOG.md to transition DEPLOYED → VERIFIED + commit post-merge. **Unchanged remediation** — this is the branch every release takes unless it declares otherwise. |
| G-CL3 (pre-commit completeness limb) | A `required` (or armed `required-if`) member of the canonical Stage 13 output set is absent from the tree carrying the VERIFIED stamp, or a `required-if` predicate resolved INDETERMINATE | Produce the missing output, then re-run the close — it is idempotent per phase and converges. **Do NOT repair this by hand-writing the output, and do NOT repair it by removing the member from the manifest.** A hand-composed output fabricates the mechanism claim the output exists to carry, which is precisely why the producing phase refused to compose one; and de-listing a member converts a blocked close into a silent one. On an INDETERMINATE predicate the repair is to the SEAM, not to the verdict: restore the cross-tool read of the cutover value (or set it explicitly) so the membership test can run — never grade the row not-owed to clear the block. A `**Not-produced:**` marker documents the absence and does not clear this finding. |
| G-CL3 (task-artifact class) | Artifact-Acceptance Record absent, or present but missing a declared deliverable | Populate the AAR in the release plan's Verification Evidence section — one row per declared task-artifact deliverable, per the block schema in [pipeline/stage-13-close.md](../../release/references/pipeline/stage-13-close.md) § Phase A7.5 — then re-evaluate. **Do NOT repair this by writing a RELEASE_LOG release row.** The class writes none by design, and manufacturing one is precisely the fabrication the branch exists to prevent; a row added to silence this criterion also corrupts the close-completeness checks that iterate RELEASE_LOG rows. If the class resolved to `task-artifact` in error, the repair is to correct the release plan's `domain_practice.domain` declaration — not to satisfy the wrong branch. |
| G-CL4 | Verification evidence missing | Back-fill from PR / commit log / `core/deploy/deploy.sh --check` results into release plan Verification Evidence section. **When the release declares task-artifact deliverables** (either class), the back-fill additionally includes the AAR rows — an evidence section that is complete on every automated check but silent on a declared artifact's acceptance does not satisfy this criterion. |
| G-CL5 | Operational deployment fail | Phase B-OPS rollback per [pipeline/stage-13-close.md](../../release/references/pipeline/stage-13-close.md) Mode 2. Escalate per Inter-Stage Feedback Protocol Tier 2 SCOPE CHANGE if systemic. |
| G-CL6 | Design artifact not refreshed | Identify the artifact; re-run Stage 5 design-artifact production for the affected flow class per [`design-artifact-standard.md`](../standards/design-artifact-standard.md) § 6 + § 7; commit refresh to release branch. If activation was over-declared (artifact does not need refresh), update the release plan to remove the declaration with rationale. Warn-mode posture: log failure to the shared deploy-check warn log (`$(pmo_instance_path)/deploy-check-warn-log.jsonl`) and proceed — **specified, not yet emitting**: no check emits this gate's FAIL row. Enforce-mode posture: block Milestone close, operator override per CHEAP reversibility tier (one-line `Override: <rationale>` entry in release plan deviation log). |
| G-CL7 | Goal-attainment verification missing | Stage 13 spoke runs QC4-06 retroactively; appends 1-paragraph verdict + narrative to release plan Verification Evidence section via the Stage 13 chore PR. Warn-mode: log to the shared deploy-check warn log (`$(pmo_instance_path)/deploy-check-warn-log.jsonl`) and proceed — **specified, not yet emitting**: no check emits this gate's FAIL row. Enforce-mode (post-shakedown): block Milestone close, operator override per CHEAP reversibility tier (one-line `Override: <rationale>` entry in release plan deviation log). |
| G-CL8 | Documentation Impact unresolved for ≥1 **in-scope** closed issue — i.e. one whose body carries a `### Documentation Impact` section. An issue whose body carries no such section is out of scope per the G-CL8 row's issue-scope boundary and produces no finding; do not "repair" it by adding the section, since the form it was authored on declares no such field. | Surface unresolved declarations to operator with diff evidence (per-issue: declared docs vs. `git log --follow` result on the release branch). Operator chooses: (a) extend release with docs commit on release branch via `fix(dt):` or Tier 1 `[ADJUST]` commit, (b) accept deviation with rationale in release plan deviation log, (c) flip declared value to `None — no documentation impact (rationale: <phrase>)` with documented reason. Warn-mode: log to the shared deploy-check warn log (`$(pmo_instance_path)/deploy-check-warn-log.jsonl`) and proceed — **specified, not yet emitting**: no check emits this gate's FAIL row; Check 28 emits template-surface drift rows, not this per-issue row. Enforce-mode (post-shakedown): block Milestone close, operator override per CHEAP reversibility tier (one-line `Override: <rationale>` entry in release plan deviation log). |
| G-CL9 | A promised ADR flip did not land for an ADR this release ratifies | Confirm first that the named ratifying review actually closed with this release — if it is still in flight the record is correctly `Proposed` and there is nothing to repair. Otherwise transition the ADR's `status:` to `Accepted` with a provenance clause naming the ratifying review, as its own governed change (per-ADR, never a batch edit off one approval), touching the status field and its provenance clause **only** — no structural reflow, no section normalization, so a later ADR structural-conformance sweep carries the corrected value forward instead of colliding with it. Warn-mode: log to the shared deploy-check warn log (`$(pmo_instance_path)/deploy-check-warn-log.jsonl`) and proceed — **specified, not yet emitting**: no check emits this gate's FAIL row; Check 58's advisory rows report flip-promise presence and age, not this row. Enforce-mode (post-shakedown): block Milestone close, operator override per CHEAP reversibility tier (one-line `Override: <rationale>` entry in release plan deviation log). |

---

## Versioning

**Schema version:** 2.16

**v2.16 changes (non-breaking — minor; criterion-body refinement — G3-14 + G3-15 `**Posture:**` clauses and their 2 self-repair rows, section-local to § Gate 3; NO criterion ID added, renumbered, removed, or re-typed; NO column, header, or applies-to cell changed):**

- **Reconciled the G3-14 / G3-15 drain-sink declarations to the shared stage-gate form.** All four declarations (criterion row + self-repair row, per gate) named `core/hooks/gate-g3-1N-warn-log.jsonl`. That is the **hook-layer per-rule** surface, borrowed across layers onto a stage-gate: the other 11 stage-gate warn-mode rows in this file name the shared `$(pmo_instance_path)/deploy-check-warn-log.jsonl`, and no code has ever written either per-gate file. Both gates now name the shared sink discriminated by `check: "g3-14"` / `"g3-15"`, which is the platform's only realized sink form (62 live check ids on one file).
- **Each of the four declarations gains the in-tree `specified, not yet emitting` disclosure**, because after the re-point it remains true that no check emits either gate's FAIL row. This is W1 limb 2 of the written-sink axis in [`gate-efficacy-standard.md`](../standards/gate-efficacy-standard.md) § *Written sink and terminable shakedown*: a declaration naming a sink nothing writes carries the disclosure and MUST NOT declare a drain-history flip criterion. It is a disclosure, **not** a claim that either gate has graduated (ADR-163 § Consequences · Neutral says the same of the seven sibling rows already carrying it).
- **The retired per-gate sink is named, not deleted.** Each declaration records that the `gate-g3-1N-warn-log.jsonl` file it previously declared was never written and is RETIRED. Naming it is load-bearing twice over: it keeps the historical declaration auditable, and `deploy.sh --check` Check 73 conjunct C73-b asserts *per gate* that every declaration of that basename agrees byte-for-byte — a population of 2 per gate that a bare deletion would empty, converting a live conjunct into a vacuous one.
- **Forward form recorded so it is not re-derived:** when a runner lands, rows go to the shared sink under the gate's `check:` id. A per-gate sink file is not the convention and is not reintroduced.
- **Cutover discipline (v2.16):** the declaration-form reconciliation is descriptive of state that already held, so no cutover applies — no gate's detection, posture, threshold, or evaluation moment changes.

**v2.15 changes (non-breaking — minor; criterion-body refinement — G-CL3 + 1 self-repair branch, section-local to § Gate 13; NO criterion ID added, renumbered, removed, or re-typed; NO column, header, or applies-to cell changed):**

- **Extended G-CL3's deployable branch with a PRE-COMMIT COMPLETENESS LIMB.** The criterion asserted that the terminal state is *recorded and verified*; it did not assert *when*. The `DEPLOYED → VERIFIED` transition is written into the working tree several phases before that tree is committed, so a close whose producing phases dropped an output could commit the stamp anyway and the record then claimed a completeness it did not have. The limb binds the moment: every `required` (and every armed `required-if`) member of the canonical Stage 13 output set is asserted against the tree about to be committed, and a missing member blocks before the commit. **A `required-if` predicate that cannot be evaluated resolves INDETERMINATE and BLOCKS** — never not-owed, because an output recorded satisfied *because its membership test could not run* is the same silent absence the limb closes. A `**Not-produced:**` marker is evidence and never an exemption.
- **Extended in place rather than added as a G-CL10**, because G-CL3 already owns *the release's terminal state is recorded and verified* and this is the assertion that the terminal state was not recorded ahead of its own output set; a new criterion would fork one concern across two IDs. The `task-artifact` branch is byte-unchanged — the limb conditions the deployable branch only.
- **Recorded so it is not mistaken for discharge:** the post-merge `deploy.sh` Check 48 sub-checks are the detective lane and do NOT satisfy this limb. They select `RELEASE_LOG` rows whose State cell already reads `VERIFIED`, so by construction they cannot evaluate before the stamp they are checking, and their warn posture maps a finding to exit 0.
- **Cutover discipline (v2.15):** the pre-commit completeness limb applies to releases entering Stage 13 Close strictly AFTER this change's introducing-release merge SHA recorded in the release log; the introducing release itself is exempt (reflexive-pipeline-loop discipline — it cannot fire its own new gate).

**v2.14 changes (non-breaking — minor; additive — G-PR11 + G-PR12; no criterion ID renumber; existing IDs stable):**

- Added **G-PR11** (Declared-vs-delivered File Change Matrix check — QC3.5 / Stage 9 Phase A3.7) to the Gate 9 registry — 1 criteria row + 1 self-repair row. G-PR11 closes the **inbound** half of scope-boundary verification, which was one-directional: DT and QA both confirm nothing outside the approved matrix was touched, and neither confirmed everything inside it landed. It reads the `fcm-delivery` verdict the verification-execution executor emitted against the merged PR (read-only, single-runner; evidence-freshness guard mirrors G-PR9/G-PR10). Row is `validation` / `structural` / `auto`; verdicts MATRIX-DELIVERED / MATRIX-UNDELIVERED / N/A. The check is **always-on at the executor and not plan-declared**, so it cannot be omitted by not being asked for, and it emits a coverage record on every run so a vacuous result is visible rather than silent. The prior max in the G-PR range was G-PR10.
- **G-PR11's cutover is asymmetric between its gate and its check, and the asymmetry is the point.** The **gate** takes effect for a release that enters Stage 9 after this criterion has merged; the release that introduces it does not fire its own new gate. The **check** carries no such boundary — a check is not a gate. It is forward-only and run-target-triggered, never a corpus sweep, so the introducing release runs it on its own plan during Engineering and Dev Testing, and points it deliberately at one historical plan as a non-synthetic control arm.
- Added **G-PR12** (Release-PR draft-state cleared at the GO instant — Stage 9 Phase A8 plus Phase A9) to the Gate 9 registry — 1 criteria row + 1 self-repair row. It closes the gap where Stage 9 could render GO on a pull request still in draft: Phase A8 described the draft-to-ready transition and instructed the hub to *record* it, but nothing read the state back from the host, and no Gate 9 criterion bound `isDraft` at all. The registry carried **zero** draft-state criteria before this addition, and its nearest neighbour is blind to the state by construction — G-PR5 reads `mergeable`, and a draft pull request reports `MERGEABLE`, so G-PR5 grades PASS on the exact state this criterion exists to catch. Row is `validation` / `structural` / `auto`; verdicts DRAFT-CLEARED / DRAFT-BLOCKING / DRAFT-UNVERIFIED, with N/A reserved for a release that has no pull request at all. The new max ID in the G-PR range is now **G-PR12** (prior max G-PR11).
- **The criterion grades two observations rather than one, and that is the load-bearing design choice.** A gate criterion whose evidence is a record the acting party wrote about its own action, consumed by that same actor minutes later, grades identically whether the read was performed or the expected value was transcribed — so it observes nothing. G-PR12 therefore binds a Phase A8 read-back **and** an independent gate-instant read carried on the host call Phase A9 already makes (widening the field list adds no API call), each recorded on a fixed-shape bolded anchor line carrying the observed value, a read timestamp, and the pull-request number. Naming the anchor labels verbatim in the Validation-method column makes the grading reproducible by a third party, which a content description ("the recorded observation") could not be on a surface that already carries a second, contradictory draft-state observation from the Phase A6 readiness scan.
- **The evidence-freshness guard is re-anchored, not omitted.** G-PR9, G-PR10, and G-PR11 pin a consumed verdict to the final head SHA. `isDraft` is mutable live state that moves without a commit, so no head SHA exists to pin; the guard binds the pull request's `updatedAt` instead, on the ground that every event able to change draft state also moves it. A stale observation is refreshed and re-graded, mirroring the sibling guards' stale-then-refresh disposition rather than introducing a new failure mode.
- **The self-repair row's remediable branch carries a terminal state.** The still-draft branch is the one that can loop, so it is capped at exactly one operator-mediated remedy cycle and then terminates as a NO-GO recommendation input; the agent issues no autonomous retry, consistent with the Stage-9 human-gate retry posture and with every peer self-repair row in this gate, each of which names a terminal branch. The unresolvable-read branch is fail-closed on the bounded settle allowance the readiness-scan specification already grants for this same read.
- Schema bump 2.13 → 2.14 (non-breaking minor; additive — G-PR11 and G-PR12). Schema consumers (automated gate-validation tooling, stage-gate evaluator, CER Claim agents) require no structural change — the stage-gate evaluator routes by the `Check` column and an `auto` structural row uses the existing structural checker; no evaluator, no `deploy.sh` check, and no field-lifecycle-matrix entry changes. Existing G1-01..G1-09 / G2-01..G2-13 / G3-01..G3-19 + G4-01..G4-05 + G6-01..G6-06 + G-BR1..G-BR5 + G-PR1..G-PR12 + G-EX1..G-EX9 + G-CL1..G-CL9 IDs unchanged; no ID renumber, no column, header, or type change. The new max ID in the G-PR range is **G-PR12**.
- **Version-derivation note — the instruction fired, was half-obeyed mid-branch, and is reconciled here.** Two sibling slices on this one release branch each added a Gate 9 criterion. The first, G-PR11, labelled its block **`v2.9`** — a number the mainline had already spent on an unrelated Gate 1 refinement — and left `**Schema version:**` unbumped, so the file briefly carried two different `v2.9` blocks and a header that named neither addition. The second, G-PR12, re-derived correctly from the mainline (2.12) and bumped the header to **2.13**; it did not relabel its sibling's committed block, which was the right call for a card that did not own it. **The final Engineering slice owns it, and this is the reconciliation:** one block on this branch carrying **both** additions, the duplicate `v2.9` heading removed, and the sequence gap-free. This is the standing rule stated plainly — **within** a branch, append to the block and restate the enumeration, never split it and never take a second number; **across** branches, re-derive from the mainline. On a merge conflict at the `**Schema version:**` line the resolution is unchanged: re-derive from the mainline and restate the enumeration to include every landed addition, never take either side's value. **That conflict then occurred at the Stage-6 reconciliation merge, and this block is its resolution.** Mid-branch this block was labelled **`v2.13`**, derived when the mainline held 2.12. Merging `origin/main` carried in a concurrently-authored **2.13** — the § Gate 3 G3-05 criterion-body refinement — so two independently-derived 2.13 claims collided at this line, each side having correctly re-derived against a mainline that then moved. Re-derived against the merged mainline, this block takes **2.14**; the landed 2.13 keeps its number and its enumeration below, and neither side's changes are dropped. The collision was on the version identifier **alone, never on doctrine**: the two changes are section-disjoint — the landed 2.13 refines a criterion **body** in § Gate 3 and adds no ID, while this block is **additive** in § Gate 9 and allocates G-PR11 and G-PR12, which no other block claims.
- **Cutover.** The gate takes effect for a release that enters Stage 9 after this criterion has merged; the release that introduces it does not fire its own new gate, so its own Stage 9 is graded under the pre-existing checks and the Stage-12 release-PR draft-state guard covers it unchanged.

**v2.13 changes (non-breaking — minor; criterion-body refinement — G3-05 + 1 self-repair row split + 1 enumerated re-check set, section-local to § Gate 3; NO criterion ID added, renumbered, removed, or re-typed; NO column, header, or applies-to cell changed):**

- **Recorded that AC PRESENCE is mechanically asserted at the `status: bundled` transition, and named the runner.** G3-05 was the correct owner of "AC are measurable" and always had been; what it lacked was any mechanical evaluation, so a card could take `status: bundled` carrying **zero** acceptance criteria and reach Planning undetected. The criterion body now states the assertion, names `parse_acceptance_criteria()` in `release/tools/bundle-issues-parser.py` as the runner, and names its two consumers (Stage 3 Bundle; `deploy.sh --check` Check 22's G1-05a presence arm). Measurability stays a judgment call — only presence became mechanical.
- **The failure mode was EMPTINESS, not heading depth — and the record says so, because the originating card said otherwise.** That card and its planning stage both held that the deploy-time check keyed AC detection on the `### Acceptance Criteria` H3 heading and was therefore blind to bodies predating the template. Read against the shipped code, that is false for the population in question: for the governance-intake family the AC applicability flag is set from the applies-to triple and never from a heading, so the check already reached those cards. Its verdict line emitted only when the count of *non-conforming* criteria exceeded zero, and a body with no criteria at all yields zero non-conforming ones — so the criterion **failed open on the empty set**. It had never once emitted an AC-absence finding, for any body of any era. The heading-depth blindness is real, but it lives on a different branch and is the smaller half. A fix built on the stated mechanism would have hardened a code path that was never the problem.
- **The assertion is independent of heading depth, template era, AND criterion list form.** Depth- and era-independence come free by delegating heading matching to the shared section detector rather than authoring a second grammar — the rule that a consumer of an issue-body field reads it through that detector is already recorded and is binding here. List-form independence is the third axis and was measured, not assumed: template-era cards carry AC as ordered lists, which a checkbox-only counter reads as zero criteria and cannot distinguish from having none. A predicate correcting only the heading depth would still have missed them.
- **Split the G3-05 self-repair row into `G3-05 (presence)` and `G3-05 (measurability)`**, the two-branch `G-CL3` / `G1-04` / `G1-05b` precedent, so a reader can tell which branch a finding came from. The presence row states its own anti-repair explicitly: **do not repair by re-shaping the body to the current template.** Template conformance is a different criterion, and a card whose only defect is an absent section should not be rewritten wholesale to close it. The measurability branch's remediation menu is byte-unchanged.
- **Added the presence member to the Gate-3 body-compliance precondition's minimum re-check set.** That block already declared that a title's presence does not imply the Acceptance Criteria field exists; the re-check set it prescribed did not include the check that would notice. This also discharges the successor the block names — a G1 re-check at the bundling transition, shifted left from a deploy-time gate.
- **Posture: warn by inheritance, and the reason is mechanical rather than cautious.** The originating card recommended grandfathering anchored at the introducing-release merge SHA. That anchor is **not implementable at this runner**: resolving when a card took `status: bundled` needs a per-issue timeline call, and the check holds a one-issue-list-read budget — so the clause could be written but never evaluated, which is exactly the reads-like-a-gate-but-is-not defect this criterion set exists to remove. The implementable equivalent already existed: the Check-22 arm inherits the check-wide `g1-enforcement.mode`, which resolves to `warn` with no mode file present. Every pre-existing bundled card surfaces as a warn row rather than a hard stop, no new mode machinery ships, and the flip lever is the mode file. The Stage-3 evaluation needs no such posture at all — it reads bundling candidates, so it is forward-only by construction and no pre-existing card is ever in its population.
- **Retroactive blast radius, measured rather than estimated — and the measurement is a point-in-time reading, not a standing fact.** The planning-stage figure (~29 of 141 cards failing retroactively) counted cards the check has no authority to gate. Over the gated class the number measured **2 of 86 at commit `6735600b`, 2026-08-12** — that reading is what the posture decision was taken on, and it therefore turned on *which* two rather than on volume. The gated class is an **open population** that grows whenever a card takes `status: bundled`, so both terms move independently of this criterion and the figure is expected to go stale: **re-measure against the then-current tree before relying on it**, and read a larger later count as the population having moved rather than as this measurement having been wrong. Any later figure quoted in evidence carries its own commit-and-date anchor for the same reason.
- **Columns and IDs byte-unchanged; no enumeration claimed.** No criterion ID is added, renumbered, removed, or re-typed, and no applies-to cell, table header, or column count changes — so the Gate 1→2 `structural_pass_rate` denominator in `gate-evaluation-spec.md`, the Gate-1 criterion count, and the Check 22 evaluated set are each untouched. The deploy-side change adds a second **arm** to an existing criterion, not a new criterion and not a new check rung. Same discipline as the v2.11, v2.10, v2.9 and v2.7 refinements: **extend the criterion body, do not add an ID.**
- **Layer-B coverage is stated, not assumed.** One `deploy.sh` check is extended (Check 22, the G1-05a branch); no check is added and no rung ID is claimed. Executable regression lives with the check (`core/deploy/tests/test_g1_form_family.sh`, arms I/J/K) and with the primitive (`release/tools/bundle-issues-parser.py --self-test`, arms AC-P1..AC-P12); both are CI-discovered. **Recorded residual:** Check 22 is deploy-time-only with no CI mirror, so this arm cannot become a required status context; the Stage-3 evaluation partially compensates by moving the assertion earlier.
- **Version-derivation note (the instruction fired an EIGHTH time at Commit 0, and a NINTH at the base merge).** This value was **re-derived against the mainline at Commit 0** — the mainline held **2.11** — not inherited from the working tree and not pre-allocated. The v2.11 block's append instruction is **branch-scoped by its own wording** and that branch has merged, so the first editor on this new branch re-derives and takes a number. On a merge conflict at this line the resolution remains: re-derive from the mainline and restate the enumeration to include every landed addition — never take either side's value. **That conflict then occurred, and this block is its resolution.** The base merge of `origin/main` carried in a concurrently-authored **2.12** — the § Gate 1 `U5` conformance test, from the `triage-and-backlog-instrumentation` milestone — so two independently-derived **2.12** claims collided at exactly this line, each side having correctly re-derived against a mainline that then moved. Re-derived a second time against the merged mainline, this block takes **2.13**; the landed **2.12** keeps its number and its enumeration below, and neither side's changes are dropped. The collision was on the version identifier **alone, never on doctrine**: the two changes are section-disjoint — `U5` lives in § Gate 1's § Capability-Class Usability-AC Requirement and amends that block's **set-level rejection rule**, while this block's changes live in § Gate 3 and amend that gate's **minimum re-check set** — two distinct set-level surfaces that neither overlap nor condition each other. **The next editor of this block on this branch appends its change bullets to the v2.13 block above** rather than authoring a fresh block or taking a number.

**v2.12 changes (non-breaking — minor; criterion-body refinement — 1 conditional conformance test + 1 anti-pattern + 1 self-repair branch, section-local to § Gate 1's § Capability-Class Usability-AC Requirement; NO criterion ID added, renumbered, removed, or re-typed; NO column, header, or applies-to cell changed):**

- **Added conformance test `U5` — the discrimination arm at the check's own parameter surface** — as a **conditional** fifth member of the U-series, firing only when the capability is a *mechanical check* (a script, lint, hook, gate, or CI job that emits a finding). Closes the class defect where a well-formed AC set could assert *"the check detects the thing"* but not *"and it does not flag correct content"*. Three limbs: a **named near-miss** the check must not flag, its **expected zero stated** rather than implied, and the arm **exercised at each declared invocation parameter that narrows the examined population or changes the comparison set**.
- **The scoping is inherited, not invented.** A mechanical check already satisfies this block's own `C1 ∧ C2 ∧ C3` predicate verbatim — C1 enumerates "a script / check / hook, a CI job", C2 "a deploy run, a gate evaluation, a PR event", C3 "a verdict, a blocked action" — and `E1–E5` already exclude reference-content, refactor, bookkeeping and spike work. No fresh fire-predicate and no second exempt list were authored, so nothing can drift out of sync with `C1–C3`, and the obligation cannot leak onto non-check cards.
- **Limb 3 binds to the check's parameter surface, not to the ACs' self-reported scopes.** The alternative formulation — *"at each scope the ACs themselves reference"* — is self-referential and vacuous: an AC set that references no scope satisfies it by construction, which is exactly the shape of the motivating defect. A parameter surface is enumerable from the check's own argument parser and cannot be satisfied by silence.
- **`U5` carries a deferral rider for cards that ADD a check**, stated in the rule rather than left to reviewer folklore. A check that does not yet exist has no declared invocation surface to enumerate, so limb 3 is structurally unauthorable at intake for such a card; it is satisfied by naming the intended narrowing parameters or marking the enumeration `Stage-5-deferred`, which then **binds and is graded at Stage-5 exit**. Limbs 1 and 2 are graded at intake in both cases, and an undischarged marker is an unsatisfied `U5`, never an exemption.
- **Added one anti-pattern** recording why a compliant-looking arm still shipped an over-firing check: both narrowing parameters were exercised individually and their **combination** never was, so the arm ran on the content axis while the defect lived on the scope-by-comparison-set axis. This is the entry that makes `U5` falsifiable rather than boilerplate — the risk the originating work item named explicitly, that a vacuously-satisfied precision criterion is worse than none because it looks like coverage.
- **Extended the `G1-05b` self-repair row** with the mechanical-check return-to-author text naming `U5`, the parameter enumeration, and the deferral branch. No new self-repair row and no new criterion.
- **Doctrine landed on BOTH surfaces in the same change**, as the registered prose exemption in this block requires: the `U5` row, its rider, and the anti-pattern are carried verbatim by `release/references/how-to/intake-style-guide.md` §4b. A one-sided edit is silent divergence and nothing mechanically detects it — the exemption's cost is now five hand-synced doctrine elements rather than four, and a byte-parity check for the pair is the named lever that would retire it.
- **Columns and IDs byte-unchanged; no enumeration claimed.** No criterion ID is added, renumbered, removed, or re-typed, and no applies-to cell, table header, or column count changes — so the Gate 1→2 `structural_pass_rate` denominator at `gate-evaluation-spec.md`, the Gate-1 criterion count, and the Check 22 evaluated set are each untouched. Check 22 implements G1-05b branch (i) only and therefore cannot evaluate a branch-(ii) rider; it is deliberately not extended. Same discipline as the preceding criterion-body refinements: **extend the criterion body, do not add an ID.**
- **Version-derivation note — the instruction has now fired again.** `**Schema version:** N` is a globally-sequenced identifier bound at authoring against a moving population. This value was **re-derived against the mainline at Engineering Commit 0** (mainline held 2.11; the one concurrent release branch carrying this file also read 2.11, i.e. no competing claim), not pre-allocated. On a merge conflict at this line the resolution is to **re-derive from the mainline and restate the enumeration to include every landed addition** — never to take either side's value.
- **Cutover discipline:** `U5` applies **prospectively**, to work items authored or re-crisped from its introducing release's Stage-5 entry forward. It is **not** retroactive to already-closed work items: their implementations already carry two-armed fixtures, the gap is in AC prose, and editing prose on a closed item changes no behavior. The live test set is the introducing release's own check-shipping items, graded at acceptance.

**v2.11 changes (non-breaking — minor; criterion-body refinement — G-CL3 + G-CL4 + 1 referenced predicate block + 1 self-repair branch, section-local to § Gate 13; NO criterion ID added, renumbered, removed, or re-typed; NO column, header, or applies-to cell changed):**

- **Conditioned G-CL3 and G-CL4 on the release's close class**, so a release whose deliverable class is the named value `task-artifact` closes on **artifact acceptance** rather than on a deployment it never performed. G-CL3 is re-expressed at its true altitude — *the release's terminal state is recorded and verified in the ledger appropriate to its close class* — and then branched: the deployable branch is **byte-unchanged**, and the `task-artifact` branch substitutes the **Artifact-Acceptance Record** for the RELEASE_LOG `DEPLOYED → VERIFIED` transition. G-CL4 is purely additive: the base check is unchanged for every class and the branch only **adds** the AAR requirement. Closes the class defect where work whose definition of done is an artifact had no positive lifecycle state and had to either pretend its artifact was a deployed file or stall short of close.
- **The obligation is re-anchored, never removed.** Class resolves `task-artifact` and no AAR is present ⇒ **FAIL**, not N/A. This is the difference between a typed branch and an exemption, and it is the property a later simplification is most likely to erode: an unrecorded acceptance is an unclosed release exactly as an unrecorded deployment is.
- **Added § Close-Class Conditioning Disposition** as a referenced predicate block (the § Composite-OR Oversize Predicate / § Acceptance-Fit Predicate / § Capability-Class Usability-AC Requirement placement precedent), carrying the three-rung close-class resolution ladder with its explicit no-op default, the three load-bearing properties of that ladder, and **a per-gate disposition for all nine close gates**. The disposition table is the block's reason for existing: **an unstated gate is a gap, not a no-op**, because a reader cannot distinguish "considered and deliberately left alone" from "never considered". Result: **2 conditioned, 7 explicit no-ops, 0 unstated.**
- **Both warn-mode gates are deliberately left unconditioned, and said so out loud.** `G-CL8` and `G-CL9` are the two rows where a mis-conditioning would fail *silently* — degrading to a log line rather than a block — so leaving either unstated would have been the highest-cost omission in the set. `G-CL8`'s tempting exemption (*the artifact is the doc*) is **rejected on the merits** and recorded as rejected: its scope key is a derived body property chosen precisely because form/kind literals are not evaluable for every work-item kind, and a deliverable-class exemption re-introduces that defect. `G-CL9` additionally received a second-order check — a close class that waived the Stage-9 review would make its ratifying referent vanish and the gate pass vacuously; verified that the branch changes only the close-evidence anchor and never the review path.
- **`G-CL5` is a no-op because the existing empty-set path already covers it**, verified against the phase text rather than assumed. A task-artifact release declares an empty operational manifest and passes on the path that already exists. Conditioning it would have duplicated a working trivial-pass and created two routes to one verdict.
- **The RELEASE_LOG status lattice is deliberately NOT extended.** An `ACCEPTED` state was considered and rejected on three independent grounds: it would force a RELEASE_LOG.md convention edit, collide with the Stage-13 concurrent-merge per-row `max` rule over `{DEPLOYED < VERIFIED}`, and *still* require a row — which is the thing the class exists to avoid.
- **Split the G-CL3 self-repair row into its two branches** (`G-CL3 (deployable class)` / `G-CL3 (task-artifact class)`), mirroring the two-branch `G1-04` and `G1-05b` precedent, so a reader can tell which branch a finding came from and the task-artifact case cannot inherit the deployable remediation. The task-artifact row states the anti-repair explicitly: **do not satisfy this criterion by writing a RELEASE_LOG row**, because a row added to silence it also corrupts the close-completeness checks that iterate rows. `G-CL4`'s row gains the corresponding AAR clause.
- **Columns and IDs byte-unchanged; no enumeration claimed.** No criterion ID is added, renumbered, removed, or re-typed, and no applies-to cell, table header, or column count changes — so the Gate 1→2 `structural_pass_rate` denominator at [gate-evaluation-spec.md](gate-evaluation-spec.md), the Gate-13 criterion count, and the Check 22 evaluated set are each untouched. This block deliberately makes no claim about the criterion-ID space, so it cannot go stale as other criteria are added. Same discipline as the v2.10, v2.9, v2.7 G1-05b and the G3-05 / G1-04 criterion-body refinements: **extend the criterion body, do not add an ID.**
- **Layer-B coverage is stated, not assumed — and it is deliberately absent.** No `deploy.sh` check is added or extended by this bump, and none of the checks behind the Gate-13 rows is touched. The close-completeness checks that iterate RELEASE_LOG rows are **structurally blind** to a release that writes no row: they produce no false FAIL against a task-artifact close (favourable), and equally no enforcement of it. Enforcement rests on the close-time completion-verification table plus `G-CL4`/AAR. **This is a recorded gap, not a claim of coverage** — proportionate under the governance domain guide's research-grade-controls contraindication, but real.
- **Version-derivation note (the instruction below has now fired a SEVENTH time, and this is the first time it fired across a branch boundary rather than within one).** This value was **re-derived against the mainline at Commit 0** — the mainline held **2.10** — not pre-allocated and not inherited. The v2.10 block's own instruction (*"the next editor of this block **on this branch** appends its change bullets"*) is **branch-scoped by its own wording**, and that branch has merged; the first editor on a *new* branch therefore re-derives from the mainline and takes a number, which is what the same block's re-derive-at-commit-time rule requires. The two halves are consistent, not contradictory: **within** a branch, append so the enumeration is never split; **across** branches, re-derive so no side's value is taken. On a merge conflict at this line the resolution remains: re-derive from the mainline and restate the enumeration to include every landed addition — never take either side's value. **The next editor of this block on this branch appends its change bullets to the v2.11 block above** rather than authoring a fresh block or taking a number.
- **Cutover discipline (v2.11 refinements):** the close-class conditioning applies to releases entering Stage 13 Close strictly AFTER this change's introducing-release merge SHA recorded in the release log; the introducing release itself is exempt (reflexive-pipeline-loop discipline — it cannot fire its own new gate) **yet dogfoods G-CL4 branch (b)**: it declares task-artifact deliverables and records their acceptance additively while its own close takes the unchanged deployable path. It does **not** dogfood the G-CL3 substitution, and the record says so plainly rather than implying a demonstration that did not happen — the introducing release resolves `domain: governance`, so the crux branch ships correct and fail-safe but unexercised. The first genuine end-to-end `task-artifact`-class close happens in a later release.

**v2.10 changes (non-breaking — minor; criterion-scope refinement — § Gate 1 Enforcement-Layer Split, plus a § Gate 9 G-PR1 criterion-body de-numeration; NO criterion ID added, renumbered, removed, or re-typed; NO column, header, or applies-to cell changed):**

- **Split Layer-B into Layer-B(d) standing detector and Layer-B(g) release gate**, and recorded the enforce-population contract `structural ∧ F1 ∧ ¬sub-task ∧ in-deploying-milestone` with a per-conjunct rationale. Check 22 previously held one authority over the whole open `status: bundled` population regardless of form family or milestone, which made it FAIL-capable against families the criteria were not written for and against defects belonging to other releases. The split changes **scope**, not posture: the check still ships `warn`, and nothing in this bump flips it. Decision recorded in [`ADR-120`](../ADRs/ADR-120-g1-enforcement-authority-is-class-scoped-and-release-scoped.md).
- **Added the per-family authority map, keyed by form family and never by criterion ID.** The map assigns **authority**; the pre-existing applies-to column triple assigns **applicability**; a criterion is enforced only where both hold, and an `n/a` cell is never enforced under any population choice. Keying the map by family rather than by ID is deliberate and load-bearing: a criterion-ID-keyed map would be a second enumeration of the criterion-ID space that drifts from the tables above, and it would also be swept by row-shape probes that read those tables. The criterion tables' row count and column projection are **byte-unchanged** by this bump.
- **Added the four-state release-identity resolution contract** (`RESOLVED` / `NONE` / `UNRESOLVED` / `INVALID`) with its per-state disposition, and stated its durability **precisely**: fail-closed on an asserted-but-unresolvable or invalid identity, **not-applicable** when no release is in flight, and explicitly **not** fail-closed in the general sense. The not-applicable branch is safe because of the emitter it routes to — a helper with no mode branch and no issue-counter increment — not because of a default value some later edit could flip. A summary of this contract as "fail-closed" is a durability over-claim and the block says so, because a schema outlives the release that writes it.
- **Recorded the population rationale as measurements with a read date**, decomposed per cause, and **recorded two residuals rather than closing them**: validation proves a resolved milestone exists and is open, never that it is the correct one; and branch-name detection is not total over naming history, with detached HEAD and post-merge mainline both resolving to `NONE`. Both are bounded by disclosure — the resolved slug, its source, and the milestone-set denominator are logged before any finding.
- **Named the two successors** (a runner for § Gate 3's body-compliance precondition; the release ledger as the eventual identity resolver) so the deferral is a recorded decision rather than a silent one.
- **No figure is restated from a peer schema.** An earlier draft of this bump would have carried a structural-criterion count quoted from the gate-evaluation schema's own parenthetical. That parenthetical disagrees with this file's `Check` column, so restating it would have converted an existing inconsistency into a freshly-authored governed statement. The count is therefore **not** restated here; the divergence is routed for reconciliation at its own surface. Where a count is needed, derive it from the `Check` column in the criteria table above — that column is the authority.
- **Version-derivation note (the instruction below has now fired FOUR times).** This value was **re-derived against the mainline at commit time**, not pre-allocated: the mainline held **2.8**, a sibling slice on this same release branch landed **2.9** at the preceding commit, so this bump re-derives to **2.10** and carries that slice's block forward verbatim below. The next editor of this block on this branch **appends its change bullets to the v2.10 block above** rather than authoring a fresh block or taking a number — the enumeration is restated, never split, and no side's block is discarded.
- **Cutover discipline (v2.10 refinements):** the enforce-population contract applies to gate evaluations run strictly AFTER this change's introducing-release merge SHA recorded in the release log; the introducing release itself is exempt (reflexive-pipeline-loop discipline) **yet dogfoods it** — its own milestone is the measured population, and the single gating finding it produces is a real, in-scope defect on one of its own member issues.
- **Reconciled the Layer-B(d) description to the shipped implementation** (appended by the Stage-9 pre-merge fix pass on this same release branch — the instruction above fired a SIXTH time and was obeyed: appended to this block, no fresh block authored and no number taken). The v2.10 bullets above described Layer-B(d) as a **backlog-wide standing detector** and asserted nothing is silenced. The shipped check holds exactly **one** issue-list query and it carries `--milestone`, so **Layer-B(d)'s tier semantics shipped and its population did not**: both surfaces read the deploying milestone and differ only in tier. A structural G1 defect in another milestone is not demoted to recommend-tier — it is never evaluated and emits at no tier. Corrected at four sites: the Layer-B(d) bullet, the Layer-B lead-in, the per-family authority map (which gains an explicit *any other milestone → not evaluated* row), and the four-state table (whose `Layer-B(d)` column said "unaffected" in all four rows and is "does not run" in three of them). The measurement table is re-framed as the **pre-change** rationale it always was, rather than as a description of what is emitted today. **The scope decision itself is untouched and stands** — this is a correction to the record, not to the behaviour, and no criterion ID, column, type, or applies-to cell moved. One residual is recorded rather than fixed: the check's own `NONE`-branch runtime message still names the backlog-wide detector, and that string sits in a file under concurrent edit by three sibling cards in this release; where the message and this block disagree, **this block is correct**.
- **De-numerated the dimension count out of the § Gate 9 G-PR1 criterion body** (appended by a later slice on this same release branch — the instruction above fired a FIFTH time and was obeyed: these bullets were appended to this block, no fresh block was authored and no number was taken). G-PR1 previously restated the readiness scan's dimension cardinality inline, so every change to the scan's dimension set dragged this schema into the cascade. The criterion now cites the readiness-scan spec as the owner of that set and states no count of its own. The criterion's structural intent is unchanged — Phase A1–A6 populated is still the artifact-presence check — and no ID, column, type, or applies-to cell moved. This is the same register-or-remove move the Gate 1 work applied one file over: a criterion that restates a peer's cardinality **is** a second copy of that cardinality, and a second copy drifts.

**v2.9 changes (non-breaking — minor; criterion-scope refinement — § Gate 1 Template Detection Logic + § Gate 1 Kind-Form Applicability + Adapter G1-06-Bug + G-CL8; NO criterion ID added, renumbered, removed, or re-typed):**

- **Added Step 0 (form family) to § Gate 1 Template Detection Logic.** The block previously began at "read the intake-tier label", which is only the right first question for an issue authored on one of the three governance-intake templates. Every other submission — anything carrying a work-item-kind label instead — fell through Step 1's zero-match arm and received `G1-09 FAIL: 0 or >1 intake-tier labels`, whose remediation instructs the author to apply an intake-tier label to a card that is correctly labelled. Step 0 partitions the population into four exhaustive, mutually exclusive families (F0 multi-tier / F1 governance-intake / F2 kind-form / F3 unresolved-form), so **every** issue reaches a determinate verdict traceable to a stated scope rule, and the two dispositions that were previously one are now separately stated: F0 keeps the existing emit and remediation verbatim, F3 gets its own finding naming the labels the issue carries.
- **The kind vocabulary is resolved, never enumerated.** Step 0's F2 predicate resolves the licensed kind set from the pack SSOT at read time. No kind id appears in this specification or in any gate that consumes it, so a deployment that selects a different methodology pack — or adds a kind to one — changes which cards are F2 with no edit to this file. Enumerating them would bake one methodology's archetype into a methodology-neutral gate, the shape ADR-033 rules against.
- **Added § Gate 1 Kind-Form Applicability (F2)** as a referenced predicate block, and scoped the applies-to column triple explicitly to F1. F2 applicability is **derived per issue from the body's declared field sections**, not declared per family: field presence varies *within* the kind-form family — the story kind form declares a required Acceptance Criteria field whose description restates G1-05a's own patterns, while the epic kind form declares none — so a family-wide "these fields are absent" row would be a silent blanket exemption on the story cell. The derivation is total, per-kind by construction, self-revoking, and reproducible with one `grep`.
- **Split the G1-09 self-repair row into its three cases** (F1 label-body mismatch / F0 multiple intake-tier labels / F3 unresolved form), mirroring the two-branch `G1-04` and `G1-05b` precedent, so a reader can tell which case a finding came from and the F3 case cannot inherit F0's remediation.
- **Adapter G1-06-Bug now cites the shared priority detector instead of restating a pattern.** The cell carried an illustrative regex that was unanchored; run literally against the live corpus it resolved a priority from mid-prose in bodies where the reference implementation correctly resolves none, so the specification and the implementation it points at disagreed on exactly the specificity property ADR-111 §1 names as part of its decision. The criterion now names `PRIORITY_FIELD_RE` / `parse_priority_body()` as the reference implementation and states the durable rule: a criterion that prints a grammar *is* a private grammar, because it is what an implementer copies.
- **Gave G-CL8 a stated issue-scope boundary, keyed to the issue body.** G-CL8 applies iff the issue body carries a `### Documentation Impact` section; an issue whose body carries none was authored on a form declaring no such field and is out of scope with recorded rationale. Keyed to the **body** rather than to the authoring form, because the form key has no evaluator for a work-item kind with no dedicated form (those emit on the interim `improvement.yml` vehicle) and wrongly exempts them even though their bodies carry a fully-authored declaration. Present-but-empty stays in scope and stays the gate-failing state. The G-CL8 row is marked the **authority** for this scope; the Stage-1 and Stage-13 restatements in the release-process governance file are derivative.
- **Columns and IDs byte-unchanged; no enumeration claimed.** No criterion ID is added, renumbered, removed, or re-typed, and no applies-to cell, table header, or column count changes — so the Gate 1→2 `structural_pass_rate` denominator at [`gate-evaluation-spec.md`](gate-evaluation-spec.md), the Gate-1 criterion count, and the Check 22 evaluated set are each untouched. This block deliberately makes no claim about the criterion-ID space, so it cannot go stale as other criteria are added. Same discipline as the v2.7 G1-05b and the G1-04 criterion-body refinements: extend the criterion body, do not add an ID.
- **Layer-B coverage is stated, not assumed.** `deploy.sh` Check 22 implements Step 0 and the F2 applicability derivation as of this change, and its hermetic regression net (`core/deploy/tests/test_g1_form_family.sh`) drives one fixture per live pack kind plus F0/F1/F3 fixtures and a degraded-vocabulary arm. The **judgment tier does not reach F2** — that is a stated scope boundary in the applicability block, not a pending item.
- **Cutover discipline (v2.9 refinements):** the form-family scope rule applies to gate evaluations run strictly AFTER this change's introducing-release merge SHA recorded in the release log; the introducing release itself is exempt (reflexive-pipeline-loop discipline) **yet dogfoods it** — its own bundled population is the measured before/after, with the governance-intake family's verdicts demonstrated byte-identical against a control that proves the harness executed.

**v2.8 changes (non-breaking — minor; additive — G-EX9; no criterion ID renumber; existing IDs stable):**

- Added **G-EX9** (ADR-number reconciliation tooled and complete) to Gate 12 (Execute Readiness) — 1 criterion row + 1 self-repair row, section-local to § Gate 12. Registers a **conditional** criterion asserting that when a release renumbered an ADR at the new Stage-12 Phase A.5.7, the move was performed by `release/tools/renumber-adr.py` and is complete on all six steps — including the `## Status` **Numbering provenance** note, the one step the hand-performed recovery reliably skipped. A release that authored no ADR, or whose number bound unchanged, passes trivially. Row is `validation` / `structural` / `auto`; the stage-gate evaluator routes by the `Check` column and an `auto` structural row uses the existing structural checker, so **no evaluator change and no field-lifecycle-matrix entry change**. Decision recorded in [`ADR-115`](../../release/ADRs/ADR-115-adr-number-claim-binds-at-merge.md).
- Schema bump v2.7 → v2.8 (non-breaking minor; additive — G-EX9). Existing G1-01..G1-09 / G2-01..G2-13 / G3-01..G3-19 + G4-01..G4-05 + G6-01..G6-06 + **G-BR1..G-BR5** + G-PR1..G-PR10 + G-EX1..G-EX9 + G-CL1..G-CL9 IDs unchanged (G-EX9 is the v2.8 addition; the prior max in the G-EX range was G-EX8. G-BR5 is the v2.6 addition this branch merged rather than superseded, and the sibling's **v2.7** — the G1-05b criterion-body refinement below — added, renumbered, removed and re-typed **no** criterion ID, so the enumeration carries forward unchanged across it); no ID renumber, no column/type change.
- **Version-derivation note (this bump is an instance of the defect G-EX9 governs), and the instruction below has now fired three times.** `**Schema version:** N` is a globally-sequenced identifier bound at authoring against a moving population, structurally identical to an ADR number with a one-line blast radius. This value was **re-derived against the mainline at Commit 0** (mainline held 2.5), not pre-allocated. Concurrent branches were observed holding their own 2.6 claims at the same instant; per the same binding rule those claims are **detection, not binding**. On a merge conflict at this line the resolution is to **re-derive from the mainline and restate the ID enumeration to include every landed addition** — never to take either side's value. **At the mainline merges that is exactly what happened, twice:** a sibling landed G-BR5 as v2.6 first, so this bump re-derived to **2.7** and restated its enumeration to carry G-BR5 forward; a second sibling then landed the G1-05b refinement as v2.7 before this branch merged, so it re-derived again to **2.8**. Neither side's value was taken at either merge and no side's block was discarded — the v2.7 and v2.6 entries below are the siblings', preserved verbatim.
- **Cutover discipline (v2.8 additions):** G-EX9 applies to releases entering Stage 12 strictly AFTER its introducing-release merge SHA recorded in the release log; the introducing release itself is exempt (reflexive-pipeline-loop discipline — it cannot fire its own new gate). The **tool** is not gated by this cutover: a tool is not a gate, and the introducing release uses it at Engineering Commit 0 and, if needed, pre-merge.

**v2.7 changes (non-breaking — minor; criterion-body refinement — G1-05b + 1 referenced predicate block + 1 self-repair branch; NO criterion ID added, renumbered, removed, or re-typed):**

- **Refined the G1-05b criterion body into two named branches** — **(i) per-bullet quality** (the pre-existing placeholder-leakage / commented-out-bullet / observable-state checks, unchanged) and **(ii) set-level usability coverage**: when a work item is *capability-class*, its AC set carries at least one **usability AC** — an observable end-to-end behavior the capability performs AFTER install/deploy, a verb exercised against a named realistic fixture — in addition to its presence ACs, or names one of the closed exempt classes E1–E5. Closes the class defect where a capability passes every acceptance criterion and all of CI while shipping **dormant**: an AC set asserting only presence is structurally incapable of catching a missing activation or wiring path, so the capability verifies as present and does nothing on a fresh install.
- Added **§ Capability-Class Usability-AC Requirement** to § Gate 1 as a referenced predicate block (the § Composite-OR Oversize Predicate / § Acceptance-Fit Predicate placement precedent), carrying the three-limb capability-class predicate (C1 actor ∧ C2 runtime trigger ∧ C3 non-artifact observable), the closed five-member exempt list E1–E5 keyed to the limb each fails, four anti-patterns, the required usability-AC shape, and its four mechanical conformance tests U1–U4. The block is the enforcement-side statement and owns the **verdict language** (the set-level rejection rule, the Layer-B coverage note, the two-branch self-repair row). Its **doctrine content is deliberately carried on both surfaces, not cited** — the predicate rows, exempt-class rows, anti-patterns, AC shape, and U1–U4 tests are verbatim-identical to `intake-style-guide.md` §4b so each file reads standalone; only the guide's **worked examples and authoring guidance** are cited-not-restated.
- **E1 is scoped to the *human* reader, deliberately.** On an agent platform the reader of a governance or shared-substrate file is frequently an agent, so an exempt class keyed on "what a reader reads" would swallow agent-loaded instruction content by construction — the widest possible escape hatch, sited exactly where the platform's content lives. E1 therefore says *human* reader and states that an agent-loaded instruction file is tested against C1 ∧ C2 ∧ C3 rather than claimed as E1. Recorded as inline rationale so a later simplification does not restore the bare word "reader".
- **The fourth anti-pattern guards E3 against mirroring the defect.** E3 exempts on the *absence of a runtime trigger* — the same limb (C2) whose *ex ante* satisfaction made the originating failure capability-class — so an unguarded E3 would hand that failure an exemption and swallow the governance-layer instance of the very class this change closes. The anti-pattern states that E3 is an exemption from the usability AC and never a finding that the rule will be adopted: a rule expected to change behavior should acquire a trigger rather than claim E3.
- Split the **G1-05b self-repair** into `G1-05b (per-bullet quality)` (unchanged remediation) and `G1-05b (usability-AC coverage)` (new set-level remediation), mirroring the two-branch `G1-04 (under-specified)` / `G1-04 (over-specified)` precedent, so a reader can tell which branch a finding came from.
- **Repaired 9 markdown table separator rows** that under-declared their column count, so the last column silently failed to render. The three Shape-2 gate tables declared 8 cells against a 9-column header; six Shape-1 tables declared 5 against a 6-column header. Root cause: the v2.0 `Validation method` column addition widened every header row and every data row but not the separator rows — so the column that change exists to add was invisible in rendered output across the whole file. Content-neutral: no cell text changes.
- **Columns byte-unchanged, and no ID enumeration is claimed.** G1-05b keeps `field` / `judgment` / `recommend` and its `req` | `adapt:G1-05-Bug` | `n/a` applies-to triple; its G1 Enforcement-Layer Split row is unchanged. **No criterion ID is added, renumbered, removed, or re-typed by this change**, so the Gate 1→2 `structural_pass_rate` denominator at `gate-evaluation-spec.md`, the Gate-1 criterion count, and the Check 22 evaluated set are each untouched — and this block deliberately makes no claim about the criterion-ID space, so it cannot go stale as other criteria are added. Same discipline as the G3-05 and G1-04 criterion-body refinements: extend the criterion body, do not add an ID.
- **Layer-B coverage is stated, not assumed.** `deploy.sh` Check 22 recommend-flags G1-05b today through a lightweight content proxy implementing branch (i) only; that proxy is **not** extended by this change. Branch (ii) is graded by the Layer-2 judgment path and re-run at Stage 4 Phase A0.6 (G-PL2), which re-evaluates the G1-05 family against each bundled body. The graduation lever is named: branch (ii) becomes mechanizable as a structural check once its fire predicate has a recorded body declaration; U1–U4 are already stated in mechanical form for that purpose.
- **Cutover discipline:** branch (ii) applies to acceptance criteria authored for issues entering Stage 1 Intake / Stage 2 Triage strictly AFTER this change's introducing-release merge SHA recorded in the release log; pre-cutover AC sets are grandfathered and are not re-graded; the introducing release itself is exempt (reflexive-pipeline-loop discipline) **yet dogfoods it** — the two standing deploy checks that release ships are capability-class under C1 ∧ C2 ∧ C3, and the release grades them by a conforming usability AC (both checks demonstrated emitting at runtime against committed fixture sets, not merely present).

**v2.6 changes (non-breaking — minor; additive — G-BR5; no criterion ID renumber; existing IDs stable):**

- Added **G-BR5** (Composition-lock eligibility of an addition-carrying `amend` target) to Gate G-BR (Bundle Refresh Readiness) — 1 criterion row + 1 self-repair row, section-local to § Gate G-BR. Registers the enforcement half of the **composition lock at Stage-4 Planning entry**: once a Milestone enters Stage 4 Planning its composition is closed to additions, and newly-Approved theme-matching work routes to a next bundle rather than into the running one. The criterion is **act-typed, not path-typed** — it fires on `path == amend AND issues_added > 0`, never on the `amend` path itself, so removals (triggers T3/T6), deferrals, and zero-delta re-sequences keep their existing disposition and ceremony. It **cites** the rule, the three-valued `lock_state` resolution, and the non-eligibility default defined in `release-process.md` § A7 § Composition lock rather than restating any of them: one surface states, the others cite (ADR-019). Row is `validation` / `structural` / `auto`.
- **Why `UNMARKED` is a state and not a pass.** The boundary resolves three-valued — `LOCKED` (emitted `### Composition Lock` marker) → `LOCKED [LEGACY-TITLE-INFERRED]` (attachment ∨ exact-title union, used monotonically) → `UNMARKED`. A zero probe result is an **absence**, not an affirmative eligibility finding, and no consumer may coerce it to "pre-Stage-4"; on the `UNMARKED` arm the criterion passes only with an explicit eligibility basis recorded in the `[BUNDLE AMENDMENT]` comment. Soundness therefore rests on the state machine's default rather than on probe coverage. Recorded here as inline rationale so a future release does not "simplify" the resolution back to a two-valued probe.
- Schema bump v2.5 → v2.6 (non-breaking minor; additive — G-BR5). Schema consumers (automated gate-validation tooling, stage-gate evaluator, CER Claim agents) require no structural change — the stage-gate evaluator routes by the `Check` column and an `auto` structural row uses the existing structural checker; no evaluator, no `deploy.sh` check, and no field-lifecycle-matrix entry changes. Existing G1-01..G1-09 / G2-01..G2-13 / G3-01..G3-19 + G4-01..G4-05 + G6-01..G6-06 + **G-BR1..G-BR5** + G-PR1..G-PR10 + G-EX1..G-EX8 + G-CL1..G-CL9 IDs unchanged (G-BR5 is the v2.6 addition; the prior max in the G-BR range was G-BR4); no ID renumber, no column/type change.
- **Cutover discipline (v2.6 additions):** G-BR5 applies to refresh-trigger events occurring strictly AFTER its introducing-release merge SHA recorded in the release log; pre-cutover evaluations are grandfathered; the introducing release itself is exempt (reflexive-pipeline-loop discipline — it cannot fire its own new gate).

**v2.5 changes (non-breaking — minor; additive — G2-13; no criterion ID renumber; existing IDs stable):**

- Added **G2-13** (Acceptance-fit determination recorded — elevated-rigor subset) to Gate 2 (Workflow Readiness) — 1 criterion row + 1 reference sub-block (§ Acceptance-Fit Predicate) + 1 Gate-2 adapter block (Adapter G2-13-Bug) + 1 self-repair row, section-local to § Gate 2. Registers a **scoped, conditional-binding** acceptance-quality criterion: when an issue's provenance is agent-authored (`<!-- provenance: agent-authored -->`) OR the A4 architecture evaluative-lens flagged it net-new/sweeping, the **Approve** verdict requires a recorded `#### Acceptance-Fit` anchor determination; absent a credible anchor the expected disposition is **Reject or Defer**. The two-limb fire predicate scopes it to the subset where architectural fit is genuinely in question (routine operator-authored tickets PASS trivially — no re-litigation), and it **consumes** the A4 lens's advisory classification rather than making A4 binding. Row is `validation` / `structural` / `auto`; **ships ENFORCE from v3.94** (gate-blocking — a missing `#### Acceptance-Fit` block blocks the Approve verdict when a limb fires). The criterion was originally authored to ship shadow/recommend and graduate to enforce; the flip to **day-one enforce** was **operator-ratified at the v3.94 Stage 9 Plan Review GO gate (2026-07-25)** per ADR-093 (the Automation posture is the operator-ratification lever; reversibility MODERATE). The provenance marker is an acceptance-scrutiny signal only — never a trust/authorization input (defense-in-depth; trust is decided solely by the author-association boundary, from the API, never the marker).
- Schema bump v2.4 → v2.5 (non-breaking minor; additive — G2-13). Schema consumers (automated gate-validation tooling, stage-gate evaluator, CER Claim agents) require no structural change — the stage-gate evaluator routes by the `Check` column and an `auto` structural row uses the existing structural checker; no evaluator, no field-lifecycle-matrix entry changes. Existing G1-01..G1-09 / G2-01..G2-13 / G3-01..G3-19 + G4-01..G4-05 + G6-01..G6-06 + G-BR1..G-BR4 + G-PR1..G-PR10 + G-EX1..G-EX8 + G-CL1..G-CL9 IDs unchanged (G2-13 is the v2.5 addition; the prior max in the G2 range was G2-12); no ID renumber, no column/type change.
- **Cutover discipline (v2.5 additions):** G2-13 applies to issues entering Stage 2 Triage strictly AFTER its introducing-release (v3.94) merge SHA recorded in the release log; pre-cutover issues are grandfathered; the introducing release itself is exempt (reflexive-pipeline-loop discipline — it cannot fire its own new gate). **Day-one enforcement posture is ENFORCE from v3.94** (operator-ratified at the v3.94 Stage 9 GO gate, 2026-07-25, per ADR-093 — flipping the originally-authored shadow/recommend graduation to day-one gate-blocking); the enforce posture binds only post-cutover issues, so v3.94's own issues remain exempt.

**v2.4 changes (non-breaking — minor; additive — G6-06; no criterion ID renumber; existing IDs stable):**

- Added **G6-06** (Skill packages content-fresh — PR-CI) to Gate 6 (Engineering Completeness) — 1 criterion row + 1 self-repair row, section-local to § Gate 6. Registers the PR-time `.skill` package content-freshness gate: a PR editing a rostered skill's `SKILL.md` or `references/` must rebuild its compiled `.skill` package in the same PR, mirroring `deploy.sh` Check 7's content-hash verdict pre-merge (via the new `skill-package-freshness` CI gate, single-engine with Check 7). Closes the class defect where skill-source edits merged green while the compiled package stayed stale — caught only at Stage-13 close-out, not by CI. Row is `artifact` / `structural` / `auto (warn → enforce)`; ships warn-mode-initial.
- Schema bump v2.3 → v2.4 (non-breaking minor; additive — G6-06). Schema consumers (automated gate-validation tooling, stage-gate evaluator, CER Claim agents) require no structural change — the stage-gate evaluator routes by the `Check` column and an `auto` structural row uses the existing structural checker; no evaluator, no field-lifecycle-matrix entry changes. Existing G1-01..G1-09 / G2-01..G2-12 / G3-01..G3-19 + G4-01..G4-05 + G6-01..G6-05 + G-BR1..G-BR4 + G-PR1..G-PR10 + G-EX1..G-EX8 + G-CL1..G-CL9 IDs unchanged (G6-06 is the v2.4 addition; the prior max in the G6 range was G6-05); no ID renumber, no column/type change.
- **Cutover discipline (v2.4 additions):** G6-06 applies to skill-editing releases entering Stage 6 strictly AFTER its introducing-release merge SHA recorded in the release log; the introducing release itself is exempt (reflexive-pipeline-loop discipline) yet dogfoods it green (its eval-writer reference edit + package rebuild is the live regression signal).

**v2.3 changes (non-breaking — minor; additive — G3-19; no criterion ID renumber; existing IDs stable):**

- Added **G3-19** (Release-identity-mode validity) to Gate 3 (Release Readiness). 1 new `field` / `judgment` / `recommend` criterion row + 1 self-repair row, section-local to § Gate 3. Asserts that a release declares a valid **release-identity mode** from the closed enum `{versioned, version-less}` (set at Bundle per the Release-Identity Mode declaration in `stage-03-bundle.md`) and that the declaration is consistent with what the release carries — a `versioned` release declares a bump-class *intent*; a `version-less` release carries no version string and is identified by its capability slug. Closes the enforcement gap: the identity-mode enum shipped in the Stage-3 shard but no gate criterion referenced it, and Gate 3 carried no version criterion of any kind.
- **Inline rationale — why this is an identity-mode assertion and not a version-freeness check (the non-obvious boundary call).** A reader could reasonably expect a Gate-3 "version assigned (no collision)" criterion to be a structural freeness check. It deliberately is not. Under the two-phase allocation rule (`RELEASE_PROTOCOL.md` § Versioning) the bump-class *intent* is declared at plan time and the concrete number is computed next-free and claimed by compare-and-swap only at the merge moment, so at Bundle there is no number to check; a Gate-3 freeness verdict would be both architecturally unavailable and stale by the merge. Freeness stays owned downstream by the Stage-12 atomic claim. A structural-check alternative (grepping the milestone description for a mandatory `identity-mode:` field, in the shape of the G3-10 Release Class check) was evaluated and rejected: it would impose a new mandatory milestone-description field and a new auto-check surface this criterion was not scoped to introduce, for no gain over the declared-axis assertion. Recorded here as inline rationale so a future release does not "harden" G3-19 into a structural auto-check that would contradict the two-phase model.
- Coordinated as a non-colliding gate-ID assignment: **G3-19** is the next free ID — G3-13 is a RETIRED tombstone (ADR-012, not reused), G3-16 remains reserved for the cross-milestone gate, G3-14 / G3-15 / G3-17 / G3-18 shipped. No existing ID is renumbered.
- Schema bump v2.2 → v2.3 (non-breaking minor; additive — G3-19). Schema consumers (automated gate-validation tooling, stage-gate evaluator, CER Claim agents) require no structural change: the stage-gate evaluator routes by the `Check` column and a `judgment` row uses the existing judgment scorer, so no evaluator, no `deploy.sh` check, and no field-lifecycle-matrix entry changes. Existing G1-01..G1-09 / G2-01..G2-12 / G3-01..G3-18 + G4-01..G4-05 + G6-01..G6-05 + G-BR1..G-BR4 + G-PR1..G-PR10 + G-EX1..G-EX8 + G-CL1..G-CL9 IDs unchanged (G3-19 is the v2.3 addition; the prior max in the G3 range was G3-18); no ID renumber, no column/type change.
- **Cutover discipline (v2.3 additions):** G3-19 applies to bundles/milestones entering Stage 3 strictly AFTER its introducing-release merge SHA recorded in the release log; pre-cutover evaluations grandfathered; the introducing release itself is exempt (reflexive-pipeline-loop discipline — it cannot fire its own new gate).
**v2.2 changes (non-breaking — minor; additive — G-CL9; no criterion ID renumber; existing IDs stable):**

- Added **G-CL9** (Promised ADR `Proposed → Accepted` flips landed — Stage 13 Close) to the Gate 13 registry — 1 criteria row + 1 self-repair row. Closes a class defect: an ADR that records a conditional ratification ("flips to Accepted at `<review>`") had no gate confirming the transition actually landed once that review closed, so a record could sit `Proposed` on the mainline indefinitely, silently failing any downstream gate that asks whether it is Accepted. Row is `artifact` / `structural` / `auto (warn → enforce)`.
- **G-CL9 is release-scoped by design, not by omission.** The ratifying reference inside an ADR is free text whose wording varies per record, so resolving it to a milestone and testing that milestone's closed-state is not reliably mechanizable. The operator closing a release knows which review just closed; the criterion is therefore anchored at that judgment rather than at a global scan.
- **Paired with a deliberately non-authoritative backstop.** `deploy.sh` Check 58 (`adr-flip-verify`) surfaces Proposed-with-promise records and their age as a standing drift signal, and is structurally incapable of enforcement — it cannot see whether a review has closed, so a still-pending (and therefore *correct*) record reports on every run. G-CL9 is the authority; Check 58 never is. This mirrors the existing G-CL8 + Check-28 pairing, including its lightweight-deploy-time / full-at-Stage-13 split.
- Schema bump v2.1 → v2.2 (non-breaking minor; additive — G-CL9). Schema consumers (automated gate-validation tooling, stage-gate evaluator, CER Claim agents) require no structural change. Existing G1-01..G1-09 / G2-01..G2-12 / G3-01..G3-18 + G4-01..G4-05 + G6-01..G6-05 + G-BR1..G-BR4 + G-PR1..G-PR10 + G-EX1..G-EX8 + G-CL1..G-CL8 IDs unchanged (G-CL9 is the v2.2 addition; the prior max in the G-CL range was G-CL8); no ID renumber, no column/type change.
- **Cutover discipline (v2.2 additions):** G-CL9 applies to releases entering Stage 13 Close strictly AFTER its introducing-release merge SHA recorded in the release log; the introducing release itself is exempt (reflexive-pipeline-loop discipline — it cannot fire its own new gate, and its own ADRs were authored before the gate existed).

**v2.1 changes (non-breaking — minor; additive — G-PR10; no criterion ID renumber; existing IDs stable):**

- Added **G-PR10** (Release-integration cross-issue AC check — QC3.5 / Stage 9 Phase A3.6) to the Gate 9 registry — 1 criteria row + 1 self-repair row. G-PR10 grades the release's Cross-Issue Acceptance Criteria (CIAC-N — release-scoped predicates over any ≥2 issues, no dependency edge) by reading the verdicts the verification-execution executor emitted against the merged PR (read-only, single-runner; evidence-freshness guard mirrors G-PR9 baseline-currency). Row is `validation` / `judgment` / `recommend`; verdicts RELEASE-CONSISTENT / RELEASE-INCONSISTENT / N/A. Composes with — does not duplicate — the Phase A3.5 `INT-N` chain validation (dependency pairs vs. any-≥2-issue release-scoped predicates). The new max ID in the G-PR range is now G-PR10 (prior max G-PR9).
- Schema bump v2.0 → v2.1 (non-breaking minor; additive — G-PR10). Schema consumers (automated gate-validation tooling, stage-gate evaluator, CER Claim agents) require no structural change. Existing G1-01..G1-09 / G2-01..G2-12 / G3-01..G3-18 + G4-01..G4-05 + G6-01..G6-05 + G-BR1..G-BR4 + G-PR1..G-PR9 + G-EX1..G-EX8 + G-CL1..G-CL8 IDs unchanged (G-PR10 is the v2.1 addition; the prior max in the G-PR range was G-PR9); no ID renumber, no column/type change.
- **Cutover discipline (v2.1 additions):** G-PR10 applies to releases entering Stage 9 strictly AFTER its introducing-release merge SHA (**v3.65**, ADR-073) recorded in the release log; the introducing release itself is exempt (reflexive-pipeline-loop discipline — it cannot fire its own new gate; its Stage 9 runs under the pre-existing G-PR1..G-PR9 checks).

**v2.0 changes (BREAKING — major; column addition per the versioning rule):**

- Added a **Validation method** column as the last field of every gate-criteria table (Shape-1 Gates 4/6/9/12/13 + G-BR: 5→6 col; Shape-2 Gates 1/2/3: 8→9 col) + a 6th § Schema field row defining it. Per this spec's own versioning rule — "Breaking changes (**column additions**, ID renumbering, type enum changes) increment the major version" — a column addition is a MAJOR bump.
- Lifted the inline validation CLI out of the Criterion cell of the deterministic criteria that embedded it (G3-10 / G3-11 / G3-17; G4-01..G4-05; G6-01..G6-05; G-PR5 / G-PR7 / G-PR9; G-EX1 / G-EX2 / G-EX3 / G-EX4 / G-EX7; G-CL1 / G-CL2 / G-CL8) into the new Validation method column, **verbatim** (byte-identical command strings). Criterion cells now state the pass-predicate in plain language; the reproducible command lives in Validation method. G3-05, G-CL3, and G3-18 (prose / soft references / config-field conditioning, not executable commands) carry `—`; other `judgment` rows carry `—`.
- **No criterion ID renumbered; no Type/Check/Automation column changed; no criterion removed.** ID count is identical pre/post this change.
- **Consumer impact — NONE despite the major bump.** The stage-gate evaluator routes by the `Check` column ([gate-evaluation-spec.md](gate-evaluation-spec.md) Consumer Contract) and never parses the Criterion cell for CLI; `deploy.sh` Check 22 reimplements the G1 checks natively in bash. The new column is inert to every parsing consumer. The MAJOR increment records the structural-schema change per the spec's rule; it does not imply a consumer-contract break.
- **Cutover discipline:** the Validation method column applies to all gate evaluations going forward; existing consumers require no change.

**v1.23 changes (non-breaking — minor; criterion-body refinement — G1-04; no criterion ID renumber; G1-01..G3-18 + G4/G6 + G-BR/G-PR/G-EX/G-CL stable):**

- Made the **G1-04 criterion body bidirectional** per #96: the criterion text now names BOTH the under-specification failure (no file/protocol named — WHAT missing) AND the over-specification failure (mechanism/HOW prematurely committed), so a Layer-2 evaluator scoring the criterion **text alone** detects both directions — matching the two-branch G1-04 self-repair (`G1-04 (under-specified)` / `G1-04 (over-specified)`) that already shipped. The one-directional prior wording left the over-spec self-repair unreachable unless the evaluator separately loaded `intake-style-guide.md` §5; the bidirectional criterion closes that coherence gap. The G1-04 row's **Type/Check/Automation columns are unchanged** (`field` / `judgment` / `recommend`); the self-repair menu is untouched.
- Schema bump v1.22 -> v1.23 (non-breaking minor; criterion-body refinement). Schema consumers (automated gate-validation tooling, stage-gate evaluator, CER Claim agents) require no structural change — G1-04 keeps its ID, type, check, and automation columns; only its criterion predicate gains the over-spec direction. Existing G1-01..G1-09 / G2-01..G2-12 / G3-01..G3-18 + G4-01..G4-05 + G6-01..G6-05 + G-BR1..G-BR4 + G-PR1..G-PR9 + G-EX1..G-EX8 + G-CL1..G-CL8 IDs unchanged; no ID renumber, no column/type change.
- **Cutover discipline (v1.23 additions):** the G1-04 bidirectional scoring applies to issues entering Stage 1 Intake / Stage 2 Triage strictly AFTER this change's introducing-release merge SHA recorded in the release log; pre-cutover evaluations are grandfathered; the introducing release itself is exempt (reflexive-pipeline-loop discipline).

**v1.22 changes (non-breaking — minor; additive — Gate 4 + Gate 6; no criterion ID renumber; existing IDs stable):**

- Added **Gate 4: Plan Readiness** (G4-01..G4-05) — the Planning->Solutioning/Engineering (4->5/6) exit gate. 5 `artifact`/`structural`/`auto` criteria (Implementation Sequence / File Change Matrix / Risk Register / Verification Plan / Delivery Strategy present) + 5 self-repair rows + 1 Inheritance-Rules row (inherits Gate 3->4). Closes the long-standing hole: Stage 4 was one of only two stages with no discrete gate ID (gate-evaluation-spec.md already defined the 4->5/6 metrics boundary but sourced no gate-criteria-spec IDs).
- Added **Gate 6: Engineering Completeness** (G6-01..G6-05) — the Engineering->DevTesting (6->7) exit gate. 5 criteria (PR exists / commits link source issues / parser-clean PR body / verification-evidence present / Change Description section present) + 5 self-repair rows + 1 Inheritance-Rules row (inherits Gate 6->7). Closes the second no-ID stage.
- Both gates ship **warn-mode** — structural FAILs from either gate log to the shared deploy-check warn log (`$(pmo_instance_path)/deploy-check-warn-log.jsonl`) and the boundary PROCEEDS with an operator-visible warning — **specified, not yet emitting**: no check emits this gate's FAIL row. Flip-to-enforce deferred to a 2-3-release shakedown per the G-CL6/G-CL7/G-CL8/G3-14/G3-15 shakedown-then-enforce precedent ([`bypass-mode-readiness.md`](../rules/bypass-mode-readiness.md)).
- Cross-refs: `stage-04-planning.md` §7 cites G4; `stage-06-engineering.md` §7 cites G6.
- G4/G6 use the **numeric-gate ID format** (G4-01 / G6-01), consistent with the G1-01..G3-18 numeric-gate convention for contiguous-numbered stages; the stage-abbrev format (G-PR/G-EX/G-CL) stays reserved for the compressed late stages (9/12/13). G4-* and G6-* are fresh namespaces (no prior G4-/G6- IDs existed).
- Schema bump v1.21 -> v1.22 (non-breaking minor; additive — Gate 4 + Gate 6). Schema consumers (automated gate-validation tooling, stage-gate evaluator, CER Claim agents) require no structural change. Existing G1-01..G1-09 / G2-01..G2-12 / G3-01..G3-18 + G-BR1..G-BR4 + G-PR1..G-PR9 + G-EX1..G-EX8 + G-CL1..G-CL8 IDs unchanged; no ID renumber, no column/type change.
- **Cutover discipline (v1.22 additions):** G4 applies to releases entering Stage 4 and G6 to releases entering Stage 6 strictly AFTER this change's introducing-release merge SHA recorded in the release log; the introducing release itself is exempt (reflexive-pipeline-loop discipline — it cannot fire its own new gate; its own Stage 4/6 ran before these gates existed).

**v1.21 changes (non-breaking — minor; additive — G3-18; no criterion ID renumber; G1-01..G3-17 stable):**

- Added **G3-18** (Archetype-appropriate design invocation conditioned on `delivery_approach`) to Gate 3 (Release Readiness). 1 new `field` / `judgment` / `recommend` criterion row + 1 self-repair row, section-local to § Gate 3. Conditions the design-invocation expectation at the Bundle→Planning (Stage 3→4) boundary on the declared `delivery_approach` per the §5 Skill Consumption Pattern in `methodology-parameterization-v1.md`, following the live G3-05 / §5A `deliverable_type` conditioning precedent: CASE 1 (Scrum/XP/Lean → design-first before slicing; Waterfall/PRINCE2 → phase-gate; Kanban → continuous, no sprint-slice gate), CASE 1-ARRAY (Hybrid → phased constituent governs), CASE 3 / absent / `Custom` → **base behavior UNCHANGED via an explicit no-op default** (dormant-with-`[ASSUMPTION – CONFIRM]` per ADR-033; never a silent default-fire). The conditioning **adds** a methodology lens to *when* design is expected; it never removes the base Stage 5 Phase 0 Activation Gate (the unconditional floor). Records the ADR-066 design decision. **Type-column reconciled to `field` (CD-2):** the criterion conditions on a declared axis value (`default_delivery_approach` / project `delivery_approach`) — a `field` per the § Schema enum — not a cross-issue/cross-system check (`validation`); adopts G3-05's `field` / `judgment` / `recommend` columns unchanged, the precedent this criterion mirrors. Paired Stage-8 regression gate `G3-18-absent-axis-noop` asserts the absent-axis path == pre-change behavior (trivial-PASS), the `Kanban` path does not fire, and the `Scrum` path fires.
- Coordinated as a non-colliding gate-ID assignment: **G3-18** is the next free ID — G3-13 is a RETIRED tombstone (ADR-012, not reused), G3-16 is reserved for the cross-milestone gate, G3-17 shipped. No existing ID is renumbered.
- Schema bump v1.20 → v1.21 (non-breaking minor; additive — G3-18). Schema consumers (automated gate-validation tooling, stage-gate evaluator, CER Claim agents) require no structural change. Existing G1-01..G1-09 / G2-01..G2-12 / G3-01..G3-18 + G-BR1..G-BR4 + G-PR1..G-PR9 + G-EX1..G-EX8 + G-CL1..G-CL8 IDs unchanged (G3-18 is the v1.21 addition; the prior max in the G3 range was G3-17); no ID renumber, no column/type change.
- **Cutover discipline (v1.21 additions):** G3-18 applies to bundles/milestones entering Stage 3 strictly AFTER its introducing-release merge SHA recorded in the release log; pre-cutover evaluations grandfathered; the introducing release itself exempt (reflexive-pipeline-loop discipline — it cannot fire its own new gate).

**v1.20 changes (non-breaking — minor; additive — G-PR8 registration; no criterion ID renumber; G-PR1..G-PR9 stable):**

- Added **G-PR8** (Stage 9 Phase A6.5 mid-pipeline-divergence) to the Gate 9 registry — 1 criteria row + 1 self-repair row. Closes the registry-vs-reality gap the G-PR9 addition flagged. The File dimension of Phase A6.5 (per [pipeline/stage-09-plan-review.md](../../release/references/pipeline/stage-09-plan-review.md) §5 Phase A6.5) is present across the corpus but had no registered row here — the G-PR9-introducing change explicitly left G-PR8 registration as a separate task; this change fills the G-PR1..G-PR9 hole (the max ID is already G-PR9 — no new max is added). The row is `validation` / `structural` / `auto`; verdicts CLEAN / DIVERGED-RELEASE-FILES-UNTOUCHED / DIVERGED-RELEASE-FILES-TOUCHED, HALT-eligible on the File dimension (the Phase A6.5 version dimension stays advisory, not registered as a HALT gate). Composes with — does not duplicate — G-PR9 (a second axis/window) and the Phase A6.5 advisory version dimension.
- Schema bump v1.19 → v1.20 (non-breaking minor; additive — G-PR8). Schema consumers (automated gate-validation tooling, stage-gate evaluator, CER Claim agents) require no structural change. Existing G1-01..G1-09 / G2-01..G2-12 / G3-01..G3-17 + G-BR1..G-BR4 + G-PR1..G-PR9 + G-EX1..G-EX8 + G-CL1..G-CL8 IDs unchanged (G-PR8 fills the pre-existing hole in the G-PR1..G-PR9 range — not a new max ID); no ID renumber, no column/type change.
- **Cutover discipline (v1.20 additions):** G-PR8 applies to releases entering Stage 9 strictly AFTER its introducing-release merge SHA recorded in the release log; the introducing release itself is exempt (reflexive-pipeline-loop discipline — it cannot fire its own new gate).

**v1.19 changes (non-breaking — minor; criterion-body refinement — G3-05; no criterion ID renumber; G1-01..G3-17 stable):**

- Conditioned the **G3-05 criterion body** on `deliverable_type` (project-schema.md §4) per #351: where the project declares `deliverable_type`, the AC verification method must conform to the project's deliverable domain per the matching domain guide (`core/standards/domain-best-practices/<deliverable_type>.md`), evaluated via the §5A Domain-Axis Consumption Pattern in `methodology-parameterization-v1.md`. When `deliverable_type` is absent (legacy) or has no shipped guide (CASE D-2), G3-05 evaluates the base measurability check unchanged — the conditioning **adds** a domain-conformance lens, it never removes the base verifiability requirement. The G3-05 criterion row's **Type/Check/Automation columns are unchanged** (`field` / `judgment` / `recommend`), exactly the pattern the v1.13 two-branch G3-05 self-repair set ("The G3-05 criterion row itself is unchanged"); the self-repair menu (file-state branch / behavioral-domain branch) is untouched.
- Schema bump v1.18 → v1.19 (non-breaking minor; criterion-body refinement). Schema consumers (automated gate-validation tooling, stage-gate evaluator, CER Claim agents) require no structural change — G3-05 keeps its ID, type, check, and automation columns; only its criterion predicate gains a domain-conformance clause. Existing G1-01..G1-09 / G2-01..G2-12 / G3-01..G3-17 + G-BR1..G-BR4 + G-PR1..G-PR9 + G-EX1..G-EX8 + G-CL1..G-CL8 IDs unchanged; no ID renumber, no column/type change.
- **Cutover discipline (v1.19 additions):** the G3-05 `deliverable_type` conditioning applies to issues/bundles entering Stage 1 / Stage 3 strictly AFTER this change's introducing-release merge SHA recorded in the release log; pre-cutover evaluations are grandfathered; the introducing release itself is exempt (reflexive-pipeline-loop discipline).

**v1.18 changes (non-breaking — minor; additive — G3-17; no criterion ID renumber; G1-01..G3-16 stable):**

- Added **G3-17** (A6 New-Track Placement Rationale present for non-identical-track-extension milestones) to Gate 3. 1 new `artifact` / `structural` / `auto` criterion + 1 self-repair row, section-local to § Gate 3. Asserts that any milestone created at Stage 3 Phase B3 carries exactly one of {a `## New-Track Placement Rationale` 4-table section (when A6 fired under condition (a) no-prior-rationale OR (b) distinct-capability-scope per `release-process.md` § Stage 3 Bundle § A6) | the one-line `A6: identical-track-extension …` acknowledgment (when A6 SKIPped because the milestone is a genuine identical-track-extension)}; absence of both FAILS (silence is never a pass). Pairs with the A6 firing-trigger move from once-per-version-major-track (version-prefix-absence sole boolean) to the per-milestone (a) OR (b) predicate; the inaugural-new-track case is retained as the strongest sub-case of (b), so no regression on inaugural-track behavior. Mirrors the live G3-10 milestone-description-completeness pattern at the same Phase-B3 altitude.
- Coordinated as a non-colliding gate-ID assignment: **G3-17** is the next free ID — G3-13 is a RETIRED tombstone (ADR-012, not reused), G3-14 / G3-15 shipped, G3-16 reserved for the cross-milestone gate. No existing ID is renumbered.
- Schema bump v1.17 → v1.18 (non-breaking minor; additive — G3-17). Schema consumers (automated gate-validation tooling, stage-gate evaluator, CER Claim agents) require no structural change. Existing G1-01..G1-09 / G2-01..G2-12 / G3-01..G3-16 + G-BR1..G-BR4 + G-PR1..G-PR9 + G-EX1..G-EX8 + G-CL1..G-CL8 IDs unchanged (G1-01..G3-16 stable; G3-17 is the v1.18 addition); no ID renumber, no column/type change.
- **Cutover discipline (v1.18 additions):** G3-17 applies to milestones entering Stage 3 strictly AFTER its introducing-release merge SHA recorded in the release log; pre-cutover milestones grandfathered; the introducing release itself exempt (reflexive-pipeline-loop discipline — it cannot fire its own new gate).

**v1.17 changes (non-breaking — minor; additive — Priority-Lifecycle sub-block; no criterion ID change; G1-01..G3-16 stable):**

- Added the **Priority-Lifecycle sub-block** under § Gate 1 (adjacent to the existing Priority-Model block; referenced from G1-06 / G2-01): codifies the temporal lifecycle of the single P-level field — **agent-estimated at intake → agent-re-evaluated at triage with full-backlog context → human-confirmed at bundle** ("suggested, not fixed"). States the decomposition rule (P1–P4 stays the single overall field; the blocking/dependency-position signal is lifted out into the separate Stage 2 A6 `Dependency-position signal`, not folded into P) and the altitude boundary (pipeline-intake priority ⊥ delivery-engine WSJF/CoD; reference-only, no value-scoring framework hardwired into the public corpus). Composes with the directional-not-authoritative handoff principle rather than duplicating it.
- Reconciled the **G2-01** criterion wording: "validated against full backlog context" → "re-evaluated (validated-or-adjusted) against full backlog context per the Priority-Lifecycle sub-block." Codifies re-evaluation (not mere validation) at triage. Criterion type/check/automation columns unchanged (`field` / `judgment` / `recommend`); not a new criterion, no ID change.
- Schema bump v1.16 → v1.17 (non-breaking minor; additive — Priority-Lifecycle sub-block; no criterion ID change). Schema consumers (automated gate-validation tooling, stage-gate evaluator, CER Claim agents) require no structural change. Existing G1-01..G1-09 / G2-01..G2-12 / G3-01..G3-16 + G-BR1..G-BR4 + G-PR1..G-PR9 + G-EX1..G-EX8 + G-CL1..G-CL8 IDs unchanged (G1-01..G3-16 stable); no ID renumber, no column/type change.
- **Cutover discipline (v1.17 additions):** the Priority-Lifecycle sub-block and the G2-01 verb reconcile apply to all issues entering the pipeline going forward.

**v1.16 changes (non-breaking — minor; criterion-shape change, no ID renumber):**

- **Repurposed G1-01 from a prefix-requirement to a title-informativeness floor.** The criterion was inverted in place: was "Title follows `[Category]: Description` format" (prefix REQUIRED); is now "Title is an informative summary — no leading `[...]:` type/category prefix (type is on the label) + a substance floor (≥ 2 words; not a bare slug; ≥ N chars)". The change holds every column value (`field` / `structural` / `auto`) and the ID, so the Gate 1→2 `structural_pass_rate` denominator at `gate-evaluation-spec.md` (`G1-01, G1-03, G1-05a, G1-06, G1-07` structural per gate-evaluation-spec.md) is **unchanged** — same discipline as the v1.13 G1-05a 4th-pattern extension (extend the criterion shape, do not renumber). The applies-to triple collapses to `req | req | req` (no per-template prefix literal remains).
- **Honest-scope note (per review-discipline R1):** G1-01 is a **syntactic** floor only — it catches a leftover bracket prefix and a bare-slug/too-short title, and it cannot judge whether a well-formed multi-word title is actually informative (a vague-but-well-formed title passes the floor). The **semantic informativeness bar** lives in [intake-style-guide.md §7](../../release/references/how-to/intake-style-guide.md) (rubric) + intake-desk elicitation (judgment), NOT in this gate. No LLM grading is added.
- **Retired Adapters G1-01-Bug / G1-01-Obs** — with no per-template prefix literal to translate, the `[Bug]:` / `[Observation]:` adapter translations are removed; a retirement note replaces them under § Gate 1 Adapter Blocks.
- **Reconciled** the Enforcement-Layer-Split row (G1-01 description), the G1-01 self-repair rows (strip-not-prepend on bracket FAIL; return-to-author on substance FAIL), the bundle-precondition (a title's *presence*, not a prefix, no longer implies body fields; re-check set names "title-informativeness floor"), and the Template-Conversion Rule (title rewritten to the floor; no `[Category]:` added).
- Pairs with the `deploy.sh` Check 22 (`g1-enforcement`) G1-01 block rewrite shipped alongside this schema change (rejects a `[...]:` prefix + the substance floor; **warn-tier unchanged** — legacy prefixed issues WARN, never FAIL `--check`; bulk re-title is a separately-authorized follow-up) and the three work-item templates' prefix-less `title: ""`.
- Schema bump v1.15 → v1.16 (non-breaking minor; criterion-shape change). Schema consumers (automated gate-validation tooling, stage-gate evaluator, CER Claim agents) require no structural change — G1-01 keeps its ID, type, check, and automation columns; only its predicate and applicability triple change. Existing G1-01..G1-09 / G2-01..G2-12 / G3-01..G3-15 + G-BR1..G-BR4 + G-PR1..G-PR9 + G-EX1..G-EX8 + G-CL1..G-CL8 IDs unchanged; no ID renumber, no column/type change.
- **Warn-tier (G1-01):** legacy issues created before this change keep their bracket prefix and are reported as a non-blocking warning by `deploy.sh --check` — they never fail the gate, and no bulk re-title is required.

**v1.15 changes (non-breaking — minor; additive only):**

- Added the **G1 Enforcement-Layer Split** subsection under § Gate 1 (after the G1 criteria table, before Template Detection Logic): names the two enforcement surfaces the `auto` Automation tag conflates — **Layer-A Form HARD-STOP** (intake template `validations: required: true`, presence-only, submission-blocking) vs. **Layer-B Gate SOFT-WARN** (`deploy.sh --check` Check 22, content-validating, structural FAIL / judgment RECOMMEND) — plus the 9-row enforcement-class table mapping each G1 criterion to its Layer-A form-required posture, Layer-B gate behavior, and HARD-STOP/SOFT-WARN class. The table is read off the existing `structural | judgment` **Check**-column partition; it introduces no new criterion and no ID. Records as durable policy that **G1-06 priority is gate-checked, NOT form-required** (`required: false` is correct because priority is body-canonical per G1-06 — validated at G2-01, mirrored to Projects at G2-12 — and "label-based priority is NOT expected"), cross-linking the Priority-Model block (added v1.14) and the G2-12 sync criterion.
- **No criterion row changed; no `validations: required:` change implied.** The subsection documents the existing form/gate split as policy — it does not alter G1-06 or any other criterion, and the intake templates' required-field sets are already aligned with the Layer-A column (the live `improvement.yml` 7-required set stands; `### Priority` stays `required: false`).
- Composes with the `deploy.sh` Check 22 extension shipped alongside this schema change: Check 22 now evaluates all 9 G1 criteria — enforces the 5 structural (G1-01/03/05a/06/09, FAIL-capable at Layer-B) and recommend-flags the 4 judgment (G1-02/04/05b/08, advisory, never FAIL) — and Check 22's `g1-enforcement` mode is decoupled to a dedicated `g1-enforcement.mode` file (falling back to the shared `deploy-check.mode`), shipping in **warn**. The warn→enforce flip is deferred to a follow-on after the shakedown window.
- Schema bump v1.14 → v1.15 (non-breaking minor; additive only). Schema consumers (automated gate-validation tooling, stage-gate evaluator, CER Claim agents) require no change — the new subsection is documentation of the existing partition. Existing G1-01..G1-09 / G2-01..G2-12 / G3-01..G3-15 + G-BR1..G-BR4 + G-PR1..G-PR9 + G-EX1..G-EX8 + G-CL1..G-CL8 IDs unchanged; no ID renumber, no column/type change.
- **Cutover discipline (v1.15 additions):** the G1 Enforcement-Layer Split is policy documentation effective immediately; the Layer-B Check 22 evaluation of G1-05a + the judgment recommend-pass apply to issues evaluated by `deploy.sh --check` strictly AFTER this change's introducing-release merge SHA recorded in the release log; pre-cutover evaluations are grandfathered; the introducing release itself is exempt (reflexive-pipeline-loop discipline).

**v1.14 changes (non-breaking — minor; additive only):**

- Added **G2-12** (Projects Priority field set + consistent with the body `### Priority` P-level; queryable via Projects API, not parsed from comment text) to Gate 2. 1 new `anchor` / `structural` / `auto` criterion + 1 self-repair row + Adapter G2-12-Bug (Severity P-level mapping). Modeled on **G2-06** (Decision Date in the Projects Date field), the in-corpus field-first precedent: the body field is canonical, the Projects field is its queryable mirror, written at Triage. **Body wins on mismatch** — self-repair re-derives the Projects field from the confirmed body P-level, never the reverse. Closes the not-yet-gate-backed `Priority (Projects)` mirror cell in `field-lifecycle-matrix.md`.
- Added the **Priority-Model block** under § Gate 1 (referenced from G1-06 / G2-01 / G2-12): a 3-row table fixing the two coexisting priority surfaces (body `### Priority` = canonical; GitHub Projects Priority field = queryable mirror) + the explicit non-surface (`priority:` label — none exists; G1-06's "label-based priority is NOT expected" stands; a label-free `status: approved` issue is conformance, not a defect) + the body-wins canonical-source rule + a scope note that priority is **not** validated at G2-04.
- Reconciled the **G2-01** criterion wording to name the body `### Priority` P-level explicitly as the validated surface (per G1-06) — disambiguates body vs. label vs. the G2-12 Projects mirror. Criterion type/check/automation columns unchanged (`field` / `judgment` / `recommend`); not a new criterion.
- **Corrected the stale "G2-04 = priority validation" framing** (no in-file text asserted it, but the Priority-Model block now states the correct mapping unconditionally so no future reader re-derives the false gap): G2-04 is *dependency-reference validity*; priority is present at G1-06 and validated at G2-01. **G2-04's own criterion row is unchanged.**
- **G1-06 and `deploy.sh` Check 22 are unchanged** — priority stays body-canonical at intake; this release only adds the G2 Projects-mirror sync gate, it does not alter the G1 body-priority anchor.
- Schema bump v1.13 → v1.14 (non-breaking minor; additive only). Schema consumers (automated gate-validation tooling, stage-gate evaluator, CER Claim agents) treat G2-12 + the Priority-Model block as additive. Existing G1-01..G1-09 / G2-01..G2-11 / G3-01..G3-15 + G-BR1..G-BR4 + G-PR1..G-PR9 + G-EX1..G-EX8 + G-CL1..G-CL8 IDs unchanged (G2-12 is the v1.14 addition); no ID renumber, no column/type change.
- **Cutover discipline (v1.14 additions):** G2-12 applies to issues entering Stage 2 Triage strictly AFTER this criterion's introducing-release merge SHA recorded in the release log; pre-cutover issues are grandfathered; the introducing release itself is exempt (reflexive-pipeline-loop discipline — it cannot fire its own new gate).

**v1.13 changes (non-breaking — minor; additive only):**

- Extended **G1-05a** with a **4th admissible pattern** — a **behavioral/domain predicate**: a `predicate:`-style outcome declaration **paired with** a `method:` declaration of how the outcome is verified, admitted only when both tokens are co-present (a bare behavioral assertion without a `method:` declaration still fails). Extended the G1-05a self-repair return-to-author message with a matching rewrite-target (d). The row's type/check/automation columns are **unchanged** (`field` / `structural` / `auto`), so the Gate 1→2 `structural_pass_rate` denominator at `gate-evaluation-spec.md` (`G1-01, G1-03, G1-05a, G1-06, G1-07 = 5 structural`, threshold = 1.0) is **unchanged** — pattern (d) is a fourth admissible shape within the existing single criterion, not a new criterion ID.
- Extended the **G3-05 self-repair** remediation from a single mandated remedy ("rewrite AC as assertions verifiable against file content or system state") to a **two-branch menu** — (a) file-state branch (verbatim the prior remedy) OR (b) behavioral/domain branch (declare a predicate + its verification method; use when a file/state rewrite would lose the AC's meaning). A file-content/system-state rewrite is **no longer the only sanctioned remediation**. The G3-05 criterion row itself is **unchanged**.
- Recorded the **"declared, verification deferred" honesty note** (short form) inline at the G1-05a and G3-05 self-repair cells: a behavioral/domain AC may be admitted with its verification method declared even before an executor for that method exists. The full honesty note is canonically stated in `release/references/pipeline/stage-04-planning.md` § 6 (Verification-Plan AC→method mapping), cited here rather than restated.
- Schema bump v1.12 → v1.13 (non-breaking minor; additive only). Schema consumers (automated gate-validation tooling, stage-gate evaluator, CER Claim agents) treat the extended G1-05a pattern set and the two-branch G3-05 self-repair as additive. Existing G1-01..G3-15 + G-BR1..G-BR4 + G-PR1..G-PR9 + G-EX1..G-EX8 + G-CL1..G-CL8 IDs unchanged; no ID renumber, no column/type change.
- **Cutover discipline (v1.13 additions):** the extended G1-05a pattern (d) and the two-branch G3-05 self-repair apply to all issues/bundles entering Stage 1 / Stage 3 going forward.

**v1.12 changes (non-breaking — minor; additive only):**

- Added **G3-14** (Mode A combined-clean parse-rate floor) to Gate 3. 1 new `validation` / `metrics` / `recommend` criterion + 1 self-repair row. Asserts the `release-planner` Mode A combined-clean parse rate ≥ `[bundling].mode_a_parse_rate_floor` (default 0.90) against the **conformant-bundleable denominator** per `release-planner` SKILL.md Step 1.5 partition (excludes type-excluded + needs-body-repair bodies); the denominator is further restricted to bodies with a *determinate* `parse_status` (`"clean"` or `"failed"` per the parser's three-valued `{"clean","deferred","failed"}` enum — `bundle-issues-parser.py` `IssueRecord.parse_status`), so operator-`deferred` bodies are set aside and the rate stays reproducible. Co-gates the `needs-body-repair` count (denominator-coverage anti-gaming) so a hollowed-out denominator cannot quiet-pass. Evaluated at the Stage 3 → 4 boundary before A2/A8 dep-graph consumption; trivial-PASS on an empty determinate denominator; on breach routes to the 3-outcome repair-then-recompute / proceed-with-documented-override / escalate-to-parser-fix disposition (not a dead-end fail). Closes the enforcement gap on the planner's own parse-quality measurement (Step 1.5 defined the denominator; nothing gated on the rate).
- Added **G3-15** (per-milestone risk-weighted size bound) to Gate 3. 1 new `validation` / `metrics` / `recommend` criterion + 1 self-repair row. Asserts `effective_pts = round_half_up(Σ(member.pts) × class_weight) ≤ upper-bound([bundling].release_size_target_pts)` per milestone, reading the `release_class_capacity_weights` config and taking the round-half-up mode by reference from the `bundle-composition-doctrine.md § 3 Step 5 Risk-Weighting` definitional home; on breach routes to the bundle-composition-doctrine § 4 tight-merge / § 3 Step 5 split-merge-reframe disposition (not a dead-end fail). Degraded path (config absent): surfaces a loud WARN and requires operator confirmation rather than silently passing with `class_weight = 1.0`. Complements the per-issue G2-11 / G3-12 family at the per-milestone-sum altitude. Paired with the risk-weighted capacity model (target ↔ enforcement pair).
- G3-14 + G3-15 are the **first two `metrics`-Check rows in Gate 3** (existing Gate-3 rows are `structural` / `judgment`); the `metrics` Check value is already defined in § Schema and the consumer note already routes `metrics → compute`, so **no enum change is needed** — only the first Gate-3 instances of an already-defined value. Coordinated as a non-colliding gate-ID set: G3-14 (parse-rate floor) + G3-15 (per-milestone size bound) here; G3-16 reserved for the cross-milestone gate; G3-13 is a RETIRED tombstone, not reused.
- Added the `[bundling].mode_a_parse_rate_floor` config field (default `0.90`, `CALIBRATE-AFTER-3`) to `core/config/platform-config.toml.template` + its row in `core/schemas/platform-config-schema.md` — the parameterize-over-hardcode home G3-14 reads by role (parity with `release_size_target_pts`). Gated for default-presence in `deploy.sh` Check 33.
- Schema bump v1.11 → v1.12 (non-breaking minor; additive only). Schema consumers (automated gate-validation tooling, stage-gate evaluator, CER Claim agents) treat the new criteria as additive. Existing G1-01..G3-13 (G3-14 + G3-15 are the v1.12 additions) + G-BR1..G-BR4 + G-PR1..G-PR9 + G-EX1..G-EX8 + G-CL1..G-CL8 IDs unchanged.
- **Cutover discipline (v1.12 additions):** G3-14 and G3-15 apply to releases/milestones entering Stage 3 strictly AFTER their introducing-release merge SHA recorded in the release log; pre-cutover releases/milestones are grandfathered; the introducing release itself is exempt (reflexive-pipeline-loop discipline — it cannot fire its own new gate).

**v1.11 changes (non-breaking — minor; additive only):**

- Added G-PR9 (GO baseline-currency) to Gate 9. 1 new structural criterion + 1 self-repair row. The GO records the baseline SHA it was rendered against and the hub evaluates a sibling-merge revalidation predicate (`git log <baseline>..origin/main --name-status --find-renames` intersected against the release's structural-blast-radius surface `SURFACE(R)` per the Stage 3 Bundle spec § A9.6.1 axis); verdict CURRENT / STALE-REVALIDATE / STALE-VOID. Composes with — does not duplicate — the live Phase-A6.5 mid-pipeline-divergence check (a second axis/window; that check is referenced repo-wide as a Gate-9 criterion but is not yet a registered row in this table — its registration is a separate task, not absorbed here, so G-PR9 is added cleanly above that gap). The predicate is `--name-status --find-renames`, NOT `--merges`, because the repo lands sibling releases via both merge and squash / fast-forward non-merge commits.
- Schema bump v1.10 → v1.11 (non-breaking minor; additive only). Schema consumers (automated gate-validation tooling, stage-gate evaluator, CER Claim agents) treat the new criterion as additive. Existing G1-01..G3-13 + G-BR1..G-BR4 + G-PR1..G-PR7 + G-EX1..G-EX8 + G-CL1..G-CL8 IDs unchanged.
- **Cutover discipline (v1.11 additions):** G-PR9 applies to releases entering Stage 9 strictly AFTER its introducing-release merge SHA recorded in the release log; the introducing release itself is exempt (reflexive-pipeline-loop discipline — it cannot fire its own new gate).

**v1.10 changes (non-breaking — minor; clarification only):**

- Added the **Template-awareness precondition** framing note under § Gate 1 — states the detect-template-before-evaluating rule and its two-directional failure mode (false-positive failures + false-negative passes) that the existing per-template column triple, Template Detection Logic, and Adapter Blocks already implement. No new criterion; no ID added.
- Extended **Adapter G1-06-Bug** with the combined multi-line-aware detector `(Priority|Severity)[\s\n]+P[1-4]` covering both field names in one pattern. Clarifies the existing Severity→Priority semantic mapping; G1-06 behavior unchanged.
- Added the **Body-compliance precondition for the `status: bundled` transition** framing note under § Gate 3 — states the re-run-G1/G3-against-the-body rule, the "labels reflect intent, not body-level compliance" principle, the higher-risk applicability list, the minimum re-check set, and the out-of-scope cases. No new criterion; no ID added.
- Schema bump v1.9 → v1.10 (non-breaking minor; clarification only — no criteria added, no IDs renumbered, no type/check/automation enum changes). Schema consumers (automated gate-validation tooling, stage-gate evaluator, CER Claim agents) require no change.

**v1.9 changes (non-breaking — minor):**

- Retired G3-13 (roadmap-cascade validation) per ADR-012 — initiative-roadmap instances de-scoped to operator-local; the Roadmap-Cascade Validation procedure + detection script are removed, and the G3-13 criterion + self-repair rows are tombstoned in place so cross-references resolve. Non-breaking: the judgment-graded criterion no longer fires (passes trivially) and the G3-13 ID is preserved (no renumbering).
- Schema bump v1.8 → v1.9 (non-breaking minor; retirement only). Existing criterion IDs unchanged.

**v1.8 changes (non-breaking — minor):**

- Added G3-13 (roadmap-cascade validation; judgment-graded recommend). 1 new criterion + 1 new self-repair row + 1 new validation procedure block (Roadmap-Cascade Validation). Per-variant rubric per `altitude:` frontmatter — Program → §3 Now/Next/Later milestone match; Portfolio → §3 Family Member Registry cluster match (per the D-UmbrellaVariant decision). Non-breaking minor; additive only.
- Schema bump v1.7 → v1.8 (non-breaking minor; additive only). Schema consumers (automated gate-validation tooling, stage-gate evaluator, CER Claim agents) treat the new criteria as additive. Existing G1-01..G3-12 + G-BR1..G-BR4 + G-PR1..G-PR7 + G-EX1..G-EX8 + G-CL1..G-CL8 IDs unchanged.
- **Cutover discipline (v1.8 additions):** G3-13 applies to all bundles entering Stage 3 going forward.
- **Audit date:** 2026-06-02 (resolved at Stage 6 first commit via `date -u +%Y-%m-%d` per [`core/standards/date-variable-convention.md`](../standards/date-variable-convention.md)).

**v1.7 changes (non-breaking — minor):**
- Added G2-11 (decomposition-review routing at Triage when ANY oversize predicate matches — P1 `size:XL` OR P2 declared decomposition-hook OR P3 AC ≥ 7 OR file ≥ 5) to Gate 2. 1 new judgment-recommend criterion + 1 self-repair row.
- Added G3-12 (decomposition-review routing at Bundle as drift-backstop for G2-11; same composite-OR predicate) to Gate 3. 1 new judgment-recommend criterion + 1 self-repair row. Mirrors the G2-10 / G3-09 dual-placement precedent (Triage + Bundle drift-backstop).
- Added § Composite-OR Oversize Predicate block under § Gate 2 — referenced from G2-11 + G3-12 criterion bodies. Defines P1 / P2 / P3 with verifiable detection commands.
- Added § Cross-Stage Decomposition Coverage block under § Gate 3 — 3-altitude table (Intake / Triage-Bundle / Engineering) per AC #5; subsumption rule (G2-11 SUBSUMES G2-10 on predicate fire; G3-12 SUBSUMES G3-09 by the same mapping); Retroactive Review Path (R1 / R2 / R3) per AC #6 (operator-elective only).
- Added Cutover discipline for G2-11 / G3-12: applies to all issues entering Triage / Bundle going forward.
- Schema bump v1.6 → v1.7 (non-breaking minor; additive only). Schema consumers (automated gate-validation tooling, stage-gate evaluator, CER Claim agents) treat the new criteria as additive. Existing G1-01..G3-11 + G-BR1..G-BR4 + G-PR1..G-PR7 + G-EX1..G-EX8 + G-CL1..G-CL8 IDs unchanged. SPLIT outcome composes with [fission-convention.md](../../release/references/protocols/fission-convention.md).
- **Note on Stage 5 spec renumbering (Stage 5 sub-task):** Stage 5 spec proposed gate IDs `G2-11` + `G3-10`. `G3-10` was claimed by a sibling (Release Class, commit `adbc8c5`); `G3-11` was claimed by a sibling (Outcome Statement, commit `8dee29f`). `G3-12` is the next-free slot — used here. Tier 1 `[ADJUST]` deviation (informational); no Stage 5 spec-decision change required.

**v1.6 changes (non-breaking — minor):**
- Added G3-11 (Release Outcome Statement heading present in milestone description) to Gate 3. 1 new structural-advisory criterion + 1 self-repair row. Non-blocking advisory at Stage 3 Phase B3.
- Added G-PR7 (Release plan implementation conforms to declared Release Outcome Statement) to Gate 9. 1 new judgment-recommend criterion + 1 self-repair row.
- Added G-CL7 (Goal-attainment verification recorded-06) to Gate 13. 1 new structural criterion + 1 self-repair row. Initial warn-mode posture per [`bypass-mode-readiness.md`](../rules/bypass-mode-readiness.md) Shakedown → Enforce Transition Checklist precedent; flip-to-enforce at 2-3 release threshold. Composes with `release-outcome-statement-template.md` (canonical authority for the Outcome schema).
- Added G-CL8 (Documentation Impact resolved for every closed issue in the release) to Gate 13. 1 new structural criterion + 1 self-repair row. Backed by [`deploy.sh`](../deploy/deploy.sh) Check 28 (`doc-impact-resolution-presence`). Initial warn-mode posture per [`bypass-mode-readiness.md`](../rules/bypass-mode-readiness.md) Shakedown → Enforce Transition Checklist precedent; flip-to-enforce at 2-3 release threshold OR warn-log drained to < 10 entries (whichever first). Composes with the new `Documentation Impact` field on `improvement.yml` + `bug.yml` (Stage 1 declaration) and the `### Documentation Impact` H3 subsection on `.github/PULL_REQUEST_TEMPLATE.md` (Stage 6 surface). Scope boundary: K1 codified corpus only.
- Extended G-PR1 wording to reference the 13-dim Release Readiness Scan (Phase A6 of Stage 9 evidence assembly is now the Release Readiness Scan per [`release-readiness-scan-spec.md`](../../release/references/specs/release-readiness-scan-spec.md); the criterion's structural intent is unchanged — Phase A1-A6 populated is still the artifact-presence check). No ID renumber; G-PR1 stable. Composes with `release-readiness-scan-spec.md` (canonical spec for the scan's 13 dims + per-dim evidence schema + 4-value status enum + output template).
- Schema bump v1.5 → v1.6 (non-breaking minor; additive only). Schema consumers (automated gate-validation tooling, stage-gate evaluator, CER Claim agents) treat the new criteria as additive. Existing G1-01..G3-09 + G3-10 + G-BR1..G-BR4 + G-PR1..G-PR6 + G-EX1..G-EX8 + G-CL1..G-CL6 IDs unchanged.
- **Cutover discipline:** Applies to all releases going forward.

**v1.5 changes (non-breaking — minor):**
- Added `improvement | bug | observation` applies-to column triple to every G1 / G2 / G3 criterion table row. Cell values: `req` / `adapt:<adapter-id>` / `n/a` / `conv`.
- Added new structural criterion G1-09 (intake-tier label matches body template) to Gate 1 + G1-09 self-repair row.
- Added Template Detection Logic block (consumed by G1-09 + by all template-aware gates) under § Gate 1.
- Added Gate 1 Adapter Blocks (Adapter G1-01-Bug, G1-01-Obs, G1-02-Bug, G1-05-Bug, G1-06-Bug) under § Gate 1 — inline lexical/semantic translation per Option C (Hybrid) recommendation.
- Added Gate 2 Adapter Blocks (Adapter G2-01-Bug) under § Gate 2.
- Added Gate 3 Adapter Blocks (Adapter G3-05-Bug) under § Gate 3.
- Added Template-Conversion Rule sub-section under § Gate 3 — observation → improvement conversion at Stage 3 Phase A1 (Bundle entry); `BUNDLE-BLOCKED: observation-template requires conversion` failure signal + conversion path + out-of-conversion paths.
- Schema bump v1.4 → v1.5 (non-breaking minor; additive only). Schema consumers (automated gate-validation tooling, stage-gate evaluator, CER Claim agents) treat the column triple as opt-in filter and continue to iterate all rows when no template filter is applied. Existing G1-01..G3-09 + G-BR1..G-BR4 + G-PR1..G-PR6 + G-EX1..G-EX8 + G-CL1..G-CL6 IDs unchanged.
- Origin: 2026-05-11 audit surfaced 3 template-discipline defects: (1) `bug.yml` issues fail G1-06 false-positively because the body uses `Severity` instead of `Priority`; (2) `observation.yml` issues pass gates trivially despite lacking AC / Affected Files / Priority required for Stage 4 Planning; (3) `improvement`-labeled issues with observation-style bodies (or vice versa) bypass both gate paths (retroactively fixed).
- **Cutover discipline:** Applies to all releases going forward.

**v1.4 changes (non-breaking — minor):**
- Added G2-09 (similarity scoring at Triage) and G2-10 (size scoring at Triage) to Gate 2. 2 new judgment criteria + 2 self-repair rows.
- Added G3-08 (in-bundle similarity scoring) and G3-09 (size scoring at Bundle) to Gate 3. 2 new judgment criteria + 2 self-repair rows.
- Added Similarity Composite-Signal Detection block (cluster + edge axis composite signal) referenced from G2-09 + G3-08.
- Added G-CL6 (design-artifact refresh-gate) to Gate 13. 1 new structural criterion + 1 self-repair row. Initial warn-mode posture per [`bypass-mode-readiness.md`](../rules/bypass-mode-readiness.md) Shakedown → Enforce Transition Checklist precedent; flip-to-enforce at 2-3 release threshold. Composes with [`design-artifact-standard.md`](../standards/design-artifact-standard.md) § 8 (canonical authority for the refresh-gate mechanism).
- Added Cutover discipline: the new criteria apply to all issues entering Triage/Bundle/Stage 13 going forward.
- Non-breaking — extends existing Gate 2, Gate 3, and Gate 13 inheritance; existing G1-01..G3-07 + G-CL1..G-CL5 IDs unchanged. Schema consumers (automated gate-validation tooling, stage-gate evaluator) treat new criteria as additive.

**v1.3 changes (non-breaking — minor):**
- Added G3-07 (cross-milestone dependency sequence validation) to Gate 3. 1 new structural criterion + 1 self-repair row + Milestone-Position Resolution algorithm definition. Non-breaking — extends existing Gate 3.
- Added Gate G-BR (Bundle Refresh Readiness, G-BR1..G-BR4). 4 new criteria + 4 self-repair rows. New named gate using stage-abbrev format (consistent with v1.2 G-PR / G-EX / G-CL precedent). Composes alongside Gate 3 at refresh-trigger events.
- Schema consumers (automated gate-validation tooling, stage-gate evaluator) should treat new criteria and new gate as additive.

**v1.2 changes (non-breaking — minor):**
- Added Gate 9 Plan Review (G-PR1..G-PR6), Gate 12 Execute Readiness (G-EX1..G-EX8), Gate 13 Close Readiness (G-CL1..G-CL5). 19 new structural criteria + 19 self-repair rows. Non-breaking — extends existing inheritance pattern; existing G1-01..G3-06 IDs unchanged. Schema consumers (automated gate-validation tooling, stage-gate evaluator) should treat new gates as additive.
- ID format extended to support stage-abbrev variant (G-PR, G-EX, G-CL) alongside the numeric-gate variant (G1-01..G3-06). Discontinuity rationale: Stages 10-11 are compressed (per [release-process.md](../../release/governance/release-process.md) Stage Compression), so a continuous numeric range would imply ranges that don't exist.
- Inheritance Rules table extended with Plan Review, Execute Readiness, and Close Readiness rows.

**v1.1 changes (non-breaking — minor):**
- G1-02 refined from single-tier (Proposal-only) to tier-branched (Proposal + Observation) per . Backwards-compatible: `improvement`-labeled issues evaluate identically to v1.0. Forward-extends: `observation`-labeled issues now have a defined G1-02 branch (previously auto-failed under literal v1.0 reading).
- Self-Repair Actions G1-02 entry split into three branches (improvement / observation / label-missing) matching the criterion branches.

Schema consumers (automated gate-validation tooling, stage-gate evaluator) should reference the schema version to detect breaking changes. Breaking changes (column additions, ID renumbering, type enum changes) increment the major version. Non-breaking changes (new criteria within existing gates, self-repair refinements) increment the minor version.
